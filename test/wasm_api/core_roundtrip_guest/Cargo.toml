[package]
name = "recoil-wasm-core-roundtrip-guest"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
spring-wasm-core = { path = "../../../rust/crates/spring-wasm-core" }

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
