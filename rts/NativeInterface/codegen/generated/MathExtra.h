// AUTO-GENERATED by rts/NativeInterface/codegen/generate.py
// Source: rts/Lua/LuaMathExtra.cpp
// Do not edit — re-run the generator instead.
#pragma once

#include <stdint.h>
#include "CommonTypes.h"

#ifdef __cplusplus
extern "C" {
#endif

// ==========================================================================
// MathExtra — auto-generated NativeInterface API
// ==========================================================================

struct hypotQuery {
    float x;
    float y;
};
struct hypotResult {
    const Error* error;
    float hypot;
};

struct clampQuery {
    float value;
    float lbound;
    float ubound;
};
struct clampResult {
    const Error* error;
    float clamp;
};

struct sgnQuery {
    float x;
};
struct sgnResult {
    const Error* error;
    float sign;
};

struct mixQuery {
    float x;
    float y;
    float a;
};
struct mixResult {
    const Error* error;
    float mix;
};

struct roundQuery {
    float x;
    int32_t decimals;
};
struct roundResult {
    const Error* error;
    float floor;
};

struct erfQuery {
    float x;
};
struct erfResult {
    const Error* error;
    float erf;
};

struct smoothstepQuery {
    float edge0;
    float edge1;
    float v;
};
struct smoothstepResult {
    const Error* error;
    float smoothstep;
};

struct bit_invQuery {
    uint32_t value;
};
struct bit_invResult {
    const Error* error;
    float result;
};

struct MathExtraApi {
    void (*hypot)(const hypotQuery* query, hypotResult* result);
    void (*clamp)(const clampQuery* query, clampResult* result);
    void (*sgn)(const sgnQuery* query, sgnResult* result);
    void (*mix)(const mixQuery* query, mixResult* result);
    void (*round)(const roundQuery* query, roundResult* result);
    void (*erf)(const erfQuery* query, erfResult* result);
    void (*smoothstep)(const smoothstepQuery* query, smoothstepResult* result);
    void (*bit_inv)(const bit_invQuery* query, bit_invResult* result);
};

extern const MathExtraApi MATHEXTRA_API;

#ifdef __cplusplus
}
#endif
