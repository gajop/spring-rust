Recoil Wasm implementation ledger
=================================

This ledger records durable status only. Numeric benchmark output belongs in
benchmarking_results.md, generated by test/native_api_parity/run_benchmarks.py.

Status:
  Core implementation present.
  Bounded post-change verification passes; several original-plan coverage items remain.

Phase status:
  Phase 0  DONE                 generator baseline and fixtures
  Phase 1  DONE                 shared semantic API model
  Phase 2  DONE                 Wasmtime/component proof
  Phase 3  DONE                 WIT, callouts, and worlds
  Phase 4  DONE                 shared Rust module API
  Phase 5  DONE                 engine runtime and lifecycle
  Phase 6  DONE                 callbacks, RmlUi, Gfx, and shapes
  Phase 7  DONE                 sandbox and synchronization policy
  Phase 8  DONE                 LuaUI environment and bounded matrix
  Phase 9  VERIFY               parity, coverage, and benchmark completion
  Phase 10 VERIFY/EXTERNAL     Linux ASAN build; release, arm64, Windows gates

Open original-plan work:
  - complete the nominal frame-budget benchmark; the bounded matrix is
    generated;
  - finish broad API and mutation-heavy end-to-end coverage;
  - review Lua-loader coverage and add conversion fuzzing;
  - add the generated-adapter performance fixture;
  - complete broad post-change parity coverage;
  - commit the implementation.

Not open implementation work:
  WASI/OS imports, content-supplied AOT, opaque native archive pointers,
  native-module trust policy, LuaMenu, LuaIntro, LuaParser/definition parsing,
  and unsupported RmlUi surfaces are original-design deferrals.

Separate engine TODOs:
  FreeType and SDL/OpenAL teardown ownership and their narrow leak suppressions.

Evidence:
  - benchmarking_results.md
  - review-response.md
  - issues.md
