/* This file is part of the Spring engine (GPL v2 or later), see LICENSE.html */

// @generated by spring-api-codegen; do not edit.

#include "WasmHostAdapterSupport.h"

using namespace recoil::wasm::generated::detail;

namespace recoil::wasm::generated::detail {

}

namespace {

NativeCalloutDispatch Dispatch_team_control_AddTeamResource(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->AddTeamResource == nullptr) { error = "NativeInterface API is unavailable: team_control.AddTeamResource"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	AddTeamResourceQuery query{};
	AddTeamResourceResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "team-id", query.teamID, error)) return NativeCalloutDispatch::handled;
	const WasmValue* value_resourceType = FindArgument(arguments, 1u, 1u, 3u, 3u, "resource-type", error);
	if (value_resourceType == nullptr) return NativeCalloutDispatch::handled;
	auto& stored_string_query_resourceType = storage.Make<std::string>();
	if (!ReadString(*value_resourceType, stored_string_query_resourceType, error)) { return NativeCalloutDispatch::handled; }
	query.resourceType = stored_string_query_resourceType.c_str();
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "amount", query.amount, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->AddTeamResource(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_AddTeamResourceExcessStats(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->AddTeamResourceExcessStats == nullptr) { error = "NativeInterface API is unavailable: team_control.AddTeamResourceExcessStats"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	AddTeamResourceExcessStatsQuery query{};
	AddTeamResourceExcessStatsResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "team-id", query.teamID, error)) return NativeCalloutDispatch::handled;
	const WasmValue* value_resourceType = FindArgument(arguments, 1u, 1u, 3u, 3u, "resource-type", error);
	if (value_resourceType == nullptr) return NativeCalloutDispatch::handled;
	auto& stored_string_query_resourceType = storage.Make<std::string>();
	if (!ReadString(*value_resourceType, stored_string_query_resourceType, error)) { return NativeCalloutDispatch::handled; }
	query.resourceType = stored_string_query_resourceType.c_str();
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "amount", query.amount, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->AddTeamResourceExcessStats(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_AssignPlayerToTeam(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->AssignPlayerToTeam == nullptr) { error = "NativeInterface API is unavailable: team_control.AssignPlayerToTeam"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	AssignPlayerToTeamQuery query{};
	AssignPlayerToTeamResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 2u, 2u, "player-id", query.playerID, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 2u, 2u, "team-id", query.teamID, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->AssignPlayerToTeam(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_GameOver(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->GameOver == nullptr) { error = "NativeInterface API is unavailable: team_control.GameOver"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	GameOverQuery query{};
	GameOverResult nativeResult{};
	const WasmValue* value_winningAllyTeams = FindArgument(arguments, 0u, 0u, 1u, 1u, "winning-ally-teams", error);
	if (value_winningAllyTeams == nullptr) return NativeCalloutDispatch::handled;
	const auto* list_winningAllyTeams = std::get_if<WasmValueList>(&(*value_winningAllyTeams).storage);
	if (list_winningAllyTeams == nullptr) { error = "Wasm argument is not a list"; return NativeCalloutDispatch::handled; }
	auto& native_winningAllyTeams = storage.Make<std::vector<std::int32_t>>();
	native_winningAllyTeams.reserve(list_winningAllyTeams->size());
	for (const auto& item : *list_winningAllyTeams) {
		std::int32_t item_value{};
		if (!ReadScalar(item, item_value, error)) { return NativeCalloutDispatch::handled; }
		native_winningAllyTeams.push_back(std::move(item_value));
	}
	query.winningAllyTeams = native_winningAllyTeams.data();
	if (!AssignCount(list_winningAllyTeams->size(), query.count, error)) { return NativeCalloutDispatch::handled; }
	nativeInterface->syncedCtrl->team->GameOver(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_KillTeam(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->KillTeam == nullptr) { error = "NativeInterface API is unavailable: team_control.KillTeam"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	KillTeamQuery query{};
	KillTeamResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 1u, 1u, "team-id", query.teamID, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->KillTeam(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_SetAlly(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->SetAlly == nullptr) { error = "NativeInterface API is unavailable: team_control.SetAlly"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetAllyQuery query{};
	SetAllyResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "first-ally-team-id", query.firstAllyTeamID, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 3u, 3u, "second-ally-team-id", query.secondAllyTeamID, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "allied", query.allied, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->SetAlly(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_SetAllyTeamStartBox(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->SetAllyTeamStartBox == nullptr) { error = "NativeInterface API is unavailable: team_control.SetAllyTeamStartBox"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetAllyTeamStartBoxQuery query{};
	SetAllyTeamStartBoxResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 5u, 5u, "ally-team-id", query.allyTeamID, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 5u, 5u, "min-x", query.minX, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 5u, 5u, "min-z", query.minZ, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 5u, 5u, "max-x", query.maxX, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 5u, 5u, "max-z", query.maxZ, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->SetAllyTeamStartBox(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_SetGlobalLos(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->SetGlobalLos == nullptr) { error = "NativeInterface API is unavailable: team_control.SetGlobalLos"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetGlobalLosQuery query{};
	SetGlobalLosResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 2u, 2u, "ally-team-id", query.allyTeamID, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 2u, 2u, "enabled", query.enabled, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->SetGlobalLos(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_SetPlayerReadyState(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->SetPlayerReadyState == nullptr) { error = "NativeInterface API is unavailable: team_control.SetPlayerReadyState"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetPlayerReadyStateQuery query{};
	SetPlayerReadyStateResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 2u, 2u, "player-id", query.playerID, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 2u, 2u, "ready", query.ready, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->SetPlayerReadyState(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_SetTeamResource(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->SetTeamResource == nullptr) { error = "NativeInterface API is unavailable: team_control.SetTeamResource"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetTeamResourceQuery query{};
	SetTeamResourceResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "team-id", query.teamID, error)) return NativeCalloutDispatch::handled;
	const WasmValue* value_resourceType = FindArgument(arguments, 1u, 1u, 3u, 3u, "resource-type", error);
	if (value_resourceType == nullptr) return NativeCalloutDispatch::handled;
	auto& stored_string_query_resourceType = storage.Make<std::string>();
	if (!ReadString(*value_resourceType, stored_string_query_resourceType, error)) { return NativeCalloutDispatch::handled; }
	query.resourceType = stored_string_query_resourceType.c_str();
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "amount", query.amount, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->SetTeamResource(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_SetTeamShareLevel(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->SetTeamShareLevel == nullptr) { error = "NativeInterface API is unavailable: team_control.SetTeamShareLevel"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetTeamShareLevelQuery query{};
	SetTeamShareLevelResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "team-id", query.teamID, error)) return NativeCalloutDispatch::handled;
	const WasmValue* value_resourceType = FindArgument(arguments, 1u, 1u, 3u, 3u, "resource-type", error);
	if (value_resourceType == nullptr) return NativeCalloutDispatch::handled;
	auto& stored_string_query_resourceType = storage.Make<std::string>();
	if (!ReadString(*value_resourceType, stored_string_query_resourceType, error)) { return NativeCalloutDispatch::handled; }
	query.resourceType = stored_string_query_resourceType.c_str();
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "share-level", query.shareLevel, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->SetTeamShareLevel(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_SetTeamStartPosition(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->SetTeamStartPosition == nullptr) { error = "NativeInterface API is unavailable: team_control.SetTeamStartPosition"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetTeamStartPositionQuery query{};
	SetTeamStartPositionResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 2u, 2u, "team-id", query.teamID, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 1u, 1u, 2u, 2u, "pos", Read_Float3, query.pos, storage, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->SetTeamStartPosition(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_ShareTeamResource(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->ShareTeamResource == nullptr) { error = "NativeInterface API is unavailable: team_control.ShareTeamResource"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	ShareTeamResourceQuery query{};
	ShareTeamResourceResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 4u, 4u, "team-id", query.teamID, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 4u, 4u, "target-team-id", query.targetTeamID, error)) return NativeCalloutDispatch::handled;
	const WasmValue* value_resourceType = FindArgument(arguments, 2u, 2u, 4u, 4u, "resource-type", error);
	if (value_resourceType == nullptr) return NativeCalloutDispatch::handled;
	auto& stored_string_query_resourceType = storage.Make<std::string>();
	if (!ReadString(*value_resourceType, stored_string_query_resourceType, error)) { return NativeCalloutDispatch::handled; }
	query.resourceType = stored_string_query_resourceType.c_str();
	if (!ReadArgument(arguments, 3u, 3u, 4u, 4u, "amount", query.amount, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->ShareTeamResource(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_TransferTeamMaxUnits(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->TransferTeamMaxUnits == nullptr) { error = "NativeInterface API is unavailable: team_control.TransferTeamMaxUnits"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	TransferTeamMaxUnitsQuery query{};
	TransferTeamMaxUnitsResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "from-team-id", query.fromTeamID, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 3u, 3u, "to-team-id", query.toTeamID, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "amount", query.amount, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->TransferTeamMaxUnits(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_team_control_UseTeamResource(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->team == nullptr || nativeInterface->syncedCtrl->team->UseTeamResource == nullptr) { error = "NativeInterface API is unavailable: team_control.UseTeamResource"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	UseTeamResourceQuery query{};
	UseTeamResourceResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "team-id", query.teamID, error)) return NativeCalloutDispatch::handled;
	const WasmValue* value_resourceType = FindArgument(arguments, 1u, 1u, 3u, 3u, "resource-type", error);
	if (value_resourceType == nullptr) return NativeCalloutDispatch::handled;
	auto& stored_string_query_resourceType = storage.Make<std::string>();
	if (!ReadString(*value_resourceType, stored_string_query_resourceType, error)) { return NativeCalloutDispatch::handled; }
	query.resourceType = stored_string_query_resourceType.c_str();
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "amount", query.amount, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->team->UseTeamResource(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

}

namespace recoil::wasm::generated {

NativeCalloutDispatch DispatchNativeCalloutModule_team_control(NativeInterface*, std::string_view, const std::vector<WasmValue>&, WasmValue&, std::string&);
NativeCalloutDispatch DispatchNativeCalloutModule_team_control(NativeInterface* nativeInterface, std::string_view function, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (detail::FunctionEquals(function, "AddTeamResource")) return Dispatch_team_control_AddTeamResource(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "AddTeamResourceExcessStats")) return Dispatch_team_control_AddTeamResourceExcessStats(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "AssignPlayerToTeam")) return Dispatch_team_control_AssignPlayerToTeam(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "GameOver")) return Dispatch_team_control_GameOver(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "KillTeam")) return Dispatch_team_control_KillTeam(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "SetAlly")) return Dispatch_team_control_SetAlly(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "SetAllyTeamStartBox")) return Dispatch_team_control_SetAllyTeamStartBox(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "SetGlobalLos")) return Dispatch_team_control_SetGlobalLos(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "SetPlayerReadyState")) return Dispatch_team_control_SetPlayerReadyState(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "SetTeamResource")) return Dispatch_team_control_SetTeamResource(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "SetTeamShareLevel")) return Dispatch_team_control_SetTeamShareLevel(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "SetTeamStartPosition")) return Dispatch_team_control_SetTeamStartPosition(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "ShareTeamResource")) return Dispatch_team_control_ShareTeamResource(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "TransferTeamMaxUnits")) return Dispatch_team_control_TransferTeamMaxUnits(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "UseTeamResource")) return Dispatch_team_control_UseTeamResource(nativeInterface, arguments, result, error);
	return NativeCalloutDispatch::notHandled;
}
}
