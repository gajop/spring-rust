/* @generated by spring-api-codegen; do not edit. */

#include "wasm/generated/WasmCoreGeneratedVariableOutputBindings.h"

#include <bit>
#include <cstdint>
#include <limits>
#include <span>
#include <string>
#include <string_view>
#include <type_traits>

#include "NativeInterface/NativeInterface.h"
#include "WasmInterface/WasmCoreAbi.h"
#include "WasmInterface/WasmCoreGeneratedSupport.h"
#include "WasmInterface/WasmCoreWire.h"

namespace recoil::wasm::core::generated {
namespace {

bool DefineGeneratedVariableOutput(wasmtime_linker_t* linker, const char* moduleName,
    const char* functionName, wasm_functype_t* type,
    wasmtime_func_unchecked_callback_t callback, HostState* state, std::string& error)
{
    wasmtime_error_t* defineError = wasmtime_linker_define_func_unchecked(
        linker,
        moduleName, std::char_traits<char>::length(moduleName),
        functionName, std::char_traits<char>::length(functionName),
        type, callback, state, nullptr);
    wasm_functype_delete(type);
    if (defineError == nullptr)
        return true;
    error = ErrorMessage(defineError);
    return false;
}

bool WriteCoreU32(std::span<std::uint8_t> bytes, std::size_t offset, std::uint32_t value)
{
    if (offset > bytes.size() || bytes.size() - offset < 4)
        return false;
    bytes[offset + 0] = static_cast<std::uint8_t>(value);
    bytes[offset + 1] = static_cast<std::uint8_t>(value >> 8);
    bytes[offset + 2] = static_cast<std::uint8_t>(value >> 16);
    bytes[offset + 3] = static_cast<std::uint8_t>(value >> 24);
    return true;
}

template<typename T>
bool NormalizeCoreCount(T value, std::uint32_t& output)
{
    using Native = std::remove_cv_t<std::remove_reference_t<T>>;
    if constexpr (std::numeric_limits<Native>::is_signed) {
        if (value < 0)
            return false;
    }
    const std::uint64_t wide = static_cast<std::uint64_t>(value);
    if (wide > std::numeric_limits<std::uint32_t>::max())
        return false;
    output = static_cast<std::uint32_t>(wide);
    return true;
}

wasm_trap_t* CoreVariableOutput_units_query_get_render_units(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsQuery == nullptr ||
        state->native->unitsQuery->GetRenderUnits == nullptr)
        return Trap("GetRenderUnits generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetRenderUnits generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader unitsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t unitsPointer = 0;
    std::uint32_t unitsCapacity = 0;
    std::uint32_t unitsIgnoredLength = 0;
    if (!unitsControl.U32(unitsPointer) || !unitsControl.U32(unitsCapacity) ||
        !unitsControl.U32(unitsIgnoredLength) || !unitsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t unitsCapacityBytes = static_cast<std::uint64_t>(unitsCapacity) * 4u;
    if (unitsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(unitsPointer, static_cast<std::size_t>(unitsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetRenderUnitsQuery query{};
    query.drawMask = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.drawMask)>>>(slots[0].i32);
    query.sendMask = slots[1].i32 != 0;
    GetRenderUnitsResult result{};
    state->native->unitsQuery->GetRenderUnits(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t unitsRequired = 0;
    if (!NormalizeCoreCount(result.count, unitsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (unitsRequired != 0 && result.units == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, unitsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || unitsCapacity < unitsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (unitsRequired != 0) {
        const std::size_t unitsBytes = static_cast<std::size_t>(unitsRequired) * 4u;
        std::span<std::uint8_t> unitsWire;
        if (!state->memory.MutableView(unitsPointer, unitsBytes, unitsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter unitsWriter(unitsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < unitsRequired; ++coreIndex) {
        if (!unitsWriter.I32(result.units[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!unitsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_units_query_get_render_units_draw_flag_changed(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsQuery == nullptr ||
        state->native->unitsQuery->GetRenderUnitsDrawFlagChanged == nullptr)
        return Trap("GetRenderUnitsDrawFlagChanged generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetRenderUnitsDrawFlagChanged generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader unitsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t unitsPointer = 0;
    std::uint32_t unitsCapacity = 0;
    std::uint32_t unitsIgnoredLength = 0;
    if (!unitsControl.U32(unitsPointer) || !unitsControl.U32(unitsCapacity) ||
        !unitsControl.U32(unitsIgnoredLength) || !unitsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t unitsCapacityBytes = static_cast<std::uint64_t>(unitsCapacity) * 4u;
    if (unitsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(unitsPointer, static_cast<std::size_t>(unitsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetRenderUnitsDrawFlagChangedQuery query{};
    query.sendMask = slots[0].i32 != 0;
    GetRenderUnitsDrawFlagChangedResult result{};
    state->native->unitsQuery->GetRenderUnitsDrawFlagChanged(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t unitsRequired = 0;
    if (!NormalizeCoreCount(result.count, unitsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (unitsRequired != 0 && result.units == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, unitsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || unitsCapacity < unitsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (unitsRequired != 0) {
        const std::size_t unitsBytes = static_cast<std::size_t>(unitsRequired) * 4u;
        std::span<std::uint8_t> unitsWire;
        if (!state->memory.MutableView(unitsPointer, unitsBytes, unitsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter unitsWriter(unitsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < unitsRequired; ++coreIndex) {
        if (!unitsWriter.I32(result.units[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!unitsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_units_query_get_team_units_counts(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsQuery == nullptr ||
        state->native->unitsQuery->GetTeamUnitsCounts == nullptr)
        return Trap("GetTeamUnitsCounts generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetTeamUnitsCounts generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader countsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t countsPointer = 0;
    std::uint32_t countsCapacity = 0;
    std::uint32_t countsIgnoredLength = 0;
    if (!countsControl.U32(countsPointer) || !countsControl.U32(countsCapacity) ||
        !countsControl.U32(countsIgnoredLength) || !countsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t countsCapacityBytes = static_cast<std::uint64_t>(countsCapacity) * 8u;
    if (countsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(countsPointer, static_cast<std::size_t>(countsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetTeamUnitsCountsQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    GetTeamUnitsCountsResult result{};
    state->native->unitsQuery->GetTeamUnitsCounts(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t countsRequired = 0;
    if (!NormalizeCoreCount(result.count, countsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (countsRequired != 0 && result.counts == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, countsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || countsCapacity < countsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (countsRequired != 0) {
        const std::size_t countsBytes = static_cast<std::size_t>(countsRequired) * 8u;
        std::span<std::uint8_t> countsWire;
        if (!state->memory.MutableView(countsPointer, countsBytes, countsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter countsWriter(countsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < countsRequired; ++coreIndex) {
        if (!countsWriter.I32(result.counts[coreIndex].unitDefID)) return Trap("generated Core wire overflow");
        if (!countsWriter.U32(result.counts[coreIndex].count)) return Trap("generated Core wire overflow");
        if (!countsWriter.Align(4u)) return Trap("generated Core record alignment overflow");
        }
        if (!countsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_units_query_get_units_in_planes(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsQuery == nullptr ||
        state->native->unitsQuery->GetUnitsInPlanes == nullptr)
        return Trap("GetUnitsInPlanes generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetUnitsInPlanes generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader unitsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t unitsPointer = 0;
    std::uint32_t unitsCapacity = 0;
    std::uint32_t unitsIgnoredLength = 0;
    if (!unitsControl.U32(unitsPointer) || !unitsControl.U32(unitsCapacity) ||
        !unitsControl.U32(unitsIgnoredLength) || !unitsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t unitsCapacityBytes = static_cast<std::uint64_t>(unitsCapacity) * 4u;
    if (unitsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(unitsPointer, static_cast<std::size_t>(unitsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    const std::uint32_t input = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 100u, inputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader reader(inputWire);

    GetUnitsInPlanesQuery query{};
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 6u; ++coreReadIndex1) {
        if (!reader.F32(query.planes.planes[coreReadIndex1].x)) return Trap("generated Core wire underflow");
        if (!reader.F32(query.planes.planes[coreReadIndex1].y)) return Trap("generated Core wire underflow");
        if (!reader.F32(query.planes.planes[coreReadIndex1].z)) return Trap("generated Core wire underflow");
        if (!reader.F32(query.planes.planes[coreReadIndex1].w)) return Trap("generated Core wire underflow");
        if (!reader.Align(4u)) return Trap("generated Core record alignment overflow");
    }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("generated Core wire underflow"); query.planes.planeCount = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.planes.planeCount)>>>(coreRaw); }
    if (!reader.Align(4u)) return Trap("generated Core record alignment overflow");
    query.allegiance = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.allegiance)>>>(slots[0].i32);
    if (!reader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    GetUnitsInPlanesResult result{};
    state->native->unitsQuery->GetUnitsInPlanes(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t unitsRequired = 0;
    if (!NormalizeCoreCount(result.count, unitsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (unitsRequired != 0 && result.units == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, unitsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || unitsCapacity < unitsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (unitsRequired != 0) {
        const std::size_t unitsBytes = static_cast<std::size_t>(unitsRequired) * 4u;
        std::span<std::uint8_t> unitsWire;
        if (!state->memory.MutableView(unitsPointer, unitsBytes, unitsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter unitsWriter(unitsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < unitsRequired; ++coreIndex) {
        if (!unitsWriter.I32(result.units[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!unitsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_units_info_get_unit_is_transporting(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsInfo == nullptr ||
        state->native->unitsInfo->GetUnitIsTransporting == nullptr)
        return Trap("GetUnitIsTransporting generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetUnitIsTransporting generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader unitIDsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCapacity = 0;
    std::uint32_t unitIDsIgnoredLength = 0;
    if (!unitIDsControl.U32(unitIDsPointer) || !unitIDsControl.U32(unitIDsCapacity) ||
        !unitIDsControl.U32(unitIDsIgnoredLength) || !unitIDsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t unitIDsCapacityBytes = static_cast<std::uint64_t>(unitIDsCapacity) * 4u;
    if (unitIDsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(unitIDsPointer, static_cast<std::size_t>(unitIDsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetUnitIsTransportingQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    GetUnitIsTransportingResult result{};
    state->native->unitsInfo->GetUnitIsTransporting(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t unitIDsRequired = 0;
    if (!NormalizeCoreCount(result.count, unitIDsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (unitIDsRequired != 0 && result.unitIDs == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, unitIDsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || unitIDsCapacity < unitIDsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (unitIDsRequired != 0) {
        const std::size_t unitIDsBytes = static_cast<std::size_t>(unitIDsRequired) * 4u;
        std::span<std::uint8_t> unitIDsWire;
        if (!state->memory.MutableView(unitIDsPointer, unitIDsBytes, unitIDsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter unitIDsWriter(unitIDsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < unitIDsRequired; ++coreIndex) {
        if (!unitIDsWriter.I32(result.unitIDs[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!unitIDsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.isTransporting)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_units_info_get_unit_nano_pieces(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsInfo == nullptr ||
        state->native->unitsInfo->GetUnitNanoPieces == nullptr)
        return Trap("GetUnitNanoPieces generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetUnitNanoPieces generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader piecesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t piecesPointer = 0;
    std::uint32_t piecesCapacity = 0;
    std::uint32_t piecesIgnoredLength = 0;
    if (!piecesControl.U32(piecesPointer) || !piecesControl.U32(piecesCapacity) ||
        !piecesControl.U32(piecesIgnoredLength) || !piecesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t piecesCapacityBytes = static_cast<std::uint64_t>(piecesCapacity) * 4u;
    if (piecesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(piecesPointer, static_cast<std::size_t>(piecesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetUnitNanoPiecesQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    GetUnitNanoPiecesResult result{};
    state->native->unitsInfo->GetUnitNanoPieces(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t piecesRequired = 0;
    if (!NormalizeCoreCount(result.count, piecesRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (piecesRequired != 0 && result.pieces == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, piecesRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || piecesCapacity < piecesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (piecesRequired != 0) {
        const std::size_t piecesBytes = static_cast<std::size_t>(piecesRequired) * 4u;
        std::span<std::uint8_t> piecesWire;
        if (!state->memory.MutableView(piecesPointer, piecesBytes, piecesWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter piecesWriter(piecesWire);
        for (std::uint32_t coreIndex = 0; coreIndex < piecesRequired; ++coreIndex) {
        if (!piecesWriter.I32(result.pieces[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!piecesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_units_info_get_unit_tooltip(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsInfo == nullptr ||
        state->native->unitsInfo->GetUnitTooltip == nullptr)
        return Trap("GetUnitTooltip generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetUnitTooltip generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader tooltipControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t tooltipPointer = 0;
    std::uint32_t tooltipCapacity = 0;
    std::uint32_t tooltipIgnoredLength = 0;
    if (!tooltipControl.U32(tooltipPointer) || !tooltipControl.U32(tooltipCapacity) ||
        !tooltipControl.U32(tooltipIgnoredLength) || !tooltipControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(tooltipPointer, tooltipCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetUnitTooltipQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    GetUnitTooltipResult result{};
    state->native->unitsInfo->GetUnitTooltip(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t tooltipRequiredSize = result.tooltip == nullptr ? 0u : std::char_traits<char>::length(result.tooltip);
    if (tooltipRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t tooltipRequired = static_cast<std::uint32_t>(tooltipRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, tooltipRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || tooltipCapacity < tooltipRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (tooltipRequired != 0 && !state->memory.Write(tooltipPointer, result.tooltip, tooltipRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_units_commands_get_full_build_queue(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsCommands == nullptr ||
        state->native->unitsCommands->GetFullBuildQueue == nullptr)
        return Trap("GetFullBuildQueue generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetFullBuildQueue generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader entriesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t entriesPointer = 0;
    std::uint32_t entriesCapacity = 0;
    std::uint32_t entriesIgnoredLength = 0;
    if (!entriesControl.U32(entriesPointer) || !entriesControl.U32(entriesCapacity) ||
        !entriesControl.U32(entriesIgnoredLength) || !entriesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t entriesCapacityBytes = static_cast<std::uint64_t>(entriesCapacity) * 8u;
    if (entriesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(entriesPointer, static_cast<std::size_t>(entriesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetFullBuildQueueQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    GetFullBuildQueueResult result{};
    state->native->unitsCommands->GetFullBuildQueue(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t entriesRequired = 0;
    if (!NormalizeCoreCount(result.count, entriesRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (entriesRequired != 0 && result.entries == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, entriesRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || entriesCapacity < entriesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (entriesRequired != 0) {
        const std::size_t entriesBytes = static_cast<std::size_t>(entriesRequired) * 8u;
        std::span<std::uint8_t> entriesWire;
        if (!state->memory.MutableView(entriesPointer, entriesBytes, entriesWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter entriesWriter(entriesWire);
        for (std::uint32_t coreIndex = 0; coreIndex < entriesRequired; ++coreIndex) {
        if (!entriesWriter.I32(result.entries[coreIndex].unitDefID)) return Trap("generated Core wire overflow");
        if (!entriesWriter.U32(result.entries[coreIndex].numOrdered)) return Trap("generated Core wire overflow");
        if (!entriesWriter.Align(4u)) return Trap("generated Core record alignment overflow");
        }
        if (!entriesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_units_commands_get_real_build_queue(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsCommands == nullptr ||
        state->native->unitsCommands->GetRealBuildQueue == nullptr)
        return Trap("GetRealBuildQueue generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetRealBuildQueue generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader unitDefIDsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t unitDefIDsPointer = 0;
    std::uint32_t unitDefIDsCapacity = 0;
    std::uint32_t unitDefIDsIgnoredLength = 0;
    if (!unitDefIDsControl.U32(unitDefIDsPointer) || !unitDefIDsControl.U32(unitDefIDsCapacity) ||
        !unitDefIDsControl.U32(unitDefIDsIgnoredLength) || !unitDefIDsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t unitDefIDsCapacityBytes = static_cast<std::uint64_t>(unitDefIDsCapacity) * 4u;
    if (unitDefIDsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(unitDefIDsPointer, static_cast<std::size_t>(unitDefIDsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetRealBuildQueueQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    GetRealBuildQueueResult result{};
    state->native->unitsCommands->GetRealBuildQueue(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t unitDefIDsRequired = 0;
    if (!NormalizeCoreCount(result.count, unitDefIDsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (unitDefIDsRequired != 0 && result.unitDefIDs == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, unitDefIDsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || unitDefIDsCapacity < unitDefIDsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (unitDefIDsRequired != 0) {
        const std::size_t unitDefIDsBytes = static_cast<std::size_t>(unitDefIDsRequired) * 4u;
        std::span<std::uint8_t> unitDefIDsWire;
        if (!state->memory.MutableView(unitDefIDsPointer, unitDefIDsBytes, unitDefIDsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter unitDefIDsWriter(unitDefIDsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < unitDefIDsRequired; ++coreIndex) {
        if (!unitDefIDsWriter.I32(result.unitDefIDs[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!unitDefIDsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_teams_get_ally_team_list(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->teams == nullptr ||
        state->native->teams->GetAllyTeamList == nullptr)
        return Trap("GetAllyTeamList generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetAllyTeamList generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader allyTeamsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t allyTeamsPointer = 0;
    std::uint32_t allyTeamsCapacity = 0;
    std::uint32_t allyTeamsIgnoredLength = 0;
    if (!allyTeamsControl.U32(allyTeamsPointer) || !allyTeamsControl.U32(allyTeamsCapacity) ||
        !allyTeamsControl.U32(allyTeamsIgnoredLength) || !allyTeamsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t allyTeamsCapacityBytes = static_cast<std::uint64_t>(allyTeamsCapacity) * 4u;
    if (allyTeamsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(allyTeamsPointer, static_cast<std::size_t>(allyTeamsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetAllyTeamListQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetAllyTeamListResult result{};
    state->native->teams->GetAllyTeamList(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t allyTeamsRequired = 0;
    if (!NormalizeCoreCount(result.count, allyTeamsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (allyTeamsRequired != 0 && result.allyTeams == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, allyTeamsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || allyTeamsCapacity < allyTeamsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (allyTeamsRequired != 0) {
        const std::size_t allyTeamsBytes = static_cast<std::size_t>(allyTeamsRequired) * 4u;
        std::span<std::uint8_t> allyTeamsWire;
        if (!state->memory.MutableView(allyTeamsPointer, allyTeamsBytes, allyTeamsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter allyTeamsWriter(allyTeamsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < allyTeamsRequired; ++coreIndex) {
        if (!allyTeamsWriter.I32(result.allyTeams[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!allyTeamsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_teams_get_player_list(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->teams == nullptr ||
        state->native->teams->GetPlayerList == nullptr)
        return Trap("GetPlayerList generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetPlayerList generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader playersControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t playersPointer = 0;
    std::uint32_t playersCapacity = 0;
    std::uint32_t playersIgnoredLength = 0;
    if (!playersControl.U32(playersPointer) || !playersControl.U32(playersCapacity) ||
        !playersControl.U32(playersIgnoredLength) || !playersControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t playersCapacityBytes = static_cast<std::uint64_t>(playersCapacity) * 4u;
    if (playersCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(playersPointer, static_cast<std::size_t>(playersCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetPlayerListQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    query.active = slots[1].i32 != 0;
    GetPlayerListResult result{};
    state->native->teams->GetPlayerList(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t playersRequired = 0;
    if (!NormalizeCoreCount(result.count, playersRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (playersRequired != 0 && result.players == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, playersRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || playersCapacity < playersRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (playersRequired != 0) {
        const std::size_t playersBytes = static_cast<std::size_t>(playersRequired) * 4u;
        std::span<std::uint8_t> playersWire;
        if (!state->memory.MutableView(playersPointer, playersBytes, playersWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter playersWriter(playersWire);
        for (std::uint32_t coreIndex = 0; coreIndex < playersRequired; ++coreIndex) {
        if (!playersWriter.I32(result.players[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!playersWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_teams_get_player_list_in_ally_team(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->teams == nullptr ||
        state->native->teams->GetPlayerListInAllyTeam == nullptr)
        return Trap("GetPlayerListInAllyTeam generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetPlayerListInAllyTeam generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader playersControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t playersPointer = 0;
    std::uint32_t playersCapacity = 0;
    std::uint32_t playersIgnoredLength = 0;
    if (!playersControl.U32(playersPointer) || !playersControl.U32(playersCapacity) ||
        !playersControl.U32(playersIgnoredLength) || !playersControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t playersCapacityBytes = static_cast<std::uint64_t>(playersCapacity) * 4u;
    if (playersCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(playersPointer, static_cast<std::size_t>(playersCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetPlayerListInAllyTeamQuery query{};
    query.allyTeamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.allyTeamID)>>>(slots[0].i32);
    GetPlayerListInAllyTeamResult result{};
    state->native->teams->GetPlayerListInAllyTeam(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t playersRequired = 0;
    if (!NormalizeCoreCount(result.count, playersRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (playersRequired != 0 && result.players == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, playersRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || playersCapacity < playersRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (playersRequired != 0) {
        const std::size_t playersBytes = static_cast<std::size_t>(playersRequired) * 4u;
        std::span<std::uint8_t> playersWire;
        if (!state->memory.MutableView(playersPointer, playersBytes, playersWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter playersWriter(playersWire);
        for (std::uint32_t coreIndex = 0; coreIndex < playersRequired; ++coreIndex) {
        if (!playersWriter.I32(result.players[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!playersWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_teams_get_player_list_in_team(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->teams == nullptr ||
        state->native->teams->GetPlayerListInTeam == nullptr)
        return Trap("GetPlayerListInTeam generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetPlayerListInTeam generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader playersControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t playersPointer = 0;
    std::uint32_t playersCapacity = 0;
    std::uint32_t playersIgnoredLength = 0;
    if (!playersControl.U32(playersPointer) || !playersControl.U32(playersCapacity) ||
        !playersControl.U32(playersIgnoredLength) || !playersControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t playersCapacityBytes = static_cast<std::uint64_t>(playersCapacity) * 4u;
    if (playersCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(playersPointer, static_cast<std::size_t>(playersCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetPlayerListInTeamQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    GetPlayerListInTeamResult result{};
    state->native->teams->GetPlayerListInTeam(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t playersRequired = 0;
    if (!NormalizeCoreCount(result.count, playersRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (playersRequired != 0 && result.players == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, playersRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || playersCapacity < playersRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (playersRequired != 0) {
        const std::size_t playersBytes = static_cast<std::size_t>(playersRequired) * 4u;
        std::span<std::uint8_t> playersWire;
        if (!state->memory.MutableView(playersPointer, playersBytes, playersWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter playersWriter(playersWire);
        for (std::uint32_t coreIndex = 0; coreIndex < playersRequired; ++coreIndex) {
        if (!playersWriter.I32(result.players[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!playersWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_teams_get_team_list(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->teams == nullptr ||
        state->native->teams->GetTeamList == nullptr)
        return Trap("GetTeamList generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetTeamList generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader teamsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t teamsPointer = 0;
    std::uint32_t teamsCapacity = 0;
    std::uint32_t teamsIgnoredLength = 0;
    if (!teamsControl.U32(teamsPointer) || !teamsControl.U32(teamsCapacity) ||
        !teamsControl.U32(teamsIgnoredLength) || !teamsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t teamsCapacityBytes = static_cast<std::uint64_t>(teamsCapacity) * 4u;
    if (teamsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(teamsPointer, static_cast<std::size_t>(teamsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetTeamListQuery query{};
    query.allyTeamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.allyTeamID)>>>(slots[0].i32);
    GetTeamListResult result{};
    state->native->teams->GetTeamList(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t teamsRequired = 0;
    if (!NormalizeCoreCount(result.count, teamsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (teamsRequired != 0 && result.teams == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, teamsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || teamsCapacity < teamsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (teamsRequired != 0) {
        const std::size_t teamsBytes = static_cast<std::size_t>(teamsRequired) * 4u;
        std::span<std::uint8_t> teamsWire;
        if (!state->memory.MutableView(teamsPointer, teamsBytes, teamsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter teamsWriter(teamsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < teamsRequired; ++coreIndex) {
        if (!teamsWriter.I32(result.teams[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!teamsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_teams_get_team_lua_ai(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->teams == nullptr ||
        state->native->teams->GetTeamLuaAI == nullptr)
        return Trap("GetTeamLuaAI generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetTeamLuaAI generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader luaAIControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t luaAIPointer = 0;
    std::uint32_t luaAICapacity = 0;
    std::uint32_t luaAIIgnoredLength = 0;
    if (!luaAIControl.U32(luaAIPointer) || !luaAIControl.U32(luaAICapacity) ||
        !luaAIControl.U32(luaAIIgnoredLength) || !luaAIControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(luaAIPointer, luaAICapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetTeamLuaAIQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    GetTeamLuaAIResult result{};
    state->native->teams->GetTeamLuaAI(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t luaAIRequiredSize = result.luaAI == nullptr ? 0u : std::char_traits<char>::length(result.luaAI);
    if (luaAIRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t luaAIRequired = static_cast<std::uint32_t>(luaAIRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, luaAIRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || luaAICapacity < luaAIRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (luaAIRequired != 0 && !state->memory.Write(luaAIPointer, result.luaAI, luaAIRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_teams_get_team_stats_history(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->teams == nullptr ||
        state->native->teams->GetTeamStatsHistory == nullptr)
        return Trap("GetTeamStatsHistory generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("GetTeamStatsHistory generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader historyControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t historyPointer = 0;
    std::uint32_t historyCapacity = 0;
    std::uint32_t historyIgnoredLength = 0;
    if (!historyControl.U32(historyPointer) || !historyControl.U32(historyCapacity) ||
        !historyControl.U32(historyIgnoredLength) || !historyControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t historyCapacityBytes = static_cast<std::uint64_t>(historyCapacity) * 76u;
    if (historyCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(historyPointer, static_cast<std::size_t>(historyCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetTeamStatsHistoryQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    query.startIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.startIndex)>>>(slots[1].i32);
    query.endIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.endIndex)>>>(slots[2].i32);
    GetTeamStatsHistoryResult result{};
    state->native->teams->GetTeamStatsHistory(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t historyRequired = 0;
    if (!NormalizeCoreCount(result.count, historyRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (historyRequired != 0 && result.history == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, historyRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || historyCapacity < historyRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (historyRequired != 0) {
        const std::size_t historyBytes = static_cast<std::size_t>(historyRequired) * 76u;
        std::span<std::uint8_t> historyWire;
        if (!state->memory.MutableView(historyPointer, historyBytes, historyWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter historyWriter(historyWire);
        for (std::uint32_t coreIndex = 0; coreIndex < historyRequired; ++coreIndex) {
        if (!historyWriter.F32(result.history[coreIndex].metalUsed)) return Trap("generated Core wire overflow");
        if (!historyWriter.F32(result.history[coreIndex].metalProduced)) return Trap("generated Core wire overflow");
        if (!historyWriter.F32(result.history[coreIndex].metalExcess)) return Trap("generated Core wire overflow");
        if (!historyWriter.F32(result.history[coreIndex].metalReceived)) return Trap("generated Core wire overflow");
        if (!historyWriter.F32(result.history[coreIndex].metalSent)) return Trap("generated Core wire overflow");
        if (!historyWriter.F32(result.history[coreIndex].energyUsed)) return Trap("generated Core wire overflow");
        if (!historyWriter.F32(result.history[coreIndex].energyProduced)) return Trap("generated Core wire overflow");
        if (!historyWriter.F32(result.history[coreIndex].energyExcess)) return Trap("generated Core wire overflow");
        if (!historyWriter.F32(result.history[coreIndex].energyReceived)) return Trap("generated Core wire overflow");
        if (!historyWriter.F32(result.history[coreIndex].energySent)) return Trap("generated Core wire overflow");
        if (!historyWriter.F32(result.history[coreIndex].damageDealt)) return Trap("generated Core wire overflow");
        if (!historyWriter.F32(result.history[coreIndex].damageReceived)) return Trap("generated Core wire overflow");
        if (!historyWriter.U32(result.history[coreIndex].unitsProduced)) return Trap("generated Core wire overflow");
        if (!historyWriter.U32(result.history[coreIndex].unitsDied)) return Trap("generated Core wire overflow");
        if (!historyWriter.U32(result.history[coreIndex].unitsReceived)) return Trap("generated Core wire overflow");
        if (!historyWriter.U32(result.history[coreIndex].unitsSent)) return Trap("generated Core wire overflow");
        if (!historyWriter.U32(result.history[coreIndex].unitsCaptured)) return Trap("generated Core wire overflow");
        if (!historyWriter.U32(result.history[coreIndex].unitsOutCaptured)) return Trap("generated Core wire overflow");
        if (!historyWriter.U32(result.history[coreIndex].unitsKilled)) return Trap("generated Core wire overflow");
        if (!historyWriter.Align(4u)) return Trap("generated Core record alignment overflow");
        }
        if (!historyWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_features_get_all_features(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->features == nullptr ||
        state->native->features->GetAllFeatures == nullptr)
        return Trap("GetAllFeatures generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetAllFeatures generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader featuresControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t featuresPointer = 0;
    std::uint32_t featuresCapacity = 0;
    std::uint32_t featuresIgnoredLength = 0;
    if (!featuresControl.U32(featuresPointer) || !featuresControl.U32(featuresCapacity) ||
        !featuresControl.U32(featuresIgnoredLength) || !featuresControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t featuresCapacityBytes = static_cast<std::uint64_t>(featuresCapacity) * 4u;
    if (featuresCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(featuresPointer, static_cast<std::size_t>(featuresCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetAllFeaturesQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetAllFeaturesResult result{};
    state->native->features->GetAllFeatures(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t featuresRequired = 0;
    if (!NormalizeCoreCount(result.count, featuresRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (featuresRequired != 0 && result.features == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, featuresRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || featuresCapacity < featuresRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (featuresRequired != 0) {
        const std::size_t featuresBytes = static_cast<std::size_t>(featuresRequired) * 4u;
        std::span<std::uint8_t> featuresWire;
        if (!state->memory.MutableView(featuresPointer, featuresBytes, featuresWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter featuresWriter(featuresWire);
        for (std::uint32_t coreIndex = 0; coreIndex < featuresRequired; ++coreIndex) {
        if (!featuresWriter.I32(result.features[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!featuresWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_features_get_features_in_cylinder(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->features == nullptr ||
        state->native->features->GetFeaturesInCylinder == nullptr)
        return Trap("GetFeaturesInCylinder generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 5)
        return Trap("GetFeaturesInCylinder generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader featuresControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t featuresPointer = 0;
    std::uint32_t featuresCapacity = 0;
    std::uint32_t featuresIgnoredLength = 0;
    if (!featuresControl.U32(featuresPointer) || !featuresControl.U32(featuresCapacity) ||
        !featuresControl.U32(featuresIgnoredLength) || !featuresControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t featuresCapacityBytes = static_cast<std::uint64_t>(featuresCapacity) * 4u;
    if (featuresCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(featuresPointer, static_cast<std::size_t>(featuresCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetFeaturesInCylinderQuery query{};
    query.x = slots[0].f32;
    query.z = slots[1].f32;
    query.radius = slots[2].f32;
    query.height = slots[3].f32;
    GetFeaturesInCylinderResult result{};
    state->native->features->GetFeaturesInCylinder(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t featuresRequired = 0;
    if (!NormalizeCoreCount(result.count, featuresRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (featuresRequired != 0 && result.features == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, featuresRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || featuresCapacity < featuresRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (featuresRequired != 0) {
        const std::size_t featuresBytes = static_cast<std::size_t>(featuresRequired) * 4u;
        std::span<std::uint8_t> featuresWire;
        if (!state->memory.MutableView(featuresPointer, featuresBytes, featuresWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter featuresWriter(featuresWire);
        for (std::uint32_t coreIndex = 0; coreIndex < featuresRequired; ++coreIndex) {
        if (!featuresWriter.I32(result.features[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!featuresWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_features_get_features_in_rectangle(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->features == nullptr ||
        state->native->features->GetFeaturesInRectangle == nullptr)
        return Trap("GetFeaturesInRectangle generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 5)
        return Trap("GetFeaturesInRectangle generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader featuresControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t featuresPointer = 0;
    std::uint32_t featuresCapacity = 0;
    std::uint32_t featuresIgnoredLength = 0;
    if (!featuresControl.U32(featuresPointer) || !featuresControl.U32(featuresCapacity) ||
        !featuresControl.U32(featuresIgnoredLength) || !featuresControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t featuresCapacityBytes = static_cast<std::uint64_t>(featuresCapacity) * 4u;
    if (featuresCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(featuresPointer, static_cast<std::size_t>(featuresCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetFeaturesInRectangleQuery query{};
    query.minX = slots[0].f32;
    query.minZ = slots[1].f32;
    query.maxX = slots[2].f32;
    query.maxZ = slots[3].f32;
    GetFeaturesInRectangleResult result{};
    state->native->features->GetFeaturesInRectangle(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t featuresRequired = 0;
    if (!NormalizeCoreCount(result.count, featuresRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (featuresRequired != 0 && result.features == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, featuresRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || featuresCapacity < featuresRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (featuresRequired != 0) {
        const std::size_t featuresBytes = static_cast<std::size_t>(featuresRequired) * 4u;
        std::span<std::uint8_t> featuresWire;
        if (!state->memory.MutableView(featuresPointer, featuresBytes, featuresWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter featuresWriter(featuresWire);
        for (std::uint32_t coreIndex = 0; coreIndex < featuresRequired; ++coreIndex) {
        if (!featuresWriter.I32(result.features[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!featuresWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_features_get_features_in_sphere(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->features == nullptr ||
        state->native->features->GetFeaturesInSphere == nullptr)
        return Trap("GetFeaturesInSphere generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetFeaturesInSphere generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader featuresControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t featuresPointer = 0;
    std::uint32_t featuresCapacity = 0;
    std::uint32_t featuresIgnoredLength = 0;
    if (!featuresControl.U32(featuresPointer) || !featuresControl.U32(featuresCapacity) ||
        !featuresControl.U32(featuresIgnoredLength) || !featuresControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t featuresCapacityBytes = static_cast<std::uint64_t>(featuresCapacity) * 4u;
    if (featuresCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(featuresPointer, static_cast<std::size_t>(featuresCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    const std::uint32_t input = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 12u, inputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader reader(inputWire);

    GetFeaturesInSphereQuery query{};
    if (!reader.F32(query.center.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.center.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.center.z)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated Core record alignment overflow");
    query.radius = slots[0].f32;
    if (!reader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    GetFeaturesInSphereResult result{};
    state->native->features->GetFeaturesInSphere(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t featuresRequired = 0;
    if (!NormalizeCoreCount(result.count, featuresRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (featuresRequired != 0 && result.features == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, featuresRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || featuresCapacity < featuresRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (featuresRequired != 0) {
        const std::size_t featuresBytes = static_cast<std::size_t>(featuresRequired) * 4u;
        std::span<std::uint8_t> featuresWire;
        if (!state->memory.MutableView(featuresPointer, featuresBytes, featuresWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter featuresWriter(featuresWire);
        for (std::uint32_t coreIndex = 0; coreIndex < featuresRequired; ++coreIndex) {
        if (!featuresWriter.I32(result.features[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!featuresWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_features_get_render_features(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->features == nullptr ||
        state->native->features->GetRenderFeatures == nullptr)
        return Trap("GetRenderFeatures generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetRenderFeatures generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader featuresControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t featuresPointer = 0;
    std::uint32_t featuresCapacity = 0;
    std::uint32_t featuresIgnoredLength = 0;
    if (!featuresControl.U32(featuresPointer) || !featuresControl.U32(featuresCapacity) ||
        !featuresControl.U32(featuresIgnoredLength) || !featuresControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t featuresCapacityBytes = static_cast<std::uint64_t>(featuresCapacity) * 4u;
    if (featuresCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(featuresPointer, static_cast<std::size_t>(featuresCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetRenderFeaturesQuery query{};
    query.drawMask = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.drawMask)>>>(slots[0].i32);
    query.sendMask = slots[1].i32 != 0;
    GetRenderFeaturesResult result{};
    state->native->features->GetRenderFeatures(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t featuresRequired = 0;
    if (!NormalizeCoreCount(result.count, featuresRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (featuresRequired != 0 && result.features == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, featuresRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || featuresCapacity < featuresRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (featuresRequired != 0) {
        const std::size_t featuresBytes = static_cast<std::size_t>(featuresRequired) * 4u;
        std::span<std::uint8_t> featuresWire;
        if (!state->memory.MutableView(featuresPointer, featuresBytes, featuresWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter featuresWriter(featuresWire);
        for (std::uint32_t coreIndex = 0; coreIndex < featuresRequired; ++coreIndex) {
        if (!featuresWriter.I32(result.features[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!featuresWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_features_get_render_features_draw_flag_changed(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->features == nullptr ||
        state->native->features->GetRenderFeaturesDrawFlagChanged == nullptr)
        return Trap("GetRenderFeaturesDrawFlagChanged generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetRenderFeaturesDrawFlagChanged generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader featuresControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t featuresPointer = 0;
    std::uint32_t featuresCapacity = 0;
    std::uint32_t featuresIgnoredLength = 0;
    if (!featuresControl.U32(featuresPointer) || !featuresControl.U32(featuresCapacity) ||
        !featuresControl.U32(featuresIgnoredLength) || !featuresControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t featuresCapacityBytes = static_cast<std::uint64_t>(featuresCapacity) * 4u;
    if (featuresCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(featuresPointer, static_cast<std::size_t>(featuresCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetRenderFeaturesDrawFlagChangedQuery query{};
    query.sendMask = slots[0].i32 != 0;
    GetRenderFeaturesDrawFlagChangedResult result{};
    state->native->features->GetRenderFeaturesDrawFlagChanged(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t featuresRequired = 0;
    if (!NormalizeCoreCount(result.count, featuresRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (featuresRequired != 0 && result.features == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, featuresRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || featuresCapacity < featuresRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (featuresRequired != 0) {
        const std::size_t featuresBytes = static_cast<std::size_t>(featuresRequired) * 4u;
        std::span<std::uint8_t> featuresWire;
        if (!state->memory.MutableView(featuresPointer, featuresBytes, featuresWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter featuresWriter(featuresWire);
        for (std::uint32_t coreIndex = 0; coreIndex < featuresRequired; ++coreIndex) {
        if (!featuresWriter.I32(result.features[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!featuresWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_projectiles_get_all_projectiles(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->projectiles == nullptr ||
        state->native->projectiles->GetAllProjectiles == nullptr)
        return Trap("GetAllProjectiles generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetAllProjectiles generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader projectilesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t projectilesPointer = 0;
    std::uint32_t projectilesCapacity = 0;
    std::uint32_t projectilesIgnoredLength = 0;
    if (!projectilesControl.U32(projectilesPointer) || !projectilesControl.U32(projectilesCapacity) ||
        !projectilesControl.U32(projectilesIgnoredLength) || !projectilesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t projectilesCapacityBytes = static_cast<std::uint64_t>(projectilesCapacity) * 4u;
    if (projectilesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(projectilesPointer, static_cast<std::size_t>(projectilesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    const std::uint32_t input = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 8u, inputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader reader(inputWire);

    GetAllProjectilesQuery query{};
    if (!reader.Bool(query.options.excludeWeaponProjectiles)) return Trap("generated Core wire underflow");
    if (!reader.Bool(query.options.excludePieceProjectiles)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated Core record alignment overflow");
    if (!reader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    GetAllProjectilesResult result{};
    state->native->projectiles->GetAllProjectiles(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t projectilesRequired = 0;
    if (!NormalizeCoreCount(result.count, projectilesRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (projectilesRequired != 0 && result.projectiles == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, projectilesRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || projectilesCapacity < projectilesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (projectilesRequired != 0) {
        const std::size_t projectilesBytes = static_cast<std::size_t>(projectilesRequired) * 4u;
        std::span<std::uint8_t> projectilesWire;
        if (!state->memory.MutableView(projectilesPointer, projectilesBytes, projectilesWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter projectilesWriter(projectilesWire);
        for (std::uint32_t coreIndex = 0; coreIndex < projectilesRequired; ++coreIndex) {
        if (!projectilesWriter.I32(result.projectiles[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!projectilesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_projectiles_get_projectiles_in_rectangle(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->projectiles == nullptr ||
        state->native->projectiles->GetProjectilesInRectangle == nullptr)
        return Trap("GetProjectilesInRectangle generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 6)
        return Trap("GetProjectilesInRectangle generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 7u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[5].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader projectilesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t projectilesPointer = 0;
    std::uint32_t projectilesCapacity = 0;
    std::uint32_t projectilesIgnoredLength = 0;
    if (!projectilesControl.U32(projectilesPointer) || !projectilesControl.U32(projectilesCapacity) ||
        !projectilesControl.U32(projectilesIgnoredLength) || !projectilesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t projectilesCapacityBytes = static_cast<std::uint64_t>(projectilesCapacity) * 4u;
    if (projectilesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(projectilesPointer, static_cast<std::size_t>(projectilesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    const std::uint32_t input = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 8u, inputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader reader(inputWire);

    GetProjectilesInRectangleQuery query{};
    query.minX = slots[0].f32;
    query.minZ = slots[1].f32;
    query.maxX = slots[2].f32;
    query.maxZ = slots[3].f32;
    if (!reader.Bool(query.options.excludeWeaponProjectiles)) return Trap("generated Core wire underflow");
    if (!reader.Bool(query.options.excludePieceProjectiles)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated Core record alignment overflow");
    if (!reader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    GetProjectilesInRectangleResult result{};
    state->native->projectiles->GetProjectilesInRectangle(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t projectilesRequired = 0;
    if (!NormalizeCoreCount(result.count, projectilesRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (projectilesRequired != 0 && result.projectiles == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, projectilesRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || projectilesCapacity < projectilesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (projectilesRequired != 0) {
        const std::size_t projectilesBytes = static_cast<std::size_t>(projectilesRequired) * 4u;
        std::span<std::uint8_t> projectilesWire;
        if (!state->memory.MutableView(projectilesPointer, projectilesBytes, projectilesWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter projectilesWriter(projectilesWire);
        for (std::uint32_t coreIndex = 0; coreIndex < projectilesRequired; ++coreIndex) {
        if (!projectilesWriter.I32(result.projectiles[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!projectilesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_projectiles_get_projectiles_in_sphere(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->projectiles == nullptr ||
        state->native->projectiles->GetProjectilesInSphere == nullptr)
        return Trap("GetProjectilesInSphere generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetProjectilesInSphere generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader projectilesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t projectilesPointer = 0;
    std::uint32_t projectilesCapacity = 0;
    std::uint32_t projectilesIgnoredLength = 0;
    if (!projectilesControl.U32(projectilesPointer) || !projectilesControl.U32(projectilesCapacity) ||
        !projectilesControl.U32(projectilesIgnoredLength) || !projectilesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t projectilesCapacityBytes = static_cast<std::uint64_t>(projectilesCapacity) * 4u;
    if (projectilesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(projectilesPointer, static_cast<std::size_t>(projectilesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    const std::uint32_t input = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 20u, inputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader reader(inputWire);

    GetProjectilesInSphereQuery query{};
    if (!reader.F32(query.center.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.center.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.center.z)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated Core record alignment overflow");
    query.radius = slots[0].f32;
    if (!reader.Bool(query.options.excludeWeaponProjectiles)) return Trap("generated Core wire underflow");
    if (!reader.Bool(query.options.excludePieceProjectiles)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated Core record alignment overflow");
    if (!reader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    GetProjectilesInSphereResult result{};
    state->native->projectiles->GetProjectilesInSphere(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t projectilesRequired = 0;
    if (!NormalizeCoreCount(result.count, projectilesRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (projectilesRequired != 0 && result.projectiles == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, projectilesRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || projectilesCapacity < projectilesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (projectilesRequired != 0) {
        const std::size_t projectilesBytes = static_cast<std::size_t>(projectilesRequired) * 4u;
        std::span<std::uint8_t> projectilesWire;
        if (!state->memory.MutableView(projectilesPointer, projectilesBytes, projectilesWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter projectilesWriter(projectilesWire);
        for (std::uint32_t coreIndex = 0; coreIndex < projectilesRequired; ++coreIndex) {
        if (!projectilesWriter.I32(result.projectiles[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!projectilesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_unit_defs_get_unit_def_i_ds(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefIDs == nullptr)
        return Trap("GetUnitDefIDs generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetUnitDefIDs generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader idsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCapacity = 0;
    std::uint32_t idsIgnoredLength = 0;
    if (!idsControl.U32(idsPointer) || !idsControl.U32(idsCapacity) ||
        !idsControl.U32(idsIgnoredLength) || !idsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t idsCapacityBytes = static_cast<std::uint64_t>(idsCapacity) * 4u;
    if (idsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(idsPointer, static_cast<std::size_t>(idsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetUnitDefIDsQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetUnitDefIDsResult result{};
    state->native->unitDefs->GetUnitDefIDs(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t idsRequired = 0;
    if (!NormalizeCoreCount(result.count, idsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (idsRequired != 0 && result.ids == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, idsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || idsCapacity < idsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (idsRequired != 0) {
        const std::size_t idsBytes = static_cast<std::size_t>(idsRequired) * 4u;
        std::span<std::uint8_t> idsWire;
        if (!state->memory.MutableView(idsPointer, idsBytes, idsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter idsWriter(idsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < idsRequired; ++coreIndex) {
        if (!idsWriter.I32(result.ids[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!idsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_feature_defs_get_feature_def_i_ds(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->featureDefs == nullptr ||
        state->native->featureDefs->GetFeatureDefIDs == nullptr)
        return Trap("GetFeatureDefIDs generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetFeatureDefIDs generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader idsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCapacity = 0;
    std::uint32_t idsIgnoredLength = 0;
    if (!idsControl.U32(idsPointer) || !idsControl.U32(idsCapacity) ||
        !idsControl.U32(idsIgnoredLength) || !idsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t idsCapacityBytes = static_cast<std::uint64_t>(idsCapacity) * 4u;
    if (idsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(idsPointer, static_cast<std::size_t>(idsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetFeatureDefIDsQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetFeatureDefIDsResult result{};
    state->native->featureDefs->GetFeatureDefIDs(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t idsRequired = 0;
    if (!NormalizeCoreCount(result.count, idsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (idsRequired != 0 && result.ids == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, idsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || idsCapacity < idsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (idsRequired != 0) {
        const std::size_t idsBytes = static_cast<std::size_t>(idsRequired) * 4u;
        std::span<std::uint8_t> idsWire;
        if (!state->memory.MutableView(idsPointer, idsBytes, idsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter idsWriter(idsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < idsRequired; ++coreIndex) {
        if (!idsWriter.I32(result.ids[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!idsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_feature_defs_get_feature_def_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->featureDefs == nullptr ||
        state->native->featureDefs->GetFeatureDefName == nullptr)
        return Trap("GetFeatureDefName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetFeatureDefName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader nameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t namePointer = 0;
    std::uint32_t nameCapacity = 0;
    std::uint32_t nameIgnoredLength = 0;
    if (!nameControl.U32(namePointer) || !nameControl.U32(nameCapacity) ||
        !nameControl.U32(nameIgnoredLength) || !nameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(namePointer, nameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetFeatureDefNameQuery query{};
    query.featureDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.featureDefID)>>>(slots[0].i32);
    GetFeatureDefNameResult result{};
    state->native->featureDefs->GetFeatureDefName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t nameRequiredSize = result.name == nullptr ? 0u : std::char_traits<char>::length(result.name);
    if (nameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t nameRequired = static_cast<std::uint32_t>(nameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, nameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || nameCapacity < nameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (nameRequired != 0 && !state->memory.Write(namePointer, result.name, nameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_weapon_defs_get_weapon_def_i_ds(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->weaponDefs == nullptr ||
        state->native->weaponDefs->GetWeaponDefIDs == nullptr)
        return Trap("GetWeaponDefIDs generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetWeaponDefIDs generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader idsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCapacity = 0;
    std::uint32_t idsIgnoredLength = 0;
    if (!idsControl.U32(idsPointer) || !idsControl.U32(idsCapacity) ||
        !idsControl.U32(idsIgnoredLength) || !idsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t idsCapacityBytes = static_cast<std::uint64_t>(idsCapacity) * 4u;
    if (idsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(idsPointer, static_cast<std::size_t>(idsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetWeaponDefIDsQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetWeaponDefIDsResult result{};
    state->native->weaponDefs->GetWeaponDefIDs(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t idsRequired = 0;
    if (!NormalizeCoreCount(result.count, idsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (idsRequired != 0 && result.ids == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, idsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || idsCapacity < idsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (idsRequired != 0) {
        const std::size_t idsBytes = static_cast<std::size_t>(idsRequired) * 4u;
        std::span<std::uint8_t> idsWire;
        if (!state->memory.MutableView(idsPointer, idsBytes, idsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter idsWriter(idsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < idsRequired; ++coreIndex) {
        if (!idsWriter.I32(result.ids[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!idsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_weapon_defs_get_weapon_def_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->weaponDefs == nullptr ||
        state->native->weaponDefs->GetWeaponDefName == nullptr)
        return Trap("GetWeaponDefName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetWeaponDefName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader nameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t namePointer = 0;
    std::uint32_t nameCapacity = 0;
    std::uint32_t nameIgnoredLength = 0;
    if (!nameControl.U32(namePointer) || !nameControl.U32(nameCapacity) ||
        !nameControl.U32(nameIgnoredLength) || !nameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(namePointer, nameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetWeaponDefNameQuery query{};
    query.weaponDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.weaponDefID)>>>(slots[0].i32);
    GetWeaponDefNameResult result{};
    state->native->weaponDefs->GetWeaponDefName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t nameRequiredSize = result.name == nullptr ? 0u : std::char_traits<char>::length(result.name);
    if (nameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t nameRequired = static_cast<std::uint32_t>(nameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, nameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || nameCapacity < nameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (nameRequired != 0 && !state->memory.Write(namePointer, result.name, nameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_game_get_map_start_positions(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->game == nullptr ||
        state->native->game->GetMapStartPositions == nullptr)
        return Trap("GetMapStartPositions generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetMapStartPositions generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader positionsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t positionsPointer = 0;
    std::uint32_t positionsCapacity = 0;
    std::uint32_t positionsIgnoredLength = 0;
    if (!positionsControl.U32(positionsPointer) || !positionsControl.U32(positionsCapacity) ||
        !positionsControl.U32(positionsIgnoredLength) || !positionsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t positionsCapacityBytes = static_cast<std::uint64_t>(positionsCapacity) * 16u;
    if (positionsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(positionsPointer, static_cast<std::size_t>(positionsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetMapStartPositionsQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetMapStartPositionsResult result{};
    state->native->game->GetMapStartPositions(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t positionsRequired = 0;
    if (!NormalizeCoreCount(result.count, positionsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (positionsRequired != 0 && result.positions == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, positionsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || positionsCapacity < positionsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (positionsRequired != 0) {
        const std::size_t positionsBytes = static_cast<std::size_t>(positionsRequired) * 16u;
        std::span<std::uint8_t> positionsWire;
        if (!state->memory.MutableView(positionsPointer, positionsBytes, positionsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter positionsWriter(positionsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < positionsRequired; ++coreIndex) {
        if (!positionsWriter.F32(result.positions[coreIndex].pos.x)) return Trap("generated Core wire overflow");
        if (!positionsWriter.F32(result.positions[coreIndex].pos.y)) return Trap("generated Core wire overflow");
        if (!positionsWriter.F32(result.positions[coreIndex].pos.z)) return Trap("generated Core wire overflow");
        if (!positionsWriter.Align(4u)) return Trap("generated Core record alignment overflow");
        if (!positionsWriter.I32(result.positions[coreIndex].teamID)) return Trap("generated Core wire overflow");
        if (!positionsWriter.Align(4u)) return Trap("generated Core record alignment overflow");
        }
        if (!positionsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_terrain_get_ground_info(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->terrain == nullptr ||
        state->native->terrain->GetGroundInfo == nullptr)
        return Trap("GetGroundInfo generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetGroundInfo generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 44u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader terrainTypeNameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t terrainTypeNamePointer = 0;
    std::uint32_t terrainTypeNameCapacity = 0;
    std::uint32_t terrainTypeNameIgnoredLength = 0;
    if (!terrainTypeNameControl.U32(terrainTypeNamePointer) || !terrainTypeNameControl.U32(terrainTypeNameCapacity) ||
        !terrainTypeNameControl.U32(terrainTypeNameIgnoredLength) || !terrainTypeNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(terrainTypeNamePointer, terrainTypeNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetGroundInfoQuery query{};
    query.x = slots[0].f32;
    query.z = slots[1].f32;
    GetGroundInfoResult result{};
    state->native->terrain->GetGroundInfo(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t terrainTypeNameRequiredSize = result.terrainTypeName == nullptr ? 0u : std::char_traits<char>::length(result.terrainTypeName);
    if (terrainTypeNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t terrainTypeNameRequired = static_cast<std::uint32_t>(terrainTypeNameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, terrainTypeNameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || terrainTypeNameCapacity < terrainTypeNameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (terrainTypeNameRequired != 0 && !state->memory.Write(terrainTypeNamePointer, result.terrainTypeName, terrainTypeNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 32u);
    WireWriter writer(fixedWire);
    if (!writer.I32(result.terrainTypeIndex)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.metalExtraction)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hardness)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.tankSpeed)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.kbotSpeed)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hoverSpeed)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.shipSpeed)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.receiveTracks)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_terrain_get_terrain_type_data(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->terrain == nullptr ||
        state->native->terrain->GetTerrainTypeData == nullptr)
        return Trap("GetTerrainTypeData generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetTerrainTypeData generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 40u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader nameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t namePointer = 0;
    std::uint32_t nameCapacity = 0;
    std::uint32_t nameIgnoredLength = 0;
    if (!nameControl.U32(namePointer) || !nameControl.U32(nameCapacity) ||
        !nameControl.U32(nameIgnoredLength) || !nameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(namePointer, nameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetTerrainTypeDataQuery query{};
    query.terrainTypeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.terrainTypeIndex)>>>(slots[0].i32);
    GetTerrainTypeDataResult result{};
    state->native->terrain->GetTerrainTypeData(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t nameRequiredSize = result.name == nullptr ? 0u : std::char_traits<char>::length(result.name);
    if (nameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t nameRequired = static_cast<std::uint32_t>(nameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, nameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || nameCapacity < nameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (nameRequired != 0 && !state->memory.Write(namePointer, result.name, nameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 28u);
    WireWriter writer(fixedWire);
    if (!writer.I32(result.index)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hardness)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.tankSpeed)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.kbotSpeed)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hoverSpeed)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.shipSpeed)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.receiveTracks)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_player_get_player_traffic(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->player == nullptr ||
        state->native->player->GetPlayerTraffic == nullptr)
        return Trap("GetPlayerTraffic generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetPlayerTraffic generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader trafficControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t trafficPointer = 0;
    std::uint32_t trafficCapacity = 0;
    std::uint32_t trafficIgnoredLength = 0;
    if (!trafficControl.U32(trafficPointer) || !trafficControl.U32(trafficCapacity) ||
        !trafficControl.U32(trafficIgnoredLength) || !trafficControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t trafficCapacityBytes = static_cast<std::uint64_t>(trafficCapacity) * 20u;
    if (trafficCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(trafficPointer, static_cast<std::size_t>(trafficCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetPlayerTrafficQuery query{};
    query.playerID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.playerID)>>>(slots[0].i32);
    query.packetID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.packetID)>>>(slots[1].i32);
    GetPlayerTrafficResult result{};
    state->native->player->GetPlayerTraffic(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t trafficRequired = 0;
    if (!NormalizeCoreCount(result.count, trafficRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (trafficRequired != 0 && result.traffic == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, trafficRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || trafficCapacity < trafficRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (trafficRequired != 0) {
        const std::size_t trafficBytes = static_cast<std::size_t>(trafficRequired) * 20u;
        std::span<std::uint8_t> trafficWire;
        if (!state->memory.MutableView(trafficPointer, trafficBytes, trafficWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter trafficWriter(trafficWire);
        for (std::uint32_t coreIndex = 0; coreIndex < trafficRequired; ++coreIndex) {
        if (!trafficWriter.I32(result.traffic[coreIndex].playerID)) return Trap("generated Core wire overflow");
        if (!trafficWriter.U32(result.traffic[coreIndex].packetsSent)) return Trap("generated Core wire overflow");
        if (!trafficWriter.U32(result.traffic[coreIndex].packetsReceived)) return Trap("generated Core wire overflow");
        if (!trafficWriter.U32(result.traffic[coreIndex].bytesSent)) return Trap("generated Core wire overflow");
        if (!trafficWriter.U32(result.traffic[coreIndex].bytesReceived)) return Trap("generated Core wire overflow");
        if (!trafficWriter.Align(4u)) return Trap("generated Core record alignment overflow");
        }
        if (!trafficWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_path_finder_get_path_node_costs(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->pathFinder == nullptr ||
        state->native->pathFinder->GetPathNodeCosts == nullptr)
        return Trap("GetPathNodeCosts generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetPathNodeCosts generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader costsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t costsPointer = 0;
    std::uint32_t costsCapacity = 0;
    std::uint32_t costsIgnoredLength = 0;
    if (!costsControl.U32(costsPointer) || !costsControl.U32(costsCapacity) ||
        !costsControl.U32(costsIgnoredLength) || !costsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t costsCapacityBytes = static_cast<std::uint64_t>(costsCapacity) * 4u;
    if (costsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(costsPointer, static_cast<std::size_t>(costsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetPathNodeCostsQuery query{};
    query.overlayIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.overlayIndex)>>>(slots[0].i32);
    GetPathNodeCostsResult result{};
    state->native->pathFinder->GetPathNodeCosts(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t costsRequired = 0;
    if (!NormalizeCoreCount(result.count, costsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (costsRequired != 0 && result.costs == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, costsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || costsCapacity < costsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (costsRequired != 0) {
        const std::size_t costsBytes = static_cast<std::size_t>(costsRequired) * 4u;
        std::span<std::uint8_t> costsWire;
        if (!state->memory.MutableView(costsPointer, costsBytes, costsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter costsWriter(costsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < costsRequired; ++coreIndex) {
        if (!costsWriter.F32(result.costs[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!costsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_path_finder_get_path_way_points(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->pathFinder == nullptr ||
        state->native->pathFinder->GetPathWayPoints == nullptr)
        return Trap("GetPathWayPoints generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetPathWayPoints generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 24u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader pointsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t pointsPointer = 0;
    std::uint32_t pointsCapacity = 0;
    std::uint32_t pointsIgnoredLength = 0;
    if (!pointsControl.U32(pointsPointer) || !pointsControl.U32(pointsCapacity) ||
        !pointsControl.U32(pointsIgnoredLength) || !pointsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t pointsCapacityBytes = static_cast<std::uint64_t>(pointsCapacity) * 12u;
    if (pointsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(pointsPointer, static_cast<std::size_t>(pointsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader startsControl(std::span<const std::uint8_t>(outputWire.data() + 12u, 12));
    std::uint32_t startsPointer = 0;
    std::uint32_t startsCapacity = 0;
    std::uint32_t startsIgnoredLength = 0;
    if (!startsControl.U32(startsPointer) || !startsControl.U32(startsCapacity) ||
        !startsControl.U32(startsIgnoredLength) || !startsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t startsCapacityBytes = static_cast<std::uint64_t>(startsCapacity) * 4u;
    if (startsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(startsPointer, static_cast<std::size_t>(startsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetPathWayPointsQuery query{};
    query.pathID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.pathID)>>>(slots[0].i32);
    GetPathWayPointsResult result{};
    state->native->pathFinder->GetPathWayPoints(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t pointsRequired = 0;
    if (!NormalizeCoreCount(result.pointCount, pointsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (pointsRequired != 0 && result.points == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, pointsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || pointsCapacity < pointsRequired;
    std::uint32_t startsRequired = 0;
    if (!NormalizeCoreCount(result.startCount, startsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (startsRequired != 0 && result.starts == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 20u, startsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || startsCapacity < startsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (pointsRequired != 0) {
        const std::size_t pointsBytes = static_cast<std::size_t>(pointsRequired) * 12u;
        std::span<std::uint8_t> pointsWire;
        if (!state->memory.MutableView(pointsPointer, pointsBytes, pointsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter pointsWriter(pointsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < pointsRequired; ++coreIndex) {
        if (!pointsWriter.F32(result.points[coreIndex].x)) return Trap("generated Core wire overflow");
        if (!pointsWriter.F32(result.points[coreIndex].y)) return Trap("generated Core wire overflow");
        if (!pointsWriter.F32(result.points[coreIndex].z)) return Trap("generated Core wire overflow");
        if (!pointsWriter.Align(4u)) return Trap("generated Core record alignment overflow");
        }
        if (!pointsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    if (startsRequired != 0) {
        const std::size_t startsBytes = static_cast<std::size_t>(startsRequired) * 4u;
        std::span<std::uint8_t> startsWire;
        if (!state->memory.MutableView(startsPointer, startsBytes, startsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter startsWriter(startsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < startsRequired; ++coreIndex) {
        if (!startsWriter.I32(result.starts[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!startsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_platform_get_architecture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->platform == nullptr ||
        state->native->platform->GetArchitecture == nullptr)
        return Trap("GetArchitecture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetArchitecture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader architectureControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t architecturePointer = 0;
    std::uint32_t architectureCapacity = 0;
    std::uint32_t architectureIgnoredLength = 0;
    if (!architectureControl.U32(architecturePointer) || !architectureControl.U32(architectureCapacity) ||
        !architectureControl.U32(architectureIgnoredLength) || !architectureControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(architecturePointer, architectureCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetArchitectureQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetArchitectureResult result{};
    state->native->platform->GetArchitecture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t architectureRequiredSize = result.architecture == nullptr ? 0u : std::char_traits<char>::length(result.architecture);
    if (architectureRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t architectureRequired = static_cast<std::uint32_t>(architectureRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, architectureRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || architectureCapacity < architectureRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (architectureRequired != 0 && !state->memory.Write(architecturePointer, result.architecture, architectureRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_move_ctrl_get_unit_estimated_path(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->moveCtrl == nullptr ||
        state->native->moveCtrl->GetUnitEstimatedPath == nullptr)
        return Trap("GetUnitEstimatedPath generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetUnitEstimatedPath generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 24u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader waypointsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t waypointsPointer = 0;
    std::uint32_t waypointsCapacity = 0;
    std::uint32_t waypointsIgnoredLength = 0;
    if (!waypointsControl.U32(waypointsPointer) || !waypointsControl.U32(waypointsCapacity) ||
        !waypointsControl.U32(waypointsIgnoredLength) || !waypointsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t waypointsCapacityBytes = static_cast<std::uint64_t>(waypointsCapacity) * 16u;
    if (waypointsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(waypointsPointer, static_cast<std::size_t>(waypointsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader startsControl(std::span<const std::uint8_t>(outputWire.data() + 12u, 12));
    std::uint32_t startsPointer = 0;
    std::uint32_t startsCapacity = 0;
    std::uint32_t startsIgnoredLength = 0;
    if (!startsControl.U32(startsPointer) || !startsControl.U32(startsCapacity) ||
        !startsControl.U32(startsIgnoredLength) || !startsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t startsCapacityBytes = static_cast<std::uint64_t>(startsCapacity) * 4u;
    if (startsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(startsPointer, static_cast<std::size_t>(startsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetUnitEstimatedPathQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    GetUnitEstimatedPathResult result{};
    state->native->moveCtrl->GetUnitEstimatedPath(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t waypointsRequired = 0;
    if (!NormalizeCoreCount(result.count, waypointsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (waypointsRequired != 0 && result.waypoints == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, waypointsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || waypointsCapacity < waypointsRequired;
    std::uint32_t startsRequired = 0;
    if (!NormalizeCoreCount(result.startCount, startsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (startsRequired != 0 && result.starts == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 20u, startsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || startsCapacity < startsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (waypointsRequired != 0) {
        const std::size_t waypointsBytes = static_cast<std::size_t>(waypointsRequired) * 16u;
        std::span<std::uint8_t> waypointsWire;
        if (!state->memory.MutableView(waypointsPointer, waypointsBytes, waypointsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter waypointsWriter(waypointsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < waypointsRequired; ++coreIndex) {
        if (!waypointsWriter.F32(result.waypoints[coreIndex].pos.x)) return Trap("generated Core wire overflow");
        if (!waypointsWriter.F32(result.waypoints[coreIndex].pos.y)) return Trap("generated Core wire overflow");
        if (!waypointsWriter.F32(result.waypoints[coreIndex].pos.z)) return Trap("generated Core wire overflow");
        if (!waypointsWriter.Align(4u)) return Trap("generated Core record alignment overflow");
        if (!waypointsWriter.F32(result.waypoints[coreIndex].eta)) return Trap("generated Core wire overflow");
        if (!waypointsWriter.Align(4u)) return Trap("generated Core record alignment overflow");
        }
        if (!waypointsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    if (startsRequired != 0) {
        const std::size_t startsBytes = static_cast<std::size_t>(startsRequired) * 4u;
        std::span<std::uint8_t> startsWire;
        if (!state->memory.MutableView(startsPointer, startsBytes, startsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter startsWriter(startsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < startsRequired; ++coreIndex) {
        if (!startsWriter.I32(result.starts[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!startsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_input_get_active_command(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->input == nullptr ||
        state->native->input->GetActiveCommand == nullptr)
        return Trap("GetActiveCommand generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetActiveCommand generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 24u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader commandNameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t commandNamePointer = 0;
    std::uint32_t commandNameCapacity = 0;
    std::uint32_t commandNameIgnoredLength = 0;
    if (!commandNameControl.U32(commandNamePointer) || !commandNameControl.U32(commandNameCapacity) ||
        !commandNameControl.U32(commandNameIgnoredLength) || !commandNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(commandNamePointer, commandNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetActiveCommandQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetActiveCommandResult result{};
    state->native->input->GetActiveCommand(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t commandNameRequiredSize = result.commandName == nullptr ? 0u : std::char_traits<char>::length(result.commandName);
    if (commandNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t commandNameRequired = static_cast<std::uint32_t>(commandNameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, commandNameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || commandNameCapacity < commandNameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (commandNameRequired != 0 && !state->memory.Write(commandNamePointer, result.commandName, commandNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 12u);
    WireWriter writer(fixedWire);
    if (!writer.I32(result.commandIndex)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.commandID)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.commandType)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_input_get_default_command(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->input == nullptr ||
        state->native->input->GetDefaultCommand == nullptr)
        return Trap("GetDefaultCommand generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetDefaultCommand generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 24u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader commandNameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t commandNamePointer = 0;
    std::uint32_t commandNameCapacity = 0;
    std::uint32_t commandNameIgnoredLength = 0;
    if (!commandNameControl.U32(commandNamePointer) || !commandNameControl.U32(commandNameCapacity) ||
        !commandNameControl.U32(commandNameIgnoredLength) || !commandNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(commandNamePointer, commandNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetDefaultCommandQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetDefaultCommandResult result{};
    state->native->input->GetDefaultCommand(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t commandNameRequiredSize = result.commandName == nullptr ? 0u : std::char_traits<char>::length(result.commandName);
    if (commandNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t commandNameRequired = static_cast<std::uint32_t>(commandNameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, commandNameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || commandNameCapacity < commandNameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (commandNameRequired != 0 && !state->memory.Write(commandNamePointer, result.commandName, commandNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 12u);
    WireWriter writer(fixedWire);
    if (!writer.I32(result.commandIndex)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.commandID)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.commandType)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_input_get_key_symbol(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->input == nullptr ||
        state->native->input->GetKeySymbol == nullptr)
        return Trap("GetKeySymbol generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetKeySymbol generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 24u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader keyCodeNameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t keyCodeNamePointer = 0;
    std::uint32_t keyCodeNameCapacity = 0;
    std::uint32_t keyCodeNameIgnoredLength = 0;
    if (!keyCodeNameControl.U32(keyCodeNamePointer) || !keyCodeNameControl.U32(keyCodeNameCapacity) ||
        !keyCodeNameControl.U32(keyCodeNameIgnoredLength) || !keyCodeNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(keyCodeNamePointer, keyCodeNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader keyCodeDefaultNameControl(std::span<const std::uint8_t>(outputWire.data() + 12u, 12));
    std::uint32_t keyCodeDefaultNamePointer = 0;
    std::uint32_t keyCodeDefaultNameCapacity = 0;
    std::uint32_t keyCodeDefaultNameIgnoredLength = 0;
    if (!keyCodeDefaultNameControl.U32(keyCodeDefaultNamePointer) || !keyCodeDefaultNameControl.U32(keyCodeDefaultNameCapacity) ||
        !keyCodeDefaultNameControl.U32(keyCodeDefaultNameIgnoredLength) || !keyCodeDefaultNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(keyCodeDefaultNamePointer, keyCodeDefaultNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetKeySymbolQuery query{};
    query.keyCode = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.keyCode)>>>(slots[0].i32);
    GetKeySymbolResult result{};
    state->native->input->GetKeySymbol(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t keyCodeNameRequiredSize = result.keyCodeName == nullptr ? 0u : std::char_traits<char>::length(result.keyCodeName);
    if (keyCodeNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t keyCodeNameRequired = static_cast<std::uint32_t>(keyCodeNameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, keyCodeNameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || keyCodeNameCapacity < keyCodeNameRequired;
    const std::size_t keyCodeDefaultNameRequiredSize = result.keyCodeDefaultName == nullptr ? 0u : std::char_traits<char>::length(result.keyCodeDefaultName);
    if (keyCodeDefaultNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t keyCodeDefaultNameRequired = static_cast<std::uint32_t>(keyCodeDefaultNameRequiredSize);
    if (!WriteCoreU32(outputWire, 20u, keyCodeDefaultNameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || keyCodeDefaultNameCapacity < keyCodeDefaultNameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (keyCodeNameRequired != 0 && !state->memory.Write(keyCodeNamePointer, result.keyCodeName, keyCodeNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    if (keyCodeDefaultNameRequired != 0 && !state->memory.Write(keyCodeDefaultNamePointer, result.keyCodeDefaultName, keyCodeDefaultNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_input_get_mouse_cursor(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->input == nullptr ||
        state->native->input->GetMouseCursor == nullptr)
        return Trap("GetMouseCursor generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetMouseCursor generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader cursorControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t cursorPointer = 0;
    std::uint32_t cursorCapacity = 0;
    std::uint32_t cursorIgnoredLength = 0;
    if (!cursorControl.U32(cursorPointer) || !cursorControl.U32(cursorCapacity) ||
        !cursorControl.U32(cursorIgnoredLength) || !cursorControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(cursorPointer, cursorCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetMouseCursorQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetMouseCursorResult result{};
    state->native->input->GetMouseCursor(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t cursorRequiredSize = result.cursor == nullptr ? 0u : std::char_traits<char>::length(result.cursor);
    if (cursorRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t cursorRequired = static_cast<std::uint32_t>(cursorRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, cursorRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || cursorCapacity < cursorRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (cursorRequired != 0 && !state->memory.Write(cursorPointer, result.cursor, cursorRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_input_get_pressed_keys(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->input == nullptr ||
        state->native->input->GetPressedKeys == nullptr)
        return Trap("GetPressedKeys generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetPressedKeys generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader keysControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t keysPointer = 0;
    std::uint32_t keysCapacity = 0;
    std::uint32_t keysIgnoredLength = 0;
    if (!keysControl.U32(keysPointer) || !keysControl.U32(keysCapacity) ||
        !keysControl.U32(keysIgnoredLength) || !keysControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t keysCapacityBytes = static_cast<std::uint64_t>(keysCapacity) * 4u;
    if (keysCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(keysPointer, static_cast<std::size_t>(keysCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetPressedKeysQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetPressedKeysResult result{};
    state->native->input->GetPressedKeys(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t keysRequired = 0;
    if (!NormalizeCoreCount(result.count, keysRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (keysRequired != 0 && result.keys == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, keysRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || keysCapacity < keysRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (keysRequired != 0) {
        const std::size_t keysBytes = static_cast<std::size_t>(keysRequired) * 4u;
        std::span<std::uint8_t> keysWire;
        if (!state->memory.MutableView(keysPointer, keysBytes, keysWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter keysWriter(keysWire);
        for (std::uint32_t coreIndex = 0; coreIndex < keysRequired; ++coreIndex) {
        if (!keysWriter.I32(result.keys[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!keysWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_input_get_pressed_scans(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->input == nullptr ||
        state->native->input->GetPressedScans == nullptr)
        return Trap("GetPressedScans generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetPressedScans generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader scansControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t scansPointer = 0;
    std::uint32_t scansCapacity = 0;
    std::uint32_t scansIgnoredLength = 0;
    if (!scansControl.U32(scansPointer) || !scansControl.U32(scansCapacity) ||
        !scansControl.U32(scansIgnoredLength) || !scansControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t scansCapacityBytes = static_cast<std::uint64_t>(scansCapacity) * 4u;
    if (scansCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(scansPointer, static_cast<std::size_t>(scansCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetPressedScansQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetPressedScansResult result{};
    state->native->input->GetPressedScans(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t scansRequired = 0;
    if (!NormalizeCoreCount(result.count, scansRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (scansRequired != 0 && result.scans == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, scansRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || scansCapacity < scansRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (scansRequired != 0) {
        const std::size_t scansBytes = static_cast<std::size_t>(scansRequired) * 4u;
        std::span<std::uint8_t> scansWire;
        if (!state->memory.MutableView(scansPointer, scansBytes, scansWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter scansWriter(scansWire);
        for (std::uint32_t coreIndex = 0; coreIndex < scansRequired; ++coreIndex) {
        if (!scansWriter.I32(result.scans[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!scansWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_input_get_scan_symbol(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->input == nullptr ||
        state->native->input->GetScanSymbol == nullptr)
        return Trap("GetScanSymbol generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetScanSymbol generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 24u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader scanCodeNameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t scanCodeNamePointer = 0;
    std::uint32_t scanCodeNameCapacity = 0;
    std::uint32_t scanCodeNameIgnoredLength = 0;
    if (!scanCodeNameControl.U32(scanCodeNamePointer) || !scanCodeNameControl.U32(scanCodeNameCapacity) ||
        !scanCodeNameControl.U32(scanCodeNameIgnoredLength) || !scanCodeNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(scanCodeNamePointer, scanCodeNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader scanCodeDefaultNameControl(std::span<const std::uint8_t>(outputWire.data() + 12u, 12));
    std::uint32_t scanCodeDefaultNamePointer = 0;
    std::uint32_t scanCodeDefaultNameCapacity = 0;
    std::uint32_t scanCodeDefaultNameIgnoredLength = 0;
    if (!scanCodeDefaultNameControl.U32(scanCodeDefaultNamePointer) || !scanCodeDefaultNameControl.U32(scanCodeDefaultNameCapacity) ||
        !scanCodeDefaultNameControl.U32(scanCodeDefaultNameIgnoredLength) || !scanCodeDefaultNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(scanCodeDefaultNamePointer, scanCodeDefaultNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetScanSymbolQuery query{};
    query.scanCode = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.scanCode)>>>(slots[0].i32);
    GetScanSymbolResult result{};
    state->native->input->GetScanSymbol(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t scanCodeNameRequiredSize = result.scanCodeName == nullptr ? 0u : std::char_traits<char>::length(result.scanCodeName);
    if (scanCodeNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t scanCodeNameRequired = static_cast<std::uint32_t>(scanCodeNameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, scanCodeNameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || scanCodeNameCapacity < scanCodeNameRequired;
    const std::size_t scanCodeDefaultNameRequiredSize = result.scanCodeDefaultName == nullptr ? 0u : std::char_traits<char>::length(result.scanCodeDefaultName);
    if (scanCodeDefaultNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t scanCodeDefaultNameRequired = static_cast<std::uint32_t>(scanCodeDefaultNameRequiredSize);
    if (!WriteCoreU32(outputWire, 20u, scanCodeDefaultNameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || scanCodeDefaultNameCapacity < scanCodeDefaultNameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (scanCodeNameRequired != 0 && !state->memory.Write(scanCodeNamePointer, result.scanCodeName, scanCodeNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    if (scanCodeDefaultNameRequired != 0 && !state->memory.Write(scanCodeDefaultNamePointer, result.scanCodeDefaultName, scanCodeDefaultNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_display_get_map_draw_mode(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->display == nullptr ||
        state->native->display->GetMapDrawMode == nullptr)
        return Trap("GetMapDrawMode generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetMapDrawMode generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader modeControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t modePointer = 0;
    std::uint32_t modeCapacity = 0;
    std::uint32_t modeIgnoredLength = 0;
    if (!modeControl.U32(modePointer) || !modeControl.U32(modeCapacity) ||
        !modeControl.U32(modeIgnoredLength) || !modeControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(modePointer, modeCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetMapDrawModeQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetMapDrawModeResult result{};
    state->native->display->GetMapDrawMode(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t modeRequiredSize = result.mode == nullptr ? 0u : std::char_traits<char>::length(result.mode);
    if (modeRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t modeRequired = static_cast<std::uint32_t>(modeRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, modeRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || modeCapacity < modeRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (modeRequired != 0 && !state->memory.Write(modePointer, result.mode, modeRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_display_get_mini_map_dual_screen(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->display == nullptr ||
        state->native->display->GetMiniMapDualScreen == nullptr)
        return Trap("GetMiniMapDualScreen generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetMiniMapDualScreen generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader positionControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t positionPointer = 0;
    std::uint32_t positionCapacity = 0;
    std::uint32_t positionIgnoredLength = 0;
    if (!positionControl.U32(positionPointer) || !positionControl.U32(positionCapacity) ||
        !positionControl.U32(positionIgnoredLength) || !positionControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(positionPointer, positionCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetMiniMapDualScreenQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetMiniMapDualScreenResult result{};
    state->native->display->GetMiniMapDualScreen(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t positionRequiredSize = result.position == nullptr ? 0u : std::char_traits<char>::length(result.position);
    if (positionRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t positionRequired = static_cast<std::uint32_t>(positionRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, positionRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || positionCapacity < positionRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (positionRequired != 0 && !state->memory.Write(positionPointer, result.position, positionRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.dualScreen)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_display_get_water_mode(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->display == nullptr ||
        state->native->display->GetWaterMode == nullptr)
        return Trap("GetWaterMode generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetWaterMode generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader nameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t namePointer = 0;
    std::uint32_t nameCapacity = 0;
    std::uint32_t nameIgnoredLength = 0;
    if (!nameControl.U32(namePointer) || !nameControl.U32(nameCapacity) ||
        !nameControl.U32(nameIgnoredLength) || !nameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(namePointer, nameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetWaterModeQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetWaterModeResult result{};
    state->native->display->GetWaterMode(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t nameRequiredSize = result.name == nullptr ? 0u : std::char_traits<char>::length(result.name);
    if (nameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t nameRequired = static_cast<std::uint32_t>(nameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, nameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || nameCapacity < nameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (nameRequired != 0 && !state->memory.Write(namePointer, result.name, nameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.I32(result.mode)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_selection_get_group_list(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->selection == nullptr ||
        state->native->selection->GetGroupList == nullptr)
        return Trap("GetGroupList generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetGroupList generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader groupsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t groupsPointer = 0;
    std::uint32_t groupsCapacity = 0;
    std::uint32_t groupsIgnoredLength = 0;
    if (!groupsControl.U32(groupsPointer) || !groupsControl.U32(groupsCapacity) ||
        !groupsControl.U32(groupsIgnoredLength) || !groupsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t groupsCapacityBytes = static_cast<std::uint64_t>(groupsCapacity) * 4u;
    if (groupsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(groupsPointer, static_cast<std::size_t>(groupsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetGroupListQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetGroupListResult result{};
    state->native->selection->GetGroupList(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t groupsRequired = 0;
    if (!NormalizeCoreCount(result.count, groupsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (groupsRequired != 0 && result.groups == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, groupsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || groupsCapacity < groupsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (groupsRequired != 0) {
        const std::size_t groupsBytes = static_cast<std::size_t>(groupsRequired) * 4u;
        std::span<std::uint8_t> groupsWire;
        if (!state->memory.MutableView(groupsPointer, groupsBytes, groupsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter groupsWriter(groupsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < groupsRequired; ++coreIndex) {
        if (!groupsWriter.I32(result.groups[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!groupsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_selection_get_group_units(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->selection == nullptr ||
        state->native->selection->GetGroupUnits == nullptr)
        return Trap("GetGroupUnits generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetGroupUnits generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader unitsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t unitsPointer = 0;
    std::uint32_t unitsCapacity = 0;
    std::uint32_t unitsIgnoredLength = 0;
    if (!unitsControl.U32(unitsPointer) || !unitsControl.U32(unitsCapacity) ||
        !unitsControl.U32(unitsIgnoredLength) || !unitsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t unitsCapacityBytes = static_cast<std::uint64_t>(unitsCapacity) * 4u;
    if (unitsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(unitsPointer, static_cast<std::size_t>(unitsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetGroupUnitsQuery query{};
    query.groupID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.groupID)>>>(slots[0].i32);
    GetGroupUnitsResult result{};
    state->native->selection->GetGroupUnits(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t unitsRequired = 0;
    if (!NormalizeCoreCount(result.count, unitsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (unitsRequired != 0 && result.units == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, unitsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || unitsCapacity < unitsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (unitsRequired != 0) {
        const std::size_t unitsBytes = static_cast<std::size_t>(unitsRequired) * 4u;
        std::span<std::uint8_t> unitsWire;
        if (!state->memory.MutableView(unitsPointer, unitsBytes, unitsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter unitsWriter(unitsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < unitsRequired; ++coreIndex) {
        if (!unitsWriter.I32(result.units[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!unitsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_selection_get_selected_units(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->selection == nullptr ||
        state->native->selection->GetSelectedUnits == nullptr)
        return Trap("GetSelectedUnits generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetSelectedUnits generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader unitsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t unitsPointer = 0;
    std::uint32_t unitsCapacity = 0;
    std::uint32_t unitsIgnoredLength = 0;
    if (!unitsControl.U32(unitsPointer) || !unitsControl.U32(unitsCapacity) ||
        !unitsControl.U32(unitsIgnoredLength) || !unitsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t unitsCapacityBytes = static_cast<std::uint64_t>(unitsCapacity) * 4u;
    if (unitsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(unitsPointer, static_cast<std::size_t>(unitsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetSelectedUnitsQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetSelectedUnitsResult result{};
    state->native->selection->GetSelectedUnits(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t unitsRequired = 0;
    if (!NormalizeCoreCount(result.count, unitsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (unitsRequired != 0 && result.units == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, unitsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || unitsCapacity < unitsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (unitsRequired != 0) {
        const std::size_t unitsBytes = static_cast<std::size_t>(unitsRequired) * 4u;
        std::span<std::uint8_t> unitsWire;
        if (!state->memory.MutableView(unitsPointer, unitsBytes, unitsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter unitsWriter(unitsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < unitsRequired; ++coreIndex) {
        if (!unitsWriter.I32(result.units[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!unitsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_selection_get_selected_units_sorted(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->selection == nullptr ||
        state->native->selection->GetSelectedUnitsSorted == nullptr)
        return Trap("GetSelectedUnitsSorted generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetSelectedUnitsSorted generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader unitsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t unitsPointer = 0;
    std::uint32_t unitsCapacity = 0;
    std::uint32_t unitsIgnoredLength = 0;
    if (!unitsControl.U32(unitsPointer) || !unitsControl.U32(unitsCapacity) ||
        !unitsControl.U32(unitsIgnoredLength) || !unitsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t unitsCapacityBytes = static_cast<std::uint64_t>(unitsCapacity) * 4u;
    if (unitsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(unitsPointer, static_cast<std::size_t>(unitsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetSelectedUnitsSortedQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetSelectedUnitsSortedResult result{};
    state->native->selection->GetSelectedUnitsSorted(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t unitsRequired = 0;
    if (!NormalizeCoreCount(result.count, unitsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (unitsRequired != 0 && result.units == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, unitsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || unitsCapacity < unitsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (unitsRequired != 0) {
        const std::size_t unitsBytes = static_cast<std::size_t>(unitsRequired) * 4u;
        std::span<std::uint8_t> unitsWire;
        if (!state->memory.MutableView(unitsPointer, unitsBytes, unitsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter unitsWriter(unitsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < unitsRequired; ++coreIndex) {
        if (!unitsWriter.I32(result.units[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!unitsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_messages_get_current_tooltip(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->messages == nullptr ||
        state->native->messages->GetCurrentTooltip == nullptr)
        return Trap("GetCurrentTooltip generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetCurrentTooltip generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader tooltipControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t tooltipPointer = 0;
    std::uint32_t tooltipCapacity = 0;
    std::uint32_t tooltipIgnoredLength = 0;
    if (!tooltipControl.U32(tooltipPointer) || !tooltipControl.U32(tooltipCapacity) ||
        !tooltipControl.U32(tooltipIgnoredLength) || !tooltipControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(tooltipPointer, tooltipCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetCurrentTooltipQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetCurrentTooltipResult result{};
    state->native->messages->GetCurrentTooltip(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t tooltipRequiredSize = result.tooltip == nullptr ? 0u : std::char_traits<char>::length(result.tooltip);
    if (tooltipRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t tooltipRequired = static_cast<std::uint32_t>(tooltipRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, tooltipRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || tooltipCapacity < tooltipRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (tooltipRequired != 0 && !state->memory.Write(tooltipPointer, result.tooltip, tooltipRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_gfx_download_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->DownloadVBO == nullptr)
        return Trap("DownloadVBO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 6)
        return Trap("DownloadVBO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 7u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[5].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valuesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCapacity = 0;
    std::uint32_t valuesIgnoredLength = 0;
    if (!valuesControl.U32(valuesPointer) || !valuesControl.U32(valuesCapacity) ||
        !valuesControl.U32(valuesIgnoredLength) || !valuesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesCapacityBytes = static_cast<std::uint64_t>(valuesCapacity) * 4u;
    if (valuesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(valuesPointer, static_cast<std::size_t>(valuesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GfxVBODownloadQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[2].i32);
    query.elementCount = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementCount)>>>(slots[3].i32);
    query.forceGPURead = slots[4].i32 != 0;
    GfxVBODownloadResult result{};
    state->native->gfx->DownloadVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t valuesRequired = 0;
    if (!NormalizeCoreCount(result.count, valuesRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (valuesRequired != 0 && result.values == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, valuesRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valuesCapacity < valuesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valuesRequired != 0) {
        const std::size_t valuesBytes = static_cast<std::size_t>(valuesRequired) * 4u;
        std::span<std::uint8_t> valuesWire;
        if (!state->memory.MutableView(valuesPointer, valuesBytes, valuesWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter valuesWriter(valuesWire);
        for (std::uint32_t coreIndex = 0; coreIndex < valuesRequired; ++coreIndex) {
        if (!valuesWriter.F32(result.values[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!valuesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_gfx_get_engine_model_uniform_data_def(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetEngineModelUniformDataDef == nullptr)
        return Trap("GetEngineModelUniformDataDef generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetEngineModelUniformDataDef generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GfxEmptyQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GfxStringResult result{};
    state->native->gfx->GetEngineModelUniformDataDef(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_gfx_get_engine_uniform_buffer_def(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetEngineUniformBufferDef == nullptr)
        return Trap("GetEngineUniformBufferDef generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetEngineUniformBufferDef generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GfxEngineUniformBufferDefQuery query{};
    query.index = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.index)>>>(slots[0].i32);
    GfxStringResult result{};
    state->native->gfx->GetEngineUniformBufferDef(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_gfx_get_font_info(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetFontInfo == nullptr)
        return Trap("GetFontInfo generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetFontInfo generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 64u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader pathControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCapacity = 0;
    std::uint32_t pathIgnoredLength = 0;
    if (!pathControl.U32(pathPointer) || !pathControl.U32(pathCapacity) ||
        !pathControl.U32(pathIgnoredLength) || !pathControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(pathPointer, pathCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader familyControl(std::span<const std::uint8_t>(outputWire.data() + 12u, 12));
    std::uint32_t familyPointer = 0;
    std::uint32_t familyCapacity = 0;
    std::uint32_t familyIgnoredLength = 0;
    if (!familyControl.U32(familyPointer) || !familyControl.U32(familyCapacity) ||
        !familyControl.U32(familyIgnoredLength) || !familyControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(familyPointer, familyCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader styleControl(std::span<const std::uint8_t>(outputWire.data() + 24u, 12));
    std::uint32_t stylePointer = 0;
    std::uint32_t styleCapacity = 0;
    std::uint32_t styleIgnoredLength = 0;
    if (!styleControl.U32(stylePointer) || !styleControl.U32(styleCapacity) ||
        !styleControl.U32(styleIgnoredLength) || !styleControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(stylePointer, styleCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GfxFontQuery query{};
    query.fontID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fontID)>>>(slots[0].i32);
    GfxFontInfoResult result{};
    state->native->gfx->GetFontInfo(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t pathRequiredSize = result.path == nullptr ? 0u : std::char_traits<char>::length(result.path);
    if (pathRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t pathRequired = static_cast<std::uint32_t>(pathRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, pathRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || pathCapacity < pathRequired;
    const std::size_t familyRequiredSize = result.family == nullptr ? 0u : std::char_traits<char>::length(result.family);
    if (familyRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t familyRequired = static_cast<std::uint32_t>(familyRequiredSize);
    if (!WriteCoreU32(outputWire, 20u, familyRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || familyCapacity < familyRequired;
    const std::size_t styleRequiredSize = result.style == nullptr ? 0u : std::char_traits<char>::length(result.style);
    if (styleRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t styleRequired = static_cast<std::uint32_t>(styleRequiredSize);
    if (!WriteCoreU32(outputWire, 32u, styleRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || styleCapacity < styleRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (pathRequired != 0 && !state->memory.Write(pathPointer, result.path, pathRequired)) return Trap("generated Core variable output range changed unexpectedly");
    if (familyRequired != 0 && !state->memory.Write(familyPointer, result.family, familyRequired)) return Trap("generated Core variable output range changed unexpectedly");
    if (styleRequired != 0 && !state->memory.Write(stylePointer, result.style, styleRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(36u, 28u);
    WireWriter writer(fixedWire);
    if (!writer.F32(result.size)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.lineHeight)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.descender)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.outlineWidth)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.outlineWeight)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.textureWidth)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.textureHeight)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_gfx_get_shader_log(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetShaderLog == nullptr)
        return Trap("GetShaderLog generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetShaderLog generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GfxEmptyQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GfxStringResult result{};
    state->native->gfx->GetShaderLog(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_gfx_get_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetString == nullptr)
        return Trap("GetString generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetString generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GfxGetStringQuery query{};
    query.pname = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.pname)>>>(slots[0].i32);
    GfxStringResult result{};
    state->native->gfx->GetString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_gfx_read_pixels(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->ReadPixels == nullptr)
        return Trap("ReadPixels generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 6)
        return Trap("ReadPixels generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 7u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[5].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valuesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCapacity = 0;
    std::uint32_t valuesIgnoredLength = 0;
    if (!valuesControl.U32(valuesPointer) || !valuesControl.U32(valuesCapacity) ||
        !valuesControl.U32(valuesIgnoredLength) || !valuesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesCapacityBytes = static_cast<std::uint64_t>(valuesCapacity) * 4u;
    if (valuesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(valuesPointer, static_cast<std::size_t>(valuesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GfxReadPixelsQuery query{};
    query.x = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.x)>>>(slots[0].i32);
    query.y = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.y)>>>(slots[1].i32);
    query.width = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.width)>>>(slots[2].i32);
    query.height = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.height)>>>(slots[3].i32);
    query.format = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.format)>>>(slots[4].i32);
    GfxReadPixelsResult result{};
    state->native->gfx->ReadPixels(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t valuesRequired = 0;
    if (!NormalizeCoreCount(result.count, valuesRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (valuesRequired != 0 && result.values == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, valuesRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valuesCapacity < valuesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valuesRequired != 0) {
        const std::size_t valuesBytes = static_cast<std::size_t>(valuesRequired) * 4u;
        std::span<std::uint8_t> valuesWire;
        if (!state->memory.MutableView(valuesPointer, valuesBytes, valuesWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter valuesWriter(valuesWire);
        for (std::uint32_t coreIndex = 0; coreIndex < valuesRequired; ++coreIndex) {
        if (!valuesWriter.F32(result.values[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!valuesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.U32(result.components)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_ground_decals_get_all_ground_decals(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->groundDecals == nullptr ||
        state->native->groundDecals->GetAllGroundDecals == nullptr)
        return Trap("GetAllGroundDecals generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetAllGroundDecals generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader decalIDsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t decalIDsPointer = 0;
    std::uint32_t decalIDsCapacity = 0;
    std::uint32_t decalIDsIgnoredLength = 0;
    if (!decalIDsControl.U32(decalIDsPointer) || !decalIDsControl.U32(decalIDsCapacity) ||
        !decalIDsControl.U32(decalIDsIgnoredLength) || !decalIDsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t decalIDsCapacityBytes = static_cast<std::uint64_t>(decalIDsCapacity) * 4u;
    if (decalIDsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(decalIDsPointer, static_cast<std::size_t>(decalIDsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetAllGroundDecalsQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetAllGroundDecalsResult result{};
    state->native->groundDecals->GetAllGroundDecals(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t decalIDsRequired = 0;
    if (!NormalizeCoreCount(result.count, decalIDsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (decalIDsRequired != 0 && result.decalIDs == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, decalIDsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || decalIDsCapacity < decalIDsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (decalIDsRequired != 0) {
        const std::size_t decalIDsBytes = static_cast<std::size_t>(decalIDsRequired) * 4u;
        std::span<std::uint8_t> decalIDsWire;
        if (!state->memory.MutableView(decalIDsPointer, decalIDsBytes, decalIDsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter decalIDsWriter(decalIDsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < decalIDsRequired; ++coreIndex) {
        if (!decalIDsWriter.U32(result.decalIDs[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!decalIDsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_ground_decals_get_ground_decal_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->groundDecals == nullptr ||
        state->native->groundDecals->GetGroundDecalTexture == nullptr)
        return Trap("GetGroundDecalTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetGroundDecalTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader textureControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t texturePointer = 0;
    std::uint32_t textureCapacity = 0;
    std::uint32_t textureIgnoredLength = 0;
    if (!textureControl.U32(texturePointer) || !textureControl.U32(textureCapacity) ||
        !textureControl.U32(textureIgnoredLength) || !textureControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(texturePointer, textureCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetGroundDecalTextureQuery query{};
    query.decalID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.decalID)>>>(slots[0].i32);
    query.mainTex = slots[1].i32 != 0;
    GetGroundDecalTextureResult result{};
    state->native->groundDecals->GetGroundDecalTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t textureRequiredSize = result.texture == nullptr ? 0u : std::char_traits<char>::length(result.texture);
    if (textureRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t textureRequired = static_cast<std::uint32_t>(textureRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, textureRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || textureCapacity < textureRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (textureRequired != 0 && !state->memory.Write(texturePointer, result.texture, textureRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_ground_decals_get_ground_decal_type(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->groundDecals == nullptr ||
        state->native->groundDecals->GetGroundDecalType == nullptr)
        return Trap("GetGroundDecalType generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetGroundDecalType generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader typeControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t typePointer = 0;
    std::uint32_t typeCapacity = 0;
    std::uint32_t typeIgnoredLength = 0;
    if (!typeControl.U32(typePointer) || !typeControl.U32(typeCapacity) ||
        !typeControl.U32(typeIgnoredLength) || !typeControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(typePointer, typeCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetGroundDecalTypeQuery query{};
    query.decalID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.decalID)>>>(slots[0].i32);
    GetGroundDecalTypeResult result{};
    state->native->groundDecals->GetGroundDecalType(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t typeRequiredSize = result.type == nullptr ? 0u : std::char_traits<char>::length(result.type);
    if (typeRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t typeRequired = static_cast<std::uint32_t>(typeRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, typeRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || typeCapacity < typeRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (typeRequired != 0 && !state->memory.Write(typePointer, result.type, typeRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_system_control_get_game_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->GetGameName == nullptr)
        return Trap("GetGameName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetGameName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader nameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t namePointer = 0;
    std::uint32_t nameCapacity = 0;
    std::uint32_t nameIgnoredLength = 0;
    if (!nameControl.U32(namePointer) || !nameControl.U32(nameCapacity) ||
        !nameControl.U32(nameIgnoredLength) || !nameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(namePointer, nameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetGameNameQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetGameNameResult result{};
    state->native->systemControl->GetGameName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t nameRequiredSize = result.name == nullptr ? 0u : std::char_traits<char>::length(result.name);
    if (nameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t nameRequired = static_cast<std::uint32_t>(nameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, nameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || nameCapacity < nameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (nameRequired != 0 && !state->memory.Write(namePointer, result.name, nameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_system_control_get_menu_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->GetMenuName == nullptr)
        return Trap("GetMenuName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetMenuName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader nameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t namePointer = 0;
    std::uint32_t nameCapacity = 0;
    std::uint32_t nameIgnoredLength = 0;
    if (!nameControl.U32(namePointer) || !nameControl.U32(nameCapacity) ||
        !nameControl.U32(nameIgnoredLength) || !nameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(namePointer, nameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetMenuNameQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetMenuNameResult result{};
    state->native->systemControl->GetMenuName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t nameRequiredSize = result.name == nullptr ? 0u : std::char_traits<char>::length(result.name);
    if (nameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t nameRequired = static_cast<std::uint32_t>(nameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, nameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || nameCapacity < nameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (nameRequired != 0 && !state->memory.Write(namePointer, result.name, nameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_system_control_get_replay_file_path(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->GetReplayFilePath == nullptr)
        return Trap("GetReplayFilePath generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetReplayFilePath generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader pathControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCapacity = 0;
    std::uint32_t pathIgnoredLength = 0;
    if (!pathControl.U32(pathPointer) || !pathControl.U32(pathCapacity) ||
        !pathControl.U32(pathIgnoredLength) || !pathControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(pathPointer, pathCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetReplayFilePathQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetReplayFilePathResult result{};
    state->native->systemControl->GetReplayFilePath(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t pathRequiredSize = result.path == nullptr ? 0u : std::char_traits<char>::length(result.path);
    if (pathRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t pathRequired = static_cast<std::uint32_t>(pathRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, pathRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || pathCapacity < pathRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (pathRequired != 0 && !state->memory.Write(pathPointer, result.path, pathRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_system_control_get_replay_recording_file_path(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->GetReplayRecordingFilePath == nullptr)
        return Trap("GetReplayRecordingFilePath generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetReplayRecordingFilePath generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader pathControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCapacity = 0;
    std::uint32_t pathIgnoredLength = 0;
    if (!pathControl.U32(pathPointer) || !pathControl.U32(pathCapacity) ||
        !pathControl.U32(pathIgnoredLength) || !pathControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(pathPointer, pathCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetReplayRecordingFilePathQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetReplayRecordingFilePathResult result{};
    state->native->systemControl->GetReplayRecordingFilePath(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t pathRequiredSize = result.path == nullptr ? 0u : std::char_traits<char>::length(result.path);
    if (pathRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t pathRequired = static_cast<std::uint32_t>(pathRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, pathRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || pathCapacity < pathRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (pathRequired != 0 && !state->memory.Write(pathPointer, result.path, pathRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_system_control_get_window_display_mode(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->GetWindowDisplayMode == nullptr)
        return Trap("GetWindowDisplayMode generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetWindowDisplayMode generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 32u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader formatNameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t formatNamePointer = 0;
    std::uint32_t formatNameCapacity = 0;
    std::uint32_t formatNameIgnoredLength = 0;
    if (!formatNameControl.U32(formatNamePointer) || !formatNameControl.U32(formatNameCapacity) ||
        !formatNameControl.U32(formatNameIgnoredLength) || !formatNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(formatNamePointer, formatNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetWindowDisplayModeQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetWindowDisplayModeResult result{};
    state->native->systemControl->GetWindowDisplayMode(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t formatNameRequiredSize = result.formatName == nullptr ? 0u : std::char_traits<char>::length(result.formatName);
    if (formatNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t formatNameRequired = static_cast<std::uint32_t>(formatNameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, formatNameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || formatNameCapacity < formatNameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (formatNameRequired != 0 && !state->memory.Write(formatNamePointer, result.formatName, formatNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 20u);
    WireWriter writer(fixedWire);
    if (!writer.I32(result.width)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.height)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.bpp)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.refresh)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_context_get_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextGetName == nullptr)
        return Trap("ContextGetName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ContextGetName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader nameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t namePointer = 0;
    std::uint32_t nameCapacity = 0;
    std::uint32_t nameIgnoredLength = 0;
    if (!nameControl.U32(namePointer) || !nameControl.U32(nameCapacity) ||
        !nameControl.U32(nameIgnoredLength) || !nameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(namePointer, nameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlContextHandleQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    RmlContextGetNameResult result{};
    state->native->rmlUi->ContextGetName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t nameRequiredSize = result.name == nullptr ? 0u : std::char_traits<char>::length(result.name);
    if (nameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t nameRequired = static_cast<std::uint32_t>(nameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, nameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || nameCapacity < nameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (nameRequired != 0 && !state->memory.Write(namePointer, result.name, nameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_data_model_get_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelGetString == nullptr)
        return Trap("DataModelGetString generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("DataModelGetString generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlDataModelVariableHandleQuery query{};
    query.variableHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.variableHandle)>>>(slots[0].i64);
    RmlDataModelGetStringResult result{};
    state->native->rmlUi->DataModelGetString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_document_get_title(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentGetTitle == nullptr)
        return Trap("DocumentGetTitle generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("DocumentGetTitle generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlDocumentHandleQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    RmlDocumentGetStringResult result{};
    state->native->rmlUi->DocumentGetTitle(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_document_get_url(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentGetUrl == nullptr)
        return Trap("DocumentGetUrl generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("DocumentGetUrl generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlDocumentHandleQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    RmlDocumentGetStringResult result{};
    state->native->rmlUi->DocumentGetUrl(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_element_form_control_input_get_selection(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementFormControlInputGetSelection == nullptr)
        return Trap("ElementFormControlInputGetSelection generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementFormControlInputGetSelection generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 24u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader textControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t textPointer = 0;
    std::uint32_t textCapacity = 0;
    std::uint32_t textIgnoredLength = 0;
    if (!textControl.U32(textPointer) || !textControl.U32(textCapacity) ||
        !textControl.U32(textIgnoredLength) || !textControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(textPointer, textCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlElementHandleQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    RmlElementFormControlSelectionResult result{};
    state->native->rmlUi->ElementFormControlInputGetSelection(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t textRequiredSize = result.text == nullptr ? 0u : std::char_traits<char>::length(result.text);
    if (textRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t textRequired = static_cast<std::uint32_t>(textRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, textRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || textCapacity < textRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (textRequired != 0 && !state->memory.Write(textPointer, result.text, textRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 12u);
    WireWriter writer(fixedWire);
    if (!writer.I32(result.start)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.end)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_element_form_control_text_area_get_selection(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementFormControlTextAreaGetSelection == nullptr)
        return Trap("ElementFormControlTextAreaGetSelection generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementFormControlTextAreaGetSelection generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 24u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader textControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t textPointer = 0;
    std::uint32_t textCapacity = 0;
    std::uint32_t textIgnoredLength = 0;
    if (!textControl.U32(textPointer) || !textControl.U32(textCapacity) ||
        !textControl.U32(textIgnoredLength) || !textControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(textPointer, textCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlElementHandleQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    RmlElementFormControlSelectionResult result{};
    state->native->rmlUi->ElementFormControlTextAreaGetSelection(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t textRequiredSize = result.text == nullptr ? 0u : std::char_traits<char>::length(result.text);
    if (textRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t textRequired = static_cast<std::uint32_t>(textRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, textRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || textCapacity < textRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (textRequired != 0 && !state->memory.Write(textPointer, result.text, textRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 12u);
    WireWriter writer(fixedWire);
    if (!writer.I32(result.start)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.end)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_element_get_class_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetClassName == nullptr)
        return Trap("ElementGetClassName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementGetClassName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlElementHandleQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    RmlElementGetStringResult result{};
    state->native->rmlUi->ElementGetClassName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_element_get_id(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetId == nullptr)
        return Trap("ElementGetId generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementGetId generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlElementHandleQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    RmlElementGetStringResult result{};
    state->native->rmlUi->ElementGetId(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_element_get_inner_rml(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetInnerRml == nullptr)
        return Trap("ElementGetInnerRml generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementGetInnerRml generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlElementHandleQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    RmlElementGetStringResult result{};
    state->native->rmlUi->ElementGetInnerRml(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_element_get_tag_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetTagName == nullptr)
        return Trap("ElementGetTagName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementGetTagName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlElementHandleQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    RmlElementGetStringResult result{};
    state->native->rmlUi->ElementGetTagName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_element_get_value(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetValue == nullptr)
        return Trap("ElementGetValue generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementGetValue generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlElementHandleQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    RmlElementGetStringResult result{};
    state->native->rmlUi->ElementGetValue(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_event_get_type(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->EventGetType == nullptr)
        return Trap("EventGetType generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("EventGetType generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlEventHandleQuery query{};
    query.eventHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventHandle)>>>(slots[0].i64);
    RmlEventGetStringResult result{};
    state->native->rmlUi->EventGetType(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_rml_ui_get_version(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->GetVersion == nullptr)
        return Trap("GetVersion generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetVersion generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader versionControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t versionPointer = 0;
    std::uint32_t versionCapacity = 0;
    std::uint32_t versionIgnoredLength = 0;
    if (!versionControl.U32(versionPointer) || !versionControl.U32(versionCapacity) ||
        !versionControl.U32(versionIgnoredLength) || !versionControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(versionPointer, versionCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlGetVersionQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    RmlGetVersionResult result{};
    state->native->rmlUi->GetVersion(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t versionRequiredSize = result.version == nullptr ? 0u : std::char_traits<char>::length(result.version);
    if (versionRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t versionRequired = static_cast<std::uint32_t>(versionRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, versionRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || versionCapacity < versionRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (versionRequired != 0 && !state->memory.Write(versionPointer, result.version, versionRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_unsynced_read_get_clipboard(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedRead == nullptr ||
        state->native->unsyncedRead->GetClipboard == nullptr)
        return Trap("GetClipboard generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetClipboard generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader textControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t textPointer = 0;
    std::uint32_t textCapacity = 0;
    std::uint32_t textIgnoredLength = 0;
    if (!textControl.U32(textPointer) || !textControl.U32(textCapacity) ||
        !textControl.U32(textIgnoredLength) || !textControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(textPointer, textCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetClipboardQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetClipboardResult result{};
    state->native->unsyncedRead->GetClipboard(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t textRequiredSize = result.text == nullptr ? 0u : std::char_traits<char>::length(result.text);
    if (textRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t textRequired = static_cast<std::uint32_t>(textRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, textRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || textCapacity < textRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (textRequired != 0 && !state->memory.Write(textPointer, result.text, textRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_unsynced_read_get_last_message_positions(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedRead == nullptr ||
        state->native->unsyncedRead->GetLastMessagePositions == nullptr)
        return Trap("GetLastMessagePositions generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetLastMessagePositions generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader positionsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t positionsPointer = 0;
    std::uint32_t positionsCapacity = 0;
    std::uint32_t positionsIgnoredLength = 0;
    if (!positionsControl.U32(positionsPointer) || !positionsControl.U32(positionsCapacity) ||
        !positionsControl.U32(positionsIgnoredLength) || !positionsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t positionsCapacityBytes = static_cast<std::uint64_t>(positionsCapacity) * 12u;
    if (positionsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(positionsPointer, static_cast<std::size_t>(positionsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetLastMessagePositionsQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetLastMessagePositionsResult result{};
    state->native->unsyncedRead->GetLastMessagePositions(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t positionsRequired = 0;
    if (!NormalizeCoreCount(result.count, positionsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (positionsRequired != 0 && result.positions == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, positionsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || positionsCapacity < positionsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (positionsRequired != 0) {
        const std::size_t positionsBytes = static_cast<std::size_t>(positionsRequired) * 12u;
        std::span<std::uint8_t> positionsWire;
        if (!state->memory.MutableView(positionsPointer, positionsBytes, positionsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter positionsWriter(positionsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < positionsRequired; ++coreIndex) {
        if (!positionsWriter.F32(result.positions[coreIndex].x)) return Trap("generated Core wire overflow");
        if (!positionsWriter.F32(result.positions[coreIndex].y)) return Trap("generated Core wire overflow");
        if (!positionsWriter.F32(result.positions[coreIndex].z)) return Trap("generated Core wire overflow");
        if (!positionsWriter.Align(4u)) return Trap("generated Core record alignment overflow");
        }
        if (!positionsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_unsynced_read_get_piece_projectile_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedRead == nullptr ||
        state->native->unsyncedRead->GetPieceProjectileName == nullptr)
        return Trap("GetPieceProjectileName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetPieceProjectileName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader nameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t namePointer = 0;
    std::uint32_t nameCapacity = 0;
    std::uint32_t nameIgnoredLength = 0;
    if (!nameControl.U32(namePointer) || !nameControl.U32(nameCapacity) ||
        !nameControl.U32(nameIgnoredLength) || !nameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(namePointer, nameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetPieceProjectileNameQuery query{};
    query.projectileID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.projectileID)>>>(slots[0].i32);
    GetPieceProjectileNameResult result{};
    state->native->unsyncedRead->GetPieceProjectileName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t nameRequiredSize = result.name == nullptr ? 0u : std::char_traits<char>::length(result.name);
    if (nameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t nameRequired = static_cast<std::uint32_t>(nameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, nameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || nameCapacity < nameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (nameRequired != 0 && !state->memory.Write(namePointer, result.name, nameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_unsynced_read_get_prev_frame_sync_checksum(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedRead == nullptr ||
        state->native->unsyncedRead->GetPrevFrameSyncChecksum == nullptr)
        return Trap("GetPrevFrameSyncChecksum generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetPrevFrameSyncChecksum generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader checksumControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t checksumPointer = 0;
    std::uint32_t checksumCapacity = 0;
    std::uint32_t checksumIgnoredLength = 0;
    if (!checksumControl.U32(checksumPointer) || !checksumControl.U32(checksumCapacity) ||
        !checksumControl.U32(checksumIgnoredLength) || !checksumControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(checksumPointer, checksumCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetPrevFrameSyncChecksumQuery query{};
    query._unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query._unused)>>>(slots[0].i32);
    GetPrevFrameSyncChecksumResult result{};
    state->native->unsyncedRead->GetPrevFrameSyncChecksum(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t checksumRequiredSize = result.checksum == nullptr ? 0u : std::char_traits<char>::length(result.checksum);
    if (checksumRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t checksumRequired = static_cast<std::uint32_t>(checksumRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, checksumRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || checksumCapacity < checksumRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (checksumRequired != 0 && !state->memory.Write(checksumPointer, result.checksum, checksumRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_unit_rendering_get_features_in_screen_rectangle(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedRead == nullptr || state->native->unsyncedRead->unitRendering == nullptr ||
        state->native->unsyncedRead->unitRendering->GetFeaturesInScreenRectangle == nullptr)
        return Trap("GetFeaturesInScreenRectangle generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 5)
        return Trap("GetFeaturesInScreenRectangle generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader featureIDsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t featureIDsPointer = 0;
    std::uint32_t featureIDsCapacity = 0;
    std::uint32_t featureIDsIgnoredLength = 0;
    if (!featureIDsControl.U32(featureIDsPointer) || !featureIDsControl.U32(featureIDsCapacity) ||
        !featureIDsControl.U32(featureIDsIgnoredLength) || !featureIDsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t featureIDsCapacityBytes = static_cast<std::uint64_t>(featureIDsCapacity) * 4u;
    if (featureIDsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(featureIDsPointer, static_cast<std::size_t>(featureIDsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetFeaturesInScreenRectangleQuery query{};
    query.left = slots[0].f32;
    query.top = slots[1].f32;
    query.right = slots[2].f32;
    query.bottom = slots[3].f32;
    GetFeaturesInScreenRectangleResult result{};
    state->native->unsyncedRead->unitRendering->GetFeaturesInScreenRectangle(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t featureIDsRequired = 0;
    if (!NormalizeCoreCount(result.count, featureIDsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (featureIDsRequired != 0 && result.featureIDs == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, featureIDsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || featureIDsCapacity < featureIDsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (featureIDsRequired != 0) {
        const std::size_t featureIDsBytes = static_cast<std::size_t>(featureIDsRequired) * 4u;
        std::span<std::uint8_t> featureIDsWire;
        if (!state->memory.MutableView(featureIDsPointer, featureIDsBytes, featureIDsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter featureIDsWriter(featureIDsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < featureIDsRequired; ++coreIndex) {
        if (!featureIDsWriter.I32(result.featureIDs[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!featureIDsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_unit_rendering_get_unit_icon(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedRead == nullptr || state->native->unsyncedRead->unitRendering == nullptr ||
        state->native->unsyncedRead->unitRendering->GetUnitIcon == nullptr)
        return Trap("GetUnitIcon generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetUnitIcon generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 40u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader iconNameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t iconNamePointer = 0;
    std::uint32_t iconNameCapacity = 0;
    std::uint32_t iconNameIgnoredLength = 0;
    if (!iconNameControl.U32(iconNamePointer) || !iconNameControl.U32(iconNameCapacity) ||
        !iconNameControl.U32(iconNameIgnoredLength) || !iconNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(iconNamePointer, iconNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetUnitIconQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    GetUnitIconResult result{};
    state->native->unsyncedRead->unitRendering->GetUnitIcon(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t iconNameRequiredSize = result.iconName == nullptr ? 0u : std::char_traits<char>::length(result.iconName);
    if (iconNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t iconNameRequired = static_cast<std::uint32_t>(iconNameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, iconNameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || iconNameCapacity < iconNameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (iconNameRequired != 0 && !state->memory.Write(iconNamePointer, result.iconName, iconNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 28u);
    WireWriter writer(fixedWire);
    for (std::size_t coreWriteIndex1 = 0; coreWriteIndex1 < 4u; ++coreWriteIndex1) {
        if (!writer.F32(result.atlasTexCoords[coreWriteIndex1])) return Trap("generated Core wire overflow");
    }
    if (!writer.F32(result.size)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.distance)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.radiusAdjust)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_unit_rendering_get_unit_icon_data(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedRead == nullptr || state->native->unsyncedRead->unitRendering == nullptr ||
        state->native->unsyncedRead->unitRendering->GetUnitIconData == nullptr)
        return Trap("GetUnitIconData generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetUnitIconData generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 40u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader iconNameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t iconNamePointer = 0;
    std::uint32_t iconNameCapacity = 0;
    std::uint32_t iconNameIgnoredLength = 0;
    if (!iconNameControl.U32(iconNamePointer) || !iconNameControl.U32(iconNameCapacity) ||
        !iconNameControl.U32(iconNameIgnoredLength) || !iconNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(iconNamePointer, iconNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetUnitIconDataQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    query.fullData = slots[1].i32 != 0;
    GetUnitIconDataResult result{};
    state->native->unsyncedRead->unitRendering->GetUnitIconData(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t iconNameRequiredSize = result.iconName == nullptr ? 0u : std::char_traits<char>::length(result.iconName);
    if (iconNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t iconNameRequired = static_cast<std::uint32_t>(iconNameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, iconNameRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || iconNameCapacity < iconNameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (iconNameRequired != 0 && !state->memory.Write(iconNamePointer, result.iconName, iconNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 28u);
    WireWriter writer(fixedWire);
    for (std::size_t coreWriteIndex1 = 0; coreWriteIndex1 < 4u; ++coreWriteIndex1) {
        if (!writer.F32(result.atlasTexCoords[coreWriteIndex1])) return Trap("generated Core wire overflow");
    }
    if (!writer.F32(result.size)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.distance)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.radiusAdjust)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_unit_rendering_get_units_in_screen_rectangle(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedRead == nullptr || state->native->unsyncedRead->unitRendering == nullptr ||
        state->native->unsyncedRead->unitRendering->GetUnitsInScreenRectangle == nullptr)
        return Trap("GetUnitsInScreenRectangle generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 6)
        return Trap("GetUnitsInScreenRectangle generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 7u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[5].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader unitIDsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCapacity = 0;
    std::uint32_t unitIDsIgnoredLength = 0;
    if (!unitIDsControl.U32(unitIDsPointer) || !unitIDsControl.U32(unitIDsCapacity) ||
        !unitIDsControl.U32(unitIDsIgnoredLength) || !unitIDsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t unitIDsCapacityBytes = static_cast<std::uint64_t>(unitIDsCapacity) * 4u;
    if (unitIDsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(unitIDsPointer, static_cast<std::size_t>(unitIDsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetUnitsInScreenRectangleQuery query{};
    query.left = slots[0].f32;
    query.top = slots[1].f32;
    query.right = slots[2].f32;
    query.bottom = slots[3].f32;
    query.allegiance = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.allegiance)>>>(slots[4].i32);
    GetUnitsInScreenRectangleResult result{};
    state->native->unsyncedRead->unitRendering->GetUnitsInScreenRectangle(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t unitIDsRequired = 0;
    if (!NormalizeCoreCount(result.count, unitIDsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (unitIDsRequired != 0 && result.unitIDs == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, unitIDsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || unitIDsCapacity < unitIDsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (unitIDsRequired != 0) {
        const std::size_t unitIDsBytes = static_cast<std::size_t>(unitIDsRequired) * 4u;
        std::span<std::uint8_t> unitIDsWire;
        if (!state->memory.MutableView(unitIDsPointer, unitIDsBytes, unitIDsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter unitIDsWriter(unitIDsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < unitIDsRequired; ++coreIndex) {
        if (!unitIDsWriter.I32(result.unitIDs[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!unitIDsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_unit_rendering_get_visible_features(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedRead == nullptr || state->native->unsyncedRead->unitRendering == nullptr ||
        state->native->unsyncedRead->unitRendering->GetVisibleFeatures == nullptr)
        return Trap("GetVisibleFeatures generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("GetVisibleFeatures generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader featureIDsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t featureIDsPointer = 0;
    std::uint32_t featureIDsCapacity = 0;
    std::uint32_t featureIDsIgnoredLength = 0;
    if (!featureIDsControl.U32(featureIDsPointer) || !featureIDsControl.U32(featureIDsCapacity) ||
        !featureIDsControl.U32(featureIDsIgnoredLength) || !featureIDsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t featureIDsCapacityBytes = static_cast<std::uint64_t>(featureIDsCapacity) * 4u;
    if (featureIDsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(featureIDsPointer, static_cast<std::size_t>(featureIDsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    const std::uint32_t input = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 8u, inputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader reader(inputWire);

    GetVisibleFeaturesQuery query{};
    query.allyTeamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.allyTeamID)>>>(slots[0].i32);
    query.radius = slots[1].f32;
    if (!reader.Bool(query.options.includeIcons)) return Trap("generated Core wire underflow");
    if (!reader.Bool(query.options.includeGeos)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated Core record alignment overflow");
    if (!reader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    GetVisibleFeaturesResult result{};
    state->native->unsyncedRead->unitRendering->GetVisibleFeatures(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t featureIDsRequired = 0;
    if (!NormalizeCoreCount(result.count, featureIDsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (featureIDsRequired != 0 && result.featureIDs == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, featureIDsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || featureIDsCapacity < featureIDsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (featureIDsRequired != 0) {
        const std::size_t featureIDsBytes = static_cast<std::size_t>(featureIDsRequired) * 4u;
        std::span<std::uint8_t> featureIDsWire;
        if (!state->memory.MutableView(featureIDsPointer, featureIDsBytes, featureIDsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter featureIDsWriter(featureIDsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < featureIDsRequired; ++coreIndex) {
        if (!featureIDsWriter.I32(result.featureIDs[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!featureIDsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_unit_rendering_get_visible_projectiles(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedRead == nullptr || state->native->unsyncedRead->unitRendering == nullptr ||
        state->native->unsyncedRead->unitRendering->GetVisibleProjectiles == nullptr)
        return Trap("GetVisibleProjectiles generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetVisibleProjectiles generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader projectileIDsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t projectileIDsPointer = 0;
    std::uint32_t projectileIDsCapacity = 0;
    std::uint32_t projectileIDsIgnoredLength = 0;
    if (!projectileIDsControl.U32(projectileIDsPointer) || !projectileIDsControl.U32(projectileIDsCapacity) ||
        !projectileIDsControl.U32(projectileIDsIgnoredLength) || !projectileIDsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t projectileIDsCapacityBytes = static_cast<std::uint64_t>(projectileIDsCapacity) * 4u;
    if (projectileIDsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(projectileIDsPointer, static_cast<std::size_t>(projectileIDsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    const std::uint32_t input = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 12u, inputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader reader(inputWire);

    GetVisibleProjectilesQuery query{};
    query.allyTeamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.allyTeamID)>>>(slots[0].i32);
    if (!reader.Bool(query.options.includeSyncedProjectiles)) return Trap("generated Core wire underflow");
    if (!reader.Bool(query.options.includeWeaponProjectiles)) return Trap("generated Core wire underflow");
    if (!reader.Bool(query.options.includePieceProjectiles)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated Core record alignment overflow");
    if (!reader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    GetVisibleProjectilesResult result{};
    state->native->unsyncedRead->unitRendering->GetVisibleProjectiles(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t projectileIDsRequired = 0;
    if (!NormalizeCoreCount(result.count, projectileIDsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (projectileIDsRequired != 0 && result.projectileIDs == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, projectileIDsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || projectileIDsCapacity < projectileIDsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (projectileIDsRequired != 0) {
        const std::size_t projectileIDsBytes = static_cast<std::size_t>(projectileIDsRequired) * 4u;
        std::span<std::uint8_t> projectileIDsWire;
        if (!state->memory.MutableView(projectileIDsPointer, projectileIDsBytes, projectileIDsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter projectileIDsWriter(projectileIDsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < projectileIDsRequired; ++coreIndex) {
        if (!projectileIDsWriter.I32(result.projectileIDs[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!projectileIDsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableOutput_unit_rendering_get_visible_units(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedRead == nullptr || state->native->unsyncedRead->unitRendering == nullptr ||
        state->native->unsyncedRead->unitRendering->GetVisibleUnits == nullptr)
        return Trap("GetVisibleUnits generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("GetVisibleUnits generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<std::uint8_t> outputWire;
    if (!state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader unitIDsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCapacity = 0;
    std::uint32_t unitIDsIgnoredLength = 0;
    if (!unitIDsControl.U32(unitIDsPointer) || !unitIDsControl.U32(unitIDsCapacity) ||
        !unitIDsControl.U32(unitIDsIgnoredLength) || !unitIDsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t unitIDsCapacityBytes = static_cast<std::uint64_t>(unitIDsCapacity) * 4u;
    if (unitIDsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(unitIDsPointer, static_cast<std::size_t>(unitIDsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetVisibleUnitsQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    query.radius = slots[1].f32;
    query.includeIcons = slots[2].i32 != 0;
    GetVisibleUnitsResult result{};
    state->native->unsyncedRead->unitRendering->GetVisibleUnits(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    std::uint32_t unitIDsRequired = 0;
    if (!NormalizeCoreCount(result.count, unitIDsRequired)) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (unitIDsRequired != 0 && result.unitIDs == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, unitIDsRequired)) return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || unitIDsCapacity < unitIDsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (unitIDsRequired != 0) {
        const std::size_t unitIDsBytes = static_cast<std::size_t>(unitIDsRequired) * 4u;
        std::span<std::uint8_t> unitIDsWire;
        if (!state->memory.MutableView(unitIDsPointer, unitIDsBytes, unitIDsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter unitIDsWriter(unitIDsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < unitIDsRequired; ++coreIndex) {
        if (!unitIDsWriter.I32(result.unitIDs[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!unitIDsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}


} // namespace

bool RegisterGeneratedVariableOutputImports(wasmtime_linker_t* linker, HostState* state,
    std::string& error)
{
    if (linker == nullptr || state == nullptr || state->native == nullptr) {
        error = "cannot register generated variable-output Core imports without linker/host/native API";
        return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:units-query", "get-render-units",
                MakeFuncType(params, 3, results, 1), CoreVariableOutput_units_query_get_render_units, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:units-query", "get-render-units-draw-flag-changed",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_units_query_get_render_units_draw_flag_changed, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:units-query", "get-team-units-counts",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_units_query_get_team_units_counts, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:units-query", "get-units-in-planes",
                MakeFuncType(params, 3, results, 1), CoreVariableOutput_units_query_get_units_in_planes, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:units-info", "get-unit-is-transporting",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_units_info_get_unit_is_transporting, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:units-info", "get-unit-nano-pieces",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_units_info_get_unit_nano_pieces, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:units-info", "get-unit-tooltip",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_units_info_get_unit_tooltip, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:units-commands", "get-full-build-queue",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_units_commands_get_full_build_queue, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:units-commands", "get-real-build-queue",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_units_commands_get_real_build_queue, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:teams", "get-ally-team-list",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_teams_get_ally_team_list, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:teams", "get-player-list",
                MakeFuncType(params, 3, results, 1), CoreVariableOutput_teams_get_player_list, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:teams", "get-player-list-in-ally-team",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_teams_get_player_list_in_ally_team, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:teams", "get-player-list-in-team",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_teams_get_player_list_in_team, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:teams", "get-team-list",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_teams_get_team_list, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:teams", "get-team-lua-ai",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_teams_get_team_lua_ai, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:teams", "get-team-stats-history",
                MakeFuncType(params, 4, results, 1), CoreVariableOutput_teams_get_team_stats_history, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:features", "get-all-features",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_features_get_all_features, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_F32, WASM_F32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:features", "get-features-in-cylinder",
                MakeFuncType(params, 5, results, 1), CoreVariableOutput_features_get_features_in_cylinder, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_F32, WASM_F32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:features", "get-features-in-rectangle",
                MakeFuncType(params, 5, results, 1), CoreVariableOutput_features_get_features_in_rectangle, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:features", "get-features-in-sphere",
                MakeFuncType(params, 3, results, 1), CoreVariableOutput_features_get_features_in_sphere, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:features", "get-render-features",
                MakeFuncType(params, 3, results, 1), CoreVariableOutput_features_get_render_features, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:features", "get-render-features-draw-flag-changed",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_features_get_render_features_draw_flag_changed, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:projectiles", "get-all-projectiles",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_projectiles_get_all_projectiles, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_F32, WASM_F32, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:projectiles", "get-projectiles-in-rectangle",
                MakeFuncType(params, 6, results, 1), CoreVariableOutput_projectiles_get_projectiles_in_rectangle, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:projectiles", "get-projectiles-in-sphere",
                MakeFuncType(params, 3, results, 1), CoreVariableOutput_projectiles_get_projectiles_in_sphere, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:unit-defs", "get-unit-def-i-ds",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_unit_defs_get_unit_def_i_ds, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:feature-defs", "get-feature-def-i-ds",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_feature_defs_get_feature_def_i_ds, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:feature-defs", "get-feature-def-name",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_feature_defs_get_feature_def_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:weapon-defs", "get-weapon-def-i-ds",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_weapon_defs_get_weapon_def_i_ds, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:weapon-defs", "get-weapon-def-name",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_weapon_defs_get_weapon_def_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:game", "get-map-start-positions",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_game_get_map_start_positions, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:terrain", "get-ground-info",
                MakeFuncType(params, 3, results, 1), CoreVariableOutput_terrain_get_ground_info, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:terrain", "get-terrain-type-data",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_terrain_get_terrain_type_data, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:player", "get-player-traffic",
                MakeFuncType(params, 3, results, 1), CoreVariableOutput_player_get_player_traffic, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:path-finder", "get-path-node-costs",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_path_finder_get_path_node_costs, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:path-finder", "get-path-way-points",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_path_finder_get_path_way_points, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:platform", "get-architecture",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_platform_get_architecture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:move-ctrl", "get-unit-estimated-path",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_move_ctrl_get_unit_estimated_path, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:input", "get-active-command",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_input_get_active_command, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:input", "get-default-command",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_input_get_default_command, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:input", "get-key-symbol",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_input_get_key_symbol, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:input", "get-mouse-cursor",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_input_get_mouse_cursor, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:input", "get-pressed-keys",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_input_get_pressed_keys, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:input", "get-pressed-scans",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_input_get_pressed_scans, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:input", "get-scan-symbol",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_input_get_scan_symbol, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:display", "get-map-draw-mode",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_display_get_map_draw_mode, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:display", "get-mini-map-dual-screen",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_display_get_mini_map_dual_screen, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:display", "get-water-mode",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_display_get_water_mode, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:selection", "get-group-list",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_selection_get_group_list, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:selection", "get-group-units",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_selection_get_group_units, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:selection", "get-selected-units",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_selection_get_selected_units, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:selection", "get-selected-units-sorted",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_selection_get_selected_units_sorted, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:messages", "get-current-tooltip",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_messages_get_current_tooltip, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:gfx", "download-vbo",
                MakeFuncType(params, 6, results, 1), CoreVariableOutput_gfx_download_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:gfx", "get-engine-model-uniform-data-def",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_gfx_get_engine_model_uniform_data_def, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:gfx", "get-engine-uniform-buffer-def",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_gfx_get_engine_uniform_buffer_def, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:gfx", "get-font-info",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_gfx_get_font_info, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:gfx", "get-shader-log",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_gfx_get_shader_log, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:gfx", "get-string",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_gfx_get_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:gfx", "read-pixels",
                MakeFuncType(params, 6, results, 1), CoreVariableOutput_gfx_read_pixels, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:ground-decals", "get-all-ground-decals",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_ground_decals_get_all_ground_decals, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:ground-decals", "get-ground-decal-texture",
                MakeFuncType(params, 3, results, 1), CoreVariableOutput_ground_decals_get_ground_decal_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:ground-decals", "get-ground-decal-type",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_ground_decals_get_ground_decal_type, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:system-control", "get-game-name",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_system_control_get_game_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:system-control", "get-menu-name",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_system_control_get_menu_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:system-control", "get-replay-file-path",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_system_control_get_replay_file_path, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:system-control", "get-replay-recording-file-path",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_system_control_get_replay_recording_file_path, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:system-control", "get-window-display-mode",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_system_control_get_window_display_mode, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "context-get-name",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_context_get_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "data-model-get-string",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_data_model_get_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "document-get-title",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_document_get_title, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "document-get-url",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_document_get_url, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "element-form-control-input-get-selection",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_element_form_control_input_get_selection, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "element-form-control-text-area-get-selection",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_element_form_control_text_area_get_selection, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "element-get-class-name",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_element_get_class_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "element-get-id",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_element_get_id, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "element-get-inner-rml",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_element_get_inner_rml, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "element-get-tag-name",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_element_get_tag_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "element-get-value",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_element_get_value, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "event-get-type",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_event_get_type, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:rml-ui", "get-version",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_rml_ui_get_version, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:unsynced-read", "get-clipboard",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_unsynced_read_get_clipboard, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:unsynced-read", "get-last-message-positions",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_unsynced_read_get_last_message_positions, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:unsynced-read", "get-piece-projectile-name",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_unsynced_read_get_piece_projectile_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:unsynced-read", "get-prev-frame-sync-checksum",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_unsynced_read_get_prev_frame_sync_checksum, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_F32, WASM_F32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:unit-rendering", "get-features-in-screen-rectangle",
                MakeFuncType(params, 5, results, 1), CoreVariableOutput_unit_rendering_get_features_in_screen_rectangle, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:unit-rendering", "get-unit-icon",
                MakeFuncType(params, 2, results, 1), CoreVariableOutput_unit_rendering_get_unit_icon, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:unit-rendering", "get-unit-icon-data",
                MakeFuncType(params, 3, results, 1), CoreVariableOutput_unit_rendering_get_unit_icon_data, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_F32, WASM_F32, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:unit-rendering", "get-units-in-screen-rectangle",
                MakeFuncType(params, 6, results, 1), CoreVariableOutput_unit_rendering_get_units_in_screen_rectangle, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:unit-rendering", "get-visible-features",
                MakeFuncType(params, 4, results, 1), CoreVariableOutput_unit_rendering_get_visible_features, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:unit-rendering", "get-visible-projectiles",
                MakeFuncType(params, 3, results, 1), CoreVariableOutput_unit_rendering_get_visible_projectiles, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableOutput(linker, "spring:unit-rendering", "get-visible-units",
                MakeFuncType(params, 4, results, 1), CoreVariableOutput_unit_rendering_get_visible_units, state, error))
            return false;
    }

    return true;
}

static_assert(92 >= 0, "generated variable-output Core callback count");

} // namespace recoil::wasm::core::generated
