/* @generated by spring-api-codegen; do not edit. */

#include "wasm/generated/WasmCoreGeneratedOptionBindings.h"

#include <array>
#include <bit>
#include <cstdint>
#include <span>
#include <string_view>
#include <type_traits>

#include "NativeInterface/NativeInterface.h"
#include "WasmInterface/core/host/WasmCoreAbi.h"
#include "WasmInterface/core/host/WasmCoreGeneratedSupport.h"
#include "WasmInterface/core/host/WasmCoreWire.h"

namespace recoil::wasm::core::generated {
namespace {

bool DefineGeneratedOption(wasmtime_linker_t* linker, const char* moduleName,
    const char* functionName, wasm_functype_t* type,
    wasmtime_func_unchecked_callback_t callback, HostState* state, std::string& error)
{
    wasmtime_error_t* defineError = wasmtime_linker_define_func_unchecked(
        linker,
        moduleName, std::char_traits<char>::length(moduleName),
        functionName, std::char_traits<char>::length(functionName),
        type, callback, state, nullptr);
    wasm_functype_delete(type);
    if (defineError == nullptr)
        return true;
    error = ErrorMessage(defineError);
    return false;
}

wasm_trap_t* CoreOption_units_info_get_unit_last_attacker(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsInfo == nullptr ||
        state->native->unitsInfo->GetUnitLastAttacker == nullptr)
        return Trap("GetUnitLastAttacker generated Core binding is unavailable");
    if (2 != 0 && (slots == nullptr || slotCount != 2))
        return Trap("GetUnitLastAttacker generated Core ABI signature mismatch");
    if (2 == 0 && slotCount != 0)
        return Trap("GetUnitLastAttacker generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetUnitLastAttackerQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    GetUnitLastAttackerResult result{};
    state->native->unitsInfo->GetUnitLastAttacker(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.Bool(result.hasAttacker)) return Trap("generated option output overflow");
    if (result.hasAttacker) {
        if (!writer.I32(result.attacker.attackerID)) return Trap("generated Core wire overflow");
        if (!writer.I32(result.attacker.attackerDefID)) return Trap("generated Core wire overflow");
        if (!writer.I32(result.attacker.attackerTeam)) return Trap("generated Core wire overflow");
        if (!writer.Align(4u)) return Trap("generated option record alignment overflow");
    } else {
        std::remove_cv_t<std::remove_reference_t<decltype(result.attacker)>> coreDefault{};
        if (!writer.I32(coreDefault.attackerID)) return Trap("generated Core wire overflow");
        if (!writer.I32(coreDefault.attackerDefID)) return Trap("generated Core wire overflow");
        if (!writer.I32(coreDefault.attackerTeam)) return Trap("generated Core wire overflow");
        if (!writer.Align(4u)) return Trap("generated option record alignment overflow");
    }
    if (!writer.Finish(4u)) return Trap("generated option output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated option output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreOption_units_info_get_unit_shield_state(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsInfo == nullptr ||
        state->native->unitsInfo->GetUnitShieldState == nullptr)
        return Trap("GetUnitShieldState generated Core binding is unavailable");
    if (3 != 0 && (slots == nullptr || slotCount != 3))
        return Trap("GetUnitShieldState generated Core ABI signature mismatch");
    if (3 == 0 && slotCount != 0)
        return Trap("GetUnitShieldState generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetUnitShieldStateQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    query.weaponNum = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.weaponNum)>>>(slots[1].i32);
    GetUnitShieldStateResult result{};
    state->native->unitsInfo->GetUnitShieldState(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.Bool(result.hasShield)) return Trap("generated option output overflow");
    if (result.hasShield) {
        if (!writer.Bool(result.shield.shieldEnabled)) return Trap("generated Core wire overflow");
        if (!writer.F32(result.shield.shieldPower)) return Trap("generated Core wire overflow");
        if (!writer.F32(result.shield.shieldAlpha)) return Trap("generated Core wire overflow");
        if (!writer.Align(4u)) return Trap("generated option record alignment overflow");
    } else {
        std::remove_cv_t<std::remove_reference_t<decltype(result.shield)>> coreDefault{};
        if (!writer.Bool(coreDefault.shieldEnabled)) return Trap("generated Core wire overflow");
        if (!writer.F32(coreDefault.shieldPower)) return Trap("generated Core wire overflow");
        if (!writer.F32(coreDefault.shieldAlpha)) return Trap("generated Core wire overflow");
        if (!writer.Align(4u)) return Trap("generated option record alignment overflow");
    }
    if (!writer.Finish(4u)) return Trap("generated option output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated option output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreOption_units_info_get_unit_stockpile(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsInfo == nullptr ||
        state->native->unitsInfo->GetUnitStockpile == nullptr)
        return Trap("GetUnitStockpile generated Core binding is unavailable");
    if (2 != 0 && (slots == nullptr || slotCount != 2))
        return Trap("GetUnitStockpile generated Core ABI signature mismatch");
    if (2 == 0 && slotCount != 0)
        return Trap("GetUnitStockpile generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetUnitStockpileQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    GetUnitStockpileResult result{};
    state->native->unitsInfo->GetUnitStockpile(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.Bool(result.hasStockpile)) return Trap("generated option output overflow");
    if (result.hasStockpile) {
        if (!writer.U32(result.stockpile.stockpile)) return Trap("generated Core wire overflow");
        if (!writer.U32(result.stockpile.stockpileQueueSize)) return Trap("generated Core wire overflow");
        if (!writer.F32(result.stockpile.buildPercent)) return Trap("generated Core wire overflow");
        if (!writer.Align(4u)) return Trap("generated option record alignment overflow");
    } else {
        std::remove_cv_t<std::remove_reference_t<decltype(result.stockpile)>> coreDefault{};
        if (!writer.U32(coreDefault.stockpile)) return Trap("generated Core wire overflow");
        if (!writer.U32(coreDefault.stockpileQueueSize)) return Trap("generated Core wire overflow");
        if (!writer.F32(coreDefault.buildPercent)) return Trap("generated Core wire overflow");
        if (!writer.Align(4u)) return Trap("generated option record alignment overflow");
    }
    if (!writer.Finish(4u)) return Trap("generated option output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated option output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreOption_path_finder_get_next_way_point(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->pathFinder == nullptr ||
        state->native->pathFinder->GetNextWayPoint == nullptr)
        return Trap("GetNextWayPoint generated Core binding is unavailable");
    if (4 != 0 && (slots == nullptr || slotCount != 4))
        return Trap("GetNextWayPoint generated Core ABI signature mismatch");
    if (4 == 0 && slotCount != 0)
        return Trap("GetNextWayPoint generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t input = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 12u, inputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetNextWayPointQuery query{};
    query.pathID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.pathID)>>>(slots[0].i32);
    if (!reader.F32(query.callerPos.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.callerPos.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.callerPos.z)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    query.minDist = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetNextWayPointResult result{};
    state->native->pathFinder->GetNextWayPoint(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.Bool(result.hasWaypoint)) return Trap("generated option output overflow");
    if (result.hasWaypoint) {
        if (!writer.F32(result.waypoint.x)) return Trap("generated Core wire overflow");
        if (!writer.F32(result.waypoint.y)) return Trap("generated Core wire overflow");
        if (!writer.F32(result.waypoint.z)) return Trap("generated Core wire overflow");
        if (!writer.Align(4u)) return Trap("generated option record alignment overflow");
    } else {
        std::remove_cv_t<std::remove_reference_t<decltype(result.waypoint)>> coreDefault{};
        if (!writer.F32(coreDefault.x)) return Trap("generated Core wire overflow");
        if (!writer.F32(coreDefault.y)) return Trap("generated Core wire overflow");
        if (!writer.F32(coreDefault.z)) return Trap("generated Core wire overflow");
        if (!writer.Align(4u)) return Trap("generated option record alignment overflow");
    }
    if (!writer.Finish(4u)) return Trap("generated option output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated option output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreOption_camera_set_camera_target(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->cameraApi == nullptr ||
        state->native->cameraApi->SetCameraTarget == nullptr)
        return Trap("SetCameraTarget generated Core binding is unavailable");
    if (1 != 0 && (slots == nullptr || slotCount != 1))
        return Trap("SetCameraTarget generated Core ABI signature mismatch");
    if (1 == 0 && slotCount != 0)
        return Trap("SetCameraTarget generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t input = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 44u, inputWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(inputWire);

    SetCameraTargetQuery query{};
    if (!reader.F32(query.target.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.target.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.target.z)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.options.hasTransitionTime = corePresent;
    }
    if (!reader.F32(query.options.transitionTime)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.options.hasDirX = corePresent;
    }
    if (!reader.F32(query.options.dirX)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.options.hasDirY = corePresent;
    }
    if (!reader.F32(query.options.dirY)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.options.hasDirZ = corePresent;
    }
    if (!reader.F32(query.options.dirZ)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetCameraTargetResult result{};
    state->native->cameraApi->SetCameraTarget(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreOption_tracing_trace_ray_ground_between_positions(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->tracing == nullptr ||
        state->native->tracing->TraceRayGroundBetweenPositions == nullptr)
        return Trap("TraceRayGroundBetweenPositions generated Core binding is unavailable");
    if (2 != 0 && (slots == nullptr || slotCount != 2))
        return Trap("TraceRayGroundBetweenPositions generated Core ABI signature mismatch");
    if (2 == 0 && slotCount != 0)
        return Trap("TraceRayGroundBetweenPositions generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t input = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 32u, inputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 32u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    TraceRayGroundBetweenPositionsQuery query{};
    if (!reader.F32(query.start.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.start.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.start.z)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    if (!reader.F32(query.end.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.end.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.end.z)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.options.hasTestWater = corePresent;
    }
    if (!reader.Bool(query.options.testWater)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    TraceRayGroundBetweenPositionsResult result{};
    state->native->tracing->TraceRayGroundBetweenPositions(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 32> wire{};
    WireWriter writer(wire);
    if (!writer.Bool(result.hit)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hitLength)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hitPos.x)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hitPos.y)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hitPos.z)) return Trap("generated Core wire overflow");
    if (!writer.Align(4u)) return Trap("generated option record alignment overflow");
    if (!writer.F32(result.hitNormal.x)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hitNormal.y)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hitNormal.z)) return Trap("generated Core wire overflow");
    if (!writer.Align(4u)) return Trap("generated option record alignment overflow");
    if (!writer.Finish(4u)) return Trap("generated option output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated option output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreOption_tracing_trace_ray_ground_in_direction(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->tracing == nullptr ||
        state->native->tracing->TraceRayGroundInDirection == nullptr)
        return Trap("TraceRayGroundInDirection generated Core binding is unavailable");
    if (2 != 0 && (slots == nullptr || slotCount != 2))
        return Trap("TraceRayGroundInDirection generated Core ABI signature mismatch");
    if (2 == 0 && slotCount != 0)
        return Trap("TraceRayGroundInDirection generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t input = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 40u, inputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 32u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    TraceRayGroundInDirectionQuery query{};
    if (!reader.F32(query.start.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.start.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.start.z)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    if (!reader.F32(query.dir.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.dir.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.dir.z)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.options.hasLength = corePresent;
    }
    if (!reader.F32(query.options.length)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.options.hasTestWater = corePresent;
    }
    if (!reader.Bool(query.options.testWater)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    TraceRayGroundInDirectionResult result{};
    state->native->tracing->TraceRayGroundInDirection(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 32> wire{};
    WireWriter writer(wire);
    if (!writer.Bool(result.hit)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hitLength)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hitPos.x)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hitPos.y)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hitPos.z)) return Trap("generated Core wire overflow");
    if (!writer.Align(4u)) return Trap("generated option record alignment overflow");
    if (!writer.F32(result.hitNormal.x)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hitNormal.y)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.hitNormal.z)) return Trap("generated Core wire overflow");
    if (!writer.Align(4u)) return Trap("generated option record alignment overflow");
    if (!writer.Finish(4u)) return Trap("generated option output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated option output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreOption_unsynced_ctrl_set_atmosphere(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetAtmosphere == nullptr)
        return Trap("SetAtmosphere generated Core binding is unavailable");
    if (1 != 0 && (slots == nullptr || slotCount != 1))
        return Trap("SetAtmosphere generated Core ABI signature mismatch");
    if (1 == 0 && slotCount != 0)
        return Trap("SetAtmosphere generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t input = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 116u, inputWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(inputWire);

    SetAtmosphereQuery query{};
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasFogColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.fogColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasSkyColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.skyColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasSunColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.sunColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasCloudColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.cloudColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasSkyAxisAngle = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.skyAxisAngle[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasFogStart = corePresent;
    }
    if (!reader.F32(query.params.fogStart)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasFogEnd = corePresent;
    }
    if (!reader.F32(query.params.fogEnd)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetAtmosphereResult result{};
    state->native->unsyncedCtrl->SetAtmosphere(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreOption_unsynced_ctrl_set_map_rendering_params(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetMapRenderingParams == nullptr)
        return Trap("SetMapRenderingParams generated Core binding is unavailable");
    if (1 != 0 && (slots == nullptr || slotCount != 1))
        return Trap("SetMapRenderingParams generated Core ABI signature mismatch");
    if (1 == 0 && slotCount != 0)
        return Trap("SetMapRenderingParams generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t input = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 64u, inputWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(inputWire);

    SetMapRenderingParamsQuery query{};
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasSplatTexScales = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.splatTexScales[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasSplatTexMults = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.splatTexMults[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasVoidWater = corePresent;
    }
    if (!reader.Bool(query.params.voidWater)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasVoidGround = corePresent;
    }
    if (!reader.Bool(query.params.voidGround)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasSplatDetailNormalDiffuseAlpha = corePresent;
    }
    if (!reader.Bool(query.params.splatDetailNormalDiffuseAlpha)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetMapRenderingParamsResult result{};
    state->native->unsyncedCtrl->SetMapRenderingParams(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreOption_unsynced_ctrl_set_shock_front_factors(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetShockFrontFactors == nullptr)
        return Trap("SetShockFrontFactors generated Core binding is unavailable");
    if (1 != 0 && (slots == nullptr || slotCount != 1))
        return Trap("SetShockFrontFactors generated Core ABI signature mismatch");
    if (1 == 0 && slotCount != 0)
        return Trap("SetShockFrontFactors generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t input = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 24u, inputWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(inputWire);

    SetShockFrontFactorsQuery query{};
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.options.hasMinArea = corePresent;
    }
    if (!reader.F32(query.options.minArea)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.options.hasMinPower = corePresent;
    }
    if (!reader.F32(query.options.minPower)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.options.hasDistAdj = corePresent;
    }
    if (!reader.F32(query.options.distAdj)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetShockFrontFactorsResult result{};
    state->native->unsyncedCtrl->SetShockFrontFactors(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreOption_unsynced_ctrl_set_sun_lighting(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetSunLighting == nullptr)
        return Trap("SetSunLighting generated Core binding is unavailable");
    if (1 != 0 && (slots == nullptr || slotCount != 1))
        return Trap("SetSunLighting generated Core ABI signature mismatch");
    if (1 == 0 && slotCount != 0)
        return Trap("SetSunLighting generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t input = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 144u, inputWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(inputWire);

    SetSunLightingQuery query{};
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasGroundAmbientColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.groundAmbientColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasGroundDiffuseColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.groundDiffuseColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasGroundSpecularColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.groundSpecularColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasModelAmbientColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.modelAmbientColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasModelDiffuseColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.modelDiffuseColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasModelSpecularColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 4u; ++coreReadIndex1) {
        if (!reader.F32(query.params.modelSpecularColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasSpecularExponent = corePresent;
    }
    if (!reader.F32(query.params.specularExponent)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasGroundShadowDensity = corePresent;
    }
    if (!reader.F32(query.params.groundShadowDensity)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasModelShadowDensity = corePresent;
    }
    if (!reader.F32(query.params.modelShadowDensity)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetSunLightingResult result{};
    state->native->unsyncedCtrl->SetSunLighting(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreOption_unsynced_ctrl_set_water_params(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetWaterParams == nullptr)
        return Trap("SetWaterParams generated Core binding is unavailable");
    if (1 != 0 && (slots == nullptr || slotCount != 1))
        return Trap("SetWaterParams generated Core ABI signature mismatch");
    if (1 == 0 && slotCount != 0)
        return Trap("SetWaterParams generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t input = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 328u, inputWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(inputWire);

    SetWaterParamsQuery query{};
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasAbsorb = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 3u; ++coreReadIndex1) {
        if (!reader.F32(query.params.absorb[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasBaseColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 3u; ++coreReadIndex1) {
        if (!reader.F32(query.params.baseColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasMinColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 3u; ++coreReadIndex1) {
        if (!reader.F32(query.params.minColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasSurfaceColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 3u; ++coreReadIndex1) {
        if (!reader.F32(query.params.surfaceColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasDiffuseColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 3u; ++coreReadIndex1) {
        if (!reader.F32(query.params.diffuseColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasSpecularColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 3u; ++coreReadIndex1) {
        if (!reader.F32(query.params.specularColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasPlaneColor = corePresent;
    }
    for (std::size_t coreReadIndex1 = 0; coreReadIndex1 < 3u; ++coreReadIndex1) {
        if (!reader.F32(query.params.planeColor[coreReadIndex1])) return Trap("generated Core wire underflow");
    }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasRepeatX = corePresent;
    }
    if (!reader.F32(query.params.repeatX)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasRepeatY = corePresent;
    }
    if (!reader.F32(query.params.repeatY)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasSurfaceAlpha = corePresent;
    }
    if (!reader.F32(query.params.surfaceAlpha)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasAmbientFactor = corePresent;
    }
    if (!reader.F32(query.params.ambientFactor)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasDiffuseFactor = corePresent;
    }
    if (!reader.F32(query.params.diffuseFactor)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasSpecularFactor = corePresent;
    }
    if (!reader.F32(query.params.specularFactor)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasSpecularPower = corePresent;
    }
    if (!reader.F32(query.params.specularPower)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasFresnelMin = corePresent;
    }
    if (!reader.F32(query.params.fresnelMin)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasFresnelMax = corePresent;
    }
    if (!reader.F32(query.params.fresnelMax)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasFresnelPower = corePresent;
    }
    if (!reader.F32(query.params.fresnelPower)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasReflectionDistortion = corePresent;
    }
    if (!reader.F32(query.params.reflectionDistortion)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasBlurBase = corePresent;
    }
    if (!reader.F32(query.params.blurBase)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasBlurExponent = corePresent;
    }
    if (!reader.F32(query.params.blurExponent)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasPerlinStartFreq = corePresent;
    }
    if (!reader.F32(query.params.perlinStartFreq)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasPerlinLacunarity = corePresent;
    }
    if (!reader.F32(query.params.perlinLacunarity)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasPerlinAmplitude = corePresent;
    }
    if (!reader.F32(query.params.perlinAmplitude)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasWindSpeed = corePresent;
    }
    if (!reader.F32(query.params.windSpeed)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasWaveOffsetFactor = corePresent;
    }
    if (!reader.F32(query.params.waveOffsetFactor)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasWaveLength = corePresent;
    }
    if (!reader.F32(query.params.waveLength)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasWaveFoamDistortion = corePresent;
    }
    if (!reader.F32(query.params.waveFoamDistortion)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasWaveFoamIntensity = corePresent;
    }
    if (!reader.F32(query.params.waveFoamIntensity)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasCausticsResolution = corePresent;
    }
    if (!reader.F32(query.params.causticsResolution)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasCausticsStrength = corePresent;
    }
    if (!reader.F32(query.params.causticsStrength)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasNumTiles = corePresent;
    }
    if (!reader.F32(query.params.numTiles)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasShoreWaves = corePresent;
    }
    if (!reader.Bool(query.params.shoreWaves)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasForceRendering = corePresent;
    }
    if (!reader.Bool(query.params.forceRendering)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.params.hasHasWaterPlane = corePresent;
    }
    if (!reader.Bool(query.params.hasWaterPlane)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetWaterParamsResult result{};
    state->native->unsyncedCtrl->SetWaterParams(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreOption_rml_ui_document_show(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentShow == nullptr)
        return Trap("DocumentShow generated Core binding is unavailable");
    if (2 != 0 && (slots == nullptr || slotCount != 2))
        return Trap("DocumentShow generated Core ABI signature mismatch");
    if (2 == 0 && slotCount != 0)
        return Trap("DocumentShow generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t input = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    if (!state->memory.View(input, 16u, inputWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(inputWire);

    RmlDocumentShowQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.options.hasModal = corePresent;
    }
    { std::int32_t coreRaw = 0; if (!reader.I32(coreRaw)) return Trap("generated Core wire underflow"); query.options.modal = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options.modal)>>>(coreRaw); }
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated option presence underflow");
        query.options.hasFocus = corePresent;
    }
    { std::int32_t coreRaw = 0; if (!reader.I32(coreRaw)) return Trap("generated Core wire underflow"); query.options.focus = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options.focus)>>>(coreRaw); }
    if (!reader.Align(4u)) return Trap("generated option record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlDocumentBoolResult result{};
    state->native->rmlUi->DocumentShow(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}


} // namespace

bool RegisterGeneratedOptionImports(wasmtime_linker_t* linker, HostState* state,
    std::string& error)
{
    if (linker == nullptr || state == nullptr || state->native == nullptr) {
        error = "cannot register generated option Core imports without linker/host/native API";
        return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedOption(linker, "spring:units-info", "get-unit-last-attacker",
                MakeFuncType(params, 2, results, 1), CoreOption_units_info_get_unit_last_attacker, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedOption(linker, "spring:units-info", "get-unit-shield-state",
                MakeFuncType(params, 3, results, 1), CoreOption_units_info_get_unit_shield_state, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedOption(linker, "spring:units-info", "get-unit-stockpile",
                MakeFuncType(params, 2, results, 1), CoreOption_units_info_get_unit_stockpile, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedOption(linker, "spring:path-finder", "get-next-way-point",
                MakeFuncType(params, 4, results, 1), CoreOption_path_finder_get_next_way_point, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedOption(linker, "spring:camera", "set-camera-target",
                MakeFuncType(params, 1, results, 1), CoreOption_camera_set_camera_target, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedOption(linker, "spring:tracing", "trace-ray-ground-between-positions",
                MakeFuncType(params, 2, results, 1), CoreOption_tracing_trace_ray_ground_between_positions, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedOption(linker, "spring:tracing", "trace-ray-ground-in-direction",
                MakeFuncType(params, 2, results, 1), CoreOption_tracing_trace_ray_ground_in_direction, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedOption(linker, "spring:unsynced-ctrl", "set-atmosphere",
                MakeFuncType(params, 1, results, 1), CoreOption_unsynced_ctrl_set_atmosphere, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedOption(linker, "spring:unsynced-ctrl", "set-map-rendering-params",
                MakeFuncType(params, 1, results, 1), CoreOption_unsynced_ctrl_set_map_rendering_params, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedOption(linker, "spring:unsynced-ctrl", "set-shock-front-factors",
                MakeFuncType(params, 1, results, 1), CoreOption_unsynced_ctrl_set_shock_front_factors, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedOption(linker, "spring:unsynced-ctrl", "set-sun-lighting",
                MakeFuncType(params, 1, results, 1), CoreOption_unsynced_ctrl_set_sun_lighting, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedOption(linker, "spring:unsynced-ctrl", "set-water-params",
                MakeFuncType(params, 1, results, 1), CoreOption_unsynced_ctrl_set_water_params, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedOption(linker, "spring:rml-ui", "document-show",
                MakeFuncType(params, 2, results, 1), CoreOption_rml_ui_document_show, state, error))
            return false;
    }

    return true;
}

static_assert(13 >= 0, "generated option Core callback count");

} // namespace recoil::wasm::core::generated
