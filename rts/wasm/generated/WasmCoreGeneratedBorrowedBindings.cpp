/* @generated by spring-api-codegen; do not edit. */

#include "wasm/generated/WasmCoreGeneratedBorrowedBindings.h"

#include <array>
#include <bit>
#include <cstdint>
#include <limits>
#include <span>
#include <string_view>
#include <type_traits>

#include "NativeInterface/NativeInterface.h"
#include "WasmInterface/WasmCoreAbi.h"
#include "WasmInterface/WasmCoreGeneratedSupport.h"
#include "WasmInterface/WasmCoreWire.h"

namespace recoil::wasm::core::generated {
namespace {

template<typename T>
bool AssignCoreBorrowedCount(std::uint32_t value, T& output)
{
    using Native = std::remove_cv_t<std::remove_reference_t<T>>;
    if (static_cast<std::uint64_t>(value) >
        static_cast<std::uint64_t>(std::numeric_limits<Native>::max()))
        return false;
    output = static_cast<Native>(value);
    return true;
}

bool DefineGeneratedBorrowed(wasmtime_linker_t* linker, const char* moduleName,
    const char* functionName, wasm_functype_t* type,
    wasmtime_func_unchecked_callback_t callback, HostState* state, std::string& error)
{
    wasmtime_error_t* defineError = wasmtime_linker_define_func_unchecked(
        linker,
        moduleName, std::char_traits<char>::length(moduleName),
        functionName, std::char_traits<char>::length(functionName),
        type, callback, state, nullptr);
    wasm_functype_delete(type);
    if (defineError == nullptr)
        return true;
    error = ErrorMessage(defineError);
    return false;
}

wasm_trap_t* CoreBorrowed_units_info_get_unit_build_params(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsInfo == nullptr ||
        state->native->unitsInfo->GetUnitBuildParams == nullptr)
        return Trap("GetUnitBuildParams borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("GetUnitBuildParams borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetUnitBuildParamsQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t paramNamePointer = 0;
    std::uint32_t paramNameCount = 0;
    if (!reader.U32(paramNamePointer) || !reader.U32(paramNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (paramNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> paramNameBytes;
    if (!state->memory.View(paramNamePointer, static_cast<std::size_t>(paramNameCount) + 1u, paramNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (paramNameBytes[paramNameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.paramName = reinterpret_cast<const char*>(paramNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetUnitBuildParamsResult result{};
    state->native->unitsInfo->GetUnitBuildParams(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.Bool(result.hasValue)) return Trap("borrowed Core option overflow");
    if (result.hasValue) {
        if (!writer.F32(result.value.number)) return Trap("borrowed Core wire overflow");
        if (!writer.Bool(result.value.boolean)) return Trap("borrowed Core wire overflow");
        if (!writer.Bool(result.value.useBoolean)) return Trap("borrowed Core wire overflow");
        if (!writer.Align(4u)) return Trap("borrowed Core record alignment overflow");
    } else {
        std::remove_cv_t<std::remove_reference_t<decltype(result.value)>> coreAbsent{};
        if (!writer.F32(coreAbsent.number)) return Trap("borrowed Core wire overflow");
        if (!writer.Bool(coreAbsent.boolean)) return Trap("borrowed Core wire overflow");
        if (!writer.Bool(coreAbsent.useBoolean)) return Trap("borrowed Core wire overflow");
        if (!writer.Align(4u)) return Trap("borrowed Core record alignment overflow");
    }
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_units_info_get_unit_sensor_radius(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsInfo == nullptr ||
        state->native->unitsInfo->GetUnitSensorRadius == nullptr)
        return Trap("GetUnitSensorRadius borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("GetUnitSensorRadius borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 28u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetUnitSensorRadiusQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t typePointer = 0;
    std::uint32_t typeCount = 0;
    if (!reader.U32(typePointer) || !reader.U32(typeCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (typeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> typeBytes;
    if (!state->memory.View(typePointer, static_cast<std::size_t>(typeCount) + 1u, typeBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (typeBytes[typeCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.type = reinterpret_cast<const char*>(typeBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetUnitSensorRadiusResult result{};
    state->native->unitsInfo->GetUnitSensorRadius(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 28> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.radius.los)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.radius.airLos)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.radius.radar)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.radius.sonar)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.radius.seismic)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.radius.radarJammer)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.radius.sonarJammer)) return Trap("borrowed Core wire overflow");
    if (!writer.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_units_weapons_get_unit_weapon_state(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsWeapons == nullptr ||
        state->native->unitsWeapons->GetUnitWeaponState == nullptr)
        return Trap("GetUnitWeaponState borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("GetUnitWeaponState borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 52u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetUnitWeaponStateQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    query.weaponNum = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.weaponNum)>>>(slots[1].i32);
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (keyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, static_cast<std::size_t>(keyCount) + 1u, keyBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (keyBytes[keyCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.key = reinterpret_cast<const char*>(keyBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetUnitWeaponStateResult result{};
    state->native->unitsWeapons->GetUnitWeaponState(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 52> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.state.reloadTime)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.state.reloadFrame)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.state.range)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.state.projectileSpeed)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.state.accuracy)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.state.sprayAngle)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.state.aimFromHeight)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.state.salvoSize)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.state.salvoDelay)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.state.salvoError)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.state.targetMoveError)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.state.turnRate)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.state.autoTarget)) return Trap("borrowed Core wire overflow");
    if (!writer.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_units_pieces_get_model_root_piece(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsPieces == nullptr ||
        state->native->unitsPieces->GetModelRootPiece == nullptr)
        return Trap("GetModelRootPiece borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("GetModelRootPiece borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetModelRootPieceQuery query{};
    std::uint32_t modelNamePointer = 0;
    std::uint32_t modelNameCount = 0;
    if (!reader.U32(modelNamePointer) || !reader.U32(modelNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (modelNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> modelNameBytes;
    if (!state->memory.View(modelNamePointer, static_cast<std::size_t>(modelNameCount) + 1u, modelNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (modelNameBytes[modelNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.modelName = reinterpret_cast<const char*>(modelNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetModelRootPieceResult result{};
    state->native->unitsPieces->GetModelRootPiece(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.rootPiece), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_teams_get_team_resource_stats(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->teams == nullptr ||
        state->native->teams->GetTeamResourceStats == nullptr)
        return Trap("GetTeamResourceStats borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("GetTeamResourceStats borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 72u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetTeamResourceStatsQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourcePointer = 0;
    std::uint32_t resourceCount = 0;
    if (!reader.U32(resourcePointer) || !reader.U32(resourceCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (resourceCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> resourceBytes;
    if (!state->memory.View(resourcePointer, static_cast<std::size_t>(resourceCount) + 1u, resourceBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (resourceBytes[resourceCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.resource = reinterpret_cast<const char*>(resourceBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetTeamResourceStatsResult result{};
    state->native->teams->GetTeamResourceStats(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 72> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.resources.metalCurrent)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalStorage)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalPull)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalIncome)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalExpense)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalShared)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalSent)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalReceived)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalExcess)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyCurrent)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyStorage)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyPull)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyIncome)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyExpense)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyShared)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energySent)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyReceived)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyExcess)) return Trap("borrowed Core wire overflow");
    if (!writer.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_teams_get_team_resources(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->teams == nullptr ||
        state->native->teams->GetTeamResources == nullptr)
        return Trap("GetTeamResources borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("GetTeamResources borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 72u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetTeamResourcesQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourcePointer = 0;
    std::uint32_t resourceCount = 0;
    if (!reader.U32(resourcePointer) || !reader.U32(resourceCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (resourceCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> resourceBytes;
    if (!state->memory.View(resourcePointer, static_cast<std::size_t>(resourceCount) + 1u, resourceBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (resourceBytes[resourceCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.resource = reinterpret_cast<const char*>(resourceBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetTeamResourcesResult result{};
    state->native->teams->GetTeamResources(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 72> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.resources.metalCurrent)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalStorage)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalPull)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalIncome)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalExpense)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalShared)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalSent)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalReceived)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.metalExcess)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyCurrent)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyStorage)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyPull)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyIncome)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyExpense)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyShared)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energySent)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyReceived)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.resources.energyExcess)) return Trap("borrowed Core wire overflow");
    if (!writer.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_defs_get_unit_def_id_by_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefIDByName == nullptr)
        return Trap("GetUnitDefIDByName borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("GetUnitDefIDByName borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetUnitDefIDByNameQuery query{};
    std::uint32_t unitDefNamePointer = 0;
    std::uint32_t unitDefNameCount = 0;
    if (!reader.U32(unitDefNamePointer) || !reader.U32(unitDefNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (unitDefNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> unitDefNameBytes;
    if (!state->memory.View(unitDefNamePointer, static_cast<std::size_t>(unitDefNameCount) + 1u, unitDefNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (unitDefNameBytes[unitDefNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.unitDefName = reinterpret_cast<const char*>(unitDefNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetUnitDefIDByNameResult result{};
    state->native->unitDefs->GetUnitDefIDByName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.id), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_defs_get_unit_def_param_bool(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefParamBool == nullptr)
        return Trap("GetUnitDefParamBool borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("GetUnitDefParamBool borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetUnitDefParamBoolQuery query{};
    query.unitDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitDefID)>>>(slots[0].i32);
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (keyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, static_cast<std::size_t>(keyCount) + 1u, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (keyBytes[keyCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.key = reinterpret_cast<const char*>(keyBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetUnitDefParamBoolResult result{};
    state->native->unitDefs->GetUnitDefParamBool(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_defs_get_unit_def_param_float(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefParamFloat == nullptr)
        return Trap("GetUnitDefParamFloat borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("GetUnitDefParamFloat borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetUnitDefParamFloatQuery query{};
    query.unitDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitDefID)>>>(slots[0].i32);
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (keyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, static_cast<std::size_t>(keyCount) + 1u, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (keyBytes[keyCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.key = reinterpret_cast<const char*>(keyBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetUnitDefParamFloatResult result{};
    state->native->unitDefs->GetUnitDefParamFloat(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(std::bit_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_defs_get_unit_def_param_int(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefParamInt == nullptr)
        return Trap("GetUnitDefParamInt borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("GetUnitDefParamInt borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetUnitDefParamIntQuery query{};
    query.unitDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitDefID)>>>(slots[0].i32);
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (keyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, static_cast<std::size_t>(keyCount) + 1u, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (keyBytes[keyCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.key = reinterpret_cast<const char*>(keyBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetUnitDefParamIntResult result{};
    state->native->unitDefs->GetUnitDefParamInt(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_defs_get_unit_def_param_type(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefParamType == nullptr)
        return Trap("GetUnitDefParamType borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("GetUnitDefParamType borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetUnitDefParamTypeQuery query{};
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (keyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, static_cast<std::size_t>(keyCount) + 1u, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (keyBytes[keyCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.key = reinterpret_cast<const char*>(keyBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetUnitDefParamTypeResult result{};
    state->native->unitDefs->GetUnitDefParamType(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.type), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_feature_defs_get_feature_def_id_by_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->featureDefs == nullptr ||
        state->native->featureDefs->GetFeatureDefIDByName == nullptr)
        return Trap("GetFeatureDefIDByName borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("GetFeatureDefIDByName borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetFeatureDefIDByNameQuery query{};
    std::uint32_t featureDefNamePointer = 0;
    std::uint32_t featureDefNameCount = 0;
    if (!reader.U32(featureDefNamePointer) || !reader.U32(featureDefNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (featureDefNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> featureDefNameBytes;
    if (!state->memory.View(featureDefNamePointer, static_cast<std::size_t>(featureDefNameCount) + 1u, featureDefNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (featureDefNameBytes[featureDefNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.featureDefName = reinterpret_cast<const char*>(featureDefNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetFeatureDefIDByNameResult result{};
    state->native->featureDefs->GetFeatureDefIDByName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.id), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_weapon_defs_get_weapon_def_id(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->weaponDefs == nullptr ||
        state->native->weaponDefs->GetWeaponDefID == nullptr)
        return Trap("GetWeaponDefID borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("GetWeaponDefID borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetWeaponDefIDQuery query{};
    std::uint32_t weaponDefNamePointer = 0;
    std::uint32_t weaponDefNameCount = 0;
    if (!reader.U32(weaponDefNamePointer) || !reader.U32(weaponDefNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (weaponDefNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> weaponDefNameBytes;
    if (!state->memory.View(weaponDefNamePointer, static_cast<std::size_t>(weaponDefNameCount) + 1u, weaponDefNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (weaponDefNameBytes[weaponDefNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.weaponDefName = reinterpret_cast<const char*>(weaponDefNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetWeaponDefIDResult result{};
    state->native->weaponDefs->GetWeaponDefID(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.id), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_math_extra_bit_bits(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->mathExtra == nullptr ||
        state->native->mathExtra->BitBits == nullptr)
        return Trap("BitBits borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("BitBits borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    BitBitsQuery query{};
    std::uint32_t bitsPointer = 0;
    std::uint32_t bitsCount = 0;
    if (!reader.U32(bitsPointer) || !reader.U32(bitsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (bitsCount == 0) {
        query.bits = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t bitsBytes64 = static_cast<std::uint64_t>(bitsCount) * 4u;
        if (bitsBytes64 > std::numeric_limits<std::size_t>::max() || (bitsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> bitsBytes;
        if (!state->memory.View(bitsPointer, static_cast<std::size_t>(bitsBytes64), bitsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::uint32_t) == 4u, "borrowed Core/native element width mismatch");
        query.bits = reinterpret_cast<std::remove_reference_t<decltype(query.bits)>>(bitsBytes.data());
    }
    if (!AssignCoreBorrowedCount(bitsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    BitBitsResult result{};
    state->native->mathExtra->BitBits(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.bits), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_math_extra_diag(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->mathExtra == nullptr ||
        state->native->mathExtra->Diag == nullptr)
        return Trap("Diag borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("Diag borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    DiagQuery query{};
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCount = 0;
    if (!reader.U32(valuesPointer) || !reader.U32(valuesCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valuesCount == 0) {
        query.values = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t valuesBytes64 = static_cast<std::uint64_t>(valuesCount) * 4u;
        if (valuesBytes64 > std::numeric_limits<std::size_t>::max() || (valuesPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> valuesBytes;
        if (!state->memory.View(valuesPointer, static_cast<std::size_t>(valuesBytes64), valuesBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(float) == 4u, "borrowed Core/native element width mismatch");
        query.values = reinterpret_cast<std::remove_reference_t<decltype(query.values)>>(valuesBytes.data());
    }
    if (!AssignCoreBorrowedCount(valuesCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    DiagResult result{};
    state->native->mathExtra->Diag(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(std::bit_cast<std::uint32_t>(result.length), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_encoding_is_valid_base64(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->encoding == nullptr ||
        state->native->encoding->IsValidBase64 == nullptr)
        return Trap("IsValidBase64 borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("IsValidBase64 borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    IsValidBase64Query query{};
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (textCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, static_cast<std::size_t>(textCount) + 1u, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (textBytes[textCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.text = reinterpret_cast<const char*>(textBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    IsValidBase64Result result{};
    state->native->encoding->IsValidBase64(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.valid ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_encoding_is_valid_base64_url(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->encoding == nullptr ||
        state->native->encoding->IsValidBase64Url == nullptr)
        return Trap("IsValidBase64Url borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("IsValidBase64Url borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    IsValidBase64UrlQuery query{};
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (textCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, static_cast<std::size_t>(textCount) + 1u, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (textBytes[textCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.text = reinterpret_cast<const char*>(textBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    IsValidBase64UrlResult result{};
    state->native->encoding->IsValidBase64Url(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.valid ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_path_finder_request_path(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->pathFinder == nullptr ||
        state->native->pathFinder->RequestPath == nullptr)
        return Trap("RequestPath borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("RequestPath borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 36u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RequestPathQuery query{};
    query.moveDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.moveDefID)>>>(slots[0].i32);
    bool moveDefNamePresent = false;
    std::uint32_t moveDefNamePointer = 0;
    std::uint32_t moveDefNameCount = 0;
    if (!reader.Bool(moveDefNamePresent) || !reader.U32(moveDefNamePointer) || !reader.U32(moveDefNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.hasMoveDefName = moveDefNamePresent;
    if (!moveDefNamePresent) {
        query.moveDefName = nullptr;
    } else {
        if (moveDefNameCount == std::numeric_limits<std::uint32_t>::max()) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> moveDefNameBytes;
        if (!state->memory.View(moveDefNamePointer, static_cast<std::size_t>(moveDefNameCount) + 1u, moveDefNameBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        if (moveDefNameBytes[moveDefNameCount] != 0) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        query.moveDefName = reinterpret_cast<const char*>(moveDefNameBytes.data());
    }
    if (!reader.F32(query.startPos.x)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.startPos.y)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.startPos.z)) return Trap("borrowed Core wire underflow");
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!reader.F32(query.endPos.x)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.endPos.y)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.endPos.z)) return Trap("borrowed Core wire underflow");
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    query.radius = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RequestPathResult result{};
    state->native->pathFinder->RequestPath(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.pathID), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_input_get_key_code(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->input == nullptr ||
        state->native->input->GetKeyCode == nullptr)
        return Trap("GetKeyCode borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("GetKeyCode borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetKeyCodeQuery query{};
    std::uint32_t keySymPointer = 0;
    std::uint32_t keySymCount = 0;
    if (!reader.U32(keySymPointer) || !reader.U32(keySymCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (keySymCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keySymBytes;
    if (!state->memory.View(keySymPointer, static_cast<std::size_t>(keySymCount) + 1u, keySymBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (keySymBytes[keySymCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.keySym = reinterpret_cast<const char*>(keySymBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetKeyCodeResult result{};
    state->native->input->GetKeyCode(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.keyCode), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_debug_input_emulate_text_editing(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->debugInput == nullptr ||
        state->native->debugInput->EmulateTextEditing == nullptr)
        return Trap("EmulateTextEditing borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("EmulateTextEditing borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    EmulateTextEditingQuery query{};
    std::uint32_t utf8TextPointer = 0;
    std::uint32_t utf8TextCount = 0;
    if (!reader.U32(utf8TextPointer) || !reader.U32(utf8TextCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (utf8TextCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> utf8TextBytes;
    if (!state->memory.View(utf8TextPointer, static_cast<std::size_t>(utf8TextCount) + 1u, utf8TextBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (utf8TextBytes[utf8TextCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.utf8Text = reinterpret_cast<const char*>(utf8TextBytes.data());
    query.start = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.start)>>>(slots[0].i32);
    query.length = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.length)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    EmulateTextEditingResult result{};
    state->native->debugInput->EmulateTextEditing(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.consumed ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_debug_input_emulate_text_input(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->debugInput == nullptr ||
        state->native->debugInput->EmulateTextInput == nullptr)
        return Trap("EmulateTextInput borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("EmulateTextInput borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    EmulateTextInputQuery query{};
    std::uint32_t utf8TextPointer = 0;
    std::uint32_t utf8TextCount = 0;
    if (!reader.U32(utf8TextPointer) || !reader.U32(utf8TextCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (utf8TextCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> utf8TextBytes;
    if (!state->memory.View(utf8TextPointer, static_cast<std::size_t>(utf8TextCount) + 1u, utf8TextBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (utf8TextBytes[utf8TextCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.utf8Text = reinterpret_cast<const char*>(utf8TextBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    EmulateTextInputResult result{};
    state->native->debugInput->EmulateTextInput(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.consumed ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_selection_deselect_unit_array(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->selection == nullptr ||
        state->native->selection->DeselectUnitArray == nullptr)
        return Trap("DeselectUnitArray borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("DeselectUnitArray borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    DeselectUnitArrayQuery query{};
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCount = 0;
    if (!reader.U32(unitIDsPointer) || !reader.U32(unitIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (unitIDsCount == 0) {
        query.unitIDs = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t unitIDsBytes64 = static_cast<std::uint64_t>(unitIDsCount) * 4u;
        if (unitIDsBytes64 > std::numeric_limits<std::size_t>::max() || (unitIDsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> unitIDsBytes;
        if (!state->memory.View(unitIDsPointer, static_cast<std::size_t>(unitIDsBytes64), unitIDsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::int32_t) == 4u, "borrowed Core/native element width mismatch");
        query.unitIDs = reinterpret_cast<std::remove_reference_t<decltype(query.unitIDs)>>(unitIDsBytes.data());
    }
    if (!AssignCoreBorrowedCount(unitIDsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    DeselectUnitArrayResult result{};
    state->native->selection->DeselectUnitArray(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_selection_select_unit_array(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->selection == nullptr ||
        state->native->selection->SelectUnitArray == nullptr)
        return Trap("SelectUnitArray borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SelectUnitArray borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SelectUnitArrayQuery query{};
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCount = 0;
    if (!reader.U32(unitIDsPointer) || !reader.U32(unitIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (unitIDsCount == 0) {
        query.unitIDs = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t unitIDsBytes64 = static_cast<std::uint64_t>(unitIDsCount) * 4u;
        if (unitIDsBytes64 > std::numeric_limits<std::size_t>::max() || (unitIDsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> unitIDsBytes;
        if (!state->memory.View(unitIDsPointer, static_cast<std::size_t>(unitIDsBytes64), unitIDsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::int32_t) == 4u, "borrowed Core/native element width mismatch");
        query.unitIDs = reinterpret_cast<std::remove_reference_t<decltype(query.unitIDs)>>(unitIDsBytes.data());
    }
    if (!AssignCoreBorrowedCount(unitIDsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.append = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SelectUnitArrayResult result{};
    state->native->selection->SelectUnitArray(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_sound_load_sound_def(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->soundApi == nullptr ||
        state->native->soundApi->LoadSoundDef == nullptr)
        return Trap("LoadSoundDef borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("LoadSoundDef borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    LoadSoundDefQuery query{};
    std::uint32_t soundNamePointer = 0;
    std::uint32_t soundNameCount = 0;
    if (!reader.U32(soundNamePointer) || !reader.U32(soundNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (soundNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> soundNameBytes;
    if (!state->memory.View(soundNamePointer, static_cast<std::size_t>(soundNameCount) + 1u, soundNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (soundNameBytes[soundNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.soundName = reinterpret_cast<const char*>(soundNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    LoadSoundDefResult result{};
    state->native->soundApi->LoadSoundDef(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_sound_play_sound_file(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->soundApi == nullptr ||
        state->native->soundApi->PlaySoundFile == nullptr)
        return Trap("PlaySoundFile borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("PlaySoundFile borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 32u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    PlaySoundFileQuery query{};
    std::uint32_t soundFilePointer = 0;
    std::uint32_t soundFileCount = 0;
    if (!reader.U32(soundFilePointer) || !reader.U32(soundFileCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (soundFileCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> soundFileBytes;
    if (!state->memory.View(soundFilePointer, static_cast<std::size_t>(soundFileCount) + 1u, soundFileBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (soundFileBytes[soundFileCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.soundFile = reinterpret_cast<const char*>(soundFileBytes.data());
    query.volume = slots[0].f32;
    if (!reader.F32(query.pos.x)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.pos.y)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.pos.z)) return Trap("borrowed Core wire underflow");
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!reader.F32(query.velocity.x)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.velocity.y)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.velocity.z)) return Trap("borrowed Core wire underflow");
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    query.channel = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.channel)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    PlaySoundFileResult result{};
    state->native->soundApi->PlaySoundFile(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_sound_play_sound_stream(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->soundApi == nullptr ||
        state->native->soundApi->PlaySoundStream == nullptr)
        return Trap("PlaySoundStream borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("PlaySoundStream borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    PlaySoundStreamQuery query{};
    std::uint32_t oggFilePointer = 0;
    std::uint32_t oggFileCount = 0;
    if (!reader.U32(oggFilePointer) || !reader.U32(oggFileCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (oggFileCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> oggFileBytes;
    if (!state->memory.View(oggFilePointer, static_cast<std::size_t>(oggFileCount) + 1u, oggFileBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (oggFileBytes[oggFileCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.oggFile = reinterpret_cast<const char*>(oggFileBytes.data());
    query.volume = slots[0].f32;
    query.enqueue = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    PlaySoundStreamResult result{};
    state->native->soundApi->PlaySoundStream(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_sound_preload_sound_item(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->soundApi == nullptr ||
        state->native->soundApi->PreloadSoundItem == nullptr)
        return Trap("PreloadSoundItem borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("PreloadSoundItem borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    PreloadSoundItemQuery query{};
    std::uint32_t soundNamePointer = 0;
    std::uint32_t soundNameCount = 0;
    if (!reader.U32(soundNamePointer) || !reader.U32(soundNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (soundNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> soundNameBytes;
    if (!state->memory.View(soundNamePointer, static_cast<std::size_t>(soundNameCount) + 1u, soundNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (soundNameBytes[soundNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.soundName = reinterpret_cast<const char*>(soundNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    PreloadSoundItemResult result{};
    state->native->soundApi->PreloadSoundItem(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_messages_send_commands(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->messages == nullptr ||
        state->native->messages->SendCommands == nullptr)
        return Trap("SendCommands borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("SendCommands borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SendCommandsQuery query{};
    std::uint32_t commandPointer = 0;
    std::uint32_t commandCount = 0;
    if (!reader.U32(commandPointer) || !reader.U32(commandCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (commandCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> commandBytes;
    if (!state->memory.View(commandPointer, static_cast<std::size_t>(commandCount) + 1u, commandBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (commandBytes[commandCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.command = reinterpret_cast<const char*>(commandBytes.data());
    std::uint32_t restPointer = 0;
    std::uint32_t restCount = 0;
    if (!reader.U32(restPointer) || !reader.U32(restCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (restCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> restBytes;
    if (!state->memory.View(restPointer, static_cast<std::size_t>(restCount) + 1u, restBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (restBytes[restCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.rest = reinterpret_cast<const char*>(restBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SendCommandsResult result{};
    state->native->messages->SendCommands(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_config_get_config_float(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->config == nullptr ||
        state->native->config->GetConfigFloat == nullptr)
        return Trap("GetConfigFloat borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("GetConfigFloat borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetConfigFloatQuery query{};
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (keyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, static_cast<std::size_t>(keyCount) + 1u, keyBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (keyBytes[keyCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.key = reinterpret_cast<const char*>(keyBytes.data());
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("borrowed Core option presence underflow");
        query.hasDefault = corePresent;
    }
    if (!reader.F32(query.defaultValue)) return Trap("borrowed Core wire underflow");
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetConfigFloatResult result{};
    state->native->config->GetConfigFloat(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.value)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_config_get_config_int(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->config == nullptr ||
        state->native->config->GetConfigInt == nullptr)
        return Trap("GetConfigInt borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("GetConfigInt borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetConfigIntQuery query{};
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (keyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, static_cast<std::size_t>(keyCount) + 1u, keyBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (keyBytes[keyCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.key = reinterpret_cast<const char*>(keyBytes.data());
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("borrowed Core option presence underflow");
        query.hasDefault = corePresent;
    }
    { std::int32_t coreRaw = 0; if (!reader.I32(coreRaw)) return Trap("borrowed Core wire underflow"); query.defaultValue = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.defaultValue)>>>(coreRaw); }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetConfigIntResult result{};
    state->native->config->GetConfigInt(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.I32(result.value)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_config_set_config_float(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->config == nullptr ||
        state->native->config->SetConfigFloat == nullptr)
        return Trap("SetConfigFloat borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("SetConfigFloat borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetConfigFloatQuery query{};
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (keyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, static_cast<std::size_t>(keyCount) + 1u, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (keyBytes[keyCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.key = reinterpret_cast<const char*>(keyBytes.data());
    query.value = slots[0].f32;
    query.useOverlay = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetConfigFloatResult result{};
    state->native->config->SetConfigFloat(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_config_set_config_int(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->config == nullptr ||
        state->native->config->SetConfigInt == nullptr)
        return Trap("SetConfigInt borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("SetConfigInt borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetConfigIntQuery query{};
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (keyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, static_cast<std::size_t>(keyCount) + 1u, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (keyBytes[keyCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.key = reinterpret_cast<const char*>(keyBytes.data());
    query.value = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.value)>>>(slots[0].i32);
    query.useOverlay = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetConfigIntResult result{};
    state->native->config->SetConfigInt(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_config_set_config_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->config == nullptr ||
        state->native->config->SetConfigString == nullptr)
        return Trap("SetConfigString borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetConfigString borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetConfigStringQuery query{};
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (keyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, static_cast<std::size_t>(keyCount) + 1u, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (keyBytes[keyCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.key = reinterpret_cast<const char*>(keyBytes.data());
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    query.useOverlay = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetConfigStringResult result{};
    state->native->config->SetConfigString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_config_set_log_section_filter_level(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->config == nullptr ||
        state->native->config->SetLogSectionFilterLevel == nullptr)
        return Trap("SetLogSectionFilterLevel borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetLogSectionFilterLevel borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetLogSectionFilterLevelQuery query{};
    std::uint32_t sectionPointer = 0;
    std::uint32_t sectionCount = 0;
    if (!reader.U32(sectionPointer) || !reader.U32(sectionCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (sectionCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> sectionBytes;
    if (!state->memory.View(sectionPointer, static_cast<std::size_t>(sectionCount) + 1u, sectionBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (sectionBytes[sectionCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.section = reinterpret_cast<const char*>(sectionBytes.data());
    query.level = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.level)>>>(slots[0].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetLogSectionFilterLevelResult result{};
    state->native->config->SetLogSectionFilterLevel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_utils_get_cegid(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->utils == nullptr ||
        state->native->utils->GetCEGID == nullptr)
        return Trap("GetCEGID borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("GetCEGID borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetCEGIDQuery query{};
    std::uint32_t cegNamePointer = 0;
    std::uint32_t cegNameCount = 0;
    if (!reader.U32(cegNamePointer) || !reader.U32(cegNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (cegNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> cegNameBytes;
    if (!state->memory.View(cegNamePointer, static_cast<std::size_t>(cegNameCount) + 1u, cegNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (cegNameBytes[cegNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.cegName = reinterpret_cast<const char*>(cegNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetCEGIDResult result{};
    state->native->utils->GetCEGID(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.cegID), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_assign_mouse_cursor(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->AssignMouseCursor == nullptr)
        return Trap("AssignMouseCursor borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("AssignMouseCursor borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    AssignMouseCursorQuery query{};
    std::uint32_t commandNamePointer = 0;
    std::uint32_t commandNameCount = 0;
    if (!reader.U32(commandNamePointer) || !reader.U32(commandNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (commandNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> commandNameBytes;
    if (!state->memory.View(commandNamePointer, static_cast<std::size_t>(commandNameCount) + 1u, commandNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (commandNameBytes[commandNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.commandName = reinterpret_cast<const char*>(commandNameBytes.data());
    std::uint32_t cursorFileNamePointer = 0;
    std::uint32_t cursorFileNameCount = 0;
    if (!reader.U32(cursorFileNamePointer) || !reader.U32(cursorFileNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (cursorFileNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> cursorFileNameBytes;
    if (!state->memory.View(cursorFileNamePointer, static_cast<std::size_t>(cursorFileNameCount) + 1u, cursorFileNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (cursorFileNameBytes[cursorFileNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.cursorFileName = reinterpret_cast<const char*>(cursorFileNameBytes.data());
    query.overwrite = slots[0].i32 != 0;
    query.hotSpotTopLeft = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    AssignMouseCursorResult result{};
    state->native->unsyncedCtrl->AssignMouseCursor(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_deselect_unit_map(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->DeselectUnitMap == nullptr)
        return Trap("DeselectUnitMap borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("DeselectUnitMap borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    DeselectUnitMapQuery query{};
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCount = 0;
    if (!reader.U32(unitIDsPointer) || !reader.U32(unitIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (unitIDsCount == 0) {
        query.unitIDs = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t unitIDsBytes64 = static_cast<std::uint64_t>(unitIDsCount) * 4u;
        if (unitIDsBytes64 > std::numeric_limits<std::size_t>::max() || (unitIDsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> unitIDsBytes;
        if (!state->memory.View(unitIDsPointer, static_cast<std::size_t>(unitIDsBytes64), unitIDsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::int32_t) == 4u, "borrowed Core/native element width mismatch");
        query.unitIDs = reinterpret_cast<std::remove_reference_t<decltype(query.unitIDs)>>(unitIDsBytes.data());
    }
    if (!AssignCoreBorrowedCount(unitIDsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    DeselectUnitMapResult result{};
    state->native->unsyncedCtrl->DeselectUnitMap(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_draw_unit_commands(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->DrawUnitCommands == nullptr)
        return Trap("DrawUnitCommands borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("DrawUnitCommands borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    DrawUnitCommandsQuery query{};
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCount = 0;
    if (!reader.U32(unitIDsPointer) || !reader.U32(unitIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (unitIDsCount == 0) {
        query.unitIDs = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t unitIDsBytes64 = static_cast<std::uint64_t>(unitIDsCount) * 4u;
        if (unitIDsBytes64 > std::numeric_limits<std::size_t>::max() || (unitIDsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> unitIDsBytes;
        if (!state->memory.View(unitIDsPointer, static_cast<std::size_t>(unitIDsBytes64), unitIDsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::int32_t) == 4u, "borrowed Core/native element width mismatch");
        query.unitIDs = reinterpret_cast<std::remove_reference_t<decltype(query.unitIDs)>>(unitIDsBytes.data());
    }
    if (!AssignCoreBorrowedCount(unitIDsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.tableOrArray = slots[0].i32 != 0;
    query.queueDrawDepth = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.queueDrawDepth)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    DrawUnitCommandsResult result{};
    state->native->unsyncedCtrl->DrawUnitCommands(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_load_cmd_colors_config(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->LoadCmdColorsConfig == nullptr)
        return Trap("LoadCmdColorsConfig borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("LoadCmdColorsConfig borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    LoadCmdColorsConfigQuery query{};
    std::uint32_t filenamePointer = 0;
    std::uint32_t filenameCount = 0;
    if (!reader.U32(filenamePointer) || !reader.U32(filenameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (filenameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> filenameBytes;
    if (!state->memory.View(filenamePointer, static_cast<std::size_t>(filenameCount) + 1u, filenameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (filenameBytes[filenameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.filename = reinterpret_cast<const char*>(filenameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    LoadCmdColorsConfigResult result{};
    state->native->unsyncedCtrl->LoadCmdColorsConfig(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_load_ctrl_panel_config(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->LoadCtrlPanelConfig == nullptr)
        return Trap("LoadCtrlPanelConfig borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("LoadCtrlPanelConfig borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    LoadCtrlPanelConfigQuery query{};
    std::uint32_t filenamePointer = 0;
    std::uint32_t filenameCount = 0;
    if (!reader.U32(filenamePointer) || !reader.U32(filenameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (filenameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> filenameBytes;
    if (!state->memory.View(filenamePointer, static_cast<std::size_t>(filenameCount) + 1u, filenameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (filenameBytes[filenameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.filename = reinterpret_cast<const char*>(filenameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    LoadCtrlPanelConfigResult result{};
    state->native->unsyncedCtrl->LoadCtrlPanelConfig(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_load_model_textures(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->LoadModelTextures == nullptr)
        return Trap("LoadModelTextures borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("LoadModelTextures borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    LoadModelTexturesQuery query{};
    std::uint32_t modelNamePointer = 0;
    std::uint32_t modelNameCount = 0;
    if (!reader.U32(modelNamePointer) || !reader.U32(modelNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (modelNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> modelNameBytes;
    if (!state->memory.View(modelNamePointer, static_cast<std::size_t>(modelNameCount) + 1u, modelNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (modelNameBytes[modelNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.modelName = reinterpret_cast<const char*>(modelNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    LoadModelTexturesResult result{};
    state->native->unsyncedCtrl->LoadModelTextures(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_replace_mouse_cursor(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->ReplaceMouseCursor == nullptr)
        return Trap("ReplaceMouseCursor borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ReplaceMouseCursor borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    ReplaceMouseCursorQuery query{};
    std::uint32_t oldCursorFileNamePointer = 0;
    std::uint32_t oldCursorFileNameCount = 0;
    if (!reader.U32(oldCursorFileNamePointer) || !reader.U32(oldCursorFileNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (oldCursorFileNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> oldCursorFileNameBytes;
    if (!state->memory.View(oldCursorFileNamePointer, static_cast<std::size_t>(oldCursorFileNameCount) + 1u, oldCursorFileNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (oldCursorFileNameBytes[oldCursorFileNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.oldCursorFileName = reinterpret_cast<const char*>(oldCursorFileNameBytes.data());
    std::uint32_t newCursorFileNamePointer = 0;
    std::uint32_t newCursorFileNameCount = 0;
    if (!reader.U32(newCursorFileNamePointer) || !reader.U32(newCursorFileNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (newCursorFileNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> newCursorFileNameBytes;
    if (!state->memory.View(newCursorFileNamePointer, static_cast<std::size_t>(newCursorFileNameCount) + 1u, newCursorFileNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (newCursorFileNameBytes[newCursorFileNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.newCursorFileName = reinterpret_cast<const char*>(newCursorFileNameBytes.data());
    query.hotSpotTopLeft = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    ReplaceMouseCursorResult result{};
    state->native->unsyncedCtrl->ReplaceMouseCursor(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_select_unit_map(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SelectUnitMap == nullptr)
        return Trap("SelectUnitMap borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SelectUnitMap borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SelectUnitMapQuery query{};
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCount = 0;
    if (!reader.U32(unitIDsPointer) || !reader.U32(unitIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (unitIDsCount == 0) {
        query.unitIDs = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t unitIDsBytes64 = static_cast<std::uint64_t>(unitIDsCount) * 4u;
        if (unitIDsBytes64 > std::numeric_limits<std::size_t>::max() || (unitIDsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> unitIDsBytes;
        if (!state->memory.View(unitIDsPointer, static_cast<std::size_t>(unitIDsBytes64), unitIDsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::int32_t) == 4u, "borrowed Core/native element width mismatch");
        query.unitIDs = reinterpret_cast<std::remove_reference_t<decltype(query.unitIDs)>>(unitIDsBytes.data());
    }
    if (!AssignCoreBorrowedCount(unitIDsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.append = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SelectUnitMapResult result{};
    state->native->unsyncedCtrl->SelectUnitMap(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_set_clipboard(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetClipboard == nullptr)
        return Trap("SetClipboard borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("SetClipboard borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetClipboardQuery query{};
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (textCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, static_cast<std::size_t>(textCount) + 1u, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (textBytes[textCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.text = reinterpret_cast<const char*>(textBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetClipboardResult result{};
    state->native->unsyncedCtrl->SetClipboard(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_set_map_shading_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetMapShadingTexture == nullptr)
        return Trap("SetMapShadingTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetMapShadingTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetMapShadingTextureQuery query{};
    std::uint32_t texTypePointer = 0;
    std::uint32_t texTypeCount = 0;
    if (!reader.U32(texTypePointer) || !reader.U32(texTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (texTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> texTypeBytes;
    if (!state->memory.View(texTypePointer, static_cast<std::size_t>(texTypeCount) + 1u, texTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (texTypeBytes[texTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.texType = reinterpret_cast<const char*>(texTypeBytes.data());
    std::uint32_t texNamePointer = 0;
    std::uint32_t texNameCount = 0;
    if (!reader.U32(texNamePointer) || !reader.U32(texNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (texNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> texNameBytes;
    if (!state->memory.View(texNamePointer, static_cast<std::size_t>(texNameCount) + 1u, texNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (texNameBytes[texNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.texName = reinterpret_cast<const char*>(texNameBytes.data());
    query.num = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.num)>>>(slots[0].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetMapShadingTextureResult result{};
    state->native->unsyncedCtrl->SetMapShadingTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_set_mouse_cursor(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetMouseCursor == nullptr)
        return Trap("SetMouseCursor borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetMouseCursor borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetMouseCursorQuery query{};
    std::uint32_t cursorNamePointer = 0;
    std::uint32_t cursorNameCount = 0;
    if (!reader.U32(cursorNamePointer) || !reader.U32(cursorNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (cursorNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> cursorNameBytes;
    if (!state->memory.View(cursorNamePointer, static_cast<std::size_t>(cursorNameCount) + 1u, cursorNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (cursorNameBytes[cursorNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.cursorName = reinterpret_cast<const char*>(cursorNameBytes.data());
    query.scale = slots[0].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetMouseCursorResult result{};
    state->native->unsyncedCtrl->SetMouseCursor(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_set_sky_box_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetSkyBoxTexture == nullptr)
        return Trap("SetSkyBoxTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("SetSkyBoxTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetSkyBoxTextureQuery query{};
    std::uint32_t texNamePointer = 0;
    std::uint32_t texNameCount = 0;
    if (!reader.U32(texNamePointer) || !reader.U32(texNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (texNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> texNameBytes;
    if (!state->memory.View(texNamePointer, static_cast<std::size_t>(texNameCount) + 1u, texNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (texNameBytes[texNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.texName = reinterpret_cast<const char*>(texNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetSkyBoxTextureResult result{};
    state->native->unsyncedCtrl->SetSkyBoxTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_set_unit_def_icon(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetUnitDefIcon == nullptr)
        return Trap("SetUnitDefIcon borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetUnitDefIcon borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitDefIconQuery query{};
    query.unitDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitDefID)>>>(slots[0].i32);
    std::uint32_t iconNamePointer = 0;
    std::uint32_t iconNameCount = 0;
    if (!reader.U32(iconNamePointer) || !reader.U32(iconNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (iconNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> iconNameBytes;
    if (!state->memory.View(iconNamePointer, static_cast<std::size_t>(iconNameCount) + 1u, iconNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (iconNameBytes[iconNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.iconName = reinterpret_cast<const char*>(iconNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitDefIconResult result{};
    state->native->unsyncedCtrl->SetUnitDefIcon(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_set_unit_def_image(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetUnitDefImage == nullptr)
        return Trap("SetUnitDefImage borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetUnitDefImage borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitDefImageQuery query{};
    query.unitDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitDefID)>>>(slots[0].i32);
    std::uint32_t imagePointer = 0;
    std::uint32_t imageCount = 0;
    if (!reader.U32(imagePointer) || !reader.U32(imageCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (imageCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> imageBytes;
    if (!state->memory.View(imagePointer, static_cast<std::size_t>(imageCount) + 1u, imageBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (imageBytes[imageCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.image = reinterpret_cast<const char*>(imageBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitDefImageResult result{};
    state->native->unsyncedCtrl->SetUnitDefImage(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_set_unit_icon(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetUnitIcon == nullptr)
        return Trap("SetUnitIcon borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetUnitIcon borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitIconQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t iconNamePointer = 0;
    std::uint32_t iconNameCount = 0;
    if (!reader.U32(iconNamePointer) || !reader.U32(iconNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (iconNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> iconNameBytes;
    if (!state->memory.View(iconNamePointer, static_cast<std::size_t>(iconNameCount) + 1u, iconNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (iconNameBytes[iconNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.iconName = reinterpret_cast<const char*>(iconNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitIconResult result{};
    state->native->unsyncedCtrl->SetUnitIcon(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_set_wm_caption(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetWMCaption == nullptr)
        return Trap("SetWMCaption borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("SetWMCaption borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetWMCaptionQuery query{};
    std::uint32_t titlePointer = 0;
    std::uint32_t titleCount = 0;
    if (!reader.U32(titlePointer) || !reader.U32(titleCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (titleCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> titleBytes;
    if (!state->memory.View(titlePointer, static_cast<std::size_t>(titleCount) + 1u, titleBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (titleBytes[titleCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.title = reinterpret_cast<const char*>(titleBytes.data());
    std::uint32_t titleShortPointer = 0;
    std::uint32_t titleShortCount = 0;
    if (!reader.U32(titleShortPointer) || !reader.U32(titleShortCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (titleShortCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> titleShortBytes;
    if (!state->memory.View(titleShortPointer, static_cast<std::size_t>(titleShortCount) + 1u, titleShortBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (titleShortBytes[titleShortCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.titleShort = reinterpret_cast<const char*>(titleShortBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetWMCaptionResult result{};
    state->native->unsyncedCtrl->SetWMCaption(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_set_wm_icon(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetWMIcon == nullptr)
        return Trap("SetWMIcon borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetWMIcon borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetWMIconQuery query{};
    std::uint32_t iconFileNamePointer = 0;
    std::uint32_t iconFileNameCount = 0;
    if (!reader.U32(iconFileNamePointer) || !reader.U32(iconFileNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (iconFileNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> iconFileNameBytes;
    if (!state->memory.View(iconFileNamePointer, static_cast<std::size_t>(iconFileNameCount) + 1u, iconFileNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (iconFileNameBytes[iconFileNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.iconFileName = reinterpret_cast<const char*>(iconFileNameBytes.data());
    query.forceResolution = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetWMIconResult result{};
    state->native->unsyncedCtrl->SetWMIcon(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unsynced_ctrl_set_water_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetWaterTexture == nullptr)
        return Trap("SetWaterTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("SetWaterTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetWaterTextureQuery query{};
    std::uint32_t texTypePointer = 0;
    std::uint32_t texTypeCount = 0;
    if (!reader.U32(texTypePointer) || !reader.U32(texTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (texTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> texTypeBytes;
    if (!state->memory.View(texTypePointer, static_cast<std::size_t>(texTypeCount) + 1u, texTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (texTypeBytes[texTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.texType = reinterpret_cast<const char*>(texTypeBytes.data());
    std::uint32_t texNamePointer = 0;
    std::uint32_t texNameCount = 0;
    if (!reader.U32(texNamePointer) || !reader.U32(texNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (texNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> texNameBytes;
    if (!state->memory.View(texNamePointer, static_cast<std::size_t>(texNameCount) + 1u, texNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (texNameBytes[texNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.texName = reinterpret_cast<const char*>(texNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetWaterTextureResult result{};
    state->native->unsyncedCtrl->SetWaterTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_add_atlas_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->AddAtlasTexture == nullptr)
        return Trap("AddAtlasTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("AddAtlasTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxAtlasTextureQuery query{};
    std::uint32_t atlasNamePointer = 0;
    std::uint32_t atlasNameCount = 0;
    if (!reader.U32(atlasNamePointer) || !reader.U32(atlasNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (atlasNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> atlasNameBytes;
    if (!state->memory.View(atlasNamePointer, static_cast<std::size_t>(atlasNameCount) + 1u, atlasNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (atlasNameBytes[atlasNameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.atlasName = reinterpret_cast<const char*>(atlasNameBytes.data());
    std::uint32_t textureNamePointer = 0;
    std::uint32_t textureNameCount = 0;
    if (!reader.U32(textureNamePointer) || !reader.U32(textureNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (textureNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textureNameBytes;
    if (!state->memory.View(textureNamePointer, static_cast<std::size_t>(textureNameCount) + 1u, textureNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (textureNameBytes[textureNameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.textureName = reinterpret_cast<const char*>(textureNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->AddAtlasTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_add_fallback_font(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->AddFallbackFont == nullptr)
        return Trap("AddFallbackFont borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("AddFallbackFont borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxStringQuery query{};
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->AddFallbackFont(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_add_feature_defs_to_submission_vao(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->AddFeatureDefsToSubmissionVAO == nullptr)
        return Trap("AddFeatureDefsToSubmissionVAO borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("AddFeatureDefsToSubmissionVAO borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVAOSubmissionQuery query{};
    query.vaoID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vaoID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (idsCount == 0) {
        query.ids = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
        if (idsBytes64 > std::numeric_limits<std::size_t>::max() || (idsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> idsBytes;
        if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::uint32_t) == 4u, "borrowed Core/native element width mismatch");
        query.ids = reinterpret_cast<std::remove_reference_t<decltype(query.ids)>>(idsBytes.data());
    }
    if (!AssignCoreBorrowedCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->AddFeatureDefsToSubmissionVAO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_add_features_to_submission_vao(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->AddFeaturesToSubmissionVAO == nullptr)
        return Trap("AddFeaturesToSubmissionVAO borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("AddFeaturesToSubmissionVAO borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVAOSubmissionQuery query{};
    query.vaoID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vaoID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (idsCount == 0) {
        query.ids = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
        if (idsBytes64 > std::numeric_limits<std::size_t>::max() || (idsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> idsBytes;
        if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::uint32_t) == 4u, "borrowed Core/native element width mismatch");
        query.ids = reinterpret_cast<std::remove_reference_t<decltype(query.ids)>>(idsBytes.data());
    }
    if (!AssignCoreBorrowedCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->AddFeaturesToSubmissionVAO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_add_unit_defs_to_submission_vao(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->AddUnitDefsToSubmissionVAO == nullptr)
        return Trap("AddUnitDefsToSubmissionVAO borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("AddUnitDefsToSubmissionVAO borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVAOSubmissionQuery query{};
    query.vaoID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vaoID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (idsCount == 0) {
        query.ids = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
        if (idsBytes64 > std::numeric_limits<std::size_t>::max() || (idsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> idsBytes;
        if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::uint32_t) == 4u, "borrowed Core/native element width mismatch");
        query.ids = reinterpret_cast<std::remove_reference_t<decltype(query.ids)>>(idsBytes.data());
    }
    if (!AssignCoreBorrowedCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->AddUnitDefsToSubmissionVAO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_add_units_to_submission_vao(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->AddUnitsToSubmissionVAO == nullptr)
        return Trap("AddUnitsToSubmissionVAO borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("AddUnitsToSubmissionVAO borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVAOSubmissionQuery query{};
    query.vaoID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vaoID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (idsCount == 0) {
        query.ids = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
        if (idsBytes64 > std::numeric_limits<std::size_t>::max() || (idsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> idsBytes;
        if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::uint32_t) == 4u, "borrowed Core/native element width mismatch");
        query.ids = reinterpret_cast<std::remove_reference_t<decltype(query.ids)>>(idsBytes.data());
    }
    if (!AssignCoreBorrowedCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->AddUnitsToSubmissionVAO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_bind_image_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->BindImageTexture == nullptr)
        return Trap("BindImageTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 7u)
        return Trap("BindImageTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 8u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[6].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxBindImageTextureQuery query{};
    query.unit = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unit)>>>(slots[0].i32);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.level = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.level)>>>(slots[1].i32);
    query.layer = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.layer)>>>(slots[2].i32);
    query.layered = slots[3].i32 != 0;
    query.access = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.access)>>>(slots[4].i32);
    query.format = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.format)>>>(slots[5].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->BindImageTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_bind_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->BindTexture == nullptr)
        return Trap("BindTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("BindTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextureBindQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.texNum = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.texNum)>>>(slots[0].i32);
    query.enable = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->BindTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_change_texture_params(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->ChangeTextureParams == nullptr)
        return Trap("ChangeTextureParams borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("ChangeTextureParams borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 64u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxChangeTextureParamsQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("borrowed Core wire underflow"); query.params.target = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.target)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("borrowed Core wire underflow"); query.params.format = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.format)>>>(coreRaw); }
    { std::int32_t coreRaw = 0; if (!reader.I32(coreRaw)) return Trap("borrowed Core wire underflow"); query.params.border = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.border)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("borrowed Core wire underflow"); query.params.minFilter = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.minFilter)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("borrowed Core wire underflow"); query.params.magFilter = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.magFilter)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("borrowed Core wire underflow"); query.params.wrapS = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.wrapS)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("borrowed Core wire underflow"); query.params.wrapT = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.wrapT)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("borrowed Core wire underflow"); query.params.wrapR = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.wrapR)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("borrowed Core wire underflow"); query.params.compareFunc = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.compareFunc)>>>(coreRaw); }
    if (!reader.F32(query.params.lodBias)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.params.aniso)) return Trap("borrowed Core wire underflow");
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("borrowed Core wire underflow"); query.params.samples = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.samples)>>>(coreRaw); }
    if (!reader.Bool(query.params.fbo)) return Trap("borrowed Core wire underflow");
    if (!reader.Bool(query.params.fboDepth)) return Trap("borrowed Core wire underflow");
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->ChangeTextureParams(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_copy_to_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->CopyToTexture == nullptr)
        return Trap("CopyToTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 9u)
        return Trap("CopyToTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 10u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[8].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxCopyToTextureQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.xoff = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.xoff)>>>(slots[0].i32);
    query.yoff = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.yoff)>>>(slots[1].i32);
    query.x = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.x)>>>(slots[2].i32);
    query.y = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.y)>>>(slots[3].i32);
    query.width = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.width)>>>(slots[4].i32);
    query.height = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.height)>>>(slots[5].i32);
    query.target = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.target)>>>(slots[6].i32);
    query.level = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.level)>>>(slots[7].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->CopyToTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_create_shader(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->CreateShader == nullptr)
        return Trap("CreateShader borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("CreateShader borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 80u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxCreateShaderQuery query{};
    std::uint32_t definitionsPointer = 0;
    std::uint32_t definitionsCount = 0;
    if (!reader.U32(definitionsPointer) || !reader.U32(definitionsCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (definitionsCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> definitionsBytes;
    if (!state->memory.View(definitionsPointer, static_cast<std::size_t>(definitionsCount) + 1u, definitionsBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (definitionsBytes[definitionsCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.definitions = reinterpret_cast<const char*>(definitionsBytes.data());
    std::uint32_t vertexPointer = 0;
    std::uint32_t vertexCount = 0;
    if (!reader.U32(vertexPointer) || !reader.U32(vertexCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (vertexCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> vertexBytes;
    if (!state->memory.View(vertexPointer, static_cast<std::size_t>(vertexCount) + 1u, vertexBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (vertexBytes[vertexCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.vertex = reinterpret_cast<const char*>(vertexBytes.data());
    std::uint32_t tcsPointer = 0;
    std::uint32_t tcsCount = 0;
    if (!reader.U32(tcsPointer) || !reader.U32(tcsCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (tcsCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> tcsBytes;
    if (!state->memory.View(tcsPointer, static_cast<std::size_t>(tcsCount) + 1u, tcsBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (tcsBytes[tcsCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.tcs = reinterpret_cast<const char*>(tcsBytes.data());
    std::uint32_t tesPointer = 0;
    std::uint32_t tesCount = 0;
    if (!reader.U32(tesPointer) || !reader.U32(tesCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (tesCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> tesBytes;
    if (!state->memory.View(tesPointer, static_cast<std::size_t>(tesCount) + 1u, tesBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (tesBytes[tesCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.tes = reinterpret_cast<const char*>(tesBytes.data());
    std::uint32_t geometryPointer = 0;
    std::uint32_t geometryCount = 0;
    if (!reader.U32(geometryPointer) || !reader.U32(geometryCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (geometryCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> geometryBytes;
    if (!state->memory.View(geometryPointer, static_cast<std::size_t>(geometryCount) + 1u, geometryBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (geometryBytes[geometryCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.geometry = reinterpret_cast<const char*>(geometryBytes.data());
    std::uint32_t fragmentPointer = 0;
    std::uint32_t fragmentCount = 0;
    if (!reader.U32(fragmentPointer) || !reader.U32(fragmentCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (fragmentCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> fragmentBytes;
    if (!state->memory.View(fragmentPointer, static_cast<std::size_t>(fragmentCount) + 1u, fragmentBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (fragmentBytes[fragmentCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.fragment = reinterpret_cast<const char*>(fragmentBytes.data());
    std::uint32_t computePointer = 0;
    std::uint32_t computeCount = 0;
    if (!reader.U32(computePointer) || !reader.U32(computeCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (computeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> computeBytes;
    if (!state->memory.View(computePointer, static_cast<std::size_t>(computeCount) + 1u, computeBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (computeBytes[computeCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.compute = reinterpret_cast<const char*>(computeBytes.data());
    if (!reader.Bool(query.options.hasGeoInputType)) return Trap("borrowed Core wire underflow");
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("borrowed Core wire underflow"); query.options.geoInputType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options.geoInputType)>>>(coreRaw); }
    if (!reader.Bool(query.options.hasGeoOutputType)) return Trap("borrowed Core wire underflow");
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("borrowed Core wire underflow"); query.options.geoOutputType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options.geoOutputType)>>>(coreRaw); }
    if (!reader.Bool(query.options.hasGeoOutputVerts)) return Trap("borrowed Core wire underflow");
    { std::int32_t coreRaw = 0; if (!reader.I32(coreRaw)) return Trap("borrowed Core wire underflow"); query.options.geoOutputVerts = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options.geoOutputVerts)>>>(coreRaw); }
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxCreateShaderResult result{};
    state->native->gfx->CreateShader(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.U32(result.shaderID)) return Trap("borrowed Core wire overflow");
    if (!writer.U32(result.glProgramID)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_delete_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->DeleteTexture == nullptr)
        return Trap("DeleteTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("DeleteTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextureNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->DeleteTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_delete_texture_atlas(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->DeleteTextureAtlas == nullptr)
        return Trap("DeleteTextureAtlas borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("DeleteTextureAtlas borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextureNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->DeleteTextureAtlas(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_delete_texture_fbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->DeleteTextureFBO == nullptr)
        return Trap("DeleteTextureFBO borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("DeleteTextureFBO borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextureNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->DeleteTextureFBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_finalize_texture_atlas(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->FinalizeTextureAtlas == nullptr)
        return Trap("FinalizeTextureAtlas borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("FinalizeTextureAtlas borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextureNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->FinalizeTextureAtlas(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_font_get_text_height(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->FontGetTextHeight == nullptr)
        return Trap("FontGetTextHeight borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 6u)
        return Trap("FontGetTextHeight borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 7u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[5].i32);
    if (!state->memory.Contains(output, 12u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxFontTextQuery query{};
    query.fontID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fontID)>>>(slots[0].i32);
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (textCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, static_cast<std::size_t>(textCount) + 1u, textBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (textBytes[textCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.text = reinterpret_cast<const char*>(textBytes.data());
    query.x = slots[1].f32;
    query.y = slots[2].f32;
    query.size = slots[3].f32;
    std::uint32_t optionsPointer = 0;
    std::uint32_t optionsCount = 0;
    if (!reader.U32(optionsPointer) || !reader.U32(optionsCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (optionsCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> optionsBytes;
    if (!state->memory.View(optionsPointer, static_cast<std::size_t>(optionsCount) + 1u, optionsBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (optionsBytes[optionsCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.options = reinterpret_cast<const char*>(optionsBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxTextHeightResult result{};
    state->native->gfx->FontGetTextHeight(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 12> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.height)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.descender)) return Trap("borrowed Core wire overflow");
    if (!writer.I32(result.lines)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_font_get_text_width(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->FontGetTextWidth == nullptr)
        return Trap("FontGetTextWidth borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 5u)
        return Trap("FontGetTextWidth borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxFontTextQuery query{};
    query.fontID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fontID)>>>(slots[0].i32);
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (textCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, static_cast<std::size_t>(textCount) + 1u, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (textBytes[textCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.text = reinterpret_cast<const char*>(textBytes.data());
    query.x = slots[1].f32;
    query.y = slots[2].f32;
    query.size = slots[3].f32;
    std::uint32_t optionsPointer = 0;
    std::uint32_t optionsCount = 0;
    if (!reader.U32(optionsPointer) || !reader.U32(optionsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (optionsCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> optionsBytes;
    if (!state->memory.View(optionsPointer, static_cast<std::size_t>(optionsCount) + 1u, optionsBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (optionsBytes[optionsCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.options = reinterpret_cast<const char*>(optionsBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxFloatResult result{};
    state->native->gfx->FontGetTextWidth(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(std::bit_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_font_print(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->FontPrint == nullptr)
        return Trap("FontPrint borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 5u)
        return Trap("FontPrint borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxFontTextQuery query{};
    query.fontID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fontID)>>>(slots[0].i32);
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (textCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, static_cast<std::size_t>(textCount) + 1u, textBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (textBytes[textCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.text = reinterpret_cast<const char*>(textBytes.data());
    query.x = slots[1].f32;
    query.y = slots[2].f32;
    query.size = slots[3].f32;
    std::uint32_t optionsPointer = 0;
    std::uint32_t optionsCount = 0;
    if (!reader.U32(optionsPointer) || !reader.U32(optionsCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (optionsCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> optionsBytes;
    if (!state->memory.View(optionsPointer, static_cast<std::size_t>(optionsCount) + 1u, optionsBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (optionsBytes[optionsCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.options = reinterpret_cast<const char*>(optionsBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->FontPrint(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_font_print_world(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->FontPrintWorld == nullptr)
        return Trap("FontPrintWorld borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("FontPrintWorld borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 28u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxFontWorldTextQuery query{};
    query.fontID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fontID)>>>(slots[0].i32);
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (textCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, static_cast<std::size_t>(textCount) + 1u, textBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (textBytes[textCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.text = reinterpret_cast<const char*>(textBytes.data());
    if (!reader.F32(query.pos.x)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.pos.y)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.pos.z)) return Trap("borrowed Core wire underflow");
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    query.size = slots[1].f32;
    std::uint32_t optionsPointer = 0;
    std::uint32_t optionsCount = 0;
    if (!reader.U32(optionsPointer) || !reader.U32(optionsCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (optionsCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> optionsBytes;
    if (!state->memory.View(optionsPointer, static_cast<std::size_t>(optionsCount) + 1u, optionsBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (optionsBytes[optionsCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.options = reinterpret_cast<const char*>(optionsBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->FontPrintWorld(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_generate_mipmap(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GenerateMipmap == nullptr)
        return Trap("GenerateMipmap borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("GenerateMipmap borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextureNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->GenerateMipmap(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_get_atlas_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetAtlasTexture == nullptr)
        return Trap("GetAtlasTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("GetAtlasTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 20u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxAtlasTextureQuery query{};
    std::uint32_t atlasNamePointer = 0;
    std::uint32_t atlasNameCount = 0;
    if (!reader.U32(atlasNamePointer) || !reader.U32(atlasNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (atlasNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> atlasNameBytes;
    if (!state->memory.View(atlasNamePointer, static_cast<std::size_t>(atlasNameCount) + 1u, atlasNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (atlasNameBytes[atlasNameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.atlasName = reinterpret_cast<const char*>(atlasNameBytes.data());
    std::uint32_t textureNamePointer = 0;
    std::uint32_t textureNameCount = 0;
    if (!reader.U32(textureNamePointer) || !reader.U32(textureNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (textureNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textureNameBytes;
    if (!state->memory.View(textureNamePointer, static_cast<std::size_t>(textureNameCount) + 1u, textureNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (textureNameBytes[textureNameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.textureName = reinterpret_cast<const char*>(textureNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxAtlasTextureResult result{};
    state->native->gfx->GetAtlasTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 20> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.x1)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.x2)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.y1)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.y2)) return Trap("borrowed Core wire overflow");
    if (!writer.I32(result.pageNum)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_get_fixed_state(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetFixedState == nullptr)
        return Trap("GetFixedState borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("GetFixedState borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 172u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxFixedStateQuery query{};
    std::uint32_t paramPointer = 0;
    std::uint32_t paramCount = 0;
    if (!reader.U32(paramPointer) || !reader.U32(paramCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (paramCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> paramBytes;
    if (!state->memory.View(paramPointer, static_cast<std::size_t>(paramCount) + 1u, paramBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (paramBytes[paramCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.param = reinterpret_cast<const char*>(paramBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxFixedStateResult result{};
    state->native->gfx->GetFixedState(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 172> wire{};
    WireWriter writer(wire);
    for (std::size_t borrowedWriteIndex1=0; borrowedWriteIndex1<8u; ++borrowedWriteIndex1) {
        if (!writer.Bool(result.bools[borrowedWriteIndex1])) return Trap("borrowed Core wire overflow");
    }
    if (!writer.U32(result.boolCount)) return Trap("borrowed Core wire overflow");
    for (std::size_t borrowedWriteIndex1=0; borrowedWriteIndex1<16u; ++borrowedWriteIndex1) {
        if (!writer.I32(result.ints[borrowedWriteIndex1])) return Trap("borrowed Core wire overflow");
    }
    if (!writer.U32(result.intCount)) return Trap("borrowed Core wire overflow");
    for (std::size_t borrowedWriteIndex1=0; borrowedWriteIndex1<16u; ++borrowedWriteIndex1) {
        if (!writer.F32(result.floats[borrowedWriteIndex1])) return Trap("borrowed Core wire overflow");
    }
    if (!writer.U32(result.floatCount)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_get_global_tex_coords(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetGlobalTexCoords == nullptr)
        return Trap("GetGlobalTexCoords borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("GetGlobalTexCoords borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 20u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxStringQuery query{};
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxAtlasTextureResult result{};
    state->native->gfx->GetGlobalTexCoords(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 20> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.x1)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.x2)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.y1)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.y2)) return Trap("borrowed Core wire overflow");
    if (!writer.I32(result.pageNum)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_get_subroutine_index(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetSubroutineIndex == nullptr)
        return Trap("GetSubroutineIndex borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("GetSubroutineIndex borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxSubroutineIndexQuery query{};
    query.shaderID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.shaderID)>>>(slots[0].i32);
    query.shaderType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.shaderType)>>>(slots[1].i32);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxSubroutineIndexResult result{};
    state->native->gfx->GetSubroutineIndex(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.I32(result.index)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_get_text_height(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetTextHeight == nullptr)
        return Trap("GetTextHeight borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("GetTextHeight borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 12u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxStringQuery query{};
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxTextHeightResult result{};
    state->native->gfx->GetTextHeight(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 12> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.height)) return Trap("borrowed Core wire overflow");
    if (!writer.F32(result.descender)) return Trap("borrowed Core wire overflow");
    if (!writer.I32(result.lines)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_get_text_width(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetTextWidth == nullptr)
        return Trap("GetTextWidth borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("GetTextWidth borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxStringQuery query{};
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxFloatResult result{};
    state->native->gfx->GetTextWidth(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(std::bit_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_get_uniform_location(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetUniformLocation == nullptr)
        return Trap("GetUniformLocation borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("GetUniformLocation borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxUniformLocationQuery query{};
    query.shaderID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.shaderID)>>>(slots[0].i32);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUniformLocationResult result{};
    state->native->gfx->GetUniformLocation(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.location), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_has_extension(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->HasExtension == nullptr)
        return Trap("HasExtension borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("HasExtension borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxStringQuery query{};
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->HasExtension(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_instance_data_from_feature_defs_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->InstanceDataFromFeatureDefsVBO == nullptr)
        return Trap("InstanceDataFromFeatureDefsVBO borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 5u)
        return Trap("InstanceDataFromFeatureDefsVBO borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBOInstanceDataQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (idsCount == 0) {
        query.ids = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
        if (idsBytes64 > std::numeric_limits<std::size_t>::max() || (idsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> idsBytes;
        if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::uint32_t) == 4u, "borrowed Core/native element width mismatch");
        query.ids = reinterpret_cast<std::remove_reference_t<decltype(query.ids)>>(idsBytes.data());
    }
    if (!AssignCoreBorrowedCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[2].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[3].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->InstanceDataFromFeatureDefsVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_instance_data_from_features_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->InstanceDataFromFeaturesVBO == nullptr)
        return Trap("InstanceDataFromFeaturesVBO borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 5u)
        return Trap("InstanceDataFromFeaturesVBO borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBOInstanceDataQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (idsCount == 0) {
        query.ids = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
        if (idsBytes64 > std::numeric_limits<std::size_t>::max() || (idsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> idsBytes;
        if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::uint32_t) == 4u, "borrowed Core/native element width mismatch");
        query.ids = reinterpret_cast<std::remove_reference_t<decltype(query.ids)>>(idsBytes.data());
    }
    if (!AssignCoreBorrowedCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[2].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[3].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->InstanceDataFromFeaturesVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_instance_data_from_unit_defs_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->InstanceDataFromUnitDefsVBO == nullptr)
        return Trap("InstanceDataFromUnitDefsVBO borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 5u)
        return Trap("InstanceDataFromUnitDefsVBO borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBOInstanceDataQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (idsCount == 0) {
        query.ids = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
        if (idsBytes64 > std::numeric_limits<std::size_t>::max() || (idsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> idsBytes;
        if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::uint32_t) == 4u, "borrowed Core/native element width mismatch");
        query.ids = reinterpret_cast<std::remove_reference_t<decltype(query.ids)>>(idsBytes.data());
    }
    if (!AssignCoreBorrowedCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[2].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[3].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->InstanceDataFromUnitDefsVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_instance_data_from_units_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->InstanceDataFromUnitsVBO == nullptr)
        return Trap("InstanceDataFromUnitsVBO borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 5u)
        return Trap("InstanceDataFromUnitsVBO borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBOInstanceDataQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (idsCount == 0) {
        query.ids = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
        if (idsBytes64 > std::numeric_limits<std::size_t>::max() || (idsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> idsBytes;
        if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::uint32_t) == 4u, "borrowed Core/native element width mismatch");
        query.ids = reinterpret_cast<std::remove_reference_t<decltype(query.ids)>>(idsBytes.data());
    }
    if (!AssignCoreBorrowedCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[2].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[3].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->InstanceDataFromUnitsVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_load_font(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->LoadFont == nullptr)
        return Trap("LoadFont borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("LoadFont borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxLoadFontQuery query{};
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCount = 0;
    if (!reader.U32(pathPointer) || !reader.U32(pathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (pathCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pathBytes;
    if (!state->memory.View(pathPointer, static_cast<std::size_t>(pathCount) + 1u, pathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (pathBytes[pathCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.path = reinterpret_cast<const char*>(pathBytes.data());
    query.size = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.size)>>>(slots[0].i32);
    query.outlineWidth = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.outlineWidth)>>>(slots[1].i32);
    query.outlineWeight = slots[2].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxFontResult result{};
    state->native->gfx->LoadFont(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.fontID), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_matrix_data_from_projectiles_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->MatrixDataFromProjectilesVBO == nullptr)
        return Trap("MatrixDataFromProjectilesVBO borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 5u)
        return Trap("MatrixDataFromProjectilesVBO borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBOInstanceDataQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (idsCount == 0) {
        query.ids = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
        if (idsBytes64 > std::numeric_limits<std::size_t>::max() || (idsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> idsBytes;
        if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::uint32_t) == 4u, "borrowed Core/native element width mismatch");
        query.ids = reinterpret_cast<std::remove_reference_t<decltype(query.ids)>>(idsBytes.data());
    }
    if (!AssignCoreBorrowedCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[2].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[3].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->MatrixDataFromProjectilesVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_object_label(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->ObjectLabel == nullptr)
        return Trap("ObjectLabel borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ObjectLabel borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxObjectLabelQuery query{};
    query.identifier = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.identifier)>>>(slots[0].i32);
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[1].i32);
    std::uint32_t labelPointer = 0;
    std::uint32_t labelCount = 0;
    if (!reader.U32(labelPointer) || !reader.U32(labelCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (labelCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> labelBytes;
    if (!state->memory.View(labelPointer, static_cast<std::size_t>(labelCount) + 1u, labelBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (labelBytes[labelCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.label = reinterpret_cast<const char*>(labelBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->ObjectLabel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_push_debug_group(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->PushDebugGroup == nullptr)
        return Trap("PushDebugGroup borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("PushDebugGroup borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxPushDebugGroupQuery query{};
    query.id = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.id)>>>(slots[0].i32);
    std::uint32_t messagePointer = 0;
    std::uint32_t messageCount = 0;
    if (!reader.U32(messagePointer) || !reader.U32(messageCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (messageCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> messageBytes;
    if (!state->memory.View(messagePointer, static_cast<std::size_t>(messageCount) + 1u, messageBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (messageBytes[messageCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.message = reinterpret_cast<const char*>(messageBytes.data());
    query.sourceIsThirdParty = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->PushDebugGroup(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_save_image(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->SaveImage == nullptr)
        return Trap("SaveImage borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 6u)
        return Trap("SaveImage borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 7u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[5].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 20u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxSaveImageQuery query{};
    query.x = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.x)>>>(slots[0].i32);
    query.y = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.y)>>>(slots[1].i32);
    query.width = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.width)>>>(slots[2].i32);
    query.height = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.height)>>>(slots[3].i32);
    std::uint32_t filenamePointer = 0;
    std::uint32_t filenameCount = 0;
    if (!reader.U32(filenamePointer) || !reader.U32(filenameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (filenameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> filenameBytes;
    if (!state->memory.View(filenamePointer, static_cast<std::size_t>(filenameCount) + 1u, filenameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (filenameBytes[filenameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.filename = reinterpret_cast<const char*>(filenameBytes.data());
    if (!reader.Bool(query.options.alpha)) return Trap("borrowed Core wire underflow");
    if (!reader.Bool(query.options.yflip)) return Trap("borrowed Core wire underflow");
    if (!reader.Bool(query.options.grayscale16bit)) return Trap("borrowed Core wire underflow");
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    query.readBuffer = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.readBuffer)>>>(slots[4].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->SaveImage(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_set_fbo_attachment(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->SetFBOAttachment == nullptr)
        return Trap("SetFBOAttachment borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 7u)
        return Trap("SetFBOAttachment borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 8u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[6].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxFBOAttachmentQuery query{};
    query.fboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fboID)>>>(slots[0].i32);
    query.attachment = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attachment)>>>(slots[1].i32);
    std::uint32_t textureNamePointer = 0;
    std::uint32_t textureNameCount = 0;
    if (!reader.U32(textureNamePointer) || !reader.U32(textureNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (textureNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textureNameBytes;
    if (!state->memory.View(textureNamePointer, static_cast<std::size_t>(textureNameCount) + 1u, textureNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (textureNameBytes[textureNameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.textureName = reinterpret_cast<const char*>(textureNameBytes.data());
    query.textureTarget = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.textureTarget)>>>(slots[2].i32);
    query.mipLevel = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.mipLevel)>>>(slots[3].i32);
    query.rboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.rboID)>>>(slots[4].i32);
    query.useRBO = slots[5].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->SetFBOAttachment(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_set_fbo_draw_buffers(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->SetFBODrawBuffers == nullptr)
        return Trap("SetFBODrawBuffers borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetFBODrawBuffers borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxFBODrawBuffersQuery query{};
    query.fboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fboID)>>>(slots[0].i32);
    std::uint32_t buffersPointer = 0;
    std::uint32_t buffersCount = 0;
    if (!reader.U32(buffersPointer) || !reader.U32(buffersCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (buffersCount == 0) {
        query.buffers = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i32 = static_cast<std::int32_t>(Status::NotAvailable);
            return nullptr;
        }
        const std::uint64_t buffersBytes64 = static_cast<std::uint64_t>(buffersCount) * 4u;
        if (buffersBytes64 > std::numeric_limits<std::size_t>::max() || (buffersPointer % 4u) != 0u) {
            slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
            return nullptr;
        }
        std::span<const std::uint8_t> buffersBytes;
        if (!state->memory.View(buffersPointer, static_cast<std::size_t>(buffersBytes64), buffersBytes)) {
            slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
            return nullptr;
        }
        static_assert(sizeof(std::uint32_t) == 4u, "borrowed Core/native element width mismatch");
        query.buffers = reinterpret_cast<std::remove_reference_t<decltype(query.buffers)>>(buffersBytes.data());
    }
    if (!AssignCoreBorrowedCount(buffersCount, query.bufferCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->SetFBODrawBuffers(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_set_feature_buffer_uniforms(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->SetFeatureBufferUniforms == nullptr)
        return Trap("SetFeatureBufferUniforms borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("SetFeatureBufferUniforms borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxObjectBufferUniformsQuery query{};
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[0].i32);
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCount = 0;
    if (!reader.U32(valuesPointer) || !reader.U32(valuesCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valuesCount == 0) {
        query.values = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t valuesBytes64 = static_cast<std::uint64_t>(valuesCount) * 4u;
        if (valuesBytes64 > std::numeric_limits<std::size_t>::max() || (valuesPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> valuesBytes;
        if (!state->memory.View(valuesPointer, static_cast<std::size_t>(valuesBytes64), valuesBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(float) == 4u, "borrowed Core/native element width mismatch");
        query.values = reinterpret_cast<std::remove_reference_t<decltype(query.values)>>(valuesBytes.data());
    }
    if (!AssignCoreBorrowedCount(valuesCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.offset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.offset)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxObjectBufferUniformsResult result{};
    state->native->gfx->SetFeatureBufferUniforms(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.count), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_set_unit_buffer_uniforms(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->SetUnitBufferUniforms == nullptr)
        return Trap("SetUnitBufferUniforms borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("SetUnitBufferUniforms borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxObjectBufferUniformsQuery query{};
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[0].i32);
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCount = 0;
    if (!reader.U32(valuesPointer) || !reader.U32(valuesCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valuesCount == 0) {
        query.values = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t valuesBytes64 = static_cast<std::uint64_t>(valuesCount) * 4u;
        if (valuesBytes64 > std::numeric_limits<std::size_t>::max() || (valuesPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> valuesBytes;
        if (!state->memory.View(valuesPointer, static_cast<std::size_t>(valuesBytes64), valuesBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(float) == 4u, "borrowed Core/native element width mismatch");
        query.values = reinterpret_cast<std::remove_reference_t<decltype(query.values)>>(valuesBytes.data());
    }
    if (!AssignCoreBorrowedCount(valuesCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.offset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.offset)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxObjectBufferUniformsResult result{};
    state->native->gfx->SetUnitBufferUniforms(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.count), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_text(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->Text == nullptr)
        return Trap("Text borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("Text borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextQuery query{};
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (textCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, static_cast<std::size_t>(textCount) + 1u, textBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (textBytes[textCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.text = reinterpret_cast<const char*>(textBytes.data());
    query.x = slots[0].f32;
    query.y = slots[1].f32;
    query.size = slots[2].f32;
    std::uint32_t optionsPointer = 0;
    std::uint32_t optionsCount = 0;
    if (!reader.U32(optionsPointer) || !reader.U32(optionsCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (optionsCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> optionsBytes;
    if (!state->memory.View(optionsPointer, static_cast<std::size_t>(optionsCount) + 1u, optionsBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (optionsBytes[optionsCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.options = reinterpret_cast<const char*>(optionsBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->Text(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_texture_info(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->TextureInfo == nullptr)
        return Trap("TextureInfo borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("TextureInfo borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 24u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxTextureNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxTextureInfoResult result{};
    state->native->gfx->TextureInfo(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 24> wire{};
    WireWriter writer(wire);
    if (!writer.I32(result.xsize)) return Trap("borrowed Core wire overflow");
    if (!writer.I32(result.ysize)) return Trap("borrowed Core wire overflow");
    if (!writer.I32(result.zsize)) return Trap("borrowed Core wire overflow");
    if (!writer.U32(result.id)) return Trap("borrowed Core wire overflow");
    if (!writer.U32(result.target)) return Trap("borrowed Core wire overflow");
    if (!writer.U32(result.fbo)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_uniform_array_float(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->UniformArrayFloat == nullptr)
        return Trap("UniformArrayFloat borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("UniformArrayFloat borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxUniformArrayFloatQuery query{};
    query.location = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.location)>>>(slots[0].i32);
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCount = 0;
    if (!reader.U32(valuesPointer) || !reader.U32(valuesCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (valuesCount == 0) {
        query.values = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i32 = static_cast<std::int32_t>(Status::NotAvailable);
            return nullptr;
        }
        const std::uint64_t valuesBytes64 = static_cast<std::uint64_t>(valuesCount) * 4u;
        if (valuesBytes64 > std::numeric_limits<std::size_t>::max() || (valuesPointer % 4u) != 0u) {
            slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
            return nullptr;
        }
        std::span<const std::uint8_t> valuesBytes;
        if (!state->memory.View(valuesPointer, static_cast<std::size_t>(valuesBytes64), valuesBytes)) {
            slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
            return nullptr;
        }
        static_assert(sizeof(float) == 4u, "borrowed Core/native element width mismatch");
        query.values = reinterpret_cast<std::remove_reference_t<decltype(query.values)>>(valuesBytes.data());
    }
    if (!AssignCoreBorrowedCount(valuesCount, query.count)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->UniformArrayFloat(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_uniform_array_int(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->UniformArrayInt == nullptr)
        return Trap("UniformArrayInt borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("UniformArrayInt borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxUniformArrayIntQuery query{};
    query.location = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.location)>>>(slots[0].i32);
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCount = 0;
    if (!reader.U32(valuesPointer) || !reader.U32(valuesCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (valuesCount == 0) {
        query.values = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i32 = static_cast<std::int32_t>(Status::NotAvailable);
            return nullptr;
        }
        const std::uint64_t valuesBytes64 = static_cast<std::uint64_t>(valuesCount) * 4u;
        if (valuesBytes64 > std::numeric_limits<std::size_t>::max() || (valuesPointer % 4u) != 0u) {
            slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
            return nullptr;
        }
        std::span<const std::uint8_t> valuesBytes;
        if (!state->memory.View(valuesPointer, static_cast<std::size_t>(valuesBytes64), valuesBytes)) {
            slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
            return nullptr;
        }
        static_assert(sizeof(std::int32_t) == 4u, "borrowed Core/native element width mismatch");
        query.values = reinterpret_cast<std::remove_reference_t<decltype(query.values)>>(valuesBytes.data());
    }
    if (!AssignCoreBorrowedCount(valuesCount, query.count)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->UniformArrayInt(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_uniform_matrix(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->UniformMatrix == nullptr)
        return Trap("UniformMatrix borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("UniformMatrix borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxUniformMatrixQuery query{};
    query.location = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.location)>>>(slots[0].i32);
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCount = 0;
    if (!reader.U32(valuesPointer) || !reader.U32(valuesCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (valuesCount == 0) {
        query.values = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i32 = static_cast<std::int32_t>(Status::NotAvailable);
            return nullptr;
        }
        const std::uint64_t valuesBytes64 = static_cast<std::uint64_t>(valuesCount) * 4u;
        if (valuesBytes64 > std::numeric_limits<std::size_t>::max() || (valuesPointer % 4u) != 0u) {
            slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
            return nullptr;
        }
        std::span<const std::uint8_t> valuesBytes;
        if (!state->memory.View(valuesPointer, static_cast<std::size_t>(valuesBytes64), valuesBytes)) {
            slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
            return nullptr;
        }
        static_assert(sizeof(float) == 4u, "borrowed Core/native element width mismatch");
        query.values = reinterpret_cast<std::remove_reference_t<decltype(query.values)>>(valuesBytes.data());
    }
    if (!AssignCoreBorrowedCount(valuesCount, query.count)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.transpose = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->UniformMatrix(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_upload_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->UploadTexture == nullptr)
        return Trap("UploadTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 11u)
        return Trap("UploadTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 12u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[10].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxUploadTextureQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.target = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.target)>>>(slots[0].i32);
    query.level = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.level)>>>(slots[1].i32);
    query.xoff = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.xoff)>>>(slots[2].i32);
    query.yoff = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.yoff)>>>(slots[3].i32);
    query.zoff = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.zoff)>>>(slots[4].i32);
    query.width = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.width)>>>(slots[5].i32);
    query.height = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.height)>>>(slots[6].i32);
    query.depth = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.depth)>>>(slots[7].i32);
    query.format = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.format)>>>(slots[8].i32);
    query.pixelType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.pixelType)>>>(slots[9].i32);
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCount = 0;
    if (!reader.U32(dataPointer) || !reader.U32(dataCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (dataCount == 0) {
        query.data = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i32 = static_cast<std::int32_t>(Status::NotAvailable);
            return nullptr;
        }
        const std::uint64_t dataBytes64 = static_cast<std::uint64_t>(dataCount) * 1u;
        if (dataBytes64 > std::numeric_limits<std::size_t>::max() || (dataPointer % 1u) != 0u) {
            slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
            return nullptr;
        }
        std::span<const std::uint8_t> dataBytes;
        if (!state->memory.View(dataPointer, static_cast<std::size_t>(dataBytes64), dataBytes)) {
            slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
            return nullptr;
        }
        static_assert(sizeof(std::uint8_t) == 1u, "borrowed Core/native element width mismatch");
        query.data = reinterpret_cast<std::remove_reference_t<decltype(query.data)>>(dataBytes.data());
    }
    if (!AssignCoreBorrowedCount(dataCount, query.dataSize)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->UploadTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_gfx_upload_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->UploadVBO == nullptr)
        return Trap("UploadVBO borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 6u)
        return Trap("UploadVBO borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 7u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[5].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBOUploadQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCount = 0;
    if (!reader.U32(dataPointer) || !reader.U32(dataCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (dataCount == 0) {
        query.data = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t dataBytes64 = static_cast<std::uint64_t>(dataCount) * 4u;
        if (dataBytes64 > std::numeric_limits<std::size_t>::max() || (dataPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> dataBytes;
        if (!state->memory.View(dataPointer, static_cast<std::size_t>(dataBytes64), dataBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(float) == 4u, "borrowed Core/native element width mismatch");
        query.data = reinterpret_cast<std::remove_reference_t<decltype(query.data)>>(dataBytes.data());
    }
    if (!AssignCoreBorrowedCount(dataCount, query.dataCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[2].i32);
    query.dataStartIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataStartIndex)>>>(slots[3].i32);
    query.dataFinishIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataFinishIndex)>>>(slots[4].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxVBOUploadResult result{};
    state->native->gfx->UploadVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.bytesWritten), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_icons_add_unit_icon(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->icons == nullptr ||
        state->native->icons->AddUnitIcon == nullptr)
        return Trap("AddUnitIcon borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 8u)
        return Trap("AddUnitIcon borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 9u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[7].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    AddUnitIconQuery query{};
    std::uint32_t iconNamePointer = 0;
    std::uint32_t iconNameCount = 0;
    if (!reader.U32(iconNamePointer) || !reader.U32(iconNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (iconNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> iconNameBytes;
    if (!state->memory.View(iconNamePointer, static_cast<std::size_t>(iconNameCount) + 1u, iconNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (iconNameBytes[iconNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.iconName = reinterpret_cast<const char*>(iconNameBytes.data());
    std::uint32_t texFilePointer = 0;
    std::uint32_t texFileCount = 0;
    if (!reader.U32(texFilePointer) || !reader.U32(texFileCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (texFileCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> texFileBytes;
    if (!state->memory.View(texFilePointer, static_cast<std::size_t>(texFileCount) + 1u, texFileBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (texFileBytes[texFileCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.texFile = reinterpret_cast<const char*>(texFileBytes.data());
    query.size = slots[0].f32;
    query.distance = slots[1].f32;
    query.radiusAdjust = slots[2].i32 != 0;
    query.u0 = slots[3].f32;
    query.v0 = slots[4].f32;
    query.u1 = slots[5].f32;
    query.v1 = slots[6].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    AddUnitIconResult result{};
    state->native->icons->AddUnitIcon(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_icons_free_unit_icon(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->icons == nullptr ||
        state->native->icons->FreeUnitIcon == nullptr)
        return Trap("FreeUnitIcon borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("FreeUnitIcon borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    FreeUnitIconQuery query{};
    std::uint32_t iconNamePointer = 0;
    std::uint32_t iconNameCount = 0;
    if (!reader.U32(iconNamePointer) || !reader.U32(iconNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (iconNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> iconNameBytes;
    if (!state->memory.View(iconNamePointer, static_cast<std::size_t>(iconNameCount) + 1u, iconNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (iconNameBytes[iconNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.iconName = reinterpret_cast<const char*>(iconNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    FreeUnitIconResult result{};
    state->native->icons->FreeUnitIcon(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_markers_add_world_text(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->markers == nullptr ||
        state->native->markers->AddWorldText == nullptr)
        return Trap("AddWorldText borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("AddWorldText borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 20u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    AddWorldTextQuery query{};
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (textCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, static_cast<std::size_t>(textCount) + 1u, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (textBytes[textCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.text = reinterpret_cast<const char*>(textBytes.data());
    if (!reader.F32(query.pos.x)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.pos.y)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.pos.z)) return Trap("borrowed Core wire underflow");
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    AddWorldTextResult result{};
    state->native->markers->AddWorldText(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_markers_marker_add_point(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->markers == nullptr ||
        state->native->markers->MarkerAddPoint == nullptr)
        return Trap("MarkerAddPoint borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("MarkerAddPoint borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 20u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    MarkerAddPointQuery query{};
    if (!reader.F32(query.pos.x)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.pos.y)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.pos.z)) return Trap("borrowed Core wire underflow");
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (textCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, static_cast<std::size_t>(textCount) + 1u, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (textBytes[textCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.text = reinterpret_cast<const char*>(textBytes.data());
    query.localOnly = slots[0].i32 != 0;
    query.playerID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.playerID)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    MarkerAddPointResult result{};
    state->native->markers->MarkerAddPoint(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_ground_decals_set_ground_decal_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->groundDecals == nullptr ||
        state->native->groundDecals->SetGroundDecalTexture == nullptr)
        return Trap("SetGroundDecalTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("SetGroundDecalTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetGroundDecalTextureQuery query{};
    query.decalID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.decalID)>>>(slots[0].i32);
    std::uint32_t textureNamePointer = 0;
    std::uint32_t textureNameCount = 0;
    if (!reader.U32(textureNamePointer) || !reader.U32(textureNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (textureNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textureNameBytes;
    if (!state->memory.View(textureNamePointer, static_cast<std::size_t>(textureNameCount) + 1u, textureNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (textureNameBytes[textureNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.textureName = reinterpret_cast<const char*>(textureNameBytes.data());
    query.mainTex = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetGroundDecalTextureResult result{};
    state->native->groundDecals->SetGroundDecalTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_system_control_clear_watch_dog_timer(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->ClearWatchDogTimer == nullptr)
        return Trap("ClearWatchDogTimer borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ClearWatchDogTimer borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    ClearWatchDogTimerQuery query{};
    std::uint32_t threadNamePointer = 0;
    std::uint32_t threadNameCount = 0;
    if (!reader.U32(threadNamePointer) || !reader.U32(threadNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (threadNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> threadNameBytes;
    if (!state->memory.View(threadNamePointer, static_cast<std::size_t>(threadNameCount) + 1u, threadNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (threadNameBytes[threadNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.threadName = reinterpret_cast<const char*>(threadNameBytes.data());
    query.keepStopped = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    ClearWatchDogTimerResult result{};
    state->native->systemControl->ClearWatchDogTimer(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_system_control_reload(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->Reload == nullptr)
        return Trap("Reload borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("Reload borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    ReloadQuery query{};
    std::uint32_t startScriptPointer = 0;
    std::uint32_t startScriptCount = 0;
    if (!reader.U32(startScriptPointer) || !reader.U32(startScriptCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (startScriptCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> startScriptBytes;
    if (!state->memory.View(startScriptPointer, static_cast<std::size_t>(startScriptCount) + 1u, startScriptBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (startScriptBytes[startScriptCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.startScript = reinterpret_cast<const char*>(startScriptBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    ReloadResult result{};
    state->native->systemControl->Reload(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_system_control_restart(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->Restart == nullptr)
        return Trap("Restart borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("Restart borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RestartQuery query{};
    std::uint32_t cmdArgsPointer = 0;
    std::uint32_t cmdArgsCount = 0;
    if (!reader.U32(cmdArgsPointer) || !reader.U32(cmdArgsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (cmdArgsCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> cmdArgsBytes;
    if (!state->memory.View(cmdArgsPointer, static_cast<std::size_t>(cmdArgsCount) + 1u, cmdArgsBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (cmdArgsBytes[cmdArgsCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.cmdArgs = reinterpret_cast<const char*>(cmdArgsBytes.data());
    std::uint32_t startScriptPointer = 0;
    std::uint32_t startScriptCount = 0;
    if (!reader.U32(startScriptPointer) || !reader.U32(startScriptCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (startScriptCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> startScriptBytes;
    if (!state->memory.View(startScriptPointer, static_cast<std::size_t>(startScriptCount) + 1u, startScriptBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (startScriptBytes[startScriptCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.startScript = reinterpret_cast<const char*>(startScriptBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RestartResult result{};
    state->native->systemControl->Restart(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_system_control_set_share_level(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->SetShareLevel == nullptr)
        return Trap("SetShareLevel borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetShareLevel borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetShareLevelQuery query{};
    std::uint32_t resourcePointer = 0;
    std::uint32_t resourceCount = 0;
    if (!reader.U32(resourcePointer) || !reader.U32(resourceCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (resourceCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceBytes;
    if (!state->memory.View(resourcePointer, static_cast<std::size_t>(resourceCount) + 1u, resourceBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (resourceBytes[resourceCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.resource = reinterpret_cast<const char*>(resourceBytes.data());
    query.level = slots[0].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetShareLevelResult result{};
    state->native->systemControl->SetShareLevel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_system_control_share_resources(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->ShareResources == nullptr)
        return Trap("ShareResources borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ShareResources borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    ShareResourcesQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourcePointer = 0;
    std::uint32_t resourceCount = 0;
    if (!reader.U32(resourcePointer) || !reader.U32(resourceCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (resourceCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceBytes;
    if (!state->memory.View(resourcePointer, static_cast<std::size_t>(resourceCount) + 1u, resourceBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (resourceBytes[resourceCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.resource = reinterpret_cast<const char*>(resourceBytes.data());
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    ShareResourcesResult result{};
    state->native->systemControl->ShareResources(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_system_control_start(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->Start == nullptr)
        return Trap("Start borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("Start borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    StartQuery query{};
    std::uint32_t cmdArgsPointer = 0;
    std::uint32_t cmdArgsCount = 0;
    if (!reader.U32(cmdArgsPointer) || !reader.U32(cmdArgsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (cmdArgsCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> cmdArgsBytes;
    if (!state->memory.View(cmdArgsPointer, static_cast<std::size_t>(cmdArgsCount) + 1u, cmdArgsBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (cmdArgsBytes[cmdArgsCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.cmdArgs = reinterpret_cast<const char*>(cmdArgsBytes.data());
    std::uint32_t startScriptPointer = 0;
    std::uint32_t startScriptCount = 0;
    if (!reader.U32(startScriptPointer) || !reader.U32(startScriptCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (startScriptCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> startScriptBytes;
    if (!state->memory.View(startScriptPointer, static_cast<std::size_t>(startScriptCount) + 1u, startScriptBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (startScriptBytes[startScriptCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.startScript = reinterpret_cast<const char*>(startScriptBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    StartResult result{};
    state->native->systemControl->Start(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_add_translation_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->AddTranslationString == nullptr)
        return Trap("AddTranslationString borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("AddTranslationString borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlAddTranslationStringQuery query{};
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (keyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, static_cast<std::size_t>(keyCount) + 1u, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (keyBytes[keyCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.key = reinterpret_cast<const char*>(keyBytes.data());
    std::uint32_t translationPointer = 0;
    std::uint32_t translationCount = 0;
    if (!reader.U32(translationPointer) || !reader.U32(translationCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (translationCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> translationBytes;
    if (!state->memory.View(translationPointer, static_cast<std::size_t>(translationCount) + 1u, translationBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (translationBytes[translationCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.translation = reinterpret_cast<const char*>(translationBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlAddTranslationStringResult result{};
    state->native->rmlUi->AddTranslationString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_clear_document_path_requests(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ClearDocumentPathRequests == nullptr)
        return Trap("ClearDocumentPathRequests borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("ClearDocumentPathRequests borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlClearDocumentPathRequestsQuery query{};
    std::uint32_t documentPathPointer = 0;
    std::uint32_t documentPathCount = 0;
    if (!reader.U32(documentPathPointer) || !reader.U32(documentPathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (documentPathCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> documentPathBytes;
    if (!state->memory.View(documentPathPointer, static_cast<std::size_t>(documentPathCount) + 1u, documentPathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (documentPathBytes[documentPathCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.documentPath = reinterpret_cast<const char*>(documentPathBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlClearDocumentPathRequestsResult result{};
    state->native->rmlUi->ClearDocumentPathRequests(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_context_activate_theme(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextActivateTheme == nullptr)
        return Trap("ContextActivateTheme borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ContextActivateTheme borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlContextStringBoolQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.value = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlContextBoolResult result{};
    state->native->rmlUi->ContextActivateTheme(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_context_create_data_model(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextCreateDataModel == nullptr)
        return Trap("ContextCreateDataModel borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ContextCreateDataModel borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlContextCreateDataModelQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlContextOpenDataModelResult result{};
    state->native->rmlUi->ContextCreateDataModel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.dataModelHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_context_create_document(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextCreateDocument == nullptr)
        return Trap("ContextCreateDocument borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ContextCreateDocument borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlContextCreateDocumentQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t tagPointer = 0;
    std::uint32_t tagCount = 0;
    if (!reader.U32(tagPointer) || !reader.U32(tagCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (tagCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> tagBytes;
    if (!state->memory.View(tagPointer, static_cast<std::size_t>(tagCount) + 1u, tagBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (tagBytes[tagCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.tag = reinterpret_cast<const char*>(tagBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlContextCreateDocumentResult result{};
    state->native->rmlUi->ContextCreateDocument(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.documentHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_context_get_document(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextGetDocument == nullptr)
        return Trap("ContextGetDocument borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ContextGetDocument borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlContextGetDocumentQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlContextGetDocumentResult result{};
    state->native->rmlUi->ContextGetDocument(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.documentHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_context_is_theme_active(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextIsThemeActive == nullptr)
        return Trap("ContextIsThemeActive borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ContextIsThemeActive borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlContextStringQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlContextBoolResult result{};
    state->native->rmlUi->ContextIsThemeActive(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_context_load_document(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextLoadDocument == nullptr)
        return Trap("ContextLoadDocument borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ContextLoadDocument borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlContextLoadDocumentQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t documentPathPointer = 0;
    std::uint32_t documentPathCount = 0;
    if (!reader.U32(documentPathPointer) || !reader.U32(documentPathCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (documentPathCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> documentPathBytes;
    if (!state->memory.View(documentPathPointer, static_cast<std::size_t>(documentPathCount) + 1u, documentPathBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (documentPathBytes[documentPathCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.documentPath = reinterpret_cast<const char*>(documentPathBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlContextLoadDocumentResult result{};
    state->native->rmlUi->ContextLoadDocument(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.documentHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_context_open_data_model(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextOpenDataModel == nullptr)
        return Trap("ContextOpenDataModel borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ContextOpenDataModel borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlContextOpenDataModelQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlContextOpenDataModelResult result{};
    state->native->rmlUi->ContextOpenDataModel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.dataModelHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_context_process_text_input(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextProcessTextInput == nullptr)
        return Trap("ContextProcessTextInput borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ContextProcessTextInput borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlContextTextInputQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (textCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, static_cast<std::size_t>(textCount) + 1u, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (textBytes[textCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.text = reinterpret_cast<const char*>(textBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlContextBoolResult result{};
    state->native->rmlUi->ContextProcessTextInput(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_context_remove_data_model(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextRemoveDataModel == nullptr)
        return Trap("ContextRemoveDataModel borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ContextRemoveDataModel borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlContextStringQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlContextBoolResult result{};
    state->native->rmlUi->ContextRemoveDataModel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_context_remove_event_listener(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextRemoveEventListener == nullptr)
        return Trap("ContextRemoveEventListener borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("ContextRemoveEventListener borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlContextEventListenerRemoveQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    query.eventListenerHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventListenerHandle)>>>(slots[1].i64);
    std::uint32_t eventPointer = 0;
    std::uint32_t eventCount = 0;
    if (!reader.U32(eventPointer) || !reader.U32(eventCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (eventCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> eventBytes;
    if (!state->memory.View(eventPointer, static_cast<std::size_t>(eventCount) + 1u, eventBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (eventBytes[eventCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.event = reinterpret_cast<const char*>(eventBytes.data());
    query.inCapturePhase = slots[2].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ContextRemoveEventListener(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_create_context(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->CreateContext == nullptr)
        return Trap("CreateContext borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("CreateContext borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlCreateContextQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlCreateContextResult result{};
    state->native->rmlUi->CreateContext(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.contextHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_data_model_bind_bool(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindBool == nullptr)
        return Trap("DataModelBindBool borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("DataModelBindBool borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindBoolQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.initialValue = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindBool(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_data_model_bind_color(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindColor == nullptr)
        return Trap("DataModelBindColor borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 7u)
        return Trap("DataModelBindColor borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 8u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[5].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[6].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindColorQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.red = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.red)>>>(slots[1].i32);
    query.green = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.green)>>>(slots[2].i32);
    query.blue = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.blue)>>>(slots[3].i32);
    query.alpha = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.alpha)>>>(slots[4].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindColor(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_data_model_bind_float(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindFloat == nullptr)
        return Trap("DataModelBindFloat borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("DataModelBindFloat borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindFloatQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.initialValue = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindFloat(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_data_model_bind_int(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindInt == nullptr)
        return Trap("DataModelBindInt borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("DataModelBindInt borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindIntQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.initialValue = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.initialValue)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindInt(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_data_model_bind_percent(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindPercent == nullptr)
        return Trap("DataModelBindPercent borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("DataModelBindPercent borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindPercentQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.initialValue = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindPercent(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_data_model_bind_pixels(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindPixels == nullptr)
        return Trap("DataModelBindPixels borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("DataModelBindPixels borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindPixelsQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.initialValue = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindPixels(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_data_model_bind_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindString == nullptr)
        return Trap("DataModelBindString borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("DataModelBindString borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindStringQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    std::uint32_t initialValuePointer = 0;
    std::uint32_t initialValueCount = 0;
    if (!reader.U32(initialValuePointer) || !reader.U32(initialValueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (initialValueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> initialValueBytes;
    if (!state->memory.View(initialValuePointer, static_cast<std::size_t>(initialValueCount) + 1u, initialValueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (initialValueBytes[initialValueCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.initialValue = reinterpret_cast<const char*>(initialValueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_data_model_set_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelSetString == nullptr)
        return Trap("DataModelSetString borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("DataModelSetString borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlDataModelVariableStringQuery query{};
    query.variableHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.variableHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->DataModelSetString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_document_append_to_style_sheet(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentAppendToStyleSheet == nullptr)
        return Trap("DocumentAppendToStyleSheet borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("DocumentAppendToStyleSheet borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlDocumentStringQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlDocumentBoolResult result{};
    state->native->rmlUi->DocumentAppendToStyleSheet(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_document_create_element(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentCreateElement == nullptr)
        return Trap("DocumentCreateElement borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("DocumentCreateElement borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDocumentCreateElementQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    std::uint32_t tagNamePointer = 0;
    std::uint32_t tagNameCount = 0;
    if (!reader.U32(tagNamePointer) || !reader.U32(tagNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (tagNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> tagNameBytes;
    if (!state->memory.View(tagNamePointer, static_cast<std::size_t>(tagNameCount) + 1u, tagNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (tagNameBytes[tagNameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.tagName = reinterpret_cast<const char*>(tagNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDocumentCreateElementResult result{};
    state->native->rmlUi->DocumentCreateElement(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.elementPtrHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_document_create_text_node(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentCreateTextNode == nullptr)
        return Trap("DocumentCreateTextNode borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("DocumentCreateTextNode borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDocumentStringQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDocumentCreateElementResult result{};
    state->native->rmlUi->DocumentCreateTextNode(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.elementPtrHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_document_load_external_script(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentLoadExternalScript == nullptr)
        return Trap("DocumentLoadExternalScript borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("DocumentLoadExternalScript borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlDocumentStringQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlDocumentBoolResult result{};
    state->native->rmlUi->DocumentLoadExternalScript(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_document_load_inline_script(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentLoadInlineScript == nullptr)
        return Trap("DocumentLoadInlineScript borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("DocumentLoadInlineScript borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlDocumentInlineScriptQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    std::uint32_t contentPointer = 0;
    std::uint32_t contentCount = 0;
    if (!reader.U32(contentPointer) || !reader.U32(contentCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (contentCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> contentBytes;
    if (!state->memory.View(contentPointer, static_cast<std::size_t>(contentCount) + 1u, contentBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (contentBytes[contentCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.content = reinterpret_cast<const char*>(contentBytes.data());
    std::uint32_t sourcePathPointer = 0;
    std::uint32_t sourcePathCount = 0;
    if (!reader.U32(sourcePathPointer) || !reader.U32(sourcePathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (sourcePathCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> sourcePathBytes;
    if (!state->memory.View(sourcePathPointer, static_cast<std::size_t>(sourcePathCount) + 1u, sourcePathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (sourcePathBytes[sourcePathCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.sourcePath = reinterpret_cast<const char*>(sourcePathBytes.data());
    query.sourceLine = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.sourceLine)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlDocumentBoolResult result{};
    state->native->rmlUi->DocumentLoadInlineScript(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_document_set_title(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentSetTitle == nullptr)
        return Trap("DocumentSetTitle borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("DocumentSetTitle borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlDocumentSetTitleQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    std::uint32_t titlePointer = 0;
    std::uint32_t titleCount = 0;
    if (!reader.U32(titlePointer) || !reader.U32(titleCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (titleCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> titleBytes;
    if (!state->memory.View(titlePointer, static_cast<std::size_t>(titleCount) + 1u, titleBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (titleBytes[titleCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.title = reinterpret_cast<const char*>(titleBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlDocumentBoolResult result{};
    state->native->rmlUi->DocumentSetTitle(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_are_pseudo_classes_set(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementArePseudoClassesSet == nullptr)
        return Trap("ElementArePseudoClassesSet borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementArePseudoClassesSet borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementArePseudoClassesSet(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_closest(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementClosest == nullptr)
        return Trap("ElementClosest borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ElementClosest borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlElementGetElementResult result{};
    state->native->rmlUi->ElementClosest(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.elementHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_dispatch_event(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementDispatchEvent == nullptr)
        return Trap("ElementDispatchEvent borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementDispatchEvent borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementDispatchEventQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t eventPointer = 0;
    std::uint32_t eventCount = 0;
    if (!reader.U32(eventPointer) || !reader.U32(eventCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (eventCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> eventBytes;
    if (!state->memory.View(eventPointer, static_cast<std::size_t>(eventCount) + 1u, eventBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (eventBytes[eventCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.event = reinterpret_cast<const char*>(eventBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementDispatchEvent(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_form_submit(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementFormSubmit == nullptr)
        return Trap("ElementFormSubmit borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementFormSubmit borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementFormSubmitQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementFormSubmit(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_get_element_by_id(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetElementById == nullptr)
        return Trap("ElementGetElementById borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ElementGetElementById borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlElementGetElementResult result{};
    state->native->rmlUi->ElementGetElementById(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.elementHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_get_elements_by_class_name_count(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetElementsByClassNameCount == nullptr)
        return Trap("ElementGetElementsByClassNameCount borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementGetElementsByClassNameCount borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementGetIntResult result{};
    state->native->rmlUi->ElementGetElementsByClassNameCount(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_get_elements_by_tag_name_count(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetElementsByTagNameCount == nullptr)
        return Trap("ElementGetElementsByTagNameCount borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementGetElementsByTagNameCount borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementGetIntResult result{};
    state->native->rmlUi->ElementGetElementsByTagNameCount(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_has_attribute(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementHasAttribute == nullptr)
        return Trap("ElementHasAttribute borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementHasAttribute borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementHasAttribute(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_is_class_set(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementIsClassSet == nullptr)
        return Trap("ElementIsClassSet borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementIsClassSet borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementIsClassSet(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_is_pseudo_class_set(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementIsPseudoClassSet == nullptr)
        return Trap("ElementIsPseudoClassSet borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementIsPseudoClassSet borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementIsPseudoClassSet(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_matches(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementMatches == nullptr)
        return Trap("ElementMatches borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementMatches borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementMatches(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_query_selector(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementQuerySelector == nullptr)
        return Trap("ElementQuerySelector borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ElementQuerySelector borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlElementGetElementResult result{};
    state->native->rmlUi->ElementQuerySelector(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.elementHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_query_selector_all_count(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementQuerySelectorAllCount == nullptr)
        return Trap("ElementQuerySelectorAllCount borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementQuerySelectorAllCount borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementGetIntResult result{};
    state->native->rmlUi->ElementQuerySelectorAllCount(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_remove_attribute(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementRemoveAttribute == nullptr)
        return Trap("ElementRemoveAttribute borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementRemoveAttribute borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementRemoveAttribute(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_remove_event_listener(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementRemoveEventListener == nullptr)
        return Trap("ElementRemoveEventListener borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("ElementRemoveEventListener borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementEventListenerRemoveQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    query.eventListenerHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventListenerHandle)>>>(slots[1].i64);
    std::uint32_t eventPointer = 0;
    std::uint32_t eventCount = 0;
    if (!reader.U32(eventPointer) || !reader.U32(eventCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (eventCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> eventBytes;
    if (!state->memory.View(eventPointer, static_cast<std::size_t>(eventCount) + 1u, eventBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (eventBytes[eventCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.event = reinterpret_cast<const char*>(eventBytes.data());
    query.inCapturePhase = slots[2].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementRemoveEventListener(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_set_attribute(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementSetAttribute == nullptr)
        return Trap("ElementSetAttribute borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementSetAttribute borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementSetAttributeQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementSetAttribute(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_set_class(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementSetClass == nullptr)
        return Trap("ElementSetClass borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ElementSetClass borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementStringBoolQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.value = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementSetClass(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_set_class_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementSetClassName == nullptr)
        return Trap("ElementSetClassName borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementSetClassName borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementSetStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementSetClassName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_set_id(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementSetId == nullptr)
        return Trap("ElementSetId borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementSetId borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementSetStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementSetId(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_set_inner_rml(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementSetInnerRml == nullptr)
        return Trap("ElementSetInnerRml borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("ElementSetInnerRml borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementSetStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (valueCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, static_cast<std::size_t>(valueCount) + 1u, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (valueBytes[valueCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.value = reinterpret_cast<const char*>(valueBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementSetInnerRml(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_set_pseudo_class(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementSetPseudoClass == nullptr)
        return Trap("ElementSetPseudoClass borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ElementSetPseudoClass borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementStringBoolQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    query.value = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementSetPseudoClass(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_tab_set_set_panel(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementTabSetSetPanel == nullptr)
        return Trap("ElementTabSetSetPanel borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ElementTabSetSetPanel borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementTabSetIndexStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    query.index = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.index)>>>(slots[1].i32);
    std::uint32_t rmlPointer = 0;
    std::uint32_t rmlCount = 0;
    if (!reader.U32(rmlPointer) || !reader.U32(rmlCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (rmlCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> rmlBytes;
    if (!state->memory.View(rmlPointer, static_cast<std::size_t>(rmlCount) + 1u, rmlBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (rmlBytes[rmlCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.rml = reinterpret_cast<const char*>(rmlBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementTabSetSetPanel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_element_tab_set_set_tab(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementTabSetSetTab == nullptr)
        return Trap("ElementTabSetSetTab borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("ElementTabSetSetTab borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementTabSetIndexStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    query.index = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.index)>>>(slots[1].i32);
    std::uint32_t rmlPointer = 0;
    std::uint32_t rmlCount = 0;
    if (!reader.U32(rmlPointer) || !reader.U32(rmlCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (rmlCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> rmlBytes;
    if (!state->memory.View(rmlPointer, static_cast<std::size_t>(rmlCount) + 1u, rmlBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (rmlBytes[rmlCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.rml = reinterpret_cast<const char*>(rmlBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementTabSetSetTab(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_event_get_parameter_bool(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->EventGetParameterBool == nullptr)
        return Trap("EventGetParameterBool borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("EventGetParameterBool borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlEventParameterQuery query{};
    query.eventHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlEventGetBoolResult result{};
    state->native->rmlUi->EventGetParameterBool(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.Bool(result.value)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_event_get_parameter_float(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->EventGetParameterFloat == nullptr)
        return Trap("EventGetParameterFloat borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("EventGetParameterFloat borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlEventParameterQuery query{};
    query.eventHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlEventGetFloatResult result{};
    state->native->rmlUi->EventGetParameterFloat(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.value)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_event_get_parameter_int(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->EventGetParameterInt == nullptr)
        return Trap("EventGetParameterInt borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("EventGetParameterInt borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlEventParameterQuery query{};
    query.eventHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlEventGetIntResult result{};
    state->native->rmlUi->EventGetParameterInt(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.I32(result.value)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_event_get_parameter_type(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->EventGetParameterType == nullptr)
        return Trap("EventGetParameterType borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("EventGetParameterType borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlEventParameterQuery query{};
    query.eventHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlEventGetIntResult result{};
    state->native->rmlUi->EventGetParameterType(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.I32(result.value)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(4u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_get_context(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->GetContext == nullptr)
        return Trap("GetContext borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("GetContext borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlGetContextQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlGetContextResult result{};
    state->native->rmlUi->GetContext(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.contextHandle)) return Trap("borrowed Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("borrowed Core wire overflow");
    if (!writer.Finish(8u)) return Trap("borrowed Core output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("borrowed Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_load_font_face(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->LoadFontFace == nullptr)
        return Trap("LoadFontFace borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("LoadFontFace borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlLoadFontFaceQuery query{};
    std::uint32_t filePathPointer = 0;
    std::uint32_t filePathCount = 0;
    if (!reader.U32(filePathPointer) || !reader.U32(filePathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (filePathCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> filePathBytes;
    if (!state->memory.View(filePathPointer, static_cast<std::size_t>(filePathCount) + 1u, filePathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (filePathBytes[filePathCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.filePath = reinterpret_cast<const char*>(filePathBytes.data());
    query.fallback = slots[0].i32 != 0;
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("borrowed Core option presence underflow");
        query.hasWeight = corePresent;
    }
    { std::int32_t coreRaw = 0; if (!reader.I32(coreRaw)) return Trap("borrowed Core wire underflow"); query.weight = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.weight)>>>(coreRaw); }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlLoadFontFaceResult result{};
    state->native->rmlUi->LoadFontFace(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_regiser_event_type(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->RegiserEventType == nullptr)
        return Trap("RegiserEventType borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("RegiserEventType borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 24u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlRegisterEventTypeQuery query{};
    std::uint32_t eventTypePointer = 0;
    std::uint32_t eventTypeCount = 0;
    if (!reader.U32(eventTypePointer) || !reader.U32(eventTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (eventTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> eventTypeBytes;
    if (!state->memory.View(eventTypePointer, static_cast<std::size_t>(eventTypeCount) + 1u, eventTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (eventTypeBytes[eventTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.eventType = reinterpret_cast<const char*>(eventTypeBytes.data());
    if (!reader.Bool(query.options.interruptible)) return Trap("borrowed Core wire underflow");
    if (!reader.Bool(query.options.bubbles)) return Trap("borrowed Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("borrowed Core option presence underflow");
        query.options.hasDefaultPhase = corePresent;
    }
    { std::int32_t coreRaw = 0; if (!reader.I32(coreRaw)) return Trap("borrowed Core wire underflow"); query.options.defaultPhase = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options.defaultPhase)>>>(coreRaw); }
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlRegisterEventTypeResult result{};
    state->native->rmlUi->RegiserEventType(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.eventID), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_register_event_type(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->RegisterEventType == nullptr)
        return Trap("RegisterEventType borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("RegisterEventType borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 24u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlRegisterEventTypeQuery query{};
    std::uint32_t eventTypePointer = 0;
    std::uint32_t eventTypeCount = 0;
    if (!reader.U32(eventTypePointer) || !reader.U32(eventTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (eventTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> eventTypeBytes;
    if (!state->memory.View(eventTypePointer, static_cast<std::size_t>(eventTypeCount) + 1u, eventTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (eventTypeBytes[eventTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.eventType = reinterpret_cast<const char*>(eventTypeBytes.data());
    if (!reader.Bool(query.options.interruptible)) return Trap("borrowed Core wire underflow");
    if (!reader.Bool(query.options.bubbles)) return Trap("borrowed Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("borrowed Core option presence underflow");
        query.options.hasDefaultPhase = corePresent;
    }
    { std::int32_t coreRaw = 0; if (!reader.I32(coreRaw)) return Trap("borrowed Core wire underflow"); query.options.defaultPhase = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options.defaultPhase)>>>(coreRaw); }
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlRegisterEventTypeResult result{};
    state->native->rmlUi->RegisterEventType(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.eventID), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_remove_context_by_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->RemoveContextByName == nullptr)
        return Trap("RemoveContextByName borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("RemoveContextByName borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlRemoveContextByNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlRemoveContextByNameResult result{};
    state->native->rmlUi->RemoveContextByName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_set_debug_context_by_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->SetDebugContextByName == nullptr)
        return Trap("SetDebugContextByName borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("SetDebugContextByName borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlSetDebugContextByNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlSetDebugContextByNameResult result{};
    state->native->rmlUi->SetDebugContextByName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_set_mouse_cursor_alias(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->SetMouseCursorAlias == nullptr)
        return Trap("SetMouseCursorAlias borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("SetMouseCursorAlias borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlSetMouseCursorAliasQuery query{};
    std::uint32_t rmlNamePointer = 0;
    std::uint32_t rmlNameCount = 0;
    if (!reader.U32(rmlNamePointer) || !reader.U32(rmlNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (rmlNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> rmlNameBytes;
    if (!state->memory.View(rmlNamePointer, static_cast<std::size_t>(rmlNameCount) + 1u, rmlNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (rmlNameBytes[rmlNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.rmlName = reinterpret_cast<const char*>(rmlNameBytes.data());
    std::uint32_t recoilNamePointer = 0;
    std::uint32_t recoilNameCount = 0;
    if (!reader.U32(recoilNamePointer) || !reader.U32(recoilNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (recoilNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> recoilNameBytes;
    if (!state->memory.View(recoilNamePointer, static_cast<std::size_t>(recoilNameCount) + 1u, recoilNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (recoilNameBytes[recoilNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.recoilName = reinterpret_cast<const char*>(recoilNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlSetMouseCursorAliasResult result{};
    state->native->rmlUi->SetMouseCursorAlias(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_rml_ui_sol_lua_data_model_set_dirty(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->SolLuaDataModelSetDirty == nullptr)
        return Trap("SolLuaDataModelSetDirty borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SolLuaDataModelSetDirty borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlSolLuaDataModelSetDirtyQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t propertyPointer = 0;
    std::uint32_t propertyCount = 0;
    if (!reader.U32(propertyPointer) || !reader.U32(propertyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (propertyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> propertyBytes;
    if (!state->memory.View(propertyPointer, static_cast<std::size_t>(propertyCount) + 1u, propertyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (propertyBytes[propertyCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.property = reinterpret_cast<const char*>(propertyBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->SolLuaDataModelSetDirty(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_vfs_compress_folder(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->CompressFolder == nullptr)
        return Trap("CompressFolder borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("CompressFolder borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 32u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    CompressFolderQuery query{};
    std::uint32_t folderPathPointer = 0;
    std::uint32_t folderPathCount = 0;
    if (!reader.U32(folderPathPointer) || !reader.U32(folderPathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (folderPathCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> folderPathBytes;
    if (!state->memory.View(folderPathPointer, static_cast<std::size_t>(folderPathCount) + 1u, folderPathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (folderPathBytes[folderPathCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.folderPath = reinterpret_cast<const char*>(folderPathBytes.data());
    std::uint32_t archiveTypePointer = 0;
    std::uint32_t archiveTypeCount = 0;
    if (!reader.U32(archiveTypePointer) || !reader.U32(archiveTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (archiveTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> archiveTypeBytes;
    if (!state->memory.View(archiveTypePointer, static_cast<std::size_t>(archiveTypeCount) + 1u, archiveTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (archiveTypeBytes[archiveTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.archiveType = reinterpret_cast<const char*>(archiveTypeBytes.data());
    std::uint32_t compressedFilePathPointer = 0;
    std::uint32_t compressedFilePathCount = 0;
    if (!reader.U32(compressedFilePathPointer) || !reader.U32(compressedFilePathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (compressedFilePathCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> compressedFilePathBytes;
    if (!state->memory.View(compressedFilePathPointer, static_cast<std::size_t>(compressedFilePathCount) + 1u, compressedFilePathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (compressedFilePathBytes[compressedFilePathCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.compressedFilePath = reinterpret_cast<const char*>(compressedFilePathBytes.data());
    query.includeFolder = slots[0].i32 != 0;
    std::uint32_t modePointer = 0;
    std::uint32_t modeCount = 0;
    if (!reader.U32(modePointer) || !reader.U32(modeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (modeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> modeBytes;
    if (!state->memory.View(modePointer, static_cast<std::size_t>(modeCount) + 1u, modeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (modeBytes[modeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.mode = reinterpret_cast<const char*>(modeBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    CompressFolderResult result{};
    state->native->vfs->CompressFolder(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_vfs_create_dir(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->CreateDir == nullptr)
        return Trap("CreateDir borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("CreateDir borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    CreateDirQuery query{};
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCount = 0;
    if (!reader.U32(pathPointer) || !reader.U32(pathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (pathCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pathBytes;
    if (!state->memory.View(pathPointer, static_cast<std::size_t>(pathCount) + 1u, pathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (pathBytes[pathCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.path = reinterpret_cast<const char*>(pathBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    CreateDirResult result{};
    state->native->vfs->CreateDir(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_vfs_download_archive(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->DownloadArchive == nullptr)
        return Trap("DownloadArchive borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("DownloadArchive borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    DownloadArchiveQuery query{};
    std::uint32_t filenamePointer = 0;
    std::uint32_t filenameCount = 0;
    if (!reader.U32(filenamePointer) || !reader.U32(filenameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (filenameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> filenameBytes;
    if (!state->memory.View(filenamePointer, static_cast<std::size_t>(filenameCount) + 1u, filenameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (filenameBytes[filenameCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.filename = reinterpret_cast<const char*>(filenameBytes.data());
    std::uint32_t categoryPointer = 0;
    std::uint32_t categoryCount = 0;
    if (!reader.U32(categoryPointer) || !reader.U32(categoryCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (categoryCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> categoryBytes;
    if (!state->memory.View(categoryPointer, static_cast<std::size_t>(categoryCount) + 1u, categoryBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    if (categoryBytes[categoryCount] != 0) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.category = reinterpret_cast<const char*>(categoryBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    DownloadArchiveResult result{};
    state->native->vfs->DownloadArchive(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreBorrowed_vfs_extract_mod_archive_file(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->ExtractModArchiveFile == nullptr)
        return Trap("ExtractModArchiveFile borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("ExtractModArchiveFile borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    ExtractModArchiveFileQuery query{};
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCount = 0;
    if (!reader.U32(pathPointer) || !reader.U32(pathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (pathCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pathBytes;
    if (!state->memory.View(pathPointer, static_cast<std::size_t>(pathCount) + 1u, pathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (pathBytes[pathCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.path = reinterpret_cast<const char*>(pathBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    ExtractModArchiveFileResult result{};
    state->native->vfs->ExtractModArchiveFile(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_vfs_file_exists(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->FileExists == nullptr)
        return Trap("FileExists borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("FileExists borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    FileExistsQuery query{};
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCount = 0;
    if (!reader.U32(pathPointer) || !reader.U32(pathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (pathCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pathBytes;
    if (!state->memory.View(pathPointer, static_cast<std::size_t>(pathCount) + 1u, pathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (pathBytes[pathCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.path = reinterpret_cast<const char*>(pathBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    FileExistsResult result{};
    state->native->vfs->FileExists(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.exists ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_vfs_get_file_size(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->GetFileSize == nullptr)
        return Trap("GetFileSize borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("GetFileSize borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetFileSizeQuery query{};
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCount = 0;
    if (!reader.U32(pathPointer) || !reader.U32(pathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (pathCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pathBytes;
    if (!state->memory.View(pathPointer, static_cast<std::size_t>(pathCount) + 1u, pathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (pathBytes[pathCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.path = reinterpret_cast<const char*>(pathBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetFileSizeResult result{};
    state->native->vfs->GetFileSize(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.size), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_vfs_get_map_square_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->GetMapSquareTexture == nullptr)
        return Trap("GetMapSquareTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 5u)
        return Trap("GetMapSquareTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetMapSquareTextureQuery query{};
    query.texSquareX = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.texSquareX)>>>(slots[0].i32);
    query.texSquareY = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.texSquareY)>>>(slots[1].i32);
    query.lodMin = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.lodMin)>>>(slots[2].i32);
    std::uint32_t textureNamePointer = 0;
    std::uint32_t textureNameCount = 0;
    if (!reader.U32(textureNamePointer) || !reader.U32(textureNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (textureNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textureNameBytes;
    if (!state->memory.View(textureNamePointer, static_cast<std::size_t>(textureNameCount) + 1u, textureNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (textureNameBytes[textureNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.textureName = reinterpret_cast<const char*>(textureNameBytes.data());
    query.lodMax = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.lodMax)>>>(slots[3].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetMapSquareTextureResult result{};
    state->native->vfs->GetMapSquareTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_vfs_has_archive(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->HasArchive == nullptr)
        return Trap("HasArchive borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("HasArchive borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    HasArchiveQuery query{};
    std::uint32_t archiveNamePointer = 0;
    std::uint32_t archiveNameCount = 0;
    if (!reader.U32(archiveNamePointer) || !reader.U32(archiveNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (archiveNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> archiveNameBytes;
    if (!state->memory.View(archiveNamePointer, static_cast<std::size_t>(archiveNameCount) + 1u, archiveNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (archiveNameBytes[archiveNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.archiveName = reinterpret_cast<const char*>(archiveNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    HasArchiveResult result{};
    state->native->vfs->HasArchive(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.hasArchive ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_vfs_is_directory(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->IsDirectory == nullptr)
        return Trap("IsDirectory borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("IsDirectory borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    IsDirectoryQuery query{};
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCount = 0;
    if (!reader.U32(pathPointer) || !reader.U32(pathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (pathCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pathBytes;
    if (!state->memory.View(pathPointer, static_cast<std::size_t>(pathCount) + 1u, pathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (pathBytes[pathCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.path = reinterpret_cast<const char*>(pathBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    IsDirectoryResult result{};
    state->native->vfs->IsDirectory(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.isDirectory ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_vfs_set_map_square_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->SetMapSquareTexture == nullptr)
        return Trap("SetMapSquareTexture borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("SetMapSquareTexture borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetMapSquareTextureQuery query{};
    query.texSquareX = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.texSquareX)>>>(slots[0].i32);
    query.texSquareY = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.texSquareY)>>>(slots[1].i32);
    std::uint32_t textureNamePointer = 0;
    std::uint32_t textureNameCount = 0;
    if (!reader.U32(textureNamePointer) || !reader.U32(textureNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (textureNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textureNameBytes;
    if (!state->memory.View(textureNamePointer, static_cast<std::size_t>(textureNameCount) + 1u, textureNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (textureNameBytes[textureNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.textureName = reinterpret_cast<const char*>(textureNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetMapSquareTextureResult result{};
    state->native->vfs->SetMapSquareTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_team_control_add_team_resource(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->AddTeamResource == nullptr)
        return Trap("AddTeamResource borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("AddTeamResource borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    AddTeamResourceQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (resourceTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, static_cast<std::size_t>(resourceTypeCount) + 1u, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (resourceTypeBytes[resourceTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.resourceType = reinterpret_cast<const char*>(resourceTypeBytes.data());
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    AddTeamResourceResult result{};
    state->native->syncedCtrl->team->AddTeamResource(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_team_control_add_team_resource_excess_stats(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->AddTeamResourceExcessStats == nullptr)
        return Trap("AddTeamResourceExcessStats borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("AddTeamResourceExcessStats borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    AddTeamResourceExcessStatsQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (resourceTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, static_cast<std::size_t>(resourceTypeCount) + 1u, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (resourceTypeBytes[resourceTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.resourceType = reinterpret_cast<const char*>(resourceTypeBytes.data());
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    AddTeamResourceExcessStatsResult result{};
    state->native->syncedCtrl->team->AddTeamResourceExcessStats(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_team_control_game_over(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->GameOver == nullptr)
        return Trap("GameOver borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 1u)
        return Trap("GameOver borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GameOverQuery query{};
    std::uint32_t winningAllyTeamsPointer = 0;
    std::uint32_t winningAllyTeamsCount = 0;
    if (!reader.U32(winningAllyTeamsPointer) || !reader.U32(winningAllyTeamsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (winningAllyTeamsCount == 0) {
        query.winningAllyTeams = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t winningAllyTeamsBytes64 = static_cast<std::uint64_t>(winningAllyTeamsCount) * 4u;
        if (winningAllyTeamsBytes64 > std::numeric_limits<std::size_t>::max() || (winningAllyTeamsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> winningAllyTeamsBytes;
        if (!state->memory.View(winningAllyTeamsPointer, static_cast<std::size_t>(winningAllyTeamsBytes64), winningAllyTeamsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::int32_t) == 4u, "borrowed Core/native element width mismatch");
        query.winningAllyTeams = reinterpret_cast<std::remove_reference_t<decltype(query.winningAllyTeams)>>(winningAllyTeamsBytes.data());
    }
    if (!AssignCoreBorrowedCount(winningAllyTeamsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GameOverResult result{};
    state->native->syncedCtrl->team->GameOver(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_team_control_set_team_resource(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->SetTeamResource == nullptr)
        return Trap("SetTeamResource borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("SetTeamResource borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetTeamResourceQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (resourceTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, static_cast<std::size_t>(resourceTypeCount) + 1u, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (resourceTypeBytes[resourceTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.resourceType = reinterpret_cast<const char*>(resourceTypeBytes.data());
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetTeamResourceResult result{};
    state->native->syncedCtrl->team->SetTeamResource(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_team_control_set_team_share_level(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->SetTeamShareLevel == nullptr)
        return Trap("SetTeamShareLevel borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("SetTeamShareLevel borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetTeamShareLevelQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (resourceTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, static_cast<std::size_t>(resourceTypeCount) + 1u, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (resourceTypeBytes[resourceTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.resourceType = reinterpret_cast<const char*>(resourceTypeBytes.data());
    query.shareLevel = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetTeamShareLevelResult result{};
    state->native->syncedCtrl->team->SetTeamShareLevel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_team_control_share_team_resource(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->ShareTeamResource == nullptr)
        return Trap("ShareTeamResource borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("ShareTeamResource borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    ShareTeamResourceQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    query.targetTeamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.targetTeamID)>>>(slots[1].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (resourceTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, static_cast<std::size_t>(resourceTypeCount) + 1u, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (resourceTypeBytes[resourceTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.resourceType = reinterpret_cast<const char*>(resourceTypeBytes.data());
    query.amount = slots[2].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    ShareTeamResourceResult result{};
    state->native->syncedCtrl->team->ShareTeamResource(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_team_control_use_team_resource(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->UseTeamResource == nullptr)
        return Trap("UseTeamResource borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("UseTeamResource borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    UseTeamResourceQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (resourceTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, static_cast<std::size_t>(resourceTypeCount) + 1u, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (resourceTypeBytes[resourceTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.resourceType = reinterpret_cast<const char*>(resourceTypeBytes.data());
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    UseTeamResourceResult result{};
    state->native->syncedCtrl->team->UseTeamResource(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_add_unit_resource(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->AddUnitResource == nullptr)
        return Trap("AddUnitResource borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("AddUnitResource borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    AddUnitResourceQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (resourceTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, static_cast<std::size_t>(resourceTypeCount) + 1u, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (resourceTypeBytes[resourceTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.resourceType = reinterpret_cast<const char*>(resourceTypeBytes.data());
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    AddUnitResourceResult result{};
    state->native->syncedCtrl->unit->AddUnitResource(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_bugger_off(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->BuggerOff == nullptr)
        return Trap("BuggerOff borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("BuggerOff borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 32u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    BuggerOffQuery query{};
    if (!reader.F32(query.pos.x)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.pos.y)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.pos.z)) return Trap("borrowed Core wire underflow");
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    query.radius = slots[0].f32;
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[1].i32);
    if (!reader.Bool(query.options.spherical)) return Trap("borrowed Core wire underflow");
    if (!reader.Bool(query.options.forced)) return Trap("borrowed Core wire underflow");
    { std::int32_t coreRaw = 0; if (!reader.I32(coreRaw)) return Trap("borrowed Core wire underflow"); query.options.excludeUnitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options.excludeUnitID)>>>(coreRaw); }
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    std::uint32_t excludeUnitDefIDsPointer = 0;
    std::uint32_t excludeUnitDefIDsCount = 0;
    if (!reader.U32(excludeUnitDefIDsPointer) || !reader.U32(excludeUnitDefIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (excludeUnitDefIDsCount == 0) {
        query.excludeUnitDefIDs = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t excludeUnitDefIDsBytes64 = static_cast<std::uint64_t>(excludeUnitDefIDsCount) * 4u;
        if (excludeUnitDefIDsBytes64 > std::numeric_limits<std::size_t>::max() || (excludeUnitDefIDsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> excludeUnitDefIDsBytes;
        if (!state->memory.View(excludeUnitDefIDsPointer, static_cast<std::size_t>(excludeUnitDefIDsBytes64), excludeUnitDefIDsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::int32_t) == 4u, "borrowed Core/native element width mismatch");
        query.excludeUnitDefIDs = reinterpret_cast<std::remove_reference_t<decltype(query.excludeUnitDefIDs)>>(excludeUnitDefIDsBytes.data());
    }
    if (!AssignCoreBorrowedCount(excludeUnitDefIDsCount, query.excludeUnitDefCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    BuggerOffResult result{};
    state->native->syncedCtrl->unit->BuggerOff(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_give_order_to_unit_array(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->GiveOrderToUnitArray == nullptr)
        return Trap("GiveOrderToUnitArray borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("GiveOrderToUnitArray borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GiveOrderToUnitArrayQuery query{};
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCount = 0;
    if (!reader.U32(unitIDsPointer) || !reader.U32(unitIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (unitIDsCount == 0) {
        query.unitIDs = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t unitIDsBytes64 = static_cast<std::uint64_t>(unitIDsCount) * 4u;
        if (unitIDsBytes64 > std::numeric_limits<std::size_t>::max() || (unitIDsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> unitIDsBytes;
        if (!state->memory.View(unitIDsPointer, static_cast<std::size_t>(unitIDsBytes64), unitIDsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::int32_t) == 4u, "borrowed Core/native element width mismatch");
        query.unitIDs = reinterpret_cast<std::remove_reference_t<decltype(query.unitIDs)>>(unitIDsBytes.data());
    }
    if (!AssignCoreBorrowedCount(unitIDsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.cmdID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.cmdID)>>>(slots[0].i32);
    std::uint32_t paramsPointer = 0;
    std::uint32_t paramsCount = 0;
    if (!reader.U32(paramsPointer) || !reader.U32(paramsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (paramsCount == 0) {
        query.params = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t paramsBytes64 = static_cast<std::uint64_t>(paramsCount) * 4u;
        if (paramsBytes64 > std::numeric_limits<std::size_t>::max() || (paramsPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> paramsBytes;
        if (!state->memory.View(paramsPointer, static_cast<std::size_t>(paramsBytes64), paramsBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(float) == 4u, "borrowed Core/native element width mismatch");
        query.params = reinterpret_cast<std::remove_reference_t<decltype(query.params)>>(paramsBytes.data());
    }
    if (!AssignCoreBorrowedCount(paramsCount, query.paramCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.options = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options)>>>(slots[1].i32);
    query.timeout = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.timeout)>>>(slots[2].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GiveOrderToUnitArrayResult result{};
    state->native->syncedCtrl->unit->GiveOrderToUnitArray(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_set_unit_build_params(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitBuildParams == nullptr)
        return Trap("SetUnitBuildParams borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetUnitBuildParams borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 20u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitBuildParamsQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t paramNamePointer = 0;
    std::uint32_t paramNameCount = 0;
    if (!reader.U32(paramNamePointer) || !reader.U32(paramNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (paramNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> paramNameBytes;
    if (!state->memory.View(paramNamePointer, static_cast<std::size_t>(paramNameCount) + 1u, paramNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (paramNameBytes[paramNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.paramName = reinterpret_cast<const char*>(paramNameBytes.data());
    if (!reader.F32(query.value.number)) return Trap("borrowed Core wire underflow");
    if (!reader.Bool(query.value.boolean)) return Trap("borrowed Core wire underflow");
    if (!reader.Bool(query.value.useBoolean)) return Trap("borrowed Core wire underflow");
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitBuildParamsResult result{};
    state->native->syncedCtrl->unit->SetUnitBuildParams(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_set_unit_flanking(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitFlanking == nullptr)
        return Trap("SetUnitFlanking borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetUnitFlanking borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 20u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitFlankingQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t typePointer = 0;
    std::uint32_t typeCount = 0;
    if (!reader.U32(typePointer) || !reader.U32(typeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (typeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> typeBytes;
    if (!state->memory.View(typePointer, static_cast<std::size_t>(typeCount) + 1u, typeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (typeBytes[typeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.type = reinterpret_cast<const char*>(typeBytes.data());
    if (!reader.F32(query.args.x)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.args.y)) return Trap("borrowed Core wire underflow");
    if (!reader.F32(query.args.z)) return Trap("borrowed Core wire underflow");
    if (!reader.Align(4u)) return Trap("borrowed Core record alignment overflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitFlankingResult result{};
    state->native->syncedCtrl->unit->SetUnitFlanking(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_set_unit_nano_pieces(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitNanoPieces == nullptr)
        return Trap("SetUnitNanoPieces borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetUnitNanoPieces borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitNanoPiecesQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t pieceIndicesPointer = 0;
    std::uint32_t pieceIndicesCount = 0;
    if (!reader.U32(pieceIndicesPointer) || !reader.U32(pieceIndicesCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (pieceIndicesCount == 0) {
        query.pieceIndices = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::NotAvailable)));
            return nullptr;
        }
        const std::uint64_t pieceIndicesBytes64 = static_cast<std::uint64_t>(pieceIndicesCount) * 4u;
        if (pieceIndicesBytes64 > std::numeric_limits<std::size_t>::max() || (pieceIndicesPointer % 4u) != 0u) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
            return nullptr;
        }
        std::span<const std::uint8_t> pieceIndicesBytes;
        if (!state->memory.View(pieceIndicesPointer, static_cast<std::size_t>(pieceIndicesBytes64), pieceIndicesBytes)) {
            slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
            return nullptr;
        }
        static_assert(sizeof(std::int32_t) == 4u, "borrowed Core/native element width mismatch");
        query.pieceIndices = reinterpret_cast<std::remove_reference_t<decltype(query.pieceIndices)>>(pieceIndicesBytes.data());
    }
    if (!AssignCoreBorrowedCount(pieceIndicesCount, query.pieceCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitNanoPiecesResult result{};
    state->native->syncedCtrl->unit->SetUnitNanoPieces(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_set_unit_resourcing(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitResourcing == nullptr)
        return Trap("SetUnitResourcing borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("SetUnitResourcing borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitResourcingQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t typePointer = 0;
    std::uint32_t typeCount = 0;
    if (!reader.U32(typePointer) || !reader.U32(typeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (typeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> typeBytes;
    if (!state->memory.View(typePointer, static_cast<std::size_t>(typeCount) + 1u, typeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (typeBytes[typeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.type = reinterpret_cast<const char*>(typeBytes.data());
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitResourcingResult result{};
    state->native->syncedCtrl->unit->SetUnitResourcing(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_set_unit_sensor_radius(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitSensorRadius == nullptr)
        return Trap("SetUnitSensorRadius borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("SetUnitSensorRadius borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitSensorRadiusQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t sensorTypePointer = 0;
    std::uint32_t sensorTypeCount = 0;
    if (!reader.U32(sensorTypePointer) || !reader.U32(sensorTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (sensorTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> sensorTypeBytes;
    if (!state->memory.View(sensorTypePointer, static_cast<std::size_t>(sensorTypeCount) + 1u, sensorTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (sensorTypeBytes[sensorTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.sensorType = reinterpret_cast<const char*>(sensorTypeBytes.data());
    query.radius = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.radius)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitSensorRadiusResult result{};
    state->native->syncedCtrl->unit->SetUnitSensorRadius(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.newRadius), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_set_unit_storage(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitStorage == nullptr)
        return Trap("SetUnitStorage borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("SetUnitStorage borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitStorageQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t resourcePointer = 0;
    std::uint32_t resourceCount = 0;
    if (!reader.U32(resourcePointer) || !reader.U32(resourceCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (resourceCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceBytes;
    if (!state->memory.View(resourcePointer, static_cast<std::size_t>(resourceCount) + 1u, resourceBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (resourceBytes[resourceCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.resource = reinterpret_cast<const char*>(resourceBytes.data());
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitStorageResult result{};
    state->native->syncedCtrl->unit->SetUnitStorage(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_set_unit_tooltip(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitTooltip == nullptr)
        return Trap("SetUnitTooltip borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetUnitTooltip borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitTooltipQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t tooltipPointer = 0;
    std::uint32_t tooltipCount = 0;
    if (!reader.U32(tooltipPointer) || !reader.U32(tooltipCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (tooltipCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> tooltipBytes;
    if (!state->memory.View(tooltipPointer, static_cast<std::size_t>(tooltipCount) + 1u, tooltipBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (tooltipBytes[tooltipCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.tooltip = reinterpret_cast<const char*>(tooltipBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitTooltipResult result{};
    state->native->syncedCtrl->unit->SetUnitTooltip(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_set_unit_weapon_damages(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitWeaponDamages == nullptr)
        return Trap("SetUnitWeaponDamages borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("SetUnitWeaponDamages borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitWeaponDamagesQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    query.weaponNum = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.weaponNum)>>>(slots[1].i32);
    std::uint32_t damageKeyPointer = 0;
    std::uint32_t damageKeyCount = 0;
    if (!reader.U32(damageKeyPointer) || !reader.U32(damageKeyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (damageKeyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> damageKeyBytes;
    if (!state->memory.View(damageKeyPointer, static_cast<std::size_t>(damageKeyCount) + 1u, damageKeyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (damageKeyBytes[damageKeyCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.damageKey = reinterpret_cast<const char*>(damageKeyBytes.data());
    query.damageValue = slots[2].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitWeaponDamagesResult result{};
    state->native->syncedCtrl->unit->SetUnitWeaponDamages(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_set_unit_weapon_state(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitWeaponState == nullptr)
        return Trap("SetUnitWeaponState borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("SetUnitWeaponState borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitWeaponStateQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    query.weaponNum = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.weaponNum)>>>(slots[1].i32);
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (keyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, static_cast<std::size_t>(keyCount) + 1u, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (keyBytes[keyCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.key = reinterpret_cast<const char*>(keyBytes.data());
    query.value = slots[2].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitWeaponStateResult result{};
    state->native->syncedCtrl->unit->SetUnitWeaponState(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_unit_control_use_unit_resource(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->UseUnitResource == nullptr)
        return Trap("UseUnitResource borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 3u)
        return Trap("UseUnitResource borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    UseUnitResourceQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (resourceTypeCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, static_cast<std::size_t>(resourceTypeCount) + 1u, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (resourceTypeBytes[resourceTypeCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.resourceType = reinterpret_cast<const char*>(resourceTypeBytes.data());
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    UseUnitResourceResult result{};
    state->native->syncedCtrl->unit->UseUnitResource(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_terrain_control_set_terrain_type_data(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->terrain == nullptr ||
        state->native->syncedCtrl->terrain->SetTerrainTypeData == nullptr)
        return Trap("SetTerrainTypeData borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 8u)
        return Trap("SetTerrainTypeData borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 9u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[7].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetTerrainTypeDataQuery query{};
    query.typeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.typeIndex)>>>(slots[0].i32);
    query.tankSpeed = slots[1].f32;
    query.kbotSpeed = slots[2].f32;
    query.hoverSpeed = slots[3].f32;
    query.shipSpeed = slots[4].f32;
    query.hardness = slots[5].f32;
    query.receiveTracks = slots[6].i32 != 0;
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (nameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, static_cast<std::size_t>(nameCount) + 1u, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (nameBytes[nameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.name = reinterpret_cast<const char*>(nameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetTerrainTypeDataResult result{};
    state->native->syncedCtrl->terrain->SetTerrainTypeData(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_projectile_control_set_projectile_ceg(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->projectile == nullptr ||
        state->native->syncedCtrl->projectile->SetProjectileCEG == nullptr)
        return Trap("SetProjectileCEG borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("SetProjectileCEG borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetProjectileCEGQuery query{};
    query.projectileID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.projectileID)>>>(slots[0].i32);
    std::uint32_t cegNamePointer = 0;
    std::uint32_t cegNameCount = 0;
    if (!reader.U32(cegNamePointer) || !reader.U32(cegNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (cegNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> cegNameBytes;
    if (!state->memory.View(cegNamePointer, static_cast<std::size_t>(cegNameCount) + 1u, cegNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (cegNameBytes[cegNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.cegName = reinterpret_cast<const char*>(cegNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetProjectileCEGResult result{};
    state->native->syncedCtrl->projectile->SetProjectileCEG(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.cegID), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_projectile_control_set_projectile_damages(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->projectile == nullptr ||
        state->native->syncedCtrl->projectile->SetProjectileDamages == nullptr)
        return Trap("SetProjectileDamages borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 4u)
        return Trap("SetProjectileDamages borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetProjectileDamagesQuery query{};
    query.projectileID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.projectileID)>>>(slots[0].i32);
    query.unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unused)>>>(slots[1].i32);
    std::uint32_t damageKeyPointer = 0;
    std::uint32_t damageKeyCount = 0;
    if (!reader.U32(damageKeyPointer) || !reader.U32(damageKeyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (damageKeyCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> damageKeyBytes;
    if (!state->memory.View(damageKeyPointer, static_cast<std::size_t>(damageKeyCount) + 1u, damageKeyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (damageKeyBytes[damageKeyCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.damageKey = reinterpret_cast<const char*>(damageKeyBytes.data());
    query.damageValue = slots[2].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetProjectileDamagesResult result{};
    state->native->syncedCtrl->projectile->SetProjectileDamages(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreBorrowed_cob_script_get_cob_script_id(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->cobScript == nullptr ||
        state->native->syncedCtrl->cobScript->GetCOBScriptID == nullptr)
        return Trap("GetCOBScriptID borrowed Core binding is unavailable");
    if (slots == nullptr || slotCount != 2u)
        return Trap("GetCOBScriptID borrowed Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok()) return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError)) return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetCOBScriptIDQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t funcNamePointer = 0;
    std::uint32_t funcNameCount = 0;
    if (!reader.U32(funcNamePointer) || !reader.U32(funcNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (funcNameCount == std::numeric_limits<std::uint32_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> funcNameBytes;
    if (!state->memory.View(funcNamePointer, static_cast<std::size_t>(funcNameCount) + 1u, funcNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    if (funcNameBytes[funcNameCount] != 0) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.funcName = reinterpret_cast<const char*>(funcNameBytes.data());
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetCOBScriptIDResult result{};
    state->native->syncedCtrl->cobScript->GetCOBScriptID(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.funcID), errorCode));
    return nullptr;
}


} // namespace

bool RegisterGeneratedBorrowedImports(wasmtime_linker_t* linker, HostState* state,
    std::string& error)
{
    if (linker == nullptr || state == nullptr || state->native == nullptr) {
        error = "cannot register borrowed Core imports without linker/host/native API";
        return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:units-info", "get-unit-build-params",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_units_info_get_unit_build_params, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:units-info", "get-unit-sensor-radius",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_units_info_get_unit_sensor_radius, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:units-weapons", "get-unit-weapon-state",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_units_weapons_get_unit_weapon_state, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:units-pieces", "get-model-root-piece",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_units_pieces_get_model_root_piece, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:teams", "get-team-resource-stats",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_teams_get_team_resource_stats, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:teams", "get-team-resources",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_teams_get_team_resources, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-defs", "get-unit-def-id-by-name",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_unit_defs_get_unit_def_id_by_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-defs", "get-unit-def-param-bool",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unit_defs_get_unit_def_param_bool, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-defs", "get-unit-def-param-float",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unit_defs_get_unit_def_param_float, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-defs", "get-unit-def-param-int",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unit_defs_get_unit_def_param_int, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-defs", "get-unit-def-param-type",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_unit_defs_get_unit_def_param_type, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:feature-defs", "get-feature-def-id-by-name",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_feature_defs_get_feature_def_id_by_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:weapon-defs", "get-weapon-def-id",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_weapon_defs_get_weapon_def_id, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:math-extra", "bit-bits",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_math_extra_bit_bits, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:math-extra", "diag",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_math_extra_diag, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:encoding", "is-valid-base64",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_encoding_is_valid_base64, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:encoding", "is-valid-base64-url",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_encoding_is_valid_base64_url, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:path-finder", "request-path",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_path_finder_request_path, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:input", "get-key-code",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_input_get_key_code, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:debug-input", "emulate-text-editing",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_debug_input_emulate_text_editing, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:debug-input", "emulate-text-input",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_debug_input_emulate_text_input, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:selection", "deselect-unit-array",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_selection_deselect_unit_array, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:selection", "select-unit-array",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_selection_select_unit_array, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:sound", "load-sound-def",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_sound_load_sound_def, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:sound", "play-sound-file",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_sound_play_sound_file, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:sound", "play-sound-stream",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_sound_play_sound_stream, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:sound", "preload-sound-item",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_sound_preload_sound_item, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:messages", "send-commands",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_messages_send_commands, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:config", "get-config-float",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_config_get_config_float, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:config", "get-config-int",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_config_get_config_int, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:config", "set-config-float",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_config_set_config_float, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:config", "set-config-int",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_config_set_config_int, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:config", "set-config-string",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_config_set_config_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:config", "set-log-section-filter-level",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_config_set_log_section_filter_level, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:utils", "get-cegid",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_utils_get_cegid, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "assign-mouse-cursor",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_unsynced_ctrl_assign_mouse_cursor, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "deselect-unit-map",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_unsynced_ctrl_deselect_unit_map, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "draw-unit-commands",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_unsynced_ctrl_draw_unit_commands, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "load-cmd-colors-config",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_unsynced_ctrl_load_cmd_colors_config, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "load-ctrl-panel-config",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_unsynced_ctrl_load_ctrl_panel_config, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "load-model-textures",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_unsynced_ctrl_load_model_textures, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "replace-mouse-cursor",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unsynced_ctrl_replace_mouse_cursor, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "select-unit-map",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unsynced_ctrl_select_unit_map, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "set-clipboard",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_unsynced_ctrl_set_clipboard, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "set-map-shading-texture",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unsynced_ctrl_set_map_shading_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "set-mouse-cursor",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unsynced_ctrl_set_mouse_cursor, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "set-sky-box-texture",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_unsynced_ctrl_set_sky_box_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "set-unit-def-icon",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unsynced_ctrl_set_unit_def_icon, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "set-unit-def-image",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unsynced_ctrl_set_unit_def_image, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "set-unit-icon",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unsynced_ctrl_set_unit_icon, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "set-wm-caption",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_unsynced_ctrl_set_wm_caption, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "set-wm-icon",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unsynced_ctrl_set_wm_icon, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unsynced-ctrl", "set-water-texture",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_unsynced_ctrl_set_water_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "add-atlas-texture",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_gfx_add_atlas_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "add-fallback-font",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_gfx_add_fallback_font, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "add-feature-defs-to-submission-vao",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_add_feature_defs_to_submission_vao, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "add-features-to-submission-vao",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_add_features_to_submission_vao, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "add-unit-defs-to-submission-vao",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_add_unit_defs_to_submission_vao, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "add-units-to-submission-vao",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_add_units_to_submission_vao, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "bind-image-texture",
                MakeFuncType(params, 7, results, 1), CoreBorrowed_gfx_bind_image_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "bind-texture",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_gfx_bind_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "change-texture-params",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_gfx_change_texture_params, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "copy-to-texture",
                MakeFuncType(params, 9, results, 1), CoreBorrowed_gfx_copy_to_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "create-shader",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_create_shader, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "delete-texture",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_gfx_delete_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "delete-texture-atlas",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_gfx_delete_texture_atlas, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "delete-texture-fbo",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_gfx_delete_texture_fbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "finalize-texture-atlas",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_gfx_finalize_texture_atlas, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_F32, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "font-get-text-height",
                MakeFuncType(params, 6, results, 1), CoreBorrowed_gfx_font_get_text_height, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_F32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "font-get-text-width",
                MakeFuncType(params, 5, results, 1), CoreBorrowed_gfx_font_get_text_width, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_F32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "font-print",
                MakeFuncType(params, 5, results, 1), CoreBorrowed_gfx_font_print, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "font-print-world",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_gfx_font_print_world, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "generate-mipmap",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_gfx_generate_mipmap, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "get-atlas-texture",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_get_atlas_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "get-fixed-state",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_get_fixed_state, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "get-global-tex-coords",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_get_global_tex_coords, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "get-subroutine-index",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_gfx_get_subroutine_index, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "get-text-height",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_get_text_height, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "get-text-width",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_gfx_get_text_width, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "get-uniform-location",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_get_uniform_location, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "has-extension",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_gfx_has_extension, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "instance-data-from-feature-defs-vbo",
                MakeFuncType(params, 5, results, 1), CoreBorrowed_gfx_instance_data_from_feature_defs_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "instance-data-from-features-vbo",
                MakeFuncType(params, 5, results, 1), CoreBorrowed_gfx_instance_data_from_features_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "instance-data-from-unit-defs-vbo",
                MakeFuncType(params, 5, results, 1), CoreBorrowed_gfx_instance_data_from_unit_defs_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "instance-data-from-units-vbo",
                MakeFuncType(params, 5, results, 1), CoreBorrowed_gfx_instance_data_from_units_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "load-font",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_gfx_load_font, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "matrix-data-from-projectiles-vbo",
                MakeFuncType(params, 5, results, 1), CoreBorrowed_gfx_matrix_data_from_projectiles_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "object-label",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_gfx_object_label, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "push-debug-group",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_gfx_push_debug_group, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "save-image",
                MakeFuncType(params, 6, results, 1), CoreBorrowed_gfx_save_image, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "set-fbo-attachment",
                MakeFuncType(params, 7, results, 1), CoreBorrowed_gfx_set_fbo_attachment, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "set-fbo-draw-buffers",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_set_fbo_draw_buffers, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "set-feature-buffer-uniforms",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_gfx_set_feature_buffer_uniforms, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "set-unit-buffer-uniforms",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_gfx_set_unit_buffer_uniforms, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_F32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "text",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_gfx_text, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "texture-info",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_texture_info, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "uniform-array-float",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_uniform_array_float, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "uniform-array-int",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_gfx_uniform_array_int, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "uniform-matrix",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_gfx_uniform_matrix, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "upload-texture",
                MakeFuncType(params, 11, results, 1), CoreBorrowed_gfx_upload_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:gfx", "upload-vbo",
                MakeFuncType(params, 6, results, 1), CoreBorrowed_gfx_upload_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_F32, WASM_I32, WASM_F32, WASM_F32, WASM_F32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:icons", "add-unit-icon",
                MakeFuncType(params, 8, results, 1), CoreBorrowed_icons_add_unit_icon, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:icons", "free-unit-icon",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_icons_free_unit_icon, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:markers", "add-world-text",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_markers_add_world_text, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:markers", "marker-add-point",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_markers_marker_add_point, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:ground-decals", "set-ground-decal-texture",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_ground_decals_set_ground_decal_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:system-control", "clear-watch-dog-timer",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_system_control_clear_watch_dog_timer, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:system-control", "reload",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_system_control_reload, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:system-control", "restart",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_system_control_restart, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:system-control", "set-share-level",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_system_control_set_share_level, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:system-control", "share-resources",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_system_control_share_resources, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:system-control", "start",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_system_control_start, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "add-translation-string",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_rml_ui_add_translation_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "clear-document-path-requests",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_rml_ui_clear_document_path_requests, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "context-activate-theme",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_context_activate_theme, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "context-create-data-model",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_context_create_data_model, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "context-create-document",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_context_create_document, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "context-get-document",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_context_get_document, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "context-is-theme-active",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_context_is_theme_active, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "context-load-document",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_context_load_document, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "context-open-data-model",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_context_open_data_model, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "context-process-text-input",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_context_process_text_input, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "context-remove-data-model",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_context_remove_data_model, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "context-remove-event-listener",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_rml_ui_context_remove_event_listener, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "create-context",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_create_context, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "data-model-bind-bool",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_rml_ui_data_model_bind_bool, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "data-model-bind-color",
                MakeFuncType(params, 7, results, 1), CoreBorrowed_rml_ui_data_model_bind_color, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "data-model-bind-float",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_rml_ui_data_model_bind_float, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "data-model-bind-int",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_rml_ui_data_model_bind_int, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "data-model-bind-percent",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_rml_ui_data_model_bind_percent, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "data-model-bind-pixels",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_rml_ui_data_model_bind_pixels, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "data-model-bind-string",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_data_model_bind_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "data-model-set-string",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_data_model_set_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "document-append-to-style-sheet",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_document_append_to_style_sheet, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "document-create-element",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_document_create_element, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "document-create-text-node",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_document_create_text_node, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "document-load-external-script",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_document_load_external_script, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "document-load-inline-script",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_document_load_inline_script, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "document-set-title",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_document_set_title, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-are-pseudo-classes-set",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_are_pseudo_classes_set, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-closest",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_element_closest, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-dispatch-event",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_dispatch_event, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-form-submit",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_form_submit, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-get-element-by-id",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_element_get_element_by_id, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-get-elements-by-class-name-count",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_get_elements_by_class_name_count, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-get-elements-by-tag-name-count",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_get_elements_by_tag_name_count, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-has-attribute",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_has_attribute, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-is-class-set",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_is_class_set, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-is-pseudo-class-set",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_is_pseudo_class_set, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-matches",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_matches, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-query-selector",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_element_query_selector, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-query-selector-all-count",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_query_selector_all_count, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-remove-attribute",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_remove_attribute, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-remove-event-listener",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_rml_ui_element_remove_event_listener, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-set-attribute",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_set_attribute, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-set-class",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_element_set_class, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-set-class-name",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_set_class_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-set-id",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_set_id, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-set-inner-rml",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_element_set_inner_rml, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-set-pseudo-class",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_element_set_pseudo_class, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-tab-set-set-panel",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_element_tab_set_set_panel, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "element-tab-set-set-tab",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_element_tab_set_set_tab, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "event-get-parameter-bool",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_event_get_parameter_bool, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "event-get-parameter-float",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_event_get_parameter_float, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "event-get-parameter-int",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_event_get_parameter_int, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "event-get-parameter-type",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_rml_ui_event_get_parameter_type, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "get-context",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_get_context, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "load-font-face",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_load_font_face, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "regiser-event-type",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_rml_ui_regiser_event_type, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "register-event-type",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_rml_ui_register_event_type, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "remove-context-by-name",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_rml_ui_remove_context_by_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "set-debug-context-by-name",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_rml_ui_set_debug_context_by_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "set-mouse-cursor-alias",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_rml_ui_set_mouse_cursor_alias, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:rml-ui", "sol-lua-data-model-set-dirty",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_rml_ui_sol_lua_data_model_set_dirty, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:vfs", "compress-folder",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_vfs_compress_folder, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:vfs", "create-dir",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_vfs_create_dir, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedBorrowed(linker, "spring:vfs", "download-archive",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_vfs_download_archive, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:vfs", "extract-mod-archive-file",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_vfs_extract_mod_archive_file, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:vfs", "file-exists",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_vfs_file_exists, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:vfs", "get-file-size",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_vfs_get_file_size, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:vfs", "get-map-square-texture",
                MakeFuncType(params, 5, results, 1), CoreBorrowed_vfs_get_map_square_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:vfs", "has-archive",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_vfs_has_archive, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:vfs", "is-directory",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_vfs_is_directory, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:vfs", "set-map-square-texture",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_vfs_set_map_square_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:team-control", "add-team-resource",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_team_control_add_team_resource, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:team-control", "add-team-resource-excess-stats",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_team_control_add_team_resource_excess_stats, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:team-control", "game-over",
                MakeFuncType(params, 1, results, 1), CoreBorrowed_team_control_game_over, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:team-control", "set-team-resource",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_team_control_set_team_resource, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:team-control", "set-team-share-level",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_team_control_set_team_share_level, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:team-control", "share-team-resource",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_team_control_share_team_resource, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:team-control", "use-team-resource",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_team_control_use_team_resource, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "add-unit-resource",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_unit_control_add_unit_resource, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "bugger-off",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_unit_control_bugger_off, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "give-order-to-unit-array",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_unit_control_give_order_to_unit_array, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "set-unit-build-params",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unit_control_set_unit_build_params, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "set-unit-flanking",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unit_control_set_unit_flanking, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "set-unit-nano-pieces",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unit_control_set_unit_nano_pieces, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "set-unit-resourcing",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_unit_control_set_unit_resourcing, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "set-unit-sensor-radius",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_unit_control_set_unit_sensor_radius, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "set-unit-storage",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_unit_control_set_unit_storage, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "set-unit-tooltip",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_unit_control_set_unit_tooltip, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "set-unit-weapon-damages",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_unit_control_set_unit_weapon_damages, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "set-unit-weapon-state",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_unit_control_set_unit_weapon_state, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:unit-control", "use-unit-resource",
                MakeFuncType(params, 3, results, 1), CoreBorrowed_unit_control_use_unit_resource, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_F32, WASM_F32, WASM_F32, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:terrain-control", "set-terrain-type-data",
                MakeFuncType(params, 8, results, 1), CoreBorrowed_terrain_control_set_terrain_type_data, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:projectile-control", "set-projectile-ceg",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_projectile_control_set_projectile_ceg, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:projectile-control", "set-projectile-damages",
                MakeFuncType(params, 4, results, 1), CoreBorrowed_projectile_control_set_projectile_damages, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedBorrowed(linker, "spring:cob-script", "get-cob-script-id",
                MakeFuncType(params, 2, results, 1), CoreBorrowed_cob_script_get_cob_script_id, state, error))
            return false;
    }

    return true;
}

static_assert(208u >= 0u, "generated borrowed Core callback count");

} // namespace recoil::wasm::core::generated
