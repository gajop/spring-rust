package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface los {
  record spring-error { code: s32 }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record get-closest-valid-position-query {
    unit-def-id: s32,
    x: f32,
    z: f32,
    radius: f32,
  }

  record get-closest-valid-position-result {
    position: float3,
  }

  record get-position-los-state-query {
    pos: float3,
    ally-team-id: s32,
  }

  record get-position-los-state-result {
    state: position-los-state,
  }

  record get-radar-error-params-query {
    ally-team-id: s32,
  }

  record get-radar-error-params-result {
    params: radar-error-params,
  }

  record is-pos-in-air-los-query {
    pos: float3,
    ally-team-id: s32,
  }

  record is-pos-in-air-los-result {
    in-air-los: bool,
  }

  record is-pos-in-los-query {
    pos: float3,
    ally-team-id: s32,
  }

  record is-pos-in-los-result {
    in-los: bool,
  }

  record is-pos-in-radar-query {
    pos: float3,
    ally-team-id: s32,
  }

  record is-pos-in-radar-result {
    in-radar: bool,
  }

  record is-unit-in-air-los-query {
    unit-id: s32,
    ally-team-id: s32,
  }

  record is-unit-in-air-los-result {
    in-air-los: bool,
  }

  record is-unit-in-jammer-query {
    unit-id: s32,
    ally-team-id: s32,
  }

  record is-unit-in-jammer-result {
    in-jammer: bool,
  }

  record is-unit-in-los-query {
    unit-id: s32,
    ally-team-id: s32,
  }

  record is-unit-in-los-result {
    in-los: bool,
  }

  record is-unit-in-radar-query {
    unit-id: s32,
    ally-team-id: s32,
  }

  record is-unit-in-radar-result {
    in-radar: bool,
  }

  record position-los-state {
    in-los-or-radar: bool,
    in-los: bool,
    in-radar: bool,
    in-jammer: bool,
  }

  record radar-error-params {
    radar-error-size: f32,
    base-radar-error-size: f32,
    base-radar-error-mult: f32,
  }

  /// read-only
  get-closest-valid-position: func(unit-def-id: s32, x: f32, z: f32, radius: f32) -> result<float3, spring-error>;

  /// read-only
  get-position-los-state: func(pos: float3, ally-team-id: s32) -> result<position-los-state, spring-error>;

  /// read-only
  get-radar-error-params: func(ally-team-id: s32) -> result<radar-error-params, spring-error>;

  /// read-only
  is-pos-in-air-los: func(pos: float3, ally-team-id: s32) -> result<bool, spring-error>;

  /// read-only
  is-pos-in-los: func(pos: float3, ally-team-id: s32) -> result<bool, spring-error>;

  /// read-only
  is-pos-in-radar: func(pos: float3, ally-team-id: s32) -> result<bool, spring-error>;

  /// read-only
  is-unit-in-air-los: func(unit-id: s32, ally-team-id: s32) -> result<bool, spring-error>;

  /// read-only
  is-unit-in-jammer: func(unit-id: s32, ally-team-id: s32) -> result<bool, spring-error>;

  /// read-only
  is-unit-in-los: func(unit-id: s32, ally-team-id: s32) -> result<bool, spring-error>;

  /// read-only
  is-unit-in-radar: func(unit-id: s32, ally-team-id: s32) -> result<bool, spring-error>;

}
