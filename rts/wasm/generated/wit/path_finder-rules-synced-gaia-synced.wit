package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface path-finder-rules-synced-gaia-synced {
  record spring-error { code: s32 }

  record delete-path-query {
    path-id: u32,
  }

  record delete-path-result {
    success: bool,
  }

  record set-path-node-cost-query {
    overlay-index: u32,
    cost-index: u32,
    cost: f32,
  }

  record set-path-node-cost-result {
    success: bool,
  }

  record set-path-node-costs-query {
    overlay-index: u32,
  }

  record set-path-node-costs-result {
    success: bool,
  }

  /// mutating
  delete-path: func(path-id: u32) -> result<bool, spring-error>;

  /// mutating
  set-path-node-cost: func(overlay-index: u32, cost-index: u32, cost: f32) -> result<bool, spring-error>;

  /// mutating
  set-path-node-costs: func(overlay-index: u32) -> result<bool, spring-error>;

}
