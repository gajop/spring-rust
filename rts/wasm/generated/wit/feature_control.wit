package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface feature-control {
  record spring-error { code: s32 }

  record add-feature-damage-query {
    feature-id: s32,
    damage: f32,
    paralyze-time: f32,
    weapon-def-id: s32,
    attacker-id: s32,
    impulse: float3,
  }

  record add-feature-damage-result {
    success: bool,
  }

  record create-feature-query {
    feature-def: def-ref,
    pos: float3,
    facing: s32,
    team-id: s32,
    feature-id: s32,
  }

  record create-feature-result {
    feature-id: s32,
  }

  record create-feature-wreck-query {
    feature-id: s32,
    wreck-level: s32,
    do-smoke: bool,
  }

  record create-feature-wreck-result {
    feature-id: s32,
  }

  record create-unit-wreck-query {
    unit-id: s32,
    wreck-level: s32,
    do-smoke: bool,
  }

  record create-unit-wreck-result {
    feature-id: s32,
  }

  record def-ref {
    name: string,
    id: s32,
  }

  record destroy-feature-query {
    feature-id: s32,
  }

  record destroy-feature-result {
    success: bool,
  }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record set-feature-always-visible-query {
    feature-id: s32,
    always-visible: bool,
  }

  record set-feature-always-visible-result {
    success: bool,
  }

  record set-feature-blocking-options {
    blocking: bool,
    solid-objects: bool,
    projectiles: bool,
    quad-map-rays: bool,
    crushable: bool,
    block-enemy-pushing: bool,
    block-height-changes: bool,
  }

  record set-feature-blocking-query {
    feature-id: s32,
    options: set-feature-blocking-options,
  }

  record set-feature-blocking-result {
    success: bool,
  }

  record set-feature-collision-volume-data-query {
    feature-id: s32,
    scales: float3,
    offsets: float3,
    volume-type: s32,
    test-type: s32,
    primary-axis: s32,
  }

  record set-feature-collision-volume-data-result {
    success: bool,
  }

  record set-feature-direction-query {
    feature-id: s32,
    front-dir: float3,
    right-dir: float3,
  }

  record set-feature-direction-result {
    success: bool,
  }

  record set-feature-fire-time-query {
    feature-id: s32,
    fire-time: f32,
  }

  record set-feature-fire-time-result {
    success: bool,
  }

  record set-feature-heading-and-up-dir-query {
    feature-id: s32,
    heading: s32,
    up-dir: float3,
  }

  record set-feature-heading-and-up-dir-result {
    success: bool,
  }

  record set-feature-health-query {
    feature-id: s32,
    health: f32,
    check-destruction: bool,
  }

  record set-feature-health-result {
    success: bool,
  }

  record set-feature-mass-query {
    feature-id: s32,
    mass: f32,
  }

  record set-feature-mass-result {
    success: bool,
  }

  record set-feature-max-health-query {
    feature-id: s32,
    max-health: f32,
  }

  record set-feature-max-health-result {
    success: bool,
  }

  record set-feature-mid-and-aim-pos-query {
    feature-id: s32,
    mid-pos: float3,
    aim-pos: float3,
    set-relative: bool,
  }

  record set-feature-mid-and-aim-pos-result {
    success: bool,
  }

  record set-feature-move-ctrl-query {
    feature-id: s32,
    enable: bool,
    velocity-or-mask: float3,
    acceleration-or-impulse-mask: float3,
    movement-mask: float3,
  }

  record set-feature-move-ctrl-result {
    success: bool,
  }

  record set-feature-no-select-query {
    feature-id: s32,
    no-select: bool,
  }

  record set-feature-no-select-result {
    success: bool,
  }

  record set-feature-physics-query {
    feature-id: s32,
    pos: float3,
    velocity: float3,
    rotation: float3,
    drag: float3,
  }

  record set-feature-physics-result {
    success: bool,
  }

  record set-feature-piece-collision-volume-data-query {
    feature-id: s32,
    piece-index: s32,
    enable: bool,
    scales: float3,
    offsets: float3,
    volume-type: s32,
    primary-axis: s32,
  }

  record set-feature-piece-collision-volume-data-result {
    success: bool,
  }

  record set-feature-piece-matrix-query {
    feature-id: s32,
    piece-index: s32,
    matrix: list<f32>,
  }

  record set-feature-piece-matrix-result {
    block-script-anims: bool,
  }

  record set-feature-piece-visible-query {
    feature-id: s32,
    piece-index: s32,
    visible: bool,
  }

  record set-feature-piece-visible-result {
    success: bool,
  }

  record set-feature-position-query {
    feature-id: s32,
    pos: float3,
    snap-to-ground: bool,
  }

  record set-feature-position-result {
    success: bool,
  }

  record set-feature-radius-and-height-query {
    feature-id: s32,
    radius: f32,
    height: f32,
  }

  record set-feature-radius-and-height-result {
    success: bool,
  }

  record set-feature-reclaim-query {
    feature-id: s32,
    reclaim-left: f32,
  }

  record set-feature-reclaim-result {
    success: bool,
  }

  record set-feature-resources-query {
    feature-id: s32,
    metal: f32,
    energy: f32,
    reclaim-time: f32,
    reclaim-left: f32,
    feature-def-metal: f32,
    feature-def-energy: f32,
  }

  record set-feature-resources-result {
    success: bool,
  }

  record set-feature-resurrect-query {
    feature-id: s32,
    unit-def: def-ref,
    facing: s32,
    progress: f32,
  }

  record set-feature-resurrect-result {
    success: bool,
  }

  record set-feature-rotation-query {
    feature-id: s32,
    rotation: float3,
  }

  record set-feature-rotation-result {
    success: bool,
  }

  record set-feature-selection-volume-data-query {
    feature-id: s32,
    scales: float3,
    offsets: float3,
    volume-type: s32,
    primary-axis: s32,
    use-cont-hit-test: bool,
  }

  record set-feature-selection-volume-data-result {
    success: bool,
  }

  record set-feature-smoke-time-query {
    feature-id: s32,
    smoke-time: f32,
  }

  record set-feature-smoke-time-result {
    success: bool,
  }

  record set-feature-use-air-los-query {
    feature-id: s32,
    use-air-los: bool,
  }

  record set-feature-use-air-los-result {
    success: bool,
  }

  record set-feature-velocity-query {
    feature-id: s32,
    velocity: float3,
  }

  record set-feature-velocity-result {
    success: bool,
  }

  record transfer-feature-query {
    feature-id: s32,
    new-team-id: s32,
  }

  record transfer-feature-result {
    success: bool,
  }

  /// mutating
  add-feature-damage: func(feature-id: s32, damage: f32, paralyze-time: f32, weapon-def-id: s32, attacker-id: s32, impulse: float3) -> result<bool, spring-error>;

  /// mutating
  create-feature: func(feature-def: def-ref, pos: float3, facing: s32, team-id: s32, feature-id: s32) -> result<s32, spring-error>;

  /// mutating
  create-feature-wreck: func(feature-id: s32, wreck-level: s32, do-smoke: bool) -> result<s32, spring-error>;

  /// mutating
  create-unit-wreck: func(unit-id: s32, wreck-level: s32, do-smoke: bool) -> result<s32, spring-error>;

  /// mutating
  destroy-feature: func(feature-id: s32) -> result<bool, spring-error>;

  /// mutating
  set-feature-always-visible: func(feature-id: s32, always-visible: bool) -> result<bool, spring-error>;

  /// mutating
  set-feature-blocking: func(feature-id: s32, options: set-feature-blocking-options) -> result<bool, spring-error>;

  /// mutating
  set-feature-collision-volume-data: func(feature-id: s32, scales: float3, offsets: float3, volume-type: s32, test-type: s32, primary-axis: s32) -> result<bool, spring-error>;

  /// mutating
  set-feature-direction: func(feature-id: s32, front-dir: float3, right-dir: float3) -> result<bool, spring-error>;

  /// mutating
  set-feature-fire-time: func(feature-id: s32, fire-time: f32) -> result<bool, spring-error>;

  /// mutating
  set-feature-heading-and-up-dir: func(feature-id: s32, heading: s32, up-dir: float3) -> result<bool, spring-error>;

  /// mutating
  set-feature-health: func(feature-id: s32, health: f32, check-destruction: bool) -> result<bool, spring-error>;

  /// mutating
  set-feature-mass: func(feature-id: s32, mass: f32) -> result<bool, spring-error>;

  /// mutating
  set-feature-max-health: func(feature-id: s32, max-health: f32) -> result<bool, spring-error>;

  /// mutating
  set-feature-mid-and-aim-pos: func(feature-id: s32, mid-pos: float3, aim-pos: float3, set-relative: bool) -> result<bool, spring-error>;

  /// mutating
  set-feature-move-ctrl: func(feature-id: s32, enable: bool, velocity-or-mask: float3, acceleration-or-impulse-mask: float3, movement-mask: float3) -> result<bool, spring-error>;

  /// mutating
  set-feature-no-select: func(feature-id: s32, no-select: bool) -> result<bool, spring-error>;

  /// mutating
  set-feature-physics: func(feature-id: s32, pos: float3, velocity: float3, rotation: float3, drag: float3) -> result<bool, spring-error>;

  /// mutating
  set-feature-piece-collision-volume-data: func(feature-id: s32, piece-index: s32, enable: bool, scales: float3, offsets: float3, volume-type: s32, primary-axis: s32) -> result<bool, spring-error>;

  /// mutating
  set-feature-piece-matrix: func(feature-id: s32, piece-index: s32, matrix: list<f32>) -> result<bool, spring-error>;

  /// mutating
  set-feature-piece-visible: func(feature-id: s32, piece-index: s32, visible: bool) -> result<bool, spring-error>;

  /// mutating
  set-feature-position: func(feature-id: s32, pos: float3, snap-to-ground: bool) -> result<bool, spring-error>;

  /// mutating
  set-feature-radius-and-height: func(feature-id: s32, radius: f32, height: f32) -> result<bool, spring-error>;

  /// mutating
  set-feature-reclaim: func(feature-id: s32, reclaim-left: f32) -> result<bool, spring-error>;

  /// mutating
  set-feature-resources: func(feature-id: s32, metal: f32, energy: f32, reclaim-time: f32, reclaim-left: f32, feature-def-metal: f32, feature-def-energy: f32) -> result<bool, spring-error>;

  /// mutating
  set-feature-resurrect: func(feature-id: s32, unit-def: def-ref, facing: s32, progress: f32) -> result<bool, spring-error>;

  /// mutating
  set-feature-rotation: func(feature-id: s32, rotation: float3) -> result<bool, spring-error>;

  /// mutating
  set-feature-selection-volume-data: func(feature-id: s32, scales: float3, offsets: float3, volume-type: s32, primary-axis: s32, use-cont-hit-test: bool) -> result<bool, spring-error>;

  /// mutating
  set-feature-smoke-time: func(feature-id: s32, smoke-time: f32) -> result<bool, spring-error>;

  /// mutating
  set-feature-use-air-los: func(feature-id: s32, use-air-los: bool) -> result<bool, spring-error>;

  /// mutating
  set-feature-velocity: func(feature-id: s32, velocity: float3) -> result<bool, spring-error>;

  /// mutating
  transfer-feature: func(feature-id: s32, new-team-id: s32) -> result<bool, spring-error>;

}
