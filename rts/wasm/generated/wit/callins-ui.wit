package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface callins-ui {
  record spring-error { code: s32 }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record game-progress-query {
    game-frame: s32,
  }

  record game-setup-player-state {
    player-id: s32,
    state: string,
  }

  record game-setup-query {
    state: string,
    ready: bool,
    player-states: list<game-setup-player-state>,
  }

  record game-setup-result {
    handled: bool,
    ready: bool,
  }

  record pong-query {
    ping-tag: u8,
    packet-send-time-millis: s64,
    packet-recv-time-millis: s64,
  }

  record string-callin-result {
    value: string,
  }

  record update-query {
    delta-seconds: f32,
  }

  record world-tooltip-query {
    kind: s32,
    unit-id: s32,
    feature-id: s32,
    ground-pos: float3,
  }

  /// aggregation: ignore
  game-progress: func(query: game-progress-query) -> result<_, spring-error>;

  /// aggregation: ignore
  game-setup: func(query: game-setup-query) -> result<game-setup-result, spring-error>;

  /// aggregation: ignore
  pong: func(query: pong-query) -> result<_, spring-error>;

  /// aggregation: ignore
  update: func(query: update-query) -> result<_, spring-error>;

  /// aggregation: first-non-empty
  world-tooltip: func(query: world-tooltip-query) -> result<string-callin-result, spring-error>;

}
