package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface path-finder-rules-synced-rules-unsynced-gaia-synced-gaia-unsynced-ui {
  record spring-error { code: s32 }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record free-path-node-costs-array-query {
    overlay-index: u32,
  }

  record free-path-node-costs-array-result {
    success: bool,
  }

  record get-next-way-point-query {
    path-id: u32,
    caller-pos: float3,
    min-dist: f32,
  }

  record get-next-way-point-result {
    waypoint: option<float3>,
  }

  record get-path-node-cost-query {
    x: u32,
    z: u32,
  }

  record get-path-node-cost-result {
    cost: f32,
  }

  record get-path-node-costs-query {
    overlay-index: u32,
  }

  record get-path-node-costs-result {
    costs: list<f32>,
  }

  record get-path-way-points-query {
    path-id: u32,
  }

  record get-path-way-points-result {
    points: list<float3>,
    starts: list<s32>,
  }

  record init-path-node-costs-array-query {
    overlay-index: u32,
    size-x: u32,
    size-z: u32,
  }

  record init-path-node-costs-array-result {
    success: bool,
  }

  record request-path-query {
    move-def-id: u32,
    move-def-name: option<string>,
    start-pos: float3,
    end-pos: float3,
    radius: f32,
  }

  record request-path-result {
    path-id: u32,
  }

  /// read-only
  free-path-node-costs-array: func(overlay-index: u32) -> result<bool, spring-error>;

  /// read-only
  get-next-way-point: func(path-id: u32, caller-pos: float3, min-dist: f32) -> result<option<float3>, spring-error>;

  /// read-only
  get-path-node-cost: func(x: u32, z: u32) -> result<f32, spring-error>;

  /// read-only
  get-path-node-costs: func(overlay-index: u32) -> result<list<f32>, spring-error>;

  record get-path-way-points-value {
    points: list<float3>,
    starts: list<s32>,
  }

  /// read-only
  get-path-way-points: func(path-id: u32) -> result<get-path-way-points-value, spring-error>;

  /// read-only
  init-path-node-costs-array: func(overlay-index: u32, size-x: u32, size-z: u32) -> result<bool, spring-error>;

  /// read-only
  request-path: func(move-def-id: u32, move-def-name: option<string>, start-pos: float3, end-pos: float3, radius: f32) -> result<u32, spring-error>;

}
