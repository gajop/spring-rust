package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface utils {
  record spring-error { code: s32 }

  record closest-build-pos-query {
    team-id: s32,
    unit-def-id: s32,
    pos: float3,
    search-radius: f32,
    min-dist: s32,
    facing: s32,
  }

  record closest-build-pos-result {
    build-pos: float3,
  }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record get-cegid-query {
    ceg-name: string,
  }

  record get-cegid-result {
    ceg-id: s32,
  }

  record get-feature-def-dimensions-query {
    feature-def-id: s32,
  }

  record get-feature-def-dimensions-result {
    dimensions: unit-def-dimensions,
  }

  record get-unit-def-dimensions-query {
    unit-def-id: s32,
  }

  record get-unit-def-dimensions-result {
    dimensions: unit-def-dimensions,
  }

  record pos2-build-pos-query {
    unit-def-id: s32,
    pos: float3,
    facing: s32,
  }

  record pos2-build-pos-result {
    build-pos: float3,
  }

  record test-build-order-query {
    unit-def-id: s32,
    pos: float3,
    facing: s32,
  }

  record test-build-order-result {
    status: s32,
    can-build: bool,
    feature: s32,
  }

  record test-move-order-options {
    test-terrain: bool,
    test-objects: bool,
    center-only: bool,
  }

  record test-move-order-query {
    unit-def-id: s32,
    pos: float3,
    dir: float3,
    options: test-move-order-options,
  }

  record test-move-order-result {
    can-move: bool,
  }

  record unit-def-dimensions {
    height: f32,
    radius: f32,
    midx: f32,
    minx: f32,
    maxx: f32,
    midy: f32,
    miny: f32,
    maxy: f32,
    midz: f32,
    minz: f32,
    maxz: f32,
  }

  /// read-only
  closest-build-pos: func(team-id: s32, unit-def-id: s32, pos: float3, search-radius: f32, min-dist: s32, facing: s32) -> result<float3, spring-error>;

  /// read-only
  get-cegid: func(ceg-name: string) -> result<s32, spring-error>;

  /// read-only
  get-feature-def-dimensions: func(feature-def-id: s32) -> result<unit-def-dimensions, spring-error>;

  /// read-only
  get-unit-def-dimensions: func(unit-def-id: s32) -> result<unit-def-dimensions, spring-error>;

  /// read-only
  pos2-build-pos: func(unit-def-id: s32, pos: float3, facing: s32) -> result<float3, spring-error>;

  record test-build-order-value {
    status: s32,
    can-build: bool,
    feature: s32,
  }

  /// read-only
  test-build-order: func(unit-def-id: s32, pos: float3, facing: s32) -> result<test-build-order-value, spring-error>;

  /// read-only
  test-move-order: func(unit-def-id: s32, pos: float3, dir: float3, options: test-move-order-options) -> result<bool, spring-error>;

}
