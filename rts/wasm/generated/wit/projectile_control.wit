package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface projectile-control {
  record spring-error { code: s32 }

  record delete-projectile-query {
    projectile-id: s32,
  }

  record delete-projectile-result {
    success: bool,
  }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record native-projectile-params {
    pos: float3,
    speed: float3,
    spread: float3,
    end: float3,
    owner: s32,
    team: s32,
    weapon-num: s32,
    ttl: f32,
    gravity: f32,
    tracking: f32,
    max-range: f32,
    up-time: f32,
    start-alpha: f32,
    end-alpha: f32,
    model: string,
    ceg-tag: string,
  }

  record projectile-target-ref {
    target-id: s32,
    target-type: s32,
    pos: float3,
    is-ground-target: bool,
  }

  record set-piece-projectile-params-query {
    projectile-id: s32,
    expl-flags: s32,
    spin-angle: f32,
    spin-speed: f32,
    spin-vec: float3,
  }

  record set-piece-projectile-params-result {
    success: bool,
  }

  record set-projectile-always-visible-query {
    projectile-id: s32,
    always-visible: bool,
  }

  record set-projectile-always-visible-result {
    success: bool,
  }

  record set-projectile-ceg-query {
    projectile-id: s32,
    ceg-name: string,
  }

  record set-projectile-ceg-result {
    ceg-id: s32,
  }

  record set-projectile-collision-query {
    projectile-id: s32,
  }

  record set-projectile-collision-result {
    success: bool,
  }

  record set-projectile-damages-query {
    projectile-id: s32,
    unused: s32,
    damage-key: string,
    damage-value: f32,
  }

  record set-projectile-damages-result {
    success: bool,
  }

  record set-projectile-gravity-query {
    projectile-id: s32,
    gravity: f32,
  }

  record set-projectile-gravity-result {
    success: bool,
  }

  record set-projectile-ignore-tracking-error-query {
    projectile-id: s32,
    ignore: bool,
  }

  record set-projectile-ignore-tracking-error-result {
    success: bool,
  }

  record set-projectile-is-intercepted-query {
    projectile-id: s32,
    intercepted: bool,
  }

  record set-projectile-is-intercepted-result {
    success: bool,
  }

  record set-projectile-move-control-query {
    projectile-id: s32,
    enable: bool,
  }

  record set-projectile-move-control-result {
    success: bool,
  }

  record set-projectile-position-query {
    projectile-id: s32,
    pos: float3,
  }

  record set-projectile-position-result {
    success: bool,
  }

  record set-projectile-target-query {
    projectile-id: s32,
    target: projectile-target-ref,
  }

  record set-projectile-target-result {
    success: bool,
  }

  record set-projectile-time-to-live-query {
    projectile-id: s32,
    time-to-live: s32,
  }

  record set-projectile-time-to-live-result {
    success: bool,
  }

  record set-projectile-use-air-los-query {
    projectile-id: s32,
    use-air-los: bool,
  }

  record set-projectile-use-air-los-result {
    success: bool,
  }

  record set-projectile-velocity-query {
    projectile-id: s32,
    velocity: float3,
  }

  record set-projectile-velocity-result {
    success: bool,
  }

  record spawn-projectile-query {
    weapon-def-id: s32,
    projectile-params: native-projectile-params,
  }

  record spawn-projectile-result {
    projectile-id: s32,
  }

  /// mutating
  delete-projectile: func(projectile-id: s32) -> result<bool, spring-error>;

  /// mutating
  set-piece-projectile-params: func(projectile-id: s32, expl-flags: s32, spin-angle: f32, spin-speed: f32, spin-vec: float3) -> result<bool, spring-error>;

  /// mutating
  set-projectile-always-visible: func(projectile-id: s32, always-visible: bool) -> result<bool, spring-error>;

  /// mutating
  set-projectile-ceg: func(projectile-id: s32, ceg-name: string) -> result<s32, spring-error>;

  /// mutating
  set-projectile-collision: func(projectile-id: s32) -> result<bool, spring-error>;

  /// mutating
  set-projectile-damages: func(projectile-id: s32, unused: s32, damage-key: string, damage-value: f32) -> result<bool, spring-error>;

  /// mutating
  set-projectile-gravity: func(projectile-id: s32, gravity: f32) -> result<bool, spring-error>;

  /// mutating
  set-projectile-ignore-tracking-error: func(projectile-id: s32, ignore: bool) -> result<bool, spring-error>;

  /// mutating
  set-projectile-is-intercepted: func(projectile-id: s32, intercepted: bool) -> result<bool, spring-error>;

  /// mutating
  set-projectile-move-control: func(projectile-id: s32, enable: bool) -> result<bool, spring-error>;

  /// mutating
  set-projectile-position: func(projectile-id: s32, pos: float3) -> result<bool, spring-error>;

  /// mutating
  set-projectile-target: func(projectile-id: s32, target: projectile-target-ref) -> result<bool, spring-error>;

  /// mutating
  set-projectile-time-to-live: func(projectile-id: s32, time-to-live: s32) -> result<bool, spring-error>;

  /// mutating
  set-projectile-use-air-los: func(projectile-id: s32, use-air-los: bool) -> result<bool, spring-error>;

  /// mutating
  set-projectile-velocity: func(projectile-id: s32, velocity: float3) -> result<bool, spring-error>;

  /// mutating
  spawn-projectile: func(weapon-def-id: s32, projectile-params: native-projectile-params) -> result<s32, spring-error>;

}
