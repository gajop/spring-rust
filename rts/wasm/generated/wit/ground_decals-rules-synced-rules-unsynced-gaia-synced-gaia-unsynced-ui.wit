package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface ground-decals-rules-synced-rules-unsynced-gaia-synced-gaia-unsynced-ui {
  record spring-error { code: s32 }

  record create-ground-decal-query {
    unused: u8,
  }

  record create-ground-decal-result {
    decal-id: u32,
    success: bool,
  }

  record destroy-ground-decal-query {
    decal-id: u32,
  }

  record destroy-ground-decal-result {
    success: bool,
  }

  record set-ground-decal-alpha-query {
    decal-id: u32,
    alpha: f32,
    alpha-falloff: f32,
  }

  record set-ground-decal-alpha-result {
    success: bool,
  }

  record set-ground-decal-creation-frame-query {
    decal-id: u32,
    creation-frame-min: f32,
    creation-frame-max: f32,
  }

  record set-ground-decal-creation-frame-result {
    success: bool,
  }

  record set-ground-decal-glow-params-query {
    decal-id: u32,
    glow: f32,
    glow-falloff: f32,
  }

  record set-ground-decal-glow-params-result {
    success: bool,
  }

  record set-ground-decal-misc-query {
    decal-id: u32,
    dot-elim-exp: f32,
    ref-height: f32,
    min-height: f32,
    max-height: f32,
    force-height-mode: f32,
  }

  record set-ground-decal-misc-result {
    success: bool,
  }

  record set-ground-decal-normal-query {
    decal-id: u32,
    normal-x: f32,
    normal-y: f32,
    normal-z: f32,
  }

  record set-ground-decal-normal-result {
    success: bool,
  }

  record set-ground-decal-pos-and-dims-query {
    decal-id: u32,
    mid-pos-x: f32,
    mid-pos-z: f32,
    size-x: f32,
    size-z: f32,
    proj-cube-height: f32,
  }

  record set-ground-decal-pos-and-dims-result {
    success: bool,
  }

  record set-ground-decal-quad-pos-and-height-query {
    decal-id: u32,
    pos-tlx: f32,
    pos-tly: f32,
    pos-trx: f32,
    pos-try: f32,
    pos-brx: f32,
    pos-bry: f32,
    pos-blx: f32,
    pos-bly: f32,
    proj-cube-height: f32,
  }

  record set-ground-decal-quad-pos-and-height-result {
    success: bool,
  }

  record set-ground-decal-rotation-query {
    decal-id: u32,
    rotation: f32,
  }

  record set-ground-decal-rotation-result {
    success: bool,
  }

  record set-ground-decal-texture-params-query {
    decal-id: u32,
    tex-wrap-distance: f32,
    tex-traveled-distance: f32,
  }

  record set-ground-decal-texture-params-result {
    success: bool,
  }

  record set-ground-decal-texture-query {
    decal-id: u32,
    texture-name: string,
    main-tex: bool,
  }

  record set-ground-decal-texture-result {
    success: bool,
  }

  record set-ground-decal-tint-query {
    decal-id: u32,
    tint-r: f32,
    tint-g: f32,
    tint-b: f32,
    tint-a: f32,
  }

  record set-ground-decal-tint-result {
    success: bool,
  }

  record set-ground-decal-user-data-query {
    decal-id: u32,
    quad-index: u32,
    value-x: f32,
    value-y: f32,
    value-z: f32,
    value-w: f32,
  }

  record set-ground-decal-user-data-result {
    success: bool,
  }

  record create-ground-decal-value {
    decal-id: u32,
    success: bool,
  }

  /// read-only
  create-ground-decal: func(unused: u8) -> result<create-ground-decal-value, spring-error>;

  /// read-only
  destroy-ground-decal: func(decal-id: u32) -> result<bool, spring-error>;

  /// read-only
  set-ground-decal-alpha: func(decal-id: u32, alpha: f32, alpha-falloff: f32) -> result<bool, spring-error>;

  /// read-only
  set-ground-decal-creation-frame: func(decal-id: u32, creation-frame-min: f32, creation-frame-max: f32) -> result<bool, spring-error>;

  /// read-only
  set-ground-decal-glow-params: func(decal-id: u32, glow: f32, glow-falloff: f32) -> result<bool, spring-error>;

  /// read-only
  set-ground-decal-misc: func(decal-id: u32, dot-elim-exp: f32, ref-height: f32, min-height: f32, max-height: f32, force-height-mode: f32) -> result<bool, spring-error>;

  /// read-only
  set-ground-decal-normal: func(decal-id: u32, normal-x: f32, normal-y: f32, normal-z: f32) -> result<bool, spring-error>;

  /// read-only
  set-ground-decal-pos-and-dims: func(decal-id: u32, mid-pos-x: f32, mid-pos-z: f32, size-x: f32, size-z: f32, proj-cube-height: f32) -> result<bool, spring-error>;

  /// read-only
  set-ground-decal-quad-pos-and-height: func(decal-id: u32, pos-tlx: f32, pos-tly: f32, pos-trx: f32, pos-try: f32, pos-brx: f32, pos-bry: f32, pos-blx: f32, pos-bly: f32, proj-cube-height: f32) -> result<bool, spring-error>;

  /// read-only
  set-ground-decal-rotation: func(decal-id: u32, rotation: f32) -> result<bool, spring-error>;

  /// read-only
  set-ground-decal-texture: func(decal-id: u32, texture-name: string, main-tex: bool) -> result<bool, spring-error>;

  /// read-only
  set-ground-decal-texture-params: func(decal-id: u32, tex-wrap-distance: f32, tex-traveled-distance: f32) -> result<bool, spring-error>;

  /// read-only
  set-ground-decal-tint: func(decal-id: u32, tint-r: f32, tint-g: f32, tint-b: f32, tint-a: f32) -> result<bool, spring-error>;

  /// read-only
  set-ground-decal-user-data: func(decal-id: u32, quad-index: u32, value-x: f32, value-y: f32, value-z: f32, value-w: f32) -> result<bool, spring-error>;

}
