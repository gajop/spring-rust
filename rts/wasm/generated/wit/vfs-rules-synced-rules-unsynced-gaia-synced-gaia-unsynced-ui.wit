package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface vfs-rules-synced-rules-unsynced-gaia-synced-gaia-unsynced-ui {
  record spring-error { code: s32 }

  record calculate-hash-query {
    data: list<u8>,
    hash-type: s32,
  }

  record calculate-hash-result {
    hash: string,
  }

  record create-dir-query {
    path: string,
  }

  record create-dir-result {
    success: bool,
  }

  record dir-entry {
    name: string,
    is-directory: bool,
  }

  record extract-mod-archive-file-query {
    path: string,
  }

  record extract-mod-archive-file-result {
    success: bool,
  }

  record file-exists-query {
    path: string,
  }

  record file-exists-result {
    exists: bool,
  }

  record file-info {
    name: string,
    size: u32,
    mode: u32,
    is-directory: bool,
  }

  record get-archives-query {
    unused: u8,
  }

  record get-archives-result {
    archives: list<string>,
  }

  record get-file-info-query {
    path: string,
  }

  record get-file-info-result {
    info: file-info,
    exists: bool,
  }

  record get-file-size-query {
    path: string,
  }

  record get-file-size-result {
    size: u32,
  }

  record get-map-square-texture-info-query {
    unused: u8,
  }

  record get-map-square-texture-info-result {
    square-size: s32,
    num-squares-x: s32,
    num-squares-z: s32,
  }

  record is-directory-query {
    path: string,
  }

  record is-directory-result {
    is-directory: bool,
  }

  record list-dir-query {
    path: string,
    pattern: string,
    mode: string,
    recursive: bool,
  }

  record list-dir-result {
    entries: list<dir-entry>,
  }

  record load-file-query {
    path: string,
    mode: string,
  }

  record load-file-result {
    data: list<u8>,
  }

  record pack-f32-query {
    values: list<f32>,
  }

  record pack-f32-result {
    data: list<u8>,
  }

  record pack-s16-query {
    values: list<s16>,
  }

  record pack-s16-result {
    data: list<u8>,
  }

  record pack-s32-query {
    values: list<s32>,
  }

  record pack-s32-result {
    data: list<u8>,
  }

  record pack-s8-query {
    values: list<s8>,
  }

  record pack-s8-result {
    data: list<u8>,
  }

  record pack-u16-query {
    values: list<u16>,
  }

  record pack-u16-result {
    data: list<u8>,
  }

  record pack-u32-query {
    values: list<u32>,
  }

  record pack-u32-result {
    data: list<u8>,
  }

  record pack-u8-query {
    values: list<u8>,
  }

  record pack-u8-result {
    data: list<u8>,
  }

  record read-file-as-string-query {
    path: string,
  }

  record read-file-as-string-result {
    content: string,
    content-length: u32,
  }

  record read-file-query {
    path: string,
  }

  record read-file-result {
    data: list<u8>,
  }

  record set-map-square-texture-query {
    tex-square-x: s32,
    tex-square-y: s32,
    texture-name: string,
  }

  record set-map-square-texture-result {
    success: bool,
  }

  record sub-dirs-query {
    path: string,
    pattern: string,
    mode: string,
    recursive: bool,
  }

  record sub-dirs-result {
    dirs: list<string>,
  }

  record unpack-f32-query {
    data: list<u8>,
    byte-offset: u32,
    count: u32,
  }

  record unpack-f32-result {
    values: list<f32>,
  }

  record unpack-s16-query {
    data: list<u8>,
    byte-offset: u32,
    count: u32,
  }

  record unpack-s16-result {
    values: list<s16>,
  }

  record unpack-s32-query {
    data: list<u8>,
    byte-offset: u32,
    count: u32,
  }

  record unpack-s32-result {
    values: list<s32>,
  }

  record unpack-s8-query {
    data: list<u8>,
    byte-offset: u32,
    count: u32,
  }

  record unpack-s8-result {
    values: list<s8>,
  }

  record unpack-u16-query {
    data: list<u8>,
    byte-offset: u32,
    count: u32,
  }

  record unpack-u16-result {
    values: list<u16>,
  }

  record unpack-u32-query {
    data: list<u8>,
    byte-offset: u32,
    count: u32,
  }

  record unpack-u32-result {
    values: list<u32>,
  }

  record unpack-u8-query {
    data: list<u8>,
    byte-offset: u32,
    count: u32,
  }

  record unpack-u8-result {
    values: list<u8>,
  }

  record zlib-compress-query {
    data: list<u8>,
  }

  record zlib-compress-result {
    data: list<u8>,
  }

  record zlib-decompress-query {
    data: list<u8>,
  }

  record zlib-decompress-result {
    data: list<u8>,
  }

  /// read-only
  calculate-hash: func(data: list<u8>, hash-type: s32) -> result<string, spring-error>;

  /// read-only
  create-dir: func(path: string) -> result<bool, spring-error>;

  /// read-only
  dir-list: func(path: string, pattern: string, mode: string, recursive: bool) -> result<list<dir-entry>, spring-error>;

  /// read-only
  extract-mod-archive-file: func(path: string) -> result<bool, spring-error>;

  /// read-only
  file-exists: func(path: string) -> result<bool, spring-error>;

  /// read-only
  get-archives: func(unused: u8) -> result<list<string>, spring-error>;

  record get-file-info-value {
    info: file-info,
    exists: bool,
  }

  /// read-only
  get-file-info: func(path: string) -> result<get-file-info-value, spring-error>;

  /// read-only
  get-file-size: func(path: string) -> result<u32, spring-error>;

  record get-map-square-texture-info-value {
    square-size: s32,
    num-squares-x: s32,
    num-squares-z: s32,
  }

  /// read-only
  get-map-square-texture-info: func(unused: u8) -> result<get-map-square-texture-info-value, spring-error>;

  /// read-only
  is-directory: func(path: string) -> result<bool, spring-error>;

  /// read-only
  list-dir: func(path: string, pattern: string, mode: string, recursive: bool) -> result<list<dir-entry>, spring-error>;

  /// read-only
  load-file: func(path: string, mode: string) -> result<list<u8>, spring-error>;

  /// read-only
  pack-f32: func(values: list<f32>) -> result<list<u8>, spring-error>;

  /// read-only
  pack-s16: func(values: list<s16>) -> result<list<u8>, spring-error>;

  /// read-only
  pack-s32: func(values: list<s32>) -> result<list<u8>, spring-error>;

  /// read-only
  pack-s8: func(values: list<s8>) -> result<list<u8>, spring-error>;

  /// read-only
  pack-u16: func(values: list<u16>) -> result<list<u8>, spring-error>;

  /// read-only
  pack-u32: func(values: list<u32>) -> result<list<u8>, spring-error>;

  /// read-only
  pack-u8: func(values: list<u8>) -> result<list<u8>, spring-error>;

  /// read-only
  read-file: func(path: string) -> result<list<u8>, spring-error>;

  /// read-only
  read-file-as-string: func(path: string) -> result<string, spring-error>;

  /// read-only
  set-map-square-texture: func(tex-square-x: s32, tex-square-y: s32, texture-name: string) -> result<bool, spring-error>;

  /// read-only
  sub-dirs: func(path: string, pattern: string, mode: string, recursive: bool) -> result<list<string>, spring-error>;

  /// read-only
  unpack-f32: func(data: list<u8>, byte-offset: u32, count: u32) -> result<list<f32>, spring-error>;

  /// read-only
  unpack-s16: func(data: list<u8>, byte-offset: u32, count: u32) -> result<list<s16>, spring-error>;

  /// read-only
  unpack-s32: func(data: list<u8>, byte-offset: u32, count: u32) -> result<list<s32>, spring-error>;

  /// read-only
  unpack-s8: func(data: list<u8>, byte-offset: u32, count: u32) -> result<list<s8>, spring-error>;

  /// read-only
  unpack-u16: func(data: list<u8>, byte-offset: u32, count: u32) -> result<list<u16>, spring-error>;

  /// read-only
  unpack-u32: func(data: list<u8>, byte-offset: u32, count: u32) -> result<list<u32>, spring-error>;

  /// read-only
  unpack-u8: func(data: list<u8>, byte-offset: u32, count: u32) -> result<list<u8>, spring-error>;

  /// read-only
  zlib-compress: func(data: list<u8>) -> result<list<u8>, spring-error>;

  /// read-only
  zlib-decompress: func(data: list<u8>) -> result<list<u8>, spring-error>;

}
