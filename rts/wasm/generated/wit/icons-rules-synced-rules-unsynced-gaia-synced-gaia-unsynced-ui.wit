package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface icons-rules-synced-rules-unsynced-gaia-synced-gaia-unsynced-ui {
  record spring-error { code: s32 }

  record add-unit-icon-query {
    icon-name: string,
    tex-file: string,
    size: f32,
    distance: f32,
    radius-adjust: bool,
    u0: f32,
    v0: f32,
    u1: f32,
    v1: f32,
  }

  record add-unit-icon-result {
    success: bool,
  }

  record free-unit-icon-query {
    icon-name: string,
  }

  record free-unit-icon-result {
    success: bool,
  }

  record unit-icon-set-draw-query {
    unit-id: s32,
    draw-icon: bool,
  }

  record unit-icon-set-draw-result {
    success: bool,
  }

  /// read-only
  add-unit-icon: func(icon-name: string, tex-file: string, size: f32, distance: f32, radius-adjust: bool, u0: f32, v0: f32, u1: f32, v1: f32) -> result<bool, spring-error>;

  /// read-only
  free-unit-icon: func(icon-name: string) -> result<bool, spring-error>;

  /// read-only
  unit-icon-set-draw: func(unit-id: s32, draw-icon: bool) -> result<bool, spring-error>;

}
