package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface unsynced-ctrl {
  record spring-error { code: s32 }

  record assign-mouse-cursor-query {
    command-name: string,
    cursor-file-name: string,
    overwrite: bool,
    hot-spot-top-left: bool,
  }

  record assign-mouse-cursor-result {
    success: bool,
  }

  record atmosphere-params {
    fog-color: option<list<f32>>,
    sky-color: option<list<f32>>,
    sun-color: option<list<f32>>,
    cloud-color: option<list<f32>>,
    sky-axis-angle: option<list<f32>>,
    fog-start: option<f32>,
    fog-end: option<f32>,
  }

  record def-ref {
    name: string,
    id: s32,
  }

  record deselect-unit-map-query {
    unit-i-ds: list<s32>,
  }

  record deselect-unit-map-result {
    success: bool,
  }

  record draw-unit-commands-query {
    unit-i-ds: list<s32>,
    table-or-array: bool,
    queue-draw-depth: s32,
  }

  record draw-unit-commands-result {
    success: bool,
  }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record float4 {
    x: f32,
    y: f32,
    z: f32,
    w: f32,
  }

  record force-layout-update-query {
    unused: u8,
  }

  record force-layout-update-result {
    success: bool,
  }

  record force-tesselation-update-query {
    normal: bool,
    shadow: bool,
  }

  record force-tesselation-update-result {
    success: bool,
  }

  record get-water-texture-query {
    tex-type: string,
  }

  record get-water-texture-result {
    tex-name: string,
  }

  record load-cmd-colors-config-query {
    filename: string,
  }

  record load-cmd-colors-config-result {
    success: bool,
  }

  record load-ctrl-panel-config-query {
    filename: string,
  }

  record load-ctrl-panel-config-result {
    success: bool,
  }

  record load-model-textures-query {
    model-name: string,
  }

  record load-model-textures-result {
    success: bool,
  }

  record map-rendering-params {
    splat-tex-scales: option<list<f32>>,
    splat-tex-mults: option<list<f32>>,
    void-water: option<bool>,
    void-ground: option<bool>,
    splat-detail-normal-diffuse-alpha: option<bool>,
  }

  record pause-dolly-camera-query {
    percent: f32,
  }

  record pause-dolly-camera-result {
    success: bool,
  }

  record preload-feature-def-model-query {
    def-id: s32,
  }

  record preload-feature-def-model-result {
    success: bool,
  }

  record preload-unit-def-model-query {
    def-id: s32,
  }

  record preload-unit-def-model-result {
    success: bool,
  }

  record replace-mouse-cursor-query {
    old-cursor-file-name: string,
    new-cursor-file-name: string,
    hot-spot-top-left: bool,
  }

  record replace-mouse-cursor-result {
    success: bool,
  }

  record resume-dolly-camera-query {
    unused: u8,
  }

  record resume-dolly-camera-result {
    success: bool,
  }

  record rgb-color {
    r: f32,
    g: f32,
    b: f32,
  }

  record run-dolly-camera-query {
    runtime-ms: f32,
  }

  record run-dolly-camera-result {
    success: bool,
  }

  record sdl-set-text-input-rect-query {
    x: s32,
    y: s32,
    w: s32,
    h: s32,
  }

  record sdl-set-text-input-rect-result {
    success: bool,
  }

  record sdl-start-text-input-query {
    unused: u8,
  }

  record sdl-start-text-input-result {
    success: bool,
  }

  record sdl-stop-text-input-query {
    unused: u8,
  }

  record sdl-stop-text-input-result {
    success: bool,
  }

  record select-unit-map-query {
    unit-i-ds: list<s32>,
    append: bool,
  }

  record select-unit-map-result {
    success: bool,
  }

  record set-active-command-options {
    left-click: bool,
    right-click: bool,
    alt: bool,
    ctrl: bool,
    meta: bool,
    shift: bool,
  }

  record set-active-command-query {
    cmd-index: s32,
    button: s32,
    options: set-active-command-options,
  }

  record set-active-command-result {
    success: bool,
  }

  record set-atmosphere-query {
    params: atmosphere-params,
  }

  record set-atmosphere-result {
    success: bool,
  }

  record set-auto-show-metal-query {
    enable: bool,
  }

  record set-auto-show-metal-result {
    success: bool,
  }

  record set-box-selection-by-engine-query {
    state: bool,
  }

  record set-box-selection-by-engine-result {
    success: bool,
  }

  record set-build-facing-query {
    facing: s32,
  }

  record set-build-facing-result {
    success: bool,
  }

  record set-build-spacing-query {
    spacing: s32,
  }

  record set-build-spacing-result {
    success: bool,
  }

  record set-camera-offset-query {
    pos-offset: float3,
    tilt-offset: float3,
  }

  record set-camera-offset-result {
    success: bool,
  }

  record set-clipboard-query {
    text: string,
  }

  record set-clipboard-result {
    success: bool,
  }

  record set-custom-command-draw-data-query {
    cmd-id: s32,
    cmd-reference: def-ref,
    color: float4,
    show-area: bool,
  }

  record set-custom-command-draw-data-result {
    success: bool,
  }

  record set-custom-palette-color-query {
    index: s32,
    r: f32,
    g: f32,
    b: f32,
  }

  record set-custom-palette-color-result {
    success: bool,
  }

  record set-dolly-camera-curve-query {
    degree: s32,
    control-points: list<float4>,
    knots: list<f32>,
  }

  record set-dolly-camera-curve-result {
    success: bool,
  }

  record set-dolly-camera-look-curve-query {
    degree: s32,
    control-points: list<float4>,
    knots: list<f32>,
  }

  record set-dolly-camera-look-curve-result {
    success: bool,
  }

  record set-dolly-camera-look-position-query {
    position: float3,
  }

  record set-dolly-camera-look-position-result {
    success: bool,
  }

  record set-dolly-camera-look-unit-query {
    unit-id: s32,
  }

  record set-dolly-camera-look-unit-result {
    success: bool,
  }

  record set-dolly-camera-mode-query {
    mode: s32,
  }

  record set-dolly-camera-mode-result {
    success: bool,
  }

  record set-dolly-camera-position-query {
    position: float3,
  }

  record set-dolly-camera-position-result {
    success: bool,
  }

  record set-dolly-camera-relative-mode-query {
    mode: s32,
  }

  record set-dolly-camera-relative-mode-result {
    success: bool,
  }

  record set-draw-ground-deferred-query {
    draw-deferred: bool,
    draw-forward: bool,
  }

  record set-draw-ground-deferred-result {
    success: bool,
    deferred: bool,
    forward: bool,
  }

  record set-draw-ground-query {
    draw-ground: bool,
  }

  record set-draw-ground-result {
    success: bool,
  }

  record set-draw-models-deferred-query {
    draw-units-deferred: bool,
    draw-features-deferred: bool,
    draw-units-forward: bool,
    draw-features-forward: bool,
  }

  record set-draw-models-deferred-result {
    success: bool,
    units-deferred: bool,
    features-deferred: bool,
    units-forward: bool,
    features-forward: bool,
  }

  record set-draw-selection-info-query {
    draw: bool,
  }

  record set-draw-selection-info-result {
    success: bool,
  }

  record set-draw-sky-query {
    draw-sky: bool,
  }

  record set-draw-sky-result {
    success: bool,
  }

  record set-draw-water-query {
    draw-water: bool,
  }

  record set-draw-water-result {
    success: bool,
  }

  record set-engine-build-square-rendering-query {
    enabled: bool,
  }

  record set-engine-build-square-rendering-result {
    success: bool,
  }

  record set-feature-always-update-matrix-query {
    feature-id: s32,
    enable: bool,
  }

  record set-feature-always-update-matrix-result {
    success: bool,
  }

  record set-feature-engine-draw-mask-query {
    feature-id: s32,
    mask: u32,
  }

  record set-feature-engine-draw-mask-result {
    success: bool,
  }

  record set-feature-fade-query {
    feature-id: s32,
    allow: bool,
  }

  record set-feature-fade-result {
    success: bool,
  }

  record set-feature-no-draw-query {
    feature-id: s32,
    no-draw: bool,
  }

  record set-feature-no-draw-result {
    success: bool,
  }

  record set-feature-palette-index-query {
    feature-id: s32,
    custom-index: s32,
  }

  record set-feature-palette-index-result {
    success: bool,
  }

  record set-last-message-position-query {
    pos: float3,
  }

  record set-last-message-position-result {
    success: bool,
  }

  record set-los-view-colors-query {
    always: rgb-color,
    los: rgb-color,
    radar: rgb-color,
    jam: rgb-color,
    radar2: rgb-color,
  }

  record set-los-view-colors-result {
    success: bool,
  }

  record set-map-rendering-params-query {
    params: map-rendering-params,
  }

  record set-map-rendering-params-result {
    success: bool,
  }

  record set-map-shader-query {
    standard-shader-id: s32,
    deferred-shader-id: s32,
  }

  record set-map-shader-result {
    success: bool,
  }

  record set-map-shading-texture-query {
    tex-type: string,
    tex-name: string,
    num: s32,
  }

  record set-map-shading-texture-result {
    success: bool,
  }

  record set-mini-map-rotation-query {
    radians: f32,
  }

  record set-mini-map-rotation-result {
    success: bool,
    rotation: s32,
  }

  record set-mouse-cursor-query {
    cursor-name: string,
    scale: f32,
  }

  record set-mouse-cursor-result {
    success: bool,
  }

  record set-nano-projectile-params-query {
    r: f32,
    v: f32,
    a: f32,
    rand-r: f32,
    rand-v: f32,
    rand-a: f32,
  }

  record set-nano-projectile-params-result {
    success: bool,
  }

  record set-shock-front-factors-options {
    min-area: option<f32>,
    min-power: option<f32>,
    dist-adj: option<f32>,
  }

  record set-shock-front-factors-query {
    options: set-shock-front-factors-options,
  }

  record set-shock-front-factors-result {
    success: bool,
  }

  record set-sky-box-texture-query {
    tex-name: string,
  }

  record set-sky-box-texture-result {
    success: bool,
  }

  record set-sun-direction-query {
    dir: float3,
    intensity: f32,
  }

  record set-sun-direction-result {
    success: bool,
  }

  record set-sun-lighting-query {
    params: sun-lighting-params,
  }

  record set-sun-lighting-result {
    success: bool,
  }

  record set-unit-always-update-matrix-query {
    unit-id: s32,
    always-update-matrix: bool,
  }

  record set-unit-always-update-matrix-result {
    success: bool,
  }

  record set-unit-def-icon-query {
    unit-def-id: s32,
    icon-name: string,
  }

  record set-unit-def-icon-result {
    success: bool,
  }

  record set-unit-def-image-query {
    unit-def-id: s32,
    image: string,
  }

  record set-unit-def-image-result {
    success: bool,
  }

  record set-unit-engine-draw-mask-query {
    unit-id: s32,
    draw-mask: u32,
  }

  record set-unit-engine-draw-mask-result {
    success: bool,
  }

  record set-unit-icon-draw-query {
    unit-id: s32,
    draw-icon: bool,
  }

  record set-unit-icon-draw-result {
    success: bool,
  }

  record set-unit-icon-query {
    unit-id: s32,
    icon-name: string,
  }

  record set-unit-icon-result {
    success: bool,
  }

  record set-unit-leave-tracks-query {
    unit-id: s32,
    leave-tracks: bool,
  }

  record set-unit-leave-tracks-result {
    success: bool,
  }

  record set-unit-no-draw-query {
    unit-id: s32,
    no-draw: bool,
  }

  record set-unit-no-draw-result {
    success: bool,
  }

  record set-unit-no-group-query {
    unit-id: s32,
    no-group: bool,
  }

  record set-unit-no-group-result {
    success: bool,
  }

  record set-unit-no-minimap-query {
    unit-id: s32,
    no-minimap: bool,
  }

  record set-unit-no-minimap-result {
    success: bool,
  }

  record set-unit-no-select-query {
    unit-id: s32,
    no-select: bool,
  }

  record set-unit-no-select-result {
    success: bool,
  }

  record set-unit-palette-index-query {
    unit-id: s32,
    custom-index: s32,
  }

  record set-unit-palette-index-result {
    success: bool,
  }

  record set-video-capturing-mode-query {
    allow-capture-mode: bool,
  }

  record set-video-capturing-mode-result {
    success: bool,
  }

  record set-video-capturing-time-offset-query {
    time-offset: f32,
  }

  record set-video-capturing-time-offset-result {
    success: bool,
  }

  record set-wm-caption-query {
    title: string,
    title-short: string,
  }

  record set-wm-caption-result {
    success: bool,
  }

  record set-wm-icon-query {
    icon-file-name: string,
    force-resolution: bool,
  }

  record set-wm-icon-result {
    success: bool,
  }

  record set-water-params-query {
    params: water-params,
  }

  record set-water-params-result {
    success: bool,
  }

  record set-water-texture-query {
    tex-type: string,
    tex-name: string,
  }

  record set-water-texture-result {
    success: bool,
  }

  record set-window-geometry-options {
    full-screen: bool,
    borderless: bool,
  }

  record set-window-geometry-query {
    display-index: s32,
    window-pos-x: s32,
    window-pos-y: s32,
    window-size-x: s32,
    window-size-y: s32,
    options: set-window-geometry-options,
  }

  record set-window-geometry-result {
    success: bool,
  }

  record set-window-maximized-query {
    unused: u8,
  }

  record set-window-maximized-result {
    maximized: bool,
  }

  record set-window-minimized-query {
    unused: u8,
  }

  record set-window-minimized-result {
    minimized: bool,
  }

  record sun-lighting-params {
    ground-ambient-color: option<list<f32>>,
    ground-diffuse-color: option<list<f32>>,
    ground-specular-color: option<list<f32>>,
    model-ambient-color: option<list<f32>>,
    model-diffuse-color: option<list<f32>>,
    model-specular-color: option<list<f32>>,
    specular-exponent: option<f32>,
    ground-shadow-density: option<f32>,
    model-shadow-density: option<f32>,
  }

  record warp-mouse-query {
    x: s32,
    y: s32,
  }

  record warp-mouse-result {
    success: bool,
  }

  record water-params {
    absorb: option<list<f32>>,
    base-color: option<list<f32>>,
    min-color: option<list<f32>>,
    surface-color: option<list<f32>>,
    diffuse-color: option<list<f32>>,
    specular-color: option<list<f32>>,
    plane-color: option<list<f32>>,
    repeat-x: option<f32>,
    repeat-y: option<f32>,
    surface-alpha: option<f32>,
    ambient-factor: option<f32>,
    diffuse-factor: option<f32>,
    specular-factor: option<f32>,
    specular-power: option<f32>,
    fresnel-min: option<f32>,
    fresnel-max: option<f32>,
    fresnel-power: option<f32>,
    reflection-distortion: option<f32>,
    blur-base: option<f32>,
    blur-exponent: option<f32>,
    perlin-start-freq: option<f32>,
    perlin-lacunarity: option<f32>,
    perlin-amplitude: option<f32>,
    wind-speed: option<f32>,
    wave-offset-factor: option<f32>,
    wave-length: option<f32>,
    wave-foam-distortion: option<f32>,
    wave-foam-intensity: option<f32>,
    caustics-resolution: option<f32>,
    caustics-strength: option<f32>,
    num-tiles: option<f32>,
    shore-waves: option<bool>,
    force-rendering: option<bool>,
    has-water-plane: option<bool>,
  }

  /// read-only
  assign-mouse-cursor: func(command-name: string, cursor-file-name: string, overwrite: bool, hot-spot-top-left: bool) -> result<bool, spring-error>;

  /// read-only
  deselect-unit-map: func(unit-i-ds: list<s32>) -> result<bool, spring-error>;

  /// read-only
  draw-unit-commands: func(unit-i-ds: list<s32>, table-or-array: bool, queue-draw-depth: s32) -> result<bool, spring-error>;

  /// read-only
  force-layout-update: func(unused: u8) -> result<bool, spring-error>;

  /// read-only
  force-tesselation-update: func(normal: bool, shadow: bool) -> result<bool, spring-error>;

  /// read-only
  get-water-texture: func(tex-type: string) -> result<string, spring-error>;

  /// read-only
  load-cmd-colors-config: func(filename: string) -> result<bool, spring-error>;

  /// read-only
  load-ctrl-panel-config: func(filename: string) -> result<bool, spring-error>;

  /// read-only
  load-model-textures: func(model-name: string) -> result<bool, spring-error>;

  /// read-only
  pause-dolly-camera: func(percent: f32) -> result<bool, spring-error>;

  /// read-only
  preload-feature-def-model: func(def-id: s32) -> result<bool, spring-error>;

  /// read-only
  preload-unit-def-model: func(def-id: s32) -> result<bool, spring-error>;

  /// read-only
  replace-mouse-cursor: func(old-cursor-file-name: string, new-cursor-file-name: string, hot-spot-top-left: bool) -> result<bool, spring-error>;

  /// read-only
  resume-dolly-camera: func(unused: u8) -> result<bool, spring-error>;

  /// read-only
  run-dolly-camera: func(runtime-ms: f32) -> result<bool, spring-error>;

  /// read-only
  sdl-set-text-input-rect: func(x: s32, y: s32, w: s32, h: s32) -> result<bool, spring-error>;

  /// read-only
  sdl-start-text-input: func(unused: u8) -> result<bool, spring-error>;

  /// read-only
  sdl-stop-text-input: func(unused: u8) -> result<bool, spring-error>;

  /// read-only
  select-unit-map: func(unit-i-ds: list<s32>, append: bool) -> result<bool, spring-error>;

  /// read-only
  set-active-command: func(cmd-index: s32, button: s32, options: set-active-command-options) -> result<bool, spring-error>;

  /// read-only
  set-atmosphere: func(params: atmosphere-params) -> result<bool, spring-error>;

  /// read-only
  set-auto-show-metal: func(enable: bool) -> result<bool, spring-error>;

  /// read-only
  set-box-selection-by-engine: func(state: bool) -> result<bool, spring-error>;

  /// read-only
  set-build-facing: func(facing: s32) -> result<bool, spring-error>;

  /// read-only
  set-build-spacing: func(spacing: s32) -> result<bool, spring-error>;

  /// read-only
  set-camera-offset: func(pos-offset: float3, tilt-offset: float3) -> result<bool, spring-error>;

  /// read-only
  set-clipboard: func(text: string) -> result<bool, spring-error>;

  /// read-only
  set-custom-command-draw-data: func(cmd-id: s32, cmd-reference: def-ref, color: float4, show-area: bool) -> result<bool, spring-error>;

  /// read-only
  set-custom-palette-color: func(index: s32, r: f32, g: f32, b: f32) -> result<bool, spring-error>;

  /// read-only
  set-dolly-camera-curve: func(degree: s32, control-points: list<float4>, knots: list<f32>) -> result<bool, spring-error>;

  /// read-only
  set-dolly-camera-look-curve: func(degree: s32, control-points: list<float4>, knots: list<f32>) -> result<bool, spring-error>;

  /// read-only
  set-dolly-camera-look-position: func(position: float3) -> result<bool, spring-error>;

  /// read-only
  set-dolly-camera-look-unit: func(unit-id: s32) -> result<bool, spring-error>;

  /// read-only
  set-dolly-camera-mode: func(mode: s32) -> result<bool, spring-error>;

  /// read-only
  set-dolly-camera-position: func(position: float3) -> result<bool, spring-error>;

  /// read-only
  set-dolly-camera-relative-mode: func(mode: s32) -> result<bool, spring-error>;

  /// read-only
  set-draw-ground: func(draw-ground: bool) -> result<bool, spring-error>;

  record set-draw-ground-deferred-value {
    success: bool,
    deferred: bool,
    forward: bool,
  }

  /// read-only
  set-draw-ground-deferred: func(draw-deferred: bool, draw-forward: bool) -> result<set-draw-ground-deferred-value, spring-error>;

  record set-draw-models-deferred-value {
    success: bool,
    units-deferred: bool,
    features-deferred: bool,
    units-forward: bool,
    features-forward: bool,
  }

  /// read-only
  set-draw-models-deferred: func(draw-units-deferred: bool, draw-features-deferred: bool, draw-units-forward: bool, draw-features-forward: bool) -> result<set-draw-models-deferred-value, spring-error>;

  /// read-only
  set-draw-selection-info: func(draw: bool) -> result<bool, spring-error>;

  /// read-only
  set-draw-sky: func(draw-sky: bool) -> result<bool, spring-error>;

  /// read-only
  set-draw-water: func(draw-water: bool) -> result<bool, spring-error>;

  /// read-only
  set-engine-build-square-rendering: func(enabled: bool) -> result<bool, spring-error>;

  /// read-only
  set-feature-always-update-matrix: func(feature-id: s32, enable: bool) -> result<bool, spring-error>;

  /// read-only
  set-feature-engine-draw-mask: func(feature-id: s32, mask: u32) -> result<bool, spring-error>;

  /// read-only
  set-feature-fade: func(feature-id: s32, allow: bool) -> result<bool, spring-error>;

  /// read-only
  set-feature-no-draw: func(feature-id: s32, no-draw: bool) -> result<bool, spring-error>;

  /// read-only
  set-feature-palette-index: func(feature-id: s32, custom-index: s32) -> result<bool, spring-error>;

  /// read-only
  set-last-message-position: func(pos: float3) -> result<bool, spring-error>;

  /// read-only
  set-los-view-colors: func(always: rgb-color, los: rgb-color, radar: rgb-color, jam: rgb-color, radar2: rgb-color) -> result<bool, spring-error>;

  /// read-only
  set-map-rendering-params: func(params: map-rendering-params) -> result<bool, spring-error>;

  /// read-only
  set-map-shader: func(standard-shader-id: s32, deferred-shader-id: s32) -> result<bool, spring-error>;

  /// read-only
  set-map-shading-texture: func(tex-type: string, tex-name: string, num: s32) -> result<bool, spring-error>;

  record set-mini-map-rotation-value {
    success: bool,
    rotation: s32,
  }

  /// read-only
  set-mini-map-rotation: func(radians: f32) -> result<set-mini-map-rotation-value, spring-error>;

  /// read-only
  set-mouse-cursor: func(cursor-name: string, scale: f32) -> result<bool, spring-error>;

  /// read-only
  set-nano-projectile-params: func(r: f32, v: f32, a: f32, rand-r: f32, rand-v: f32, rand-a: f32) -> result<bool, spring-error>;

  /// read-only
  set-shock-front-factors: func(options: set-shock-front-factors-options) -> result<bool, spring-error>;

  /// read-only
  set-sky-box-texture: func(tex-name: string) -> result<bool, spring-error>;

  /// read-only
  set-sun-direction: func(dir: float3, intensity: f32) -> result<bool, spring-error>;

  /// read-only
  set-sun-lighting: func(params: sun-lighting-params) -> result<bool, spring-error>;

  /// read-only
  set-unit-always-update-matrix: func(unit-id: s32, always-update-matrix: bool) -> result<bool, spring-error>;

  /// read-only
  set-unit-def-icon: func(unit-def-id: s32, icon-name: string) -> result<bool, spring-error>;

  /// read-only
  set-unit-def-image: func(unit-def-id: s32, image: string) -> result<bool, spring-error>;

  /// read-only
  set-unit-engine-draw-mask: func(unit-id: s32, draw-mask: u32) -> result<bool, spring-error>;

  /// read-only
  set-unit-icon: func(unit-id: s32, icon-name: string) -> result<bool, spring-error>;

  /// read-only
  set-unit-icon-draw: func(unit-id: s32, draw-icon: bool) -> result<bool, spring-error>;

  /// read-only
  set-unit-leave-tracks: func(unit-id: s32, leave-tracks: bool) -> result<bool, spring-error>;

  /// read-only
  set-unit-no-draw: func(unit-id: s32, no-draw: bool) -> result<bool, spring-error>;

  /// read-only
  set-unit-no-group: func(unit-id: s32, no-group: bool) -> result<bool, spring-error>;

  /// read-only
  set-unit-no-minimap: func(unit-id: s32, no-minimap: bool) -> result<bool, spring-error>;

  /// read-only
  set-unit-no-select: func(unit-id: s32, no-select: bool) -> result<bool, spring-error>;

  /// read-only
  set-unit-palette-index: func(unit-id: s32, custom-index: s32) -> result<bool, spring-error>;

  /// read-only
  set-video-capturing-mode: func(allow-capture-mode: bool) -> result<bool, spring-error>;

  /// read-only
  set-video-capturing-time-offset: func(time-offset: f32) -> result<bool, spring-error>;

  /// read-only
  set-wm-caption: func(title: string, title-short: string) -> result<bool, spring-error>;

  /// read-only
  set-wm-icon: func(icon-file-name: string, force-resolution: bool) -> result<bool, spring-error>;

  /// read-only
  set-water-params: func(params: water-params) -> result<bool, spring-error>;

  /// read-only
  set-water-texture: func(tex-type: string, tex-name: string) -> result<bool, spring-error>;

  /// read-only
  set-window-geometry: func(display-index: s32, window-pos-x: s32, window-pos-y: s32, window-size-x: s32, window-size-y: s32, options: set-window-geometry-options) -> result<bool, spring-error>;

  /// read-only
  set-window-maximized: func(unused: u8) -> result<bool, spring-error>;

  /// read-only
  set-window-minimized: func(unused: u8) -> result<bool, spring-error>;

  /// read-only
  warp-mouse: func(x: s32, y: s32) -> result<bool, spring-error>;

}
