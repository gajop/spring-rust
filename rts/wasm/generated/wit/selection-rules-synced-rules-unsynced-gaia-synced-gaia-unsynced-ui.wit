package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface selection-rules-synced-rules-unsynced-gaia-synced-gaia-unsynced-ui {
  record spring-error { code: s32 }

  record deselect-unit-array-query {
    unit-i-ds: list<s32>,
  }

  record deselect-unit-array-result {
    success: bool,
  }

  record deselect-unit-query {
    unit-id: s32,
  }

  record deselect-unit-result {
    success: bool,
  }

  record select-unit-array-query {
    unit-i-ds: list<s32>,
    append: bool,
  }

  record select-unit-array-result {
    success: bool,
  }

  record select-unit-query {
    unit-id: s32,
    append: bool,
  }

  record select-unit-result {
    success: bool,
  }

  record set-unit-group-query {
    unit-id: s32,
    group-id: s32,
  }

  record set-unit-group-result {
    success: bool,
  }

  /// read-only
  deselect-unit: func(unit-id: s32) -> result<bool, spring-error>;

  /// read-only
  deselect-unit-array: func(unit-i-ds: list<s32>) -> result<bool, spring-error>;

  /// read-only
  select-unit: func(unit-id: s32, append: bool) -> result<bool, spring-error>;

  /// read-only
  select-unit-array: func(unit-i-ds: list<s32>, append: bool) -> result<bool, spring-error>;

  /// read-only
  set-unit-group: func(unit-id: s32, group-id: s32) -> result<bool, spring-error>;

}
