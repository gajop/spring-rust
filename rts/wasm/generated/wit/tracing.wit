package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface tracing {
  record spring-error { code: s32 }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record ray {
    origin: float3,
    direction: float3,
    length: f32,
    %flags: u32,
    ally-team-id: s32,
  }

  record trace-ray-between-positions-query {
    start: float3,
    end: float3,
    %type: string,
  }

  record trace-ray-between-positions-result {
    hits: list<trace-ray-hit>,
  }

  record trace-ray-features-query {
    ray: ray,
  }

  record trace-ray-features-result {
    hit: bool,
    hit-type: s32,
    hit-id: s32,
    hit-pos: float3,
    hit-normal: float3,
  }

  record trace-ray-ground-between-positions-options {
    test-water: option<bool>,
  }

  record trace-ray-ground-between-positions-query {
    start: float3,
    end: float3,
    options: trace-ray-ground-between-positions-options,
  }

  record trace-ray-ground-between-positions-result {
    hit: bool,
    hit-length: f32,
    hit-pos: float3,
    hit-normal: float3,
  }

  record trace-ray-ground-in-direction-options {
    length: option<f32>,
    test-water: option<bool>,
  }

  record trace-ray-ground-in-direction-query {
    start: float3,
    dir: float3,
    options: trace-ray-ground-in-direction-options,
  }

  record trace-ray-ground-in-direction-result {
    hit: bool,
    hit-length: f32,
    hit-pos: float3,
    hit-normal: float3,
  }

  record trace-ray-hit {
    hit-length: f32,
    object-id: s32,
    object-type: s32,
  }

  record trace-ray-in-direction-options {
    max-length: option<f32>,
  }

  record trace-ray-in-direction-query {
    pos: float3,
    dir: float3,
    options: trace-ray-in-direction-options,
    %type: string,
  }

  record trace-ray-in-direction-result {
    hits: list<trace-ray-hit>,
  }

  record trace-ray-query {
    ray: ray,
  }

  record trace-ray-result {
    hit: bool,
    hit-type: s32,
    hit-id: s32,
    hit-pos: float3,
    hit-normal: float3,
  }

  record trace-ray-units-query {
    ray: ray,
  }

  record trace-ray-units-result {
    hit: bool,
    hit-type: s32,
    hit-id: s32,
    hit-pos: float3,
    hit-normal: float3,
  }

  record trace-ray-value {
    hit: bool,
    hit-type: s32,
    hit-id: s32,
    hit-pos: float3,
    hit-normal: float3,
  }

  /// read-only
  trace-ray: func(ray: ray) -> result<trace-ray-value, spring-error>;

  /// read-only
  trace-ray-between-positions: func(start: float3, end: float3, %type: string) -> result<list<trace-ray-hit>, spring-error>;

  record trace-ray-features-value {
    hit: bool,
    hit-type: s32,
    hit-id: s32,
    hit-pos: float3,
    hit-normal: float3,
  }

  /// read-only
  trace-ray-features: func(ray: ray) -> result<trace-ray-features-value, spring-error>;

  record trace-ray-ground-between-positions-value {
    hit: bool,
    hit-length: f32,
    hit-pos: float3,
    hit-normal: float3,
  }

  /// read-only
  trace-ray-ground-between-positions: func(start: float3, end: float3, options: trace-ray-ground-between-positions-options) -> result<trace-ray-ground-between-positions-value, spring-error>;

  record trace-ray-ground-in-direction-value {
    hit: bool,
    hit-length: f32,
    hit-pos: float3,
    hit-normal: float3,
  }

  /// read-only
  trace-ray-ground-in-direction: func(start: float3, dir: float3, options: trace-ray-ground-in-direction-options) -> result<trace-ray-ground-in-direction-value, spring-error>;

  /// read-only
  trace-ray-in-direction: func(pos: float3, dir: float3, options: trace-ray-in-direction-options, %type: string) -> result<list<trace-ray-hit>, spring-error>;

  record trace-ray-units-value {
    hit: bool,
    hit-type: s32,
    hit-id: s32,
    hit-pos: float3,
    hit-normal: float3,
  }

  /// read-only
  trace-ray-units: func(ray: ray) -> result<trace-ray-units-value, spring-error>;

}
