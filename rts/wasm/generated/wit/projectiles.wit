package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface projectiles {
  record spring-error { code: s32 }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record get-all-projectiles-options {
    exclude-weapon-projectiles: bool,
    exclude-piece-projectiles: bool,
  }

  record get-all-projectiles-query {
    options: get-all-projectiles-options,
  }

  record get-all-projectiles-result {
    projectiles: list<s32>,
  }

  record get-piece-projectile-params-query {
    projectile-id: s32,
  }

  record get-piece-projectile-params-result {
    params: piece-projectile-params,
    is-piece-projectile: bool,
  }

  record get-projectile-ally-team-id-query {
    projectile-id: s32,
  }

  record get-projectile-ally-team-id-result {
    ally-team-id: s32,
  }

  record get-projectile-damages-query {
    projectile-id: s32,
    tag: string,
  }

  record get-projectile-damages-result {
    damages: projectile-damages,
  }

  record get-projectile-def-id-query {
    projectile-id: s32,
  }

  record get-projectile-def-id-result {
    def-id: s32,
  }

  record get-projectile-direction-query {
    projectile-id: s32,
  }

  record get-projectile-direction-result {
    direction: float3,
  }

  record get-projectile-gravity-query {
    projectile-id: s32,
  }

  record get-projectile-gravity-result {
    gravity: float3,
  }

  record get-projectile-is-intercepted-query {
    projectile-id: s32,
  }

  record get-projectile-is-intercepted-result {
    is-intercepted: bool,
  }

  record get-projectile-owner-id-query {
    projectile-id: s32,
  }

  record get-projectile-owner-id-result {
    owner-id: s32,
  }

  record get-projectile-position-query {
    projectile-id: s32,
  }

  record get-projectile-position-result {
    position: float3,
  }

  record get-projectile-target-query {
    projectile-id: s32,
  }

  record get-projectile-target-result {
    target: projectile-target,
  }

  record get-projectile-team-id-query {
    projectile-id: s32,
  }

  record get-projectile-team-id-result {
    team-id: s32,
  }

  record get-projectile-time-to-live-query {
    projectile-id: s32,
  }

  record get-projectile-time-to-live-result {
    ttl: f32,
  }

  record get-projectile-type-query {
    projectile-id: s32,
  }

  record get-projectile-type-result {
    weapon: bool,
    piece: bool,
  }

  record get-projectile-velocity-query {
    projectile-id: s32,
  }

  record get-projectile-velocity-result {
    velocity: float3,
  }

  record get-projectiles-in-rectangle-options {
    exclude-weapon-projectiles: bool,
    exclude-piece-projectiles: bool,
  }

  record get-projectiles-in-rectangle-query {
    min-x: f32,
    min-z: f32,
    max-x: f32,
    max-z: f32,
    options: get-projectiles-in-rectangle-options,
  }

  record get-projectiles-in-rectangle-result {
    projectiles: list<s32>,
  }

  record get-projectiles-in-sphere-options {
    exclude-weapon-projectiles: bool,
    exclude-piece-projectiles: bool,
  }

  record get-projectiles-in-sphere-query {
    center: float3,
    radius: f32,
    options: get-projectiles-in-sphere-options,
  }

  record get-projectiles-in-sphere-result {
    projectiles: list<s32>,
  }

  record piece-projectile-params {
    pos: float3,
    speed: float3,
    gravity: float3,
    spin-vec: float3,
    expl-flags: s32,
    spin-angle: f32,
    spin-speed: f32,
    model-piece-num: s32,
    model-object-type: s32,
    model-name: string,
    team: s32,
  }

  record projectile-damages {
    damages: list<f32>,
    paralyze-damage-time: f32,
    impulse-factor: f32,
    impulse-boost: f32,
    crater-mult: f32,
    crater-boost: f32,
    default-damage: f32,
    dyn-damage-exp: f32,
    dyn-damage-min: f32,
    dyn-damage-range: f32,
    dyn-damage-inverted: bool,
    crater-area-of-effect: f32,
    damage-area-of-effect: f32,
    edge-effectiveness: f32,
    explosion-speed: f32,
  }

  record projectile-target {
    target-type: s32,
    target-id: s32,
    target-pos: float3,
  }

  /// read-only
  get-all-projectiles: func(options: get-all-projectiles-options) -> result<list<s32>, spring-error>;

  record get-piece-projectile-params-value {
    params: piece-projectile-params,
    is-piece-projectile: bool,
  }

  /// read-only
  get-piece-projectile-params: func(projectile-id: s32) -> result<get-piece-projectile-params-value, spring-error>;

  /// read-only
  get-projectile-ally-team-id: func(projectile-id: s32) -> result<s32, spring-error>;

  /// read-only
  get-projectile-damages: func(projectile-id: s32, tag: string) -> result<projectile-damages, spring-error>;

  /// read-only
  get-projectile-def-id: func(projectile-id: s32) -> result<s32, spring-error>;

  /// read-only
  get-projectile-direction: func(projectile-id: s32) -> result<float3, spring-error>;

  /// read-only
  get-projectile-gravity: func(projectile-id: s32) -> result<float3, spring-error>;

  /// read-only
  get-projectile-is-intercepted: func(projectile-id: s32) -> result<bool, spring-error>;

  /// read-only
  get-projectile-owner-id: func(projectile-id: s32) -> result<s32, spring-error>;

  /// read-only
  get-projectile-position: func(projectile-id: s32) -> result<float3, spring-error>;

  /// read-only
  get-projectile-target: func(projectile-id: s32) -> result<projectile-target, spring-error>;

  /// read-only
  get-projectile-team-id: func(projectile-id: s32) -> result<s32, spring-error>;

  /// read-only
  get-projectile-time-to-live: func(projectile-id: s32) -> result<f32, spring-error>;

  record get-projectile-type-value {
    weapon: bool,
    piece: bool,
  }

  /// read-only
  get-projectile-type: func(projectile-id: s32) -> result<get-projectile-type-value, spring-error>;

  /// read-only
  get-projectile-velocity: func(projectile-id: s32) -> result<float3, spring-error>;

  /// read-only
  get-projectiles-in-rectangle: func(min-x: f32, min-z: f32, max-x: f32, max-z: f32, options: get-projectiles-in-rectangle-options) -> result<list<s32>, spring-error>;

  /// read-only
  get-projectiles-in-sphere: func(center: float3, radius: f32, options: get-projectiles-in-sphere-options) -> result<list<s32>, spring-error>;

}
