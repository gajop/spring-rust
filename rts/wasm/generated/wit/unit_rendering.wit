package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface unit-rendering {
  record spring-error { code: s32 }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record get-camera-rotation-query {
    unused: u8,
  }

  record get-camera-rotation-result {
    rot-x: f32,
    rot-y: f32,
    rot-z: f32,
  }

  record get-camera-vectors-query {
    unused: u8,
  }

  record get-camera-vectors-result {
    forward: float3,
    up: float3,
    right: float3,
  }

  record get-features-in-screen-rectangle-query {
    left: f32,
    top: f32,
    right: f32,
    bottom: f32,
  }

  record get-features-in-screen-rectangle-result {
    feature-i-ds: list<s32>,
  }

  record get-frustum-planes-query {
    unused: u8,
  }

  record get-frustum-planes-result {
    planes: list<f32>,
  }

  record get-unit-always-update-matrix-query {
    unit-id: s32,
  }

  record get-unit-always-update-matrix-result {
    always-update-matrix: bool,
  }

  record get-unit-draw-flag-query {
    unit-id: s32,
  }

  record get-unit-draw-flag-result {
    draw-flag: u8,
  }

  record get-unit-engine-draw-mask-query {
    unit-id: s32,
  }

  record get-unit-engine-draw-mask-result {
    engine-draw-mask: u32,
  }

  record get-unit-icon-data-query {
    unit-id: s32,
    full-data: bool,
  }

  record get-unit-icon-data-result {
    icon-name: string,
    atlas-tex-coords: list<f32>,
    size: f32,
    distance: f32,
    radius-adjust: bool,
  }

  record get-unit-icon-query {
    unit-id: s32,
  }

  record get-unit-icon-result {
    icon-name: string,
    atlas-tex-coords: list<f32>,
    size: f32,
    distance: f32,
    radius-adjust: bool,
  }

  record get-unit-lua-draw-query {
    unit-id: s32,
  }

  record get-unit-lua-draw-result {
    lua-draw: bool,
  }

  record get-unit-no-draw-query {
    unit-id: s32,
  }

  record get-unit-no-draw-result {
    no-draw: bool,
  }

  record get-unit-no-group-query {
    unit-id: s32,
  }

  record get-unit-no-group-result {
    no-group: bool,
  }

  record get-unit-no-minimap-query {
    unit-id: s32,
  }

  record get-unit-no-minimap-result {
    no-minimap: bool,
  }

  record get-unit-no-select-query {
    unit-id: s32,
  }

  record get-unit-no-select-result {
    no-select: bool,
  }

  record get-unit-selection-volume-data-query {
    unit-id: s32,
  }

  record get-unit-selection-volume-data-result {
    scales: float3,
    offsets: float3,
    volume-type: s32,
    use-cont-hit-test: bool,
    primary-axis: s32,
    ignore-hits: bool,
  }

  record get-unit-transform-matrix-query {
    unit-id: s32,
  }

  record get-unit-transform-matrix-result {
    matrix: list<f32>,
  }

  record get-unit-view-position-query {
    unit-id: s32,
    use-mid-pos: bool,
  }

  record get-unit-view-position-result {
    position: float3,
  }

  record get-units-in-screen-rectangle-query {
    left: f32,
    top: f32,
    right: f32,
    bottom: f32,
    allegiance: s32,
  }

  record get-units-in-screen-rectangle-result {
    unit-i-ds: list<s32>,
  }

  record get-visible-features-options {
    include-icons: bool,
    include-geos: bool,
  }

  record get-visible-features-query {
    ally-team-id: s32,
    radius: f32,
    options: get-visible-features-options,
  }

  record get-visible-features-result {
    feature-i-ds: list<s32>,
  }

  record get-visible-projectiles-options {
    include-synced-projectiles: bool,
    include-weapon-projectiles: bool,
    include-piece-projectiles: bool,
  }

  record get-visible-projectiles-query {
    ally-team-id: s32,
    options: get-visible-projectiles-options,
  }

  record get-visible-projectiles-result {
    projectile-i-ds: list<s32>,
  }

  record get-visible-units-query {
    team-id: s32,
    radius: f32,
    include-icons: bool,
  }

  record get-visible-units-result {
    unit-i-ds: list<s32>,
  }

  record is-unit-icon-query {
    unit-id: s32,
  }

  record is-unit-icon-result {
    is-icon: bool,
  }

  record is-unit-in-view-query {
    unit-id: s32,
  }

  record is-unit-in-view-result {
    in-view: bool,
  }

  record is-unit-visible-query {
    unit-id: s32,
    radius: f32,
    check-icon: bool,
  }

  record is-unit-visible-result {
    visible: bool,
  }

  record get-camera-rotation-value {
    rot-x: f32,
    rot-y: f32,
    rot-z: f32,
  }

  /// read-only
  get-camera-rotation: func(unused: u8) -> result<get-camera-rotation-value, spring-error>;

  record get-camera-vectors-value {
    forward: float3,
    up: float3,
    right: float3,
  }

  /// read-only
  get-camera-vectors: func(unused: u8) -> result<get-camera-vectors-value, spring-error>;

  /// read-only
  get-features-in-screen-rectangle: func(left: f32, top: f32, right: f32, bottom: f32) -> result<list<s32>, spring-error>;

  /// read-only
  get-frustum-planes: func(unused: u8) -> result<list<f32>, spring-error>;

  /// read-only
  get-unit-always-update-matrix: func(unit-id: s32) -> result<bool, spring-error>;

  /// read-only
  get-unit-draw-flag: func(unit-id: s32) -> result<u8, spring-error>;

  /// read-only
  get-unit-engine-draw-mask: func(unit-id: s32) -> result<u32, spring-error>;

  record get-unit-icon-value {
    icon-name: string,
    atlas-tex-coords: list<f32>,
    size: f32,
    distance: f32,
    radius-adjust: bool,
  }

  /// read-only
  get-unit-icon: func(unit-id: s32) -> result<get-unit-icon-value, spring-error>;

  record get-unit-icon-data-value {
    icon-name: string,
    atlas-tex-coords: list<f32>,
    size: f32,
    distance: f32,
    radius-adjust: bool,
  }

  /// read-only
  get-unit-icon-data: func(unit-id: s32, full-data: bool) -> result<get-unit-icon-data-value, spring-error>;

  /// read-only
  get-unit-lua-draw: func(unit-id: s32) -> result<bool, spring-error>;

  /// read-only
  get-unit-no-draw: func(unit-id: s32) -> result<bool, spring-error>;

  /// read-only
  get-unit-no-group: func(unit-id: s32) -> result<bool, spring-error>;

  /// read-only
  get-unit-no-minimap: func(unit-id: s32) -> result<bool, spring-error>;

  /// read-only
  get-unit-no-select: func(unit-id: s32) -> result<bool, spring-error>;

  record get-unit-selection-volume-data-value {
    scales: float3,
    offsets: float3,
    volume-type: s32,
    use-cont-hit-test: bool,
    primary-axis: s32,
    ignore-hits: bool,
  }

  /// read-only
  get-unit-selection-volume-data: func(unit-id: s32) -> result<get-unit-selection-volume-data-value, spring-error>;

  /// read-only
  get-unit-transform-matrix: func(unit-id: s32) -> result<list<f32>, spring-error>;

  /// read-only
  get-unit-view-position: func(unit-id: s32, use-mid-pos: bool) -> result<float3, spring-error>;

  /// read-only
  get-units-in-screen-rectangle: func(left: f32, top: f32, right: f32, bottom: f32, allegiance: s32) -> result<list<s32>, spring-error>;

  /// read-only
  get-visible-features: func(ally-team-id: s32, radius: f32, options: get-visible-features-options) -> result<list<s32>, spring-error>;

  /// read-only
  get-visible-projectiles: func(ally-team-id: s32, options: get-visible-projectiles-options) -> result<list<s32>, spring-error>;

  /// read-only
  get-visible-units: func(team-id: s32, radius: f32, include-icons: bool) -> result<list<s32>, spring-error>;

  /// read-only
  is-unit-icon: func(unit-id: s32) -> result<bool, spring-error>;

  /// read-only
  is-unit-in-view: func(unit-id: s32) -> result<bool, spring-error>;

  /// read-only
  is-unit-visible: func(unit-id: s32, radius: f32, check-icon: bool) -> result<bool, spring-error>;

}
