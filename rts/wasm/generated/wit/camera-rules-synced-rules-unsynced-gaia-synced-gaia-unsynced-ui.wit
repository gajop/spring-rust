package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface camera-rules-synced-rules-unsynced-gaia-synced-gaia-unsynced-ui {
  record spring-error { code: s32 }

  record camera-state {
    name: string,
    pos: float3,
    dir: float3,
    up: float3,
    right: float3,
    fov: f32,
    rx: f32,
    ry: f32,
    rz: f32,
    dist: f32,
    height: f32,
    angle: f32,
  }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record set-camera-state-query {
    state: camera-state,
    transition-time: f32,
    transition-time-factor: f32,
    transition-time-exponent: f32,
  }

  record set-camera-state-result {
    success: bool,
  }

  record set-camera-target-options {
    transition-time: option<f32>,
    dir-x: option<f32>,
    dir-y: option<f32>,
    dir-z: option<f32>,
  }

  record set-camera-target-query {
    target: float3,
    options: set-camera-target-options,
  }

  record set-camera-target-result {
    success: bool,
  }

  /// read-only
  set-camera-state: func(state: camera-state, transition-time: f32, transition-time-factor: f32, transition-time-exponent: f32) -> result<bool, spring-error>;

  /// read-only
  set-camera-target: func(target: float3, options: set-camera-target-options) -> result<bool, spring-error>;

}
