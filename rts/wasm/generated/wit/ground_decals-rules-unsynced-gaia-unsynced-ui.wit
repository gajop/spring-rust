package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface ground-decals-rules-unsynced-gaia-unsynced-ui {
  record spring-error { code: s32 }

  record get-all-ground-decals-query {
    unused: u8,
  }

  record get-all-ground-decals-result {
    decal-i-ds: list<u32>,
  }

  record get-ground-decal-alpha-query {
    decal-id: u32,
  }

  record get-ground-decal-alpha-result {
    alpha: f32,
    alpha-falloff: f32,
  }

  record get-ground-decal-creation-frame-query {
    decal-id: u32,
  }

  record get-ground-decal-creation-frame-result {
    creation-frame-min: f32,
    creation-frame-max: f32,
  }

  record get-ground-decal-glow-params-query {
    decal-id: u32,
  }

  record get-ground-decal-glow-params-result {
    glow: f32,
    glow-falloff: f32,
  }

  record get-ground-decal-middle-pos-query {
    decal-id: u32,
  }

  record get-ground-decal-middle-pos-result {
    mid-pos: list<f32>,
    success: bool,
  }

  record get-ground-decal-misc-query {
    decal-id: u32,
  }

  record get-ground-decal-misc-result {
    dot-elim-exp: f32,
    ref-height: f32,
    min-height: f32,
    max-height: f32,
    force-height-mode: f32,
  }

  record get-ground-decal-normal-query {
    decal-id: u32,
  }

  record get-ground-decal-normal-result {
    normal: list<f32>,
  }

  record get-ground-decal-owner-query {
    decal-id: u32,
  }

  record get-ground-decal-owner-result {
    has-owner: bool,
    owner-id: s32,
  }

  record get-ground-decal-quad-pos-query {
    decal-id: u32,
  }

  record get-ground-decal-quad-pos-result {
    positions: list<f32>,
    success: bool,
  }

  record get-ground-decal-rotation-query {
    decal-id: u32,
  }

  record get-ground-decal-rotation-result {
    rotation: f32,
    success: bool,
  }

  record get-ground-decal-size-and-height-query {
    decal-id: u32,
  }

  record get-ground-decal-size-and-height-result {
    size-x: f32,
    size-z: f32,
    height: f32,
    success: bool,
  }

  record get-ground-decal-texture-params-query {
    decal-id: u32,
  }

  record get-ground-decal-texture-params-result {
    tex-wrap-distance: f32,
    tex-traveled-distance: f32,
  }

  record get-ground-decal-texture-query {
    decal-id: u32,
    main-tex: bool,
  }

  record get-ground-decal-texture-result {
    texture: string,
  }

  record get-ground-decal-textures-options {
    main-tex: option<bool>,
    include-filenames: bool,
  }

  record get-ground-decal-textures-query {
    options: get-ground-decal-textures-options,
  }

  record get-ground-decal-textures-result {
    textures: list<string>,
    filenames: list<string>,
  }

  record get-ground-decal-tint-query {
    decal-id: u32,
  }

  record get-ground-decal-tint-result {
    tint: list<f32>,
  }

  record get-ground-decal-type-query {
    decal-id: u32,
  }

  record get-ground-decal-type-result {
    %type: string,
  }

  record get-ground-decal-user-data-query {
    decal-id: u32,
    quad-index: u32,
  }

  record get-ground-decal-user-data-result {
    values: list<f32>,
    success: bool,
  }

  /// read-only
  get-all-ground-decals: func(unused: u8) -> result<list<u32>, spring-error>;

  record get-ground-decal-alpha-value {
    alpha: f32,
    alpha-falloff: f32,
  }

  /// read-only
  get-ground-decal-alpha: func(decal-id: u32) -> result<get-ground-decal-alpha-value, spring-error>;

  record get-ground-decal-creation-frame-value {
    creation-frame-min: f32,
    creation-frame-max: f32,
  }

  /// read-only
  get-ground-decal-creation-frame: func(decal-id: u32) -> result<get-ground-decal-creation-frame-value, spring-error>;

  record get-ground-decal-glow-params-value {
    glow: f32,
    glow-falloff: f32,
  }

  /// read-only
  get-ground-decal-glow-params: func(decal-id: u32) -> result<get-ground-decal-glow-params-value, spring-error>;

  record get-ground-decal-middle-pos-value {
    mid-pos: list<f32>,
    success: bool,
  }

  /// read-only
  get-ground-decal-middle-pos: func(decal-id: u32) -> result<get-ground-decal-middle-pos-value, spring-error>;

  record get-ground-decal-misc-value {
    dot-elim-exp: f32,
    ref-height: f32,
    min-height: f32,
    max-height: f32,
    force-height-mode: f32,
  }

  /// read-only
  get-ground-decal-misc: func(decal-id: u32) -> result<get-ground-decal-misc-value, spring-error>;

  /// read-only
  get-ground-decal-normal: func(decal-id: u32) -> result<list<f32>, spring-error>;

  record get-ground-decal-owner-value {
    has-owner: bool,
    owner-id: s32,
  }

  /// read-only
  get-ground-decal-owner: func(decal-id: u32) -> result<get-ground-decal-owner-value, spring-error>;

  record get-ground-decal-quad-pos-value {
    positions: list<f32>,
    success: bool,
  }

  /// read-only
  get-ground-decal-quad-pos: func(decal-id: u32) -> result<get-ground-decal-quad-pos-value, spring-error>;

  record get-ground-decal-rotation-value {
    rotation: f32,
    success: bool,
  }

  /// read-only
  get-ground-decal-rotation: func(decal-id: u32) -> result<get-ground-decal-rotation-value, spring-error>;

  record get-ground-decal-size-and-height-value {
    size-x: f32,
    size-z: f32,
    height: f32,
    success: bool,
  }

  /// read-only
  get-ground-decal-size-and-height: func(decal-id: u32) -> result<get-ground-decal-size-and-height-value, spring-error>;

  /// read-only
  get-ground-decal-texture: func(decal-id: u32, main-tex: bool) -> result<string, spring-error>;

  record get-ground-decal-texture-params-value {
    tex-wrap-distance: f32,
    tex-traveled-distance: f32,
  }

  /// read-only
  get-ground-decal-texture-params: func(decal-id: u32) -> result<get-ground-decal-texture-params-value, spring-error>;

  record get-ground-decal-textures-value {
    textures: list<string>,
    filenames: list<string>,
  }

  /// read-only
  get-ground-decal-textures: func(options: get-ground-decal-textures-options) -> result<get-ground-decal-textures-value, spring-error>;

  /// read-only
  get-ground-decal-tint: func(decal-id: u32) -> result<list<f32>, spring-error>;

  /// read-only
  get-ground-decal-type: func(decal-id: u32) -> result<string, spring-error>;

  record get-ground-decal-user-data-value {
    values: list<f32>,
    success: bool,
  }

  /// read-only
  get-ground-decal-user-data: func(decal-id: u32, quad-index: u32) -> result<get-ground-decal-user-data-value, spring-error>;

}
