package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface move-ctrl-rules-synced-rules-unsynced-gaia-synced-gaia-unsynced-ui {
  record spring-error { code: s32 }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record get-unit-estimated-path-query {
    unit-id: s32,
  }

  record get-unit-estimated-path-result {
    waypoints: list<path-waypoint>,
    starts: list<s32>,
  }

  record get-unit-move-type-data-query {
    unit-id: s32,
  }

  record get-unit-move-type-data-result {
    data: move-type-data,
  }

  record move-type-data {
    name: string,
    max-speed: f32,
    max-wanted-speed: f32,
    goal-x: f32,
    goal-y: f32,
    goal-z: f32,
    turn-rate: f32,
    acc-rate: f32,
    dec-rate: f32,
    max-reverse-speed: f32,
    wanted-speed: f32,
    current-speed: f32,
    delta-speed: f32,
    max-bank: f32,
    max-pitch: f32,
    max-aileron: f32,
    max-elevator: f32,
    max-rudder: f32,
  }

  record path-waypoint {
    pos: float3,
    eta: f32,
  }

  record get-unit-estimated-path-value {
    waypoints: list<path-waypoint>,
    starts: list<s32>,
  }

  /// read-only
  get-unit-estimated-path: func(unit-id: s32) -> result<get-unit-estimated-path-value, spring-error>;

  /// read-only
  get-unit-move-type-data: func(unit-id: s32) -> result<move-type-data, spring-error>;

}
