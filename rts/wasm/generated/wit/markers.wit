package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface markers {
  record spring-error { code: s32 }

  record add-world-icon-query {
    cmd-id: s32,
    pos: float3,
  }

  record add-world-icon-result {
    success: bool,
  }

  record add-world-text-query {
    text: string,
    pos: float3,
  }

  record add-world-text-result {
    success: bool,
  }

  record add-world-unit-query {
    unit-def-id: s32,
    pos: float3,
    team-id: s32,
    facing: s32,
  }

  record add-world-unit-result {
    success: bool,
  }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record marker-add-line-query {
    %from: float3,
    to: float3,
    local-only: bool,
    player-id: s32,
  }

  record marker-add-line-result {
    success: bool,
  }

  record marker-add-point-query {
    pos: float3,
    text: string,
    local-only: bool,
    player-id: s32,
  }

  record marker-add-point-result {
    success: bool,
  }

  record marker-erase-position-options {
    local-only: bool,
    always-erase: bool,
  }

  record marker-erase-position-query {
    pos: float3,
    unused: f32,
    options: marker-erase-position-options,
    player-id: s32,
  }

  record marker-erase-position-result {
    success: bool,
  }

  /// read-only
  add-world-icon: func(cmd-id: s32, pos: float3) -> result<bool, spring-error>;

  /// read-only
  add-world-text: func(text: string, pos: float3) -> result<bool, spring-error>;

  /// read-only
  add-world-unit: func(unit-def-id: s32, pos: float3, team-id: s32, facing: s32) -> result<bool, spring-error>;

  /// read-only
  marker-add-line: func(%from: float3, to: float3, local-only: bool, player-id: s32) -> result<bool, spring-error>;

  /// read-only
  marker-add-point: func(pos: float3, text: string, local-only: bool, player-id: s32) -> result<bool, spring-error>;

  /// read-only
  marker-erase-position: func(pos: float3, unused: f32, options: marker-erase-position-options, player-id: s32) -> result<bool, spring-error>;

}
