package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface sound-rules-synced-rules-unsynced-gaia-synced-gaia-unsynced-ui {
  record spring-error { code: s32 }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record load-sound-def-query {
    sound-name: string,
  }

  record load-sound-def-result {
    success: bool,
  }

  record pause-sound-stream-query {
    unused: u8,
  }

  record pause-sound-stream-result {
    success: bool,
  }

  record play-sound-file-query {
    sound-file: string,
    volume: f32,
    pos: float3,
    velocity: float3,
    channel: s32,
  }

  record play-sound-file-result {
    success: bool,
  }

  record play-sound-stream-query {
    ogg-file: string,
    volume: f32,
    enqueue: bool,
  }

  record play-sound-stream-result {
    success: bool,
  }

  record preload-sound-item-query {
    sound-name: string,
  }

  record preload-sound-item-result {
    success: bool,
  }

  record set-sound-effect-params-query {
    params: sound-effect-params,
  }

  record set-sound-effect-params-result {
    success: bool,
  }

  record set-sound-stream-volume-query {
    volume: f32,
  }

  record set-sound-stream-volume-result {
    success: bool,
  }

  record sound-effect-params {
    preset: string,
  }

  record stop-sound-stream-query {
    unused: u8,
  }

  record stop-sound-stream-result {
    success: bool,
  }

  /// read-only
  load-sound-def: func(sound-name: string) -> result<bool, spring-error>;

  /// read-only
  pause-sound-stream: func(unused: u8) -> result<bool, spring-error>;

  /// read-only
  play-sound-file: func(sound-file: string, volume: f32, pos: float3, velocity: float3, channel: s32) -> result<bool, spring-error>;

  /// read-only
  play-sound-stream: func(ogg-file: string, volume: f32, enqueue: bool) -> result<bool, spring-error>;

  /// read-only
  preload-sound-item: func(sound-name: string) -> result<bool, spring-error>;

  /// read-only
  set-sound-effect-params: func(params: sound-effect-params) -> result<bool, spring-error>;

  /// read-only
  set-sound-stream-volume: func(volume: f32) -> result<bool, spring-error>;

  /// read-only
  stop-sound-stream: func(unused: u8) -> result<bool, spring-error>;

}
