package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface features-rules-synced-rules-unsynced-gaia-synced-gaia-unsynced-ui {
  record spring-error { code: s32 }

  record collision-volume-data {
    scale-x: f32,
    scale-y: f32,
    scale-z: f32,
    offset-x: f32,
    offset-y: f32,
    offset-z: f32,
    volume-type: s32,
    test-type: s32,
    primary-axis: s32,
    disabled: bool,
  }

  record feature-blocking-state {
    is-blocking: bool,
    is-solid-object-collidable: bool,
    is-projectile-collidable: bool,
    is-ray-segment-collidable: bool,
    crushable: bool,
    block-enemy-pushing: bool,
    block-height-changes: bool,
  }

  record feature-health {
    health: f32,
    max-health: f32,
    reclaim-left: f32,
    resurrect-progress: f32,
  }

  record feature-last-hit-piece {
    name: string,
    piece-num: s32,
    frame: s32,
    was-hit: bool,
  }

  record feature-position-ext {
    position: float3,
    mid-position: float3,
    aim-position: float3,
  }

  record feature-resources {
    metal: f32,
    def-metal: f32,
    energy: f32,
    def-energy: f32,
    reclaim-left: f32,
    reclaim-time: f32,
  }

  record feature-resurrect {
    resurrect-as: string,
    resurrect-def-id: s32,
    facing-dir: s32,
  }

  record feature-rotation {
    pitch: f32,
    yaw: f32,
    roll: f32,
  }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record get-all-features-query {
    unused: u8,
  }

  record get-all-features-result {
    features: list<s32>,
  }

  record get-feature-ally-team-query {
    feature-id: s32,
  }

  record get-feature-ally-team-result {
    ally-team-id: s32,
  }

  record get-feature-blocking-query {
    feature-id: s32,
  }

  record get-feature-blocking-result {
    blocking-state: feature-blocking-state,
  }

  record get-feature-collision-volume-data-query {
    feature-id: s32,
  }

  record get-feature-collision-volume-data-result {
    volume: collision-volume-data,
  }

  record get-feature-def-id-query {
    feature-id: s32,
  }

  record get-feature-def-id-result {
    def-id: s32,
  }

  record get-feature-direction-query {
    feature-id: s32,
  }

  record get-feature-direction-result {
    direction: float3,
  }

  record get-feature-fire-time-query {
    feature-id: s32,
  }

  record get-feature-fire-time-result {
    fire-time: f32,
  }

  record get-feature-heading-query {
    feature-id: s32,
  }

  record get-feature-heading-result {
    heading: s32,
  }

  record get-feature-health-query {
    feature-id: s32,
  }

  record get-feature-health-result {
    health: feature-health,
  }

  record get-feature-height-query {
    feature-id: s32,
  }

  record get-feature-height-result {
    height: f32,
  }

  record get-feature-last-attacked-piece-query {
    feature-id: s32,
  }

  record get-feature-last-attacked-piece-result {
    piece: feature-last-hit-piece,
  }

  record get-feature-mass-query {
    feature-id: s32,
  }

  record get-feature-mass-result {
    mass: f32,
  }

  record get-feature-no-select-query {
    feature-id: s32,
  }

  record get-feature-no-select-result {
    no-select: bool,
  }

  record get-feature-piece-collision-volume-data-query {
    feature-id: s32,
    piece-num: s32,
  }

  record get-feature-piece-collision-volume-data-result {
    volume: collision-volume-data,
  }

  record get-feature-position-ext-query {
    feature-id: s32,
  }

  record get-feature-position-ext-result {
    position: feature-position-ext,
  }

  record get-feature-position-query {
    feature-id: s32,
  }

  record get-feature-position-result {
    position: float3,
  }

  record get-feature-radius-query {
    feature-id: s32,
  }

  record get-feature-radius-result {
    radius: f32,
  }

  record get-feature-resources-query {
    feature-id: s32,
  }

  record get-feature-resources-result {
    resources: feature-resources,
  }

  record get-feature-resurrect-query {
    feature-id: s32,
  }

  record get-feature-resurrect-result {
    resurrect: feature-resurrect,
    can-resurrect: bool,
  }

  record get-feature-rotation-query {
    feature-id: s32,
  }

  record get-feature-rotation-result {
    rotation: feature-rotation,
  }

  record get-feature-separation-query {
    feature-id1: s32,
    feature-id2: s32,
    positional: bool,
  }

  record get-feature-separation-result {
    separation: f32,
  }

  record get-feature-smoke-time-query {
    feature-id: s32,
  }

  record get-feature-smoke-time-result {
    smoke-time: f32,
  }

  record get-feature-team-query {
    feature-id: s32,
  }

  record get-feature-team-result {
    team-id: s32,
  }

  record get-feature-velocity-query {
    feature-id: s32,
  }

  record get-feature-velocity-result {
    velocity: float3,
  }

  record get-features-in-cylinder-query {
    x: f32,
    z: f32,
    radius: f32,
    height: f32,
  }

  record get-features-in-cylinder-result {
    features: list<s32>,
  }

  record get-features-in-rectangle-query {
    min-x: f32,
    min-z: f32,
    max-x: f32,
    max-z: f32,
  }

  record get-features-in-rectangle-result {
    features: list<s32>,
  }

  record get-features-in-sphere-query {
    center: float3,
    radius: f32,
  }

  record get-features-in-sphere-result {
    features: list<s32>,
  }

  record valid-feature-id-query {
    feature-id: s32,
  }

  record valid-feature-id-result {
    valid: bool,
  }

  /// read-only
  get-all-features: func(unused: u8) -> result<list<s32>, spring-error>;

  /// read-only
  get-feature-ally-team: func(feature-id: s32) -> result<s32, spring-error>;

  /// read-only
  get-feature-blocking: func(feature-id: s32) -> result<feature-blocking-state, spring-error>;

  /// read-only
  get-feature-collision-volume-data: func(feature-id: s32) -> result<collision-volume-data, spring-error>;

  /// read-only
  get-feature-def-id: func(feature-id: s32) -> result<s32, spring-error>;

  /// read-only
  get-feature-direction: func(feature-id: s32) -> result<float3, spring-error>;

  /// read-only
  get-feature-fire-time: func(feature-id: s32) -> result<f32, spring-error>;

  /// read-only
  get-feature-heading: func(feature-id: s32) -> result<s32, spring-error>;

  /// read-only
  get-feature-health: func(feature-id: s32) -> result<feature-health, spring-error>;

  /// read-only
  get-feature-height: func(feature-id: s32) -> result<f32, spring-error>;

  /// read-only
  get-feature-last-attacked-piece: func(feature-id: s32) -> result<feature-last-hit-piece, spring-error>;

  /// read-only
  get-feature-mass: func(feature-id: s32) -> result<f32, spring-error>;

  /// read-only
  get-feature-no-select: func(feature-id: s32) -> result<bool, spring-error>;

  /// read-only
  get-feature-piece-collision-volume-data: func(feature-id: s32, piece-num: s32) -> result<collision-volume-data, spring-error>;

  /// read-only
  get-feature-position: func(feature-id: s32) -> result<float3, spring-error>;

  /// read-only
  get-feature-position-ext: func(feature-id: s32) -> result<feature-position-ext, spring-error>;

  /// read-only
  get-feature-radius: func(feature-id: s32) -> result<f32, spring-error>;

  /// read-only
  get-feature-resources: func(feature-id: s32) -> result<feature-resources, spring-error>;

  record get-feature-resurrect-value {
    resurrect: feature-resurrect,
    can-resurrect: bool,
  }

  /// read-only
  get-feature-resurrect: func(feature-id: s32) -> result<get-feature-resurrect-value, spring-error>;

  /// read-only
  get-feature-rotation: func(feature-id: s32) -> result<feature-rotation, spring-error>;

  /// read-only
  get-feature-separation: func(feature-id1: s32, feature-id2: s32, positional: bool) -> result<f32, spring-error>;

  /// read-only
  get-feature-smoke-time: func(feature-id: s32) -> result<f32, spring-error>;

  /// read-only
  get-feature-team: func(feature-id: s32) -> result<s32, spring-error>;

  /// read-only
  get-feature-velocity: func(feature-id: s32) -> result<float3, spring-error>;

  /// read-only
  get-features-in-cylinder: func(x: f32, z: f32, radius: f32, height: f32) -> result<list<s32>, spring-error>;

  /// read-only
  get-features-in-rectangle: func(min-x: f32, min-z: f32, max-x: f32, max-z: f32) -> result<list<s32>, spring-error>;

  /// read-only
  get-features-in-sphere: func(center: float3, radius: f32) -> result<list<s32>, spring-error>;

  /// read-only
  valid-feature-id: func(feature-id: s32) -> result<bool, spring-error>;

}
