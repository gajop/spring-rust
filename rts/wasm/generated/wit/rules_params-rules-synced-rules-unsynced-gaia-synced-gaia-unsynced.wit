package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface rules-params-rules-synced-rules-unsynced-gaia-synced-gaia-unsynced {
  record spring-error { code: s32 }

  record get-feature-rules-param-query {
    feature-id: s32,
    param-name: string,
  }

  record get-feature-rules-param-result {
    value: rules-param-value,
    los: s32,
    exists: bool,
  }

  record get-feature-rules-params-query {
    feature-id: s32,
  }

  record get-feature-rules-params-result {
    names: list<string>,
  }

  record get-game-rules-param-query {
    param-name: string,
  }

  record get-game-rules-param-result {
    value: rules-param-value,
    los: s32,
    exists: bool,
  }

  record get-game-rules-params-query {
    unused: u8,
  }

  record get-game-rules-params-result {
    names: list<string>,
  }

  record get-player-rules-param-query {
    player-id: s32,
    param-name: string,
  }

  record get-player-rules-param-result {
    value: rules-param-value,
    los: s32,
    exists: bool,
  }

  record get-player-rules-params-query {
    player-id: s32,
  }

  record get-player-rules-params-result {
    names: list<string>,
  }

  record get-team-rules-param-query {
    team-id: s32,
    param-name: string,
  }

  record get-team-rules-param-result {
    value: rules-param-value,
    los: s32,
    exists: bool,
  }

  record get-team-rules-params-query {
    team-id: s32,
  }

  record get-team-rules-params-result {
    names: list<string>,
  }

  record get-unit-rules-param-query {
    unit-id: s32,
    param-name: string,
  }

  record get-unit-rules-param-result {
    value: rules-param-value,
    los: s32,
    exists: bool,
  }

  record get-unit-rules-params-query {
    unit-id: s32,
  }

  record get-unit-rules-params-result {
    names: list<string>,
  }

  record rules-param-value {
    %type: rules-param-type,
  }

  enum rules-param-type {
    rulesparam-type-bool,
    rulesparam-type-float,
    rulesparam-type-string,
  }

  record get-feature-rules-param-value {
    value: rules-param-value,
    los: s32,
    exists: bool,
  }

  /// read-only
  get-feature-rules-param: func(feature-id: s32, param-name: string) -> result<get-feature-rules-param-value, spring-error>;

  /// read-only
  get-feature-rules-params: func(feature-id: s32) -> result<list<string>, spring-error>;

  record get-game-rules-param-value {
    value: rules-param-value,
    los: s32,
    exists: bool,
  }

  /// read-only
  get-game-rules-param: func(param-name: string) -> result<get-game-rules-param-value, spring-error>;

  /// read-only
  get-game-rules-params: func(unused: u8) -> result<list<string>, spring-error>;

  record get-player-rules-param-value {
    value: rules-param-value,
    los: s32,
    exists: bool,
  }

  /// read-only
  get-player-rules-param: func(player-id: s32, param-name: string) -> result<get-player-rules-param-value, spring-error>;

  /// read-only
  get-player-rules-params: func(player-id: s32) -> result<list<string>, spring-error>;

  record get-team-rules-param-value {
    value: rules-param-value,
    los: s32,
    exists: bool,
  }

  /// read-only
  get-team-rules-param: func(team-id: s32, param-name: string) -> result<get-team-rules-param-value, spring-error>;

  /// read-only
  get-team-rules-params: func(team-id: s32) -> result<list<string>, spring-error>;

  record get-unit-rules-param-value {
    value: rules-param-value,
    los: s32,
    exists: bool,
  }

  /// read-only
  get-unit-rules-param: func(unit-id: s32, param-name: string) -> result<get-unit-rules-param-value, spring-error>;

  /// read-only
  get-unit-rules-params: func(unit-id: s32) -> result<list<string>, spring-error>;

}
