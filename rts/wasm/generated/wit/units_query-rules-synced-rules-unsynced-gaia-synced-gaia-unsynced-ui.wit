package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface units-query-rules-synced-rules-unsynced-gaia-synced-gaia-unsynced-ui {
  record spring-error { code: s32 }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record float4 {
    x: f32,
    y: f32,
    z: f32,
    w: f32,
  }

  record get-all-units-query {
    unused: u8,
  }

  record get-all-units-result {
    units: list<s32>,
  }

  record get-closest-enemy-unit-options {
    use-los: bool,
    sphere-dist-test: bool,
    check-sight-dist: bool,
  }

  record get-closest-enemy-unit-query {
    pos: float3,
    range: f32,
    ally-team-id: s32,
    options: get-closest-enemy-unit-options,
  }

  record get-closest-enemy-unit-result {
    unit-id: s32,
  }

  record get-team-unit-count-query {
    team-id: s32,
  }

  record get-team-unit-count-result {
    count: u32,
  }

  record get-team-unit-def-count-query {
    team-id: s32,
    unit-def-id: s32,
  }

  record get-team-unit-def-count-result {
    count: u32,
  }

  record get-team-units-by-defs-query {
    team-id: s32,
    unit-def-i-ds: list<s32>,
  }

  record get-team-units-by-defs-result {
    units: list<s32>,
  }

  record get-team-units-counts-query {
    team-id: s32,
  }

  record get-team-units-counts-result {
    counts: list<unit-def-count>,
  }

  record get-team-units-query {
    team-id: s32,
  }

  record get-team-units-result {
    units: list<s32>,
  }

  record get-team-units-sorted-query {
    team-id: s32,
  }

  record get-team-units-sorted-result {
    groups: list<team-units-by-def>,
  }

  record get-unit-array-centroid-query {
    unit-i-ds: list<s32>,
  }

  record get-unit-array-centroid-result {
    centroid: float3,
  }

  record get-unit-map-centroid-query {
    unit-i-ds: list<s32>,
  }

  record get-unit-map-centroid-result {
    centroid: float3,
  }

  record get-unit-nearest-ally-query {
    unit-id: s32,
    range: f32,
  }

  record get-unit-nearest-ally-result {
    unit-id: s32,
  }

  record get-unit-nearest-enemy-options {
    use-los: bool,
    sphere-dist-test: bool,
    check-sight-dist: bool,
  }

  record get-unit-nearest-enemy-query {
    unit-id: s32,
    range: f32,
    options: get-unit-nearest-enemy-options,
  }

  record get-unit-nearest-enemy-result {
    unit-id: s32,
  }

  record get-unit-separation-options {
    positional: bool,
    check-map: bool,
  }

  record get-unit-separation-query {
    unit-id1: s32,
    unit-id2: s32,
    options: get-unit-separation-options,
  }

  record get-unit-separation-result {
    separation: f32,
  }

  record get-units-in-box-query {
    xmin: f32,
    ymin: f32,
    zmin: f32,
    xmax: f32,
    ymax: f32,
    zmax: f32,
    allegiance: s32,
  }

  record get-units-in-box-result {
    units: list<s32>,
  }

  record get-units-in-cylinder-query {
    x: f32,
    z: f32,
    radius: f32,
    allegiance: s32,
  }

  record get-units-in-cylinder-result {
    units: list<s32>,
  }

  record get-units-in-planes-query {
    planes: planes-query,
    allegiance: s32,
  }

  record get-units-in-planes-result {
    units: list<s32>,
  }

  record get-units-in-rectangle-query {
    xmin: f32,
    zmin: f32,
    xmax: f32,
    zmax: f32,
    allegiance: s32,
  }

  record get-units-in-rectangle-result {
    units: list<s32>,
  }

  record get-units-in-sphere-query {
    x: f32,
    y: f32,
    z: f32,
    radius: f32,
    allegiance: s32,
  }

  record get-units-in-sphere-result {
    units: list<s32>,
  }

  record planes-query {
    planes: list<float4>,
    plane-count: u32,
  }

  record team-units-by-def {
    unit-def-id: s32,
    units: list<s32>,
  }

  record unit-def-count {
    unit-def-id: s32,
    count: u32,
  }

  record valid-unit-id-query {
    unit-id: s32,
  }

  record valid-unit-id-result {
    valid: bool,
  }

  /// read-only
  get-all-units: func(unused: u8) -> result<list<s32>, spring-error>;

  /// read-only
  get-closest-enemy-unit: func(pos: float3, range: f32, ally-team-id: s32, options: get-closest-enemy-unit-options) -> result<s32, spring-error>;

  /// read-only
  get-team-unit-count: func(team-id: s32) -> result<u32, spring-error>;

  /// read-only
  get-team-unit-def-count: func(team-id: s32, unit-def-id: s32) -> result<u32, spring-error>;

  /// read-only
  get-team-units: func(team-id: s32) -> result<list<s32>, spring-error>;

  /// read-only
  get-team-units-by-defs: func(team-id: s32, unit-def-i-ds: list<s32>) -> result<list<s32>, spring-error>;

  /// read-only
  get-team-units-counts: func(team-id: s32) -> result<list<unit-def-count>, spring-error>;

  /// read-only
  get-team-units-sorted: func(team-id: s32) -> result<list<team-units-by-def>, spring-error>;

  /// read-only
  get-unit-array-centroid: func(unit-i-ds: list<s32>) -> result<float3, spring-error>;

  /// read-only
  get-unit-map-centroid: func(unit-i-ds: list<s32>) -> result<float3, spring-error>;

  /// read-only
  get-unit-nearest-ally: func(unit-id: s32, range: f32) -> result<s32, spring-error>;

  /// read-only
  get-unit-nearest-enemy: func(unit-id: s32, range: f32, options: get-unit-nearest-enemy-options) -> result<s32, spring-error>;

  /// read-only
  get-unit-separation: func(unit-id1: s32, unit-id2: s32, options: get-unit-separation-options) -> result<f32, spring-error>;

  /// read-only
  get-units-in-box: func(xmin: f32, ymin: f32, zmin: f32, xmax: f32, ymax: f32, zmax: f32, allegiance: s32) -> result<list<s32>, spring-error>;

  /// read-only
  get-units-in-cylinder: func(x: f32, z: f32, radius: f32, allegiance: s32) -> result<list<s32>, spring-error>;

  /// read-only
  get-units-in-planes: func(planes: planes-query, allegiance: s32) -> result<list<s32>, spring-error>;

  /// read-only
  get-units-in-rectangle: func(xmin: f32, zmin: f32, xmax: f32, zmax: f32, allegiance: s32) -> result<list<s32>, spring-error>;

  /// read-only
  get-units-in-sphere: func(x: f32, y: f32, z: f32, radius: f32, allegiance: s32) -> result<list<s32>, spring-error>;

  /// read-only
  valid-unit-id: func(unit-id: s32) -> result<bool, spring-error>;

}
