package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface config-rules-synced-rules-unsynced-gaia-synced-gaia-unsynced-ui {
  record spring-error { code: s32 }

  record set-config-float-query {
    key: string,
    value: f32,
    use-overlay: bool,
  }

  record set-config-float-result {
    success: bool,
  }

  record set-config-int-query {
    key: string,
    value: s32,
    use-overlay: bool,
  }

  record set-config-int-result {
    success: bool,
  }

  record set-config-string-query {
    key: string,
    value: string,
    use-overlay: bool,
  }

  record set-config-string-result {
    success: bool,
  }

  record set-log-section-filter-level-query {
    section: string,
    level: s32,
  }

  record set-log-section-filter-level-result {
    success: bool,
  }

  /// read-only
  set-config-float: func(key: string, value: f32, use-overlay: bool) -> result<bool, spring-error>;

  /// read-only
  set-config-int: func(key: string, value: s32, use-overlay: bool) -> result<bool, spring-error>;

  /// read-only
  set-config-string: func(key: string, value: string, use-overlay: bool) -> result<bool, spring-error>;

  /// read-only
  set-log-section-filter-level: func(section: string, level: s32) -> result<bool, spring-error>;

}
