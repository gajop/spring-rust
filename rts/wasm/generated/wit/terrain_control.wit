package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface terrain-control {
  record spring-error { code: s32 }

  record add-grass-query {
    x: f32,
    z: f32,
    grass-value: u8,
  }

  record add-grass-result {
    success: bool,
  }

  record add-height-map-query {
    x: f32,
    z: f32,
    height: f32,
  }

  record add-height-map-result {
    success: bool,
  }

  record add-original-height-map-query {
    x: f32,
    z: f32,
    height: f32,
  }

  record add-original-height-map-result {
    success: bool,
  }

  record add-smooth-mesh-query {
    x: f32,
    z: f32,
    height: f32,
  }

  record add-smooth-mesh-result {
    success: bool,
  }

  record adjust-height-map-query {
    x1: f32,
    z1: f32,
    x2: f32,
    z2: f32,
    height: f32,
  }

  record adjust-height-map-result {
    success: bool,
  }

  record adjust-original-height-map-query {
    x1: f32,
    z1: f32,
    x2: f32,
    z2: f32,
    height: f32,
  }

  record adjust-original-height-map-result {
    success: bool,
  }

  record adjust-smooth-mesh-query {
    x1: f32,
    z1: f32,
    x2: f32,
    z2: f32,
    height: f32,
  }

  record adjust-smooth-mesh-result {
    success: bool,
  }

  record level-height-map-query {
    x1: f32,
    z1: f32,
    x2: f32,
    z2: f32,
    height: f32,
  }

  record level-height-map-result {
    success: bool,
  }

  record level-original-height-map-query {
    x1: f32,
    z1: f32,
    x2: f32,
    z2: f32,
    height: f32,
  }

  record level-original-height-map-result {
    success: bool,
  }

  record level-smooth-mesh-query {
    x1: f32,
    z1: f32,
    x2: f32,
    z2: f32,
    height: f32,
  }

  record level-smooth-mesh-result {
    success: bool,
  }

  record rebuild-smooth-mesh-query {
    unused: u8,
  }

  record rebuild-smooth-mesh-result {
    success: bool,
  }

  record remove-grass-query {
    x: f32,
    z: f32,
  }

  record remove-grass-result {
    success: bool,
  }

  record revert-height-map-query {
    x1: f32,
    z1: f32,
    x2: f32,
    z2: f32,
    orig-factor: f32,
  }

  record revert-height-map-result {
    success: bool,
  }

  record revert-original-height-map-query {
    x1: f32,
    z1: f32,
    x2: f32,
    z2: f32,
    orig-factor: f32,
  }

  record revert-original-height-map-result {
    success: bool,
  }

  record revert-smooth-mesh-query {
    x1: f32,
    z1: f32,
    x2: f32,
    z2: f32,
    orig-factor: f32,
  }

  record revert-smooth-mesh-result {
    success: bool,
  }

  record set-height-map-func-query {
    callback: u32,
    user-data: u32,
  }

  record set-height-map-func-result {
    success: bool,
  }

  record set-height-map-query {
    x: f32,
    z: f32,
    height: f32,
    terraform: f32,
  }

  record set-height-map-result {
    success: bool,
  }

  record set-map-square-terrain-type-query {
    x: s32,
    z: s32,
    terrain-type: s32,
  }

  record set-map-square-terrain-type-result {
    success: bool,
  }

  record set-original-height-map-func-query {
    callback: u32,
    user-data: u32,
  }

  record set-original-height-map-func-result {
    success: bool,
  }

  record set-original-height-map-query {
    x: f32,
    z: f32,
    height: f32,
    factor: f32,
  }

  record set-original-height-map-result {
    success: bool,
  }

  record set-smooth-mesh-func-query {
    callback: u32,
    user-data: u32,
  }

  record set-smooth-mesh-func-result {
    success: bool,
  }

  record set-smooth-mesh-query {
    x: f32,
    z: f32,
    height: f32,
    terraform: f32,
  }

  record set-smooth-mesh-result {
    success: bool,
  }

  record set-terrain-type-data-query {
    type-index: s32,
    tank-speed: f32,
    kbot-speed: f32,
    hover-speed: f32,
    ship-speed: f32,
    hardness: f32,
    receive-tracks: bool,
    name: string,
  }

  record set-terrain-type-data-result {
    success: bool,
  }

  record set-tidal-query {
    tidal: f32,
  }

  record set-tidal-result {
    success: bool,
  }

  record set-wind-query {
    min-wind: f32,
    max-wind: f32,
  }

  record set-wind-result {
    success: bool,
  }

  /// mutating
  add-grass: func(x: f32, z: f32, grass-value: u8) -> result<bool, spring-error>;

  /// mutating
  add-height-map: func(x: f32, z: f32, height: f32) -> result<bool, spring-error>;

  /// mutating
  add-original-height-map: func(x: f32, z: f32, height: f32) -> result<bool, spring-error>;

  /// mutating
  add-smooth-mesh: func(x: f32, z: f32, height: f32) -> result<bool, spring-error>;

  /// mutating
  adjust-height-map: func(x1: f32, z1: f32, x2: f32, z2: f32, height: f32) -> result<bool, spring-error>;

  /// mutating
  adjust-original-height-map: func(x1: f32, z1: f32, x2: f32, z2: f32, height: f32) -> result<bool, spring-error>;

  /// mutating
  adjust-smooth-mesh: func(x1: f32, z1: f32, x2: f32, z2: f32, height: f32) -> result<bool, spring-error>;

  /// mutating
  level-height-map: func(x1: f32, z1: f32, x2: f32, z2: f32, height: f32) -> result<bool, spring-error>;

  /// mutating
  level-original-height-map: func(x1: f32, z1: f32, x2: f32, z2: f32, height: f32) -> result<bool, spring-error>;

  /// mutating
  level-smooth-mesh: func(x1: f32, z1: f32, x2: f32, z2: f32, height: f32) -> result<bool, spring-error>;

  /// mutating
  rebuild-smooth-mesh: func(unused: u8) -> result<bool, spring-error>;

  /// mutating
  remove-grass: func(x: f32, z: f32) -> result<bool, spring-error>;

  /// mutating
  revert-height-map: func(x1: f32, z1: f32, x2: f32, z2: f32, orig-factor: f32) -> result<bool, spring-error>;

  /// mutating
  revert-original-height-map: func(x1: f32, z1: f32, x2: f32, z2: f32, orig-factor: f32) -> result<bool, spring-error>;

  /// mutating
  revert-smooth-mesh: func(x1: f32, z1: f32, x2: f32, z2: f32, orig-factor: f32) -> result<bool, spring-error>;

  /// mutating
  set-height-map: func(x: f32, z: f32, height: f32, terraform: f32) -> result<bool, spring-error>;

  /// mutating
  set-height-map-func: func(callback: u32, user-data: u32) -> result<bool, spring-error>;

  /// mutating
  set-map-square-terrain-type: func(x: s32, z: s32, terrain-type: s32) -> result<bool, spring-error>;

  /// mutating
  set-original-height-map: func(x: f32, z: f32, height: f32, factor: f32) -> result<bool, spring-error>;

  /// mutating
  set-original-height-map-func: func(callback: u32, user-data: u32) -> result<bool, spring-error>;

  /// mutating
  set-smooth-mesh: func(x: f32, z: f32, height: f32, terraform: f32) -> result<bool, spring-error>;

  /// mutating
  set-smooth-mesh-func: func(callback: u32, user-data: u32) -> result<bool, spring-error>;

  /// mutating
  set-terrain-type-data: func(type-index: s32, tank-speed: f32, kbot-speed: f32, hover-speed: f32, ship-speed: f32, hardness: f32, receive-tracks: bool, name: string) -> result<bool, spring-error>;

  /// mutating
  set-tidal: func(tidal: f32) -> result<bool, spring-error>;

  /// mutating
  set-wind: func(min-wind: f32, max-wind: f32) -> result<bool, spring-error>;

}
