package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface lights {
  record spring-error { code: s32 }

  record add-light-tracking-target-query {
    light-handle: u32,
    object-id: s32,
    track-unit: bool,
    enable-tracking: bool,
  }

  record add-light-tracking-target-result {
    success: bool,
  }

  record add-map-light-query {
    params: light-params,
  }

  record add-map-light-result {
    light-handle: u32,
  }

  record add-model-light-query {
    params: light-params,
  }

  record add-model-light-result {
    light-handle: u32,
  }

  record light-params {
    position: list<f32>,
    direction: list<f32>,
    ambient-color: list<f32>,
    diffuse-color: list<f32>,
    specular-color: list<f32>,
    intensity-weight: list<f32>,
    attenuation: list<f32>,
    ambient-decay-rate: list<f32>,
    diffuse-decay-rate: list<f32>,
    specular-decay-rate: list<f32>,
    decay-function-type: list<f32>,
    radius: f32,
    fov: f32,
    ttl: u32,
    priority: u32,
    ignore-los: bool,
    local-space: bool,
  }

  record set-map-light-tracking-state-query {
    light-handle: u32,
    object-id: s32,
    enable-tracking: bool,
    track-unit: bool,
  }

  record set-map-light-tracking-state-result {
    success: bool,
  }

  record set-model-light-tracking-state-query {
    light-handle: u32,
    object-id: s32,
    enable-tracking: bool,
    track-unit: bool,
  }

  record set-model-light-tracking-state-result {
    success: bool,
  }

  record update-map-light-query {
    light-handle: u32,
    params: light-params,
  }

  record update-map-light-result {
    success: bool,
  }

  record update-model-light-query {
    light-handle: u32,
    params: light-params,
  }

  record update-model-light-result {
    success: bool,
  }

  /// read-only
  add-light-tracking-target: func(light-handle: u32, object-id: s32, track-unit: bool, enable-tracking: bool) -> result<bool, spring-error>;

  /// read-only
  add-map-light: func(params: light-params) -> result<u32, spring-error>;

  /// read-only
  add-model-light: func(params: light-params) -> result<u32, spring-error>;

  /// read-only
  set-map-light-tracking-state: func(light-handle: u32, object-id: s32, enable-tracking: bool, track-unit: bool) -> result<bool, spring-error>;

  /// read-only
  set-model-light-tracking-state: func(light-handle: u32, object-id: s32, enable-tracking: bool, track-unit: bool) -> result<bool, spring-error>;

  /// read-only
  update-map-light: func(light-handle: u32, params: light-params) -> result<bool, spring-error>;

  /// read-only
  update-model-light: func(light-handle: u32, params: light-params) -> result<bool, spring-error>;

}
