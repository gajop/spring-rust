package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface units-commands-rules-synced-gaia-synced {
  record spring-error { code: s32 }

  record command-ffi {
    cmd-id: s32,
    options: u8,
    tag: s32,
    ai-command-id: s32,
    time-out: f32,
    params: list<f32>,
  }

  record give-order-array-to-unit-map-query {
    unit-i-ds: list<s32>,
    commands: list<command-ffi>,
  }

  record give-order-array-to-unit-map-result {
    units-ordered: s32,
  }

  record give-order-query {
    cmd-id: s32,
    params: list<f32>,
    options: u32,
    timeout: s32,
  }

  record give-order-result {
    success: bool,
  }

  record give-order-to-unit-map-query {
    unit-i-ds: list<s32>,
    cmd-id: s32,
    params: list<f32>,
    options: u32,
    timeout: s32,
  }

  record give-order-to-unit-map-result {
    units-ordered: s32,
  }

  /// mutating
  give-order: func(cmd-id: s32, params: list<f32>, options: u32, timeout: s32) -> result<bool, spring-error>;

  /// mutating
  give-order-array-to-unit-map: func(unit-i-ds: list<s32>, commands: list<command-ffi>) -> result<s32, spring-error>;

  /// mutating
  give-order-to-unit-map: func(unit-i-ds: list<s32>, cmd-id: s32, params: list<f32>, options: u32, timeout: s32) -> result<s32, spring-error>;

}
