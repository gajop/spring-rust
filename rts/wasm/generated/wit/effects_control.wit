package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface effects-control {
  record spring-error { code: s32 }

  record def-ref {
    name: string,
    id: s32,
  }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record native-explosion-params {
    damages: f32,
    weapon-def-id: s32,
    owner-id: s32,
    hit-unit-id: s32,
    hit-feature-id: s32,
    crater-area-of-effect: f32,
    damage-area-of-effect: f32,
    edge-effectiveness: f32,
    explosion-speed: f32,
    gfx-mod: f32,
    impact-only: bool,
    ignore-owner: bool,
    damage-ground: bool,
    projectile-id: s32,
  }

  record spawn-ceg-query {
    ceg: def-ref,
    pos: float3,
    dir: float3,
    radius: f32,
    damage: f32,
    dmg-mod: f32,
  }

  record spawn-ceg-result {
    success: bool,
    ceg-id: s32,
  }

  record spawn-explosion-query {
    pos: float3,
    dir: float3,
    explosion-params: native-explosion-params,
  }

  record spawn-explosion-result {
    success: bool,
  }

  record spawn-sfx-query {
    unit-id: s32,
    sfx-id: s32,
    pos: float3,
    dir: float3,
    radius: f32,
    damage: f32,
    absolute: bool,
  }

  record spawn-sfx-result {
    success: bool,
  }

  record spawn-ceg-value {
    success: bool,
    ceg-id: s32,
  }

  /// mutating
  spawn-ceg: func(ceg: def-ref, pos: float3, dir: float3, radius: f32, damage: f32, dmg-mod: f32) -> result<spawn-ceg-value, spring-error>;

  /// mutating
  spawn-explosion: func(pos: float3, dir: float3, explosion-params: native-explosion-params) -> result<bool, spring-error>;

  /// mutating
  spawn-sfx: func(unit-id: s32, sfx-id: s32, pos: float3, dir: float3, radius: f32, damage: f32, absolute: bool) -> result<bool, spring-error>;

}
