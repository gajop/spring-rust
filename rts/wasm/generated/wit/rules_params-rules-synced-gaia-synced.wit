package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface rules-params-rules-synced-gaia-synced {
  record spring-error { code: s32 }

  record rules-param-value {
    %type: rules-param-type,
  }

  record set-feature-rules-param-query {
    feature-id: s32,
    param-name: string,
    value: rules-param-value,
    los: s32,
  }

  record set-feature-rules-param-result {
    success: bool,
  }

  record set-game-rules-param-query {
    param-name: string,
    value: rules-param-value,
    los: s32,
  }

  record set-game-rules-param-result {
    success: bool,
  }

  record set-player-rules-param-query {
    player-id: s32,
    param-name: string,
    value: rules-param-value,
    los: s32,
  }

  record set-player-rules-param-result {
    success: bool,
  }

  record set-team-rules-param-query {
    team-id: s32,
    param-name: string,
    value: rules-param-value,
    los: s32,
  }

  record set-team-rules-param-result {
    success: bool,
  }

  record set-unit-rules-param-query {
    unit-id: s32,
    param-name: string,
    value: rules-param-value,
    los: s32,
  }

  record set-unit-rules-param-result {
    success: bool,
  }

  enum rules-param-type {
    rulesparam-type-bool,
    rulesparam-type-float,
    rulesparam-type-string,
  }

  /// mutating
  set-feature-rules-param: func(feature-id: s32, param-name: string, value: rules-param-value, los: s32) -> result<bool, spring-error>;

  /// mutating
  set-game-rules-param: func(param-name: string, value: rules-param-value, los: s32) -> result<bool, spring-error>;

  /// mutating
  set-player-rules-param: func(player-id: s32, param-name: string, value: rules-param-value, los: s32) -> result<bool, spring-error>;

  /// mutating
  set-team-rules-param: func(team-id: s32, param-name: string, value: rules-param-value, los: s32) -> result<bool, spring-error>;

  /// mutating
  set-unit-rules-param: func(unit-id: s32, param-name: string, value: rules-param-value, los: s32) -> result<bool, spring-error>;

}
