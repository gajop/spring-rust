package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface game-config {
  record spring-error { code: s32 }

  record set-cheating-enabled-query {
    enabled: bool,
  }

  record set-cheating-enabled-result {
    success: bool,
  }

  record set-experience-grade-query {
    exp-grade: f32,
    exp-power-scale: f32,
    exp-health-scale: f32,
    exp-reload-scale: f32,
  }

  record set-experience-grade-result {
    success: bool,
  }

  record set-god-mode-options {
    control-allies: bool,
    control-enemies: bool,
  }

  record set-god-mode-query {
    options: set-god-mode-options,
  }

  record set-god-mode-result {
    success: bool,
  }

  record set-no-pause-query {
    no-pause: bool,
  }

  record set-no-pause-result {
    success: bool,
  }

  record set-radar-error-params-query {
    ally-team-id: s32,
    ally-team-error-size: f32,
    base-error-size: f32,
    base-error-mult: f32,
  }

  record set-radar-error-params-result {
    success: bool,
  }

  record set-square-building-mask-query {
    x: s32,
    z: s32,
    mask: u16,
  }

  record set-square-building-mask-result {
    success: bool,
  }

  /// mutating
  set-cheating-enabled: func(enabled: bool) -> result<bool, spring-error>;

  /// mutating
  set-experience-grade: func(exp-grade: f32, exp-power-scale: f32, exp-health-scale: f32, exp-reload-scale: f32) -> result<bool, spring-error>;

  /// mutating
  set-god-mode: func(options: set-god-mode-options) -> result<bool, spring-error>;

  /// mutating
  set-no-pause: func(no-pause: bool) -> result<bool, spring-error>;

  /// mutating
  set-radar-error-params: func(ally-team-id: s32, ally-team-error-size: f32, base-error-size: f32, base-error-mult: f32) -> result<bool, spring-error>;

  /// mutating
  set-square-building-mask: func(x: s32, z: s32, mask: u16) -> result<bool, spring-error>;

}
