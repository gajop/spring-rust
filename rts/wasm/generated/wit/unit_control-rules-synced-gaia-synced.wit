package recoil:spring-api@1.0.0;

/* @generated by spring-api-codegen; do not edit. */

interface unit-control-rules-synced-gaia-synced {
  record spring-error { code: s32 }

  record add-object-decal-query {
    unit-id: s32,
  }

  record add-object-decal-result {
    success: bool,
  }

  record add-unit-damage-query {
    unit-id: s32,
    damage: f32,
    paralyze-time: f32,
    weapon-def-id: s32,
    attacker-id: s32,
    impulse: float3,
  }

  record add-unit-damage-result {
    success: bool,
  }

  record add-unit-experience-query {
    unit-id: s32,
    experience: f32,
  }

  record add-unit-experience-result {
    success: bool,
  }

  record add-unit-impulse-query {
    unit-id: s32,
    impulse: float3,
    decay-rate: f32,
  }

  record add-unit-impulse-result {
    success: bool,
  }

  record add-unit-resource-query {
    unit-id: s32,
    resource-type: string,
    amount: f32,
  }

  record add-unit-resource-result {
    success: bool,
  }

  record add-unit-seismic-ping-query {
    unit-id: s32,
    ping-size: f32,
  }

  record add-unit-seismic-ping-result {
    success: bool,
  }

  record bugger-off-options {
    spherical: bool,
    forced: bool,
    exclude-unit-id: s32,
  }

  record bugger-off-query {
    pos: float3,
    radius: f32,
    team-id: s32,
    options: bugger-off-options,
    exclude-unit-def-i-ds: list<s32>,
  }

  record bugger-off-result {
    success: bool,
  }

  record clear-unit-goal-query {
    unit-id: s32,
    cancel-raw: bool,
  }

  record clear-unit-goal-result {
    success: bool,
  }

  record create-unit-options {
    build: bool,
    flatten-ground: bool,
    unit-id: s32,
    builder-id: s32,
  }

  record create-unit-query {
    unit-def: def-ref,
    pos: float3,
    facing: s32,
    team-id: s32,
    options: create-unit-options,
  }

  record create-unit-result {
    unit-id: s32,
  }

  record def-ref {
    name: string,
    id: s32,
  }

  record destroy-unit-options {
    selfd: bool,
    reclaimed: bool,
    attacker-id: s32,
    recycle-id: bool,
  }

  record destroy-unit-query {
    unit-id: s32,
    options: destroy-unit-options,
  }

  record destroy-unit-result {
    success: bool,
  }

  record edit-unit-cmd-desc-query {
    unit-id: s32,
    cmd-desc-index: u32,
    cmd-desc: native-command-description,
  }

  record edit-unit-cmd-desc-result {
    success: bool,
  }

  record float3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record force-unit-collision-update-query {
    unit-id: s32,
  }

  record force-unit-collision-update-result {
    success: bool,
  }

  record get-unit-physical-state-query {
    unit-id: s32,
  }

  record get-unit-physical-state-result {
    physical-state: u32,
  }

  record give-order-array-to-unit-array-query {
    unit-i-ds: list<s32>,
    commands: list<native-command>,
    pairwise: bool,
  }

  record give-order-array-to-unit-array-result {
    units-ordered: s32,
  }

  record give-order-array-to-unit-query {
    unit-id: s32,
    commands: list<native-command>,
  }

  record give-order-array-to-unit-result {
    success: bool,
  }

  record give-order-to-unit-array-query {
    unit-i-ds: list<s32>,
    cmd-id: s32,
    params: list<f32>,
    options: u32,
    timeout: s32,
  }

  record give-order-to-unit-array-result {
    success: bool,
  }

  record give-order-to-unit-query {
    unit-id: s32,
    cmd-id: s32,
    params: list<f32>,
    options: u32,
    timeout: s32,
  }

  record give-order-to-unit-result {
    success: bool,
  }

  record insert-unit-cmd-desc-query {
    unit-id: s32,
    cmd-desc-index: s32,
    cmd-desc: native-command-description,
  }

  record insert-unit-cmd-desc-result {
    success: bool,
  }

  record native-command {
    cmd-id: s32,
    params: list<f32>,
    options: u32,
    timeout: s32,
  }

  record native-command-description {
    id: s32,
    %type: s32,
    queueing: bool,
    hidden: bool,
    disabled: bool,
    show-unique: bool,
    only-texture: bool,
    name: string,
    action: string,
    iconname: string,
    mouseicon: string,
    tooltip: string,
    params: list<string>,
  }

  record number-or-bool {
    number: f32,
    boolean: bool,
    use-boolean: bool,
  }

  record remove-object-decal-query {
    unit-id: s32,
  }

  record remove-object-decal-result {
    success: bool,
  }

  record remove-unit-cmd-desc-query {
    unit-id: s32,
    cmd-desc-index: s32,
  }

  record remove-unit-cmd-desc-result {
    success: bool,
  }

  record set-factory-bugger-off-options {
    perform: bool,
    offset: f32,
    radius: f32,
    rel-heading: s32,
    spherical: bool,
    forced: bool,
  }

  record set-factory-bugger-off-query {
    unit-id: s32,
    options: set-factory-bugger-off-options,
  }

  record set-factory-bugger-off-result {
    perform: bool,
  }

  record set-unit-always-visible-query {
    unit-id: s32,
    always-visible: bool,
  }

  record set-unit-always-visible-result {
    success: bool,
  }

  record set-unit-armored-query {
    unit-id: s32,
    armored-state: bool,
    armored-multiple: f32,
  }

  record set-unit-armored-result {
    success: bool,
  }

  record set-unit-blocking-options {
    blocking: bool,
    solid-objects: bool,
    projectiles: bool,
    quad-map-rays: bool,
    crushable: bool,
    block-enemy-pushing: bool,
    block-height-changes: bool,
  }

  record set-unit-blocking-query {
    unit-id: s32,
    options: set-unit-blocking-options,
  }

  record set-unit-blocking-result {
    success: bool,
  }

  record set-unit-build-params-query {
    unit-id: s32,
    param-name: string,
    value: number-or-bool,
  }

  record set-unit-build-params-result {
    success: bool,
  }

  record set-unit-build-speed-query {
    unit-id: s32,
    build-speed: f32,
    repair-speed: f32,
    reclaim-speed: f32,
    resurrect-speed: f32,
    capture-speed: f32,
    terraform-speed: f32,
  }

  record set-unit-build-speed-result {
    success: bool,
  }

  record set-unit-buildee-radius-query {
    unit-id: s32,
    radius: f32,
  }

  record set-unit-buildee-radius-result {
    success: bool,
  }

  record set-unit-cloak-query {
    unit-id: s32,
    cloak: number-or-bool,
    cloak-arg: number-or-bool,
  }

  record set-unit-cloak-result {
    success: bool,
  }

  record set-unit-collision-volume-data-query {
    unit-id: s32,
    scales: float3,
    offsets: float3,
    volume-type: s32,
    test-type: s32,
    primary-axis: s32,
  }

  record set-unit-collision-volume-data-result {
    success: bool,
  }

  record set-unit-costs-query {
    unit-id: s32,
    costs: unit-cost-overrides,
  }

  record set-unit-costs-result {
    success: bool,
  }

  record set-unit-crashing-query {
    unit-id: s32,
    want-crash: bool,
  }

  record set-unit-crashing-result {
    state-changed: bool,
  }

  record set-unit-direction-query {
    unit-id: s32,
    front-dir: float3,
    right-dir: float3,
  }

  record set-unit-direction-result {
    success: bool,
  }

  record set-unit-experience-query {
    unit-id: s32,
    experience: f32,
  }

  record set-unit-experience-result {
    success: bool,
  }

  record set-unit-flanking-query {
    unit-id: s32,
    %type: string,
    args: float3,
  }

  record set-unit-flanking-result {
    success: bool,
  }

  record set-unit-harvest-storage-query {
    unit-id: s32,
    stored-metal: f32,
    max-stored-metal: f32,
    stored-energy: f32,
    max-stored-energy: f32,
  }

  record set-unit-harvest-storage-result {
    success: bool,
  }

  record set-unit-heading-and-up-dir-query {
    unit-id: s32,
    heading: s32,
    up-dir: float3,
  }

  record set-unit-heading-and-up-dir-result {
    success: bool,
  }

  record set-unit-heading-query {
    unit-id: s32,
    heading: s32,
    use-smoothing: bool,
  }

  record set-unit-heading-result {
    success: bool,
  }

  record set-unit-health-query {
    unit-id: s32,
    value: unit-health-value,
  }

  record set-unit-health-result {
    success: bool,
  }

  record set-unit-land-goal-query {
    unit-id: s32,
    pos: float3,
    radius-sq: f32,
  }

  record set-unit-land-goal-result {
    success: bool,
  }

  record set-unit-leaves-ghost-options {
    leaves-ghost: bool,
    leave-dead-ghost: bool,
  }

  record set-unit-leaves-ghost-query {
    unit-id: s32,
    options: set-unit-leaves-ghost-options,
  }

  record set-unit-leaves-ghost-result {
    success: bool,
  }

  record set-unit-loading-transport-query {
    unit-id: s32,
    transport-id: s32,
  }

  record set-unit-loading-transport-result {
    success: bool,
  }

  record set-unit-los-mask-query {
    unit-id: s32,
    ally-team-id: s32,
    los-mask: u8,
  }

  record set-unit-los-mask-result {
    success: bool,
  }

  record set-unit-los-state-query {
    unit-id: s32,
    ally-team-id: s32,
    los-state: u8,
  }

  record set-unit-los-state-result {
    success: bool,
  }

  record set-unit-mass-query {
    unit-id: s32,
    mass: f32,
  }

  record set-unit-mass-result {
    success: bool,
  }

  record set-unit-max-health-query {
    unit-id: s32,
    max-health: f32,
  }

  record set-unit-max-health-result {
    success: bool,
  }

  record set-unit-max-range-query {
    unit-id: s32,
    max-range: f32,
  }

  record set-unit-max-range-result {
    success: bool,
  }

  record set-unit-metal-extraction-query {
    unit-id: s32,
    depth: f32,
    range: f32,
  }

  record set-unit-metal-extraction-result {
    success: bool,
  }

  record set-unit-mid-and-aim-pos-query {
    unit-id: s32,
    mid-pos: float3,
    aim-pos: float3,
    set-relative: bool,
  }

  record set-unit-mid-and-aim-pos-result {
    success: bool,
  }

  record set-unit-move-goal-query {
    unit-id: s32,
    pos: float3,
    radius: f32,
    speed: f32,
    raw: bool,
  }

  record set-unit-move-goal-result {
    success: bool,
  }

  record set-unit-nano-pieces-query {
    unit-id: s32,
    piece-indices: list<s32>,
  }

  record set-unit-nano-pieces-result {
    success: bool,
  }

  record set-unit-neutral-query {
    unit-id: s32,
    neutral: bool,
  }

  record set-unit-neutral-result {
    success: bool,
  }

  record set-unit-physical-state-bit-query {
    unit-id: s32,
    state-bit: s32,
  }

  record set-unit-physical-state-bit-result {
    success: bool,
  }

  record set-unit-physics-query {
    unit-id: s32,
    pos: float3,
    velocity: float3,
    rotation: float3,
    drag: float3,
  }

  record set-unit-physics-result {
    success: bool,
  }

  record set-unit-piece-collision-volume-data-query {
    unit-id: s32,
    piece-index: s32,
    enable: bool,
    scales: float3,
    offsets: float3,
    volume-type: s32,
    primary-axis: s32,
  }

  record set-unit-piece-collision-volume-data-result {
    success: bool,
  }

  record set-unit-piece-matrix-query {
    unit-id: s32,
    piece-index: s32,
    matrix: list<f32>,
  }

  record set-unit-piece-matrix-result {
    block-script-anims: bool,
  }

  record set-unit-piece-parent-query {
    unit-id: s32,
    child-piece-index: s32,
    parent-piece-index: s32,
  }

  record set-unit-piece-parent-result {
    success: bool,
  }

  record set-unit-piece-visible-query {
    unit-id: s32,
    piece-index: s32,
    visible: bool,
  }

  record set-unit-piece-visible-result {
    success: bool,
  }

  record set-unit-pos-error-params-query {
    unit-id: s32,
    pos-error-vector: float3,
    pos-error-delta: float3,
    next-pos-error-update: s32,
    ally-team-id: s32,
    set-pos-error-bit: bool,
  }

  record set-unit-pos-error-params-result {
    success: bool,
  }

  record set-unit-position-query {
    unit-id: s32,
    pos: float3,
  }

  record set-unit-position-result {
    success: bool,
  }

  record set-unit-radius-and-height-query {
    unit-id: s32,
    radius: f32,
    height: f32,
  }

  record set-unit-radius-and-height-result {
    success: bool,
  }

  record set-unit-resourcing-query {
    unit-id: s32,
    %type: string,
    amount: f32,
  }

  record set-unit-resourcing-result {
    success: bool,
  }

  record set-unit-rotation-query {
    unit-id: s32,
    rotation: float3,
  }

  record set-unit-rotation-result {
    success: bool,
  }

  record set-unit-seismic-signature-query {
    unit-id: s32,
    seismic-signature: f32,
  }

  record set-unit-seismic-signature-result {
    success: bool,
  }

  record set-unit-selection-volume-data-query {
    unit-id: s32,
    scales: float3,
    offsets: float3,
    volume-type: s32,
    test-type: s32,
    primary-axis: s32,
  }

  record set-unit-selection-volume-data-result {
    success: bool,
  }

  record set-unit-sensor-radius-query {
    unit-id: s32,
    sensor-type: string,
    radius: s32,
  }

  record set-unit-sensor-radius-result {
    new-radius: s32,
  }

  record set-unit-shield-recharge-delay-query {
    unit-id: s32,
    weapon-num: s32,
    recharge-delay: f32,
  }

  record set-unit-shield-recharge-delay-result {
    success: bool,
  }

  record set-unit-shield-state-query {
    unit-id: s32,
    weapon-num: s32,
    enabled: bool,
    power: f32,
  }

  record set-unit-shield-state-result {
    success: bool,
  }

  record set-unit-sonar-stealth-query {
    unit-id: s32,
    sonar-stealth: bool,
  }

  record set-unit-sonar-stealth-result {
    success: bool,
  }

  record set-unit-stealth-query {
    unit-id: s32,
    stealth: bool,
  }

  record set-unit-stealth-result {
    success: bool,
  }

  record set-unit-stockpile-query {
    unit-id: s32,
    stockpile: s32,
    build-percent: f32,
  }

  record set-unit-stockpile-result {
    success: bool,
  }

  record set-unit-storage-query {
    unit-id: s32,
    %resource: string,
    amount: f32,
  }

  record set-unit-storage-result {
    success: bool,
  }

  record set-unit-target-options {
    manual-fire: bool,
    user-target: bool,
  }

  record set-unit-target-query {
    unit-id: s32,
    target: unit-target-ref,
    options: set-unit-target-options,
    weapon-num: s32,
  }

  record set-unit-target-result {
    success: bool,
  }

  record set-unit-tooltip-query {
    unit-id: s32,
    tooltip: string,
  }

  record set-unit-tooltip-result {
    success: bool,
  }

  record set-unit-use-air-los-query {
    unit-id: s32,
    use-air-los: bool,
  }

  record set-unit-use-air-los-result {
    success: bool,
  }

  record set-unit-use-weapons-options {
    force-use-weapons: bool,
    allow-use-weapons: bool,
  }

  record set-unit-use-weapons-query {
    unit-id: s32,
    options: set-unit-use-weapons-options,
  }

  record set-unit-use-weapons-result {
    success: bool,
  }

  record set-unit-velocity-query {
    unit-id: s32,
    velocity: float3,
  }

  record set-unit-velocity-result {
    success: bool,
  }

  record set-unit-weapon-damages-query {
    unit-id: s32,
    weapon-num: s32,
    damage-key: string,
    damage-value: f32,
  }

  record set-unit-weapon-damages-result {
    success: bool,
  }

  record set-unit-weapon-state-query {
    unit-id: s32,
    weapon-num: s32,
    key: string,
    value: f32,
  }

  record set-unit-weapon-state-result {
    success: bool,
  }

  record transfer-unit-query {
    unit-id: s32,
    new-team-id: s32,
    given: bool,
    adjust-unit-limit: bool,
  }

  record transfer-unit-result {
    success: bool,
  }

  record unit-attach-query {
    transporter-id: s32,
    transportee-id: s32,
    piece-num: s32,
  }

  record unit-attach-result {
    success: bool,
  }

  record unit-cost-overrides {
    build-time: f32,
    metal-cost: f32,
    energy-cost: f32,
  }

  record unit-detach-from-air-query {
    transportee-id: s32,
    pos: float3,
  }

  record unit-detach-from-air-result {
    success: bool,
  }

  record unit-detach-query {
    transportee-id: s32,
  }

  record unit-detach-result {
    success: bool,
  }

  record unit-finish-command-query {
    unit-id: s32,
  }

  record unit-finish-command-result {
    success: bool,
  }

  record unit-health-value {
    health: f32,
    capture: f32,
    paralyze: f32,
    build: f32,
    use-amounts: bool,
  }

  record unit-target-ref {
    target-id: s32,
    pos: float3,
    is-ground-target: bool,
  }

  record unit-weapon-fire-query {
    unit-id: s32,
    weapon-num: s32,
  }

  record unit-weapon-fire-result {
    success: bool,
  }

  record unit-weapon-hold-fire-query {
    unit-id: s32,
    weapon-num: s32,
  }

  record unit-weapon-hold-fire-result {
    success: bool,
  }

  record use-unit-resource-query {
    unit-id: s32,
    resource-type: string,
    amount: f32,
  }

  record use-unit-resource-result {
    success: bool,
  }

  /// mutating
  add-object-decal: func(unit-id: s32) -> result<bool, spring-error>;

  /// mutating
  add-unit-damage: func(unit-id: s32, damage: f32, paralyze-time: f32, weapon-def-id: s32, attacker-id: s32, impulse: float3) -> result<bool, spring-error>;

  /// mutating
  add-unit-experience: func(unit-id: s32, experience: f32) -> result<bool, spring-error>;

  /// mutating
  add-unit-impulse: func(unit-id: s32, impulse: float3, decay-rate: f32) -> result<bool, spring-error>;

  /// mutating
  add-unit-resource: func(unit-id: s32, resource-type: string, amount: f32) -> result<bool, spring-error>;

  /// mutating
  add-unit-seismic-ping: func(unit-id: s32, ping-size: f32) -> result<bool, spring-error>;

  /// mutating
  bugger-off: func(pos: float3, radius: f32, team-id: s32, options: bugger-off-options, exclude-unit-def-i-ds: list<s32>) -> result<bool, spring-error>;

  /// mutating
  clear-unit-goal: func(unit-id: s32, cancel-raw: bool) -> result<bool, spring-error>;

  /// mutating
  create-unit: func(unit-def: def-ref, pos: float3, facing: s32, team-id: s32, options: create-unit-options) -> result<s32, spring-error>;

  /// mutating
  destroy-unit: func(unit-id: s32, options: destroy-unit-options) -> result<bool, spring-error>;

  /// mutating
  edit-unit-cmd-desc: func(unit-id: s32, cmd-desc-index: u32, cmd-desc: native-command-description) -> result<bool, spring-error>;

  /// mutating
  force-unit-collision-update: func(unit-id: s32) -> result<bool, spring-error>;

  /// read-only
  get-unit-physical-state: func(unit-id: s32) -> result<u32, spring-error>;

  /// mutating
  give-order-array-to-unit: func(unit-id: s32, commands: list<native-command>) -> result<bool, spring-error>;

  /// mutating
  give-order-array-to-unit-array: func(unit-i-ds: list<s32>, commands: list<native-command>, pairwise: bool) -> result<s32, spring-error>;

  /// mutating
  give-order-to-unit: func(unit-id: s32, cmd-id: s32, params: list<f32>, options: u32, timeout: s32) -> result<bool, spring-error>;

  /// mutating
  give-order-to-unit-array: func(unit-i-ds: list<s32>, cmd-id: s32, params: list<f32>, options: u32, timeout: s32) -> result<bool, spring-error>;

  /// mutating
  insert-unit-cmd-desc: func(unit-id: s32, cmd-desc-index: s32, cmd-desc: native-command-description) -> result<bool, spring-error>;

  /// mutating
  remove-object-decal: func(unit-id: s32) -> result<bool, spring-error>;

  /// mutating
  remove-unit-cmd-desc: func(unit-id: s32, cmd-desc-index: s32) -> result<bool, spring-error>;

  /// mutating
  set-factory-bugger-off: func(unit-id: s32, options: set-factory-bugger-off-options) -> result<bool, spring-error>;

  /// mutating
  set-unit-always-visible: func(unit-id: s32, always-visible: bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-armored: func(unit-id: s32, armored-state: bool, armored-multiple: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-blocking: func(unit-id: s32, options: set-unit-blocking-options) -> result<bool, spring-error>;

  /// mutating
  set-unit-build-params: func(unit-id: s32, param-name: string, value: number-or-bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-build-speed: func(unit-id: s32, build-speed: f32, repair-speed: f32, reclaim-speed: f32, resurrect-speed: f32, capture-speed: f32, terraform-speed: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-buildee-radius: func(unit-id: s32, radius: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-cloak: func(unit-id: s32, cloak: number-or-bool, cloak-arg: number-or-bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-collision-volume-data: func(unit-id: s32, scales: float3, offsets: float3, volume-type: s32, test-type: s32, primary-axis: s32) -> result<bool, spring-error>;

  /// mutating
  set-unit-costs: func(unit-id: s32, costs: unit-cost-overrides) -> result<bool, spring-error>;

  /// mutating
  set-unit-crashing: func(unit-id: s32, want-crash: bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-direction: func(unit-id: s32, front-dir: float3, right-dir: float3) -> result<bool, spring-error>;

  /// mutating
  set-unit-experience: func(unit-id: s32, experience: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-flanking: func(unit-id: s32, %type: string, args: float3) -> result<bool, spring-error>;

  /// mutating
  set-unit-harvest-storage: func(unit-id: s32, stored-metal: f32, max-stored-metal: f32, stored-energy: f32, max-stored-energy: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-heading: func(unit-id: s32, heading: s32, use-smoothing: bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-heading-and-up-dir: func(unit-id: s32, heading: s32, up-dir: float3) -> result<bool, spring-error>;

  /// mutating
  set-unit-health: func(unit-id: s32, value: unit-health-value) -> result<bool, spring-error>;

  /// mutating
  set-unit-land-goal: func(unit-id: s32, pos: float3, radius-sq: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-leaves-ghost: func(unit-id: s32, options: set-unit-leaves-ghost-options) -> result<bool, spring-error>;

  /// mutating
  set-unit-loading-transport: func(unit-id: s32, transport-id: s32) -> result<bool, spring-error>;

  /// mutating
  set-unit-los-mask: func(unit-id: s32, ally-team-id: s32, los-mask: u8) -> result<bool, spring-error>;

  /// mutating
  set-unit-los-state: func(unit-id: s32, ally-team-id: s32, los-state: u8) -> result<bool, spring-error>;

  /// mutating
  set-unit-mass: func(unit-id: s32, mass: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-max-health: func(unit-id: s32, max-health: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-max-range: func(unit-id: s32, max-range: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-metal-extraction: func(unit-id: s32, depth: f32, range: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-mid-and-aim-pos: func(unit-id: s32, mid-pos: float3, aim-pos: float3, set-relative: bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-move-goal: func(unit-id: s32, pos: float3, radius: f32, speed: f32, raw: bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-nano-pieces: func(unit-id: s32, piece-indices: list<s32>) -> result<bool, spring-error>;

  /// mutating
  set-unit-neutral: func(unit-id: s32, neutral: bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-physical-state-bit: func(unit-id: s32, state-bit: s32) -> result<bool, spring-error>;

  /// mutating
  set-unit-physics: func(unit-id: s32, pos: float3, velocity: float3, rotation: float3, drag: float3) -> result<bool, spring-error>;

  /// mutating
  set-unit-piece-collision-volume-data: func(unit-id: s32, piece-index: s32, enable: bool, scales: float3, offsets: float3, volume-type: s32, primary-axis: s32) -> result<bool, spring-error>;

  /// mutating
  set-unit-piece-matrix: func(unit-id: s32, piece-index: s32, matrix: list<f32>) -> result<bool, spring-error>;

  /// mutating
  set-unit-piece-parent: func(unit-id: s32, child-piece-index: s32, parent-piece-index: s32) -> result<bool, spring-error>;

  /// mutating
  set-unit-piece-visible: func(unit-id: s32, piece-index: s32, visible: bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-pos-error-params: func(unit-id: s32, pos-error-vector: float3, pos-error-delta: float3, next-pos-error-update: s32, ally-team-id: s32, set-pos-error-bit: bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-position: func(unit-id: s32, pos: float3) -> result<bool, spring-error>;

  /// mutating
  set-unit-radius-and-height: func(unit-id: s32, radius: f32, height: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-resourcing: func(unit-id: s32, %type: string, amount: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-rotation: func(unit-id: s32, rotation: float3) -> result<bool, spring-error>;

  /// mutating
  set-unit-seismic-signature: func(unit-id: s32, seismic-signature: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-selection-volume-data: func(unit-id: s32, scales: float3, offsets: float3, volume-type: s32, test-type: s32, primary-axis: s32) -> result<bool, spring-error>;

  /// mutating
  set-unit-sensor-radius: func(unit-id: s32, sensor-type: string, radius: s32) -> result<s32, spring-error>;

  /// mutating
  set-unit-shield-recharge-delay: func(unit-id: s32, weapon-num: s32, recharge-delay: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-shield-state: func(unit-id: s32, weapon-num: s32, enabled: bool, power: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-sonar-stealth: func(unit-id: s32, sonar-stealth: bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-stealth: func(unit-id: s32, stealth: bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-stockpile: func(unit-id: s32, stockpile: s32, build-percent: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-storage: func(unit-id: s32, %resource: string, amount: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-target: func(unit-id: s32, target: unit-target-ref, options: set-unit-target-options, weapon-num: s32) -> result<bool, spring-error>;

  /// mutating
  set-unit-tooltip: func(unit-id: s32, tooltip: string) -> result<bool, spring-error>;

  /// mutating
  set-unit-use-air-los: func(unit-id: s32, use-air-los: bool) -> result<bool, spring-error>;

  /// mutating
  set-unit-use-weapons: func(unit-id: s32, options: set-unit-use-weapons-options) -> result<bool, spring-error>;

  /// mutating
  set-unit-velocity: func(unit-id: s32, velocity: float3) -> result<bool, spring-error>;

  /// mutating
  set-unit-weapon-damages: func(unit-id: s32, weapon-num: s32, damage-key: string, damage-value: f32) -> result<bool, spring-error>;

  /// mutating
  set-unit-weapon-state: func(unit-id: s32, weapon-num: s32, key: string, value: f32) -> result<bool, spring-error>;

  /// mutating
  transfer-unit: func(unit-id: s32, new-team-id: s32, given: bool, adjust-unit-limit: bool) -> result<bool, spring-error>;

  /// mutating
  unit-attach: func(transporter-id: s32, transportee-id: s32, piece-num: s32) -> result<bool, spring-error>;

  /// mutating
  unit-detach: func(transportee-id: s32) -> result<bool, spring-error>;

  /// mutating
  unit-detach-from-air: func(transportee-id: s32, pos: float3) -> result<bool, spring-error>;

  /// mutating
  unit-finish-command: func(unit-id: s32) -> result<bool, spring-error>;

  /// mutating
  unit-weapon-fire: func(unit-id: s32, weapon-num: s32) -> result<bool, spring-error>;

  /// mutating
  unit-weapon-hold-fire: func(unit-id: s32, weapon-num: s32) -> result<bool, spring-error>;

  /// mutating
  use-unit-resource: func(unit-id: s32, resource-type: string, amount: f32) -> result<bool, spring-error>;

}
