/* This file is part of the Spring engine (GPL v2 or later), see LICENSE.html */

// @generated by spring-api-codegen; do not edit.

#include "WasmHostAdapterSupport.h"

using namespace recoil::wasm::generated::detail;

namespace recoil::wasm::generated::detail {

}

namespace {

NativeCalloutDispatch Dispatch_terrain_control_AddGrass(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->AddGrass == nullptr) { error = "NativeInterface API is unavailable: terrain_control.AddGrass"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	AddGrassQuery query{};
	AddGrassResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "x", query.x, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 3u, 3u, "z", query.z, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "grass-value", query.grassValue, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->AddGrass(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_AddHeightMap(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->AddHeightMap == nullptr) { error = "NativeInterface API is unavailable: terrain_control.AddHeightMap"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	AddHeightMapQuery query{};
	AddHeightMapResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "x", query.x, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 3u, 3u, "z", query.z, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "height", query.height, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->AddHeightMap(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_AddOriginalHeightMap(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->AddOriginalHeightMap == nullptr) { error = "NativeInterface API is unavailable: terrain_control.AddOriginalHeightMap"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	AddOriginalHeightMapQuery query{};
	AddOriginalHeightMapResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "x", query.x, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 3u, 3u, "z", query.z, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "height", query.height, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->AddOriginalHeightMap(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_AddSmoothMesh(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->AddSmoothMesh == nullptr) { error = "NativeInterface API is unavailable: terrain_control.AddSmoothMesh"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	AddSmoothMeshQuery query{};
	AddSmoothMeshResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "x", query.x, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 3u, 3u, "z", query.z, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "height", query.height, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->AddSmoothMesh(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_AdjustHeightMap(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->AdjustHeightMap == nullptr) { error = "NativeInterface API is unavailable: terrain_control.AdjustHeightMap"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	AdjustHeightMapQuery query{};
	AdjustHeightMapResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 5u, 5u, "x1", query.x1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 5u, 5u, "z1", query.z1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 5u, 5u, "x2", query.x2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 5u, 5u, "z2", query.z2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 5u, 5u, "height", query.height, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->AdjustHeightMap(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_AdjustOriginalHeightMap(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->AdjustOriginalHeightMap == nullptr) { error = "NativeInterface API is unavailable: terrain_control.AdjustOriginalHeightMap"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	AdjustOriginalHeightMapQuery query{};
	AdjustOriginalHeightMapResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 5u, 5u, "x1", query.x1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 5u, 5u, "z1", query.z1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 5u, 5u, "x2", query.x2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 5u, 5u, "z2", query.z2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 5u, 5u, "height", query.height, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->AdjustOriginalHeightMap(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_AdjustSmoothMesh(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->AdjustSmoothMesh == nullptr) { error = "NativeInterface API is unavailable: terrain_control.AdjustSmoothMesh"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	AdjustSmoothMeshQuery query{};
	AdjustSmoothMeshResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 5u, 5u, "x1", query.x1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 5u, 5u, "z1", query.z1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 5u, 5u, "x2", query.x2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 5u, 5u, "z2", query.z2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 5u, 5u, "height", query.height, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->AdjustSmoothMesh(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_LevelHeightMap(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->LevelHeightMap == nullptr) { error = "NativeInterface API is unavailable: terrain_control.LevelHeightMap"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	LevelHeightMapQuery query{};
	LevelHeightMapResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 5u, 5u, "x1", query.x1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 5u, 5u, "z1", query.z1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 5u, 5u, "x2", query.x2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 5u, 5u, "z2", query.z2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 5u, 5u, "height", query.height, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->LevelHeightMap(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_LevelOriginalHeightMap(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->LevelOriginalHeightMap == nullptr) { error = "NativeInterface API is unavailable: terrain_control.LevelOriginalHeightMap"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	LevelOriginalHeightMapQuery query{};
	LevelOriginalHeightMapResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 5u, 5u, "x1", query.x1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 5u, 5u, "z1", query.z1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 5u, 5u, "x2", query.x2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 5u, 5u, "z2", query.z2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 5u, 5u, "height", query.height, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->LevelOriginalHeightMap(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_LevelSmoothMesh(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->LevelSmoothMesh == nullptr) { error = "NativeInterface API is unavailable: terrain_control.LevelSmoothMesh"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	LevelSmoothMeshQuery query{};
	LevelSmoothMeshResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 5u, 5u, "x1", query.x1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 5u, 5u, "z1", query.z1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 5u, 5u, "x2", query.x2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 5u, 5u, "z2", query.z2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 5u, 5u, "height", query.height, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->LevelSmoothMesh(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_RebuildSmoothMesh(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->RebuildSmoothMesh == nullptr) { error = "NativeInterface API is unavailable: terrain_control.RebuildSmoothMesh"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	RebuildSmoothMeshQuery query{};
	RebuildSmoothMeshResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 0u, 1u, "unused", query._unused, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->RebuildSmoothMesh(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_RemoveGrass(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->RemoveGrass == nullptr) { error = "NativeInterface API is unavailable: terrain_control.RemoveGrass"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	RemoveGrassQuery query{};
	RemoveGrassResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 2u, 2u, "x", query.x, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 2u, 2u, "z", query.z, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->RemoveGrass(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_RevertHeightMap(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->RevertHeightMap == nullptr) { error = "NativeInterface API is unavailable: terrain_control.RevertHeightMap"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	RevertHeightMapQuery query{};
	RevertHeightMapResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 5u, 5u, "x1", query.x1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 5u, 5u, "z1", query.z1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 5u, 5u, "x2", query.x2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 5u, 5u, "z2", query.z2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 5u, 5u, "orig-factor", query.origFactor, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->RevertHeightMap(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_RevertOriginalHeightMap(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->RevertOriginalHeightMap == nullptr) { error = "NativeInterface API is unavailable: terrain_control.RevertOriginalHeightMap"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	RevertOriginalHeightMapQuery query{};
	RevertOriginalHeightMapResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 5u, 5u, "x1", query.x1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 5u, 5u, "z1", query.z1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 5u, 5u, "x2", query.x2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 5u, 5u, "z2", query.z2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 5u, 5u, "orig-factor", query.origFactor, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->RevertOriginalHeightMap(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_RevertSmoothMesh(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->RevertSmoothMesh == nullptr) { error = "NativeInterface API is unavailable: terrain_control.RevertSmoothMesh"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	RevertSmoothMeshQuery query{};
	RevertSmoothMeshResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 5u, 5u, "x1", query.x1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 5u, 5u, "z1", query.z1, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 5u, 5u, "x2", query.x2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 5u, 5u, "z2", query.z2, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 5u, 5u, "orig-factor", query.origFactor, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->RevertSmoothMesh(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_SetHeightMap(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->SetHeightMap == nullptr) { error = "NativeInterface API is unavailable: terrain_control.SetHeightMap"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetHeightMapQuery query{};
	SetHeightMapResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 4u, 4u, "x", query.x, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 4u, 4u, "z", query.z, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 4u, 4u, "height", query.height, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 4u, 4u, "terraform", query.terraform, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->SetHeightMap(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_SetMapSquareTerrainType(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->SetMapSquareTerrainType == nullptr) { error = "NativeInterface API is unavailable: terrain_control.SetMapSquareTerrainType"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetMapSquareTerrainTypeQuery query{};
	SetMapSquareTerrainTypeResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "x", query.x, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 3u, 3u, "z", query.z, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "terrain-type", query.terrainType, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->SetMapSquareTerrainType(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_SetOriginalHeightMap(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->SetOriginalHeightMap == nullptr) { error = "NativeInterface API is unavailable: terrain_control.SetOriginalHeightMap"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetOriginalHeightMapQuery query{};
	SetOriginalHeightMapResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 4u, 4u, "x", query.x, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 4u, 4u, "z", query.z, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 4u, 4u, "height", query.height, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 4u, 4u, "factor", query.factor, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->SetOriginalHeightMap(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_SetSmoothMesh(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->SetSmoothMesh == nullptr) { error = "NativeInterface API is unavailable: terrain_control.SetSmoothMesh"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetSmoothMeshQuery query{};
	SetSmoothMeshResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 4u, 4u, "x", query.x, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 4u, 4u, "z", query.z, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 4u, 4u, "height", query.height, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 4u, 4u, "terraform", query.terraform, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->SetSmoothMesh(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_SetTerrainTypeData(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->SetTerrainTypeData == nullptr) { error = "NativeInterface API is unavailable: terrain_control.SetTerrainTypeData"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetTerrainTypeDataQuery query{};
	SetTerrainTypeDataResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 8u, 8u, "type-index", query.typeIndex, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 8u, 8u, "tank-speed", query.tankSpeed, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 8u, 8u, "kbot-speed", query.kbotSpeed, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 8u, 8u, "hover-speed", query.hoverSpeed, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 8u, 8u, "ship-speed", query.shipSpeed, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 5u, 5u, 8u, 8u, "hardness", query.hardness, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 6u, 6u, 8u, 8u, "receive-tracks", query.receiveTracks, error)) return NativeCalloutDispatch::handled;
	const WasmValue* value_name = FindArgument(arguments, 7u, 7u, 8u, 8u, "name", error);
	if (value_name == nullptr) return NativeCalloutDispatch::handled;
	auto& stored_string_query_name = storage.Make<std::string>();
	if (!ReadString(*value_name, stored_string_query_name, error)) { return NativeCalloutDispatch::handled; }
	query.name = stored_string_query_name.c_str();
	nativeInterface->syncedCtrl->terrain->SetTerrainTypeData(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_SetTidal(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->SetTidal == nullptr) { error = "NativeInterface API is unavailable: terrain_control.SetTidal"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetTidalQuery query{};
	SetTidalResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 1u, 1u, "tidal", query.tidal, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->SetTidal(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_terrain_control_SetWind(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->terrain == nullptr || nativeInterface->syncedCtrl->terrain->SetWind == nullptr) { error = "NativeInterface API is unavailable: terrain_control.SetWind"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetWindQuery query{};
	SetWindResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 2u, 2u, "min-wind", query.minWind, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 2u, 2u, "max-wind", query.maxWind, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->terrain->SetWind(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

}

namespace recoil::wasm::generated {

const NativeCalloutTarget* ResolveNativeCalloutModule_terrain_control(std::string_view);
NativeCalloutDispatch DispatchNativeCalloutModule_terrain_control(NativeInterface*, std::string_view, const std::vector<WasmValue>&, WasmValue&, std::string&);
const NativeCalloutTarget* ResolveNativeCalloutModule_terrain_control(std::string_view function)
{
	if (detail::FunctionEquals(function, "AddGrass")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_AddGrass}; return &target; }
	if (detail::FunctionEquals(function, "AddHeightMap")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_AddHeightMap}; return &target; }
	if (detail::FunctionEquals(function, "AddOriginalHeightMap")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_AddOriginalHeightMap}; return &target; }
	if (detail::FunctionEquals(function, "AddSmoothMesh")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_AddSmoothMesh}; return &target; }
	if (detail::FunctionEquals(function, "AdjustHeightMap")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_AdjustHeightMap}; return &target; }
	if (detail::FunctionEquals(function, "AdjustOriginalHeightMap")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_AdjustOriginalHeightMap}; return &target; }
	if (detail::FunctionEquals(function, "AdjustSmoothMesh")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_AdjustSmoothMesh}; return &target; }
	if (detail::FunctionEquals(function, "LevelHeightMap")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_LevelHeightMap}; return &target; }
	if (detail::FunctionEquals(function, "LevelOriginalHeightMap")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_LevelOriginalHeightMap}; return &target; }
	if (detail::FunctionEquals(function, "LevelSmoothMesh")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_LevelSmoothMesh}; return &target; }
	if (detail::FunctionEquals(function, "RebuildSmoothMesh")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_RebuildSmoothMesh}; return &target; }
	if (detail::FunctionEquals(function, "RemoveGrass")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_RemoveGrass}; return &target; }
	if (detail::FunctionEquals(function, "RevertHeightMap")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_RevertHeightMap}; return &target; }
	if (detail::FunctionEquals(function, "RevertOriginalHeightMap")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_RevertOriginalHeightMap}; return &target; }
	if (detail::FunctionEquals(function, "RevertSmoothMesh")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_RevertSmoothMesh}; return &target; }
	if (detail::FunctionEquals(function, "SetHeightMap")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_SetHeightMap}; return &target; }
	if (detail::FunctionEquals(function, "SetMapSquareTerrainType")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_SetMapSquareTerrainType}; return &target; }
	if (detail::FunctionEquals(function, "SetOriginalHeightMap")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_SetOriginalHeightMap}; return &target; }
	if (detail::FunctionEquals(function, "SetSmoothMesh")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_SetSmoothMesh}; return &target; }
	if (detail::FunctionEquals(function, "SetTerrainTypeData")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_SetTerrainTypeData}; return &target; }
	if (detail::FunctionEquals(function, "SetTidal")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_SetTidal}; return &target; }
	if (detail::FunctionEquals(function, "SetWind")) { static constexpr NativeCalloutTarget target{&Dispatch_terrain_control_SetWind}; return &target; }
	return nullptr;
}

NativeCalloutDispatch DispatchNativeCalloutModule_terrain_control(NativeInterface* nativeInterface, std::string_view function, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	const NativeCalloutTarget* target = ResolveNativeCalloutModule_terrain_control(function);
	if (target == nullptr) return NativeCalloutDispatch::notHandled;
	return target->invoke(nativeInterface, arguments, result, error);
}
}
