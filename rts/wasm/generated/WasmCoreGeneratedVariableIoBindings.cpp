/* @generated by spring-api-codegen; do not edit. */

#include "wasm/generated/WasmCoreGeneratedVariableIoBindings.h"

#include <array>
#include <bit>
#include <cstdint>
#include <limits>
#include <span>
#include <string>
#include <string_view>
#include <type_traits>
#include <vector>

#include "NativeInterface/NativeInterface.h"
#include "WasmInterface/core/host/WasmCoreAbi.h"
#include "WasmInterface/core/host/WasmCoreGeneratedSupport.h"
#include "WasmInterface/core/host/WasmCoreWire.h"

namespace recoil::wasm::core::generated {
namespace {

template<typename T>
bool AssignCoreCount(std::uint32_t value, T& output)
{
    using Native = std::remove_cv_t<std::remove_reference_t<T>>;
    static_assert(std::is_integral_v<Native>);
    if (static_cast<std::uint64_t>(value) >
        static_cast<std::uint64_t>(std::numeric_limits<Native>::max()))
        return false;
    output = static_cast<Native>(value);
    return true;
}

bool WriteCoreU32(std::span<std::uint8_t> bytes, std::size_t offset, std::uint32_t value)
{
    if (offset > bytes.size() || bytes.size() - offset < 4)
        return false;
    bytes[offset + 0] = static_cast<std::uint8_t>(value);
    bytes[offset + 1] = static_cast<std::uint8_t>(value >> 8);
    bytes[offset + 2] = static_cast<std::uint8_t>(value >> 16);
    bytes[offset + 3] = static_cast<std::uint8_t>(value >> 24);
    return true;
}

bool DefineGeneratedVariableIo(wasmtime_linker_t* linker, const char* moduleName,
    const char* functionName, wasm_functype_t* type,
    wasmtime_func_unchecked_callback_t callback, HostState* state, std::string& error)
{
    wasmtime_error_t* defineError = wasmtime_linker_define_func_unchecked(
        linker,
        moduleName, std::char_traits<char>::length(moduleName),
        functionName, std::char_traits<char>::length(functionName),
        type, callback, state, nullptr);
    wasm_functype_delete(type);
    if (defineError == nullptr)
        return true;
    error = ErrorMessage(defineError);
    return false;
}

wasm_trap_t* CoreVariableIo_unit_defs_get_unit_def_custom_param(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefCustomParam == nullptr)
        return Trap("GetUnitDefCustomParam generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetUnitDefCustomParam generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetUnitDefCustomParamQuery query{};
    query.unitDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitDefID)>>>(slots[0].i32);
    std::uint32_t keyInputPointer = 0;
    std::uint32_t keyInputCount = 0;
    if (!reader.U32(keyInputPointer) || !reader.U32(keyInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyInputWire;
    if (!state->memory.View(keyInputPointer, keyInputCount, keyInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader keyInputReader(keyInputWire);
    std::string keyInputStorage;
    { std::uint32_t coreLength = 0; if (!keyInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!keyInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } keyInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.key = keyInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetUnitDefCustomParamResult result{};
    state->native->unitDefs->GetUnitDefCustomParam(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_unit_defs_get_unit_def_param_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefParamString == nullptr)
        return Trap("GetUnitDefParamString generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetUnitDefParamString generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetUnitDefParamStringQuery query{};
    query.unitDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitDefID)>>>(slots[0].i32);
    std::uint32_t keyInputPointer = 0;
    std::uint32_t keyInputCount = 0;
    if (!reader.U32(keyInputPointer) || !reader.U32(keyInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyInputWire;
    if (!state->memory.View(keyInputPointer, keyInputCount, keyInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader keyInputReader(keyInputWire);
    std::string keyInputStorage;
    { std::uint32_t coreLength = 0; if (!keyInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!keyInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } keyInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.key = keyInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetUnitDefParamStringResult result{};
    state->native->unitDefs->GetUnitDefParamString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_feature_defs_get_feature_def_custom_param(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->featureDefs == nullptr ||
        state->native->featureDefs->GetFeatureDefCustomParam == nullptr)
        return Trap("GetFeatureDefCustomParam generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetFeatureDefCustomParam generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetFeatureDefCustomParamQuery query{};
    query.featureDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.featureDefID)>>>(slots[0].i32);
    std::uint32_t keyInputPointer = 0;
    std::uint32_t keyInputCount = 0;
    if (!reader.U32(keyInputPointer) || !reader.U32(keyInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyInputWire;
    if (!state->memory.View(keyInputPointer, keyInputCount, keyInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader keyInputReader(keyInputWire);
    std::string keyInputStorage;
    { std::uint32_t coreLength = 0; if (!keyInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!keyInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } keyInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.key = keyInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetFeatureDefCustomParamResult result{};
    state->native->featureDefs->GetFeatureDefCustomParam(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_weapon_defs_get_weapon_def_custom_param(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->weaponDefs == nullptr ||
        state->native->weaponDefs->GetWeaponDefCustomParam == nullptr)
        return Trap("GetWeaponDefCustomParam generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetWeaponDefCustomParam generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetWeaponDefCustomParamQuery query{};
    query.weaponDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.weaponDefID)>>>(slots[0].i32);
    std::uint32_t keyInputPointer = 0;
    std::uint32_t keyInputCount = 0;
    if (!reader.U32(keyInputPointer) || !reader.U32(keyInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyInputWire;
    if (!state->memory.View(keyInputPointer, keyInputCount, keyInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader keyInputReader(keyInputWire);
    std::string keyInputStorage;
    { std::uint32_t coreLength = 0; if (!keyInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!keyInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } keyInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.key = keyInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetWeaponDefCustomParamResult result{};
    state->native->weaponDefs->GetWeaponDefCustomParam(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_game_get_map_option(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->game == nullptr ||
        state->native->game->GetMapOption == nullptr)
        return Trap("GetMapOption generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetMapOption generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetMapOptionQuery query{};
    std::uint32_t keyInputPointer = 0;
    std::uint32_t keyInputCount = 0;
    if (!reader.U32(keyInputPointer) || !reader.U32(keyInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyInputWire;
    if (!state->memory.View(keyInputPointer, keyInputCount, keyInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader keyInputReader(keyInputWire);
    std::string keyInputStorage;
    { std::uint32_t coreLength = 0; if (!keyInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!keyInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } keyInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.key = keyInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetMapOptionResult result{};
    state->native->game->GetMapOption(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_game_get_mod_option(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->game == nullptr ||
        state->native->game->GetModOption == nullptr)
        return Trap("GetModOption generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetModOption generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetModOptionQuery query{};
    std::uint32_t keyInputPointer = 0;
    std::uint32_t keyInputCount = 0;
    if (!reader.U32(keyInputPointer) || !reader.U32(keyInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyInputWire;
    if (!state->memory.View(keyInputPointer, keyInputCount, keyInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader keyInputReader(keyInputWire);
    std::string keyInputStorage;
    { std::uint32_t coreLength = 0; if (!keyInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!keyInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } keyInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.key = keyInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetModOptionResult result{};
    state->native->game->GetModOption(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_encoding_decode_base64(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->encoding == nullptr ||
        state->native->encoding->DecodeBase64 == nullptr)
        return Trap("DecodeBase64 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("DecodeBase64 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader decodedControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t decodedPointer = 0;
    std::uint32_t decodedCapacity = 0;
    std::uint32_t decodedIgnoredLength = 0;
    if (!decodedControl.U32(decodedPointer) || !decodedControl.U32(decodedCapacity) ||
        !decodedControl.U32(decodedIgnoredLength) || !decodedControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t decodedCapacityBytes = static_cast<std::uint64_t>(decodedCapacity) * 4u;
    if (decodedCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(decodedPointer, static_cast<std::size_t>(decodedCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    DecodeBase64Query query{};
    std::uint32_t textInputPointer = 0;
    std::uint32_t textInputCount = 0;
    if (!reader.U32(textInputPointer) || !reader.U32(textInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textInputWire;
    if (!state->memory.View(textInputPointer, textInputCount, textInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader textInputReader(textInputWire);
    std::string textInputStorage;
    { std::uint32_t coreLength = 0; if (!textInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!textInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } textInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.text = textInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    DecodeBase64Result result{};
    state->native->encoding->DecodeBase64(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.decodedLength) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t decodedRequired = static_cast<std::uint32_t>(result.decodedLength);
    if (decodedRequired != 0 && result.decoded == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, decodedRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || decodedCapacity < decodedRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (decodedRequired != 0) {
        const std::size_t decodedBytes = static_cast<std::size_t>(decodedRequired) * 4u;
        std::span<std::uint8_t> decodedWire;
        if (!state->memory.MutableView(decodedPointer, decodedBytes, decodedWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter decodedWriter(decodedWire);
        for (std::uint32_t coreIndex = 0; coreIndex < decodedRequired; ++coreIndex) {
        if (!decodedWriter.U32(result.decoded[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!decodedWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_encoding_decode_base64_url(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->encoding == nullptr ||
        state->native->encoding->DecodeBase64Url == nullptr)
        return Trap("DecodeBase64Url generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("DecodeBase64Url generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader decodedControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t decodedPointer = 0;
    std::uint32_t decodedCapacity = 0;
    std::uint32_t decodedIgnoredLength = 0;
    if (!decodedControl.U32(decodedPointer) || !decodedControl.U32(decodedCapacity) ||
        !decodedControl.U32(decodedIgnoredLength) || !decodedControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t decodedCapacityBytes = static_cast<std::uint64_t>(decodedCapacity) * 4u;
    if (decodedCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(decodedPointer, static_cast<std::size_t>(decodedCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    DecodeBase64UrlQuery query{};
    std::uint32_t textInputPointer = 0;
    std::uint32_t textInputCount = 0;
    if (!reader.U32(textInputPointer) || !reader.U32(textInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textInputWire;
    if (!state->memory.View(textInputPointer, textInputCount, textInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader textInputReader(textInputWire);
    std::string textInputStorage;
    { std::uint32_t coreLength = 0; if (!textInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!textInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } textInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.text = textInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    DecodeBase64UrlResult result{};
    state->native->encoding->DecodeBase64Url(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.decodedLength) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t decodedRequired = static_cast<std::uint32_t>(result.decodedLength);
    if (decodedRequired != 0 && result.decoded == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, decodedRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || decodedCapacity < decodedRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (decodedRequired != 0) {
        const std::size_t decodedBytes = static_cast<std::size_t>(decodedRequired) * 4u;
        std::span<std::uint8_t> decodedWire;
        if (!state->memory.MutableView(decodedPointer, decodedBytes, decodedWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter decodedWriter(decodedWire);
        for (std::uint32_t coreIndex = 0; coreIndex < decodedRequired; ++coreIndex) {
        if (!decodedWriter.U32(result.decoded[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!decodedWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_encoding_encode_base64(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->encoding == nullptr ||
        state->native->encoding->EncodeBase64 == nullptr)
        return Trap("EncodeBase64 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("EncodeBase64 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader encodedControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t encodedPointer = 0;
    std::uint32_t encodedCapacity = 0;
    std::uint32_t encodedIgnoredLength = 0;
    if (!encodedControl.U32(encodedPointer) || !encodedControl.U32(encodedCapacity) ||
        !encodedControl.U32(encodedIgnoredLength) || !encodedControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(encodedPointer, encodedCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    EncodeBase64Query query{};
    std::uint32_t textInputPointer = 0;
    std::uint32_t textInputCount = 0;
    if (!reader.U32(textInputPointer) || !reader.U32(textInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t textInputBytes64 = static_cast<std::uint64_t>(textInputCount) * 4u;
    if (textInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> textInputWire;
    if (!state->memory.View(textInputPointer, static_cast<std::size_t>(textInputBytes64), textInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader textInputReader(textInputWire);
    std::vector<std::uint8_t> textInputStorage;
    textInputStorage.reserve(textInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < textInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!textInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        textInputStorage.push_back(item);
    }
    if (!textInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.text = textInputStorage.data();
    if (!AssignCoreCount(textInputCount, query.textLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.stripPadding = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    EncodeBase64Result result{};
    state->native->encoding->EncodeBase64(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t encodedRequiredSize = result.encoded == nullptr ? 0u : std::char_traits<char>::length(result.encoded);
    if (encodedRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t encodedRequired = static_cast<std::uint32_t>(encodedRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, encodedRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || encodedCapacity < encodedRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (encodedRequired != 0 && !state->memory.Write(encodedPointer, result.encoded, encodedRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_encoding_encode_base64_url(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->encoding == nullptr ||
        state->native->encoding->EncodeBase64Url == nullptr)
        return Trap("EncodeBase64Url generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("EncodeBase64Url generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader encodedControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t encodedPointer = 0;
    std::uint32_t encodedCapacity = 0;
    std::uint32_t encodedIgnoredLength = 0;
    if (!encodedControl.U32(encodedPointer) || !encodedControl.U32(encodedCapacity) ||
        !encodedControl.U32(encodedIgnoredLength) || !encodedControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(encodedPointer, encodedCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    EncodeBase64UrlQuery query{};
    std::uint32_t textInputPointer = 0;
    std::uint32_t textInputCount = 0;
    if (!reader.U32(textInputPointer) || !reader.U32(textInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t textInputBytes64 = static_cast<std::uint64_t>(textInputCount) * 4u;
    if (textInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> textInputWire;
    if (!state->memory.View(textInputPointer, static_cast<std::size_t>(textInputBytes64), textInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader textInputReader(textInputWire);
    std::vector<std::uint8_t> textInputStorage;
    textInputStorage.reserve(textInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < textInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!textInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        textInputStorage.push_back(item);
    }
    if (!textInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.text = textInputStorage.data();
    if (!AssignCoreCount(textInputCount, query.textLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    EncodeBase64UrlResult result{};
    state->native->encoding->EncodeBase64Url(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t encodedRequiredSize = result.encoded == nullptr ? 0u : std::char_traits<char>::length(result.encoded);
    if (encodedRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t encodedRequired = static_cast<std::uint32_t>(encodedRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, encodedRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || encodedCapacity < encodedRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (encodedRequired != 0 && !state->memory.Write(encodedPointer, result.encoded, encodedRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_input_get_key_from_scan_symbol(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->input == nullptr ||
        state->native->input->GetKeyFromScanSymbol == nullptr)
        return Trap("GetKeyFromScanSymbol generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetKeyFromScanSymbol generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader keyNameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t keyNamePointer = 0;
    std::uint32_t keyNameCapacity = 0;
    std::uint32_t keyNameIgnoredLength = 0;
    if (!keyNameControl.U32(keyNamePointer) || !keyNameControl.U32(keyNameCapacity) ||
        !keyNameControl.U32(keyNameIgnoredLength) || !keyNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(keyNamePointer, keyNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetKeyFromScanSymbolQuery query{};
    std::uint32_t scanSymbolInputPointer = 0;
    std::uint32_t scanSymbolInputCount = 0;
    if (!reader.U32(scanSymbolInputPointer) || !reader.U32(scanSymbolInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> scanSymbolInputWire;
    if (!state->memory.View(scanSymbolInputPointer, scanSymbolInputCount, scanSymbolInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader scanSymbolInputReader(scanSymbolInputWire);
    std::string scanSymbolInputStorage;
    { std::uint32_t coreLength = 0; if (!scanSymbolInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!scanSymbolInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } scanSymbolInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.scanSymbol = scanSymbolInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetKeyFromScanSymbolResult result{};
    state->native->input->GetKeyFromScanSymbol(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t keyNameRequiredSize = result.keyName == nullptr ? 0u : std::char_traits<char>::length(result.keyName);
    if (keyNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t keyNameRequired = static_cast<std::uint32_t>(keyNameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, keyNameRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || keyNameCapacity < keyNameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (keyNameRequired != 0 && !state->memory.Write(keyNamePointer, result.keyName, keyNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_config_get_config_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->config == nullptr ||
        state->native->config->GetConfigString == nullptr)
        return Trap("GetConfigString generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetConfigString generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 20u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetConfigStringQuery query{};
    std::uint32_t keyInputPointer = 0;
    std::uint32_t keyInputCount = 0;
    if (!reader.U32(keyInputPointer) || !reader.U32(keyInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyInputWire;
    if (!state->memory.View(keyInputPointer, keyInputCount, keyInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader keyInputReader(keyInputWire);
    std::string keyInputStorage;
    { std::uint32_t coreLength = 0; if (!keyInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!keyInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } keyInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.key = keyInputStorage.c_str();
    bool defaultValueInputPresent = false;
    std::uint32_t defaultValueInputPointer = 0;
    std::uint32_t defaultValueInputCount = 0;
    if (!reader.Bool(defaultValueInputPresent) || !reader.U32(defaultValueInputPointer) || !reader.U32(defaultValueInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.hasDefault = defaultValueInputPresent;
    std::string defaultValueInputStorage;
    if (!defaultValueInputPresent) {
        query.defaultValue = nullptr;
    } else {
        std::span<const std::uint8_t> defaultValueInputBytes;
        if (!state->memory.View(defaultValueInputPointer, defaultValueInputCount, defaultValueInputBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
        defaultValueInputStorage.assign(reinterpret_cast<const char*>(defaultValueInputBytes.data()), defaultValueInputBytes.size());
        query.defaultValue = defaultValueInputStorage.c_str();
    }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetConfigStringResult result{};
    state->native->config->GetConfigString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_tracing_trace_ray_between_positions(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->tracing == nullptr ||
        state->native->tracing->TraceRayBetweenPositions == nullptr)
        return Trap("TraceRayBetweenPositions generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("TraceRayBetweenPositions generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 32u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader hitsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t hitsPointer = 0;
    std::uint32_t hitsCapacity = 0;
    std::uint32_t hitsIgnoredLength = 0;
    if (!hitsControl.U32(hitsPointer) || !hitsControl.U32(hitsCapacity) ||
        !hitsControl.U32(hitsIgnoredLength) || !hitsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t hitsCapacityBytes = static_cast<std::uint64_t>(hitsCapacity) * 12u;
    if (hitsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(hitsPointer, static_cast<std::size_t>(hitsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    TraceRayBetweenPositionsQuery query{};
    if (!reader.F32(query.start.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.start.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.start.z)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.end.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.end.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.end.z)) return Trap("generated Core wire underflow");
    std::uint32_t typeInputPointer = 0;
    std::uint32_t typeInputCount = 0;
    if (!reader.U32(typeInputPointer) || !reader.U32(typeInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> typeInputWire;
    if (!state->memory.View(typeInputPointer, typeInputCount, typeInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader typeInputReader(typeInputWire);
    std::string typeInputStorage;
    { std::uint32_t coreLength = 0; if (!typeInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!typeInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } typeInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.type = typeInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    TraceRayBetweenPositionsResult result{};
    state->native->tracing->TraceRayBetweenPositions(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.count) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t hitsRequired = static_cast<std::uint32_t>(result.count);
    if (hitsRequired != 0 && result.hits == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, hitsRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || hitsCapacity < hitsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (hitsRequired != 0) {
        const std::size_t hitsBytes = static_cast<std::size_t>(hitsRequired) * 12u;
        std::span<std::uint8_t> hitsWire;
        if (!state->memory.MutableView(hitsPointer, hitsBytes, hitsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter hitsWriter(hitsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < hitsRequired; ++coreIndex) {
        if (!hitsWriter.F32(result.hits[coreIndex].hitLength)) return Trap("generated Core wire overflow");
        if (!hitsWriter.I32(result.hits[coreIndex].objectID)) return Trap("generated Core wire overflow");
        if (!hitsWriter.I32(result.hits[coreIndex].objectType)) return Trap("generated Core wire overflow");
        }
        if (!hitsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_tracing_trace_ray_in_direction(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->tracing == nullptr ||
        state->native->tracing->TraceRayInDirection == nullptr)
        return Trap("TraceRayInDirection generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("TraceRayInDirection generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 40u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader hitsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t hitsPointer = 0;
    std::uint32_t hitsCapacity = 0;
    std::uint32_t hitsIgnoredLength = 0;
    if (!hitsControl.U32(hitsPointer) || !hitsControl.U32(hitsCapacity) ||
        !hitsControl.U32(hitsIgnoredLength) || !hitsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t hitsCapacityBytes = static_cast<std::uint64_t>(hitsCapacity) * 12u;
    if (hitsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(hitsPointer, static_cast<std::size_t>(hitsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    TraceRayInDirectionQuery query{};
    if (!reader.F32(query.pos.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.pos.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.pos.z)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.dir.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.dir.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.dir.z)) return Trap("generated Core wire underflow");
    {
        bool corePresent = false;
        if (!reader.Bool(corePresent)) return Trap("generated Core option presence underflow");
        query.options.hasMaxLength = corePresent;
    }
    if (!reader.F32(query.options.maxLength)) return Trap("generated Core wire underflow");
    if (!reader.Align(4u)) return Trap("generated Core option record alignment underflow");
    std::uint32_t typeInputPointer = 0;
    std::uint32_t typeInputCount = 0;
    if (!reader.U32(typeInputPointer) || !reader.U32(typeInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> typeInputWire;
    if (!state->memory.View(typeInputPointer, typeInputCount, typeInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader typeInputReader(typeInputWire);
    std::string typeInputStorage;
    { std::uint32_t coreLength = 0; if (!typeInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!typeInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } typeInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.type = typeInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    TraceRayInDirectionResult result{};
    state->native->tracing->TraceRayInDirection(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.count) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t hitsRequired = static_cast<std::uint32_t>(result.count);
    if (hitsRequired != 0 && result.hits == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, hitsRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || hitsCapacity < hitsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (hitsRequired != 0) {
        const std::size_t hitsBytes = static_cast<std::size_t>(hitsRequired) * 12u;
        std::span<std::uint8_t> hitsWire;
        if (!state->memory.MutableView(hitsPointer, hitsBytes, hitsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter hitsWriter(hitsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < hitsRequired; ++coreIndex) {
        if (!hitsWriter.F32(result.hits[coreIndex].hitLength)) return Trap("generated Core wire overflow");
        if (!hitsWriter.I32(result.hits[coreIndex].objectID)) return Trap("generated Core wire overflow");
        if (!hitsWriter.I32(result.hits[coreIndex].objectType)) return Trap("generated Core wire overflow");
        }
        if (!hitsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_unsynced_ctrl_get_water_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->GetWaterTexture == nullptr)
        return Trap("GetWaterTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetWaterTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader texNameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t texNamePointer = 0;
    std::uint32_t texNameCapacity = 0;
    std::uint32_t texNameIgnoredLength = 0;
    if (!texNameControl.U32(texNamePointer) || !texNameControl.U32(texNameCapacity) ||
        !texNameControl.U32(texNameIgnoredLength) || !texNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(texNamePointer, texNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetWaterTextureQuery query{};
    std::uint32_t texTypeInputPointer = 0;
    std::uint32_t texTypeInputCount = 0;
    if (!reader.U32(texTypeInputPointer) || !reader.U32(texTypeInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> texTypeInputWire;
    if (!state->memory.View(texTypeInputPointer, texTypeInputCount, texTypeInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader texTypeInputReader(texTypeInputWire);
    std::string texTypeInputStorage;
    { std::uint32_t coreLength = 0; if (!texTypeInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!texTypeInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } texTypeInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.texType = texTypeInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetWaterTextureResult result{};
    state->native->unsyncedCtrl->GetWaterTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t texNameRequiredSize = result.texName == nullptr ? 0u : std::char_traits<char>::length(result.texName);
    if (texNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t texNameRequired = static_cast<std::uint32_t>(texNameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, texNameRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || texNameCapacity < texNameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (texNameRequired != 0 && !state->memory.Write(texNamePointer, result.texName, texNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_gfx_font_wrap_text(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->FontWrapText == nullptr)
        return Trap("FontWrapText generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 6)
        return Trap("FontWrapText generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 7u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[4].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[5].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader textControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t textPointer = 0;
    std::uint32_t textCapacity = 0;
    std::uint32_t textIgnoredLength = 0;
    if (!textControl.U32(textPointer) || !textControl.U32(textCapacity) ||
        !textControl.U32(textIgnoredLength) || !textControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(textPointer, textCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GfxFontWrapTextQuery query{};
    query.fontID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fontID)>>>(slots[0].i32);
    std::uint32_t textInputPointer = 0;
    std::uint32_t textInputCount = 0;
    if (!reader.U32(textInputPointer) || !reader.U32(textInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textInputWire;
    if (!state->memory.View(textInputPointer, textInputCount, textInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader textInputReader(textInputWire);
    std::string textInputStorage;
    { std::uint32_t coreLength = 0; if (!textInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!textInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } textInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.text = textInputStorage.c_str();
    query.maxWidth = slots[1].f32;
    query.maxHeight = slots[2].f32;
    query.size = slots[3].f32;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxFontWrapTextResult result{};
    state->native->gfx->FontWrapText(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t textRequiredSize = result.text == nullptr ? 0u : std::char_traits<char>::length(result.text);
    if (textRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t textRequired = static_cast<std::uint32_t>(textRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, textRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || textCapacity < textRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (textRequired != 0 && !state->memory.Write(textPointer, result.text, textRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.I32(result.lines)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_gfx_get_atmosphere(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetAtmosphere == nullptr)
        return Trap("GetAtmosphere generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetAtmosphere generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 16u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 40u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader stringValueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t stringValuePointer = 0;
    std::uint32_t stringValueCapacity = 0;
    std::uint32_t stringValueIgnoredLength = 0;
    if (!stringValueControl.U32(stringValuePointer) || !stringValueControl.U32(stringValueCapacity) ||
        !stringValueControl.U32(stringValueIgnoredLength) || !stringValueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(stringValuePointer, stringValueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GfxValueQuery query{};
    std::uint32_t keyInputPointer = 0;
    std::uint32_t keyInputCount = 0;
    if (!reader.U32(keyInputPointer) || !reader.U32(keyInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyInputWire;
    if (!state->memory.View(keyInputPointer, keyInputCount, keyInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader keyInputReader(keyInputWire);
    std::string keyInputStorage;
    { std::uint32_t coreLength = 0; if (!keyInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!keyInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } keyInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.key = keyInputStorage.c_str();
    std::uint32_t modeInputPointer = 0;
    std::uint32_t modeInputCount = 0;
    if (!reader.U32(modeInputPointer) || !reader.U32(modeInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> modeInputWire;
    if (!state->memory.View(modeInputPointer, modeInputCount, modeInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader modeInputReader(modeInputWire);
    std::string modeInputStorage;
    { std::uint32_t coreLength = 0; if (!modeInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!modeInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } modeInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.mode = modeInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxValueResult result{};
    state->native->gfx->GetAtmosphere(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t stringValueRequiredSize = result.stringValue == nullptr ? 0u : std::char_traits<char>::length(result.stringValue);
    if (stringValueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t stringValueRequired = static_cast<std::uint32_t>(stringValueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, stringValueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || stringValueCapacity < stringValueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (stringValueRequired != 0 && !state->memory.Write(stringValuePointer, result.stringValue, stringValueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 28u);
    WireWriter writer(fixedWire);
    for (std::size_t coreWriteIndex1 = 0; coreWriteIndex1 < 4u; ++coreWriteIndex1) {
        if (!writer.F32(result.values[coreWriteIndex1])) return Trap("generated Core wire overflow");
    }
    if (!writer.U32(result.count)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.hasBool)) return Trap("generated Core option overflow");
    if (result.hasBool) {
        if (!writer.Bool(result.boolValue)) return Trap("generated Core wire overflow");
    } else {
        std::remove_cv_t<std::remove_reference_t<decltype(result.boolValue)>> coreAbsent{};
        if (!writer.Bool(coreAbsent)) return Trap("generated Core wire overflow");
    }
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_gfx_get_map_rendering(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetMapRendering == nullptr)
        return Trap("GetMapRendering generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetMapRendering generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 16u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 40u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader stringValueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t stringValuePointer = 0;
    std::uint32_t stringValueCapacity = 0;
    std::uint32_t stringValueIgnoredLength = 0;
    if (!stringValueControl.U32(stringValuePointer) || !stringValueControl.U32(stringValueCapacity) ||
        !stringValueControl.U32(stringValueIgnoredLength) || !stringValueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(stringValuePointer, stringValueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GfxValueQuery query{};
    std::uint32_t keyInputPointer = 0;
    std::uint32_t keyInputCount = 0;
    if (!reader.U32(keyInputPointer) || !reader.U32(keyInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyInputWire;
    if (!state->memory.View(keyInputPointer, keyInputCount, keyInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader keyInputReader(keyInputWire);
    std::string keyInputStorage;
    { std::uint32_t coreLength = 0; if (!keyInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!keyInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } keyInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.key = keyInputStorage.c_str();
    std::uint32_t modeInputPointer = 0;
    std::uint32_t modeInputCount = 0;
    if (!reader.U32(modeInputPointer) || !reader.U32(modeInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> modeInputWire;
    if (!state->memory.View(modeInputPointer, modeInputCount, modeInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader modeInputReader(modeInputWire);
    std::string modeInputStorage;
    { std::uint32_t coreLength = 0; if (!modeInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!modeInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } modeInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.mode = modeInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxValueResult result{};
    state->native->gfx->GetMapRendering(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t stringValueRequiredSize = result.stringValue == nullptr ? 0u : std::char_traits<char>::length(result.stringValue);
    if (stringValueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t stringValueRequired = static_cast<std::uint32_t>(stringValueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, stringValueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || stringValueCapacity < stringValueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (stringValueRequired != 0 && !state->memory.Write(stringValuePointer, result.stringValue, stringValueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 28u);
    WireWriter writer(fixedWire);
    for (std::size_t coreWriteIndex1 = 0; coreWriteIndex1 < 4u; ++coreWriteIndex1) {
        if (!writer.F32(result.values[coreWriteIndex1])) return Trap("generated Core wire overflow");
    }
    if (!writer.U32(result.count)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.hasBool)) return Trap("generated Core option overflow");
    if (result.hasBool) {
        if (!writer.Bool(result.boolValue)) return Trap("generated Core wire overflow");
    } else {
        std::remove_cv_t<std::remove_reference_t<decltype(result.boolValue)>> coreAbsent{};
        if (!writer.Bool(coreAbsent)) return Trap("generated Core wire overflow");
    }
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_gfx_get_sun(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetSun == nullptr)
        return Trap("GetSun generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetSun generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 16u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 40u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader stringValueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t stringValuePointer = 0;
    std::uint32_t stringValueCapacity = 0;
    std::uint32_t stringValueIgnoredLength = 0;
    if (!stringValueControl.U32(stringValuePointer) || !stringValueControl.U32(stringValueCapacity) ||
        !stringValueControl.U32(stringValueIgnoredLength) || !stringValueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(stringValuePointer, stringValueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GfxValueQuery query{};
    std::uint32_t keyInputPointer = 0;
    std::uint32_t keyInputCount = 0;
    if (!reader.U32(keyInputPointer) || !reader.U32(keyInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyInputWire;
    if (!state->memory.View(keyInputPointer, keyInputCount, keyInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader keyInputReader(keyInputWire);
    std::string keyInputStorage;
    { std::uint32_t coreLength = 0; if (!keyInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!keyInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } keyInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.key = keyInputStorage.c_str();
    std::uint32_t modeInputPointer = 0;
    std::uint32_t modeInputCount = 0;
    if (!reader.U32(modeInputPointer) || !reader.U32(modeInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> modeInputWire;
    if (!state->memory.View(modeInputPointer, modeInputCount, modeInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader modeInputReader(modeInputWire);
    std::string modeInputStorage;
    { std::uint32_t coreLength = 0; if (!modeInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!modeInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } modeInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.mode = modeInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxValueResult result{};
    state->native->gfx->GetSun(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t stringValueRequiredSize = result.stringValue == nullptr ? 0u : std::char_traits<char>::length(result.stringValue);
    if (stringValueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t stringValueRequired = static_cast<std::uint32_t>(stringValueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, stringValueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || stringValueCapacity < stringValueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (stringValueRequired != 0 && !state->memory.Write(stringValuePointer, result.stringValue, stringValueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 28u);
    WireWriter writer(fixedWire);
    for (std::size_t coreWriteIndex1 = 0; coreWriteIndex1 < 4u; ++coreWriteIndex1) {
        if (!writer.F32(result.values[coreWriteIndex1])) return Trap("generated Core wire overflow");
    }
    if (!writer.U32(result.count)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.hasBool)) return Trap("generated Core option overflow");
    if (result.hasBool) {
        if (!writer.Bool(result.boolValue)) return Trap("generated Core wire overflow");
    } else {
        std::remove_cv_t<std::remove_reference_t<decltype(result.boolValue)>> coreAbsent{};
        if (!writer.Bool(coreAbsent)) return Trap("generated Core wire overflow");
    }
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_gfx_get_water_rendering(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetWaterRendering == nullptr)
        return Trap("GetWaterRendering generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetWaterRendering generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 16u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 40u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader stringValueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t stringValuePointer = 0;
    std::uint32_t stringValueCapacity = 0;
    std::uint32_t stringValueIgnoredLength = 0;
    if (!stringValueControl.U32(stringValuePointer) || !stringValueControl.U32(stringValueCapacity) ||
        !stringValueControl.U32(stringValueIgnoredLength) || !stringValueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(stringValuePointer, stringValueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GfxValueQuery query{};
    std::uint32_t keyInputPointer = 0;
    std::uint32_t keyInputCount = 0;
    if (!reader.U32(keyInputPointer) || !reader.U32(keyInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyInputWire;
    if (!state->memory.View(keyInputPointer, keyInputCount, keyInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader keyInputReader(keyInputWire);
    std::string keyInputStorage;
    { std::uint32_t coreLength = 0; if (!keyInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!keyInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } keyInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.key = keyInputStorage.c_str();
    std::uint32_t modeInputPointer = 0;
    std::uint32_t modeInputCount = 0;
    if (!reader.U32(modeInputPointer) || !reader.U32(modeInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> modeInputWire;
    if (!state->memory.View(modeInputPointer, modeInputCount, modeInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader modeInputReader(modeInputWire);
    std::string modeInputStorage;
    { std::uint32_t coreLength = 0; if (!modeInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!modeInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } modeInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.mode = modeInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxValueResult result{};
    state->native->gfx->GetWaterRendering(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t stringValueRequiredSize = result.stringValue == nullptr ? 0u : std::char_traits<char>::length(result.stringValue);
    if (stringValueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t stringValueRequired = static_cast<std::uint32_t>(stringValueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, stringValueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || stringValueCapacity < stringValueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (stringValueRequired != 0 && !state->memory.Write(stringValuePointer, result.stringValue, stringValueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 28u);
    WireWriter writer(fixedWire);
    for (std::size_t coreWriteIndex1 = 0; coreWriteIndex1 < 4u; ++coreWriteIndex1) {
        if (!writer.F32(result.values[coreWriteIndex1])) return Trap("generated Core wire overflow");
    }
    if (!writer.U32(result.count)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.hasBool)) return Trap("generated Core option overflow");
    if (result.hasBool) {
        if (!writer.Bool(result.boolValue)) return Trap("generated Core wire overflow");
    } else {
        std::remove_cv_t<std::remove_reference_t<decltype(result.boolValue)>> coreAbsent{};
        if (!writer.Bool(coreAbsent)) return Trap("generated Core wire overflow");
    }
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_profiling_get_profiler_time_record(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->profiling == nullptr ||
        state->native->profiling->GetProfilerTimeRecord == nullptr)
        return Trap("GetProfilerTimeRecord generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetProfilerTimeRecord generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 32u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader frameDataControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t frameDataPointer = 0;
    std::uint32_t frameDataCapacity = 0;
    std::uint32_t frameDataIgnoredLength = 0;
    if (!frameDataControl.U32(frameDataPointer) || !frameDataControl.U32(frameDataCapacity) ||
        !frameDataControl.U32(frameDataIgnoredLength) || !frameDataControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t frameDataCapacityBytes = static_cast<std::uint64_t>(frameDataCapacity) * 4u;
    if (frameDataCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(frameDataPointer, static_cast<std::size_t>(frameDataCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetProfilerTimeRecordQuery query{};
    std::uint32_t nameInputPointer = 0;
    std::uint32_t nameInputCount = 0;
    if (!reader.U32(nameInputPointer) || !reader.U32(nameInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameInputWire;
    if (!state->memory.View(nameInputPointer, nameInputCount, nameInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader nameInputReader(nameInputWire);
    std::string nameInputStorage;
    { std::uint32_t coreLength = 0; if (!nameInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!nameInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } nameInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.name = nameInputStorage.c_str();
    query.includeFrameData = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetProfilerTimeRecordResult result{};
    state->native->profiling->GetProfilerTimeRecord(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.frameCount) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t frameDataRequired = static_cast<std::uint32_t>(result.frameCount);
    if (frameDataRequired != 0 && result.frameData == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, frameDataRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || frameDataCapacity < frameDataRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (frameDataRequired != 0) {
        const std::size_t frameDataBytes = static_cast<std::size_t>(frameDataRequired) * 4u;
        if constexpr (std::endian::native == std::endian::little) {
            static_assert(sizeof(float) == 4u, "generated Core output/native element width mismatch");
            if (!state->memory.Write(frameDataPointer, result.frameData, frameDataBytes)) return Trap("generated Core variable output range changed unexpectedly");
        } else {
            std::span<std::uint8_t> frameDataWire;
            if (!state->memory.MutableView(frameDataPointer, frameDataBytes, frameDataWire)) return Trap("generated Core variable output range changed unexpectedly");
            WireWriter frameDataWriter(frameDataWire);
            for (std::uint32_t coreIndex = 0; coreIndex < frameDataRequired; ++coreIndex) {
            if (!frameDataWriter.F32(result.frameData[coreIndex])) return Trap("generated Core wire overflow");
            }
            if (!frameDataWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
        }
    }
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 20u);
    WireWriter writer(fixedWire);
    if (!writer.F32(result.totalMs)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.currentMs)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.maxDt)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.timePct)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.peakPct)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_rml_ui_element_get_attribute(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetAttribute == nullptr)
        return Trap("ElementGetAttribute generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ElementGetAttribute generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlElementGetAttributeQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t nameInputPointer = 0;
    std::uint32_t nameInputCount = 0;
    if (!reader.U32(nameInputPointer) || !reader.U32(nameInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameInputWire;
    if (!state->memory.View(nameInputPointer, nameInputCount, nameInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader nameInputReader(nameInputWire);
    std::string nameInputStorage;
    { std::uint32_t coreLength = 0; if (!nameInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!nameInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } nameInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.name = nameInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlElementGetAttributeResult result{};
    state->native->rmlUi->ElementGetAttribute(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_rml_ui_element_get_elements_by_class_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetElementsByClassName == nullptr)
        return Trap("ElementGetElementsByClassName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ElementGetElementsByClassName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader elementHandlesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t elementHandlesPointer = 0;
    std::uint32_t elementHandlesCapacity = 0;
    std::uint32_t elementHandlesIgnoredLength = 0;
    if (!elementHandlesControl.U32(elementHandlesPointer) || !elementHandlesControl.U32(elementHandlesCapacity) ||
        !elementHandlesControl.U32(elementHandlesIgnoredLength) || !elementHandlesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t elementHandlesCapacityBytes = static_cast<std::uint64_t>(elementHandlesCapacity) * 8u;
    if (elementHandlesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(elementHandlesPointer, static_cast<std::size_t>(elementHandlesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valueInputPointer = 0;
    std::uint32_t valueInputCount = 0;
    if (!reader.U32(valueInputPointer) || !reader.U32(valueInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueInputWire;
    if (!state->memory.View(valueInputPointer, valueInputCount, valueInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader valueInputReader(valueInputWire);
    std::string valueInputStorage;
    { std::uint32_t coreLength = 0; if (!valueInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!valueInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } valueInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.value = valueInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlElementHandleListResult result{};
    state->native->rmlUi->ElementGetElementsByClassName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.elementHandleCount) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t elementHandlesRequired = static_cast<std::uint32_t>(result.elementHandleCount);
    if (elementHandlesRequired != 0 && result.elementHandles == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, elementHandlesRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || elementHandlesCapacity < elementHandlesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (elementHandlesRequired != 0) {
        const std::size_t elementHandlesBytes = static_cast<std::size_t>(elementHandlesRequired) * 8u;
        if constexpr (std::endian::native == std::endian::little) {
            static_assert(sizeof(std::uint64_t) == 8u, "generated Core output/native element width mismatch");
            if (!state->memory.Write(elementHandlesPointer, result.elementHandles, elementHandlesBytes)) return Trap("generated Core variable output range changed unexpectedly");
        } else {
            std::span<std::uint8_t> elementHandlesWire;
            if (!state->memory.MutableView(elementHandlesPointer, elementHandlesBytes, elementHandlesWire)) return Trap("generated Core variable output range changed unexpectedly");
            WireWriter elementHandlesWriter(elementHandlesWire);
            for (std::uint32_t coreIndex = 0; coreIndex < elementHandlesRequired; ++coreIndex) {
            if (!elementHandlesWriter.U64(result.elementHandles[coreIndex])) return Trap("generated Core wire overflow");
            }
            if (!elementHandlesWriter.Finish(8u)) return Trap("generated Core list output layout mismatch");
        }
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_rml_ui_element_get_elements_by_tag_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetElementsByTagName == nullptr)
        return Trap("ElementGetElementsByTagName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ElementGetElementsByTagName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader elementHandlesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t elementHandlesPointer = 0;
    std::uint32_t elementHandlesCapacity = 0;
    std::uint32_t elementHandlesIgnoredLength = 0;
    if (!elementHandlesControl.U32(elementHandlesPointer) || !elementHandlesControl.U32(elementHandlesCapacity) ||
        !elementHandlesControl.U32(elementHandlesIgnoredLength) || !elementHandlesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t elementHandlesCapacityBytes = static_cast<std::uint64_t>(elementHandlesCapacity) * 8u;
    if (elementHandlesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(elementHandlesPointer, static_cast<std::size_t>(elementHandlesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valueInputPointer = 0;
    std::uint32_t valueInputCount = 0;
    if (!reader.U32(valueInputPointer) || !reader.U32(valueInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueInputWire;
    if (!state->memory.View(valueInputPointer, valueInputCount, valueInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader valueInputReader(valueInputWire);
    std::string valueInputStorage;
    { std::uint32_t coreLength = 0; if (!valueInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!valueInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } valueInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.value = valueInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlElementHandleListResult result{};
    state->native->rmlUi->ElementGetElementsByTagName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.elementHandleCount) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t elementHandlesRequired = static_cast<std::uint32_t>(result.elementHandleCount);
    if (elementHandlesRequired != 0 && result.elementHandles == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, elementHandlesRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || elementHandlesCapacity < elementHandlesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (elementHandlesRequired != 0) {
        const std::size_t elementHandlesBytes = static_cast<std::size_t>(elementHandlesRequired) * 8u;
        if constexpr (std::endian::native == std::endian::little) {
            static_assert(sizeof(std::uint64_t) == 8u, "generated Core output/native element width mismatch");
            if (!state->memory.Write(elementHandlesPointer, result.elementHandles, elementHandlesBytes)) return Trap("generated Core variable output range changed unexpectedly");
        } else {
            std::span<std::uint8_t> elementHandlesWire;
            if (!state->memory.MutableView(elementHandlesPointer, elementHandlesBytes, elementHandlesWire)) return Trap("generated Core variable output range changed unexpectedly");
            WireWriter elementHandlesWriter(elementHandlesWire);
            for (std::uint32_t coreIndex = 0; coreIndex < elementHandlesRequired; ++coreIndex) {
            if (!elementHandlesWriter.U64(result.elementHandles[coreIndex])) return Trap("generated Core wire overflow");
            }
            if (!elementHandlesWriter.Finish(8u)) return Trap("generated Core list output layout mismatch");
        }
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_rml_ui_element_query_selector_all(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementQuerySelectorAll == nullptr)
        return Trap("ElementQuerySelectorAll generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ElementQuerySelectorAll generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader elementHandlesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t elementHandlesPointer = 0;
    std::uint32_t elementHandlesCapacity = 0;
    std::uint32_t elementHandlesIgnoredLength = 0;
    if (!elementHandlesControl.U32(elementHandlesPointer) || !elementHandlesControl.U32(elementHandlesCapacity) ||
        !elementHandlesControl.U32(elementHandlesIgnoredLength) || !elementHandlesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t elementHandlesCapacityBytes = static_cast<std::uint64_t>(elementHandlesCapacity) * 8u;
    if (elementHandlesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(elementHandlesPointer, static_cast<std::size_t>(elementHandlesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valueInputPointer = 0;
    std::uint32_t valueInputCount = 0;
    if (!reader.U32(valueInputPointer) || !reader.U32(valueInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueInputWire;
    if (!state->memory.View(valueInputPointer, valueInputCount, valueInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader valueInputReader(valueInputWire);
    std::string valueInputStorage;
    { std::uint32_t coreLength = 0; if (!valueInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!valueInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } valueInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.value = valueInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlElementHandleListResult result{};
    state->native->rmlUi->ElementQuerySelectorAll(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.elementHandleCount) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t elementHandlesRequired = static_cast<std::uint32_t>(result.elementHandleCount);
    if (elementHandlesRequired != 0 && result.elementHandles == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, elementHandlesRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || elementHandlesCapacity < elementHandlesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (elementHandlesRequired != 0) {
        const std::size_t elementHandlesBytes = static_cast<std::size_t>(elementHandlesRequired) * 8u;
        if constexpr (std::endian::native == std::endian::little) {
            static_assert(sizeof(std::uint64_t) == 8u, "generated Core output/native element width mismatch");
            if (!state->memory.Write(elementHandlesPointer, result.elementHandles, elementHandlesBytes)) return Trap("generated Core variable output range changed unexpectedly");
        } else {
            std::span<std::uint8_t> elementHandlesWire;
            if (!state->memory.MutableView(elementHandlesPointer, elementHandlesBytes, elementHandlesWire)) return Trap("generated Core variable output range changed unexpectedly");
            WireWriter elementHandlesWriter(elementHandlesWire);
            for (std::uint32_t coreIndex = 0; coreIndex < elementHandlesRequired; ++coreIndex) {
            if (!elementHandlesWriter.U64(result.elementHandles[coreIndex])) return Trap("generated Core wire overflow");
            }
            if (!elementHandlesWriter.Finish(8u)) return Trap("generated Core list output layout mismatch");
        }
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_rml_ui_event_get_parameter_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->EventGetParameterString == nullptr)
        return Trap("EventGetParameterString generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("EventGetParameterString generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valueControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCapacity = 0;
    std::uint32_t valueIgnoredLength = 0;
    if (!valueControl.U32(valuePointer) || !valueControl.U32(valueCapacity) ||
        !valueControl.U32(valueIgnoredLength) || !valueControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(valuePointer, valueCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    RmlEventParameterQuery query{};
    query.eventHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventHandle)>>>(slots[0].i64);
    std::uint32_t nameInputPointer = 0;
    std::uint32_t nameInputCount = 0;
    if (!reader.U32(nameInputPointer) || !reader.U32(nameInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameInputWire;
    if (!state->memory.View(nameInputPointer, nameInputCount, nameInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader nameInputReader(nameInputWire);
    std::string nameInputStorage;
    { std::uint32_t coreLength = 0; if (!nameInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!nameInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } nameInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.name = nameInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlEventGetStringResult result{};
    state->native->rmlUi->EventGetParameterString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t valueRequiredSize = result.value == nullptr ? 0u : std::char_traits<char>::length(result.value);
    if (valueRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valueRequired = static_cast<std::uint32_t>(valueRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, valueRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valueCapacity < valueRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valueRequired != 0 && !state->memory.Write(valuePointer, result.value, valueRequired)) return Trap("generated Core variable output range changed unexpectedly");
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_calculate_hash(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->CalculateHash == nullptr)
        return Trap("CalculateHash generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("CalculateHash generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader hashControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t hashPointer = 0;
    std::uint32_t hashCapacity = 0;
    std::uint32_t hashIgnoredLength = 0;
    if (!hashControl.U32(hashPointer) || !hashControl.U32(hashCapacity) ||
        !hashControl.U32(hashIgnoredLength) || !hashControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(hashPointer, hashCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    CalculateHashQuery query{};
    std::uint32_t dataInputPointer = 0;
    std::uint32_t dataInputCount = 0;
    if (!reader.U32(dataInputPointer) || !reader.U32(dataInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataInputBytes64 = static_cast<std::uint64_t>(dataInputCount) * 4u;
    if (dataInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> dataInputWire;
    if (!state->memory.View(dataInputPointer, static_cast<std::size_t>(dataInputBytes64), dataInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader dataInputReader(dataInputWire);
    std::vector<std::uint8_t> dataInputStorage;
    dataInputStorage.reserve(dataInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < dataInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!dataInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        dataInputStorage.push_back(item);
    }
    if (!dataInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.data = dataInputStorage.data();
    if (!AssignCoreCount(dataInputCount, query.dataSize)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.hashType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.hashType)>>>(slots[0].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    CalculateHashResult result{};
    state->native->vfs->CalculateHash(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t hashRequiredSize = result.hash == nullptr ? 0u : std::char_traits<char>::length(result.hash);
    if (hashRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t hashRequired = static_cast<std::uint32_t>(hashRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, hashRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || hashCapacity < hashRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (hashRequired != 0 && !state->memory.Write(hashPointer, result.hash, hashRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_get_archive_checksum(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->GetArchiveChecksum == nullptr)
        return Trap("GetArchiveChecksum generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetArchiveChecksum generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 24u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader singleChecksumControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t singleChecksumPointer = 0;
    std::uint32_t singleChecksumCapacity = 0;
    std::uint32_t singleChecksumIgnoredLength = 0;
    if (!singleChecksumControl.U32(singleChecksumPointer) || !singleChecksumControl.U32(singleChecksumCapacity) ||
        !singleChecksumControl.U32(singleChecksumIgnoredLength) || !singleChecksumControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(singleChecksumPointer, singleChecksumCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader completeChecksumControl(std::span<const std::uint8_t>(outputWire.data() + 12u, 12));
    std::uint32_t completeChecksumPointer = 0;
    std::uint32_t completeChecksumCapacity = 0;
    std::uint32_t completeChecksumIgnoredLength = 0;
    if (!completeChecksumControl.U32(completeChecksumPointer) || !completeChecksumControl.U32(completeChecksumCapacity) ||
        !completeChecksumControl.U32(completeChecksumIgnoredLength) || !completeChecksumControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(completeChecksumPointer, completeChecksumCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetArchiveChecksumQuery query{};
    std::uint32_t archiveNameInputPointer = 0;
    std::uint32_t archiveNameInputCount = 0;
    if (!reader.U32(archiveNameInputPointer) || !reader.U32(archiveNameInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> archiveNameInputWire;
    if (!state->memory.View(archiveNameInputPointer, archiveNameInputCount, archiveNameInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader archiveNameInputReader(archiveNameInputWire);
    std::string archiveNameInputStorage;
    { std::uint32_t coreLength = 0; if (!archiveNameInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!archiveNameInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } archiveNameInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.archiveName = archiveNameInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetArchiveChecksumResult result{};
    state->native->vfs->GetArchiveChecksum(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t singleChecksumRequiredSize = result.singleChecksum == nullptr ? 0u : std::char_traits<char>::length(result.singleChecksum);
    if (singleChecksumRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t singleChecksumRequired = static_cast<std::uint32_t>(singleChecksumRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, singleChecksumRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || singleChecksumCapacity < singleChecksumRequired;
    const std::size_t completeChecksumRequiredSize = result.completeChecksum == nullptr ? 0u : std::char_traits<char>::length(result.completeChecksum);
    if (completeChecksumRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t completeChecksumRequired = static_cast<std::uint32_t>(completeChecksumRequiredSize);
    if (!WriteCoreU32(outputWire, 20u, completeChecksumRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || completeChecksumCapacity < completeChecksumRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (singleChecksumRequired != 0 && !state->memory.Write(singleChecksumPointer, result.singleChecksum, singleChecksumRequired)) return Trap("generated Core variable output range changed unexpectedly");
    if (completeChecksumRequired != 0 && !state->memory.Write(completeChecksumPointer, result.completeChecksum, completeChecksumRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_get_archive_containing_file(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->GetArchiveContainingFile == nullptr)
        return Trap("GetArchiveContainingFile generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetArchiveContainingFile generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 16u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader archiveNameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t archiveNamePointer = 0;
    std::uint32_t archiveNameCapacity = 0;
    std::uint32_t archiveNameIgnoredLength = 0;
    if (!archiveNameControl.U32(archiveNamePointer) || !archiveNameControl.U32(archiveNameCapacity) ||
        !archiveNameControl.U32(archiveNameIgnoredLength) || !archiveNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(archiveNamePointer, archiveNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetArchiveContainingFileQuery query{};
    std::uint32_t pathInputPointer = 0;
    std::uint32_t pathInputCount = 0;
    if (!reader.U32(pathInputPointer) || !reader.U32(pathInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> pathInputWire;
    if (!state->memory.View(pathInputPointer, pathInputCount, pathInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader pathInputReader(pathInputWire);
    std::string pathInputStorage;
    { std::uint32_t coreLength = 0; if (!pathInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!pathInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } pathInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.path = pathInputStorage.c_str();
    std::uint32_t modeInputPointer = 0;
    std::uint32_t modeInputCount = 0;
    if (!reader.U32(modeInputPointer) || !reader.U32(modeInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> modeInputWire;
    if (!state->memory.View(modeInputPointer, modeInputCount, modeInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader modeInputReader(modeInputWire);
    std::string modeInputStorage;
    { std::uint32_t coreLength = 0; if (!modeInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!modeInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } modeInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.mode = modeInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetArchiveContainingFileResult result{};
    state->native->vfs->GetArchiveContainingFile(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t archiveNameRequiredSize = result.archiveName == nullptr ? 0u : std::char_traits<char>::length(result.archiveName);
    if (archiveNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t archiveNameRequired = static_cast<std::uint32_t>(archiveNameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, archiveNameRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || archiveNameCapacity < archiveNameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (archiveNameRequired != 0 && !state->memory.Write(archiveNamePointer, result.archiveName, archiveNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_get_archive_path(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->GetArchivePath == nullptr)
        return Trap("GetArchivePath generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetArchivePath generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader pathControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCapacity = 0;
    std::uint32_t pathIgnoredLength = 0;
    if (!pathControl.U32(pathPointer) || !pathControl.U32(pathCapacity) ||
        !pathControl.U32(pathIgnoredLength) || !pathControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(pathPointer, pathCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetArchivePathQuery query{};
    std::uint32_t archiveNameInputPointer = 0;
    std::uint32_t archiveNameInputCount = 0;
    if (!reader.U32(archiveNameInputPointer) || !reader.U32(archiveNameInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> archiveNameInputWire;
    if (!state->memory.View(archiveNameInputPointer, archiveNameInputCount, archiveNameInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader archiveNameInputReader(archiveNameInputWire);
    std::string archiveNameInputStorage;
    { std::uint32_t coreLength = 0; if (!archiveNameInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!archiveNameInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } archiveNameInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.archiveName = archiveNameInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetArchivePathResult result{};
    state->native->vfs->GetArchivePath(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t pathRequiredSize = result.path == nullptr ? 0u : std::char_traits<char>::length(result.path);
    if (pathRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t pathRequired = static_cast<std::uint32_t>(pathRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, pathRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || pathCapacity < pathRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (pathRequired != 0 && !state->memory.Write(pathPointer, result.path, pathRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_get_file_absolute_path(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->GetFileAbsolutePath == nullptr)
        return Trap("GetFileAbsolutePath generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetFileAbsolutePath generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 16u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader pathControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCapacity = 0;
    std::uint32_t pathIgnoredLength = 0;
    if (!pathControl.U32(pathPointer) || !pathControl.U32(pathCapacity) ||
        !pathControl.U32(pathIgnoredLength) || !pathControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(pathPointer, pathCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetFileAbsolutePathQuery query{};
    std::uint32_t pathInputPointer = 0;
    std::uint32_t pathInputCount = 0;
    if (!reader.U32(pathInputPointer) || !reader.U32(pathInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> pathInputWire;
    if (!state->memory.View(pathInputPointer, pathInputCount, pathInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader pathInputReader(pathInputWire);
    std::string pathInputStorage;
    { std::uint32_t coreLength = 0; if (!pathInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!pathInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } pathInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.path = pathInputStorage.c_str();
    std::uint32_t modeInputPointer = 0;
    std::uint32_t modeInputCount = 0;
    if (!reader.U32(modeInputPointer) || !reader.U32(modeInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> modeInputWire;
    if (!state->memory.View(modeInputPointer, modeInputCount, modeInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader modeInputReader(modeInputWire);
    std::string modeInputStorage;
    { std::uint32_t coreLength = 0; if (!modeInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!modeInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } modeInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.mode = modeInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetFileAbsolutePathResult result{};
    state->native->vfs->GetFileAbsolutePath(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t pathRequiredSize = result.path == nullptr ? 0u : std::char_traits<char>::length(result.path);
    if (pathRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t pathRequired = static_cast<std::uint32_t>(pathRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, pathRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || pathCapacity < pathRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (pathRequired != 0 && !state->memory.Write(pathPointer, result.path, pathRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_get_name_from_rapid_tag(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->GetNameFromRapidTag == nullptr)
        return Trap("GetNameFromRapidTag generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetNameFromRapidTag generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader archiveNameControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t archiveNamePointer = 0;
    std::uint32_t archiveNameCapacity = 0;
    std::uint32_t archiveNameIgnoredLength = 0;
    if (!archiveNameControl.U32(archiveNamePointer) || !archiveNameControl.U32(archiveNameCapacity) ||
        !archiveNameControl.U32(archiveNameIgnoredLength) || !archiveNameControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(archiveNamePointer, archiveNameCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    GetNameFromRapidTagQuery query{};
    std::uint32_t rapidTagInputPointer = 0;
    std::uint32_t rapidTagInputCount = 0;
    if (!reader.U32(rapidTagInputPointer) || !reader.U32(rapidTagInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> rapidTagInputWire;
    if (!state->memory.View(rapidTagInputPointer, rapidTagInputCount, rapidTagInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader rapidTagInputReader(rapidTagInputWire);
    std::string rapidTagInputStorage;
    { std::uint32_t coreLength = 0; if (!rapidTagInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!rapidTagInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } rapidTagInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.rapidTag = rapidTagInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetNameFromRapidTagResult result{};
    state->native->vfs->GetNameFromRapidTag(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t archiveNameRequiredSize = result.archiveName == nullptr ? 0u : std::char_traits<char>::length(result.archiveName);
    if (archiveNameRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t archiveNameRequired = static_cast<std::uint32_t>(archiveNameRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, archiveNameRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || archiveNameCapacity < archiveNameRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (archiveNameRequired != 0 && !state->memory.Write(archiveNamePointer, result.archiveName, archiveNameRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_load_file(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->LoadFile == nullptr)
        return Trap("LoadFile generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("LoadFile generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 16u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader dataControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCapacity = 0;
    std::uint32_t dataIgnoredLength = 0;
    if (!dataControl.U32(dataPointer) || !dataControl.U32(dataCapacity) ||
        !dataControl.U32(dataIgnoredLength) || !dataControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataCapacityBytes = static_cast<std::uint64_t>(dataCapacity) * 4u;
    if (dataCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(dataPointer, static_cast<std::size_t>(dataCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    LoadFileQuery query{};
    std::uint32_t pathInputPointer = 0;
    std::uint32_t pathInputCount = 0;
    if (!reader.U32(pathInputPointer) || !reader.U32(pathInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> pathInputWire;
    if (!state->memory.View(pathInputPointer, pathInputCount, pathInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader pathInputReader(pathInputWire);
    std::string pathInputStorage;
    { std::uint32_t coreLength = 0; if (!pathInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!pathInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } pathInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.path = pathInputStorage.c_str();
    std::uint32_t modeInputPointer = 0;
    std::uint32_t modeInputCount = 0;
    if (!reader.U32(modeInputPointer) || !reader.U32(modeInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> modeInputWire;
    if (!state->memory.View(modeInputPointer, modeInputCount, modeInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader modeInputReader(modeInputWire);
    std::string modeInputStorage;
    { std::uint32_t coreLength = 0; if (!modeInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!modeInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } modeInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.mode = modeInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    LoadFileResult result{};
    state->native->vfs->LoadFile(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.size) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t dataRequired = static_cast<std::uint32_t>(result.size);
    if (dataRequired != 0 && result.data == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, dataRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || dataCapacity < dataRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (dataRequired != 0) {
        const std::size_t dataBytes = static_cast<std::size_t>(dataRequired) * 4u;
        std::span<std::uint8_t> dataWire;
        if (!state->memory.MutableView(dataPointer, dataBytes, dataWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter dataWriter(dataWire);
        for (std::uint32_t coreIndex = 0; coreIndex < dataRequired; ++coreIndex) {
        if (!dataWriter.U32(result.data[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!dataWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_pack_f32(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->PackF32 == nullptr)
        return Trap("PackF32 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("PackF32 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader dataControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCapacity = 0;
    std::uint32_t dataIgnoredLength = 0;
    if (!dataControl.U32(dataPointer) || !dataControl.U32(dataCapacity) ||
        !dataControl.U32(dataIgnoredLength) || !dataControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataCapacityBytes = static_cast<std::uint64_t>(dataCapacity) * 4u;
    if (dataCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(dataPointer, static_cast<std::size_t>(dataCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    PackF32Query query{};
    std::uint32_t valuesInputPointer = 0;
    std::uint32_t valuesInputCount = 0;
    if (!reader.U32(valuesInputPointer) || !reader.U32(valuesInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (valuesInputCount == 0) {
        query.values = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) { slots[0].i32 = static_cast<std::int32_t>(Status::NotAvailable); return nullptr; }
        const std::uint64_t valuesInputBytes64 = static_cast<std::uint64_t>(valuesInputCount) * 4u;
        if (valuesInputBytes64 > std::numeric_limits<std::size_t>::max() || (valuesInputPointer % 4u) != 0u) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
        std::span<const std::uint8_t> valuesInputBytes;
        if (!state->memory.View(valuesInputPointer, static_cast<std::size_t>(valuesInputBytes64), valuesInputBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
        static_assert(sizeof(float) == 4u, "generated Core borrowed/native element width mismatch");
        query.values = reinterpret_cast<std::remove_reference_t<decltype(query.values)>>(valuesInputBytes.data());
    }
    if (!AssignCoreCount(valuesInputCount, query.count)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    PackF32Result result{};
    state->native->vfs->PackF32(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.size) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t dataRequired = static_cast<std::uint32_t>(result.size);
    if (dataRequired != 0 && result.data == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, dataRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || dataCapacity < dataRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (dataRequired != 0) {
        const std::size_t dataBytes = static_cast<std::size_t>(dataRequired) * 4u;
        std::span<std::uint8_t> dataWire;
        if (!state->memory.MutableView(dataPointer, dataBytes, dataWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter dataWriter(dataWire);
        for (std::uint32_t coreIndex = 0; coreIndex < dataRequired; ++coreIndex) {
        if (!dataWriter.U32(result.data[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!dataWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_pack_s16(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->PackS16 == nullptr)
        return Trap("PackS16 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("PackS16 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader dataControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCapacity = 0;
    std::uint32_t dataIgnoredLength = 0;
    if (!dataControl.U32(dataPointer) || !dataControl.U32(dataCapacity) ||
        !dataControl.U32(dataIgnoredLength) || !dataControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataCapacityBytes = static_cast<std::uint64_t>(dataCapacity) * 4u;
    if (dataCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(dataPointer, static_cast<std::size_t>(dataCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    PackS16Query query{};
    std::uint32_t valuesInputPointer = 0;
    std::uint32_t valuesInputCount = 0;
    if (!reader.U32(valuesInputPointer) || !reader.U32(valuesInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesInputBytes64 = static_cast<std::uint64_t>(valuesInputCount) * 4u;
    if (valuesInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> valuesInputWire;
    if (!state->memory.View(valuesInputPointer, static_cast<std::size_t>(valuesInputBytes64), valuesInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader valuesInputReader(valuesInputWire);
    std::vector<std::int16_t> valuesInputStorage;
    valuesInputStorage.reserve(valuesInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < valuesInputCount; ++coreIndex) {
        std::int16_t item{};
        { std::int32_t coreRaw = 0; if (!valuesInputReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        valuesInputStorage.push_back(item);
    }
    if (!valuesInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.values = valuesInputStorage.data();
    if (!AssignCoreCount(valuesInputCount, query.count)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    PackS16Result result{};
    state->native->vfs->PackS16(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.size) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t dataRequired = static_cast<std::uint32_t>(result.size);
    if (dataRequired != 0 && result.data == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, dataRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || dataCapacity < dataRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (dataRequired != 0) {
        const std::size_t dataBytes = static_cast<std::size_t>(dataRequired) * 4u;
        std::span<std::uint8_t> dataWire;
        if (!state->memory.MutableView(dataPointer, dataBytes, dataWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter dataWriter(dataWire);
        for (std::uint32_t coreIndex = 0; coreIndex < dataRequired; ++coreIndex) {
        if (!dataWriter.U32(result.data[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!dataWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_pack_s32(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->PackS32 == nullptr)
        return Trap("PackS32 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("PackS32 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader dataControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCapacity = 0;
    std::uint32_t dataIgnoredLength = 0;
    if (!dataControl.U32(dataPointer) || !dataControl.U32(dataCapacity) ||
        !dataControl.U32(dataIgnoredLength) || !dataControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataCapacityBytes = static_cast<std::uint64_t>(dataCapacity) * 4u;
    if (dataCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(dataPointer, static_cast<std::size_t>(dataCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    PackS32Query query{};
    std::uint32_t valuesInputPointer = 0;
    std::uint32_t valuesInputCount = 0;
    if (!reader.U32(valuesInputPointer) || !reader.U32(valuesInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (valuesInputCount == 0) {
        query.values = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) { slots[0].i32 = static_cast<std::int32_t>(Status::NotAvailable); return nullptr; }
        const std::uint64_t valuesInputBytes64 = static_cast<std::uint64_t>(valuesInputCount) * 4u;
        if (valuesInputBytes64 > std::numeric_limits<std::size_t>::max() || (valuesInputPointer % 4u) != 0u) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
        std::span<const std::uint8_t> valuesInputBytes;
        if (!state->memory.View(valuesInputPointer, static_cast<std::size_t>(valuesInputBytes64), valuesInputBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
        static_assert(sizeof(std::int32_t) == 4u, "generated Core borrowed/native element width mismatch");
        query.values = reinterpret_cast<std::remove_reference_t<decltype(query.values)>>(valuesInputBytes.data());
    }
    if (!AssignCoreCount(valuesInputCount, query.count)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    PackS32Result result{};
    state->native->vfs->PackS32(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.size) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t dataRequired = static_cast<std::uint32_t>(result.size);
    if (dataRequired != 0 && result.data == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, dataRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || dataCapacity < dataRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (dataRequired != 0) {
        const std::size_t dataBytes = static_cast<std::size_t>(dataRequired) * 4u;
        std::span<std::uint8_t> dataWire;
        if (!state->memory.MutableView(dataPointer, dataBytes, dataWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter dataWriter(dataWire);
        for (std::uint32_t coreIndex = 0; coreIndex < dataRequired; ++coreIndex) {
        if (!dataWriter.U32(result.data[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!dataWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_pack_s8(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->PackS8 == nullptr)
        return Trap("PackS8 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("PackS8 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader dataControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCapacity = 0;
    std::uint32_t dataIgnoredLength = 0;
    if (!dataControl.U32(dataPointer) || !dataControl.U32(dataCapacity) ||
        !dataControl.U32(dataIgnoredLength) || !dataControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataCapacityBytes = static_cast<std::uint64_t>(dataCapacity) * 4u;
    if (dataCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(dataPointer, static_cast<std::size_t>(dataCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    PackS8Query query{};
    std::uint32_t valuesInputPointer = 0;
    std::uint32_t valuesInputCount = 0;
    if (!reader.U32(valuesInputPointer) || !reader.U32(valuesInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesInputBytes64 = static_cast<std::uint64_t>(valuesInputCount) * 4u;
    if (valuesInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> valuesInputWire;
    if (!state->memory.View(valuesInputPointer, static_cast<std::size_t>(valuesInputBytes64), valuesInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader valuesInputReader(valuesInputWire);
    std::vector<std::int8_t> valuesInputStorage;
    valuesInputStorage.reserve(valuesInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < valuesInputCount; ++coreIndex) {
        std::int8_t item{};
        { std::int32_t coreRaw = 0; if (!valuesInputReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        valuesInputStorage.push_back(item);
    }
    if (!valuesInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.values = valuesInputStorage.data();
    if (!AssignCoreCount(valuesInputCount, query.count)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    PackS8Result result{};
    state->native->vfs->PackS8(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.size) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t dataRequired = static_cast<std::uint32_t>(result.size);
    if (dataRequired != 0 && result.data == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, dataRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || dataCapacity < dataRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (dataRequired != 0) {
        const std::size_t dataBytes = static_cast<std::size_t>(dataRequired) * 4u;
        std::span<std::uint8_t> dataWire;
        if (!state->memory.MutableView(dataPointer, dataBytes, dataWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter dataWriter(dataWire);
        for (std::uint32_t coreIndex = 0; coreIndex < dataRequired; ++coreIndex) {
        if (!dataWriter.U32(result.data[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!dataWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_pack_u16(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->PackU16 == nullptr)
        return Trap("PackU16 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("PackU16 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader dataControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCapacity = 0;
    std::uint32_t dataIgnoredLength = 0;
    if (!dataControl.U32(dataPointer) || !dataControl.U32(dataCapacity) ||
        !dataControl.U32(dataIgnoredLength) || !dataControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataCapacityBytes = static_cast<std::uint64_t>(dataCapacity) * 4u;
    if (dataCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(dataPointer, static_cast<std::size_t>(dataCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    PackU16Query query{};
    std::uint32_t valuesInputPointer = 0;
    std::uint32_t valuesInputCount = 0;
    if (!reader.U32(valuesInputPointer) || !reader.U32(valuesInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesInputBytes64 = static_cast<std::uint64_t>(valuesInputCount) * 4u;
    if (valuesInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> valuesInputWire;
    if (!state->memory.View(valuesInputPointer, static_cast<std::size_t>(valuesInputBytes64), valuesInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader valuesInputReader(valuesInputWire);
    std::vector<std::uint16_t> valuesInputStorage;
    valuesInputStorage.reserve(valuesInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < valuesInputCount; ++coreIndex) {
        std::uint16_t item{};
        { std::uint32_t coreRaw = 0; if (!valuesInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        valuesInputStorage.push_back(item);
    }
    if (!valuesInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.values = valuesInputStorage.data();
    if (!AssignCoreCount(valuesInputCount, query.count)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    PackU16Result result{};
    state->native->vfs->PackU16(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.size) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t dataRequired = static_cast<std::uint32_t>(result.size);
    if (dataRequired != 0 && result.data == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, dataRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || dataCapacity < dataRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (dataRequired != 0) {
        const std::size_t dataBytes = static_cast<std::size_t>(dataRequired) * 4u;
        std::span<std::uint8_t> dataWire;
        if (!state->memory.MutableView(dataPointer, dataBytes, dataWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter dataWriter(dataWire);
        for (std::uint32_t coreIndex = 0; coreIndex < dataRequired; ++coreIndex) {
        if (!dataWriter.U32(result.data[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!dataWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_pack_u32(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->PackU32 == nullptr)
        return Trap("PackU32 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("PackU32 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader dataControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCapacity = 0;
    std::uint32_t dataIgnoredLength = 0;
    if (!dataControl.U32(dataPointer) || !dataControl.U32(dataCapacity) ||
        !dataControl.U32(dataIgnoredLength) || !dataControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataCapacityBytes = static_cast<std::uint64_t>(dataCapacity) * 4u;
    if (dataCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(dataPointer, static_cast<std::size_t>(dataCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    PackU32Query query{};
    std::uint32_t valuesInputPointer = 0;
    std::uint32_t valuesInputCount = 0;
    if (!reader.U32(valuesInputPointer) || !reader.U32(valuesInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (valuesInputCount == 0) {
        query.values = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) { slots[0].i32 = static_cast<std::int32_t>(Status::NotAvailable); return nullptr; }
        const std::uint64_t valuesInputBytes64 = static_cast<std::uint64_t>(valuesInputCount) * 4u;
        if (valuesInputBytes64 > std::numeric_limits<std::size_t>::max() || (valuesInputPointer % 4u) != 0u) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
        std::span<const std::uint8_t> valuesInputBytes;
        if (!state->memory.View(valuesInputPointer, static_cast<std::size_t>(valuesInputBytes64), valuesInputBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
        static_assert(sizeof(std::uint32_t) == 4u, "generated Core borrowed/native element width mismatch");
        query.values = reinterpret_cast<std::remove_reference_t<decltype(query.values)>>(valuesInputBytes.data());
    }
    if (!AssignCoreCount(valuesInputCount, query.count)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    PackU32Result result{};
    state->native->vfs->PackU32(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.size) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t dataRequired = static_cast<std::uint32_t>(result.size);
    if (dataRequired != 0 && result.data == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, dataRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || dataCapacity < dataRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (dataRequired != 0) {
        const std::size_t dataBytes = static_cast<std::size_t>(dataRequired) * 4u;
        std::span<std::uint8_t> dataWire;
        if (!state->memory.MutableView(dataPointer, dataBytes, dataWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter dataWriter(dataWire);
        for (std::uint32_t coreIndex = 0; coreIndex < dataRequired; ++coreIndex) {
        if (!dataWriter.U32(result.data[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!dataWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_pack_u8(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->PackU8 == nullptr)
        return Trap("PackU8 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("PackU8 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader dataControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCapacity = 0;
    std::uint32_t dataIgnoredLength = 0;
    if (!dataControl.U32(dataPointer) || !dataControl.U32(dataCapacity) ||
        !dataControl.U32(dataIgnoredLength) || !dataControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataCapacityBytes = static_cast<std::uint64_t>(dataCapacity) * 4u;
    if (dataCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(dataPointer, static_cast<std::size_t>(dataCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    PackU8Query query{};
    std::uint32_t valuesInputPointer = 0;
    std::uint32_t valuesInputCount = 0;
    if (!reader.U32(valuesInputPointer) || !reader.U32(valuesInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesInputBytes64 = static_cast<std::uint64_t>(valuesInputCount) * 4u;
    if (valuesInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> valuesInputWire;
    if (!state->memory.View(valuesInputPointer, static_cast<std::size_t>(valuesInputBytes64), valuesInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader valuesInputReader(valuesInputWire);
    std::vector<std::uint8_t> valuesInputStorage;
    valuesInputStorage.reserve(valuesInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < valuesInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!valuesInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        valuesInputStorage.push_back(item);
    }
    if (!valuesInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.values = valuesInputStorage.data();
    if (!AssignCoreCount(valuesInputCount, query.count)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    PackU8Result result{};
    state->native->vfs->PackU8(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.size) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t dataRequired = static_cast<std::uint32_t>(result.size);
    if (dataRequired != 0 && result.data == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, dataRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || dataCapacity < dataRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (dataRequired != 0) {
        const std::size_t dataBytes = static_cast<std::size_t>(dataRequired) * 4u;
        std::span<std::uint8_t> dataWire;
        if (!state->memory.MutableView(dataPointer, dataBytes, dataWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter dataWriter(dataWire);
        for (std::uint32_t coreIndex = 0; coreIndex < dataRequired; ++coreIndex) {
        if (!dataWriter.U32(result.data[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!dataWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_read_file(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->ReadFile == nullptr)
        return Trap("ReadFile generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ReadFile generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader dataControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCapacity = 0;
    std::uint32_t dataIgnoredLength = 0;
    if (!dataControl.U32(dataPointer) || !dataControl.U32(dataCapacity) ||
        !dataControl.U32(dataIgnoredLength) || !dataControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataCapacityBytes = static_cast<std::uint64_t>(dataCapacity) * 4u;
    if (dataCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(dataPointer, static_cast<std::size_t>(dataCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    ReadFileQuery query{};
    std::uint32_t pathInputPointer = 0;
    std::uint32_t pathInputCount = 0;
    if (!reader.U32(pathInputPointer) || !reader.U32(pathInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> pathInputWire;
    if (!state->memory.View(pathInputPointer, pathInputCount, pathInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader pathInputReader(pathInputWire);
    std::string pathInputStorage;
    { std::uint32_t coreLength = 0; if (!pathInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!pathInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } pathInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.path = pathInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    ReadFileResult result{};
    state->native->vfs->ReadFile(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.size) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t dataRequired = static_cast<std::uint32_t>(result.size);
    if (dataRequired != 0 && result.data == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, dataRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || dataCapacity < dataRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (dataRequired != 0) {
        const std::size_t dataBytes = static_cast<std::size_t>(dataRequired) * 4u;
        std::span<std::uint8_t> dataWire;
        if (!state->memory.MutableView(dataPointer, dataBytes, dataWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter dataWriter(dataWire);
        for (std::uint32_t coreIndex = 0; coreIndex < dataRequired; ++coreIndex) {
        if (!dataWriter.U32(result.data[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!dataWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_read_file_as_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->ReadFileAsString == nullptr)
        return Trap("ReadFileAsString generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ReadFileAsString generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader contentControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t contentPointer = 0;
    std::uint32_t contentCapacity = 0;
    std::uint32_t contentIgnoredLength = 0;
    if (!contentControl.U32(contentPointer) || !contentControl.U32(contentCapacity) ||
        !contentControl.U32(contentIgnoredLength) || !contentControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!state->memory.Contains(contentPointer, contentCapacity)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    ReadFileAsStringQuery query{};
    std::uint32_t pathInputPointer = 0;
    std::uint32_t pathInputCount = 0;
    if (!reader.U32(pathInputPointer) || !reader.U32(pathInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> pathInputWire;
    if (!state->memory.View(pathInputPointer, pathInputCount, pathInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader pathInputReader(pathInputWire);
    std::string pathInputStorage;
    { std::uint32_t coreLength = 0; if (!pathInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!pathInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } pathInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.path = pathInputStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    ReadFileAsStringResult result{};
    state->native->vfs->ReadFileAsString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    const std::size_t contentRequiredSize = result.content == nullptr ? 0u : std::char_traits<char>::length(result.content);
    if (contentRequiredSize > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t contentRequired = static_cast<std::uint32_t>(contentRequiredSize);
    if (!WriteCoreU32(outputWire, 8u, contentRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || contentCapacity < contentRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (contentRequired != 0 && !state->memory.Write(contentPointer, result.content, contentRequired)) return Trap("generated Core variable output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_unpack_f32(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->UnpackF32 == nullptr)
        return Trap("UnpackF32 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("UnpackF32 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valuesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCapacity = 0;
    std::uint32_t valuesIgnoredLength = 0;
    if (!valuesControl.U32(valuesPointer) || !valuesControl.U32(valuesCapacity) ||
        !valuesControl.U32(valuesIgnoredLength) || !valuesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesCapacityBytes = static_cast<std::uint64_t>(valuesCapacity) * 4u;
    if (valuesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(valuesPointer, static_cast<std::size_t>(valuesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    UnpackF32Query query{};
    std::uint32_t dataInputPointer = 0;
    std::uint32_t dataInputCount = 0;
    if (!reader.U32(dataInputPointer) || !reader.U32(dataInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataInputBytes64 = static_cast<std::uint64_t>(dataInputCount) * 4u;
    if (dataInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> dataInputWire;
    if (!state->memory.View(dataInputPointer, static_cast<std::size_t>(dataInputBytes64), dataInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader dataInputReader(dataInputWire);
    std::vector<std::uint8_t> dataInputStorage;
    dataInputStorage.reserve(dataInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < dataInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!dataInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        dataInputStorage.push_back(item);
    }
    if (!dataInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.data = dataInputStorage.data();
    if (!AssignCoreCount(dataInputCount, query.dataSize)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.byteOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.byteOffset)>>>(slots[0].i32);
    query.count = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.count)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    UnpackF32Result result{};
    state->native->vfs->UnpackF32(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.count) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valuesRequired = static_cast<std::uint32_t>(result.count);
    if (valuesRequired != 0 && result.values == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, valuesRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valuesCapacity < valuesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valuesRequired != 0) {
        const std::size_t valuesBytes = static_cast<std::size_t>(valuesRequired) * 4u;
        if constexpr (std::endian::native == std::endian::little) {
            static_assert(sizeof(float) == 4u, "generated Core output/native element width mismatch");
            if (!state->memory.Write(valuesPointer, result.values, valuesBytes)) return Trap("generated Core variable output range changed unexpectedly");
        } else {
            std::span<std::uint8_t> valuesWire;
            if (!state->memory.MutableView(valuesPointer, valuesBytes, valuesWire)) return Trap("generated Core variable output range changed unexpectedly");
            WireWriter valuesWriter(valuesWire);
            for (std::uint32_t coreIndex = 0; coreIndex < valuesRequired; ++coreIndex) {
            if (!valuesWriter.F32(result.values[coreIndex])) return Trap("generated Core wire overflow");
            }
            if (!valuesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
        }
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_unpack_s16(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->UnpackS16 == nullptr)
        return Trap("UnpackS16 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("UnpackS16 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valuesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCapacity = 0;
    std::uint32_t valuesIgnoredLength = 0;
    if (!valuesControl.U32(valuesPointer) || !valuesControl.U32(valuesCapacity) ||
        !valuesControl.U32(valuesIgnoredLength) || !valuesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesCapacityBytes = static_cast<std::uint64_t>(valuesCapacity) * 4u;
    if (valuesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(valuesPointer, static_cast<std::size_t>(valuesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    UnpackS16Query query{};
    std::uint32_t dataInputPointer = 0;
    std::uint32_t dataInputCount = 0;
    if (!reader.U32(dataInputPointer) || !reader.U32(dataInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataInputBytes64 = static_cast<std::uint64_t>(dataInputCount) * 4u;
    if (dataInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> dataInputWire;
    if (!state->memory.View(dataInputPointer, static_cast<std::size_t>(dataInputBytes64), dataInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader dataInputReader(dataInputWire);
    std::vector<std::uint8_t> dataInputStorage;
    dataInputStorage.reserve(dataInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < dataInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!dataInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        dataInputStorage.push_back(item);
    }
    if (!dataInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.data = dataInputStorage.data();
    if (!AssignCoreCount(dataInputCount, query.dataSize)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.byteOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.byteOffset)>>>(slots[0].i32);
    query.count = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.count)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    UnpackS16Result result{};
    state->native->vfs->UnpackS16(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.count) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valuesRequired = static_cast<std::uint32_t>(result.count);
    if (valuesRequired != 0 && result.values == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, valuesRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valuesCapacity < valuesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valuesRequired != 0) {
        const std::size_t valuesBytes = static_cast<std::size_t>(valuesRequired) * 4u;
        std::span<std::uint8_t> valuesWire;
        if (!state->memory.MutableView(valuesPointer, valuesBytes, valuesWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter valuesWriter(valuesWire);
        for (std::uint32_t coreIndex = 0; coreIndex < valuesRequired; ++coreIndex) {
        if (!valuesWriter.I32(result.values[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!valuesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_unpack_s32(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->UnpackS32 == nullptr)
        return Trap("UnpackS32 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("UnpackS32 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valuesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCapacity = 0;
    std::uint32_t valuesIgnoredLength = 0;
    if (!valuesControl.U32(valuesPointer) || !valuesControl.U32(valuesCapacity) ||
        !valuesControl.U32(valuesIgnoredLength) || !valuesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesCapacityBytes = static_cast<std::uint64_t>(valuesCapacity) * 4u;
    if (valuesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(valuesPointer, static_cast<std::size_t>(valuesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    UnpackS32Query query{};
    std::uint32_t dataInputPointer = 0;
    std::uint32_t dataInputCount = 0;
    if (!reader.U32(dataInputPointer) || !reader.U32(dataInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataInputBytes64 = static_cast<std::uint64_t>(dataInputCount) * 4u;
    if (dataInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> dataInputWire;
    if (!state->memory.View(dataInputPointer, static_cast<std::size_t>(dataInputBytes64), dataInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader dataInputReader(dataInputWire);
    std::vector<std::uint8_t> dataInputStorage;
    dataInputStorage.reserve(dataInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < dataInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!dataInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        dataInputStorage.push_back(item);
    }
    if (!dataInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.data = dataInputStorage.data();
    if (!AssignCoreCount(dataInputCount, query.dataSize)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.byteOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.byteOffset)>>>(slots[0].i32);
    query.count = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.count)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    UnpackS32Result result{};
    state->native->vfs->UnpackS32(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.count) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valuesRequired = static_cast<std::uint32_t>(result.count);
    if (valuesRequired != 0 && result.values == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, valuesRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valuesCapacity < valuesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valuesRequired != 0) {
        const std::size_t valuesBytes = static_cast<std::size_t>(valuesRequired) * 4u;
        if constexpr (std::endian::native == std::endian::little) {
            static_assert(sizeof(std::int32_t) == 4u, "generated Core output/native element width mismatch");
            if (!state->memory.Write(valuesPointer, result.values, valuesBytes)) return Trap("generated Core variable output range changed unexpectedly");
        } else {
            std::span<std::uint8_t> valuesWire;
            if (!state->memory.MutableView(valuesPointer, valuesBytes, valuesWire)) return Trap("generated Core variable output range changed unexpectedly");
            WireWriter valuesWriter(valuesWire);
            for (std::uint32_t coreIndex = 0; coreIndex < valuesRequired; ++coreIndex) {
            if (!valuesWriter.I32(result.values[coreIndex])) return Trap("generated Core wire overflow");
            }
            if (!valuesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
        }
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_unpack_s8(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->UnpackS8 == nullptr)
        return Trap("UnpackS8 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("UnpackS8 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valuesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCapacity = 0;
    std::uint32_t valuesIgnoredLength = 0;
    if (!valuesControl.U32(valuesPointer) || !valuesControl.U32(valuesCapacity) ||
        !valuesControl.U32(valuesIgnoredLength) || !valuesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesCapacityBytes = static_cast<std::uint64_t>(valuesCapacity) * 4u;
    if (valuesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(valuesPointer, static_cast<std::size_t>(valuesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    UnpackS8Query query{};
    std::uint32_t dataInputPointer = 0;
    std::uint32_t dataInputCount = 0;
    if (!reader.U32(dataInputPointer) || !reader.U32(dataInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataInputBytes64 = static_cast<std::uint64_t>(dataInputCount) * 4u;
    if (dataInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> dataInputWire;
    if (!state->memory.View(dataInputPointer, static_cast<std::size_t>(dataInputBytes64), dataInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader dataInputReader(dataInputWire);
    std::vector<std::uint8_t> dataInputStorage;
    dataInputStorage.reserve(dataInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < dataInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!dataInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        dataInputStorage.push_back(item);
    }
    if (!dataInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.data = dataInputStorage.data();
    if (!AssignCoreCount(dataInputCount, query.dataSize)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.byteOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.byteOffset)>>>(slots[0].i32);
    query.count = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.count)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    UnpackS8Result result{};
    state->native->vfs->UnpackS8(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.count) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valuesRequired = static_cast<std::uint32_t>(result.count);
    if (valuesRequired != 0 && result.values == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, valuesRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valuesCapacity < valuesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valuesRequired != 0) {
        const std::size_t valuesBytes = static_cast<std::size_t>(valuesRequired) * 4u;
        std::span<std::uint8_t> valuesWire;
        if (!state->memory.MutableView(valuesPointer, valuesBytes, valuesWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter valuesWriter(valuesWire);
        for (std::uint32_t coreIndex = 0; coreIndex < valuesRequired; ++coreIndex) {
        if (!valuesWriter.I32(result.values[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!valuesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_unpack_u16(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->UnpackU16 == nullptr)
        return Trap("UnpackU16 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("UnpackU16 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valuesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCapacity = 0;
    std::uint32_t valuesIgnoredLength = 0;
    if (!valuesControl.U32(valuesPointer) || !valuesControl.U32(valuesCapacity) ||
        !valuesControl.U32(valuesIgnoredLength) || !valuesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesCapacityBytes = static_cast<std::uint64_t>(valuesCapacity) * 4u;
    if (valuesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(valuesPointer, static_cast<std::size_t>(valuesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    UnpackU16Query query{};
    std::uint32_t dataInputPointer = 0;
    std::uint32_t dataInputCount = 0;
    if (!reader.U32(dataInputPointer) || !reader.U32(dataInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataInputBytes64 = static_cast<std::uint64_t>(dataInputCount) * 4u;
    if (dataInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> dataInputWire;
    if (!state->memory.View(dataInputPointer, static_cast<std::size_t>(dataInputBytes64), dataInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader dataInputReader(dataInputWire);
    std::vector<std::uint8_t> dataInputStorage;
    dataInputStorage.reserve(dataInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < dataInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!dataInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        dataInputStorage.push_back(item);
    }
    if (!dataInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.data = dataInputStorage.data();
    if (!AssignCoreCount(dataInputCount, query.dataSize)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.byteOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.byteOffset)>>>(slots[0].i32);
    query.count = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.count)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    UnpackU16Result result{};
    state->native->vfs->UnpackU16(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.count) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valuesRequired = static_cast<std::uint32_t>(result.count);
    if (valuesRequired != 0 && result.values == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, valuesRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valuesCapacity < valuesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valuesRequired != 0) {
        const std::size_t valuesBytes = static_cast<std::size_t>(valuesRequired) * 4u;
        std::span<std::uint8_t> valuesWire;
        if (!state->memory.MutableView(valuesPointer, valuesBytes, valuesWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter valuesWriter(valuesWire);
        for (std::uint32_t coreIndex = 0; coreIndex < valuesRequired; ++coreIndex) {
        if (!valuesWriter.U32(result.values[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!valuesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_unpack_u32(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->UnpackU32 == nullptr)
        return Trap("UnpackU32 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("UnpackU32 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valuesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCapacity = 0;
    std::uint32_t valuesIgnoredLength = 0;
    if (!valuesControl.U32(valuesPointer) || !valuesControl.U32(valuesCapacity) ||
        !valuesControl.U32(valuesIgnoredLength) || !valuesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesCapacityBytes = static_cast<std::uint64_t>(valuesCapacity) * 4u;
    if (valuesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(valuesPointer, static_cast<std::size_t>(valuesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    UnpackU32Query query{};
    std::uint32_t dataInputPointer = 0;
    std::uint32_t dataInputCount = 0;
    if (!reader.U32(dataInputPointer) || !reader.U32(dataInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataInputBytes64 = static_cast<std::uint64_t>(dataInputCount) * 4u;
    if (dataInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> dataInputWire;
    if (!state->memory.View(dataInputPointer, static_cast<std::size_t>(dataInputBytes64), dataInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader dataInputReader(dataInputWire);
    std::vector<std::uint8_t> dataInputStorage;
    dataInputStorage.reserve(dataInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < dataInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!dataInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        dataInputStorage.push_back(item);
    }
    if (!dataInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.data = dataInputStorage.data();
    if (!AssignCoreCount(dataInputCount, query.dataSize)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.byteOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.byteOffset)>>>(slots[0].i32);
    query.count = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.count)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    UnpackU32Result result{};
    state->native->vfs->UnpackU32(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.count) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valuesRequired = static_cast<std::uint32_t>(result.count);
    if (valuesRequired != 0 && result.values == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, valuesRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valuesCapacity < valuesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valuesRequired != 0) {
        const std::size_t valuesBytes = static_cast<std::size_t>(valuesRequired) * 4u;
        if constexpr (std::endian::native == std::endian::little) {
            static_assert(sizeof(std::uint32_t) == 4u, "generated Core output/native element width mismatch");
            if (!state->memory.Write(valuesPointer, result.values, valuesBytes)) return Trap("generated Core variable output range changed unexpectedly");
        } else {
            std::span<std::uint8_t> valuesWire;
            if (!state->memory.MutableView(valuesPointer, valuesBytes, valuesWire)) return Trap("generated Core variable output range changed unexpectedly");
            WireWriter valuesWriter(valuesWire);
            for (std::uint32_t coreIndex = 0; coreIndex < valuesRequired; ++coreIndex) {
            if (!valuesWriter.U32(result.values[coreIndex])) return Trap("generated Core wire overflow");
            }
            if (!valuesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
        }
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_unpack_u8(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->UnpackU8 == nullptr)
        return Trap("UnpackU8 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("UnpackU8 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader valuesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCapacity = 0;
    std::uint32_t valuesIgnoredLength = 0;
    if (!valuesControl.U32(valuesPointer) || !valuesControl.U32(valuesCapacity) ||
        !valuesControl.U32(valuesIgnoredLength) || !valuesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesCapacityBytes = static_cast<std::uint64_t>(valuesCapacity) * 4u;
    if (valuesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(valuesPointer, static_cast<std::size_t>(valuesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    UnpackU8Query query{};
    std::uint32_t dataInputPointer = 0;
    std::uint32_t dataInputCount = 0;
    if (!reader.U32(dataInputPointer) || !reader.U32(dataInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataInputBytes64 = static_cast<std::uint64_t>(dataInputCount) * 4u;
    if (dataInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> dataInputWire;
    if (!state->memory.View(dataInputPointer, static_cast<std::size_t>(dataInputBytes64), dataInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader dataInputReader(dataInputWire);
    std::vector<std::uint8_t> dataInputStorage;
    dataInputStorage.reserve(dataInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < dataInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!dataInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        dataInputStorage.push_back(item);
    }
    if (!dataInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.data = dataInputStorage.data();
    if (!AssignCoreCount(dataInputCount, query.dataSize)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.byteOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.byteOffset)>>>(slots[0].i32);
    query.count = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.count)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    UnpackU8Result result{};
    state->native->vfs->UnpackU8(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.count) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t valuesRequired = static_cast<std::uint32_t>(result.count);
    if (valuesRequired != 0 && result.values == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, valuesRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || valuesCapacity < valuesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (valuesRequired != 0) {
        const std::size_t valuesBytes = static_cast<std::size_t>(valuesRequired) * 4u;
        std::span<std::uint8_t> valuesWire;
        if (!state->memory.MutableView(valuesPointer, valuesBytes, valuesWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter valuesWriter(valuesWire);
        for (std::uint32_t coreIndex = 0; coreIndex < valuesRequired; ++coreIndex) {
        if (!valuesWriter.U32(result.values[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!valuesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_zlib_compress(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->ZlibCompress == nullptr)
        return Trap("ZlibCompress generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ZlibCompress generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader dataControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCapacity = 0;
    std::uint32_t dataIgnoredLength = 0;
    if (!dataControl.U32(dataPointer) || !dataControl.U32(dataCapacity) ||
        !dataControl.U32(dataIgnoredLength) || !dataControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataCapacityBytes = static_cast<std::uint64_t>(dataCapacity) * 4u;
    if (dataCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(dataPointer, static_cast<std::size_t>(dataCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    ZlibCompressQuery query{};
    std::uint32_t dataInputPointer = 0;
    std::uint32_t dataInputCount = 0;
    if (!reader.U32(dataInputPointer) || !reader.U32(dataInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataInputBytes64 = static_cast<std::uint64_t>(dataInputCount) * 4u;
    if (dataInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> dataInputWire;
    if (!state->memory.View(dataInputPointer, static_cast<std::size_t>(dataInputBytes64), dataInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader dataInputReader(dataInputWire);
    std::vector<std::uint8_t> dataInputStorage;
    dataInputStorage.reserve(dataInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < dataInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!dataInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        dataInputStorage.push_back(item);
    }
    if (!dataInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.data = dataInputStorage.data();
    if (!AssignCoreCount(dataInputCount, query.dataSize)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    ZlibCompressResult result{};
    state->native->vfs->ZlibCompress(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.size) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t dataRequired = static_cast<std::uint32_t>(result.size);
    if (dataRequired != 0 && result.data == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, dataRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || dataCapacity < dataRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (dataRequired != 0) {
        const std::size_t dataBytes = static_cast<std::size_t>(dataRequired) * 4u;
        std::span<std::uint8_t> dataWire;
        if (!state->memory.MutableView(dataPointer, dataBytes, dataWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter dataWriter(dataWire);
        for (std::uint32_t coreIndex = 0; coreIndex < dataRequired; ++coreIndex) {
        if (!dataWriter.U32(result.data[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!dataWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_vfs_zlib_decompress(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->ZlibDecompress == nullptr)
        return Trap("ZlibDecompress generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ZlibDecompress generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[0].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 8u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 12u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader dataControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCapacity = 0;
    std::uint32_t dataIgnoredLength = 0;
    if (!dataControl.U32(dataPointer) || !dataControl.U32(dataCapacity) ||
        !dataControl.U32(dataIgnoredLength) || !dataControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataCapacityBytes = static_cast<std::uint64_t>(dataCapacity) * 4u;
    if (dataCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(dataPointer, static_cast<std::size_t>(dataCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    ZlibDecompressQuery query{};
    std::uint32_t dataInputPointer = 0;
    std::uint32_t dataInputCount = 0;
    if (!reader.U32(dataInputPointer) || !reader.U32(dataInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataInputBytes64 = static_cast<std::uint64_t>(dataInputCount) * 4u;
    if (dataInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> dataInputWire;
    if (!state->memory.View(dataInputPointer, static_cast<std::size_t>(dataInputBytes64), dataInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader dataInputReader(dataInputWire);
    std::vector<std::uint8_t> dataInputStorage;
    dataInputStorage.reserve(dataInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < dataInputCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!dataInputReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        dataInputStorage.push_back(item);
    }
    if (!dataInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.data = dataInputStorage.data();
    if (!AssignCoreCount(dataInputCount, query.dataSize)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    ZlibDecompressResult result{};
    state->native->vfs->ZlibDecompress(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.size) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t dataRequired = static_cast<std::uint32_t>(result.size);
    if (dataRequired != 0 && result.data == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, dataRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || dataCapacity < dataRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (dataRequired != 0) {
        const std::size_t dataBytes = static_cast<std::size_t>(dataRequired) * 4u;
        std::span<std::uint8_t> dataWire;
        if (!state->memory.MutableView(dataPointer, dataBytes, dataWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter dataWriter(dataWire);
        for (std::uint32_t coreIndex = 0; coreIndex < dataRequired; ++coreIndex) {
        if (!dataWriter.U32(result.data[coreIndex])) return Trap("generated Core wire overflow");
        }
        if (!dataWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_unsynced_read_solve_nurbs_curve(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedRead == nullptr ||
        state->native->unsyncedRead->SolveNURBSCurve == nullptr)
        return Trap("SolveNURBSCurve generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("SolveNURBSCurve generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 16u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 16u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader pointsControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t pointsPointer = 0;
    std::uint32_t pointsCapacity = 0;
    std::uint32_t pointsIgnoredLength = 0;
    if (!pointsControl.U32(pointsPointer) || !pointsControl.U32(pointsCapacity) ||
        !pointsControl.U32(pointsIgnoredLength) || !pointsControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t pointsCapacityBytes = static_cast<std::uint64_t>(pointsCapacity) * 12u;
    if (pointsCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(pointsPointer, static_cast<std::size_t>(pointsCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    SolveNURBSCurveQuery query{};
    query.degree = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.degree)>>>(slots[0].i32);
    std::uint32_t pointsInputPointer = 0;
    std::uint32_t pointsInputCount = 0;
    if (!reader.U32(pointsInputPointer) || !reader.U32(pointsInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t pointsInputBytes64 = static_cast<std::uint64_t>(pointsInputCount) * 16u;
    if (pointsInputBytes64 > std::numeric_limits<std::size_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::span<const std::uint8_t> pointsInputWire;
    if (!state->memory.View(pointsInputPointer, static_cast<std::size_t>(pointsInputBytes64), pointsInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader pointsInputReader(pointsInputWire);
    std::vector<Float4> pointsInputStorage;
    pointsInputStorage.reserve(pointsInputCount);
    for (std::uint32_t coreIndex = 0; coreIndex < pointsInputCount; ++coreIndex) {
        Float4 item{};
        if (!pointsInputReader.F32(item.x)) return Trap("generated Core wire underflow");
        if (!pointsInputReader.F32(item.y)) return Trap("generated Core wire underflow");
        if (!pointsInputReader.F32(item.z)) return Trap("generated Core wire underflow");
        if (!pointsInputReader.F32(item.w)) return Trap("generated Core wire underflow");
        pointsInputStorage.push_back(item);
    }
    if (!pointsInputReader.Finish(4u)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.points = pointsInputStorage.data();
    if (!AssignCoreCount(pointsInputCount, query.pointCount)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    std::uint32_t knotsInputPointer = 0;
    std::uint32_t knotsInputCount = 0;
    if (!reader.U32(knotsInputPointer) || !reader.U32(knotsInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (knotsInputCount == 0) {
        query.knots = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) { slots[0].i32 = static_cast<std::int32_t>(Status::NotAvailable); return nullptr; }
        const std::uint64_t knotsInputBytes64 = static_cast<std::uint64_t>(knotsInputCount) * 4u;
        if (knotsInputBytes64 > std::numeric_limits<std::size_t>::max() || (knotsInputPointer % 4u) != 0u) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
        std::span<const std::uint8_t> knotsInputBytes;
        if (!state->memory.View(knotsInputPointer, static_cast<std::size_t>(knotsInputBytes64), knotsInputBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
        static_assert(sizeof(float) == 4u, "generated Core borrowed/native element width mismatch");
        query.knots = reinterpret_cast<std::remove_reference_t<decltype(query.knots)>>(knotsInputBytes.data());
    }
    if (!AssignCoreCount(knotsInputCount, query.knotCount)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.segments = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.segments)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    SolveNURBSCurveResult result{};
    state->native->unsyncedRead->SolveNURBSCurve(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.count) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t pointsRequired = static_cast<std::uint32_t>(result.count);
    if (pointsRequired != 0 && result.points == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, pointsRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || pointsCapacity < pointsRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (pointsRequired != 0) {
        const std::size_t pointsBytes = static_cast<std::size_t>(pointsRequired) * 12u;
        std::span<std::uint8_t> pointsWire;
        if (!state->memory.MutableView(pointsPointer, pointsBytes, pointsWire)) return Trap("generated Core variable output range changed unexpectedly");
        WireWriter pointsWriter(pointsWire);
        for (std::uint32_t coreIndex = 0; coreIndex < pointsRequired; ++coreIndex) {
        if (!pointsWriter.F32(result.points[coreIndex].x)) return Trap("generated Core wire overflow");
        if (!pointsWriter.F32(result.points[coreIndex].y)) return Trap("generated Core wire overflow");
        if (!pointsWriter.F32(result.points[coreIndex].z)) return Trap("generated Core wire overflow");
        }
        if (!pointsWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
    }
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 4u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariableIo_unit_script_call_unit_script(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unitScript == nullptr ||
        state->native->syncedCtrl->unitScript->CallUnitScript == nullptr)
        return Trap("CallUnitScript generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("CallUnitScript generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t inputDescriptor = static_cast<std::uint32_t>(slots[2].i32);
    const std::uint32_t outputDescriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> inputWire;
    std::span<std::uint8_t> outputWire;
    if (!state->memory.View(inputDescriptor, 16u, inputWire) ||
        !state->memory.MutableView(outputDescriptor, 20u, outputWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(inputWire);
    WireReader retValuesControl(std::span<const std::uint8_t>(outputWire.data() + 0u, 12));
    std::uint32_t retValuesPointer = 0;
    std::uint32_t retValuesCapacity = 0;
    std::uint32_t retValuesIgnoredLength = 0;
    if (!retValuesControl.U32(retValuesPointer) || !retValuesControl.U32(retValuesCapacity) ||
        !retValuesControl.U32(retValuesIgnoredLength) || !retValuesControl.Finish(4)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t retValuesCapacityBytes = static_cast<std::uint64_t>(retValuesCapacity) * 4u;
    if (retValuesCapacityBytes > std::numeric_limits<std::size_t>::max() || !state->memory.Contains(retValuesPointer, static_cast<std::size_t>(retValuesCapacityBytes))) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }

    CallUnitScriptQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t functionNameInputPointer = 0;
    std::uint32_t functionNameInputCount = 0;
    if (!reader.U32(functionNameInputPointer) || !reader.U32(functionNameInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> functionNameInputWire;
    if (!state->memory.View(functionNameInputPointer, functionNameInputCount, functionNameInputWire)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
    WireReader functionNameInputReader(functionNameInputWire);
    std::string functionNameInputStorage;
    { std::uint32_t coreLength = 0; if (!functionNameInputReader.U32(coreLength)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } std::span<const std::uint8_t> coreBytes; if (!functionNameInputReader.Bytes(coreLength, coreBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; } functionNameInputStorage.assign(reinterpret_cast<const char*>(coreBytes.data()), coreBytes.size()); }
    query.functionName = functionNameInputStorage.c_str();
    std::uint32_t argsInputPointer = 0;
    std::uint32_t argsInputCount = 0;
    if (!reader.U32(argsInputPointer) || !reader.U32(argsInputCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (argsInputCount == 0) {
        query.args = nullptr;
    } else {
        if constexpr (std::endian::native != std::endian::little) { slots[0].i32 = static_cast<std::int32_t>(Status::NotAvailable); return nullptr; }
        const std::uint64_t argsInputBytes64 = static_cast<std::uint64_t>(argsInputCount) * 4u;
        if (argsInputBytes64 > std::numeric_limits<std::size_t>::max() || (argsInputPointer % 4u) != 0u) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
        std::span<const std::uint8_t> argsInputBytes;
        if (!state->memory.View(argsInputPointer, static_cast<std::size_t>(argsInputBytes64), argsInputBytes)) { slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds); return nullptr; }
        static_assert(sizeof(float) == 4u, "generated Core borrowed/native element width mismatch");
        query.args = reinterpret_cast<std::remove_reference_t<decltype(query.args)>>(argsInputBytes.data());
    }
    if (!AssignCoreCount(argsInputCount, query.argCount)) { slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument); return nullptr; }
    query.retCapacity = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.retCapacity)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    CallUnitScriptResult result{};
    state->native->syncedCtrl->unitScript->CallUnitScript(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    bool outputTooSmall = false;
    if (static_cast<std::uint64_t>(result.retCount) > std::numeric_limits<std::uint32_t>::max()) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    const std::uint32_t retValuesRequired = static_cast<std::uint32_t>(result.retCount);
    if (retValuesRequired != 0 && result.retValues == nullptr) { slots[0].i32 = static_cast<std::int32_t>(Status::OperationFailed); return nullptr; }
    if (!WriteCoreU32(outputWire, 8u, retValuesRequired))
        return Trap("generated Core output descriptor changed unexpectedly");
    outputTooSmall = outputTooSmall || retValuesCapacity < retValuesRequired;
    if (outputTooSmall) { slots[0].i32 = static_cast<std::int32_t>(Status::BufferOverflow); return nullptr; }
    if (retValuesRequired != 0) {
        const std::size_t retValuesBytes = static_cast<std::size_t>(retValuesRequired) * 4u;
        if constexpr (std::endian::native == std::endian::little) {
            static_assert(sizeof(float) == 4u, "generated Core output/native element width mismatch");
            if (!state->memory.Write(retValuesPointer, result.retValues, retValuesBytes)) return Trap("generated Core variable output range changed unexpectedly");
        } else {
            std::span<std::uint8_t> retValuesWire;
            if (!state->memory.MutableView(retValuesPointer, retValuesBytes, retValuesWire)) return Trap("generated Core variable output range changed unexpectedly");
            WireWriter retValuesWriter(retValuesWire);
            for (std::uint32_t coreIndex = 0; coreIndex < retValuesRequired; ++coreIndex) {
            if (!retValuesWriter.F32(result.retValues[coreIndex])) return Trap("generated Core wire overflow");
            }
            if (!retValuesWriter.Finish(4u)) return Trap("generated Core list output layout mismatch");
        }
    }
    std::span<std::uint8_t> fixedWire = outputWire.subspan(12u, 8u);
    WireWriter writer(fixedWire);
    if (!writer.Bool(result.functionFound)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    slots[0].i32 = 0;
    return nullptr;
}


} // namespace

bool RegisterGeneratedVariableIoImports(wasmtime_linker_t* linker, HostState* state,
    std::string& error)
{
    if (linker == nullptr || state == nullptr || state->native == nullptr) {
        error = "cannot register generated variable-I/O Core imports without linker/host/native API";
        return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:unit-defs", "get-unit-def-custom-param",
                MakeFuncType(params, 3, results, 1), CoreVariableIo_unit_defs_get_unit_def_custom_param, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:unit-defs", "get-unit-def-param-string",
                MakeFuncType(params, 3, results, 1), CoreVariableIo_unit_defs_get_unit_def_param_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:feature-defs", "get-feature-def-custom-param",
                MakeFuncType(params, 3, results, 1), CoreVariableIo_feature_defs_get_feature_def_custom_param, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:weapon-defs", "get-weapon-def-custom-param",
                MakeFuncType(params, 3, results, 1), CoreVariableIo_weapon_defs_get_weapon_def_custom_param, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:game", "get-map-option",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_game_get_map_option, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:game", "get-mod-option",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_game_get_mod_option, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:encoding", "decode-base64",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_encoding_decode_base64, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:encoding", "decode-base64-url",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_encoding_decode_base64_url, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:encoding", "encode-base64",
                MakeFuncType(params, 3, results, 1), CoreVariableIo_encoding_encode_base64, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:encoding", "encode-base64-url",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_encoding_encode_base64_url, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:input", "get-key-from-scan-symbol",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_input_get_key_from_scan_symbol, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:config", "get-config-string",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_config_get_config_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:tracing", "trace-ray-between-positions",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_tracing_trace_ray_between_positions, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:tracing", "trace-ray-in-direction",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_tracing_trace_ray_in_direction, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:unsynced-ctrl", "get-water-texture",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_unsynced_ctrl_get_water_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_F32, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:gfx", "font-wrap-text",
                MakeFuncType(params, 6, results, 1), CoreVariableIo_gfx_font_wrap_text, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:gfx", "get-atmosphere",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_gfx_get_atmosphere, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:gfx", "get-map-rendering",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_gfx_get_map_rendering, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:gfx", "get-sun",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_gfx_get_sun, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:gfx", "get-water-rendering",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_gfx_get_water_rendering, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:profiling", "get-profiler-time-record",
                MakeFuncType(params, 3, results, 1), CoreVariableIo_profiling_get_profiler_time_record, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:rml-ui", "element-get-attribute",
                MakeFuncType(params, 3, results, 1), CoreVariableIo_rml_ui_element_get_attribute, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:rml-ui", "element-get-elements-by-class-name",
                MakeFuncType(params, 3, results, 1), CoreVariableIo_rml_ui_element_get_elements_by_class_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:rml-ui", "element-get-elements-by-tag-name",
                MakeFuncType(params, 3, results, 1), CoreVariableIo_rml_ui_element_get_elements_by_tag_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:rml-ui", "element-query-selector-all",
                MakeFuncType(params, 3, results, 1), CoreVariableIo_rml_ui_element_query_selector_all, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:rml-ui", "event-get-parameter-string",
                MakeFuncType(params, 3, results, 1), CoreVariableIo_rml_ui_event_get_parameter_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "calculate-hash",
                MakeFuncType(params, 3, results, 1), CoreVariableIo_vfs_calculate_hash, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "get-archive-checksum",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_get_archive_checksum, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "get-archive-containing-file",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_get_archive_containing_file, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "get-archive-path",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_get_archive_path, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "get-file-absolute-path",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_get_file_absolute_path, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "get-name-from-rapid-tag",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_get_name_from_rapid_tag, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "load-file",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_load_file, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "pack-f32",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_pack_f32, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "pack-s16",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_pack_s16, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "pack-s32",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_pack_s32, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "pack-s8",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_pack_s8, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "pack-u16",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_pack_u16, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "pack-u32",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_pack_u32, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "pack-u8",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_pack_u8, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "read-file",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_read_file, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "read-file-as-string",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_read_file_as_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "unpack-f32",
                MakeFuncType(params, 4, results, 1), CoreVariableIo_vfs_unpack_f32, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "unpack-s16",
                MakeFuncType(params, 4, results, 1), CoreVariableIo_vfs_unpack_s16, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "unpack-s32",
                MakeFuncType(params, 4, results, 1), CoreVariableIo_vfs_unpack_s32, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "unpack-s8",
                MakeFuncType(params, 4, results, 1), CoreVariableIo_vfs_unpack_s8, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "unpack-u16",
                MakeFuncType(params, 4, results, 1), CoreVariableIo_vfs_unpack_u16, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "unpack-u32",
                MakeFuncType(params, 4, results, 1), CoreVariableIo_vfs_unpack_u32, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "unpack-u8",
                MakeFuncType(params, 4, results, 1), CoreVariableIo_vfs_unpack_u8, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "zlib-compress",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_zlib_compress, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:vfs", "zlib-decompress",
                MakeFuncType(params, 2, results, 1), CoreVariableIo_vfs_zlib_decompress, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:unsynced-read", "solve-nurbs-curve",
                MakeFuncType(params, 4, results, 1), CoreVariableIo_unsynced_read_solve_nurbs_curve, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariableIo(linker, "spring:unit-script", "call-unit-script",
                MakeFuncType(params, 4, results, 1), CoreVariableIo_unit_script_call_unit_script, state, error))
            return false;
    }

    return true;
}

static_assert(53 >= 0, "generated variable-I/O Core callback count");

} // namespace recoil::wasm::core::generated
