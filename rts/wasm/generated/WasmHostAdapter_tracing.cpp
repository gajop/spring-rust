/* This file is part of the Spring engine (GPL v2 or later), see LICENSE.html */

// @generated by spring-api-codegen; do not edit.

#include "WasmHostAdapterSupport.h"

using namespace recoil::wasm::generated::detail;

namespace recoil::wasm::generated::detail {

bool Read_Ray(const WasmValue& input, Ray& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_origin = FindRecordField(*record, "origin", error);
	if (value_origin == nullptr) return false;
	if (!Read_Float3(*value_origin, output.origin, storage, error)) { return false; }
	const auto* value_direction = FindRecordField(*record, "direction", error);
	if (value_direction == nullptr) return false;
	if (!Read_Float3(*value_direction, output.direction, storage, error)) { return false; }
	const auto* value_length = FindRecordField(*record, "length", error);
	if (value_length == nullptr) return false;
	if (!ReadScalar(*value_length, output.length, error)) { return false; }
	const auto* value_flags = FindRecordField(*record, "flags", error);
	if (value_flags == nullptr) return false;
	if (!ReadScalar(*value_flags, output.flags, error)) { return false; }
	const auto* value_allyTeamID = FindRecordField(*record, "ally-team-id", error);
	if (value_allyTeamID == nullptr) return false;
	if (!ReadScalar(*value_allyTeamID, output.allyTeamID, error)) { return false; }
	return true;
}

WasmValue Write_Ray(const Ray& value)
{
	WasmValueRecord fields;
	fields.emplace("origin", Write_Float3(value.origin));
	fields.emplace("direction", Write_Float3(value.direction));
	fields.emplace("length", WriteScalar(value.length));
	fields.emplace("flags", WriteScalar(value.flags));
	fields.emplace("ally-team-id", WriteScalar(value.allyTeamID));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayBetweenPositionsQuery(const WasmValue& input, TraceRayBetweenPositionsQuery& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_start = FindRecordField(*record, "start", error);
	if (value_start == nullptr) return false;
	if (!Read_Float3(*value_start, output.start, storage, error)) { return false; }
	const auto* value_end = FindRecordField(*record, "end", error);
	if (value_end == nullptr) return false;
	if (!Read_Float3(*value_end, output.end, storage, error)) { return false; }
	const auto* value_type = FindRecordField(*record, "type", error);
	if (value_type == nullptr) return false;
	auto& stored_string_output_type = storage.Make<std::string>();
	if (!ReadString(*value_type, stored_string_output_type, error)) { return false; }
	output.type = stored_string_output_type.c_str();
	return true;
}

WasmValue Write_TraceRayBetweenPositionsQuery(const TraceRayBetweenPositionsQuery& value)
{
	WasmValueRecord fields;
	fields.emplace("start", Write_Float3(value.start));
	fields.emplace("end", Write_Float3(value.end));
	fields.emplace("type", WasmValue::String((value.type == nullptr) ? std::string{} : std::string(value.type)));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayBetweenPositionsResult(const WasmValue& input, TraceRayBetweenPositionsResult& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_hits = FindRecordField(*record, "hits", error);
	if (value_hits == nullptr) return false;
	const auto* list_hits = std::get_if<WasmValueList>(&(*value_hits).storage);
	if (list_hits == nullptr) { error = "Wasm argument is not a list"; return false; }
	auto& native_hits = storage.Make<std::vector<TraceRayHit>>();
	native_hits.reserve(list_hits->size());
	for (const auto& item : *list_hits) {
		TraceRayHit item_value{};
		if (!Read_TraceRayHit(item, item_value, storage, error)) { return false; }
		native_hits.push_back(std::move(item_value));
	}
	output.hits = native_hits.data();
	if (!AssignCount(list_hits->size(), output.count, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayBetweenPositionsResult(const TraceRayBetweenPositionsResult& value)
{
	WasmValueRecord fields;
	fields.emplace("hits", WriteNativeList(value.hits, value.count, [](const auto& value) { return Write_TraceRayHit(value); }));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayFeaturesQuery(const WasmValue& input, TraceRayFeaturesQuery& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_ray = FindRecordField(*record, "ray", error);
	if (value_ray == nullptr) return false;
	if (!Read_Ray(*value_ray, output.ray, storage, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayFeaturesQuery(const TraceRayFeaturesQuery& value)
{
	WasmValueRecord fields;
	fields.emplace("ray", Write_Ray(value.ray));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayFeaturesResult(const WasmValue& input, TraceRayFeaturesResult& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_hit = FindRecordField(*record, "hit", error);
	if (value_hit == nullptr) return false;
	if (!ReadScalar(*value_hit, output.hit, error)) { return false; }
	const auto* value_hitType = FindRecordField(*record, "hit-type", error);
	if (value_hitType == nullptr) return false;
	if (!ReadScalar(*value_hitType, output.hitType, error)) { return false; }
	const auto* value_hitID = FindRecordField(*record, "hit-id", error);
	if (value_hitID == nullptr) return false;
	if (!ReadScalar(*value_hitID, output.hitID, error)) { return false; }
	const auto* value_hitPos = FindRecordField(*record, "hit-pos", error);
	if (value_hitPos == nullptr) return false;
	if (!Read_Float3(*value_hitPos, output.hitPos, storage, error)) { return false; }
	const auto* value_hitNormal = FindRecordField(*record, "hit-normal", error);
	if (value_hitNormal == nullptr) return false;
	if (!Read_Float3(*value_hitNormal, output.hitNormal, storage, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayFeaturesResult(const TraceRayFeaturesResult& value)
{
	WasmValueRecord fields;
	fields.emplace("hit", WriteScalar(value.hit));
	fields.emplace("hit-type", WriteScalar(value.hitType));
	fields.emplace("hit-id", WriteScalar(value.hitID));
	fields.emplace("hit-pos", Write_Float3(value.hitPos));
	fields.emplace("hit-normal", Write_Float3(value.hitNormal));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayGroundBetweenPositionsOptions(const WasmValue& input, TraceRayGroundBetweenPositionsOptions& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_testWater = FindRecordField(*record, "test-water", error);
	if (value_testWater == nullptr) return false;
	if (std::holds_alternative<std::monostate>((*value_testWater).storage)) {
		output.testWater = {};
		output.hasTestWater = false;
	} else {
		output.hasTestWater = true;
		if (!ReadScalar(*value_testWater, output.testWater, error)) { return false; }
	}
	return true;
}

WasmValue Write_TraceRayGroundBetweenPositionsOptions(const TraceRayGroundBetweenPositionsOptions& value)
{
	WasmValueRecord fields;
	fields.emplace("test-water", value.hasTestWater ? WriteScalar(value.testWater) : WasmValue::Unit());
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayGroundBetweenPositionsQuery(const WasmValue& input, TraceRayGroundBetweenPositionsQuery& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_start = FindRecordField(*record, "start", error);
	if (value_start == nullptr) return false;
	if (!Read_Float3(*value_start, output.start, storage, error)) { return false; }
	const auto* value_end = FindRecordField(*record, "end", error);
	if (value_end == nullptr) return false;
	if (!Read_Float3(*value_end, output.end, storage, error)) { return false; }
	const auto* value_options = FindRecordField(*record, "options", error);
	if (value_options == nullptr) return false;
	if (!Read_TraceRayGroundBetweenPositionsOptions(*value_options, output.options, storage, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayGroundBetweenPositionsQuery(const TraceRayGroundBetweenPositionsQuery& value)
{
	WasmValueRecord fields;
	fields.emplace("start", Write_Float3(value.start));
	fields.emplace("end", Write_Float3(value.end));
	fields.emplace("options", Write_TraceRayGroundBetweenPositionsOptions(value.options));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayGroundBetweenPositionsResult(const WasmValue& input, TraceRayGroundBetweenPositionsResult& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_hit = FindRecordField(*record, "hit", error);
	if (value_hit == nullptr) return false;
	if (!ReadScalar(*value_hit, output.hit, error)) { return false; }
	const auto* value_hitLength = FindRecordField(*record, "hit-length", error);
	if (value_hitLength == nullptr) return false;
	if (!ReadScalar(*value_hitLength, output.hitLength, error)) { return false; }
	const auto* value_hitPos = FindRecordField(*record, "hit-pos", error);
	if (value_hitPos == nullptr) return false;
	if (!Read_Float3(*value_hitPos, output.hitPos, storage, error)) { return false; }
	const auto* value_hitNormal = FindRecordField(*record, "hit-normal", error);
	if (value_hitNormal == nullptr) return false;
	if (!Read_Float3(*value_hitNormal, output.hitNormal, storage, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayGroundBetweenPositionsResult(const TraceRayGroundBetweenPositionsResult& value)
{
	WasmValueRecord fields;
	fields.emplace("hit", WriteScalar(value.hit));
	fields.emplace("hit-length", WriteScalar(value.hitLength));
	fields.emplace("hit-pos", Write_Float3(value.hitPos));
	fields.emplace("hit-normal", Write_Float3(value.hitNormal));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayGroundInDirectionOptions(const WasmValue& input, TraceRayGroundInDirectionOptions& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_length = FindRecordField(*record, "length", error);
	if (value_length == nullptr) return false;
	if (std::holds_alternative<std::monostate>((*value_length).storage)) {
		output.length = {};
		output.hasLength = false;
	} else {
		output.hasLength = true;
		if (!ReadScalar(*value_length, output.length, error)) { return false; }
	}
	const auto* value_testWater = FindRecordField(*record, "test-water", error);
	if (value_testWater == nullptr) return false;
	if (std::holds_alternative<std::monostate>((*value_testWater).storage)) {
		output.testWater = {};
		output.hasTestWater = false;
	} else {
		output.hasTestWater = true;
		if (!ReadScalar(*value_testWater, output.testWater, error)) { return false; }
	}
	return true;
}

WasmValue Write_TraceRayGroundInDirectionOptions(const TraceRayGroundInDirectionOptions& value)
{
	WasmValueRecord fields;
	fields.emplace("length", value.hasLength ? WriteScalar(value.length) : WasmValue::Unit());
	fields.emplace("test-water", value.hasTestWater ? WriteScalar(value.testWater) : WasmValue::Unit());
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayGroundInDirectionQuery(const WasmValue& input, TraceRayGroundInDirectionQuery& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_start = FindRecordField(*record, "start", error);
	if (value_start == nullptr) return false;
	if (!Read_Float3(*value_start, output.start, storage, error)) { return false; }
	const auto* value_dir = FindRecordField(*record, "dir", error);
	if (value_dir == nullptr) return false;
	if (!Read_Float3(*value_dir, output.dir, storage, error)) { return false; }
	const auto* value_options = FindRecordField(*record, "options", error);
	if (value_options == nullptr) return false;
	if (!Read_TraceRayGroundInDirectionOptions(*value_options, output.options, storage, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayGroundInDirectionQuery(const TraceRayGroundInDirectionQuery& value)
{
	WasmValueRecord fields;
	fields.emplace("start", Write_Float3(value.start));
	fields.emplace("dir", Write_Float3(value.dir));
	fields.emplace("options", Write_TraceRayGroundInDirectionOptions(value.options));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayGroundInDirectionResult(const WasmValue& input, TraceRayGroundInDirectionResult& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_hit = FindRecordField(*record, "hit", error);
	if (value_hit == nullptr) return false;
	if (!ReadScalar(*value_hit, output.hit, error)) { return false; }
	const auto* value_hitLength = FindRecordField(*record, "hit-length", error);
	if (value_hitLength == nullptr) return false;
	if (!ReadScalar(*value_hitLength, output.hitLength, error)) { return false; }
	const auto* value_hitPos = FindRecordField(*record, "hit-pos", error);
	if (value_hitPos == nullptr) return false;
	if (!Read_Float3(*value_hitPos, output.hitPos, storage, error)) { return false; }
	const auto* value_hitNormal = FindRecordField(*record, "hit-normal", error);
	if (value_hitNormal == nullptr) return false;
	if (!Read_Float3(*value_hitNormal, output.hitNormal, storage, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayGroundInDirectionResult(const TraceRayGroundInDirectionResult& value)
{
	WasmValueRecord fields;
	fields.emplace("hit", WriteScalar(value.hit));
	fields.emplace("hit-length", WriteScalar(value.hitLength));
	fields.emplace("hit-pos", Write_Float3(value.hitPos));
	fields.emplace("hit-normal", Write_Float3(value.hitNormal));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayHit(const WasmValue& input, TraceRayHit& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_hitLength = FindRecordField(*record, "hit-length", error);
	if (value_hitLength == nullptr) return false;
	if (!ReadScalar(*value_hitLength, output.hitLength, error)) { return false; }
	const auto* value_objectID = FindRecordField(*record, "object-id", error);
	if (value_objectID == nullptr) return false;
	if (!ReadScalar(*value_objectID, output.objectID, error)) { return false; }
	const auto* value_objectType = FindRecordField(*record, "object-type", error);
	if (value_objectType == nullptr) return false;
	if (!ReadScalar(*value_objectType, output.objectType, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayHit(const TraceRayHit& value)
{
	WasmValueRecord fields;
	fields.emplace("hit-length", WriteScalar(value.hitLength));
	fields.emplace("object-id", WriteScalar(value.objectID));
	fields.emplace("object-type", WriteScalar(value.objectType));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayInDirectionOptions(const WasmValue& input, TraceRayInDirectionOptions& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_maxLength = FindRecordField(*record, "max-length", error);
	if (value_maxLength == nullptr) return false;
	if (std::holds_alternative<std::monostate>((*value_maxLength).storage)) {
		output.maxLength = {};
		output.hasMaxLength = false;
	} else {
		output.hasMaxLength = true;
		if (!ReadScalar(*value_maxLength, output.maxLength, error)) { return false; }
	}
	return true;
}

WasmValue Write_TraceRayInDirectionOptions(const TraceRayInDirectionOptions& value)
{
	WasmValueRecord fields;
	fields.emplace("max-length", value.hasMaxLength ? WriteScalar(value.maxLength) : WasmValue::Unit());
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayInDirectionQuery(const WasmValue& input, TraceRayInDirectionQuery& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_pos = FindRecordField(*record, "pos", error);
	if (value_pos == nullptr) return false;
	if (!Read_Float3(*value_pos, output.pos, storage, error)) { return false; }
	const auto* value_dir = FindRecordField(*record, "dir", error);
	if (value_dir == nullptr) return false;
	if (!Read_Float3(*value_dir, output.dir, storage, error)) { return false; }
	const auto* value_options = FindRecordField(*record, "options", error);
	if (value_options == nullptr) return false;
	if (!Read_TraceRayInDirectionOptions(*value_options, output.options, storage, error)) { return false; }
	const auto* value_type = FindRecordField(*record, "type", error);
	if (value_type == nullptr) return false;
	auto& stored_string_output_type = storage.Make<std::string>();
	if (!ReadString(*value_type, stored_string_output_type, error)) { return false; }
	output.type = stored_string_output_type.c_str();
	return true;
}

WasmValue Write_TraceRayInDirectionQuery(const TraceRayInDirectionQuery& value)
{
	WasmValueRecord fields;
	fields.emplace("pos", Write_Float3(value.pos));
	fields.emplace("dir", Write_Float3(value.dir));
	fields.emplace("options", Write_TraceRayInDirectionOptions(value.options));
	fields.emplace("type", WasmValue::String((value.type == nullptr) ? std::string{} : std::string(value.type)));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayInDirectionResult(const WasmValue& input, TraceRayInDirectionResult& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_hits = FindRecordField(*record, "hits", error);
	if (value_hits == nullptr) return false;
	const auto* list_hits = std::get_if<WasmValueList>(&(*value_hits).storage);
	if (list_hits == nullptr) { error = "Wasm argument is not a list"; return false; }
	auto& native_hits = storage.Make<std::vector<TraceRayHit>>();
	native_hits.reserve(list_hits->size());
	for (const auto& item : *list_hits) {
		TraceRayHit item_value{};
		if (!Read_TraceRayHit(item, item_value, storage, error)) { return false; }
		native_hits.push_back(std::move(item_value));
	}
	output.hits = native_hits.data();
	if (!AssignCount(list_hits->size(), output.count, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayInDirectionResult(const TraceRayInDirectionResult& value)
{
	WasmValueRecord fields;
	fields.emplace("hits", WriteNativeList(value.hits, value.count, [](const auto& value) { return Write_TraceRayHit(value); }));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayQuery(const WasmValue& input, TraceRayQuery& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_ray = FindRecordField(*record, "ray", error);
	if (value_ray == nullptr) return false;
	if (!Read_Ray(*value_ray, output.ray, storage, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayQuery(const TraceRayQuery& value)
{
	WasmValueRecord fields;
	fields.emplace("ray", Write_Ray(value.ray));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayResult(const WasmValue& input, TraceRayResult& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_hit = FindRecordField(*record, "hit", error);
	if (value_hit == nullptr) return false;
	if (!ReadScalar(*value_hit, output.hit, error)) { return false; }
	const auto* value_hitType = FindRecordField(*record, "hit-type", error);
	if (value_hitType == nullptr) return false;
	if (!ReadScalar(*value_hitType, output.hitType, error)) { return false; }
	const auto* value_hitID = FindRecordField(*record, "hit-id", error);
	if (value_hitID == nullptr) return false;
	if (!ReadScalar(*value_hitID, output.hitID, error)) { return false; }
	const auto* value_hitPos = FindRecordField(*record, "hit-pos", error);
	if (value_hitPos == nullptr) return false;
	if (!Read_Float3(*value_hitPos, output.hitPos, storage, error)) { return false; }
	const auto* value_hitNormal = FindRecordField(*record, "hit-normal", error);
	if (value_hitNormal == nullptr) return false;
	if (!Read_Float3(*value_hitNormal, output.hitNormal, storage, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayResult(const TraceRayResult& value)
{
	WasmValueRecord fields;
	fields.emplace("hit", WriteScalar(value.hit));
	fields.emplace("hit-type", WriteScalar(value.hitType));
	fields.emplace("hit-id", WriteScalar(value.hitID));
	fields.emplace("hit-pos", Write_Float3(value.hitPos));
	fields.emplace("hit-normal", Write_Float3(value.hitNormal));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayUnitsQuery(const WasmValue& input, TraceRayUnitsQuery& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_ray = FindRecordField(*record, "ray", error);
	if (value_ray == nullptr) return false;
	if (!Read_Ray(*value_ray, output.ray, storage, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayUnitsQuery(const TraceRayUnitsQuery& value)
{
	WasmValueRecord fields;
	fields.emplace("ray", Write_Ray(value.ray));
	return WasmValue::Record(std::move(fields));
}

bool Read_TraceRayUnitsResult(const WasmValue& input, TraceRayUnitsResult& output, NativeCallStorage& storage, std::string& error)
{
	const auto* record = std::get_if<WasmValueRecord>(&input.storage);
	if (record == nullptr) { error = "Wasm argument is not a record"; return false; }
	const auto* value_hit = FindRecordField(*record, "hit", error);
	if (value_hit == nullptr) return false;
	if (!ReadScalar(*value_hit, output.hit, error)) { return false; }
	const auto* value_hitType = FindRecordField(*record, "hit-type", error);
	if (value_hitType == nullptr) return false;
	if (!ReadScalar(*value_hitType, output.hitType, error)) { return false; }
	const auto* value_hitID = FindRecordField(*record, "hit-id", error);
	if (value_hitID == nullptr) return false;
	if (!ReadScalar(*value_hitID, output.hitID, error)) { return false; }
	const auto* value_hitPos = FindRecordField(*record, "hit-pos", error);
	if (value_hitPos == nullptr) return false;
	if (!Read_Float3(*value_hitPos, output.hitPos, storage, error)) { return false; }
	const auto* value_hitNormal = FindRecordField(*record, "hit-normal", error);
	if (value_hitNormal == nullptr) return false;
	if (!Read_Float3(*value_hitNormal, output.hitNormal, storage, error)) { return false; }
	return true;
}

WasmValue Write_TraceRayUnitsResult(const TraceRayUnitsResult& value)
{
	WasmValueRecord fields;
	fields.emplace("hit", WriteScalar(value.hit));
	fields.emplace("hit-type", WriteScalar(value.hitType));
	fields.emplace("hit-id", WriteScalar(value.hitID));
	fields.emplace("hit-pos", Write_Float3(value.hitPos));
	fields.emplace("hit-normal", Write_Float3(value.hitNormal));
	return WasmValue::Record(std::move(fields));
}

}

namespace {

NativeCalloutDispatch Dispatch_tracing_TraceRay(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->tracing == nullptr || nativeInterface->tracing->TraceRay == nullptr) { error = "NativeInterface API is unavailable: tracing.TraceRay"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	TraceRayQuery query{};
	TraceRayResult nativeResult{};
	if (!ReadRecordArgument(arguments, 0u, 0u, 1u, 1u, "ray", Read_Ray, query.ray, storage, error)) return NativeCalloutDispatch::handled;
	nativeInterface->tracing->TraceRay(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	WasmValueRecord outputFields;
	outputFields.emplace("hit", WriteScalar(nativeResult.hit));
	outputFields.emplace("hit-type", WriteScalar(nativeResult.hitType));
	outputFields.emplace("hit-id", WriteScalar(nativeResult.hitID));
	outputFields.emplace("hit-pos", Write_Float3(nativeResult.hitPos));
	outputFields.emplace("hit-normal", Write_Float3(nativeResult.hitNormal));
	result = WasmValue::Record(std::move(outputFields));
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_tracing_TraceRayBetweenPositions(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->tracing == nullptr || nativeInterface->tracing->TraceRayBetweenPositions == nullptr) { error = "NativeInterface API is unavailable: tracing.TraceRayBetweenPositions"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	TraceRayBetweenPositionsQuery query{};
	TraceRayBetweenPositionsResult nativeResult{};
	if (!ReadRecordArgument(arguments, 0u, 0u, 3u, 3u, "start", Read_Float3, query.start, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 1u, 1u, 3u, 3u, "end", Read_Float3, query.end, storage, error)) return NativeCalloutDispatch::handled;
	const WasmValue* value_type = FindArgument(arguments, 2u, 2u, 3u, 3u, "type", error);
	if (value_type == nullptr) return NativeCalloutDispatch::handled;
	auto& stored_string_query_type = storage.Make<std::string>();
	if (!ReadString(*value_type, stored_string_query_type, error)) { return NativeCalloutDispatch::handled; }
	query.type = stored_string_query_type.c_str();
	nativeInterface->tracing->TraceRayBetweenPositions(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteNativeList(nativeResult.hits, nativeResult.count, [](const auto& value) { return Write_TraceRayHit(value); });
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_tracing_TraceRayFeatures(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->tracing == nullptr || nativeInterface->tracing->TraceRayFeatures == nullptr) { error = "NativeInterface API is unavailable: tracing.TraceRayFeatures"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	TraceRayFeaturesQuery query{};
	TraceRayFeaturesResult nativeResult{};
	if (!ReadRecordArgument(arguments, 0u, 0u, 1u, 1u, "ray", Read_Ray, query.ray, storage, error)) return NativeCalloutDispatch::handled;
	nativeInterface->tracing->TraceRayFeatures(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	WasmValueRecord outputFields;
	outputFields.emplace("hit", WriteScalar(nativeResult.hit));
	outputFields.emplace("hit-type", WriteScalar(nativeResult.hitType));
	outputFields.emplace("hit-id", WriteScalar(nativeResult.hitID));
	outputFields.emplace("hit-pos", Write_Float3(nativeResult.hitPos));
	outputFields.emplace("hit-normal", Write_Float3(nativeResult.hitNormal));
	result = WasmValue::Record(std::move(outputFields));
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_tracing_TraceRayGroundBetweenPositions(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->tracing == nullptr || nativeInterface->tracing->TraceRayGroundBetweenPositions == nullptr) { error = "NativeInterface API is unavailable: tracing.TraceRayGroundBetweenPositions"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	TraceRayGroundBetweenPositionsQuery query{};
	TraceRayGroundBetweenPositionsResult nativeResult{};
	if (!ReadRecordArgument(arguments, 0u, 0u, 3u, 3u, "start", Read_Float3, query.start, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 1u, 1u, 3u, 3u, "end", Read_Float3, query.end, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 2u, 2u, 3u, 3u, "options", Read_TraceRayGroundBetweenPositionsOptions, query.options, storage, error)) return NativeCalloutDispatch::handled;
	nativeInterface->tracing->TraceRayGroundBetweenPositions(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	WasmValueRecord outputFields;
	outputFields.emplace("hit", WriteScalar(nativeResult.hit));
	outputFields.emplace("hit-length", WriteScalar(nativeResult.hitLength));
	outputFields.emplace("hit-pos", Write_Float3(nativeResult.hitPos));
	outputFields.emplace("hit-normal", Write_Float3(nativeResult.hitNormal));
	result = WasmValue::Record(std::move(outputFields));
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_tracing_TraceRayGroundInDirection(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->tracing == nullptr || nativeInterface->tracing->TraceRayGroundInDirection == nullptr) { error = "NativeInterface API is unavailable: tracing.TraceRayGroundInDirection"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	TraceRayGroundInDirectionQuery query{};
	TraceRayGroundInDirectionResult nativeResult{};
	if (!ReadRecordArgument(arguments, 0u, 0u, 3u, 3u, "start", Read_Float3, query.start, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 1u, 1u, 3u, 3u, "dir", Read_Float3, query.dir, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 2u, 2u, 3u, 3u, "options", Read_TraceRayGroundInDirectionOptions, query.options, storage, error)) return NativeCalloutDispatch::handled;
	nativeInterface->tracing->TraceRayGroundInDirection(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	WasmValueRecord outputFields;
	outputFields.emplace("hit", WriteScalar(nativeResult.hit));
	outputFields.emplace("hit-length", WriteScalar(nativeResult.hitLength));
	outputFields.emplace("hit-pos", Write_Float3(nativeResult.hitPos));
	outputFields.emplace("hit-normal", Write_Float3(nativeResult.hitNormal));
	result = WasmValue::Record(std::move(outputFields));
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_tracing_TraceRayInDirection(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->tracing == nullptr || nativeInterface->tracing->TraceRayInDirection == nullptr) { error = "NativeInterface API is unavailable: tracing.TraceRayInDirection"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	TraceRayInDirectionQuery query{};
	TraceRayInDirectionResult nativeResult{};
	if (!ReadRecordArgument(arguments, 0u, 0u, 4u, 4u, "pos", Read_Float3, query.pos, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 1u, 1u, 4u, 4u, "dir", Read_Float3, query.dir, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 2u, 2u, 4u, 4u, "options", Read_TraceRayInDirectionOptions, query.options, storage, error)) return NativeCalloutDispatch::handled;
	const WasmValue* value_type = FindArgument(arguments, 3u, 3u, 4u, 4u, "type", error);
	if (value_type == nullptr) return NativeCalloutDispatch::handled;
	auto& stored_string_query_type = storage.Make<std::string>();
	if (!ReadString(*value_type, stored_string_query_type, error)) { return NativeCalloutDispatch::handled; }
	query.type = stored_string_query_type.c_str();
	nativeInterface->tracing->TraceRayInDirection(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteNativeList(nativeResult.hits, nativeResult.count, [](const auto& value) { return Write_TraceRayHit(value); });
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_tracing_TraceRayUnits(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->tracing == nullptr || nativeInterface->tracing->TraceRayUnits == nullptr) { error = "NativeInterface API is unavailable: tracing.TraceRayUnits"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	TraceRayUnitsQuery query{};
	TraceRayUnitsResult nativeResult{};
	if (!ReadRecordArgument(arguments, 0u, 0u, 1u, 1u, "ray", Read_Ray, query.ray, storage, error)) return NativeCalloutDispatch::handled;
	nativeInterface->tracing->TraceRayUnits(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	WasmValueRecord outputFields;
	outputFields.emplace("hit", WriteScalar(nativeResult.hit));
	outputFields.emplace("hit-type", WriteScalar(nativeResult.hitType));
	outputFields.emplace("hit-id", WriteScalar(nativeResult.hitID));
	outputFields.emplace("hit-pos", Write_Float3(nativeResult.hitPos));
	outputFields.emplace("hit-normal", Write_Float3(nativeResult.hitNormal));
	result = WasmValue::Record(std::move(outputFields));
	return NativeCalloutDispatch::handled;
}

}

namespace recoil::wasm::generated {

const NativeCalloutTarget* ResolveNativeCalloutModule_tracing(std::string_view);
NativeCalloutDispatch DispatchNativeCalloutModule_tracing(NativeInterface*, std::string_view, const std::vector<WasmValue>&, WasmValue&, std::string&);
const NativeCalloutTarget* ResolveNativeCalloutModule_tracing(std::string_view function)
{
	if (detail::FunctionEquals(function, "TraceRay")) { static constexpr NativeCalloutTarget target{&Dispatch_tracing_TraceRay}; return &target; }
	if (detail::FunctionEquals(function, "TraceRayBetweenPositions")) { static constexpr NativeCalloutTarget target{&Dispatch_tracing_TraceRayBetweenPositions}; return &target; }
	if (detail::FunctionEquals(function, "TraceRayFeatures")) { static constexpr NativeCalloutTarget target{&Dispatch_tracing_TraceRayFeatures}; return &target; }
	if (detail::FunctionEquals(function, "TraceRayGroundBetweenPositions")) { static constexpr NativeCalloutTarget target{&Dispatch_tracing_TraceRayGroundBetweenPositions}; return &target; }
	if (detail::FunctionEquals(function, "TraceRayGroundInDirection")) { static constexpr NativeCalloutTarget target{&Dispatch_tracing_TraceRayGroundInDirection}; return &target; }
	if (detail::FunctionEquals(function, "TraceRayInDirection")) { static constexpr NativeCalloutTarget target{&Dispatch_tracing_TraceRayInDirection}; return &target; }
	if (detail::FunctionEquals(function, "TraceRayUnits")) { static constexpr NativeCalloutTarget target{&Dispatch_tracing_TraceRayUnits}; return &target; }
	return nullptr;
}

NativeCalloutDispatch DispatchNativeCalloutModule_tracing(NativeInterface* nativeInterface, std::string_view function, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	const NativeCalloutTarget* target = ResolveNativeCalloutModule_tracing(function);
	if (target == nullptr) return NativeCalloutDispatch::notHandled;
	return target->invoke(nativeInterface, arguments, result, error);
}
}
