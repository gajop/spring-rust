/* This file is part of the Spring engine (GPL v2 or later), see LICENSE.html */

// @generated by spring-api-codegen; do not edit.

#include "WasmHostAdapterSupport.h"

using namespace recoil::wasm::generated::detail;

namespace recoil::wasm::generated::detail {

}

namespace {

NativeCalloutDispatch Dispatch_effects_control_SpawnCEG(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->effects == nullptr || nativeInterface->syncedCtrl->effects->SpawnCEG == nullptr) { error = "NativeInterface API is unavailable: effects_control.SpawnCEG"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SpawnCEGQuery query{};
	SpawnCEGResult nativeResult{};
	if (!ReadRecordArgument(arguments, 0u, 0u, 6u, 6u, "ceg", Read_DefRef, query.ceg, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 1u, 1u, 6u, 6u, "pos", Read_Float3, query.pos, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 2u, 2u, 6u, 6u, "dir", Read_Float3, query.dir, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 6u, 6u, "radius", query.radius, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 6u, 6u, "damage", query.damage, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 5u, 5u, 6u, 6u, "dmg-mod", query.dmgMod, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->effects->SpawnCEG(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	WasmValueRecord outputFields;
	outputFields.emplace("success", WriteScalar(nativeResult.success));
	outputFields.emplace("ceg-id", WriteScalar(nativeResult.cegID));
	result = WasmValue::Record(std::move(outputFields));
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_effects_control_SpawnExplosion(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->effects == nullptr || nativeInterface->syncedCtrl->effects->SpawnExplosion == nullptr) { error = "NativeInterface API is unavailable: effects_control.SpawnExplosion"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SpawnExplosionQuery query{};
	SpawnExplosionResult nativeResult{};
	if (!ReadRecordArgument(arguments, 0u, 0u, 3u, 3u, "pos", Read_Float3, query.pos, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 1u, 1u, 3u, 3u, "dir", Read_Float3, query.dir, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 2u, 2u, 3u, 3u, "explosion-params", Read_NativeExplosionParams, query.explosionParams, storage, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->effects->SpawnExplosion(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_effects_control_SpawnSFX(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->effects == nullptr || nativeInterface->syncedCtrl->effects->SpawnSFX == nullptr) { error = "NativeInterface API is unavailable: effects_control.SpawnSFX"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SpawnSFXQuery query{};
	SpawnSFXResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 7u, 7u, "unit-id", query.unitID, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 7u, 7u, "sfx-id", query.sfxID, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 2u, 2u, 7u, 7u, "pos", Read_Float3, query.pos, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadRecordArgument(arguments, 3u, 3u, 7u, 7u, "dir", Read_Float3, query.dir, storage, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 4u, 4u, 7u, 7u, "radius", query.radius, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 5u, 5u, 7u, 7u, "damage", query.damage, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 6u, 6u, 7u, 7u, "absolute", query.absolute, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->effects->SpawnSFX(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

}

namespace recoil::wasm::generated {

const NativeCalloutTarget* ResolveNativeCalloutModule_effects_control(std::string_view);
NativeCalloutDispatch DispatchNativeCalloutModule_effects_control(NativeInterface*, std::string_view, const std::vector<WasmValue>&, WasmValue&, std::string&);
const NativeCalloutTarget* ResolveNativeCalloutModule_effects_control(std::string_view function)
{
	if (detail::FunctionEquals(function, "SpawnCEG")) { static constexpr NativeCalloutTarget target{&Dispatch_effects_control_SpawnCEG}; return &target; }
	if (detail::FunctionEquals(function, "SpawnExplosion")) { static constexpr NativeCalloutTarget target{&Dispatch_effects_control_SpawnExplosion}; return &target; }
	if (detail::FunctionEquals(function, "SpawnSFX")) { static constexpr NativeCalloutTarget target{&Dispatch_effects_control_SpawnSFX}; return &target; }
	return nullptr;
}

NativeCalloutDispatch DispatchNativeCalloutModule_effects_control(NativeInterface* nativeInterface, std::string_view function, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	const NativeCalloutTarget* target = ResolveNativeCalloutModule_effects_control(function);
	if (target == nullptr) return NativeCalloutDispatch::notHandled;
	return target->invoke(nativeInterface, arguments, result, error);
}
}
