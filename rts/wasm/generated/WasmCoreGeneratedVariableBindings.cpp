/* @generated by spring-api-codegen; do not edit. */

#include "wasm/generated/WasmCoreGeneratedVariableBindings.h"

#include <array>
#include <bit>
#include <cstdint>
#include <limits>
#include <span>
#include <string>
#include <string_view>
#include <type_traits>
#include <vector>

#include "NativeInterface/NativeInterface.h"
#include "WasmInterface/core/host/WasmCoreAbi.h"
#include "WasmInterface/core/host/WasmCoreGeneratedSupport.h"
#include "WasmInterface/core/host/WasmCoreWire.h"

namespace recoil::wasm::core::generated {
namespace {

template<typename T>
bool AssignCoreCount(std::uint32_t value, T& output)
{
    using Native = std::remove_cv_t<std::remove_reference_t<T>>;
    if constexpr (std::numeric_limits<Native>::is_signed) {
        if (static_cast<std::uint64_t>(value) >
            static_cast<std::uint64_t>(std::numeric_limits<Native>::max()))
            return false;
    } else {
        if (static_cast<std::uint64_t>(value) >
            static_cast<std::uint64_t>(std::numeric_limits<Native>::max()))
            return false;
    }
    output = static_cast<Native>(value);
    return true;
}

bool DefineGeneratedVariable(wasmtime_linker_t* linker, const char* moduleName,
    const char* functionName, wasm_functype_t* type,
    wasmtime_func_unchecked_callback_t callback, HostState* state, std::string& error)
{
    wasmtime_error_t* defineError = wasmtime_linker_define_func_unchecked(
        linker,
        moduleName, std::char_traits<char>::length(moduleName),
        functionName, std::char_traits<char>::length(functionName),
        type, callback, state, nullptr);
    wasm_functype_delete(type);
    if (defineError == nullptr)
        return true;
    error = ErrorMessage(defineError);
    return false;
}

wasm_trap_t* CoreVariable_units_info_get_unit_sensor_radius(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsInfo == nullptr ||
        state->native->unitsInfo->GetUnitSensorRadius == nullptr)
        return Trap("GetUnitSensorRadius generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetUnitSensorRadius generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 28u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetUnitSensorRadiusQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t typePointer = 0;
    std::uint32_t typeCount = 0;
    if (!reader.U32(typePointer) || !reader.U32(typeCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> typeBytes;
    if (!state->memory.View(typePointer, typeCount, typeBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string typeStorage(reinterpret_cast<const char*>(typeBytes.data()), typeBytes.size());
    query.type = typeStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetUnitSensorRadiusResult result{};
    state->native->unitsInfo->GetUnitSensorRadius(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 28> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.radius.los)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.radius.airLos)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.radius.radar)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.radius.sonar)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.radius.seismic)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.radius.radarJammer)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.radius.sonarJammer)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_units_weapons_get_unit_weapon_state(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsWeapons == nullptr ||
        state->native->unitsWeapons->GetUnitWeaponState == nullptr)
        return Trap("GetUnitWeaponState generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("GetUnitWeaponState generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 52u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetUnitWeaponStateQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    query.weaponNum = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.weaponNum)>>>(slots[1].i32);
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, keyCount, keyBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string keyStorage(reinterpret_cast<const char*>(keyBytes.data()), keyBytes.size());
    query.key = keyStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetUnitWeaponStateResult result{};
    state->native->unitsWeapons->GetUnitWeaponState(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 52> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.state.reloadTime)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.state.reloadFrame)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.state.range)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.state.projectileSpeed)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.state.accuracy)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.state.sprayAngle)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.state.aimFromHeight)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.state.salvoSize)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.state.salvoDelay)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.state.salvoError)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.state.targetMoveError)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.state.turnRate)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.state.autoTarget)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_units_pieces_get_model_root_piece(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitsPieces == nullptr ||
        state->native->unitsPieces->GetModelRootPiece == nullptr)
        return Trap("GetModelRootPiece generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("GetModelRootPiece generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetModelRootPieceQuery query{};
    std::uint32_t modelNamePointer = 0;
    std::uint32_t modelNameCount = 0;
    if (!reader.U32(modelNamePointer) || !reader.U32(modelNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> modelNameBytes;
    if (!state->memory.View(modelNamePointer, modelNameCount, modelNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string modelNameStorage(reinterpret_cast<const char*>(modelNameBytes.data()), modelNameBytes.size());
    query.modelName = modelNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetModelRootPieceResult result{};
    state->native->unitsPieces->GetModelRootPiece(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.rootPiece), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_teams_get_team_resource_stats(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->teams == nullptr ||
        state->native->teams->GetTeamResourceStats == nullptr)
        return Trap("GetTeamResourceStats generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetTeamResourceStats generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 72u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetTeamResourceStatsQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourcePointer = 0;
    std::uint32_t resourceCount = 0;
    if (!reader.U32(resourcePointer) || !reader.U32(resourceCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> resourceBytes;
    if (!state->memory.View(resourcePointer, resourceCount, resourceBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string resourceStorage(reinterpret_cast<const char*>(resourceBytes.data()), resourceBytes.size());
    query.resource = resourceStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetTeamResourceStatsResult result{};
    state->native->teams->GetTeamResourceStats(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 72> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.resources.metalCurrent)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalStorage)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalPull)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalIncome)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalExpense)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalShared)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalSent)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalReceived)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalExcess)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyCurrent)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyStorage)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyPull)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyIncome)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyExpense)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyShared)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energySent)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyReceived)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyExcess)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_teams_get_team_resources(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->teams == nullptr ||
        state->native->teams->GetTeamResources == nullptr)
        return Trap("GetTeamResources generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("GetTeamResources generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 72u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetTeamResourcesQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourcePointer = 0;
    std::uint32_t resourceCount = 0;
    if (!reader.U32(resourcePointer) || !reader.U32(resourceCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> resourceBytes;
    if (!state->memory.View(resourcePointer, resourceCount, resourceBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string resourceStorage(reinterpret_cast<const char*>(resourceBytes.data()), resourceBytes.size());
    query.resource = resourceStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GetTeamResourcesResult result{};
    state->native->teams->GetTeamResources(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 72> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.resources.metalCurrent)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalStorage)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalPull)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalIncome)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalExpense)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalShared)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalSent)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalReceived)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.metalExcess)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyCurrent)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyStorage)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyPull)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyIncome)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyExpense)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyShared)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energySent)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyReceived)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.resources.energyExcess)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_defs_get_unit_def_id_by_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefIDByName == nullptr)
        return Trap("GetUnitDefIDByName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("GetUnitDefIDByName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetUnitDefIDByNameQuery query{};
    std::uint32_t unitDefNamePointer = 0;
    std::uint32_t unitDefNameCount = 0;
    if (!reader.U32(unitDefNamePointer) || !reader.U32(unitDefNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> unitDefNameBytes;
    if (!state->memory.View(unitDefNamePointer, unitDefNameCount, unitDefNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string unitDefNameStorage(reinterpret_cast<const char*>(unitDefNameBytes.data()), unitDefNameBytes.size());
    query.unitDefName = unitDefNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetUnitDefIDByNameResult result{};
    state->native->unitDefs->GetUnitDefIDByName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.id), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_defs_get_unit_def_param_bool(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefParamBool == nullptr)
        return Trap("GetUnitDefParamBool generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetUnitDefParamBool generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetUnitDefParamBoolQuery query{};
    query.unitDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitDefID)>>>(slots[0].i32);
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, keyCount, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string keyStorage(reinterpret_cast<const char*>(keyBytes.data()), keyBytes.size());
    query.key = keyStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetUnitDefParamBoolResult result{};
    state->native->unitDefs->GetUnitDefParamBool(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_defs_get_unit_def_param_float(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefParamFloat == nullptr)
        return Trap("GetUnitDefParamFloat generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetUnitDefParamFloat generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetUnitDefParamFloatQuery query{};
    query.unitDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitDefID)>>>(slots[0].i32);
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, keyCount, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string keyStorage(reinterpret_cast<const char*>(keyBytes.data()), keyBytes.size());
    query.key = keyStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetUnitDefParamFloatResult result{};
    state->native->unitDefs->GetUnitDefParamFloat(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(std::bit_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_defs_get_unit_def_param_int(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefParamInt == nullptr)
        return Trap("GetUnitDefParamInt generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetUnitDefParamInt generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetUnitDefParamIntQuery query{};
    query.unitDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitDefID)>>>(slots[0].i32);
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, keyCount, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string keyStorage(reinterpret_cast<const char*>(keyBytes.data()), keyBytes.size());
    query.key = keyStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetUnitDefParamIntResult result{};
    state->native->unitDefs->GetUnitDefParamInt(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_defs_get_unit_def_param_type(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unitDefs == nullptr ||
        state->native->unitDefs->GetUnitDefParamType == nullptr)
        return Trap("GetUnitDefParamType generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("GetUnitDefParamType generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetUnitDefParamTypeQuery query{};
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, keyCount, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string keyStorage(reinterpret_cast<const char*>(keyBytes.data()), keyBytes.size());
    query.key = keyStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetUnitDefParamTypeResult result{};
    state->native->unitDefs->GetUnitDefParamType(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.type), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_feature_defs_get_feature_def_id_by_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->featureDefs == nullptr ||
        state->native->featureDefs->GetFeatureDefIDByName == nullptr)
        return Trap("GetFeatureDefIDByName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("GetFeatureDefIDByName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetFeatureDefIDByNameQuery query{};
    std::uint32_t featureDefNamePointer = 0;
    std::uint32_t featureDefNameCount = 0;
    if (!reader.U32(featureDefNamePointer) || !reader.U32(featureDefNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> featureDefNameBytes;
    if (!state->memory.View(featureDefNamePointer, featureDefNameCount, featureDefNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string featureDefNameStorage(reinterpret_cast<const char*>(featureDefNameBytes.data()), featureDefNameBytes.size());
    query.featureDefName = featureDefNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetFeatureDefIDByNameResult result{};
    state->native->featureDefs->GetFeatureDefIDByName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.id), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_weapon_defs_get_weapon_def_id(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->weaponDefs == nullptr ||
        state->native->weaponDefs->GetWeaponDefID == nullptr)
        return Trap("GetWeaponDefID generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("GetWeaponDefID generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetWeaponDefIDQuery query{};
    std::uint32_t weaponDefNamePointer = 0;
    std::uint32_t weaponDefNameCount = 0;
    if (!reader.U32(weaponDefNamePointer) || !reader.U32(weaponDefNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> weaponDefNameBytes;
    if (!state->memory.View(weaponDefNamePointer, weaponDefNameCount, weaponDefNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string weaponDefNameStorage(reinterpret_cast<const char*>(weaponDefNameBytes.data()), weaponDefNameBytes.size());
    query.weaponDefName = weaponDefNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetWeaponDefIDResult result{};
    state->native->weaponDefs->GetWeaponDefID(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.id), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_math_extra_bit_bits(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->mathExtra == nullptr ||
        state->native->mathExtra->BitBits == nullptr)
        return Trap("BitBits generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("BitBits generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    BitBitsQuery query{};
    std::uint32_t bitsPointer = 0;
    std::uint32_t bitsCount = 0;
    if (!reader.U32(bitsPointer) || !reader.U32(bitsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t bitsBytes64 = static_cast<std::uint64_t>(bitsCount) * 4u;
    if (bitsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> bitsWire;
    if (!state->memory.View(bitsPointer, static_cast<std::size_t>(bitsBytes64), bitsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader bitsReader(bitsWire);
    std::vector<std::uint32_t> bitsStorage;
    bitsStorage.reserve(bitsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < bitsCount; ++coreIndex) {
        std::uint32_t item{};
        { std::uint32_t coreRaw = 0; if (!bitsReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        bitsStorage.push_back(item);
    }
    if (!bitsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.bits = bitsStorage.data();
    if (!AssignCoreCount(bitsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    BitBitsResult result{};
    state->native->mathExtra->BitBits(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.bits), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_math_extra_diag(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->mathExtra == nullptr ||
        state->native->mathExtra->Diag == nullptr)
        return Trap("Diag generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("Diag generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    DiagQuery query{};
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCount = 0;
    if (!reader.U32(valuesPointer) || !reader.U32(valuesCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t valuesBytes64 = static_cast<std::uint64_t>(valuesCount) * 4u;
    if (valuesBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valuesWire;
    if (!state->memory.View(valuesPointer, static_cast<std::size_t>(valuesBytes64), valuesWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader valuesReader(valuesWire);
    std::vector<float> valuesStorage;
    valuesStorage.reserve(valuesCount);
    for (std::uint32_t coreIndex = 0; coreIndex < valuesCount; ++coreIndex) {
        float item{};
        if (!valuesReader.F32(item)) return Trap("generated Core wire underflow");
        valuesStorage.push_back(item);
    }
    if (!valuesReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.values = valuesStorage.data();
    if (!AssignCoreCount(valuesCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    DiagResult result{};
    state->native->mathExtra->Diag(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(std::bit_cast<std::uint32_t>(result.length), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_encoding_is_valid_base64(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->encoding == nullptr ||
        state->native->encoding->IsValidBase64 == nullptr)
        return Trap("IsValidBase64 generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("IsValidBase64 generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    IsValidBase64Query query{};
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, textCount, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string textStorage(reinterpret_cast<const char*>(textBytes.data()), textBytes.size());
    query.text = textStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    IsValidBase64Result result{};
    state->native->encoding->IsValidBase64(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.valid ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_encoding_is_valid_base64_url(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->encoding == nullptr ||
        state->native->encoding->IsValidBase64Url == nullptr)
        return Trap("IsValidBase64Url generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("IsValidBase64Url generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    IsValidBase64UrlQuery query{};
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, textCount, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string textStorage(reinterpret_cast<const char*>(textBytes.data()), textBytes.size());
    query.text = textStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    IsValidBase64UrlResult result{};
    state->native->encoding->IsValidBase64Url(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.valid ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_input_get_key_code(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->input == nullptr ||
        state->native->input->GetKeyCode == nullptr)
        return Trap("GetKeyCode generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("GetKeyCode generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetKeyCodeQuery query{};
    std::uint32_t keySymPointer = 0;
    std::uint32_t keySymCount = 0;
    if (!reader.U32(keySymPointer) || !reader.U32(keySymCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keySymBytes;
    if (!state->memory.View(keySymPointer, keySymCount, keySymBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string keySymStorage(reinterpret_cast<const char*>(keySymBytes.data()), keySymBytes.size());
    query.keySym = keySymStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetKeyCodeResult result{};
    state->native->input->GetKeyCode(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.keyCode), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_debug_input_emulate_text_editing(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->debugInput == nullptr ||
        state->native->debugInput->EmulateTextEditing == nullptr)
        return Trap("EmulateTextEditing generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("EmulateTextEditing generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    EmulateTextEditingQuery query{};
    std::uint32_t utf8TextPointer = 0;
    std::uint32_t utf8TextCount = 0;
    if (!reader.U32(utf8TextPointer) || !reader.U32(utf8TextCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> utf8TextBytes;
    if (!state->memory.View(utf8TextPointer, utf8TextCount, utf8TextBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string utf8TextStorage(reinterpret_cast<const char*>(utf8TextBytes.data()), utf8TextBytes.size());
    query.utf8Text = utf8TextStorage.c_str();
    query.start = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.start)>>>(slots[0].i32);
    query.length = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.length)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    EmulateTextEditingResult result{};
    state->native->debugInput->EmulateTextEditing(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.consumed ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_debug_input_emulate_text_input(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->debugInput == nullptr ||
        state->native->debugInput->EmulateTextInput == nullptr)
        return Trap("EmulateTextInput generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("EmulateTextInput generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    EmulateTextInputQuery query{};
    std::uint32_t utf8TextPointer = 0;
    std::uint32_t utf8TextCount = 0;
    if (!reader.U32(utf8TextPointer) || !reader.U32(utf8TextCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> utf8TextBytes;
    if (!state->memory.View(utf8TextPointer, utf8TextCount, utf8TextBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string utf8TextStorage(reinterpret_cast<const char*>(utf8TextBytes.data()), utf8TextBytes.size());
    query.utf8Text = utf8TextStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    EmulateTextInputResult result{};
    state->native->debugInput->EmulateTextInput(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.consumed ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_selection_deselect_unit_array(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->selection == nullptr ||
        state->native->selection->DeselectUnitArray == nullptr)
        return Trap("DeselectUnitArray generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("DeselectUnitArray generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    DeselectUnitArrayQuery query{};
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCount = 0;
    if (!reader.U32(unitIDsPointer) || !reader.U32(unitIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t unitIDsBytes64 = static_cast<std::uint64_t>(unitIDsCount) * 4u;
    if (unitIDsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> unitIDsWire;
    if (!state->memory.View(unitIDsPointer, static_cast<std::size_t>(unitIDsBytes64), unitIDsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader unitIDsReader(unitIDsWire);
    std::vector<std::int32_t> unitIDsStorage;
    unitIDsStorage.reserve(unitIDsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < unitIDsCount; ++coreIndex) {
        std::int32_t item{};
        { std::int32_t coreRaw = 0; if (!unitIDsReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        unitIDsStorage.push_back(item);
    }
    if (!unitIDsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.unitIDs = unitIDsStorage.data();
    if (!AssignCoreCount(unitIDsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    DeselectUnitArrayResult result{};
    state->native->selection->DeselectUnitArray(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_selection_select_unit_array(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->selection == nullptr ||
        state->native->selection->SelectUnitArray == nullptr)
        return Trap("SelectUnitArray generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SelectUnitArray generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SelectUnitArrayQuery query{};
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCount = 0;
    if (!reader.U32(unitIDsPointer) || !reader.U32(unitIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t unitIDsBytes64 = static_cast<std::uint64_t>(unitIDsCount) * 4u;
    if (unitIDsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> unitIDsWire;
    if (!state->memory.View(unitIDsPointer, static_cast<std::size_t>(unitIDsBytes64), unitIDsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader unitIDsReader(unitIDsWire);
    std::vector<std::int32_t> unitIDsStorage;
    unitIDsStorage.reserve(unitIDsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < unitIDsCount; ++coreIndex) {
        std::int32_t item{};
        { std::int32_t coreRaw = 0; if (!unitIDsReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        unitIDsStorage.push_back(item);
    }
    if (!unitIDsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.unitIDs = unitIDsStorage.data();
    if (!AssignCoreCount(unitIDsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.append = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SelectUnitArrayResult result{};
    state->native->selection->SelectUnitArray(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_sound_load_sound_def(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->soundApi == nullptr ||
        state->native->soundApi->LoadSoundDef == nullptr)
        return Trap("LoadSoundDef generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("LoadSoundDef generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    LoadSoundDefQuery query{};
    std::uint32_t soundNamePointer = 0;
    std::uint32_t soundNameCount = 0;
    if (!reader.U32(soundNamePointer) || !reader.U32(soundNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> soundNameBytes;
    if (!state->memory.View(soundNamePointer, soundNameCount, soundNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string soundNameStorage(reinterpret_cast<const char*>(soundNameBytes.data()), soundNameBytes.size());
    query.soundName = soundNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    LoadSoundDefResult result{};
    state->native->soundApi->LoadSoundDef(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_sound_play_sound_file(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->soundApi == nullptr ||
        state->native->soundApi->PlaySoundFile == nullptr)
        return Trap("PlaySoundFile generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("PlaySoundFile generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 32u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    PlaySoundFileQuery query{};
    std::uint32_t soundFilePointer = 0;
    std::uint32_t soundFileCount = 0;
    if (!reader.U32(soundFilePointer) || !reader.U32(soundFileCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> soundFileBytes;
    if (!state->memory.View(soundFilePointer, soundFileCount, soundFileBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string soundFileStorage(reinterpret_cast<const char*>(soundFileBytes.data()), soundFileBytes.size());
    query.soundFile = soundFileStorage.c_str();
    query.volume = slots[0].f32;
    if (!reader.F32(query.pos.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.pos.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.pos.z)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.velocity.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.velocity.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.velocity.z)) return Trap("generated Core wire underflow");
    query.channel = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.channel)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    PlaySoundFileResult result{};
    state->native->soundApi->PlaySoundFile(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_sound_play_sound_stream(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->soundApi == nullptr ||
        state->native->soundApi->PlaySoundStream == nullptr)
        return Trap("PlaySoundStream generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("PlaySoundStream generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    PlaySoundStreamQuery query{};
    std::uint32_t oggFilePointer = 0;
    std::uint32_t oggFileCount = 0;
    if (!reader.U32(oggFilePointer) || !reader.U32(oggFileCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> oggFileBytes;
    if (!state->memory.View(oggFilePointer, oggFileCount, oggFileBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string oggFileStorage(reinterpret_cast<const char*>(oggFileBytes.data()), oggFileBytes.size());
    query.oggFile = oggFileStorage.c_str();
    query.volume = slots[0].f32;
    query.enqueue = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    PlaySoundStreamResult result{};
    state->native->soundApi->PlaySoundStream(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_sound_preload_sound_item(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->soundApi == nullptr ||
        state->native->soundApi->PreloadSoundItem == nullptr)
        return Trap("PreloadSoundItem generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("PreloadSoundItem generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    PreloadSoundItemQuery query{};
    std::uint32_t soundNamePointer = 0;
    std::uint32_t soundNameCount = 0;
    if (!reader.U32(soundNamePointer) || !reader.U32(soundNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> soundNameBytes;
    if (!state->memory.View(soundNamePointer, soundNameCount, soundNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string soundNameStorage(reinterpret_cast<const char*>(soundNameBytes.data()), soundNameBytes.size());
    query.soundName = soundNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    PreloadSoundItemResult result{};
    state->native->soundApi->PreloadSoundItem(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_config_set_config_float(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->config == nullptr ||
        state->native->config->SetConfigFloat == nullptr)
        return Trap("SetConfigFloat generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("SetConfigFloat generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetConfigFloatQuery query{};
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, keyCount, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string keyStorage(reinterpret_cast<const char*>(keyBytes.data()), keyBytes.size());
    query.key = keyStorage.c_str();
    query.value = slots[0].f32;
    query.useOverlay = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetConfigFloatResult result{};
    state->native->config->SetConfigFloat(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_config_set_config_int(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->config == nullptr ||
        state->native->config->SetConfigInt == nullptr)
        return Trap("SetConfigInt generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("SetConfigInt generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetConfigIntQuery query{};
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, keyCount, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string keyStorage(reinterpret_cast<const char*>(keyBytes.data()), keyBytes.size());
    query.key = keyStorage.c_str();
    query.value = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.value)>>>(slots[0].i32);
    query.useOverlay = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetConfigIntResult result{};
    state->native->config->SetConfigInt(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_config_set_config_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->config == nullptr ||
        state->native->config->SetConfigString == nullptr)
        return Trap("SetConfigString generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetConfigString generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetConfigStringQuery query{};
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, keyCount, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string keyStorage(reinterpret_cast<const char*>(keyBytes.data()), keyBytes.size());
    query.key = keyStorage.c_str();
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    query.useOverlay = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetConfigStringResult result{};
    state->native->config->SetConfigString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_config_set_log_section_filter_level(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->config == nullptr ||
        state->native->config->SetLogSectionFilterLevel == nullptr)
        return Trap("SetLogSectionFilterLevel generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetLogSectionFilterLevel generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetLogSectionFilterLevelQuery query{};
    std::uint32_t sectionPointer = 0;
    std::uint32_t sectionCount = 0;
    if (!reader.U32(sectionPointer) || !reader.U32(sectionCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> sectionBytes;
    if (!state->memory.View(sectionPointer, sectionCount, sectionBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string sectionStorage(reinterpret_cast<const char*>(sectionBytes.data()), sectionBytes.size());
    query.section = sectionStorage.c_str();
    query.level = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.level)>>>(slots[0].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetLogSectionFilterLevelResult result{};
    state->native->config->SetLogSectionFilterLevel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_utils_get_cegid(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->utils == nullptr ||
        state->native->utils->GetCEGID == nullptr)
        return Trap("GetCEGID generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("GetCEGID generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetCEGIDQuery query{};
    std::uint32_t cegNamePointer = 0;
    std::uint32_t cegNameCount = 0;
    if (!reader.U32(cegNamePointer) || !reader.U32(cegNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> cegNameBytes;
    if (!state->memory.View(cegNamePointer, cegNameCount, cegNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string cegNameStorage(reinterpret_cast<const char*>(cegNameBytes.data()), cegNameBytes.size());
    query.cegName = cegNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetCEGIDResult result{};
    state->native->utils->GetCEGID(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.cegID), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_assign_mouse_cursor(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->AssignMouseCursor == nullptr)
        return Trap("AssignMouseCursor generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("AssignMouseCursor generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    AssignMouseCursorQuery query{};
    std::uint32_t commandNamePointer = 0;
    std::uint32_t commandNameCount = 0;
    if (!reader.U32(commandNamePointer) || !reader.U32(commandNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> commandNameBytes;
    if (!state->memory.View(commandNamePointer, commandNameCount, commandNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string commandNameStorage(reinterpret_cast<const char*>(commandNameBytes.data()), commandNameBytes.size());
    query.commandName = commandNameStorage.c_str();
    std::uint32_t cursorFileNamePointer = 0;
    std::uint32_t cursorFileNameCount = 0;
    if (!reader.U32(cursorFileNamePointer) || !reader.U32(cursorFileNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> cursorFileNameBytes;
    if (!state->memory.View(cursorFileNamePointer, cursorFileNameCount, cursorFileNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string cursorFileNameStorage(reinterpret_cast<const char*>(cursorFileNameBytes.data()), cursorFileNameBytes.size());
    query.cursorFileName = cursorFileNameStorage.c_str();
    query.overwrite = slots[0].i32 != 0;
    query.hotSpotTopLeft = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    AssignMouseCursorResult result{};
    state->native->unsyncedCtrl->AssignMouseCursor(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_deselect_unit_map(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->DeselectUnitMap == nullptr)
        return Trap("DeselectUnitMap generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("DeselectUnitMap generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    DeselectUnitMapQuery query{};
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCount = 0;
    if (!reader.U32(unitIDsPointer) || !reader.U32(unitIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t unitIDsBytes64 = static_cast<std::uint64_t>(unitIDsCount) * 4u;
    if (unitIDsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> unitIDsWire;
    if (!state->memory.View(unitIDsPointer, static_cast<std::size_t>(unitIDsBytes64), unitIDsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader unitIDsReader(unitIDsWire);
    std::vector<std::int32_t> unitIDsStorage;
    unitIDsStorage.reserve(unitIDsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < unitIDsCount; ++coreIndex) {
        std::int32_t item{};
        { std::int32_t coreRaw = 0; if (!unitIDsReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        unitIDsStorage.push_back(item);
    }
    if (!unitIDsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.unitIDs = unitIDsStorage.data();
    if (!AssignCoreCount(unitIDsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    DeselectUnitMapResult result{};
    state->native->unsyncedCtrl->DeselectUnitMap(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_draw_unit_commands(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->DrawUnitCommands == nullptr)
        return Trap("DrawUnitCommands generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("DrawUnitCommands generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    DrawUnitCommandsQuery query{};
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCount = 0;
    if (!reader.U32(unitIDsPointer) || !reader.U32(unitIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t unitIDsBytes64 = static_cast<std::uint64_t>(unitIDsCount) * 4u;
    if (unitIDsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> unitIDsWire;
    if (!state->memory.View(unitIDsPointer, static_cast<std::size_t>(unitIDsBytes64), unitIDsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader unitIDsReader(unitIDsWire);
    std::vector<std::int32_t> unitIDsStorage;
    unitIDsStorage.reserve(unitIDsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < unitIDsCount; ++coreIndex) {
        std::int32_t item{};
        { std::int32_t coreRaw = 0; if (!unitIDsReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        unitIDsStorage.push_back(item);
    }
    if (!unitIDsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.unitIDs = unitIDsStorage.data();
    if (!AssignCoreCount(unitIDsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.tableOrArray = slots[0].i32 != 0;
    query.queueDrawDepth = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.queueDrawDepth)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    DrawUnitCommandsResult result{};
    state->native->unsyncedCtrl->DrawUnitCommands(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_load_cmd_colors_config(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->LoadCmdColorsConfig == nullptr)
        return Trap("LoadCmdColorsConfig generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("LoadCmdColorsConfig generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    LoadCmdColorsConfigQuery query{};
    std::uint32_t filenamePointer = 0;
    std::uint32_t filenameCount = 0;
    if (!reader.U32(filenamePointer) || !reader.U32(filenameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> filenameBytes;
    if (!state->memory.View(filenamePointer, filenameCount, filenameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string filenameStorage(reinterpret_cast<const char*>(filenameBytes.data()), filenameBytes.size());
    query.filename = filenameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    LoadCmdColorsConfigResult result{};
    state->native->unsyncedCtrl->LoadCmdColorsConfig(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_load_ctrl_panel_config(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->LoadCtrlPanelConfig == nullptr)
        return Trap("LoadCtrlPanelConfig generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("LoadCtrlPanelConfig generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    LoadCtrlPanelConfigQuery query{};
    std::uint32_t filenamePointer = 0;
    std::uint32_t filenameCount = 0;
    if (!reader.U32(filenamePointer) || !reader.U32(filenameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> filenameBytes;
    if (!state->memory.View(filenamePointer, filenameCount, filenameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string filenameStorage(reinterpret_cast<const char*>(filenameBytes.data()), filenameBytes.size());
    query.filename = filenameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    LoadCtrlPanelConfigResult result{};
    state->native->unsyncedCtrl->LoadCtrlPanelConfig(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_load_model_textures(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->LoadModelTextures == nullptr)
        return Trap("LoadModelTextures generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("LoadModelTextures generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    LoadModelTexturesQuery query{};
    std::uint32_t modelNamePointer = 0;
    std::uint32_t modelNameCount = 0;
    if (!reader.U32(modelNamePointer) || !reader.U32(modelNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> modelNameBytes;
    if (!state->memory.View(modelNamePointer, modelNameCount, modelNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string modelNameStorage(reinterpret_cast<const char*>(modelNameBytes.data()), modelNameBytes.size());
    query.modelName = modelNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    LoadModelTexturesResult result{};
    state->native->unsyncedCtrl->LoadModelTextures(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_replace_mouse_cursor(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->ReplaceMouseCursor == nullptr)
        return Trap("ReplaceMouseCursor generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ReplaceMouseCursor generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    ReplaceMouseCursorQuery query{};
    std::uint32_t oldCursorFileNamePointer = 0;
    std::uint32_t oldCursorFileNameCount = 0;
    if (!reader.U32(oldCursorFileNamePointer) || !reader.U32(oldCursorFileNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> oldCursorFileNameBytes;
    if (!state->memory.View(oldCursorFileNamePointer, oldCursorFileNameCount, oldCursorFileNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string oldCursorFileNameStorage(reinterpret_cast<const char*>(oldCursorFileNameBytes.data()), oldCursorFileNameBytes.size());
    query.oldCursorFileName = oldCursorFileNameStorage.c_str();
    std::uint32_t newCursorFileNamePointer = 0;
    std::uint32_t newCursorFileNameCount = 0;
    if (!reader.U32(newCursorFileNamePointer) || !reader.U32(newCursorFileNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> newCursorFileNameBytes;
    if (!state->memory.View(newCursorFileNamePointer, newCursorFileNameCount, newCursorFileNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string newCursorFileNameStorage(reinterpret_cast<const char*>(newCursorFileNameBytes.data()), newCursorFileNameBytes.size());
    query.newCursorFileName = newCursorFileNameStorage.c_str();
    query.hotSpotTopLeft = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    ReplaceMouseCursorResult result{};
    state->native->unsyncedCtrl->ReplaceMouseCursor(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_select_unit_map(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SelectUnitMap == nullptr)
        return Trap("SelectUnitMap generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SelectUnitMap generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SelectUnitMapQuery query{};
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCount = 0;
    if (!reader.U32(unitIDsPointer) || !reader.U32(unitIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t unitIDsBytes64 = static_cast<std::uint64_t>(unitIDsCount) * 4u;
    if (unitIDsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> unitIDsWire;
    if (!state->memory.View(unitIDsPointer, static_cast<std::size_t>(unitIDsBytes64), unitIDsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader unitIDsReader(unitIDsWire);
    std::vector<std::int32_t> unitIDsStorage;
    unitIDsStorage.reserve(unitIDsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < unitIDsCount; ++coreIndex) {
        std::int32_t item{};
        { std::int32_t coreRaw = 0; if (!unitIDsReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        unitIDsStorage.push_back(item);
    }
    if (!unitIDsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.unitIDs = unitIDsStorage.data();
    if (!AssignCoreCount(unitIDsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.append = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SelectUnitMapResult result{};
    state->native->unsyncedCtrl->SelectUnitMap(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_set_clipboard(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetClipboard == nullptr)
        return Trap("SetClipboard generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("SetClipboard generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetClipboardQuery query{};
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, textCount, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string textStorage(reinterpret_cast<const char*>(textBytes.data()), textBytes.size());
    query.text = textStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetClipboardResult result{};
    state->native->unsyncedCtrl->SetClipboard(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_set_dolly_camera_curve(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetDollyCameraCurve == nullptr)
        return Trap("SetDollyCameraCurve generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetDollyCameraCurve generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetDollyCameraCurveQuery query{};
    query.degree = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.degree)>>>(slots[0].i32);
    std::uint32_t controlPointsPointer = 0;
    std::uint32_t controlPointsCount = 0;
    if (!reader.U32(controlPointsPointer) || !reader.U32(controlPointsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t controlPointsBytes64 = static_cast<std::uint64_t>(controlPointsCount) * 16u;
    if (controlPointsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> controlPointsWire;
    if (!state->memory.View(controlPointsPointer, static_cast<std::size_t>(controlPointsBytes64), controlPointsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader controlPointsReader(controlPointsWire);
    std::vector<Float4> controlPointsStorage;
    controlPointsStorage.reserve(controlPointsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < controlPointsCount; ++coreIndex) {
        Float4 item{};
        if (!controlPointsReader.F32(item.x)) return Trap("generated Core wire underflow");
        if (!controlPointsReader.F32(item.y)) return Trap("generated Core wire underflow");
        if (!controlPointsReader.F32(item.z)) return Trap("generated Core wire underflow");
        if (!controlPointsReader.F32(item.w)) return Trap("generated Core wire underflow");
        controlPointsStorage.push_back(item);
    }
    if (!controlPointsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.controlPoints = controlPointsStorage.data();
    if (!AssignCoreCount(controlPointsCount, query.controlPointsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::uint32_t knotsPointer = 0;
    std::uint32_t knotsCount = 0;
    if (!reader.U32(knotsPointer) || !reader.U32(knotsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t knotsBytes64 = static_cast<std::uint64_t>(knotsCount) * 4u;
    if (knotsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> knotsWire;
    if (!state->memory.View(knotsPointer, static_cast<std::size_t>(knotsBytes64), knotsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader knotsReader(knotsWire);
    std::vector<float> knotsStorage;
    knotsStorage.reserve(knotsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < knotsCount; ++coreIndex) {
        float item{};
        if (!knotsReader.F32(item)) return Trap("generated Core wire underflow");
        knotsStorage.push_back(item);
    }
    if (!knotsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.knots = knotsStorage.data();
    if (!AssignCoreCount(knotsCount, query.knotsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetDollyCameraCurveResult result{};
    state->native->unsyncedCtrl->SetDollyCameraCurve(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_set_dolly_camera_look_curve(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetDollyCameraLookCurve == nullptr)
        return Trap("SetDollyCameraLookCurve generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetDollyCameraLookCurve generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetDollyCameraLookCurveQuery query{};
    query.degree = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.degree)>>>(slots[0].i32);
    std::uint32_t controlPointsPointer = 0;
    std::uint32_t controlPointsCount = 0;
    if (!reader.U32(controlPointsPointer) || !reader.U32(controlPointsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t controlPointsBytes64 = static_cast<std::uint64_t>(controlPointsCount) * 16u;
    if (controlPointsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> controlPointsWire;
    if (!state->memory.View(controlPointsPointer, static_cast<std::size_t>(controlPointsBytes64), controlPointsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader controlPointsReader(controlPointsWire);
    std::vector<Float4> controlPointsStorage;
    controlPointsStorage.reserve(controlPointsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < controlPointsCount; ++coreIndex) {
        Float4 item{};
        if (!controlPointsReader.F32(item.x)) return Trap("generated Core wire underflow");
        if (!controlPointsReader.F32(item.y)) return Trap("generated Core wire underflow");
        if (!controlPointsReader.F32(item.z)) return Trap("generated Core wire underflow");
        if (!controlPointsReader.F32(item.w)) return Trap("generated Core wire underflow");
        controlPointsStorage.push_back(item);
    }
    if (!controlPointsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.controlPoints = controlPointsStorage.data();
    if (!AssignCoreCount(controlPointsCount, query.controlPointsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::uint32_t knotsPointer = 0;
    std::uint32_t knotsCount = 0;
    if (!reader.U32(knotsPointer) || !reader.U32(knotsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t knotsBytes64 = static_cast<std::uint64_t>(knotsCount) * 4u;
    if (knotsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> knotsWire;
    if (!state->memory.View(knotsPointer, static_cast<std::size_t>(knotsBytes64), knotsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader knotsReader(knotsWire);
    std::vector<float> knotsStorage;
    knotsStorage.reserve(knotsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < knotsCount; ++coreIndex) {
        float item{};
        if (!knotsReader.F32(item)) return Trap("generated Core wire underflow");
        knotsStorage.push_back(item);
    }
    if (!knotsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.knots = knotsStorage.data();
    if (!AssignCoreCount(knotsCount, query.knotsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetDollyCameraLookCurveResult result{};
    state->native->unsyncedCtrl->SetDollyCameraLookCurve(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_set_map_shading_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetMapShadingTexture == nullptr)
        return Trap("SetMapShadingTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetMapShadingTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetMapShadingTextureQuery query{};
    std::uint32_t texTypePointer = 0;
    std::uint32_t texTypeCount = 0;
    if (!reader.U32(texTypePointer) || !reader.U32(texTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> texTypeBytes;
    if (!state->memory.View(texTypePointer, texTypeCount, texTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string texTypeStorage(reinterpret_cast<const char*>(texTypeBytes.data()), texTypeBytes.size());
    query.texType = texTypeStorage.c_str();
    std::uint32_t texNamePointer = 0;
    std::uint32_t texNameCount = 0;
    if (!reader.U32(texNamePointer) || !reader.U32(texNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> texNameBytes;
    if (!state->memory.View(texNamePointer, texNameCount, texNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string texNameStorage(reinterpret_cast<const char*>(texNameBytes.data()), texNameBytes.size());
    query.texName = texNameStorage.c_str();
    query.num = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.num)>>>(slots[0].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetMapShadingTextureResult result{};
    state->native->unsyncedCtrl->SetMapShadingTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_set_mouse_cursor(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetMouseCursor == nullptr)
        return Trap("SetMouseCursor generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetMouseCursor generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetMouseCursorQuery query{};
    std::uint32_t cursorNamePointer = 0;
    std::uint32_t cursorNameCount = 0;
    if (!reader.U32(cursorNamePointer) || !reader.U32(cursorNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> cursorNameBytes;
    if (!state->memory.View(cursorNamePointer, cursorNameCount, cursorNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string cursorNameStorage(reinterpret_cast<const char*>(cursorNameBytes.data()), cursorNameBytes.size());
    query.cursorName = cursorNameStorage.c_str();
    query.scale = slots[0].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetMouseCursorResult result{};
    state->native->unsyncedCtrl->SetMouseCursor(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_set_sky_box_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetSkyBoxTexture == nullptr)
        return Trap("SetSkyBoxTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("SetSkyBoxTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetSkyBoxTextureQuery query{};
    std::uint32_t texNamePointer = 0;
    std::uint32_t texNameCount = 0;
    if (!reader.U32(texNamePointer) || !reader.U32(texNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> texNameBytes;
    if (!state->memory.View(texNamePointer, texNameCount, texNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string texNameStorage(reinterpret_cast<const char*>(texNameBytes.data()), texNameBytes.size());
    query.texName = texNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetSkyBoxTextureResult result{};
    state->native->unsyncedCtrl->SetSkyBoxTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_set_unit_def_icon(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetUnitDefIcon == nullptr)
        return Trap("SetUnitDefIcon generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetUnitDefIcon generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitDefIconQuery query{};
    query.unitDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitDefID)>>>(slots[0].i32);
    std::uint32_t iconNamePointer = 0;
    std::uint32_t iconNameCount = 0;
    if (!reader.U32(iconNamePointer) || !reader.U32(iconNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> iconNameBytes;
    if (!state->memory.View(iconNamePointer, iconNameCount, iconNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string iconNameStorage(reinterpret_cast<const char*>(iconNameBytes.data()), iconNameBytes.size());
    query.iconName = iconNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitDefIconResult result{};
    state->native->unsyncedCtrl->SetUnitDefIcon(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_set_unit_def_image(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetUnitDefImage == nullptr)
        return Trap("SetUnitDefImage generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetUnitDefImage generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitDefImageQuery query{};
    query.unitDefID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitDefID)>>>(slots[0].i32);
    std::uint32_t imagePointer = 0;
    std::uint32_t imageCount = 0;
    if (!reader.U32(imagePointer) || !reader.U32(imageCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> imageBytes;
    if (!state->memory.View(imagePointer, imageCount, imageBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string imageStorage(reinterpret_cast<const char*>(imageBytes.data()), imageBytes.size());
    query.image = imageStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitDefImageResult result{};
    state->native->unsyncedCtrl->SetUnitDefImage(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_set_unit_icon(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetUnitIcon == nullptr)
        return Trap("SetUnitIcon generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetUnitIcon generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitIconQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t iconNamePointer = 0;
    std::uint32_t iconNameCount = 0;
    if (!reader.U32(iconNamePointer) || !reader.U32(iconNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> iconNameBytes;
    if (!state->memory.View(iconNamePointer, iconNameCount, iconNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string iconNameStorage(reinterpret_cast<const char*>(iconNameBytes.data()), iconNameBytes.size());
    query.iconName = iconNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitIconResult result{};
    state->native->unsyncedCtrl->SetUnitIcon(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_set_wm_caption(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetWMCaption == nullptr)
        return Trap("SetWMCaption generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("SetWMCaption generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetWMCaptionQuery query{};
    std::uint32_t titlePointer = 0;
    std::uint32_t titleCount = 0;
    if (!reader.U32(titlePointer) || !reader.U32(titleCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> titleBytes;
    if (!state->memory.View(titlePointer, titleCount, titleBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string titleStorage(reinterpret_cast<const char*>(titleBytes.data()), titleBytes.size());
    query.title = titleStorage.c_str();
    std::uint32_t titleShortPointer = 0;
    std::uint32_t titleShortCount = 0;
    if (!reader.U32(titleShortPointer) || !reader.U32(titleShortCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> titleShortBytes;
    if (!state->memory.View(titleShortPointer, titleShortCount, titleShortBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string titleShortStorage(reinterpret_cast<const char*>(titleShortBytes.data()), titleShortBytes.size());
    query.titleShort = titleShortStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetWMCaptionResult result{};
    state->native->unsyncedCtrl->SetWMCaption(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_set_wm_icon(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetWMIcon == nullptr)
        return Trap("SetWMIcon generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetWMIcon generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetWMIconQuery query{};
    std::uint32_t iconFileNamePointer = 0;
    std::uint32_t iconFileNameCount = 0;
    if (!reader.U32(iconFileNamePointer) || !reader.U32(iconFileNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> iconFileNameBytes;
    if (!state->memory.View(iconFileNamePointer, iconFileNameCount, iconFileNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string iconFileNameStorage(reinterpret_cast<const char*>(iconFileNameBytes.data()), iconFileNameBytes.size());
    query.iconFileName = iconFileNameStorage.c_str();
    query.forceResolution = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetWMIconResult result{};
    state->native->unsyncedCtrl->SetWMIcon(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unsynced_ctrl_set_water_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->unsyncedCtrl == nullptr ||
        state->native->unsyncedCtrl->SetWaterTexture == nullptr)
        return Trap("SetWaterTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("SetWaterTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetWaterTextureQuery query{};
    std::uint32_t texTypePointer = 0;
    std::uint32_t texTypeCount = 0;
    if (!reader.U32(texTypePointer) || !reader.U32(texTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> texTypeBytes;
    if (!state->memory.View(texTypePointer, texTypeCount, texTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string texTypeStorage(reinterpret_cast<const char*>(texTypeBytes.data()), texTypeBytes.size());
    query.texType = texTypeStorage.c_str();
    std::uint32_t texNamePointer = 0;
    std::uint32_t texNameCount = 0;
    if (!reader.U32(texNamePointer) || !reader.U32(texNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> texNameBytes;
    if (!state->memory.View(texNamePointer, texNameCount, texNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string texNameStorage(reinterpret_cast<const char*>(texNameBytes.data()), texNameBytes.size());
    query.texName = texNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetWaterTextureResult result{};
    state->native->unsyncedCtrl->SetWaterTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_add_atlas_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->AddAtlasTexture == nullptr)
        return Trap("AddAtlasTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("AddAtlasTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxAtlasTextureQuery query{};
    std::uint32_t atlasNamePointer = 0;
    std::uint32_t atlasNameCount = 0;
    if (!reader.U32(atlasNamePointer) || !reader.U32(atlasNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> atlasNameBytes;
    if (!state->memory.View(atlasNamePointer, atlasNameCount, atlasNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string atlasNameStorage(reinterpret_cast<const char*>(atlasNameBytes.data()), atlasNameBytes.size());
    query.atlasName = atlasNameStorage.c_str();
    std::uint32_t textureNamePointer = 0;
    std::uint32_t textureNameCount = 0;
    if (!reader.U32(textureNamePointer) || !reader.U32(textureNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textureNameBytes;
    if (!state->memory.View(textureNamePointer, textureNameCount, textureNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string textureNameStorage(reinterpret_cast<const char*>(textureNameBytes.data()), textureNameBytes.size());
    query.textureName = textureNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->AddAtlasTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_add_fallback_font(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->AddFallbackFont == nullptr)
        return Trap("AddFallbackFont generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("AddFallbackFont generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxStringQuery query{};
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->AddFallbackFont(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_add_feature_defs_to_submission_vao(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->AddFeatureDefsToSubmissionVAO == nullptr)
        return Trap("AddFeatureDefsToSubmissionVAO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("AddFeatureDefsToSubmissionVAO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVAOSubmissionQuery query{};
    query.vaoID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vaoID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
    if (idsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> idsWire;
    if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader idsReader(idsWire);
    std::vector<std::uint32_t> idsStorage;
    idsStorage.reserve(idsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < idsCount; ++coreIndex) {
        std::uint32_t item{};
        { std::uint32_t coreRaw = 0; if (!idsReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        idsStorage.push_back(item);
    }
    if (!idsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.ids = idsStorage.data();
    if (!AssignCoreCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->AddFeatureDefsToSubmissionVAO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_add_features_to_submission_vao(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->AddFeaturesToSubmissionVAO == nullptr)
        return Trap("AddFeaturesToSubmissionVAO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("AddFeaturesToSubmissionVAO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVAOSubmissionQuery query{};
    query.vaoID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vaoID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
    if (idsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> idsWire;
    if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader idsReader(idsWire);
    std::vector<std::uint32_t> idsStorage;
    idsStorage.reserve(idsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < idsCount; ++coreIndex) {
        std::uint32_t item{};
        { std::uint32_t coreRaw = 0; if (!idsReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        idsStorage.push_back(item);
    }
    if (!idsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.ids = idsStorage.data();
    if (!AssignCoreCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->AddFeaturesToSubmissionVAO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_add_unit_defs_to_submission_vao(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->AddUnitDefsToSubmissionVAO == nullptr)
        return Trap("AddUnitDefsToSubmissionVAO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("AddUnitDefsToSubmissionVAO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVAOSubmissionQuery query{};
    query.vaoID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vaoID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
    if (idsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> idsWire;
    if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader idsReader(idsWire);
    std::vector<std::uint32_t> idsStorage;
    idsStorage.reserve(idsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < idsCount; ++coreIndex) {
        std::uint32_t item{};
        { std::uint32_t coreRaw = 0; if (!idsReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        idsStorage.push_back(item);
    }
    if (!idsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.ids = idsStorage.data();
    if (!AssignCoreCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->AddUnitDefsToSubmissionVAO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_add_units_to_submission_vao(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->AddUnitsToSubmissionVAO == nullptr)
        return Trap("AddUnitsToSubmissionVAO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("AddUnitsToSubmissionVAO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVAOSubmissionQuery query{};
    query.vaoID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vaoID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
    if (idsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> idsWire;
    if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader idsReader(idsWire);
    std::vector<std::uint32_t> idsStorage;
    idsStorage.reserve(idsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < idsCount; ++coreIndex) {
        std::uint32_t item{};
        { std::uint32_t coreRaw = 0; if (!idsReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        idsStorage.push_back(item);
    }
    if (!idsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.ids = idsStorage.data();
    if (!AssignCoreCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->AddUnitsToSubmissionVAO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_bind_image_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->BindImageTexture == nullptr)
        return Trap("BindImageTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 7)
        return Trap("BindImageTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 8u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[6].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxBindImageTextureQuery query{};
    query.unit = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unit)>>>(slots[0].i32);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.level = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.level)>>>(slots[1].i32);
    query.layer = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.layer)>>>(slots[2].i32);
    query.layered = slots[3].i32 != 0;
    query.access = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.access)>>>(slots[4].i32);
    query.format = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.format)>>>(slots[5].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->BindImageTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_bind_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->BindTexture == nullptr)
        return Trap("BindTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("BindTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextureBindQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.texNum = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.texNum)>>>(slots[0].i32);
    query.enable = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->BindTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_change_texture_params(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->ChangeTextureParams == nullptr)
        return Trap("ChangeTextureParams generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("ChangeTextureParams generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 64u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxChangeTextureParamsQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("generated Core wire underflow"); query.params.target = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.target)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("generated Core wire underflow"); query.params.format = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.format)>>>(coreRaw); }
    { std::int32_t coreRaw = 0; if (!reader.I32(coreRaw)) return Trap("generated Core wire underflow"); query.params.border = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.border)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("generated Core wire underflow"); query.params.minFilter = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.minFilter)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("generated Core wire underflow"); query.params.magFilter = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.magFilter)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("generated Core wire underflow"); query.params.wrapS = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.wrapS)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("generated Core wire underflow"); query.params.wrapT = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.wrapT)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("generated Core wire underflow"); query.params.wrapR = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.wrapR)>>>(coreRaw); }
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("generated Core wire underflow"); query.params.compareFunc = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.compareFunc)>>>(coreRaw); }
    if (!reader.F32(query.params.lodBias)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.params.aniso)) return Trap("generated Core wire underflow");
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("generated Core wire underflow"); query.params.samples = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.params.samples)>>>(coreRaw); }
    if (!reader.Bool(query.params.fbo)) return Trap("generated Core wire underflow");
    if (!reader.Bool(query.params.fboDepth)) return Trap("generated Core wire underflow");
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->ChangeTextureParams(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_copy_to_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->CopyToTexture == nullptr)
        return Trap("CopyToTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 9)
        return Trap("CopyToTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 10u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[8].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxCopyToTextureQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.xoff = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.xoff)>>>(slots[0].i32);
    query.yoff = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.yoff)>>>(slots[1].i32);
    query.x = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.x)>>>(slots[2].i32);
    query.y = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.y)>>>(slots[3].i32);
    query.width = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.width)>>>(slots[4].i32);
    query.height = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.height)>>>(slots[5].i32);
    query.target = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.target)>>>(slots[6].i32);
    query.level = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.level)>>>(slots[7].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->CopyToTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_create_shader(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->CreateShader == nullptr)
        return Trap("CreateShader generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("CreateShader generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 80u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxCreateShaderQuery query{};
    std::uint32_t definitionsPointer = 0;
    std::uint32_t definitionsCount = 0;
    if (!reader.U32(definitionsPointer) || !reader.U32(definitionsCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> definitionsBytes;
    if (!state->memory.View(definitionsPointer, definitionsCount, definitionsBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string definitionsStorage(reinterpret_cast<const char*>(definitionsBytes.data()), definitionsBytes.size());
    query.definitions = definitionsStorage.c_str();
    std::uint32_t vertexPointer = 0;
    std::uint32_t vertexCount = 0;
    if (!reader.U32(vertexPointer) || !reader.U32(vertexCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> vertexBytes;
    if (!state->memory.View(vertexPointer, vertexCount, vertexBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string vertexStorage(reinterpret_cast<const char*>(vertexBytes.data()), vertexBytes.size());
    query.vertex = vertexStorage.c_str();
    std::uint32_t tcsPointer = 0;
    std::uint32_t tcsCount = 0;
    if (!reader.U32(tcsPointer) || !reader.U32(tcsCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> tcsBytes;
    if (!state->memory.View(tcsPointer, tcsCount, tcsBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string tcsStorage(reinterpret_cast<const char*>(tcsBytes.data()), tcsBytes.size());
    query.tcs = tcsStorage.c_str();
    std::uint32_t tesPointer = 0;
    std::uint32_t tesCount = 0;
    if (!reader.U32(tesPointer) || !reader.U32(tesCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> tesBytes;
    if (!state->memory.View(tesPointer, tesCount, tesBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string tesStorage(reinterpret_cast<const char*>(tesBytes.data()), tesBytes.size());
    query.tes = tesStorage.c_str();
    std::uint32_t geometryPointer = 0;
    std::uint32_t geometryCount = 0;
    if (!reader.U32(geometryPointer) || !reader.U32(geometryCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> geometryBytes;
    if (!state->memory.View(geometryPointer, geometryCount, geometryBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string geometryStorage(reinterpret_cast<const char*>(geometryBytes.data()), geometryBytes.size());
    query.geometry = geometryStorage.c_str();
    std::uint32_t fragmentPointer = 0;
    std::uint32_t fragmentCount = 0;
    if (!reader.U32(fragmentPointer) || !reader.U32(fragmentCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> fragmentBytes;
    if (!state->memory.View(fragmentPointer, fragmentCount, fragmentBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string fragmentStorage(reinterpret_cast<const char*>(fragmentBytes.data()), fragmentBytes.size());
    query.fragment = fragmentStorage.c_str();
    std::uint32_t computePointer = 0;
    std::uint32_t computeCount = 0;
    if (!reader.U32(computePointer) || !reader.U32(computeCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> computeBytes;
    if (!state->memory.View(computePointer, computeCount, computeBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string computeStorage(reinterpret_cast<const char*>(computeBytes.data()), computeBytes.size());
    query.compute = computeStorage.c_str();
    if (!reader.Bool(query.options.hasGeoInputType)) return Trap("generated Core wire underflow");
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("generated Core wire underflow"); query.options.geoInputType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options.geoInputType)>>>(coreRaw); }
    if (!reader.Bool(query.options.hasGeoOutputType)) return Trap("generated Core wire underflow");
    { std::uint32_t coreRaw = 0; if (!reader.U32(coreRaw)) return Trap("generated Core wire underflow"); query.options.geoOutputType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options.geoOutputType)>>>(coreRaw); }
    if (!reader.Bool(query.options.hasGeoOutputVerts)) return Trap("generated Core wire underflow");
    { std::int32_t coreRaw = 0; if (!reader.I32(coreRaw)) return Trap("generated Core wire underflow"); query.options.geoOutputVerts = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options.geoOutputVerts)>>>(coreRaw); }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxCreateShaderResult result{};
    state->native->gfx->CreateShader(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.U32(result.shaderID)) return Trap("generated Core wire overflow");
    if (!writer.U32(result.glProgramID)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_define_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->DefineVBO == nullptr)
        return Trap("DefineVBO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 7)
        return Trap("DefineVBO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 8u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[6].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBODefineQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    query.elementsCount = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementsCount)>>>(slots[1].i32);
    query.elementArray = slots[2].i32 != 0;
    query.indexType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.indexType)>>>(slots[3].i32);
    query.useDefaultAttributes = slots[4].i32 != 0;
    query.defaultAttributeCount = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.defaultAttributeCount)>>>(slots[5].i32);
    std::uint32_t attributesPointer = 0;
    std::uint32_t attributesCount = 0;
    if (!reader.U32(attributesPointer) || !reader.U32(attributesCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t attributesBytes64 = static_cast<std::uint64_t>(attributesCount) * 16u;
    if (attributesBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> attributesWire;
    if (!state->memory.View(attributesPointer, static_cast<std::size_t>(attributesBytes64), attributesWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader attributesReader(attributesWire);
    std::vector<GfxVBOAttributeOptions> attributesStorage;
    attributesStorage.reserve(attributesCount);
    for (std::uint32_t coreIndex = 0; coreIndex < attributesCount; ++coreIndex) {
        GfxVBOAttributeOptions item{};
        { std::int32_t coreRaw = 0; if (!attributesReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item.id = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item.id)>>>(coreRaw); }
        { std::uint32_t coreRaw = 0; if (!attributesReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item.type = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item.type)>>>(coreRaw); }
        { std::int32_t coreRaw = 0; if (!attributesReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item.size = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item.size)>>>(coreRaw); }
        if (!attributesReader.Bool(item.normalized)) return Trap("generated Core wire underflow");
        attributesStorage.push_back(item);
    }
    if (!attributesReader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.attributes = attributesStorage.data();
    if (!AssignCoreCount(attributesCount, query.attributeCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->DefineVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_delete_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->DeleteTexture == nullptr)
        return Trap("DeleteTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("DeleteTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextureNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->DeleteTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_delete_texture_atlas(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->DeleteTextureAtlas == nullptr)
        return Trap("DeleteTextureAtlas generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("DeleteTextureAtlas generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextureNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->DeleteTextureAtlas(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_delete_texture_fbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->DeleteTextureFBO == nullptr)
        return Trap("DeleteTextureFBO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("DeleteTextureFBO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextureNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->DeleteTextureFBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_finalize_texture_atlas(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->FinalizeTextureAtlas == nullptr)
        return Trap("FinalizeTextureAtlas generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("FinalizeTextureAtlas generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextureNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->FinalizeTextureAtlas(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_font_get_text_height(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->FontGetTextHeight == nullptr)
        return Trap("FontGetTextHeight generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 6)
        return Trap("FontGetTextHeight generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 7u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[5].i32);
    if (!state->memory.Contains(output, 12u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxFontTextQuery query{};
    query.fontID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fontID)>>>(slots[0].i32);
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, textCount, textBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string textStorage(reinterpret_cast<const char*>(textBytes.data()), textBytes.size());
    query.text = textStorage.c_str();
    query.x = slots[1].f32;
    query.y = slots[2].f32;
    query.size = slots[3].f32;
    std::uint32_t optionsPointer = 0;
    std::uint32_t optionsCount = 0;
    if (!reader.U32(optionsPointer) || !reader.U32(optionsCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> optionsBytes;
    if (!state->memory.View(optionsPointer, optionsCount, optionsBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string optionsStorage(reinterpret_cast<const char*>(optionsBytes.data()), optionsBytes.size());
    query.options = optionsStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxTextHeightResult result{};
    state->native->gfx->FontGetTextHeight(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 12> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.height)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.descender)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.lines)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_font_get_text_width(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->FontGetTextWidth == nullptr)
        return Trap("FontGetTextWidth generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 5)
        return Trap("FontGetTextWidth generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxFontTextQuery query{};
    query.fontID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fontID)>>>(slots[0].i32);
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, textCount, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string textStorage(reinterpret_cast<const char*>(textBytes.data()), textBytes.size());
    query.text = textStorage.c_str();
    query.x = slots[1].f32;
    query.y = slots[2].f32;
    query.size = slots[3].f32;
    std::uint32_t optionsPointer = 0;
    std::uint32_t optionsCount = 0;
    if (!reader.U32(optionsPointer) || !reader.U32(optionsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> optionsBytes;
    if (!state->memory.View(optionsPointer, optionsCount, optionsBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string optionsStorage(reinterpret_cast<const char*>(optionsBytes.data()), optionsBytes.size());
    query.options = optionsStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxFloatResult result{};
    state->native->gfx->FontGetTextWidth(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(std::bit_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_font_print(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->FontPrint == nullptr)
        return Trap("FontPrint generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 5)
        return Trap("FontPrint generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxFontTextQuery query{};
    query.fontID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fontID)>>>(slots[0].i32);
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, textCount, textBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string textStorage(reinterpret_cast<const char*>(textBytes.data()), textBytes.size());
    query.text = textStorage.c_str();
    query.x = slots[1].f32;
    query.y = slots[2].f32;
    query.size = slots[3].f32;
    std::uint32_t optionsPointer = 0;
    std::uint32_t optionsCount = 0;
    if (!reader.U32(optionsPointer) || !reader.U32(optionsCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> optionsBytes;
    if (!state->memory.View(optionsPointer, optionsCount, optionsBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string optionsStorage(reinterpret_cast<const char*>(optionsBytes.data()), optionsBytes.size());
    query.options = optionsStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->FontPrint(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_font_print_world(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->FontPrintWorld == nullptr)
        return Trap("FontPrintWorld generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("FontPrintWorld generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 28u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxFontWorldTextQuery query{};
    query.fontID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fontID)>>>(slots[0].i32);
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, textCount, textBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string textStorage(reinterpret_cast<const char*>(textBytes.data()), textBytes.size());
    query.text = textStorage.c_str();
    if (!reader.F32(query.pos.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.pos.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.pos.z)) return Trap("generated Core wire underflow");
    query.size = slots[1].f32;
    std::uint32_t optionsPointer = 0;
    std::uint32_t optionsCount = 0;
    if (!reader.U32(optionsPointer) || !reader.U32(optionsCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> optionsBytes;
    if (!state->memory.View(optionsPointer, optionsCount, optionsBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string optionsStorage(reinterpret_cast<const char*>(optionsBytes.data()), optionsBytes.size());
    query.options = optionsStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->FontPrintWorld(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_generate_mipmap(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GenerateMipmap == nullptr)
        return Trap("GenerateMipmap generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("GenerateMipmap generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextureNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->GenerateMipmap(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_get_atlas_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetAtlasTexture == nullptr)
        return Trap("GetAtlasTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetAtlasTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 20u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxAtlasTextureQuery query{};
    std::uint32_t atlasNamePointer = 0;
    std::uint32_t atlasNameCount = 0;
    if (!reader.U32(atlasNamePointer) || !reader.U32(atlasNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> atlasNameBytes;
    if (!state->memory.View(atlasNamePointer, atlasNameCount, atlasNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string atlasNameStorage(reinterpret_cast<const char*>(atlasNameBytes.data()), atlasNameBytes.size());
    query.atlasName = atlasNameStorage.c_str();
    std::uint32_t textureNamePointer = 0;
    std::uint32_t textureNameCount = 0;
    if (!reader.U32(textureNamePointer) || !reader.U32(textureNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textureNameBytes;
    if (!state->memory.View(textureNamePointer, textureNameCount, textureNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string textureNameStorage(reinterpret_cast<const char*>(textureNameBytes.data()), textureNameBytes.size());
    query.textureName = textureNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxAtlasTextureResult result{};
    state->native->gfx->GetAtlasTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 20> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.x1)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.x2)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.y1)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.y2)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.pageNum)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_get_fixed_state(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetFixedState == nullptr)
        return Trap("GetFixedState generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetFixedState generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 172u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxFixedStateQuery query{};
    std::uint32_t paramPointer = 0;
    std::uint32_t paramCount = 0;
    if (!reader.U32(paramPointer) || !reader.U32(paramCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> paramBytes;
    if (!state->memory.View(paramPointer, paramCount, paramBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string paramStorage(reinterpret_cast<const char*>(paramBytes.data()), paramBytes.size());
    query.param = paramStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxFixedStateResult result{};
    state->native->gfx->GetFixedState(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 172> wire{};
    WireWriter writer(wire);
    for (std::size_t coreWriteIndex1 = 0; coreWriteIndex1 < 8u; ++coreWriteIndex1) {
        if (!writer.Bool(result.bools[coreWriteIndex1])) return Trap("generated Core wire overflow");
    }
    if (!writer.U32(result.boolCount)) return Trap("generated Core wire overflow");
    for (std::size_t coreWriteIndex1 = 0; coreWriteIndex1 < 16u; ++coreWriteIndex1) {
        if (!writer.I32(result.ints[coreWriteIndex1])) return Trap("generated Core wire overflow");
    }
    if (!writer.U32(result.intCount)) return Trap("generated Core wire overflow");
    for (std::size_t coreWriteIndex1 = 0; coreWriteIndex1 < 16u; ++coreWriteIndex1) {
        if (!writer.F32(result.floats[coreWriteIndex1])) return Trap("generated Core wire overflow");
    }
    if (!writer.U32(result.floatCount)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_get_global_tex_coords(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetGlobalTexCoords == nullptr)
        return Trap("GetGlobalTexCoords generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetGlobalTexCoords generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 20u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxStringQuery query{};
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxAtlasTextureResult result{};
    state->native->gfx->GetGlobalTexCoords(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 20> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.x1)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.x2)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.y1)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.y2)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.pageNum)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_get_subroutine_index(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetSubroutineIndex == nullptr)
        return Trap("GetSubroutineIndex generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("GetSubroutineIndex generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxSubroutineIndexQuery query{};
    query.shaderID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.shaderID)>>>(slots[0].i32);
    query.shaderType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.shaderType)>>>(slots[1].i32);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxSubroutineIndexResult result{};
    state->native->gfx->GetSubroutineIndex(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.I32(result.index)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_get_text_height(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetTextHeight == nullptr)
        return Trap("GetTextHeight generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetTextHeight generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 12u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxStringQuery query{};
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxTextHeightResult result{};
    state->native->gfx->GetTextHeight(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 12> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.height)) return Trap("generated Core wire overflow");
    if (!writer.F32(result.descender)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.lines)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_get_text_width(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetTextWidth == nullptr)
        return Trap("GetTextWidth generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("GetTextWidth generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxStringQuery query{};
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxFloatResult result{};
    state->native->gfx->GetTextWidth(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(std::bit_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_get_uniform_location(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->GetUniformLocation == nullptr)
        return Trap("GetUniformLocation generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetUniformLocation generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxUniformLocationQuery query{};
    query.shaderID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.shaderID)>>>(slots[0].i32);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUniformLocationResult result{};
    state->native->gfx->GetUniformLocation(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.location), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_has_extension(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->HasExtension == nullptr)
        return Trap("HasExtension generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("HasExtension generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxStringQuery query{};
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->HasExtension(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_instance_data_from_feature_defs_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->InstanceDataFromFeatureDefsVBO == nullptr)
        return Trap("InstanceDataFromFeatureDefsVBO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 5)
        return Trap("InstanceDataFromFeatureDefsVBO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBOInstanceDataQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
    if (idsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> idsWire;
    if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader idsReader(idsWire);
    std::vector<std::uint32_t> idsStorage;
    idsStorage.reserve(idsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < idsCount; ++coreIndex) {
        std::uint32_t item{};
        { std::uint32_t coreRaw = 0; if (!idsReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        idsStorage.push_back(item);
    }
    if (!idsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.ids = idsStorage.data();
    if (!AssignCoreCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[2].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[3].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->InstanceDataFromFeatureDefsVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_instance_data_from_features_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->InstanceDataFromFeaturesVBO == nullptr)
        return Trap("InstanceDataFromFeaturesVBO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 5)
        return Trap("InstanceDataFromFeaturesVBO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBOInstanceDataQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
    if (idsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> idsWire;
    if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader idsReader(idsWire);
    std::vector<std::uint32_t> idsStorage;
    idsStorage.reserve(idsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < idsCount; ++coreIndex) {
        std::uint32_t item{};
        { std::uint32_t coreRaw = 0; if (!idsReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        idsStorage.push_back(item);
    }
    if (!idsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.ids = idsStorage.data();
    if (!AssignCoreCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[2].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[3].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->InstanceDataFromFeaturesVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_instance_data_from_unit_defs_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->InstanceDataFromUnitDefsVBO == nullptr)
        return Trap("InstanceDataFromUnitDefsVBO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 5)
        return Trap("InstanceDataFromUnitDefsVBO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBOInstanceDataQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
    if (idsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> idsWire;
    if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader idsReader(idsWire);
    std::vector<std::uint32_t> idsStorage;
    idsStorage.reserve(idsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < idsCount; ++coreIndex) {
        std::uint32_t item{};
        { std::uint32_t coreRaw = 0; if (!idsReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        idsStorage.push_back(item);
    }
    if (!idsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.ids = idsStorage.data();
    if (!AssignCoreCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[2].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[3].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->InstanceDataFromUnitDefsVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_instance_data_from_units_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->InstanceDataFromUnitsVBO == nullptr)
        return Trap("InstanceDataFromUnitsVBO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 5)
        return Trap("InstanceDataFromUnitsVBO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBOInstanceDataQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
    if (idsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> idsWire;
    if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader idsReader(idsWire);
    std::vector<std::uint32_t> idsStorage;
    idsStorage.reserve(idsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < idsCount; ++coreIndex) {
        std::uint32_t item{};
        { std::uint32_t coreRaw = 0; if (!idsReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        idsStorage.push_back(item);
    }
    if (!idsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.ids = idsStorage.data();
    if (!AssignCoreCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[2].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[3].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->InstanceDataFromUnitsVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_load_font(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->LoadFont == nullptr)
        return Trap("LoadFont generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("LoadFont generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxLoadFontQuery query{};
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCount = 0;
    if (!reader.U32(pathPointer) || !reader.U32(pathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pathBytes;
    if (!state->memory.View(pathPointer, pathCount, pathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string pathStorage(reinterpret_cast<const char*>(pathBytes.data()), pathBytes.size());
    query.path = pathStorage.c_str();
    query.size = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.size)>>>(slots[0].i32);
    query.outlineWidth = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.outlineWidth)>>>(slots[1].i32);
    query.outlineWeight = slots[2].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxFontResult result{};
    state->native->gfx->LoadFont(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.fontID), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_matrix_data_from_projectiles_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->MatrixDataFromProjectilesVBO == nullptr)
        return Trap("MatrixDataFromProjectilesVBO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 5)
        return Trap("MatrixDataFromProjectilesVBO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBOInstanceDataQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    std::uint32_t idsPointer = 0;
    std::uint32_t idsCount = 0;
    if (!reader.U32(idsPointer) || !reader.U32(idsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t idsBytes64 = static_cast<std::uint64_t>(idsCount) * 4u;
    if (idsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> idsWire;
    if (!state->memory.View(idsPointer, static_cast<std::size_t>(idsBytes64), idsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader idsReader(idsWire);
    std::vector<std::uint32_t> idsStorage;
    idsStorage.reserve(idsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < idsCount; ++coreIndex) {
        std::uint32_t item{};
        { std::uint32_t coreRaw = 0; if (!idsReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        idsStorage.push_back(item);
    }
    if (!idsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.ids = idsStorage.data();
    if (!AssignCoreCount(idsCount, query.idCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[2].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[3].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxUIntResult result{};
    state->native->gfx->MatrixDataFromProjectilesVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_object_label(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->ObjectLabel == nullptr)
        return Trap("ObjectLabel generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ObjectLabel generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxObjectLabelQuery query{};
    query.identifier = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.identifier)>>>(slots[0].i32);
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[1].i32);
    std::uint32_t labelPointer = 0;
    std::uint32_t labelCount = 0;
    if (!reader.U32(labelPointer) || !reader.U32(labelCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> labelBytes;
    if (!state->memory.View(labelPointer, labelCount, labelBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string labelStorage(reinterpret_cast<const char*>(labelBytes.data()), labelBytes.size());
    query.label = labelStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->ObjectLabel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_push_debug_group(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->PushDebugGroup == nullptr)
        return Trap("PushDebugGroup generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("PushDebugGroup generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxPushDebugGroupQuery query{};
    query.id = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.id)>>>(slots[0].i32);
    std::uint32_t messagePointer = 0;
    std::uint32_t messageCount = 0;
    if (!reader.U32(messagePointer) || !reader.U32(messageCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> messageBytes;
    if (!state->memory.View(messagePointer, messageCount, messageBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string messageStorage(reinterpret_cast<const char*>(messageBytes.data()), messageBytes.size());
    query.message = messageStorage.c_str();
    query.sourceIsThirdParty = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->PushDebugGroup(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_save_image(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->SaveImage == nullptr)
        return Trap("SaveImage generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 6)
        return Trap("SaveImage generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 7u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[5].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 20u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxSaveImageQuery query{};
    query.x = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.x)>>>(slots[0].i32);
    query.y = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.y)>>>(slots[1].i32);
    query.width = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.width)>>>(slots[2].i32);
    query.height = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.height)>>>(slots[3].i32);
    std::uint32_t filenamePointer = 0;
    std::uint32_t filenameCount = 0;
    if (!reader.U32(filenamePointer) || !reader.U32(filenameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> filenameBytes;
    if (!state->memory.View(filenamePointer, filenameCount, filenameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string filenameStorage(reinterpret_cast<const char*>(filenameBytes.data()), filenameBytes.size());
    query.filename = filenameStorage.c_str();
    if (!reader.Bool(query.options.alpha)) return Trap("generated Core wire underflow");
    if (!reader.Bool(query.options.yflip)) return Trap("generated Core wire underflow");
    if (!reader.Bool(query.options.grayscale16bit)) return Trap("generated Core wire underflow");
    query.readBuffer = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.readBuffer)>>>(slots[4].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxBoolResult result{};
    state->native->gfx->SaveImage(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_set_fbo_attachment(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->SetFBOAttachment == nullptr)
        return Trap("SetFBOAttachment generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 7)
        return Trap("SetFBOAttachment generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 8u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[6].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxFBOAttachmentQuery query{};
    query.fboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fboID)>>>(slots[0].i32);
    query.attachment = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attachment)>>>(slots[1].i32);
    std::uint32_t textureNamePointer = 0;
    std::uint32_t textureNameCount = 0;
    if (!reader.U32(textureNamePointer) || !reader.U32(textureNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textureNameBytes;
    if (!state->memory.View(textureNamePointer, textureNameCount, textureNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string textureNameStorage(reinterpret_cast<const char*>(textureNameBytes.data()), textureNameBytes.size());
    query.textureName = textureNameStorage.c_str();
    query.textureTarget = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.textureTarget)>>>(slots[2].i32);
    query.mipLevel = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.mipLevel)>>>(slots[3].i32);
    query.rboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.rboID)>>>(slots[4].i32);
    query.useRBO = slots[5].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->SetFBOAttachment(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_set_fbo_draw_buffers(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->SetFBODrawBuffers == nullptr)
        return Trap("SetFBODrawBuffers generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetFBODrawBuffers generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxFBODrawBuffersQuery query{};
    query.fboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.fboID)>>>(slots[0].i32);
    std::uint32_t buffersPointer = 0;
    std::uint32_t buffersCount = 0;
    if (!reader.U32(buffersPointer) || !reader.U32(buffersCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t buffersBytes64 = static_cast<std::uint64_t>(buffersCount) * 4u;
    if (buffersBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> buffersWire;
    if (!state->memory.View(buffersPointer, static_cast<std::size_t>(buffersBytes64), buffersWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader buffersReader(buffersWire);
    std::vector<std::uint32_t> buffersStorage;
    buffersStorage.reserve(buffersCount);
    for (std::uint32_t coreIndex = 0; coreIndex < buffersCount; ++coreIndex) {
        std::uint32_t item{};
        { std::uint32_t coreRaw = 0; if (!buffersReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        buffersStorage.push_back(item);
    }
    if (!buffersReader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.buffers = buffersStorage.data();
    if (!AssignCoreCount(buffersCount, query.bufferCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->SetFBODrawBuffers(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_set_feature_buffer_uniforms(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->SetFeatureBufferUniforms == nullptr)
        return Trap("SetFeatureBufferUniforms generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("SetFeatureBufferUniforms generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxObjectBufferUniformsQuery query{};
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[0].i32);
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCount = 0;
    if (!reader.U32(valuesPointer) || !reader.U32(valuesCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t valuesBytes64 = static_cast<std::uint64_t>(valuesCount) * 4u;
    if (valuesBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valuesWire;
    if (!state->memory.View(valuesPointer, static_cast<std::size_t>(valuesBytes64), valuesWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader valuesReader(valuesWire);
    std::vector<float> valuesStorage;
    valuesStorage.reserve(valuesCount);
    for (std::uint32_t coreIndex = 0; coreIndex < valuesCount; ++coreIndex) {
        float item{};
        if (!valuesReader.F32(item)) return Trap("generated Core wire underflow");
        valuesStorage.push_back(item);
    }
    if (!valuesReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.values = valuesStorage.data();
    if (!AssignCoreCount(valuesCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.offset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.offset)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxObjectBufferUniformsResult result{};
    state->native->gfx->SetFeatureBufferUniforms(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.count), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_set_unit_buffer_uniforms(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->SetUnitBufferUniforms == nullptr)
        return Trap("SetUnitBufferUniforms generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("SetUnitBufferUniforms generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxObjectBufferUniformsQuery query{};
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[0].i32);
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCount = 0;
    if (!reader.U32(valuesPointer) || !reader.U32(valuesCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t valuesBytes64 = static_cast<std::uint64_t>(valuesCount) * 4u;
    if (valuesBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valuesWire;
    if (!state->memory.View(valuesPointer, static_cast<std::size_t>(valuesBytes64), valuesWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader valuesReader(valuesWire);
    std::vector<float> valuesStorage;
    valuesStorage.reserve(valuesCount);
    for (std::uint32_t coreIndex = 0; coreIndex < valuesCount; ++coreIndex) {
        float item{};
        if (!valuesReader.F32(item)) return Trap("generated Core wire underflow");
        valuesStorage.push_back(item);
    }
    if (!valuesReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.values = valuesStorage.data();
    if (!AssignCoreCount(valuesCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.offset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.offset)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxObjectBufferUniformsResult result{};
    state->native->gfx->SetUnitBufferUniforms(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.count), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_shape(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->Shape == nullptr)
        return Trap("Shape generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("Shape generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxShapeQuery query{};
    query.primitive = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.primitive)>>>(slots[0].i32);
    std::uint32_t verticesPointer = 0;
    std::uint32_t verticesCount = 0;
    if (!reader.U32(verticesPointer) || !reader.U32(verticesCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t verticesBytes64 = static_cast<std::uint64_t>(verticesCount) * 64u;
    if (verticesBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> verticesWire;
    if (!state->memory.View(verticesPointer, static_cast<std::size_t>(verticesBytes64), verticesWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader verticesReader(verticesWire);
    std::vector<GfxVertexData> verticesStorage;
    verticesStorage.reserve(verticesCount);
    for (std::uint32_t coreIndex = 0; coreIndex < verticesCount; ++coreIndex) {
        GfxVertexData item{};
        for (std::size_t coreReadIndex2 = 0; coreReadIndex2 < 3u; ++coreReadIndex2) {
            if (!verticesReader.F32(item.vertex[coreReadIndex2])) return Trap("generated Core wire underflow");
        }
        for (std::size_t coreReadIndex2 = 0; coreReadIndex2 < 3u; ++coreReadIndex2) {
            if (!verticesReader.F32(item.normal[coreReadIndex2])) return Trap("generated Core wire underflow");
        }
        for (std::size_t coreReadIndex2 = 0; coreReadIndex2 < 2u; ++coreReadIndex2) {
            if (!verticesReader.F32(item.texCoord[coreReadIndex2])) return Trap("generated Core wire underflow");
        }
        for (std::size_t coreReadIndex2 = 0; coreReadIndex2 < 4u; ++coreReadIndex2) {
            if (!verticesReader.F32(item.color[coreReadIndex2])) return Trap("generated Core wire underflow");
        }
        if (!verticesReader.Bool(item.hasVertex)) return Trap("generated Core wire underflow");
        if (!verticesReader.Bool(item.hasNormal)) return Trap("generated Core wire underflow");
        if (!verticesReader.Bool(item.hasTexCoord)) return Trap("generated Core wire underflow");
        if (!verticesReader.Bool(item.hasColor)) return Trap("generated Core wire underflow");
        verticesStorage.push_back(item);
    }
    if (!verticesReader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.vertices = verticesStorage.data();
    if (!AssignCoreCount(verticesCount, query.vertexCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->Shape(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_text(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->Text == nullptr)
        return Trap("Text generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("Text generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxTextQuery query{};
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, textCount, textBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string textStorage(reinterpret_cast<const char*>(textBytes.data()), textBytes.size());
    query.text = textStorage.c_str();
    query.x = slots[0].f32;
    query.y = slots[1].f32;
    query.size = slots[2].f32;
    std::uint32_t optionsPointer = 0;
    std::uint32_t optionsCount = 0;
    if (!reader.U32(optionsPointer) || !reader.U32(optionsCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> optionsBytes;
    if (!state->memory.View(optionsPointer, optionsCount, optionsBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string optionsStorage(reinterpret_cast<const char*>(optionsBytes.data()), optionsBytes.size());
    query.options = optionsStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->Text(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_texture_info(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->TextureInfo == nullptr)
        return Trap("TextureInfo generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("TextureInfo generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 24u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GfxTextureNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxTextureInfoResult result{};
    state->native->gfx->TextureInfo(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 24> wire{};
    WireWriter writer(wire);
    if (!writer.I32(result.xsize)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.ysize)) return Trap("generated Core wire overflow");
    if (!writer.I32(result.zsize)) return Trap("generated Core wire overflow");
    if (!writer.U32(result.id)) return Trap("generated Core wire overflow");
    if (!writer.U32(result.target)) return Trap("generated Core wire overflow");
    if (!writer.U32(result.fbo)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_uniform_array_float(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->UniformArrayFloat == nullptr)
        return Trap("UniformArrayFloat generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("UniformArrayFloat generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxUniformArrayFloatQuery query{};
    query.location = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.location)>>>(slots[0].i32);
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCount = 0;
    if (!reader.U32(valuesPointer) || !reader.U32(valuesCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesBytes64 = static_cast<std::uint64_t>(valuesCount) * 4u;
    if (valuesBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valuesWire;
    if (!state->memory.View(valuesPointer, static_cast<std::size_t>(valuesBytes64), valuesWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valuesReader(valuesWire);
    std::vector<float> valuesStorage;
    valuesStorage.reserve(valuesCount);
    for (std::uint32_t coreIndex = 0; coreIndex < valuesCount; ++coreIndex) {
        float item{};
        if (!valuesReader.F32(item)) return Trap("generated Core wire underflow");
        valuesStorage.push_back(item);
    }
    if (!valuesReader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.values = valuesStorage.data();
    if (!AssignCoreCount(valuesCount, query.count)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->UniformArrayFloat(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_uniform_array_int(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->UniformArrayInt == nullptr)
        return Trap("UniformArrayInt generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("UniformArrayInt generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxUniformArrayIntQuery query{};
    query.location = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.location)>>>(slots[0].i32);
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCount = 0;
    if (!reader.U32(valuesPointer) || !reader.U32(valuesCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesBytes64 = static_cast<std::uint64_t>(valuesCount) * 4u;
    if (valuesBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valuesWire;
    if (!state->memory.View(valuesPointer, static_cast<std::size_t>(valuesBytes64), valuesWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valuesReader(valuesWire);
    std::vector<std::int32_t> valuesStorage;
    valuesStorage.reserve(valuesCount);
    for (std::uint32_t coreIndex = 0; coreIndex < valuesCount; ++coreIndex) {
        std::int32_t item{};
        { std::int32_t coreRaw = 0; if (!valuesReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        valuesStorage.push_back(item);
    }
    if (!valuesReader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.values = valuesStorage.data();
    if (!AssignCoreCount(valuesCount, query.count)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->UniformArrayInt(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_uniform_matrix(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->UniformMatrix == nullptr)
        return Trap("UniformMatrix generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("UniformMatrix generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxUniformMatrixQuery query{};
    query.location = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.location)>>>(slots[0].i32);
    std::uint32_t valuesPointer = 0;
    std::uint32_t valuesCount = 0;
    if (!reader.U32(valuesPointer) || !reader.U32(valuesCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t valuesBytes64 = static_cast<std::uint64_t>(valuesCount) * 4u;
    if (valuesBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valuesWire;
    if (!state->memory.View(valuesPointer, static_cast<std::size_t>(valuesBytes64), valuesWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader valuesReader(valuesWire);
    std::vector<float> valuesStorage;
    valuesStorage.reserve(valuesCount);
    for (std::uint32_t coreIndex = 0; coreIndex < valuesCount; ++coreIndex) {
        float item{};
        if (!valuesReader.F32(item)) return Trap("generated Core wire underflow");
        valuesStorage.push_back(item);
    }
    if (!valuesReader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.values = valuesStorage.data();
    if (!AssignCoreCount(valuesCount, query.count)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.transpose = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->UniformMatrix(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_upload_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->UploadTexture == nullptr)
        return Trap("UploadTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 11)
        return Trap("UploadTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 12u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[10].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxUploadTextureQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.target = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.target)>>>(slots[0].i32);
    query.level = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.level)>>>(slots[1].i32);
    query.xoff = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.xoff)>>>(slots[2].i32);
    query.yoff = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.yoff)>>>(slots[3].i32);
    query.zoff = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.zoff)>>>(slots[4].i32);
    query.width = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.width)>>>(slots[5].i32);
    query.height = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.height)>>>(slots[6].i32);
    query.depth = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.depth)>>>(slots[7].i32);
    query.format = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.format)>>>(slots[8].i32);
    query.pixelType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.pixelType)>>>(slots[9].i32);
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCount = 0;
    if (!reader.U32(dataPointer) || !reader.U32(dataCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    const std::uint64_t dataBytes64 = static_cast<std::uint64_t>(dataCount) * 4u;
    if (dataBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> dataWire;
    if (!state->memory.View(dataPointer, static_cast<std::size_t>(dataBytes64), dataWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader dataReader(dataWire);
    std::vector<std::uint8_t> dataStorage;
    dataStorage.reserve(dataCount);
    for (std::uint32_t coreIndex = 0; coreIndex < dataCount; ++coreIndex) {
        std::uint8_t item{};
        { std::uint32_t coreRaw = 0; if (!dataReader.U32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        dataStorage.push_back(item);
    }
    if (!dataReader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    query.data = dataStorage.data();
    if (!AssignCoreCount(dataCount, query.dataSize)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    GfxEmptyResult result{};
    state->native->gfx->UploadTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_gfx_upload_vbo(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->gfx == nullptr ||
        state->native->gfx->UploadVBO == nullptr)
        return Trap("UploadVBO generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 6)
        return Trap("UploadVBO generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 7u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[5].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GfxVBOUploadQuery query{};
    query.vboID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.vboID)>>>(slots[0].i32);
    std::uint32_t dataPointer = 0;
    std::uint32_t dataCount = 0;
    if (!reader.U32(dataPointer) || !reader.U32(dataCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t dataBytes64 = static_cast<std::uint64_t>(dataCount) * 4u;
    if (dataBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> dataWire;
    if (!state->memory.View(dataPointer, static_cast<std::size_t>(dataBytes64), dataWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader dataReader(dataWire);
    std::vector<float> dataStorage;
    dataStorage.reserve(dataCount);
    for (std::uint32_t coreIndex = 0; coreIndex < dataCount; ++coreIndex) {
        float item{};
        if (!dataReader.F32(item)) return Trap("generated Core wire underflow");
        dataStorage.push_back(item);
    }
    if (!dataReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.data = dataStorage.data();
    if (!AssignCoreCount(dataCount, query.dataCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.attributeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.attributeIndex)>>>(slots[1].i32);
    query.elementOffset = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementOffset)>>>(slots[2].i32);
    query.dataStartIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataStartIndex)>>>(slots[3].i32);
    query.dataFinishIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataFinishIndex)>>>(slots[4].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GfxVBOUploadResult result{};
    state->native->gfx->UploadVBO(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.bytesWritten), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_icons_add_unit_icon(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->icons == nullptr ||
        state->native->icons->AddUnitIcon == nullptr)
        return Trap("AddUnitIcon generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 8)
        return Trap("AddUnitIcon generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 9u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[7].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    AddUnitIconQuery query{};
    std::uint32_t iconNamePointer = 0;
    std::uint32_t iconNameCount = 0;
    if (!reader.U32(iconNamePointer) || !reader.U32(iconNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> iconNameBytes;
    if (!state->memory.View(iconNamePointer, iconNameCount, iconNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string iconNameStorage(reinterpret_cast<const char*>(iconNameBytes.data()), iconNameBytes.size());
    query.iconName = iconNameStorage.c_str();
    std::uint32_t texFilePointer = 0;
    std::uint32_t texFileCount = 0;
    if (!reader.U32(texFilePointer) || !reader.U32(texFileCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> texFileBytes;
    if (!state->memory.View(texFilePointer, texFileCount, texFileBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string texFileStorage(reinterpret_cast<const char*>(texFileBytes.data()), texFileBytes.size());
    query.texFile = texFileStorage.c_str();
    query.size = slots[0].f32;
    query.distance = slots[1].f32;
    query.radiusAdjust = slots[2].i32 != 0;
    query.u0 = slots[3].f32;
    query.v0 = slots[4].f32;
    query.u1 = slots[5].f32;
    query.v1 = slots[6].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    AddUnitIconResult result{};
    state->native->icons->AddUnitIcon(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_icons_free_unit_icon(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->icons == nullptr ||
        state->native->icons->FreeUnitIcon == nullptr)
        return Trap("FreeUnitIcon generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("FreeUnitIcon generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    FreeUnitIconQuery query{};
    std::uint32_t iconNamePointer = 0;
    std::uint32_t iconNameCount = 0;
    if (!reader.U32(iconNamePointer) || !reader.U32(iconNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> iconNameBytes;
    if (!state->memory.View(iconNamePointer, iconNameCount, iconNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string iconNameStorage(reinterpret_cast<const char*>(iconNameBytes.data()), iconNameBytes.size());
    query.iconName = iconNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    FreeUnitIconResult result{};
    state->native->icons->FreeUnitIcon(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_markers_add_world_text(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->markers == nullptr ||
        state->native->markers->AddWorldText == nullptr)
        return Trap("AddWorldText generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("AddWorldText generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 20u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    AddWorldTextQuery query{};
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, textCount, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string textStorage(reinterpret_cast<const char*>(textBytes.data()), textBytes.size());
    query.text = textStorage.c_str();
    if (!reader.F32(query.pos.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.pos.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.pos.z)) return Trap("generated Core wire underflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    AddWorldTextResult result{};
    state->native->markers->AddWorldText(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_markers_marker_add_point(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->markers == nullptr ||
        state->native->markers->MarkerAddPoint == nullptr)
        return Trap("MarkerAddPoint generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("MarkerAddPoint generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 20u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    MarkerAddPointQuery query{};
    if (!reader.F32(query.pos.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.pos.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.pos.z)) return Trap("generated Core wire underflow");
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, textCount, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string textStorage(reinterpret_cast<const char*>(textBytes.data()), textBytes.size());
    query.text = textStorage.c_str();
    query.localOnly = slots[0].i32 != 0;
    query.playerID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.playerID)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    MarkerAddPointResult result{};
    state->native->markers->MarkerAddPoint(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_ground_decals_set_ground_decal_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->groundDecals == nullptr ||
        state->native->groundDecals->SetGroundDecalTexture == nullptr)
        return Trap("SetGroundDecalTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("SetGroundDecalTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetGroundDecalTextureQuery query{};
    query.decalID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.decalID)>>>(slots[0].i32);
    std::uint32_t textureNamePointer = 0;
    std::uint32_t textureNameCount = 0;
    if (!reader.U32(textureNamePointer) || !reader.U32(textureNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textureNameBytes;
    if (!state->memory.View(textureNamePointer, textureNameCount, textureNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string textureNameStorage(reinterpret_cast<const char*>(textureNameBytes.data()), textureNameBytes.size());
    query.textureName = textureNameStorage.c_str();
    query.mainTex = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetGroundDecalTextureResult result{};
    state->native->groundDecals->SetGroundDecalTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_system_control_clear_watch_dog_timer(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->ClearWatchDogTimer == nullptr)
        return Trap("ClearWatchDogTimer generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ClearWatchDogTimer generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    ClearWatchDogTimerQuery query{};
    std::uint32_t threadNamePointer = 0;
    std::uint32_t threadNameCount = 0;
    if (!reader.U32(threadNamePointer) || !reader.U32(threadNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> threadNameBytes;
    if (!state->memory.View(threadNamePointer, threadNameCount, threadNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string threadNameStorage(reinterpret_cast<const char*>(threadNameBytes.data()), threadNameBytes.size());
    query.threadName = threadNameStorage.c_str();
    query.keepStopped = slots[0].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    ClearWatchDogTimerResult result{};
    state->native->systemControl->ClearWatchDogTimer(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_system_control_reload(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->Reload == nullptr)
        return Trap("Reload generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("Reload generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    ReloadQuery query{};
    std::uint32_t startScriptPointer = 0;
    std::uint32_t startScriptCount = 0;
    if (!reader.U32(startScriptPointer) || !reader.U32(startScriptCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> startScriptBytes;
    if (!state->memory.View(startScriptPointer, startScriptCount, startScriptBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string startScriptStorage(reinterpret_cast<const char*>(startScriptBytes.data()), startScriptBytes.size());
    query.startScript = startScriptStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    ReloadResult result{};
    state->native->systemControl->Reload(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_system_control_restart(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->Restart == nullptr)
        return Trap("Restart generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("Restart generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RestartQuery query{};
    std::uint32_t cmdArgsPointer = 0;
    std::uint32_t cmdArgsCount = 0;
    if (!reader.U32(cmdArgsPointer) || !reader.U32(cmdArgsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> cmdArgsBytes;
    if (!state->memory.View(cmdArgsPointer, cmdArgsCount, cmdArgsBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string cmdArgsStorage(reinterpret_cast<const char*>(cmdArgsBytes.data()), cmdArgsBytes.size());
    query.cmdArgs = cmdArgsStorage.c_str();
    std::uint32_t startScriptPointer = 0;
    std::uint32_t startScriptCount = 0;
    if (!reader.U32(startScriptPointer) || !reader.U32(startScriptCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> startScriptBytes;
    if (!state->memory.View(startScriptPointer, startScriptCount, startScriptBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string startScriptStorage(reinterpret_cast<const char*>(startScriptBytes.data()), startScriptBytes.size());
    query.startScript = startScriptStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RestartResult result{};
    state->native->systemControl->Restart(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_system_control_set_share_level(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->SetShareLevel == nullptr)
        return Trap("SetShareLevel generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetShareLevel generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetShareLevelQuery query{};
    std::uint32_t resourcePointer = 0;
    std::uint32_t resourceCount = 0;
    if (!reader.U32(resourcePointer) || !reader.U32(resourceCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceBytes;
    if (!state->memory.View(resourcePointer, resourceCount, resourceBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string resourceStorage(reinterpret_cast<const char*>(resourceBytes.data()), resourceBytes.size());
    query.resource = resourceStorage.c_str();
    query.level = slots[0].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetShareLevelResult result{};
    state->native->systemControl->SetShareLevel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_system_control_share_resources(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->ShareResources == nullptr)
        return Trap("ShareResources generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ShareResources generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    ShareResourcesQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourcePointer = 0;
    std::uint32_t resourceCount = 0;
    if (!reader.U32(resourcePointer) || !reader.U32(resourceCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceBytes;
    if (!state->memory.View(resourcePointer, resourceCount, resourceBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string resourceStorage(reinterpret_cast<const char*>(resourceBytes.data()), resourceBytes.size());
    query.resource = resourceStorage.c_str();
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    ShareResourcesResult result{};
    state->native->systemControl->ShareResources(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_system_control_start(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->systemControl == nullptr ||
        state->native->systemControl->Start == nullptr)
        return Trap("Start generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("Start generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    StartQuery query{};
    std::uint32_t cmdArgsPointer = 0;
    std::uint32_t cmdArgsCount = 0;
    if (!reader.U32(cmdArgsPointer) || !reader.U32(cmdArgsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> cmdArgsBytes;
    if (!state->memory.View(cmdArgsPointer, cmdArgsCount, cmdArgsBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string cmdArgsStorage(reinterpret_cast<const char*>(cmdArgsBytes.data()), cmdArgsBytes.size());
    query.cmdArgs = cmdArgsStorage.c_str();
    std::uint32_t startScriptPointer = 0;
    std::uint32_t startScriptCount = 0;
    if (!reader.U32(startScriptPointer) || !reader.U32(startScriptCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> startScriptBytes;
    if (!state->memory.View(startScriptPointer, startScriptCount, startScriptBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string startScriptStorage(reinterpret_cast<const char*>(startScriptBytes.data()), startScriptBytes.size());
    query.startScript = startScriptStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    StartResult result{};
    state->native->systemControl->Start(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_add_translation_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->AddTranslationString == nullptr)
        return Trap("AddTranslationString generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("AddTranslationString generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlAddTranslationStringQuery query{};
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, keyCount, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string keyStorage(reinterpret_cast<const char*>(keyBytes.data()), keyBytes.size());
    query.key = keyStorage.c_str();
    std::uint32_t translationPointer = 0;
    std::uint32_t translationCount = 0;
    if (!reader.U32(translationPointer) || !reader.U32(translationCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> translationBytes;
    if (!state->memory.View(translationPointer, translationCount, translationBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string translationStorage(reinterpret_cast<const char*>(translationBytes.data()), translationBytes.size());
    query.translation = translationStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlAddTranslationStringResult result{};
    state->native->rmlUi->AddTranslationString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_clear_document_path_requests(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ClearDocumentPathRequests == nullptr)
        return Trap("ClearDocumentPathRequests generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("ClearDocumentPathRequests generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlClearDocumentPathRequestsQuery query{};
    std::uint32_t documentPathPointer = 0;
    std::uint32_t documentPathCount = 0;
    if (!reader.U32(documentPathPointer) || !reader.U32(documentPathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> documentPathBytes;
    if (!state->memory.View(documentPathPointer, documentPathCount, documentPathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string documentPathStorage(reinterpret_cast<const char*>(documentPathBytes.data()), documentPathBytes.size());
    query.documentPath = documentPathStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlClearDocumentPathRequestsResult result{};
    state->native->rmlUi->ClearDocumentPathRequests(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_context_activate_theme(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextActivateTheme == nullptr)
        return Trap("ContextActivateTheme generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ContextActivateTheme generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlContextStringBoolQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.value = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlContextBoolResult result{};
    state->native->rmlUi->ContextActivateTheme(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_context_create_data_model(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextCreateDataModel == nullptr)
        return Trap("ContextCreateDataModel generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ContextCreateDataModel generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlContextCreateDataModelQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlContextOpenDataModelResult result{};
    state->native->rmlUi->ContextCreateDataModel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.dataModelHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_context_create_document(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextCreateDocument == nullptr)
        return Trap("ContextCreateDocument generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ContextCreateDocument generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlContextCreateDocumentQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t tagPointer = 0;
    std::uint32_t tagCount = 0;
    if (!reader.U32(tagPointer) || !reader.U32(tagCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> tagBytes;
    if (!state->memory.View(tagPointer, tagCount, tagBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string tagStorage(reinterpret_cast<const char*>(tagBytes.data()), tagBytes.size());
    query.tag = tagStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlContextCreateDocumentResult result{};
    state->native->rmlUi->ContextCreateDocument(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.documentHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_context_get_document(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextGetDocument == nullptr)
        return Trap("ContextGetDocument generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ContextGetDocument generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlContextGetDocumentQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlContextGetDocumentResult result{};
    state->native->rmlUi->ContextGetDocument(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.documentHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_context_is_theme_active(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextIsThemeActive == nullptr)
        return Trap("ContextIsThemeActive generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ContextIsThemeActive generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlContextStringQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlContextBoolResult result{};
    state->native->rmlUi->ContextIsThemeActive(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_context_load_document(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextLoadDocument == nullptr)
        return Trap("ContextLoadDocument generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ContextLoadDocument generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlContextLoadDocumentQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t documentPathPointer = 0;
    std::uint32_t documentPathCount = 0;
    if (!reader.U32(documentPathPointer) || !reader.U32(documentPathCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> documentPathBytes;
    if (!state->memory.View(documentPathPointer, documentPathCount, documentPathBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string documentPathStorage(reinterpret_cast<const char*>(documentPathBytes.data()), documentPathBytes.size());
    query.documentPath = documentPathStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlContextLoadDocumentResult result{};
    state->native->rmlUi->ContextLoadDocument(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.documentHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_context_open_data_model(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextOpenDataModel == nullptr)
        return Trap("ContextOpenDataModel generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ContextOpenDataModel generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlContextOpenDataModelQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlContextOpenDataModelResult result{};
    state->native->rmlUi->ContextOpenDataModel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.dataModelHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_context_process_text_input(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextProcessTextInput == nullptr)
        return Trap("ContextProcessTextInput generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ContextProcessTextInput generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlContextTextInputQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t textPointer = 0;
    std::uint32_t textCount = 0;
    if (!reader.U32(textPointer) || !reader.U32(textCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textBytes;
    if (!state->memory.View(textPointer, textCount, textBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string textStorage(reinterpret_cast<const char*>(textBytes.data()), textBytes.size());
    query.text = textStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlContextBoolResult result{};
    state->native->rmlUi->ContextProcessTextInput(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_context_remove_data_model(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextRemoveDataModel == nullptr)
        return Trap("ContextRemoveDataModel generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ContextRemoveDataModel generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlContextStringQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlContextBoolResult result{};
    state->native->rmlUi->ContextRemoveDataModel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_context_remove_event_listener(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ContextRemoveEventListener == nullptr)
        return Trap("ContextRemoveEventListener generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("ContextRemoveEventListener generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlContextEventListenerRemoveQuery query{};
    query.contextHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.contextHandle)>>>(slots[0].i64);
    query.eventListenerHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventListenerHandle)>>>(slots[1].i64);
    std::uint32_t eventPointer = 0;
    std::uint32_t eventCount = 0;
    if (!reader.U32(eventPointer) || !reader.U32(eventCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> eventBytes;
    if (!state->memory.View(eventPointer, eventCount, eventBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string eventStorage(reinterpret_cast<const char*>(eventBytes.data()), eventBytes.size());
    query.event = eventStorage.c_str();
    query.inCapturePhase = slots[2].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ContextRemoveEventListener(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_create_context(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->CreateContext == nullptr)
        return Trap("CreateContext generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("CreateContext generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlCreateContextQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlCreateContextResult result{};
    state->native->rmlUi->CreateContext(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.contextHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_data_model_bind_bool(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindBool == nullptr)
        return Trap("DataModelBindBool generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("DataModelBindBool generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindBoolQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.initialValue = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindBool(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_data_model_bind_color(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindColor == nullptr)
        return Trap("DataModelBindColor generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 7)
        return Trap("DataModelBindColor generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 8u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[5].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[6].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindColorQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.red = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.red)>>>(slots[1].i32);
    query.green = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.green)>>>(slots[2].i32);
    query.blue = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.blue)>>>(slots[3].i32);
    query.alpha = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.alpha)>>>(slots[4].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindColor(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_data_model_bind_float(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindFloat == nullptr)
        return Trap("DataModelBindFloat generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("DataModelBindFloat generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindFloatQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.initialValue = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindFloat(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_data_model_bind_int(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindInt == nullptr)
        return Trap("DataModelBindInt generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("DataModelBindInt generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindIntQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.initialValue = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.initialValue)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindInt(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_data_model_bind_percent(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindPercent == nullptr)
        return Trap("DataModelBindPercent generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("DataModelBindPercent generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindPercentQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.initialValue = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindPercent(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_data_model_bind_pixels(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindPixels == nullptr)
        return Trap("DataModelBindPixels generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("DataModelBindPixels generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[3].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindPixelsQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.initialValue = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindPixels(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_data_model_bind_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelBindString == nullptr)
        return Trap("DataModelBindString generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("DataModelBindString generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDataModelBindStringQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    std::uint32_t initialValuePointer = 0;
    std::uint32_t initialValueCount = 0;
    if (!reader.U32(initialValuePointer) || !reader.U32(initialValueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> initialValueBytes;
    if (!state->memory.View(initialValuePointer, initialValueCount, initialValueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string initialValueStorage(reinterpret_cast<const char*>(initialValueBytes.data()), initialValueBytes.size());
    query.initialValue = initialValueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDataModelBindResult result{};
    state->native->rmlUi->DataModelBindString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.variableHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_data_model_set_string(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DataModelSetString == nullptr)
        return Trap("DataModelSetString generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("DataModelSetString generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlDataModelVariableStringQuery query{};
    query.variableHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.variableHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->DataModelSetString(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_document_append_to_style_sheet(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentAppendToStyleSheet == nullptr)
        return Trap("DocumentAppendToStyleSheet generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("DocumentAppendToStyleSheet generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlDocumentStringQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlDocumentBoolResult result{};
    state->native->rmlUi->DocumentAppendToStyleSheet(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_document_create_element(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentCreateElement == nullptr)
        return Trap("DocumentCreateElement generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("DocumentCreateElement generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDocumentCreateElementQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    std::uint32_t tagNamePointer = 0;
    std::uint32_t tagNameCount = 0;
    if (!reader.U32(tagNamePointer) || !reader.U32(tagNameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> tagNameBytes;
    if (!state->memory.View(tagNamePointer, tagNameCount, tagNameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string tagNameStorage(reinterpret_cast<const char*>(tagNameBytes.data()), tagNameBytes.size());
    query.tagName = tagNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDocumentCreateElementResult result{};
    state->native->rmlUi->DocumentCreateElement(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.elementPtrHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_document_create_text_node(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentCreateTextNode == nullptr)
        return Trap("DocumentCreateTextNode generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("DocumentCreateTextNode generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlDocumentStringQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlDocumentCreateElementResult result{};
    state->native->rmlUi->DocumentCreateTextNode(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.elementPtrHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.success)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_document_load_external_script(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentLoadExternalScript == nullptr)
        return Trap("DocumentLoadExternalScript generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("DocumentLoadExternalScript generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlDocumentStringQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlDocumentBoolResult result{};
    state->native->rmlUi->DocumentLoadExternalScript(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_document_load_inline_script(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentLoadInlineScript == nullptr)
        return Trap("DocumentLoadInlineScript generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("DocumentLoadInlineScript generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlDocumentInlineScriptQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    std::uint32_t contentPointer = 0;
    std::uint32_t contentCount = 0;
    if (!reader.U32(contentPointer) || !reader.U32(contentCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> contentBytes;
    if (!state->memory.View(contentPointer, contentCount, contentBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string contentStorage(reinterpret_cast<const char*>(contentBytes.data()), contentBytes.size());
    query.content = contentStorage.c_str();
    std::uint32_t sourcePathPointer = 0;
    std::uint32_t sourcePathCount = 0;
    if (!reader.U32(sourcePathPointer) || !reader.U32(sourcePathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> sourcePathBytes;
    if (!state->memory.View(sourcePathPointer, sourcePathCount, sourcePathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string sourcePathStorage(reinterpret_cast<const char*>(sourcePathBytes.data()), sourcePathBytes.size());
    query.sourcePath = sourcePathStorage.c_str();
    query.sourceLine = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.sourceLine)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlDocumentBoolResult result{};
    state->native->rmlUi->DocumentLoadInlineScript(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_document_set_title(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->DocumentSetTitle == nullptr)
        return Trap("DocumentSetTitle generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("DocumentSetTitle generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlDocumentSetTitleQuery query{};
    query.documentHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.documentHandle)>>>(slots[0].i64);
    std::uint32_t titlePointer = 0;
    std::uint32_t titleCount = 0;
    if (!reader.U32(titlePointer) || !reader.U32(titleCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> titleBytes;
    if (!state->memory.View(titlePointer, titleCount, titleBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string titleStorage(reinterpret_cast<const char*>(titleBytes.data()), titleBytes.size());
    query.title = titleStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlDocumentBoolResult result{};
    state->native->rmlUi->DocumentSetTitle(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_are_pseudo_classes_set(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementArePseudoClassesSet == nullptr)
        return Trap("ElementArePseudoClassesSet generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementArePseudoClassesSet generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementArePseudoClassesSet(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_closest(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementClosest == nullptr)
        return Trap("ElementClosest generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ElementClosest generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlElementGetElementResult result{};
    state->native->rmlUi->ElementClosest(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.elementHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_dispatch_event(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementDispatchEvent == nullptr)
        return Trap("ElementDispatchEvent generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementDispatchEvent generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementDispatchEventQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t eventPointer = 0;
    std::uint32_t eventCount = 0;
    if (!reader.U32(eventPointer) || !reader.U32(eventCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> eventBytes;
    if (!state->memory.View(eventPointer, eventCount, eventBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string eventStorage(reinterpret_cast<const char*>(eventBytes.data()), eventBytes.size());
    query.event = eventStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementDispatchEvent(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_form_submit(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementFormSubmit == nullptr)
        return Trap("ElementFormSubmit generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementFormSubmit generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementFormSubmitQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementFormSubmit(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_get_element_by_id(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetElementById == nullptr)
        return Trap("ElementGetElementById generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ElementGetElementById generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlElementGetElementResult result{};
    state->native->rmlUi->ElementGetElementById(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.elementHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_get_elements_by_class_name_count(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetElementsByClassNameCount == nullptr)
        return Trap("ElementGetElementsByClassNameCount generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementGetElementsByClassNameCount generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementGetIntResult result{};
    state->native->rmlUi->ElementGetElementsByClassNameCount(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_get_elements_by_tag_name_count(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementGetElementsByTagNameCount == nullptr)
        return Trap("ElementGetElementsByTagNameCount generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementGetElementsByTagNameCount generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementGetIntResult result{};
    state->native->rmlUi->ElementGetElementsByTagNameCount(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_has_attribute(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementHasAttribute == nullptr)
        return Trap("ElementHasAttribute generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementHasAttribute generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementHasAttribute(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_is_class_set(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementIsClassSet == nullptr)
        return Trap("ElementIsClassSet generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementIsClassSet generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementIsClassSet(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_is_pseudo_class_set(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementIsPseudoClassSet == nullptr)
        return Trap("ElementIsPseudoClassSet generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementIsPseudoClassSet generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementIsPseudoClassSet(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_matches(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementMatches == nullptr)
        return Trap("ElementMatches generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementMatches generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementMatches(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_query_selector(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementQuerySelector == nullptr)
        return Trap("ElementQuerySelector generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ElementQuerySelector generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlElementGetElementResult result{};
    state->native->rmlUi->ElementQuerySelector(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.elementHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_query_selector_all_count(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementQuerySelectorAllCount == nullptr)
        return Trap("ElementQuerySelectorAllCount generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementQuerySelectorAllCount generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementGetIntResult result{};
    state->native->rmlUi->ElementQuerySelectorAllCount(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.value), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_remove_attribute(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementRemoveAttribute == nullptr)
        return Trap("ElementRemoveAttribute generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementRemoveAttribute generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementGetByStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementRemoveAttribute(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_remove_event_listener(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementRemoveEventListener == nullptr)
        return Trap("ElementRemoveEventListener generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("ElementRemoveEventListener generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementEventListenerRemoveQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    query.eventListenerHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventListenerHandle)>>>(slots[1].i64);
    std::uint32_t eventPointer = 0;
    std::uint32_t eventCount = 0;
    if (!reader.U32(eventPointer) || !reader.U32(eventCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> eventBytes;
    if (!state->memory.View(eventPointer, eventCount, eventBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string eventStorage(reinterpret_cast<const char*>(eventBytes.data()), eventBytes.size());
    query.event = eventStorage.c_str();
    query.inCapturePhase = slots[2].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementRemoveEventListener(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_set_attribute(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementSetAttribute == nullptr)
        return Trap("ElementSetAttribute generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementSetAttribute generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementSetAttributeQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementSetAttribute(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_set_class(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementSetClass == nullptr)
        return Trap("ElementSetClass generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ElementSetClass generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementStringBoolQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.value = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementSetClass(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_set_class_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementSetClassName == nullptr)
        return Trap("ElementSetClassName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementSetClassName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementSetStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementSetClassName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_set_id(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementSetId == nullptr)
        return Trap("ElementSetId generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementSetId generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementSetStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementSetId(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_set_inner_rml(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementSetInnerRml == nullptr)
        return Trap("ElementSetInnerRml generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("ElementSetInnerRml generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementSetStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t valuePointer = 0;
    std::uint32_t valueCount = 0;
    if (!reader.U32(valuePointer) || !reader.U32(valueCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> valueBytes;
    if (!state->memory.View(valuePointer, valueCount, valueBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string valueStorage(reinterpret_cast<const char*>(valueBytes.data()), valueBytes.size());
    query.value = valueStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementSetInnerRml(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_set_pseudo_class(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementSetPseudoClass == nullptr)
        return Trap("ElementSetPseudoClass generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ElementSetPseudoClass generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementStringBoolQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    query.value = slots[1].i32 != 0;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementSetPseudoClass(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_tab_set_set_panel(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementTabSetSetPanel == nullptr)
        return Trap("ElementTabSetSetPanel generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ElementTabSetSetPanel generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementTabSetIndexStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    query.index = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.index)>>>(slots[1].i32);
    std::uint32_t rmlPointer = 0;
    std::uint32_t rmlCount = 0;
    if (!reader.U32(rmlPointer) || !reader.U32(rmlCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> rmlBytes;
    if (!state->memory.View(rmlPointer, rmlCount, rmlBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string rmlStorage(reinterpret_cast<const char*>(rmlBytes.data()), rmlBytes.size());
    query.rml = rmlStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementTabSetSetPanel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_element_tab_set_set_tab(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->ElementTabSetSetTab == nullptr)
        return Trap("ElementTabSetSetTab generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("ElementTabSetSetTab generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlElementTabSetIndexStringQuery query{};
    query.elementHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.elementHandle)>>>(slots[0].i64);
    query.index = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.index)>>>(slots[1].i32);
    std::uint32_t rmlPointer = 0;
    std::uint32_t rmlCount = 0;
    if (!reader.U32(rmlPointer) || !reader.U32(rmlCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> rmlBytes;
    if (!state->memory.View(rmlPointer, rmlCount, rmlBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string rmlStorage(reinterpret_cast<const char*>(rmlBytes.data()), rmlBytes.size());
    query.rml = rmlStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->ElementTabSetSetTab(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_event_get_parameter_bool(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->EventGetParameterBool == nullptr)
        return Trap("EventGetParameterBool generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("EventGetParameterBool generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlEventParameterQuery query{};
    query.eventHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlEventGetBoolResult result{};
    state->native->rmlUi->EventGetParameterBool(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.Bool(result.value)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_event_get_parameter_float(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->EventGetParameterFloat == nullptr)
        return Trap("EventGetParameterFloat generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("EventGetParameterFloat generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlEventParameterQuery query{};
    query.eventHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlEventGetFloatResult result{};
    state->native->rmlUi->EventGetParameterFloat(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.F32(result.value)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_event_get_parameter_int(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->EventGetParameterInt == nullptr)
        return Trap("EventGetParameterInt generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("EventGetParameterInt generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlEventParameterQuery query{};
    query.eventHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlEventGetIntResult result{};
    state->native->rmlUi->EventGetParameterInt(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.I32(result.value)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_event_get_parameter_type(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->EventGetParameterType == nullptr)
        return Trap("EventGetParameterType generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("EventGetParameterType generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlEventParameterQuery query{};
    query.eventHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.eventHandle)>>>(slots[0].i64);
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlEventGetIntResult result{};
    state->native->rmlUi->EventGetParameterType(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.I32(result.value)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_get_context(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->GetContext == nullptr)
        return Trap("GetContext generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetContext generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[1].i32);
    if (!state->memory.Contains(output, 16u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    RmlGetContextQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    RmlGetContextResult result{};
    state->native->rmlUi->GetContext(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) { slots[0].i32 = errorCode; return nullptr; }
    std::array<std::uint8_t, 16> wire{};
    WireWriter writer(wire);
    if (!writer.U64(result.contextHandle)) return Trap("generated Core wire overflow");
    if (!writer.Bool(result.exists)) return Trap("generated Core wire overflow");
    if (!writer.Finish(8u)) return Trap("generated Core fixed output layout mismatch");
    if (!state->memory.Write(output, wire.data(), wire.size())) return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_remove_context_by_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->RemoveContextByName == nullptr)
        return Trap("RemoveContextByName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("RemoveContextByName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlRemoveContextByNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlRemoveContextByNameResult result{};
    state->native->rmlUi->RemoveContextByName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_set_debug_context_by_name(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->SetDebugContextByName == nullptr)
        return Trap("SetDebugContextByName generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("SetDebugContextByName generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlSetDebugContextByNameQuery query{};
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlSetDebugContextByNameResult result{};
    state->native->rmlUi->SetDebugContextByName(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_set_mouse_cursor_alias(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->SetMouseCursorAlias == nullptr)
        return Trap("SetMouseCursorAlias generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("SetMouseCursorAlias generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlSetMouseCursorAliasQuery query{};
    std::uint32_t rmlNamePointer = 0;
    std::uint32_t rmlNameCount = 0;
    if (!reader.U32(rmlNamePointer) || !reader.U32(rmlNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> rmlNameBytes;
    if (!state->memory.View(rmlNamePointer, rmlNameCount, rmlNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string rmlNameStorage(reinterpret_cast<const char*>(rmlNameBytes.data()), rmlNameBytes.size());
    query.rmlName = rmlNameStorage.c_str();
    std::uint32_t recoilNamePointer = 0;
    std::uint32_t recoilNameCount = 0;
    if (!reader.U32(recoilNamePointer) || !reader.U32(recoilNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> recoilNameBytes;
    if (!state->memory.View(recoilNamePointer, recoilNameCount, recoilNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string recoilNameStorage(reinterpret_cast<const char*>(recoilNameBytes.data()), recoilNameBytes.size());
    query.recoilName = recoilNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlSetMouseCursorAliasResult result{};
    state->native->rmlUi->SetMouseCursorAlias(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_rml_ui_sol_lua_data_model_set_dirty(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->rmlUi == nullptr ||
        state->native->rmlUi->SolLuaDataModelSetDirty == nullptr)
        return Trap("SolLuaDataModelSetDirty generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SolLuaDataModelSetDirty generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    RmlSolLuaDataModelSetDirtyQuery query{};
    query.dataModelHandle = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.dataModelHandle)>>>(slots[0].i64);
    std::uint32_t propertyPointer = 0;
    std::uint32_t propertyCount = 0;
    if (!reader.U32(propertyPointer) || !reader.U32(propertyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> propertyBytes;
    if (!state->memory.View(propertyPointer, propertyCount, propertyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string propertyStorage(reinterpret_cast<const char*>(propertyBytes.data()), propertyBytes.size());
    query.property = propertyStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    RmlElementBoolResult result{};
    state->native->rmlUi->SolLuaDataModelSetDirty(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_vfs_compress_folder(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->CompressFolder == nullptr)
        return Trap("CompressFolder generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("CompressFolder generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 32u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    CompressFolderQuery query{};
    std::uint32_t folderPathPointer = 0;
    std::uint32_t folderPathCount = 0;
    if (!reader.U32(folderPathPointer) || !reader.U32(folderPathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> folderPathBytes;
    if (!state->memory.View(folderPathPointer, folderPathCount, folderPathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string folderPathStorage(reinterpret_cast<const char*>(folderPathBytes.data()), folderPathBytes.size());
    query.folderPath = folderPathStorage.c_str();
    std::uint32_t archiveTypePointer = 0;
    std::uint32_t archiveTypeCount = 0;
    if (!reader.U32(archiveTypePointer) || !reader.U32(archiveTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> archiveTypeBytes;
    if (!state->memory.View(archiveTypePointer, archiveTypeCount, archiveTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string archiveTypeStorage(reinterpret_cast<const char*>(archiveTypeBytes.data()), archiveTypeBytes.size());
    query.archiveType = archiveTypeStorage.c_str();
    std::uint32_t compressedFilePathPointer = 0;
    std::uint32_t compressedFilePathCount = 0;
    if (!reader.U32(compressedFilePathPointer) || !reader.U32(compressedFilePathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> compressedFilePathBytes;
    if (!state->memory.View(compressedFilePathPointer, compressedFilePathCount, compressedFilePathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string compressedFilePathStorage(reinterpret_cast<const char*>(compressedFilePathBytes.data()), compressedFilePathBytes.size());
    query.compressedFilePath = compressedFilePathStorage.c_str();
    query.includeFolder = slots[0].i32 != 0;
    std::uint32_t modePointer = 0;
    std::uint32_t modeCount = 0;
    if (!reader.U32(modePointer) || !reader.U32(modeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> modeBytes;
    if (!state->memory.View(modePointer, modeCount, modeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string modeStorage(reinterpret_cast<const char*>(modeBytes.data()), modeBytes.size());
    query.mode = modeStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    CompressFolderResult result{};
    state->native->vfs->CompressFolder(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_vfs_create_dir(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->CreateDir == nullptr)
        return Trap("CreateDir generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("CreateDir generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    CreateDirQuery query{};
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCount = 0;
    if (!reader.U32(pathPointer) || !reader.U32(pathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pathBytes;
    if (!state->memory.View(pathPointer, pathCount, pathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string pathStorage(reinterpret_cast<const char*>(pathBytes.data()), pathBytes.size());
    query.path = pathStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    CreateDirResult result{};
    state->native->vfs->CreateDir(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_vfs_download_archive(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->DownloadArchive == nullptr)
        return Trap("DownloadArchive generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("DownloadArchive generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    WireReader reader(descriptorWire);

    DownloadArchiveQuery query{};
    std::uint32_t filenamePointer = 0;
    std::uint32_t filenameCount = 0;
    if (!reader.U32(filenamePointer) || !reader.U32(filenameCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> filenameBytes;
    if (!state->memory.View(filenamePointer, filenameCount, filenameBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string filenameStorage(reinterpret_cast<const char*>(filenameBytes.data()), filenameBytes.size());
    query.filename = filenameStorage.c_str();
    std::uint32_t categoryPointer = 0;
    std::uint32_t categoryCount = 0;
    if (!reader.U32(categoryPointer) || !reader.U32(categoryCount)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    std::span<const std::uint8_t> categoryBytes;
    if (!state->memory.View(categoryPointer, categoryCount, categoryBytes)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }
    std::string categoryStorage(reinterpret_cast<const char*>(categoryBytes.data()), categoryBytes.size());
    query.category = categoryStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::InvalidArgument);
        return nullptr;
    }
    DownloadArchiveResult result{};
    state->native->vfs->DownloadArchive(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i32 = errorCode;
    return nullptr;
}

wasm_trap_t* CoreVariable_vfs_extract_mod_archive_file(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->ExtractModArchiveFile == nullptr)
        return Trap("ExtractModArchiveFile generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("ExtractModArchiveFile generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    ExtractModArchiveFileQuery query{};
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCount = 0;
    if (!reader.U32(pathPointer) || !reader.U32(pathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pathBytes;
    if (!state->memory.View(pathPointer, pathCount, pathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string pathStorage(reinterpret_cast<const char*>(pathBytes.data()), pathBytes.size());
    query.path = pathStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    ExtractModArchiveFileResult result{};
    state->native->vfs->ExtractModArchiveFile(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_vfs_file_exists(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->FileExists == nullptr)
        return Trap("FileExists generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("FileExists generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    FileExistsQuery query{};
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCount = 0;
    if (!reader.U32(pathPointer) || !reader.U32(pathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pathBytes;
    if (!state->memory.View(pathPointer, pathCount, pathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string pathStorage(reinterpret_cast<const char*>(pathBytes.data()), pathBytes.size());
    query.path = pathStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    FileExistsResult result{};
    state->native->vfs->FileExists(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.exists ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_vfs_get_file_size(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->GetFileSize == nullptr)
        return Trap("GetFileSize generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("GetFileSize generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetFileSizeQuery query{};
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCount = 0;
    if (!reader.U32(pathPointer) || !reader.U32(pathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pathBytes;
    if (!state->memory.View(pathPointer, pathCount, pathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string pathStorage(reinterpret_cast<const char*>(pathBytes.data()), pathBytes.size());
    query.path = pathStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetFileSizeResult result{};
    state->native->vfs->GetFileSize(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.size), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_vfs_get_map_square_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->GetMapSquareTexture == nullptr)
        return Trap("GetMapSquareTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 5)
        return Trap("GetMapSquareTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[4].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetMapSquareTextureQuery query{};
    query.texSquareX = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.texSquareX)>>>(slots[0].i32);
    query.texSquareY = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.texSquareY)>>>(slots[1].i32);
    query.lodMin = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.lodMin)>>>(slots[2].i32);
    std::uint32_t textureNamePointer = 0;
    std::uint32_t textureNameCount = 0;
    if (!reader.U32(textureNamePointer) || !reader.U32(textureNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textureNameBytes;
    if (!state->memory.View(textureNamePointer, textureNameCount, textureNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string textureNameStorage(reinterpret_cast<const char*>(textureNameBytes.data()), textureNameBytes.size());
    query.textureName = textureNameStorage.c_str();
    query.lodMax = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.lodMax)>>>(slots[3].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetMapSquareTextureResult result{};
    state->native->vfs->GetMapSquareTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_vfs_has_archive(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->HasArchive == nullptr)
        return Trap("HasArchive generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("HasArchive generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    HasArchiveQuery query{};
    std::uint32_t archiveNamePointer = 0;
    std::uint32_t archiveNameCount = 0;
    if (!reader.U32(archiveNamePointer) || !reader.U32(archiveNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> archiveNameBytes;
    if (!state->memory.View(archiveNamePointer, archiveNameCount, archiveNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string archiveNameStorage(reinterpret_cast<const char*>(archiveNameBytes.data()), archiveNameBytes.size());
    query.archiveName = archiveNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    HasArchiveResult result{};
    state->native->vfs->HasArchive(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.hasArchive ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_vfs_is_directory(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->IsDirectory == nullptr)
        return Trap("IsDirectory generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("IsDirectory generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    IsDirectoryQuery query{};
    std::uint32_t pathPointer = 0;
    std::uint32_t pathCount = 0;
    if (!reader.U32(pathPointer) || !reader.U32(pathCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pathBytes;
    if (!state->memory.View(pathPointer, pathCount, pathBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string pathStorage(reinterpret_cast<const char*>(pathBytes.data()), pathBytes.size());
    query.path = pathStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    IsDirectoryResult result{};
    state->native->vfs->IsDirectory(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.isDirectory ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_vfs_set_map_square_texture(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->vfs == nullptr ||
        state->native->vfs->SetMapSquareTexture == nullptr)
        return Trap("SetMapSquareTexture generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("SetMapSquareTexture generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetMapSquareTextureQuery query{};
    query.texSquareX = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.texSquareX)>>>(slots[0].i32);
    query.texSquareY = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.texSquareY)>>>(slots[1].i32);
    std::uint32_t textureNamePointer = 0;
    std::uint32_t textureNameCount = 0;
    if (!reader.U32(textureNamePointer) || !reader.U32(textureNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> textureNameBytes;
    if (!state->memory.View(textureNamePointer, textureNameCount, textureNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string textureNameStorage(reinterpret_cast<const char*>(textureNameBytes.data()), textureNameBytes.size());
    query.textureName = textureNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetMapSquareTextureResult result{};
    state->native->vfs->SetMapSquareTexture(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_team_control_add_team_resource(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->AddTeamResource == nullptr)
        return Trap("AddTeamResource generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("AddTeamResource generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    AddTeamResourceQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, resourceTypeCount, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string resourceTypeStorage(reinterpret_cast<const char*>(resourceTypeBytes.data()), resourceTypeBytes.size());
    query.resourceType = resourceTypeStorage.c_str();
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    AddTeamResourceResult result{};
    state->native->syncedCtrl->team->AddTeamResource(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_team_control_add_team_resource_excess_stats(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->AddTeamResourceExcessStats == nullptr)
        return Trap("AddTeamResourceExcessStats generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("AddTeamResourceExcessStats generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    AddTeamResourceExcessStatsQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, resourceTypeCount, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string resourceTypeStorage(reinterpret_cast<const char*>(resourceTypeBytes.data()), resourceTypeBytes.size());
    query.resourceType = resourceTypeStorage.c_str();
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    AddTeamResourceExcessStatsResult result{};
    state->native->syncedCtrl->team->AddTeamResourceExcessStats(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_team_control_game_over(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->GameOver == nullptr)
        return Trap("GameOver generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 1)
        return Trap("GameOver generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 2u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[0].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GameOverQuery query{};
    std::uint32_t winningAllyTeamsPointer = 0;
    std::uint32_t winningAllyTeamsCount = 0;
    if (!reader.U32(winningAllyTeamsPointer) || !reader.U32(winningAllyTeamsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t winningAllyTeamsBytes64 = static_cast<std::uint64_t>(winningAllyTeamsCount) * 4u;
    if (winningAllyTeamsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> winningAllyTeamsWire;
    if (!state->memory.View(winningAllyTeamsPointer, static_cast<std::size_t>(winningAllyTeamsBytes64), winningAllyTeamsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader winningAllyTeamsReader(winningAllyTeamsWire);
    std::vector<std::int32_t> winningAllyTeamsStorage;
    winningAllyTeamsStorage.reserve(winningAllyTeamsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < winningAllyTeamsCount; ++coreIndex) {
        std::int32_t item{};
        { std::int32_t coreRaw = 0; if (!winningAllyTeamsReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        winningAllyTeamsStorage.push_back(item);
    }
    if (!winningAllyTeamsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.winningAllyTeams = winningAllyTeamsStorage.data();
    if (!AssignCoreCount(winningAllyTeamsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GameOverResult result{};
    state->native->syncedCtrl->team->GameOver(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_team_control_set_team_resource(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->SetTeamResource == nullptr)
        return Trap("SetTeamResource generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("SetTeamResource generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetTeamResourceQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, resourceTypeCount, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string resourceTypeStorage(reinterpret_cast<const char*>(resourceTypeBytes.data()), resourceTypeBytes.size());
    query.resourceType = resourceTypeStorage.c_str();
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetTeamResourceResult result{};
    state->native->syncedCtrl->team->SetTeamResource(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_team_control_set_team_share_level(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->SetTeamShareLevel == nullptr)
        return Trap("SetTeamShareLevel generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("SetTeamShareLevel generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetTeamShareLevelQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, resourceTypeCount, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string resourceTypeStorage(reinterpret_cast<const char*>(resourceTypeBytes.data()), resourceTypeBytes.size());
    query.resourceType = resourceTypeStorage.c_str();
    query.shareLevel = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetTeamShareLevelResult result{};
    state->native->syncedCtrl->team->SetTeamShareLevel(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_team_control_share_team_resource(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->ShareTeamResource == nullptr)
        return Trap("ShareTeamResource generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("ShareTeamResource generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    ShareTeamResourceQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    query.targetTeamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.targetTeamID)>>>(slots[1].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, resourceTypeCount, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string resourceTypeStorage(reinterpret_cast<const char*>(resourceTypeBytes.data()), resourceTypeBytes.size());
    query.resourceType = resourceTypeStorage.c_str();
    query.amount = slots[2].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    ShareTeamResourceResult result{};
    state->native->syncedCtrl->team->ShareTeamResource(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_team_control_use_team_resource(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->team == nullptr ||
        state->native->syncedCtrl->team->UseTeamResource == nullptr)
        return Trap("UseTeamResource generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("UseTeamResource generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    UseTeamResourceQuery query{};
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, resourceTypeCount, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string resourceTypeStorage(reinterpret_cast<const char*>(resourceTypeBytes.data()), resourceTypeBytes.size());
    query.resourceType = resourceTypeStorage.c_str();
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    UseTeamResourceResult result{};
    state->native->syncedCtrl->team->UseTeamResource(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_add_unit_resource(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->AddUnitResource == nullptr)
        return Trap("AddUnitResource generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("AddUnitResource generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    AddUnitResourceQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, resourceTypeCount, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string resourceTypeStorage(reinterpret_cast<const char*>(resourceTypeBytes.data()), resourceTypeBytes.size());
    query.resourceType = resourceTypeStorage.c_str();
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    AddUnitResourceResult result{};
    state->native->syncedCtrl->unit->AddUnitResource(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_bugger_off(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->BuggerOff == nullptr)
        return Trap("BuggerOff generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("BuggerOff generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 32u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    BuggerOffQuery query{};
    if (!reader.F32(query.pos.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.pos.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.pos.z)) return Trap("generated Core wire underflow");
    query.radius = slots[0].f32;
    query.teamID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.teamID)>>>(slots[1].i32);
    if (!reader.Bool(query.options.spherical)) return Trap("generated Core wire underflow");
    if (!reader.Bool(query.options.forced)) return Trap("generated Core wire underflow");
    { std::int32_t coreRaw = 0; if (!reader.I32(coreRaw)) return Trap("generated Core wire underflow"); query.options.excludeUnitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options.excludeUnitID)>>>(coreRaw); }
    std::uint32_t excludeUnitDefIDsPointer = 0;
    std::uint32_t excludeUnitDefIDsCount = 0;
    if (!reader.U32(excludeUnitDefIDsPointer) || !reader.U32(excludeUnitDefIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t excludeUnitDefIDsBytes64 = static_cast<std::uint64_t>(excludeUnitDefIDsCount) * 4u;
    if (excludeUnitDefIDsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> excludeUnitDefIDsWire;
    if (!state->memory.View(excludeUnitDefIDsPointer, static_cast<std::size_t>(excludeUnitDefIDsBytes64), excludeUnitDefIDsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader excludeUnitDefIDsReader(excludeUnitDefIDsWire);
    std::vector<std::int32_t> excludeUnitDefIDsStorage;
    excludeUnitDefIDsStorage.reserve(excludeUnitDefIDsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < excludeUnitDefIDsCount; ++coreIndex) {
        std::int32_t item{};
        { std::int32_t coreRaw = 0; if (!excludeUnitDefIDsReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        excludeUnitDefIDsStorage.push_back(item);
    }
    if (!excludeUnitDefIDsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.excludeUnitDefIDs = excludeUnitDefIDsStorage.data();
    if (!AssignCoreCount(excludeUnitDefIDsCount, query.excludeUnitDefCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    BuggerOffResult result{};
    state->native->syncedCtrl->unit->BuggerOff(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_give_order_to_unit_array(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->GiveOrderToUnitArray == nullptr)
        return Trap("GiveOrderToUnitArray generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("GiveOrderToUnitArray generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 16u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GiveOrderToUnitArrayQuery query{};
    std::uint32_t unitIDsPointer = 0;
    std::uint32_t unitIDsCount = 0;
    if (!reader.U32(unitIDsPointer) || !reader.U32(unitIDsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t unitIDsBytes64 = static_cast<std::uint64_t>(unitIDsCount) * 4u;
    if (unitIDsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> unitIDsWire;
    if (!state->memory.View(unitIDsPointer, static_cast<std::size_t>(unitIDsBytes64), unitIDsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader unitIDsReader(unitIDsWire);
    std::vector<std::int32_t> unitIDsStorage;
    unitIDsStorage.reserve(unitIDsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < unitIDsCount; ++coreIndex) {
        std::int32_t item{};
        { std::int32_t coreRaw = 0; if (!unitIDsReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        unitIDsStorage.push_back(item);
    }
    if (!unitIDsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.unitIDs = unitIDsStorage.data();
    if (!AssignCoreCount(unitIDsCount, query.count)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.cmdID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.cmdID)>>>(slots[0].i32);
    std::uint32_t paramsPointer = 0;
    std::uint32_t paramsCount = 0;
    if (!reader.U32(paramsPointer) || !reader.U32(paramsCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t paramsBytes64 = static_cast<std::uint64_t>(paramsCount) * 4u;
    if (paramsBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> paramsWire;
    if (!state->memory.View(paramsPointer, static_cast<std::size_t>(paramsBytes64), paramsWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader paramsReader(paramsWire);
    std::vector<float> paramsStorage;
    paramsStorage.reserve(paramsCount);
    for (std::uint32_t coreIndex = 0; coreIndex < paramsCount; ++coreIndex) {
        float item{};
        if (!paramsReader.F32(item)) return Trap("generated Core wire underflow");
        paramsStorage.push_back(item);
    }
    if (!paramsReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.params = paramsStorage.data();
    if (!AssignCoreCount(paramsCount, query.paramCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.options = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.options)>>>(slots[1].i32);
    query.timeout = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.timeout)>>>(slots[2].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GiveOrderToUnitArrayResult result{};
    state->native->syncedCtrl->unit->GiveOrderToUnitArray(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_set_unit_build_params(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitBuildParams == nullptr)
        return Trap("SetUnitBuildParams generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetUnitBuildParams generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 20u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitBuildParamsQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t paramNamePointer = 0;
    std::uint32_t paramNameCount = 0;
    if (!reader.U32(paramNamePointer) || !reader.U32(paramNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> paramNameBytes;
    if (!state->memory.View(paramNamePointer, paramNameCount, paramNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string paramNameStorage(reinterpret_cast<const char*>(paramNameBytes.data()), paramNameBytes.size());
    query.paramName = paramNameStorage.c_str();
    if (!reader.F32(query.value.number)) return Trap("generated Core wire underflow");
    if (!reader.Bool(query.value.boolean)) return Trap("generated Core wire underflow");
    if (!reader.Bool(query.value.useBoolean)) return Trap("generated Core wire underflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitBuildParamsResult result{};
    state->native->syncedCtrl->unit->SetUnitBuildParams(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_set_unit_flanking(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitFlanking == nullptr)
        return Trap("SetUnitFlanking generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetUnitFlanking generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 20u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitFlankingQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t typePointer = 0;
    std::uint32_t typeCount = 0;
    if (!reader.U32(typePointer) || !reader.U32(typeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> typeBytes;
    if (!state->memory.View(typePointer, typeCount, typeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string typeStorage(reinterpret_cast<const char*>(typeBytes.data()), typeBytes.size());
    query.type = typeStorage.c_str();
    if (!reader.F32(query.args.x)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.args.y)) return Trap("generated Core wire underflow");
    if (!reader.F32(query.args.z)) return Trap("generated Core wire underflow");
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitFlankingResult result{};
    state->native->syncedCtrl->unit->SetUnitFlanking(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_set_unit_nano_pieces(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitNanoPieces == nullptr)
        return Trap("SetUnitNanoPieces generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetUnitNanoPieces generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitNanoPiecesQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t pieceIndicesPointer = 0;
    std::uint32_t pieceIndicesCount = 0;
    if (!reader.U32(pieceIndicesPointer) || !reader.U32(pieceIndicesCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    const std::uint64_t pieceIndicesBytes64 = static_cast<std::uint64_t>(pieceIndicesCount) * 4u;
    if (pieceIndicesBytes64 > std::numeric_limits<std::size_t>::max()) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> pieceIndicesWire;
    if (!state->memory.View(pieceIndicesPointer, static_cast<std::size_t>(pieceIndicesBytes64), pieceIndicesWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader pieceIndicesReader(pieceIndicesWire);
    std::vector<std::int32_t> pieceIndicesStorage;
    pieceIndicesStorage.reserve(pieceIndicesCount);
    for (std::uint32_t coreIndex = 0; coreIndex < pieceIndicesCount; ++coreIndex) {
        std::int32_t item{};
        { std::int32_t coreRaw = 0; if (!pieceIndicesReader.I32(coreRaw)) return Trap("generated Core wire underflow"); item = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(item)>>>(coreRaw); }
        pieceIndicesStorage.push_back(item);
    }
    if (!pieceIndicesReader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    query.pieceIndices = pieceIndicesStorage.data();
    if (!AssignCoreCount(pieceIndicesCount, query.pieceCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitNanoPiecesResult result{};
    state->native->syncedCtrl->unit->SetUnitNanoPieces(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_set_unit_resourcing(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitResourcing == nullptr)
        return Trap("SetUnitResourcing generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("SetUnitResourcing generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitResourcingQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t typePointer = 0;
    std::uint32_t typeCount = 0;
    if (!reader.U32(typePointer) || !reader.U32(typeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> typeBytes;
    if (!state->memory.View(typePointer, typeCount, typeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string typeStorage(reinterpret_cast<const char*>(typeBytes.data()), typeBytes.size());
    query.type = typeStorage.c_str();
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitResourcingResult result{};
    state->native->syncedCtrl->unit->SetUnitResourcing(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_set_unit_sensor_radius(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitSensorRadius == nullptr)
        return Trap("SetUnitSensorRadius generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("SetUnitSensorRadius generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitSensorRadiusQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t sensorTypePointer = 0;
    std::uint32_t sensorTypeCount = 0;
    if (!reader.U32(sensorTypePointer) || !reader.U32(sensorTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> sensorTypeBytes;
    if (!state->memory.View(sensorTypePointer, sensorTypeCount, sensorTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string sensorTypeStorage(reinterpret_cast<const char*>(sensorTypeBytes.data()), sensorTypeBytes.size());
    query.sensorType = sensorTypeStorage.c_str();
    query.radius = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.radius)>>>(slots[1].i32);
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitSensorRadiusResult result{};
    state->native->syncedCtrl->unit->SetUnitSensorRadius(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.newRadius), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_set_unit_storage(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitStorage == nullptr)
        return Trap("SetUnitStorage generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("SetUnitStorage generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitStorageQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t resourcePointer = 0;
    std::uint32_t resourceCount = 0;
    if (!reader.U32(resourcePointer) || !reader.U32(resourceCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceBytes;
    if (!state->memory.View(resourcePointer, resourceCount, resourceBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string resourceStorage(reinterpret_cast<const char*>(resourceBytes.data()), resourceBytes.size());
    query.resource = resourceStorage.c_str();
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitStorageResult result{};
    state->native->syncedCtrl->unit->SetUnitStorage(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_set_unit_tooltip(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitTooltip == nullptr)
        return Trap("SetUnitTooltip generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetUnitTooltip generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitTooltipQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t tooltipPointer = 0;
    std::uint32_t tooltipCount = 0;
    if (!reader.U32(tooltipPointer) || !reader.U32(tooltipCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> tooltipBytes;
    if (!state->memory.View(tooltipPointer, tooltipCount, tooltipBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string tooltipStorage(reinterpret_cast<const char*>(tooltipBytes.data()), tooltipBytes.size());
    query.tooltip = tooltipStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitTooltipResult result{};
    state->native->syncedCtrl->unit->SetUnitTooltip(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_set_unit_weapon_damages(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitWeaponDamages == nullptr)
        return Trap("SetUnitWeaponDamages generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("SetUnitWeaponDamages generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitWeaponDamagesQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    query.weaponNum = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.weaponNum)>>>(slots[1].i32);
    std::uint32_t damageKeyPointer = 0;
    std::uint32_t damageKeyCount = 0;
    if (!reader.U32(damageKeyPointer) || !reader.U32(damageKeyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> damageKeyBytes;
    if (!state->memory.View(damageKeyPointer, damageKeyCount, damageKeyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string damageKeyStorage(reinterpret_cast<const char*>(damageKeyBytes.data()), damageKeyBytes.size());
    query.damageKey = damageKeyStorage.c_str();
    query.damageValue = slots[2].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitWeaponDamagesResult result{};
    state->native->syncedCtrl->unit->SetUnitWeaponDamages(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_set_unit_weapon_state(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->SetUnitWeaponState == nullptr)
        return Trap("SetUnitWeaponState generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("SetUnitWeaponState generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetUnitWeaponStateQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    query.weaponNum = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.weaponNum)>>>(slots[1].i32);
    std::uint32_t keyPointer = 0;
    std::uint32_t keyCount = 0;
    if (!reader.U32(keyPointer) || !reader.U32(keyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> keyBytes;
    if (!state->memory.View(keyPointer, keyCount, keyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string keyStorage(reinterpret_cast<const char*>(keyBytes.data()), keyBytes.size());
    query.key = keyStorage.c_str();
    query.value = slots[2].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetUnitWeaponStateResult result{};
    state->native->syncedCtrl->unit->SetUnitWeaponState(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_unit_control_use_unit_resource(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->unit == nullptr ||
        state->native->syncedCtrl->unit->UseUnitResource == nullptr)
        return Trap("UseUnitResource generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 3)
        return Trap("UseUnitResource generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[2].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    UseUnitResourceQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t resourceTypePointer = 0;
    std::uint32_t resourceTypeCount = 0;
    if (!reader.U32(resourceTypePointer) || !reader.U32(resourceTypeCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> resourceTypeBytes;
    if (!state->memory.View(resourceTypePointer, resourceTypeCount, resourceTypeBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string resourceTypeStorage(reinterpret_cast<const char*>(resourceTypeBytes.data()), resourceTypeBytes.size());
    query.resourceType = resourceTypeStorage.c_str();
    query.amount = slots[1].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    UseUnitResourceResult result{};
    state->native->syncedCtrl->unit->UseUnitResource(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_terrain_control_set_terrain_type_data(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->terrain == nullptr ||
        state->native->syncedCtrl->terrain->SetTerrainTypeData == nullptr)
        return Trap("SetTerrainTypeData generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 8)
        return Trap("SetTerrainTypeData generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 9u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[7].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetTerrainTypeDataQuery query{};
    query.typeIndex = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.typeIndex)>>>(slots[0].i32);
    query.tankSpeed = slots[1].f32;
    query.kbotSpeed = slots[2].f32;
    query.hoverSpeed = slots[3].f32;
    query.shipSpeed = slots[4].f32;
    query.hardness = slots[5].f32;
    query.receiveTracks = slots[6].i32 != 0;
    std::uint32_t namePointer = 0;
    std::uint32_t nameCount = 0;
    if (!reader.U32(namePointer) || !reader.U32(nameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> nameBytes;
    if (!state->memory.View(namePointer, nameCount, nameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string nameStorage(reinterpret_cast<const char*>(nameBytes.data()), nameBytes.size());
    query.name = nameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetTerrainTypeDataResult result{};
    state->native->syncedCtrl->terrain->SetTerrainTypeData(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_projectile_control_set_projectile_ceg(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->projectile == nullptr ||
        state->native->syncedCtrl->projectile->SetProjectileCEG == nullptr)
        return Trap("SetProjectileCEG generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("SetProjectileCEG generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetProjectileCEGQuery query{};
    query.projectileID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.projectileID)>>>(slots[0].i32);
    std::uint32_t cegNamePointer = 0;
    std::uint32_t cegNameCount = 0;
    if (!reader.U32(cegNamePointer) || !reader.U32(cegNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> cegNameBytes;
    if (!state->memory.View(cegNamePointer, cegNameCount, cegNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string cegNameStorage(reinterpret_cast<const char*>(cegNameBytes.data()), cegNameBytes.size());
    query.cegName = cegNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetProjectileCEGResult result{};
    state->native->syncedCtrl->projectile->SetProjectileCEG(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.cegID), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_projectile_control_set_projectile_damages(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->projectile == nullptr ||
        state->native->syncedCtrl->projectile->SetProjectileDamages == nullptr)
        return Trap("SetProjectileDamages generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 4)
        return Trap("SetProjectileDamages generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[3].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    SetProjectileDamagesQuery query{};
    query.projectileID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.projectileID)>>>(slots[0].i32);
    query.unused = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unused)>>>(slots[1].i32);
    std::uint32_t damageKeyPointer = 0;
    std::uint32_t damageKeyCount = 0;
    if (!reader.U32(damageKeyPointer) || !reader.U32(damageKeyCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> damageKeyBytes;
    if (!state->memory.View(damageKeyPointer, damageKeyCount, damageKeyBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string damageKeyStorage(reinterpret_cast<const char*>(damageKeyBytes.data()), damageKeyBytes.size());
    query.damageKey = damageKeyStorage.c_str();
    query.damageValue = slots[2].f32;
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    SetProjectileDamagesResult result{};
    state->native->syncedCtrl->projectile->SetProjectileDamages(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* CoreVariable_cob_script_get_cob_script_id(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->syncedCtrl == nullptr || state->native->syncedCtrl->cobScript == nullptr ||
        state->native->syncedCtrl->cobScript->GetCOBScriptID == nullptr)
        return Trap("GetCOBScriptID generated Core binding is unavailable");
    if (slots == nullptr || slotCount != 2)
        return Trap("GetCOBScriptID generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 3u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t descriptor = static_cast<std::uint32_t>(slots[1].i32);
    std::span<const std::uint8_t> descriptorWire;
    if (!state->memory.View(descriptor, 8u, descriptorWire)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    WireReader reader(descriptorWire);

    GetCOBScriptIDQuery query{};
    query.unitID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.unitID)>>>(slots[0].i32);
    std::uint32_t funcNamePointer = 0;
    std::uint32_t funcNameCount = 0;
    if (!reader.U32(funcNamePointer) || !reader.U32(funcNameCount)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    std::span<const std::uint8_t> funcNameBytes;
    if (!state->memory.View(funcNamePointer, funcNameCount, funcNameBytes)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::OutOfBounds)));
        return nullptr;
    }
    std::string funcNameStorage(reinterpret_cast<const char*>(funcNameBytes.data()), funcNameBytes.size());
    query.funcName = funcNameStorage.c_str();
    if (!reader.Finish(4u)) {
        slots[0].i64 = static_cast<std::int64_t>(PackU32(0, static_cast<std::int32_t>(Status::InvalidArgument)));
        return nullptr;
    }
    GetCOBScriptIDResult result{};
    state->native->syncedCtrl->cobScript->GetCOBScriptID(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.funcID), errorCode));
    return nullptr;
}


} // namespace

bool RegisterGeneratedVariableImports(wasmtime_linker_t* linker, HostState* state,
    std::string& error)
{
    if (linker == nullptr || state == nullptr || state->native == nullptr) {
        error = "cannot register generated variable Core imports without linker/host/native API";
        return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:units-info", "get-unit-sensor-radius",
                MakeFuncType(params, 3, results, 1), CoreVariable_units_info_get_unit_sensor_radius, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:units-weapons", "get-unit-weapon-state",
                MakeFuncType(params, 4, results, 1), CoreVariable_units_weapons_get_unit_weapon_state, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:units-pieces", "get-model-root-piece",
                MakeFuncType(params, 1, results, 1), CoreVariable_units_pieces_get_model_root_piece, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:teams", "get-team-resource-stats",
                MakeFuncType(params, 3, results, 1), CoreVariable_teams_get_team_resource_stats, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:teams", "get-team-resources",
                MakeFuncType(params, 3, results, 1), CoreVariable_teams_get_team_resources, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-defs", "get-unit-def-id-by-name",
                MakeFuncType(params, 1, results, 1), CoreVariable_unit_defs_get_unit_def_id_by_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-defs", "get-unit-def-param-bool",
                MakeFuncType(params, 2, results, 1), CoreVariable_unit_defs_get_unit_def_param_bool, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-defs", "get-unit-def-param-float",
                MakeFuncType(params, 2, results, 1), CoreVariable_unit_defs_get_unit_def_param_float, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-defs", "get-unit-def-param-int",
                MakeFuncType(params, 2, results, 1), CoreVariable_unit_defs_get_unit_def_param_int, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-defs", "get-unit-def-param-type",
                MakeFuncType(params, 1, results, 1), CoreVariable_unit_defs_get_unit_def_param_type, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:feature-defs", "get-feature-def-id-by-name",
                MakeFuncType(params, 1, results, 1), CoreVariable_feature_defs_get_feature_def_id_by_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:weapon-defs", "get-weapon-def-id",
                MakeFuncType(params, 1, results, 1), CoreVariable_weapon_defs_get_weapon_def_id, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:math-extra", "bit-bits",
                MakeFuncType(params, 1, results, 1), CoreVariable_math_extra_bit_bits, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:math-extra", "diag",
                MakeFuncType(params, 1, results, 1), CoreVariable_math_extra_diag, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:encoding", "is-valid-base64",
                MakeFuncType(params, 1, results, 1), CoreVariable_encoding_is_valid_base64, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:encoding", "is-valid-base64-url",
                MakeFuncType(params, 1, results, 1), CoreVariable_encoding_is_valid_base64_url, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:input", "get-key-code",
                MakeFuncType(params, 1, results, 1), CoreVariable_input_get_key_code, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:debug-input", "emulate-text-editing",
                MakeFuncType(params, 3, results, 1), CoreVariable_debug_input_emulate_text_editing, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:debug-input", "emulate-text-input",
                MakeFuncType(params, 1, results, 1), CoreVariable_debug_input_emulate_text_input, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:selection", "deselect-unit-array",
                MakeFuncType(params, 1, results, 1), CoreVariable_selection_deselect_unit_array, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:selection", "select-unit-array",
                MakeFuncType(params, 2, results, 1), CoreVariable_selection_select_unit_array, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:sound", "load-sound-def",
                MakeFuncType(params, 1, results, 1), CoreVariable_sound_load_sound_def, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:sound", "play-sound-file",
                MakeFuncType(params, 3, results, 1), CoreVariable_sound_play_sound_file, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:sound", "play-sound-stream",
                MakeFuncType(params, 3, results, 1), CoreVariable_sound_play_sound_stream, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:sound", "preload-sound-item",
                MakeFuncType(params, 1, results, 1), CoreVariable_sound_preload_sound_item, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:config", "set-config-float",
                MakeFuncType(params, 3, results, 1), CoreVariable_config_set_config_float, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:config", "set-config-int",
                MakeFuncType(params, 3, results, 1), CoreVariable_config_set_config_int, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:config", "set-config-string",
                MakeFuncType(params, 2, results, 1), CoreVariable_config_set_config_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:config", "set-log-section-filter-level",
                MakeFuncType(params, 2, results, 1), CoreVariable_config_set_log_section_filter_level, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:utils", "get-cegid",
                MakeFuncType(params, 1, results, 1), CoreVariable_utils_get_cegid, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "assign-mouse-cursor",
                MakeFuncType(params, 3, results, 1), CoreVariable_unsynced_ctrl_assign_mouse_cursor, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "deselect-unit-map",
                MakeFuncType(params, 1, results, 1), CoreVariable_unsynced_ctrl_deselect_unit_map, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "draw-unit-commands",
                MakeFuncType(params, 3, results, 1), CoreVariable_unsynced_ctrl_draw_unit_commands, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "load-cmd-colors-config",
                MakeFuncType(params, 1, results, 1), CoreVariable_unsynced_ctrl_load_cmd_colors_config, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "load-ctrl-panel-config",
                MakeFuncType(params, 1, results, 1), CoreVariable_unsynced_ctrl_load_ctrl_panel_config, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "load-model-textures",
                MakeFuncType(params, 1, results, 1), CoreVariable_unsynced_ctrl_load_model_textures, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "replace-mouse-cursor",
                MakeFuncType(params, 2, results, 1), CoreVariable_unsynced_ctrl_replace_mouse_cursor, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "select-unit-map",
                MakeFuncType(params, 2, results, 1), CoreVariable_unsynced_ctrl_select_unit_map, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "set-clipboard",
                MakeFuncType(params, 1, results, 1), CoreVariable_unsynced_ctrl_set_clipboard, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "set-dolly-camera-curve",
                MakeFuncType(params, 2, results, 1), CoreVariable_unsynced_ctrl_set_dolly_camera_curve, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "set-dolly-camera-look-curve",
                MakeFuncType(params, 2, results, 1), CoreVariable_unsynced_ctrl_set_dolly_camera_look_curve, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "set-map-shading-texture",
                MakeFuncType(params, 2, results, 1), CoreVariable_unsynced_ctrl_set_map_shading_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "set-mouse-cursor",
                MakeFuncType(params, 2, results, 1), CoreVariable_unsynced_ctrl_set_mouse_cursor, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "set-sky-box-texture",
                MakeFuncType(params, 1, results, 1), CoreVariable_unsynced_ctrl_set_sky_box_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "set-unit-def-icon",
                MakeFuncType(params, 2, results, 1), CoreVariable_unsynced_ctrl_set_unit_def_icon, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "set-unit-def-image",
                MakeFuncType(params, 2, results, 1), CoreVariable_unsynced_ctrl_set_unit_def_image, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "set-unit-icon",
                MakeFuncType(params, 2, results, 1), CoreVariable_unsynced_ctrl_set_unit_icon, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "set-wm-caption",
                MakeFuncType(params, 1, results, 1), CoreVariable_unsynced_ctrl_set_wm_caption, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "set-wm-icon",
                MakeFuncType(params, 2, results, 1), CoreVariable_unsynced_ctrl_set_wm_icon, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unsynced-ctrl", "set-water-texture",
                MakeFuncType(params, 1, results, 1), CoreVariable_unsynced_ctrl_set_water_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "add-atlas-texture",
                MakeFuncType(params, 1, results, 1), CoreVariable_gfx_add_atlas_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "add-fallback-font",
                MakeFuncType(params, 1, results, 1), CoreVariable_gfx_add_fallback_font, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "add-feature-defs-to-submission-vao",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_add_feature_defs_to_submission_vao, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "add-features-to-submission-vao",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_add_features_to_submission_vao, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "add-unit-defs-to-submission-vao",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_add_unit_defs_to_submission_vao, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "add-units-to-submission-vao",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_add_units_to_submission_vao, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "bind-image-texture",
                MakeFuncType(params, 7, results, 1), CoreVariable_gfx_bind_image_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "bind-texture",
                MakeFuncType(params, 3, results, 1), CoreVariable_gfx_bind_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "change-texture-params",
                MakeFuncType(params, 1, results, 1), CoreVariable_gfx_change_texture_params, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "copy-to-texture",
                MakeFuncType(params, 9, results, 1), CoreVariable_gfx_copy_to_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "create-shader",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_create_shader, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "define-vbo",
                MakeFuncType(params, 7, results, 1), CoreVariable_gfx_define_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "delete-texture",
                MakeFuncType(params, 1, results, 1), CoreVariable_gfx_delete_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "delete-texture-atlas",
                MakeFuncType(params, 1, results, 1), CoreVariable_gfx_delete_texture_atlas, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "delete-texture-fbo",
                MakeFuncType(params, 1, results, 1), CoreVariable_gfx_delete_texture_fbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "finalize-texture-atlas",
                MakeFuncType(params, 1, results, 1), CoreVariable_gfx_finalize_texture_atlas, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_F32, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "font-get-text-height",
                MakeFuncType(params, 6, results, 1), CoreVariable_gfx_font_get_text_height, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_F32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "font-get-text-width",
                MakeFuncType(params, 5, results, 1), CoreVariable_gfx_font_get_text_width, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_F32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "font-print",
                MakeFuncType(params, 5, results, 1), CoreVariable_gfx_font_print, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "font-print-world",
                MakeFuncType(params, 3, results, 1), CoreVariable_gfx_font_print_world, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "generate-mipmap",
                MakeFuncType(params, 1, results, 1), CoreVariable_gfx_generate_mipmap, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "get-atlas-texture",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_get_atlas_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "get-fixed-state",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_get_fixed_state, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "get-global-tex-coords",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_get_global_tex_coords, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "get-subroutine-index",
                MakeFuncType(params, 4, results, 1), CoreVariable_gfx_get_subroutine_index, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "get-text-height",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_get_text_height, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "get-text-width",
                MakeFuncType(params, 1, results, 1), CoreVariable_gfx_get_text_width, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "get-uniform-location",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_get_uniform_location, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "has-extension",
                MakeFuncType(params, 1, results, 1), CoreVariable_gfx_has_extension, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "instance-data-from-feature-defs-vbo",
                MakeFuncType(params, 5, results, 1), CoreVariable_gfx_instance_data_from_feature_defs_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "instance-data-from-features-vbo",
                MakeFuncType(params, 5, results, 1), CoreVariable_gfx_instance_data_from_features_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "instance-data-from-unit-defs-vbo",
                MakeFuncType(params, 5, results, 1), CoreVariable_gfx_instance_data_from_unit_defs_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "instance-data-from-units-vbo",
                MakeFuncType(params, 5, results, 1), CoreVariable_gfx_instance_data_from_units_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "load-font",
                MakeFuncType(params, 4, results, 1), CoreVariable_gfx_load_font, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "matrix-data-from-projectiles-vbo",
                MakeFuncType(params, 5, results, 1), CoreVariable_gfx_matrix_data_from_projectiles_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "object-label",
                MakeFuncType(params, 3, results, 1), CoreVariable_gfx_object_label, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "push-debug-group",
                MakeFuncType(params, 3, results, 1), CoreVariable_gfx_push_debug_group, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "save-image",
                MakeFuncType(params, 6, results, 1), CoreVariable_gfx_save_image, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "set-fbo-attachment",
                MakeFuncType(params, 7, results, 1), CoreVariable_gfx_set_fbo_attachment, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "set-fbo-draw-buffers",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_set_fbo_draw_buffers, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "set-feature-buffer-uniforms",
                MakeFuncType(params, 3, results, 1), CoreVariable_gfx_set_feature_buffer_uniforms, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "set-unit-buffer-uniforms",
                MakeFuncType(params, 3, results, 1), CoreVariable_gfx_set_unit_buffer_uniforms, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "shape",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_shape, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_F32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "text",
                MakeFuncType(params, 4, results, 1), CoreVariable_gfx_text, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "texture-info",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_texture_info, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "uniform-array-float",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_uniform_array_float, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "uniform-array-int",
                MakeFuncType(params, 2, results, 1), CoreVariable_gfx_uniform_array_int, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "uniform-matrix",
                MakeFuncType(params, 3, results, 1), CoreVariable_gfx_uniform_matrix, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "upload-texture",
                MakeFuncType(params, 11, results, 1), CoreVariable_gfx_upload_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:gfx", "upload-vbo",
                MakeFuncType(params, 6, results, 1), CoreVariable_gfx_upload_vbo, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_F32, WASM_I32, WASM_F32, WASM_F32, WASM_F32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:icons", "add-unit-icon",
                MakeFuncType(params, 8, results, 1), CoreVariable_icons_add_unit_icon, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:icons", "free-unit-icon",
                MakeFuncType(params, 1, results, 1), CoreVariable_icons_free_unit_icon, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:markers", "add-world-text",
                MakeFuncType(params, 1, results, 1), CoreVariable_markers_add_world_text, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:markers", "marker-add-point",
                MakeFuncType(params, 3, results, 1), CoreVariable_markers_marker_add_point, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:ground-decals", "set-ground-decal-texture",
                MakeFuncType(params, 3, results, 1), CoreVariable_ground_decals_set_ground_decal_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:system-control", "clear-watch-dog-timer",
                MakeFuncType(params, 2, results, 1), CoreVariable_system_control_clear_watch_dog_timer, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:system-control", "reload",
                MakeFuncType(params, 1, results, 1), CoreVariable_system_control_reload, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:system-control", "restart",
                MakeFuncType(params, 1, results, 1), CoreVariable_system_control_restart, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:system-control", "set-share-level",
                MakeFuncType(params, 2, results, 1), CoreVariable_system_control_set_share_level, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:system-control", "share-resources",
                MakeFuncType(params, 3, results, 1), CoreVariable_system_control_share_resources, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:system-control", "start",
                MakeFuncType(params, 1, results, 1), CoreVariable_system_control_start, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "add-translation-string",
                MakeFuncType(params, 1, results, 1), CoreVariable_rml_ui_add_translation_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "clear-document-path-requests",
                MakeFuncType(params, 1, results, 1), CoreVariable_rml_ui_clear_document_path_requests, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "context-activate-theme",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_context_activate_theme, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "context-create-data-model",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_context_create_data_model, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "context-create-document",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_context_create_document, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "context-get-document",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_context_get_document, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "context-is-theme-active",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_context_is_theme_active, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "context-load-document",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_context_load_document, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "context-open-data-model",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_context_open_data_model, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "context-process-text-input",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_context_process_text_input, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "context-remove-data-model",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_context_remove_data_model, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "context-remove-event-listener",
                MakeFuncType(params, 4, results, 1), CoreVariable_rml_ui_context_remove_event_listener, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "create-context",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_create_context, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "data-model-bind-bool",
                MakeFuncType(params, 4, results, 1), CoreVariable_rml_ui_data_model_bind_bool, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "data-model-bind-color",
                MakeFuncType(params, 7, results, 1), CoreVariable_rml_ui_data_model_bind_color, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "data-model-bind-float",
                MakeFuncType(params, 4, results, 1), CoreVariable_rml_ui_data_model_bind_float, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "data-model-bind-int",
                MakeFuncType(params, 4, results, 1), CoreVariable_rml_ui_data_model_bind_int, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "data-model-bind-percent",
                MakeFuncType(params, 4, results, 1), CoreVariable_rml_ui_data_model_bind_percent, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "data-model-bind-pixels",
                MakeFuncType(params, 4, results, 1), CoreVariable_rml_ui_data_model_bind_pixels, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "data-model-bind-string",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_data_model_bind_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "data-model-set-string",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_data_model_set_string, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "document-append-to-style-sheet",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_document_append_to_style_sheet, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "document-create-element",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_document_create_element, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "document-create-text-node",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_document_create_text_node, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "document-load-external-script",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_document_load_external_script, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "document-load-inline-script",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_document_load_inline_script, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "document-set-title",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_document_set_title, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-are-pseudo-classes-set",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_are_pseudo_classes_set, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-closest",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_element_closest, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-dispatch-event",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_dispatch_event, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-form-submit",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_form_submit, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-get-element-by-id",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_element_get_element_by_id, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-get-elements-by-class-name-count",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_get_elements_by_class_name_count, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-get-elements-by-tag-name-count",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_get_elements_by_tag_name_count, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-has-attribute",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_has_attribute, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-is-class-set",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_is_class_set, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-is-pseudo-class-set",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_is_pseudo_class_set, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-matches",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_matches, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-query-selector",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_element_query_selector, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-query-selector-all-count",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_query_selector_all_count, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-remove-attribute",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_remove_attribute, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-remove-event-listener",
                MakeFuncType(params, 4, results, 1), CoreVariable_rml_ui_element_remove_event_listener, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-set-attribute",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_set_attribute, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-set-class",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_element_set_class, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-set-class-name",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_set_class_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-set-id",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_set_id, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-set-inner-rml",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_element_set_inner_rml, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-set-pseudo-class",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_element_set_pseudo_class, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-tab-set-set-panel",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_element_tab_set_set_panel, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "element-tab-set-set-tab",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_element_tab_set_set_tab, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "event-get-parameter-bool",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_event_get_parameter_bool, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "event-get-parameter-float",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_event_get_parameter_float, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "event-get-parameter-int",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_event_get_parameter_int, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "event-get-parameter-type",
                MakeFuncType(params, 3, results, 1), CoreVariable_rml_ui_event_get_parameter_type, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "get-context",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_get_context, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "remove-context-by-name",
                MakeFuncType(params, 1, results, 1), CoreVariable_rml_ui_remove_context_by_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "set-debug-context-by-name",
                MakeFuncType(params, 1, results, 1), CoreVariable_rml_ui_set_debug_context_by_name, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "set-mouse-cursor-alias",
                MakeFuncType(params, 1, results, 1), CoreVariable_rml_ui_set_mouse_cursor_alias, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I64, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:rml-ui", "sol-lua-data-model-set-dirty",
                MakeFuncType(params, 2, results, 1), CoreVariable_rml_ui_sol_lua_data_model_set_dirty, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:vfs", "compress-folder",
                MakeFuncType(params, 2, results, 1), CoreVariable_vfs_compress_folder, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:vfs", "create-dir",
                MakeFuncType(params, 1, results, 1), CoreVariable_vfs_create_dir, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGeneratedVariable(linker, "spring:vfs", "download-archive",
                MakeFuncType(params, 1, results, 1), CoreVariable_vfs_download_archive, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:vfs", "extract-mod-archive-file",
                MakeFuncType(params, 1, results, 1), CoreVariable_vfs_extract_mod_archive_file, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:vfs", "file-exists",
                MakeFuncType(params, 1, results, 1), CoreVariable_vfs_file_exists, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:vfs", "get-file-size",
                MakeFuncType(params, 1, results, 1), CoreVariable_vfs_get_file_size, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:vfs", "get-map-square-texture",
                MakeFuncType(params, 5, results, 1), CoreVariable_vfs_get_map_square_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:vfs", "has-archive",
                MakeFuncType(params, 1, results, 1), CoreVariable_vfs_has_archive, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:vfs", "is-directory",
                MakeFuncType(params, 1, results, 1), CoreVariable_vfs_is_directory, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:vfs", "set-map-square-texture",
                MakeFuncType(params, 3, results, 1), CoreVariable_vfs_set_map_square_texture, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:team-control", "add-team-resource",
                MakeFuncType(params, 3, results, 1), CoreVariable_team_control_add_team_resource, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:team-control", "add-team-resource-excess-stats",
                MakeFuncType(params, 3, results, 1), CoreVariable_team_control_add_team_resource_excess_stats, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:team-control", "game-over",
                MakeFuncType(params, 1, results, 1), CoreVariable_team_control_game_over, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:team-control", "set-team-resource",
                MakeFuncType(params, 3, results, 1), CoreVariable_team_control_set_team_resource, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:team-control", "set-team-share-level",
                MakeFuncType(params, 3, results, 1), CoreVariable_team_control_set_team_share_level, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:team-control", "share-team-resource",
                MakeFuncType(params, 4, results, 1), CoreVariable_team_control_share_team_resource, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:team-control", "use-team-resource",
                MakeFuncType(params, 3, results, 1), CoreVariable_team_control_use_team_resource, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "add-unit-resource",
                MakeFuncType(params, 3, results, 1), CoreVariable_unit_control_add_unit_resource, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "bugger-off",
                MakeFuncType(params, 3, results, 1), CoreVariable_unit_control_bugger_off, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "give-order-to-unit-array",
                MakeFuncType(params, 4, results, 1), CoreVariable_unit_control_give_order_to_unit_array, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "set-unit-build-params",
                MakeFuncType(params, 2, results, 1), CoreVariable_unit_control_set_unit_build_params, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "set-unit-flanking",
                MakeFuncType(params, 2, results, 1), CoreVariable_unit_control_set_unit_flanking, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "set-unit-nano-pieces",
                MakeFuncType(params, 2, results, 1), CoreVariable_unit_control_set_unit_nano_pieces, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "set-unit-resourcing",
                MakeFuncType(params, 3, results, 1), CoreVariable_unit_control_set_unit_resourcing, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "set-unit-sensor-radius",
                MakeFuncType(params, 3, results, 1), CoreVariable_unit_control_set_unit_sensor_radius, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "set-unit-storage",
                MakeFuncType(params, 3, results, 1), CoreVariable_unit_control_set_unit_storage, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "set-unit-tooltip",
                MakeFuncType(params, 2, results, 1), CoreVariable_unit_control_set_unit_tooltip, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "set-unit-weapon-damages",
                MakeFuncType(params, 4, results, 1), CoreVariable_unit_control_set_unit_weapon_damages, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "set-unit-weapon-state",
                MakeFuncType(params, 4, results, 1), CoreVariable_unit_control_set_unit_weapon_state, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:unit-control", "use-unit-resource",
                MakeFuncType(params, 3, results, 1), CoreVariable_unit_control_use_unit_resource, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_F32, WASM_F32, WASM_F32, WASM_F32, WASM_F32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:terrain-control", "set-terrain-type-data",
                MakeFuncType(params, 8, results, 1), CoreVariable_terrain_control_set_terrain_type_data, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:projectile-control", "set-projectile-ceg",
                MakeFuncType(params, 2, results, 1), CoreVariable_projectile_control_set_projectile_ceg, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_F32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:projectile-control", "set-projectile-damages",
                MakeFuncType(params, 4, results, 1), CoreVariable_projectile_control_set_projectile_damages, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGeneratedVariable(linker, "spring:cob-script", "get-cob-script-id",
                MakeFuncType(params, 2, results, 1), CoreVariable_cob_script_get_cob_script_id, state, error))
            return false;
    }

    return true;
}

static_assert(204 >= 0, "generated variable Core callback count");

} // namespace recoil::wasm::core::generated
