/* @generated by spring-api-codegen; do not edit. */

#include "wasm/generated/WasmCoreGeneratedBindings.h"

#include <array>
#include <bit>
#include <cstdint>
#include <span>
#include <string_view>
#include <type_traits>

#include "NativeInterface/NativeInterface.h"
#include "WasmInterface/core/host/WasmCoreAbi.h"
#include "WasmInterface/core/host/WasmCoreGeneratedSupport.h"
#include "WasmInterface/core/host/WasmCoreWire.h"

namespace recoil::wasm::core::generated {
namespace {

bool DefineGenerated(wasmtime_linker_t* linker, const char* moduleName,
    const char* functionName, wasm_functype_t* type,
    wasmtime_func_unchecked_callback_t callback, HostState* state, std::string& error)
{
    wasmtime_error_t* defineError = wasmtime_linker_define_func_unchecked(
        linker,
        moduleName, std::char_traits<char>::length(moduleName),
        functionName, std::char_traits<char>::length(functionName),
        type, callback, state, nullptr);
    wasm_functype_delete(type);
    if (defineError == nullptr)
        return true;
    error = ErrorMessage(defineError);
    return false;
}

wasm_trap_t* Core_object_rendering_get_lod(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->objectRendering == nullptr ||
        state->native->objectRendering->GetLOD == nullptr)
        return Trap("GetLOD generated Core binding is unavailable");
    if (3 != 0 && (slots == nullptr || slotCount != 3))
        return Trap("GetLOD generated Core ABI signature mismatch");
    if (3 == 0 && slotCount != 0)
        return Trap("GetLOD generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    std::string memoryError;
    if (!EnsureMemory(state, caller, memoryError))
        return Trap(memoryError);
    const std::uint32_t output = static_cast<std::uint32_t>(slots[2].i32);
    if (!state->memory.Contains(output, 8u)) {
        slots[0].i32 = static_cast<std::int32_t>(Status::OutOfBounds);
        return nullptr;
    }

    GetObjectLODQuery query{};
    query.objectType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectType)>>>(slots[0].i32);
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[1].i32);
    GetObjectLODResult result{};
    state->native->objectRendering->GetLOD(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    if (errorCode != 0) {
        slots[0].i32 = errorCode;
        return nullptr;
    }
    std::array<std::uint8_t, 8> wire{};
    WireWriter writer(wire);
    if (!writer.U32(result.lodCount)) return Trap("generated Core wire overflow");
    if (!writer.U32(result.currentLOD)) return Trap("generated Core wire overflow");
    if (!writer.Finish(4u))
        return Trap("generated Core wire layout mismatch for GetLOD");
    if (!state->memory.Write(output, wire.data(), wire.size()))
        return Trap("generated Core output range changed unexpectedly");
    slots[0].i32 = 0;
    return nullptr;
}

wasm_trap_t* Core_object_rendering_set_lod_count(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->objectRendering == nullptr ||
        state->native->objectRendering->SetLODCount == nullptr)
        return Trap("SetLODCount generated Core binding is unavailable");
    if (3 != 0 && (slots == nullptr || slotCount != 3))
        return Trap("SetLODCount generated Core ABI signature mismatch");
    if (3 == 0 && slotCount != 0)
        return Trap("SetLODCount generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 4u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    SetObjectLODCountQuery query{};
    query.objectType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectType)>>>(slots[0].i32);
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[1].i32);
    query.lodCount = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.lodCount)>>>(slots[2].i32);
    ObjectRenderingResult result{};
    state->native->objectRendering->SetLODCount(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* Core_object_rendering_set_lod_distance(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->objectRendering == nullptr ||
        state->native->objectRendering->SetLODDistance == nullptr)
        return Trap("SetLODDistance generated Core binding is unavailable");
    if (4 != 0 && (slots == nullptr || slotCount != 4))
        return Trap("SetLODDistance generated Core ABI signature mismatch");
    if (4 == 0 && slotCount != 0)
        return Trap("SetLODDistance generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    SetObjectLODDistanceQuery query{};
    query.objectType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectType)>>>(slots[0].i32);
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[1].i32);
    query.lodLevel = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.lodLevel)>>>(slots[2].i32);
    query.distance = slots[3].f32;
    ObjectRenderingResult result{};
    state->native->objectRendering->SetLODDistance(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* Core_object_rendering_set_lod_length(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->objectRendering == nullptr ||
        state->native->objectRendering->SetLODLength == nullptr)
        return Trap("SetLODLength generated Core binding is unavailable");
    if (4 != 0 && (slots == nullptr || slotCount != 4))
        return Trap("SetLODLength generated Core ABI signature mismatch");
    if (4 == 0 && slotCount != 0)
        return Trap("SetLODLength generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    SetObjectLODLengthQuery query{};
    query.objectType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectType)>>>(slots[0].i32);
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[1].i32);
    query.lodLevel = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.lodLevel)>>>(slots[2].i32);
    query.length = slots[3].f32;
    ObjectRenderingResult result{};
    state->native->objectRendering->SetLODLength(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* Core_object_rendering_set_material_display_lists(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->objectRendering == nullptr ||
        state->native->objectRendering->SetMaterialDisplayLists == nullptr)
        return Trap("SetMaterialDisplayLists generated Core binding is unavailable");
    if (6 != 0 && (slots == nullptr || slotCount != 6))
        return Trap("SetMaterialDisplayLists generated Core ABI signature mismatch");
    if (6 == 0 && slotCount != 0)
        return Trap("SetMaterialDisplayLists generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 7u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    SetObjectMaterialDisplayListsQuery query{};
    query.objectType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectType)>>>(slots[0].i32);
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[1].i32);
    query.lodLevel = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.lodLevel)>>>(slots[2].i32);
    query.materialType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.materialType)>>>(slots[3].i32);
    query.preList = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.preList)>>>(slots[4].i32);
    query.postList = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.postList)>>>(slots[5].i32);
    ObjectRenderingResult result{};
    state->native->objectRendering->SetMaterialDisplayLists(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* Core_object_rendering_set_material_last_lod(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->objectRendering == nullptr ||
        state->native->objectRendering->SetMaterialLastLOD == nullptr)
        return Trap("SetMaterialLastLOD generated Core binding is unavailable");
    if (4 != 0 && (slots == nullptr || slotCount != 4))
        return Trap("SetMaterialLastLOD generated Core ABI signature mismatch");
    if (4 == 0 && slotCount != 0)
        return Trap("SetMaterialLastLOD generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 5u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    SetObjectMaterialLastLODQuery query{};
    query.objectType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectType)>>>(slots[0].i32);
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[1].i32);
    query.materialType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.materialType)>>>(slots[2].i32);
    query.lodLevel = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.lodLevel)>>>(slots[3].i32);
    ObjectRenderingResult result{};
    state->native->objectRendering->SetMaterialLastLOD(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}

wasm_trap_t* Core_object_rendering_set_piece_list(void* environment, wasmtime_caller_t* caller,
    wasmtime_val_raw_t* slots, std::size_t slotCount)
{
    auto* state = static_cast<HostState*>(environment);
    if (state == nullptr || state->native == nullptr || state->native->objectRendering == nullptr ||
        state->native->objectRendering->SetPieceList == nullptr)
        return Trap("SetPieceList generated Core binding is unavailable");
    if (5 != 0 && (slots == nullptr || slotCount != 5))
        return Trap("SetPieceList generated Core ABI signature mismatch");
    if (5 == 0 && slotCount != 0)
        return Trap("SetPieceList generated Core ABI signature mismatch");

    std::string budgetError;
    ImportGuard guard(state, 6u, budgetError);
    if (!guard.Ok())
        return Trap(budgetError);

    SetObjectPieceListQuery query{};
    query.objectType = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectType)>>>(slots[0].i32);
    query.objectID = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.objectID)>>>(slots[1].i32);
    query.lodLevel = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.lodLevel)>>>(slots[2].i32);
    query.piece = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.piece)>>>(slots[3].i32);
    query.displayList = static_cast<std::remove_cv_t<std::remove_reference_t<decltype(query.displayList)>>>(slots[4].i32);
    ObjectRenderingResult result{};
    state->native->objectRendering->SetPieceList(&query, &result);
    const std::int32_t errorCode = NativeErrorCode(result.error);
    slots[0].i64 = static_cast<std::int64_t>(PackU32(static_cast<std::uint32_t>(result.success ? 1u : 0u), errorCode));
    return nullptr;
}


} // namespace

bool RegisterGeneratedImports_object_rendering(wasmtime_linker_t* linker, HostState* state, std::string& error)
{
    if (linker == nullptr || state == nullptr || state->native == nullptr) {
        error = "cannot register generated Core Wasm imports without linker/host/native API";
        return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I32};
        if (!DefineGenerated(linker, "spring:object-rendering", "get-lod",
                MakeFuncType(params, 3, results, 1), Core_object_rendering_get_lod, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGenerated(linker, "spring:object-rendering", "set-lod-count",
                MakeFuncType(params, 3, results, 1), Core_object_rendering_set_lod_count, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_F32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGenerated(linker, "spring:object-rendering", "set-lod-distance",
                MakeFuncType(params, 4, results, 1), Core_object_rendering_set_lod_distance, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_F32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGenerated(linker, "spring:object-rendering", "set-lod-length",
                MakeFuncType(params, 4, results, 1), Core_object_rendering_set_lod_length, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGenerated(linker, "spring:object-rendering", "set-material-display-lists",
                MakeFuncType(params, 6, results, 1), Core_object_rendering_set_material_display_lists, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGenerated(linker, "spring:object-rendering", "set-material-last-lod",
                MakeFuncType(params, 4, results, 1), Core_object_rendering_set_material_last_lod, state, error))
            return false;
    }
    {
        const wasm_valkind_t params[] = {WASM_I32, WASM_I32, WASM_I32, WASM_I32, WASM_I32};
        const wasm_valkind_t results[] = {WASM_I64};
        if (!DefineGenerated(linker, "spring:object-rendering", "set-piece-list",
                MakeFuncType(params, 5, results, 1), Core_object_rendering_set_piece_list, state, error))
            return false;
    }

    return true;
}

static_assert(7 >= 0, "generated Core Wasm callback count");

} // namespace recoil::wasm::core::generated
