/* This file is part of the Spring engine (GPL v2 or later), see LICENSE.html */

// @generated by spring-api-codegen; do not edit.

#include "WasmHostAdapterSupport.h"

using namespace recoil::wasm::generated::detail;

namespace recoil::wasm::generated::detail {

}

namespace {

NativeCalloutDispatch Dispatch_game_config_SetCheatingEnabled(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->gameConfig == nullptr || nativeInterface->syncedCtrl->gameConfig->SetCheatingEnabled == nullptr) { error = "NativeInterface API is unavailable: game_config.SetCheatingEnabled"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetCheatingEnabledQuery query{};
	SetCheatingEnabledResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 1u, 1u, "enabled", query.enabled, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->gameConfig->SetCheatingEnabled(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_game_config_SetExperienceGrade(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->gameConfig == nullptr || nativeInterface->syncedCtrl->gameConfig->SetExperienceGrade == nullptr) { error = "NativeInterface API is unavailable: game_config.SetExperienceGrade"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetExperienceGradeQuery query{};
	SetExperienceGradeResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 4u, 4u, "expGrade", query.expGrade, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 4u, 4u, "expPowerScale", query.expPowerScale, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 4u, 4u, "expHealthScale", query.expHealthScale, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 4u, 4u, "expReloadScale", query.expReloadScale, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->gameConfig->SetExperienceGrade(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_game_config_SetGodMode(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->gameConfig == nullptr || nativeInterface->syncedCtrl->gameConfig->SetGodMode == nullptr) { error = "NativeInterface API is unavailable: game_config.SetGodMode"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetGodModeQuery query{};
	SetGodModeResult nativeResult{};
	if (!ReadRecordArgument(arguments, 0u, 0u, 1u, 1u, "options", Read_SetGodModeOptions, query.options, storage, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->gameConfig->SetGodMode(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_game_config_SetNoPause(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->gameConfig == nullptr || nativeInterface->syncedCtrl->gameConfig->SetNoPause == nullptr) { error = "NativeInterface API is unavailable: game_config.SetNoPause"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetNoPauseQuery query{};
	SetNoPauseResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 1u, 1u, "noPause", query.noPause, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->gameConfig->SetNoPause(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_game_config_SetRadarErrorParams(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->gameConfig == nullptr || nativeInterface->syncedCtrl->gameConfig->SetRadarErrorParams == nullptr) { error = "NativeInterface API is unavailable: game_config.SetRadarErrorParams"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetRadarErrorParamsQuery query{};
	SetRadarErrorParamsResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 4u, 4u, "allyTeamID", query.allyTeamID, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 4u, 4u, "allyTeamErrorSize", query.allyTeamErrorSize, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 4u, 4u, "baseErrorSize", query.baseErrorSize, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 3u, 3u, 4u, 4u, "baseErrorMult", query.baseErrorMult, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->gameConfig->SetRadarErrorParams(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

NativeCalloutDispatch Dispatch_game_config_SetSquareBuildingMask(NativeInterface* nativeInterface, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (nativeInterface == nullptr || nativeInterface->syncedCtrl == nullptr || nativeInterface->syncedCtrl->gameConfig == nullptr || nativeInterface->syncedCtrl->gameConfig->SetSquareBuildingMask == nullptr) { error = "NativeInterface API is unavailable: game_config.SetSquareBuildingMask"; return NativeCalloutDispatch::handled; }
	NativeCallStorage storage;
	SetSquareBuildingMaskQuery query{};
	SetSquareBuildingMaskResult nativeResult{};
	if (!ReadArgument(arguments, 0u, 0u, 3u, 3u, "x", query.x, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 1u, 1u, 3u, 3u, "z", query.z, error)) return NativeCalloutDispatch::handled;
	if (!ReadArgument(arguments, 2u, 2u, 3u, 3u, "mask", query.mask, error)) return NativeCalloutDispatch::handled;
	nativeInterface->syncedCtrl->gameConfig->SetSquareBuildingMask(&query, &nativeResult);
	if (!CheckNativeError(nativeResult, error)) return NativeCalloutDispatch::handled;
	result = WriteScalar(nativeResult.success);
	return NativeCalloutDispatch::handled;
}

}

namespace recoil::wasm::generated {

NativeCalloutDispatch DispatchNativeCalloutModule_game_config(NativeInterface*, std::string_view, const std::vector<WasmValue>&, WasmValue&, std::string&);
NativeCalloutDispatch DispatchNativeCalloutModule_game_config(NativeInterface* nativeInterface, std::string_view function, const std::vector<WasmValue>& arguments, WasmValue& result, std::string& error)
{
	if (detail::FunctionEquals(function, "SetCheatingEnabled")) return Dispatch_game_config_SetCheatingEnabled(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "SetExperienceGrade")) return Dispatch_game_config_SetExperienceGrade(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "SetGodMode")) return Dispatch_game_config_SetGodMode(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "SetNoPause")) return Dispatch_game_config_SetNoPause(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "SetRadarErrorParams")) return Dispatch_game_config_SetRadarErrorParams(nativeInterface, arguments, result, error);
	if (detail::FunctionEquals(function, "SetSquareBuildingMask")) return Dispatch_game_config_SetSquareBuildingMask(nativeInterface, arguments, result, error);
	return NativeCalloutDispatch::notHandled;
}
}
