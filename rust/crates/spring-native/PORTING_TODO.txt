Current API port TODO (one function per line; generated from api_comparison.md)

Known native API crashers / edge cases:
- RmlUi DocumentAppendToStyleSheet used to crash when called on a native-created
  document with no existing StyleSheetContainer, e.g. ContextCreateDocument("body")
  followed by DocumentAppendToStyleSheet("body { ... }"). The 2026-07-03
  SpringBoard native chonsole test reproduced it with a libunwind stack at
  NativeDocumentAppendToStyleSheet -> StyleSheetContainer::CombineStyleSheetContainer.
  ASAN was not enabled in build-amd64-linux (USE_ASAN=OFF). The API must install
  the new stylesheet directly when there is no existing container and must not
  segfault if RmlUi rejects stylesheet input.
- RmlUi ElementRemoveChild can poison later Rml shutdown if a native caller removes
  children and drops the returned ElementPtr handle on the floor. Reproducer was
  SpringBoard native chonsole repeatedly clearing root children with
  ElementHasChildNodes + ElementGetChild(0) + ElementRemoveChild before
  ElementSetInnerRml, then closing the editor. ASAN stack:
  Rml::Element::Release -> Context::ReleaseUnloadedDocuments -> Rml::Shutdown.
  The binding either needs an explicit safe way to release returned ElementPtr
  handles or a clear-children/replace-inner API that does not expose ownership
  traps to Rust callers.
