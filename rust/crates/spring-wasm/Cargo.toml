[package]
name = "spring-wasm"
version = "0.1.0"
edition = "2021"
description = "Safe owned semantic API and runtime contracts for Spring Wasm modules"
license = "GPL-2.0-or-later"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
